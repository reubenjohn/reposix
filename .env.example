# reposix — environment variables template.
#
# Copy this file to `.env` at the repo root and fill in the values. The
# real `.env` is gitignored; `.env.example` is committed so contributors
# discover which variables exist.
#
# Loading: currently no in-process dotenv loader. Source the file before
# running reposix commands that need the variables:
#
#   set -a; source .env; set +a
#   reposix list --backend teamwork --project CONF~DEMO
#
# Or pass them inline:
#
#   TEAMWORK_GRAPH_API=xxx reposix list --backend teamwork --project CONF~DEMO

# ----------------------------------------------------------------- GitHub
# Optional. Lifts GithubReadOnlyBackend's anonymous 60 req/hr limit to
# 1000 req/hr (or 5000 with a PAT). In dev, `gh auth token` prints the
# value the gh CLI is already using.
GITHUB_TOKEN=

# --------------------------------------------------------------- Atlassian
# Atlassian Teamwork Graph API token. Used by the future
# TeamworkBackend (v0.3) to read Confluence pages + Jira issues via the
# unified Atlassian cloud API.
#
# Scope: read-only for v0.1 of the adapter. Get one from
# https://id.atlassian.com/manage-profile/security/api-tokens
TEAMWORK_GRAPH_API=

# -------------------------------------------------------------- Allowlist
# reposix-core's SG-01 egress allowlist. Default is loopback-only. When
# using the GitHub or Teamwork backends, MUST be widened to include the
# real API origin(s). Comma-separated globs; only `*` on port supported.
#
# Example for dev against both real backends:
# REPOSIX_ALLOWED_ORIGINS=http://127.0.0.1:*,http://localhost:*,https://api.github.com,https://api.atlassian.com
REPOSIX_ALLOWED_ORIGINS=http://127.0.0.1:*,http://localhost:*
