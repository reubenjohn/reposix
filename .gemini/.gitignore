# Ignore everything in this directory by default; settings.json is the
# only file we want committed. Gemini CLI may write history/, oauth_creds.json,
# projects.json, state.json, etc. at runtime.
*
!.gitignore
!settings.json
