---
phase: quick-260715-mk5
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: ["owner-lane:w1:p7 public-birds-eye-roadmap-diagram (todo 2026-07-15)"]
files_modified:
  - docs/roadmap.md
  - mkdocs.yml
  - .planning/verifications/playwright/roadmap.json
  - .planning/PROJECT.md
  - .planning/PRACTICES.md
  - quality/gates/docs-build/link-resolution.py
  - quality/catalogs/docs-build.json
  - .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md

must_haves:
  truths:
    - "A public visitor to the mkdocs site sees a Roadmap page with ONE color-coded birds-eye mermaid diagram (shipped arcs → active Floor milestone → future arcs → OD-4 golden end state), labelled by ARCS/CAPABILITIES, not phase numbers or dates."
    - "docs/roadmap.md and .planning/PROJECT.md each carry a link to the other, each with an adjacent non-rendering <!-- SYNC: ... --> keep-in-check comment."
    - "The OP-9 distill checklist in .planning/PRACTICES.md reminds the milestone-close ritual to keep the roadmap↔PROJECT SYNC pair (and diagram colors) in step."
    - "link-resolution.py mechanically checks BOTH cross-link directions: DEFAULT_GLOBS now covers docs/*.md and .planning/PROJECT.md, and the docs-build/link-resolution catalog row's asserts describe that new contract."
    - "mkdocs-strict, mermaid-renders, link-resolution (extended globs) and the banned-words self-check all pass; the diagram was render-reviewed via mcp-mermaid for legibility."
  artifacts:
    - path: "docs/roadmap.md"
      provides: "Public birds-eye roadmap page with one color-coded mermaid + GitHub-URL cross-link to PROJECT.md carrying a SYNC comment"
      contains: "```mermaid"
    - path: ".planning/verifications/playwright/roadmap.json"
      provides: "Render proof for mermaid-renders.sh (mermaid_count=1, svg_counts non-zero, console_errors empty)"
      contains: "svg_counts"
    - path: "mkdocs.yml"
      provides: "Nav entry registering docs/roadmap.md"
      contains: "roadmap.md"
    - path: "quality/gates/docs-build/link-resolution.py"
      provides: "DEFAULT_GLOBS extended with docs/*.md and .planning/PROJECT.md"
      contains: ".planning/PROJECT.md"
    - path: "quality/catalogs/docs-build.json"
      provides: "docs-build/link-resolution row asserts updated to the new glob contract (catalog-first)"
      contains: ".planning/PROJECT.md"
  key_links:
    - from: "docs/roadmap.md"
      to: ".planning/PROJECT.md"
      via: "absolute GitHub blob URL + adjacent <!-- SYNC --> comment"
      pattern: "github.com/reubenjohn/reposix/blob/main/.planning/PROJECT.md"
    - from: ".planning/PROJECT.md"
      to: "docs/roadmap.md"
      via: "repo-relative ../docs/roadmap.md link + adjacent <!-- SYNC --> comment"
      pattern: "\\.\\./docs/roadmap\\.md"
    - from: "quality/gates/docs-build/link-resolution.py"
      to: "docs/*.md + .planning/PROJECT.md"
      via: "DEFAULT_GLOBS entries"
      pattern: "docs/\\*\\.md"
---

<objective>
Author `docs/roadmap.md` — ONE color-coded, birds-eye mermaid diagram showing the
project's arc from shipped capabilities → the active v0.15.0 "Floor" milestone → future
arcs converging on the OD-4 launch-readiness golden end state — register it in the mkdocs
nav, and wire a bi-directional keep-in-check link between it and `.planning/PROJECT.md`.
Then extend the link-resolution gate so BOTH cross-link directions are mechanically
checked, and pass the full docs-build gate battery.

Purpose: give the public a truthful, low-upkeep bird's-eye of where reposix is going
(arcs/capabilities, NOT phase numbers or dates), and mechanically bind the public roadmap
to the private planning source of truth so the pair never silently drifts.

Output: `docs/roadmap.md` + nav entry + render artifact; bi-directional SYNC cross-links;
OP-9 checklist line; extended `DEFAULT_GLOBS` + catalog row; optional SYNC-marker gate
built-if-trivial-else-filed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/PRACTICES.md
@quality/gates/docs-build/link-resolution.py
@mkdocs.yml

# The verbatim owner-approved lane spec (authoritative — do not deviate from its 5 points):
@.planning/todos/pending/2026-07-15-public-birds-eye-roadmap-diagram.md

<interfaces>
<!-- Facts the executor needs — already extracted, do not re-explore. -->

MERMAID RENDER GATE (quality/gates/docs-build/mermaid-renders.sh):
  - Scans `grep -rl '^```mermaid' docs/` — the fence MUST start at column 0.
  - For a TOP-LEVEL page docs/roadmap.md it demands an artifact at
    `.planning/verifications/playwright/roadmap.json` (base name, NO section subdir).
  - Artifact schema (copy from .planning/verifications/playwright/index.json):
      {
        "nav_path": "/reposix/roadmap/",
        "mermaid_count": 1,
        "svg_counts": [1],            # length must == mermaid_count; NO zero entries
        "console_errors": [],          # MUST be empty
        "captured_at_utc": "<RFC3339>",
        "agent_id": "<uuid>",
        "captured_via": "playwright MCP against mkdocs serve … roadmap page"
      }

LINK-RESOLUTION GATE (quality/gates/docs-build/link-resolution.py):
  - Current DEFAULT_GLOBS (extend, do not replace):
      docs/index.md, docs/concepts/*.md, docs/tutorials/*.md, docs/how-it-works/*.md,
      docs/guides/*.md, docs/reference/*.md, docs/benchmarks/*.md
  - LINK_RE only matches Markdown `](...)` links; skips http(s)://, mailto:, #anchors.
    → inline-code backtick paths are NOT scanned; absolute GitHub URLs are SKIPPED.
  - Resolves relative links against os.path.dirname(file). So from .planning/PROJECT.md,
    `../docs/roadmap.md` → docs/roadmap.md (exists → PASS). From docs/roadmap.md a
    relative `../.planning/PROJECT.md` would RESOLVE here but mkdocs --strict rejects
    out-of-tree links → use the absolute GitHub URL for the docs→.planning direction.
  - Catalog row `docs-build/link-resolution` (quality/catalogs/docs-build.json, top-level
    list key is `rows`; verifier.args = []) runs this with DEFAULT_GLOBS. Its
    expected.asserts NAMES the glob set verbatim → catalog-first rule: update that assert
    string when you extend the globs.

BANNED-WORDS (skill reposix-banned-words / scripts/banned-words-lint.sh):
  - The mechanical linter only scans docs/index.md + concepts/tutorials/guides + (in
    --all) how-it-works BY PATH. It does NOT scan a top-level docs/roadmap.md.
  - Therefore the roadmap page's cleanliness is a MANUAL self-check: apply Layer-1/Layer-2
    (hero + below-fold) discipline — banned in roadmap prose: `replace` (P1, everywhere),
    and the plumbing vocabulary `FUSE fusermount kernel syscall daemon inode partial-clone
    promisor stateless-connect fast-import protocol-v2`. Describe capabilities in plain
    words (file, folder, edit, commit, push, git, YAML, markdown).

MKDOCS NAV: docs/development/roadmap.md is a SEPARATE internal snapshot (nav-excluded via
  not_in_nav). The new PUBLIC page is top-level docs/roadmap.md — add it to `nav:` (e.g.
  `- Roadmap: roadmap.md` right after `- Home: index.md`). It must be IN the nav or
  mkdocs --strict fails on an orphan doc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/roadmap.md (one color-coded birds-eye mermaid), register in nav, banned-words self-check, and produce render proof</name>
  <files>docs/roadmap.md, mkdocs.yml, .planning/verifications/playwright/roadmap.json</files>
  <action>
Create `docs/roadmap.md` — a public birds-eye roadmap page whose centerpiece is ONE
color-coded mermaid diagram. Requirements (todo point 1):

- ONE `mermaid` fence, top-level (fence opener at column 0 — the render gate greps
  `^```mermaid`).
- Show the arc: **shipped capability-arcs → active "Floor" milestone → future arcs →
  golden end state (OD-4 launch-readiness)**. Convergence toward the golden node should be
  visually obvious (arrows flowing into one end-state node).
- Label nodes by **ARCS / CAPABILITIES, not phase numbers (no P114 etc.) and no dates** —
  this keeps the page truthful without weekly upkeep. Naming the active node "Floor
  (v0.15.0, active)" is fine (it is the current milestone codename); do NOT enumerate
  phase numbers or put any calendar dates in the diagram or prose. Source the arc shape
  from `.planning/PROJECT.md` § "Current Milestone: v0.15.0 Floor" (the Arc D paragraph:
  Floor → gate-shape meta-milestone → truth purge + IA rebuild → benchmark honesty →
  journey slices → launch kit + Show-HN) and § "Previously Validated Milestones" (shipped
  capabilities: git-native partial-clone substrate over REST across 4 backends, docs &
  narrative, quality gates, DVCS over REST with mirror/bus, wave-2 hardening). Golden node
  = the four OD-4 pillars in plain language: live terminal demo · CI-verified honest
  headline numbers · one-command install · Show-HN launch kit.
- Color-code with mermaid `classDef` (one class per band): shipped / active / future /
  golden. Pick fills with readable text contrast in BOTH the light and dark mkdocs-material
  themes (set explicit `color:` on light fills; avoid low-contrast pastels). Keep the graph
  simple (a left-to-right or top-down flow with a few subgraphs/nodes) — legibility beats
  completeness.
- Around the diagram add short plain-language framing prose (a one-paragraph intro + a tiny
  legend explaining the color bands). Keep the whole file well under the 20 000-char `.md`
  ceiling.
- Apply the reposix-banned-words self-check (see <interfaces>): the roadmap prose+labels
  must NOT contain `replace` or any of the FUSE-era / git-native plumbing words. Use
  plain vocabulary. (The mechanical linter won't scan this path, so this is a manual pass.)

Register the page in `mkdocs.yml` `nav:` — add `- Roadmap: roadmap.md` immediately after
`- Home: index.md`. Do NOT touch `not_in_nav` (the internal development/roadmap.md stays
excluded).

Render-review (todo point 3) — do BOTH:
1. **mcp-mermaid eyeball:** render the diagram source via the mcp-mermaid MCP tool to an
   image and inspect for spaghetti/crossing edges, overlapping labels, unreadable node
   text, and subgraphs that don't visually group. Iterate on the diagram until clean.
2. **Playwright render proof (required for the mermaid-renders gate):** build/serve the
   site (`mkdocs serve` or `mkdocs build`), navigate to the roadmap page with the
   playwright MCP, and confirm the `<div class="mermaid">`/`<pre class="mermaid">` element
   has ≥1 `<svg>` child and zero render-error console messages. Write
   `.planning/verifications/playwright/roadmap.json` with the schema in <interfaces>
   (mermaid_count=1, svg_counts=[N≥1], console_errors=[], a fresh RFC3339 captured_at_utc,
   a uuid agent_id, and an honest captured_via). If mkdocs isn't installed, note the gap
   in the summary — but the artifact must reflect a real render, never a fabricated one
   (OP-1: verify against reality).
  </action>
  <verify>
    <automated>bash quality/gates/docs-build/mkdocs-strict.sh && bash quality/gates/docs-build/mermaid-renders.sh && scripts/banned-words-lint.sh</automated>
  </verify>
  <done>docs/roadmap.md exists with one column-0 mermaid fence; `- Roadmap: roadmap.md` in nav; roadmap.json render proof committed (real SVG, no console errors); mkdocs-strict + mermaid-renders + banned-words all exit 0; mcp-mermaid eyeball review done and diagram is legible.</done>
</task>

<task type="auto">
  <name>Task 2: Bi-directional SYNC cross-links + OP-9 distill-checklist reminder</name>
  <files>docs/roadmap.md, .planning/PROJECT.md, .planning/PRACTICES.md</files>
  <action>
Wire the owner-required bi-directional keep-in-check link (todo point 2). BOTH sides carry
the link AND an adjacent non-rendering `<!-- SYNC: ... -->` HTML comment with concise
instructions ("edit either side → update the other; re-color the arcs at milestone close").

- In `docs/roadmap.md`: add a link to the private planning source using the ABSOLUTE
  GitHub URL (relative links out of docs/ break mkdocs-strict):
  `https://github.com/reubenjohn/reposix/blob/main/.planning/PROJECT.md`, immediately
  followed by e.g.
  `<!-- SYNC: paired with .planning/PROJECT.md § Current Milestone. Edit either side → update the other; re-color the arcs at milestone close. -->`
  Place it in a short "Source of truth" note near the top (mirror the existing pattern in
  docs/development/roadmap.md, which already GitHub-URLs into `.planning/`).
- In `.planning/PROJECT.md`: add a one-line pointer to the public roadmap using the
  REPO-RELATIVE path `../docs/roadmap.md` (resolves under link-resolution.py), immediately
  followed by a matching
  `<!-- SYNC: paired with docs/roadmap.md. Edit either side → update the other; re-color the arcs at milestone close. -->`
  Put it inside the `## Current Milestone: v0.15.0 Floor` section (a single line at the end
  of that section) — keep PROJECT.md's one-logical-change-per-session discipline; do not
  reflow other content.
- In `.planning/PRACTICES.md` § "OP-9 — Milestone-close ritual": append ONE line/bullet to
  the ritual reminding the distiller to keep the `docs/roadmap.md ↔ .planning/PROJECT.md`
  SYNC pair in step and re-color the roadmap's arc bands (shipped/active/future) at
  milestone close. One line — do not restructure the section.

Re-run the roadmap banned-words self-check after adding the GitHub URL + comment (the URL
must not smuggle in a banned word; the string "replace" must not appear).
  </action>
  <verify>
    <automated>grep -q 'github.com/reubenjohn/reposix/blob/main/.planning/PROJECT.md' docs/roadmap.md && grep -q 'SYNC:' docs/roadmap.md && grep -q '\.\./docs/roadmap\.md' .planning/PROJECT.md && grep -q 'SYNC:' .planning/PROJECT.md && grep -qi 'roadmap' .planning/PRACTICES.md</automated>
  </verify>
  <done>Both files link to each other; each link has an adjacent <!-- SYNC: ... --> comment; docs→.planning uses the GitHub URL, .planning→docs uses ../docs/roadmap.md; PRACTICES.md OP-9 has the one-line SYNC-pair reminder; roadmap banned-words self-check still clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extend link-resolution DEFAULT_GLOBS (catalog-first), run the full gate battery, and dispose the optional SYNC-marker gate</name>
  <files>quality/catalogs/docs-build.json, quality/gates/docs-build/link-resolution.py, .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md</files>
  <action>
Mechanically enforce link-resolution on BOTH cross-link directions (todo point 5).

CATALOG-FIRST (do this edit first, ideally as its own commit before the code change):
- In `quality/catalogs/docs-build.json` (top-level list key is `rows`), update the
  `docs-build/link-resolution` row's `expected.asserts[0]` string so it names the new glob
  contract — add `docs/*.md` and `.planning/PROJECT.md` to the enumerated set (currently it
  lists only `docs/{index.md, concepts/*.md, tutorials/*.md, how-it-works/*.md,
  guides/*.md, reference/*.md, benchmarks/*.md}`). This is the GREEN contract the mechanical
  verifier is graded against; it must describe the extended reality.

CODE:
- In `quality/gates/docs-build/link-resolution.py`, extend `DEFAULT_GLOBS` (append, do not
  remove existing entries) with `"docs/*.md"` and `".planning/PROJECT.md"`. `docs/*.md`
  now also pulls in docs/security.md + docs/why.md + docs/roadmap.md (index.md is already
  listed — the harmless overlap just double-counts, fine).
- RUN the extended gate: `python3 quality/gates/docs-build/link-resolution.py`. If it
  surfaces any NEWLY-broken relative links in the now-scanned top-level pages
  (security.md / why.md) or in .planning/PROJECT.md, FIX them (correct the target or
  convert an out-of-tree link to the GitHub-URL form). The gate must exit 0. Note:
  PROJECT.md's paths are mostly inline-code backticks which LINK_RE does not scan — only
  real `](...)` links are checked, so expect few or none.

FULL GATE BATTERY — run and confirm exit 0 for each (todo point 3 + constraints):
  bash quality/gates/docs-build/mkdocs-strict.sh
  bash quality/gates/docs-build/mermaid-renders.sh
  python3 quality/gates/docs-build/link-resolution.py
  scripts/banned-words-lint.sh
(plus the mcp-mermaid eyeball + playwright artifact from Task 1 already done.)

OPTIONAL SYNC-marker structure gate (todo point 4) — DEFAULT = FILE, build only if
trivially cheap:
- Assess honestly: a real gate needs a new verifier fn in a `quality/gates/structure/
  freshness/` submodule + registration in the `DISPATCH` dict of
  `quality/gates/structure/freshness-invariants.py` + a catalog-first row in
  `quality/catalogs/freshness-invariants.json` + a selftest. That is a multi-file
  structure-dimension addition — NOT trivial for a quick lane.
- DEFAULT ACTION: append one entry to
  `.planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md` (severity LOW, STATUS: OPEN,
  with a concrete fix-sketch): "structure gate asserting the `<!-- SYNC:` marker pair
  exists on BOTH docs/roadmap.md AND .planning/PROJECT.md — add a `verify_sync_marker_pair`
  fn to freshness-invariants DISPATCH + a catalog row `structure/roadmap-project-sync-pair`;
  fires on pre-push; catches silent drift where one side drops its SYNC comment." Match the
  existing GTH-V15-NN numbering + entry format in that file.
- ONLY build the gate inline if you judge it a genuinely trivial single-function addition
  that stays within the quick-lane budget; if so, honor catalog-first (row before code) and
  add the selftest.
  </action>
  <verify>
    <automated>python3 quality/gates/docs-build/link-resolution.py && grep -q 'docs/\*\.md' quality/gates/docs-build/link-resolution.py && grep -q '.planning/PROJECT.md' quality/gates/docs-build/link-resolution.py && python3 -c "import json;r=[x for x in json.load(open('quality/catalogs/docs-build.json'))['rows'] if x['id']=='docs-build/link-resolution'][0];a=' '.join(r['expected']['asserts']);assert 'PROJECT.md' in a and 'docs/*.md' in a, 'catalog row not updated'"</automated>
  </verify>
  <done>DEFAULT_GLOBS covers docs/*.md + .planning/PROJECT.md; the docs-build/link-resolution catalog row asserts describe the new contract; link-resolution + mkdocs-strict + mermaid-renders + banned-words all exit 0; the optional SYNC-marker gate is either filed to GOOD-TO-HAVES.md (default) or built with a catalog-first row + selftest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docs-only + gate-config change. No remote bytes are read, no `Tainted<T>` flows, no egress/side-effecting action is introduced. The one external reference added is a static GitHub URL literal in a Markdown comment/link — not a network call at build time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-mk5-01 | Tampering | link-resolution.py DEFAULT_GLOBS extension | accept | Change only widens which committed Markdown files are link-checked; strictly increases coverage, cannot weaken a gate. Catalog-first update keeps the graded contract honest. |
| T-mk5-02 | Information disclosure | docs/roadmap.md public page | accept | Content is hand-authored public roadmap prose from PROJECT.md (already-public planning surface); no secrets, no backend data, no untrusted input rendered. |
</threat_model>

<verification>
Run from repo root, all must exit 0:

```bash
bash quality/gates/docs-build/mkdocs-strict.sh          # site builds --strict, roadmap in nav
bash quality/gates/docs-build/mermaid-renders.sh        # roadmap.json render proof valid
python3 quality/gates/docs-build/link-resolution.py     # extended DEFAULT_GLOBS, both dirs green
scripts/banned-words-lint.sh                            # no regressions in scanned layers
```

Plus (agent-performed, not scriptable): mcp-mermaid eyeball review of the diagram for
legibility, and the manual reposix-banned-words self-check on docs/roadmap.md (plain
vocabulary, no `replace`, no plumbing words).
</verification>

<success_criteria>
- docs/roadmap.md: ONE color-coded birds-eye mermaid (arcs/capabilities, no phase numbers
  or dates) converging on the OD-4 golden end state; registered in mkdocs nav.
- `.planning/verifications/playwright/roadmap.json` proves a real SVG render (no console
  errors); mcp-mermaid eyeball confirms legibility.
- Bi-directional cross-links between docs/roadmap.md and .planning/PROJECT.md, each with an
  adjacent `<!-- SYNC: ... -->` comment (docs→.planning via GitHub URL, .planning→docs via
  ../docs/roadmap.md).
- PRACTICES.md OP-9 carries the one-line roadmap↔PROJECT SYNC-pair + re-color reminder.
- link-resolution.py DEFAULT_GLOBS covers docs/*.md + .planning/PROJECT.md; the
  docs-build/link-resolution catalog row asserts describe the new contract (catalog-first).
- mkdocs-strict, mermaid-renders, link-resolution, banned-words all exit 0.
- Optional SYNC-marker gate: filed to GOOD-TO-HAVES.md (default) or built catalog-first.

**Staging + push discipline (constraints — MANDATORY):**
- LOCAL COMMIT ONLY. Do NOT `git push`. This lane ends at a local commit.
- Shared working tree: `git add` EXPLICIT paths only — NEVER `git add -A` or `git add .`.
  Stage exactly: docs/roadmap.md, mkdocs.yml,
  .planning/verifications/playwright/roadmap.json, .planning/PROJECT.md,
  .planning/PRACTICES.md, quality/gates/docs-build/link-resolution.py,
  quality/catalogs/docs-build.json, and (if touched)
  .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md (+ any structure-gate files if the
  optional gate was built). Catalog-first: prefer committing the docs-build.json row change
  before the link-resolution.py code change.
</success_criteria>

<output>
After completion, create
`.planning/quick/260715-mk5-public-birds-eye-roadmap-diagram-author-/260715-mk5-SUMMARY.md`
recording: the diagram's arc structure + color bands, the render-proof result (svg_counts /
console_errors), both SYNC-comment locations, the DEFAULT_GLOBS + catalog-row diff, the
optional-gate disposition (filed vs built), any newly-surfaced broken links fixed, and any
noticing items (OD-3). Note explicitly that NO push was performed and which exact paths
were staged.
</output>
