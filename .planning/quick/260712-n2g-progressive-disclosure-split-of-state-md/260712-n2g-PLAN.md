---
phase: quick-260712-n2g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/STATE-history.md
autonomous: true
requirements:
  - DOC-HYGIENE-STATE-SPLIT
must_haves:
  truths:
    - "STATE.md is under 20000 bytes (aim ~15000 or lower for headroom)"
    - "STATE.md frontmatter (lines 1-32, the --- … --- fence) is byte-identical to the pre-split HEAD version"
    - "session-start-brief.sh still reads a valid cursor from STATE.md lines 1-24"
    - "STATE-history.md exists as a top-level .planning/ companion and holds every relocated closed/historical block VERBATIM"
    - "No content is lost — every moved heading now lives in STATE-history.md"
    - "STATE.md STILL contains the durable reference sections (Per-milestone history, Project Reference, Blockers/Concerns) — NOT relocated"
    - "The three affected structure freshness invariants still pass (no new RED)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Lean live cursor: verbatim frontmatter + Workstream C + Per-milestone history + Project Reference + Blockers + Quick Tasks table + Session Continuity + pointers"
      contains: "### Workstream C"
    - path: ".planning/STATE-history.md"
      provides: "Companion history file mirroring PROJECT-history.md convention; relocated closed/historical blocks verbatim"
      contains: "Companion to `.planning/STATE.md`"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/STATE-history.md"
      via: "progressive-disclosure pointer"
      pattern: "STATE-history\\.md"
---

<objective>
Progressive-disclosure split of `.planning/STATE.md`. The file is 21137 B, over the
project *.md 20000 B budget. Relocate the CLOSED / historical cursor prose into a new
sibling `.planning/STATE-history.md` companion (mirroring the existing
`.planning/PROJECT-history.md` / `PROJECT.md` convention), leaving STATE.md as a lean
live cursor + pointers.

Purpose: Keep STATE.md at "what an agent needs in working memory every session" so the
session-start brief and `/gsd-resume-work` surface only the live cursor, while history
stays one click away. Get STATE.md comfortably under budget (target ~15000 B).
Output: A trimmed `.planning/STATE.md` (< 20000 B) + a new `.planning/STATE-history.md`.

This is DOC-ONLY: no cargo, no build, no test-suite, no push in these tasks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/PROJECT-history.md

<!-- CONVENTION TO MIRROR: PROJECT.md points at its history companion with the style -->
<!--   > Full … lives in `.planning/PROJECT-history.md`  (PROJECT.md lines 15, 170, 199) -->
<!-- PROJECT-history.md opens with: "Companion to `.planning/PROJECT.md`. Holds … . -->
<!--   This file exists so PROJECT.md stays at 'what an agent needs in working memory -->
<!--   every session.' History is one click away when needed." — REPLICATE this shape. -->

<hard_invariants>
These are MUST-hold, non-negotiable (from the orchestrator's gate analysis):

1. FRONTMATTER IS UNTOUCHABLE. Lines 1-32 (the `---` … `---` fence:
   `gsd_state_version`, `mode`, `status`, `last_updated`, `last_activity`, and the full
   `workstreams:` cursor with `workstream_a`/`workstream_b`/`workstream_c`) stay
   BYTE-IDENTICAL and at the very top. Do NOT restructure, reorder, shorten, or even bump
   `last_updated`/`last_activity`. Tooling parses this AND
   `.claude/hooks/session-start-brief.sh` reads STATE.md LINES 1-24 as the session cursor.

2. RELOCATION, NOT DELETION. Every block moved OUT of STATE.md lands VERBATIM in
   `.planning/STATE-history.md`. When unsure whether a block is live or closed, KEEP it in
   STATE.md with a pointer rather than risk dropping live cursor state.

3. EXACT PATH. Create `.planning/STATE-history.md` — top-level `.planning/`, NOT under
   `.planning/milestones/`. (`PROJECT-history.md` already lives there and passes all
   structure invariants; do not invent a different location.)

4. KEEP THE `### Quick Tasks Completed` HEADING STRING BYTE-IDENTICAL. The orchestrator
   appends rows to this table by matching that exact heading text. Do NOT change its level
   (`###`) or wording, and keep the full table verbatim.

5. FILE-OWNERSHIP FENCE. Only `.planning/STATE.md`, `.planning/STATE-history.md` (new),
   and this quick dir's plan/summary may be modified. Do NOT `git add -A` — foreign
   uncommitted paths exist (quality/catalogs/code.json, .planning/phases/21-*,
   .planning/phases/22-*, scripts/demos/, scripts/dev/, quality/reports/verifications/docs-repro/).
   Stage ONLY the two STATE files + the quick artifacts by explicit path.
</hard_invariants>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate closed/historical blocks into STATE-history.md; trim STATE.md to the live cursor</name>
  <files>.planning/STATE-history.md, .planning/STATE.md</files>
  <action>
Perform a single, information-preserving relocation. Read the current `.planning/STATE.md`
in full first so every moved byte is preserved.

STEP A — Create `.planning/STATE-history.md` with a companion header that MIRRORS
`PROJECT-history.md` (see the context comment). Header body:

    # reposix — STATE history

    Companion to `.planning/STATE.md`. Holds the CLOSED / superseded cursor prose for
    workstreams that have shipped or been resequenced. The live cursor — frontmatter, current position, current focus, per-milestone cross-references, project reference, blockers, the
    `### Quick Tasks Completed` table, and session continuity — lives in `STATE.md`.

    This file exists so STATE.md stays at "what an agent needs in working memory every
    session": as of 2026-07-12 the only remaining live item is Workstream C's owner-cut
    v0.14.0 tag. History is one click away when needed.

Then, UNDER that header, paste the following blocks VERBATIM (exact text, in this order),
each under a clear `##`/`###` heading so nothing is lost:
  - The entire `### Workstream A — v0.13.0 (extended) — CLOSED GREEN` subsection —
    STATE.md lines 42-97 (the closed-status prose, the **PRE-TAG CHECKLIST**, the
    **RELEASE DECISION DELEGATED TO L0** block, the **RELEASE RUNBOOK (L0-owned tail)**
    block, the **Queued post-tag `/gsd-quick` meta-infra items** list, and its "Next agent
    action" line).
  - The entire `### Workstream B — v0.13.2 — QUEUED (RESEQUENCED per OD-4)` subsection —
    STATE.md lines 99-112 (the superseded queued narrative, its Plan/Status/Next/Last
    activity lines, and the 2026-07-11 NOTE).
  - The `**Phase decomposition + pre-kickoff scaffolding (superseded …)**` paragraph —
    STATE.md line 148.

STEP B — Rewrite `.planning/STATE.md` so it retains the live cursor PLUS these durable sections KEPT verbatim in STATE.md (do NOT relocate): `## Per-milestone history (cross-references)`, `## Project Reference`, `## Blockers / Concerns` — Blockers holds a LIVE owner-gate item:
  1. Frontmatter (lines 1-32) — LEAVE EXACTLY AS-IS, byte-identical, at the very top. Do
     not touch a single character (invariant 1).
  2. `# Project State` + `## Current Position`:
     - Keep the `**Mode:** serial-workstreams …` line (38) and the `**OD-3 mandate …**`
       paragraph (40) — these govern the current operating mode.
     - Insert a pointer where Workstreams A and B detail used to be:
       `> Full Workstream A (v0.13.0, CLOSED GREEN) pre-tag checklist / release runbook /`
       `> queued post-tag items, and the superseded Workstream B (v0.13.2, QUEUED) narrative,`
       `> live in \`.planning/STATE-history.md\`.`
     - Keep the ENTIRE `### Workstream C — v0.14.0 wave-2 hardening — COMPLETE (awaiting`
       `owner tag)` subsection (lines 114-134) VERBATIM — this is the live cursor.
  3. `## Current Focus`:
     - Keep the three `**Active milestones**` bullets (138-142), the `**Last shipped`
       `milestone:**` line (144), and the `**Cargo serialization rule …**` line (146).
     - Replace the superseded phase-decomposition paragraph (148) with a one-line pointer:
       `> Superseded phase-decomposition + pre-kickoff scaffolding narrative →`
       `> \`.planning/STATE-history.md\`.`
  KEEP (document order, between Current Focus and Quick Tasks): the `## Per-milestone history (cross-references)`, `## Project Reference`, and `## Blockers / Concerns` sections VERBATIM in STATE.md — do NOT relocate.
  4. `### Quick Tasks Completed` — keep the heading string EXACTLY (invariant 4) and the
     full table (175-183) VERBATIM.
  5. `## Session Continuity` (185-189) — keep it, and append one pointer line:
     `> Closed Workstream A + superseded Workstream B narrative`
     `> \`.planning/STATE-history.md\`.`

Use the Write/Edit tools (never heredoc). Preserve exact wording, emoji (✅), backticks,
and commit SHAs in every moved block — this is a relocation, not a rewrite.
  </action>
  <verify>
    <automated>test -f .planning/STATE-history.md && grep -q "PRE-TAG CHECKLIST" .planning/STATE-history.md && grep -q "RELEASE RUNBOOK" .planning/STATE-history.md && grep -q "### Workstream C" .planning/STATE.md && grep -q "### Quick Tasks Completed" .planning/STATE.md && grep -q "STATE-history.md" .planning/STATE.md</automated>
  </verify>
  <done>
STATE-history.md exists with the companion header + all three relocated blocks verbatim.
STATE.md retains the untouched frontmatter, the Workstream C subsection, the Quick Tasks
table, Session Continuity, and pointers to the history file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify budget, frontmatter integrity, cursor contract, and structure invariants</name>
  <files>.planning/STATE.md, .planning/STATE-history.md</files>
  <action>
Run the full verification gate. All checks must pass; if any fails, loop back to Task 1
and fix (do NOT proceed to commit over a red check). Run these against the working tree
BEFORE committing, so `git show HEAD:.planning/STATE.md` still yields the pre-split
original for the frontmatter comparison:

  (a) SIZE < 20000 B (target ~15000):
      wc -c .planning/STATE.md
      test "$(wc -c < .planning/STATE.md)" -lt 20000

  (b) FRONTMATTER byte-identical (lines 1-32) vs pre-split HEAD:
      cmp <(git show HEAD:.planning/STATE.md | sed -n '1,32p') <(sed -n '1,32p' .planning/STATE.md)

  (c) SESSION-START-BRIEF cursor contract (lines 1-24 still yield the frontmatter cursor):
      sed -n '1,24p' .planning/STATE.md | grep -q 'gsd_state_version'
      sed -n '1,24p' .planning/STATE.md | grep -q 'workstreams:'

  (d) STRUCTURE freshness invariants — all three exit 0 (no new RED):
      python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-orphan-docs
      python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-roadmap-or-requirements
      python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-top-level-planning-audits

  (e) CONTENT PRESERVED — moved headings now live in STATE-history.md; live cursor stays
      in STATE.md:
      test -f .planning/STATE-history.md
      grep -q "Per-milestone history (cross-references)" .planning/STATE.md
      grep -q "Project Reference" .planning/STATE.md
      grep -q "Workstream B — v0.13.2" .planning/STATE-history.md
      grep -q "### Workstream C" .planning/STATE.md
      grep -q "### Quick Tasks Completed" .planning/STATE.md

After all checks pass, commit ONLY the two STATE files + this quick's plan/summary by
EXPLICIT path (never `git add -A` — foreign uncommitted paths must stay untouched):
      git add .planning/STATE.md .planning/STATE-history.md \
        .planning/quick/260712-n2g-progressive-disclosure-split-of-state-md/
      git commit -m "docs(planning): progressive-disclosure split of STATE.md → STATE-history.md companion"
  </action>
  <verify>
    <automated>test "$(wc -c < .planning/STATE.md)" -lt 20000 && cmp <(git show HEAD:.planning/STATE.md | sed -n '1,32p') <(sed -n '1,32p' .planning/STATE.md) && sed -n '1,24p' .planning/STATE.md | grep -q 'gsd_state_version' && sed -n '1,24p' .planning/STATE.md | grep -q 'workstreams:' && python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-orphan-docs && python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-roadmap-or-requirements && python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-top-level-planning-audits && grep -q "Per-milestone history (cross-references)" .planning/STATE.md && grep -q "Project Reference" .planning/STATE.md && grep -q "Workstream B — v0.13.2" .planning/STATE-history.md</automated>
  </verify>
  <done>
STATE.md < 20000 B; frontmatter lines 1-32 byte-identical to HEAD; lines 1-24 still yield
the cursor; all three structure invariants exit 0; every moved heading verified present in
STATE-history.md; commit landed with only the two STATE files + quick artifacts staged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Doc-only relocation of local planning prose. No remote byte, no `Tainted<T>` surface, no network call, no side-effecting action. No trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-n2g-01 | Tampering | STATE.md frontmatter (tooling/session-start-brief cursor) | mitigate | Invariant 1 + verify (b) `cmp` lines 1-32 byte-identical to HEAD; verify (c) lines 1-24 still parse as cursor. |
| T-n2g-02 | Information disclosure / loss | Relocated cursor prose | mitigate | Relocation-not-deletion (invariant 2) + verify (e) grep spot-check that every moved heading now lives in STATE-history.md. |
</threat_model>

<verification>
- `wc -c .planning/STATE.md` < 20000 (target ~15000).
- Frontmatter lines 1-32 byte-identical to pre-split HEAD (`cmp`).
- Lines 1-24 still yield the session-start-brief cursor.
- Three structure freshness invariants exit 0 (no new RED).
- STATE-history.md exists with every relocated block verbatim; live cursor intact in STATE.md.
- Commit stages ONLY the two STATE files + quick artifacts (no foreign paths).
</verification>

<success_criteria>
- STATE.md is a lean live cursor under budget, with pointers to STATE-history.md.
- STATE-history.md is a faithful companion mirroring the PROJECT-history.md convention.
- Zero information lost; frontmatter untouched; orchestrator's Quick Tasks append target intact.
- No cargo/build/test/push; no foreign-owned uncommitted path modified.
</success_criteria>

<output>
After completion, create
`.planning/quick/260712-n2g-progressive-disclosure-split-of-state-md/260712-n2g-SUMMARY.md`
</output>
