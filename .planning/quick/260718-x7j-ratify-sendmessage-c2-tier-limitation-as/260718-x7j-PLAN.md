---
phase: quick-260718-x7j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ORCHESTRATION.md
  - .planning/CONSULT-DECISIONS.md
autonomous: true
requirements:
  - owner-ruling-2026-07-18-sendmessage-c2-tier-limitation
user_setup: []

must_haves:
  truths:
    - "A reader in ORCHESTRATION.md §3 (relief/liveness) hits the C2-tier SendMessage caveat in context"
    - "A reader in ORCHESTRATION.md §11 (fork/resume phase-close pattern) hits the same caveat in context"
    - "CONSULT-DECISIONS.md records the owner ruling as RATIFIED standing doctrine with the re-discovery provenance"
    - "The doctrine doc and the ledger land in ONE commit (fix-it-twice), pushed, HEAD == origin/main"
  artifacts:
    - path: ".planning/ORCHESTRATION.md"
      provides: "SendMessage C2-tier limitation caveat in §3 AND §11"
      contains: "SendMessage tier limitation"
    - path: ".planning/CONSULT-DECISIONS.md"
      provides: "2026-07-18 [OWNER] ledger entry ratifying the standing limitation"
      contains: "SendMessage C2-tier tool-grant limitation ratified as STANDING doctrine"
  key_links:
    - from: ".planning/ORCHESTRATION.md §3/§11"
      to: ".planning/CONSULT-DECISIONS.md 2026-07-18 [OWNER] entry"
      via: "cross-reference (fix-it-twice: doctrine doc + ledger in same change)"
      pattern: "CONSULT-DECISIONS"
---

<objective>
Ratify the "SendMessage disabled at C2 tier" finding as STANDING, owner-ratified doctrine.
The manager/owner RULED (2026-07-18) that this is a permanent tool-grant limitation of the
`phase-coordinator` registry entry — NOT a session/config fluke — after it was independently
re-discovered across the #63→#64→#65 handover chain. Encode it in the two authoritative
homes so the next coordinator reads the tightened rule instead of re-diagnosing it.

Purpose: stop the recurring re-discovery loop; make the strict-serialize / fresh-LEAVES
phase-close mitigation a ratified standing rule that every C2/C1 charter carries verbatim.
Output: caveat in ORCHESTRATION.md §3 + §11; a matching [OWNER] ledger entry in
CONSULT-DECISIONS.md; one commit; pushed; HEAD == origin/main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ORCHESTRATION.md
@.planning/CONSULT-DECISIONS.md

<notes>
DOCS-ONLY planning-artifact edit. No code, no cargo, no reposix/sim/git fixture setup —
leaf-isolation /tmp rules do NOT apply. The `git commit` in Task 2 is a normal shared-repo
commit of planning artifacts under the real git user (NOT a `t <t@t>` fixture identity), so
the pre-commit fixture-identity check does not fire.

File-size note: ORCHESTRATION.md is already ~21.7KB (over the 20KB .md ceiling) but is
WAIVED — `quality/gates/structure/file-size-limits.sh` runs `--warn-only` for the named
planning ledgers (ORCHESTRATION included), so it WARNs, never BLOCKs. Keep both insertions
tight (progressive-disclosure discipline); do not restate what the surrounding paragraph
already says. Do NOT split the file — the task requires the caveat IN §3/§11 so a reader
hits it in context.

Scope fence (do NOT exceed): edit ONLY .planning/ORCHESTRATION.md and
.planning/CONSULT-DECISIONS.md. Do NOT touch P126, other phases, `.claude/skills/`,
`.claude/agents/`, coordinator-dispatch, or any other artifact. The standing
promotion-sweep rule (a wider stale-reference sweep across hooks/agents/skills) is
explicitly OUT OF SCOPE for this quick — if you notice a place that should carry the
caveat, record it in the SUMMARY's noticing section; do NOT edit it.
</notes>

<anchor_ORCHESTRATION_section3>
§3 "Context budget + relief/handover protocol" — the **Liveness doctrine** paragraph
(currently lines ~137-151) ends with L0 "SendMessages the coordinator to resume the phase
close on green." The §3 caveat is placed immediately AFTER that paragraph, because that is
exactly where SendMessage/resume behavior is discussed — it explains WHY the durable CI
watch + resume-on-green SendMessage must live at L0.
</anchor_ORCHESTRATION_section3>

<anchor_ORCHESTRATION_section11>
§11 "No-fable mode ... Judgment calls" — the final paragraph (currently lines ~289-297)
ends with: "...to drive a phase close, dispatch the verifier→executor LEAVES directly (the
P122-blessed deterministic pattern), NEVER `fork` a coordinator to 'resume/close' it ...
STATE.md unchanged — caught only by verify-against-reality." The §11 caveat is anchored
immediately AFTER that sentence, beside the fresh-LEAVES phase-close guidance.
</anchor_ORCHESTRATION_section11>

<ledger_format>
CONSULT-DECISIONS.md format (from its header): `## <date> [SELF|FABLE|OWNER|MANAGER]
<one-line>` then dashed rationale + evidence fields (Context/Decision/Rationale/
Reversibility/Status/Commit shape). Entries are chronological — APPEND the new entry at
the END of the file (after the 2026-07-18 [SELF] roadmap three-block reshape entry),
preceded by a `---` separator matching the existing between-entry rule. Tag = `[OWNER]`
(the task frames this as an owner ruling). Date = the RULING date 2026-07-18.
</ledger_format>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the SendMessage C2-tier limitation caveat to ORCHESTRATION.md §3 and §11, and append the ratifying [OWNER] ledger entry to CONSULT-DECISIONS.md (fix-it-twice atomic edit)</name>
  <files>.planning/ORCHESTRATION.md, .planning/CONSULT-DECISIONS.md</files>
  <action>
Three edits, all in this task (they are the single fix-it-twice doctrine-ratification unit;
Task 2 lands them in one commit). Preserve every load-bearing fact in all three:
(a) SendMessage is a tool-grant limitation of the `phase-coordinator` registry entry — NOT
granted at the C2 tier or below; (b) a C2 cannot SendMessage/halt/resume its own background
children and a child cannot resume-by-id back to its parent C2 — the failure is
specifically **C2→child and child→C2**; (c) **L0→C2 SendMessage DOES work** (an L0
top-level session resumes a C2 by session id — confirmed repeatedly); (d) mitigation:
coordinators at C2 and below MUST serialize strictly and drive every phase close via FRESH
verifier→executor LEAVES (P122-blessed pattern), never `fork`-to-resume, never
background-and-resume a child at C2; (e) RATIFIED standing doctrine, not a temporary
workaround; (f) embed the caveat verbatim in every C2/C1 charter; (g) owner-ratified
2026-07-18.

EDIT 1 — ORCHESTRATION.md §3, immediately AFTER the "Liveness doctrine" paragraph (see
<anchor_ORCHESTRATION_section3>). Insert this paragraph (adapt whitespace to the file's
markdown style; keep it as a single bolded-lead paragraph):

  **SendMessage tier limitation (STANDING, owner-ratified 2026-07-18) — WHY the durable CI
  watch lives at L0.** SendMessage is a tool-grant limitation of the `phase-coordinator`
  registry entry: it is NOT granted at the phase-coordinator (C2) tier or below. So a C2
  cannot SendMessage / halt / resume its own background children, and a child cannot
  resume-by-id back to its parent C2 — the failure is specifically **C2→child and
  child→C2**. **L0→C2 SendMessage DOES work** (an L0 top-level session resumes a C2 by
  session id — confirmed repeatedly), which is exactly why the durable CI watch and the
  resume-on-green SendMessage sit at L0, not at the coordinator. Because a C2 cannot
  background-and-resume a child, coordinators at the C2 tier and below MUST **serialize
  strictly** and drive every phase close via **FRESH verifier→executor LEAVES** (the
  P122-blessed deterministic pattern — dispatch the leaves directly; never `fork` a
  coordinator to resume/close it, never background-and-resume a child at the C2 tier).
  This is **RATIFIED standing doctrine, not a temporary workaround** — embed the caveat
  verbatim in every C2/C1 charter. Ledger: `.planning/CONSULT-DECISIONS.md`
  (2026-07-18 [OWNER]).

EDIT 2 — ORCHESTRATION.md §11, immediately AFTER the fork/resume sentence at the end of
the Judgment-calls paragraph (see <anchor_ORCHESTRATION_section11>). Insert this paragraph:

  **SendMessage tier limitation (STANDING, owner-ratified 2026-07-18).** The reason the
  phase-close pattern above dispatches FRESH verifier→executor LEAVES rather than resuming
  a warm coordinator is a hard tool-grant limitation: SendMessage is NOT granted at the
  phase-coordinator (C2) tier or below (it is absent from the `phase-coordinator` registry
  entry's grant). A C2 therefore cannot SendMessage / halt / resume its own background
  children, and a child cannot resume-by-id back to its parent C2 — the failure is
  specifically **C2→child and child→C2**. **L0→C2 SendMessage DOES work** (an L0 session
  resumes a C2 by session id — confirmed repeatedly). Consequence: coordinators at C2 and
  below MUST **serialize strictly** and drive every phase close via fresh verifier→executor
  LEAVES — never `fork` a coordinator to resume/close it, never background-and-resume a
  child at the C2 tier. This is **RATIFIED standing doctrine, not a temporary workaround**;
  embed the caveat verbatim in every C2/C1 charter. Ledger:
  `.planning/CONSULT-DECISIONS.md` (2026-07-18 [OWNER]).

EDIT 3 — CONSULT-DECISIONS.md, APPEND at the end of the file (after the 2026-07-18 [SELF]
roadmap three-block reshape entry), preceded by a `---` separator (matching the existing
between-entry style). Add exactly:

  ## 2026-07-18 [OWNER] SendMessage C2-tier tool-grant limitation ratified as STANDING doctrine

  - **Finding:** `SendMessage` is unavailable at the phase-coordinator (C2) tier and below
    — a C2 cannot SendMessage / halt / resume its own background children, and a child
    cannot resume-by-id back to its parent C2. The failure is specifically **C2→child and
    child→C2**; **L0→C2 SendMessage DOES work** (an L0 top-level session resumes a C2 by
    session id — confirmed repeatedly). Root cause: the tool is simply not in the
    `phase-coordinator` registry entry's tool grant.
  - **Re-discovery count:** independently re-discovered across the #63→#64→#65 handover
    chain (the 2nd/3rd re-discovery this milestone) before this ratification — that
    recurrence is WHY it is now formalized rather than re-diagnosed each rotation.
  - **Ruling (owner/manager, 2026-07-18):** ratified as a **STANDING tool-grant limitation
    of the `phase-coordinator` registry grant** — NOT a session/config fluke to chase or
    "fix." It is a property of the registry entry, permanent until the grant itself changes.
  - **Standing mitigation:** coordinators at C2 and below **serialize strictly** and drive
    every phase close via **FRESH verifier→executor LEAVES** only; **never `fork`-to-resume**
    a coordinator, **never background-and-resume a child at C2**; **embed this caveat
    verbatim in every C2/C1 charter**. This is doctrine, not a workaround.
  - **Cross-reference (fix-it-twice):** mirrored into `.planning/ORCHESTRATION.md` §3 (WHY
    the durable CI watch lives at L0) and §11 (beside the fresh-LEAVES phase-close pattern)
    in the same change.
  - **Status:** RATIFIED — standing doctrine. **Reversibility:** doc/ledger-only, reversible
    via `git revert`. **Commit:** this quick's ratification commit.

Do not alter any other prose in either file. Do not re-flow unrelated paragraphs.
  </action>
  <verify>
    <automated>cd /home/reuben/workspace/reposix && test "$(grep -c 'SendMessage tier limitation' .planning/ORCHESTRATION.md)" -ge 2 && grep -q 'C2→child and child→C2' .planning/ORCHESTRATION.md && grep -q 'L0→C2 SendMessage DOES work' .planning/ORCHESTRATION.md && grep -q 'SendMessage C2-tier tool-grant limitation ratified as STANDING doctrine' .planning/CONSULT-DECISIONS.md && grep -q '2nd/3rd re-discovery this milestone' .planning/CONSULT-DECISIONS.md && echo OK</automated>
  </verify>
  <done>
ORCHESTRATION.md §3 and §11 each carry the "SendMessage tier limitation" caveat with the
C2→child/child→C2 failure statement and the "L0→C2 SendMessage DOES work" fact;
CONSULT-DECISIONS.md ends with the 2026-07-18 [OWNER] ledger entry carrying the finding,
re-discovery provenance, ruling, standing mitigation, and the ORCHESTRATION §3/§11
cross-reference. The verify command prints OK.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write SUMMARY, commit the ratification (single fix-it-twice commit), push origin main, verify HEAD == origin/main</name>
  <files>.planning/quick/260718-x7j-ratify-sendmessage-c2-tier-limitation-as/260718-x7j-SUMMARY.md</files>
  <action>
1. Write the quick-task SUMMARY at
   `.planning/quick/260718-x7j-ratify-sendmessage-c2-tier-limitation-as/260718-x7j-SUMMARY.md`
   using the summary template. Record: the two doctrine insertions (§3/§11) + the ledger
   entry; that this is a docs-only planning-artifact ratification of an owner ruling; the
   fix-it-twice pairing (doctrine doc + ledger in one commit); and a NOTICING section — if
   you spotted any C2/C1 charter template, `.claude/skills/`, `.claude/agents/`, or
   coordinator-dispatch text that should also carry this caveat, list it there as a
   deliberately-out-of-scope follow-up (do NOT edit those files; the promotion-sweep is out
   of scope for this quick).

2. Stage EXACTLY the four artifacts — the two edited files plus this quick's own
   PLAN/SUMMARY — and commit in ONE commit (the fix-it-twice pairing must land together):

   cd /home/reuben/workspace/reposix
   git add .planning/ORCHESTRATION.md .planning/CONSULT-DECISIONS.md \
     .planning/quick/260718-x7j-ratify-sendmessage-c2-tier-limitation-as/260718-x7j-PLAN.md \
     .planning/quick/260718-x7j-ratify-sendmessage-c2-tier-limitation-as/260718-x7j-SUMMARY.md

   Verify nothing else is staged: `git status --short` must show ONLY those four paths.

   Commit message (subject exactly as below; body one line of context, then the trailer):

     docs(orchestration): ratify SendMessage C2-tier limitation as standing doctrine (owner ruling) — ORCHESTRATION §3/§11 + CONSULT-DECISIONS ledger

     Owner-ratified 2026-07-18 after independent re-discovery across the #63→#64→#65
     handover chain: SendMessage is a tool-grant limitation of the phase-coordinator
     registry entry (C2→child and child→C2 both fail; L0→C2 works). Standing mitigation:
     serialize strictly, drive phase close via fresh verifier→executor LEAVES only.

     Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>

3. Push and verify the push landed:

   git push origin main
   git rev-parse HEAD origin/main   # the two SHAs MUST be identical

   The pre-push hook (~90-120s, whole-repo cost — includes the cargo/kcov suite even for a
   docs-only change) runs on push; that is expected. If it BLOCKS, read the named violated
   invariant, fix within this quick's scope (only the two doctrine files), and re-push. Do
   NOT bypass the hook. cargo, if the hook triggers any, is FOREGROUND-only — never
   backgrounded/detached.
  </action>
  <verify>
    <automated>cd /home/reuben/workspace/reposix && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" && git log -1 --pretty=%s | grep -q 'ratify SendMessage C2-tier limitation as standing doctrine' && git log -1 --pretty=%B | grep -q 'Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>' && echo PUSHED_AND_MATCHED</automated>
  </verify>
  <done>
SUMMARY.md exists with the noticing section; a single commit carries the two doctrine files
+ PLAN + SUMMARY with the mandated subject and Co-Authored-By trailer; `git push origin
main` landed; `git rev-parse HEAD origin/main` returns two identical SHAs; the verify
command prints PUSHED_AND_MATCHED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Docs-only planning-artifact edit — no code path, no network egress, no untrusted-input handling, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-x7j-01 | Tampering | `.planning/ORCHESTRATION.md`, `.planning/CONSULT-DECISIONS.md` | accept | Planning-doc prose only; change is reviewable in one commit and fully reversible via `git revert`. No runtime/security posture touched. |
</threat_model>

<verification>
- ORCHESTRATION.md §3 AND §11 each carry the SendMessage tier-limitation caveat, in context.
- The caveat preserves all load-bearing facts: registry-grant root cause, C2→child /
  child→C2 failure, L0→C2 works, strict-serialize + fresh-LEAVES mitigation, RATIFIED
  standing (not a workaround), embed-verbatim-in-every-C2/C1-charter.
- CONSULT-DECISIONS.md carries the 2026-07-18 [OWNER] entry with finding, re-discovery
  provenance (#63→#64→#65, 2nd/3rd this milestone), ruling, mitigation, and the §3/§11
  cross-reference.
- ORCHESTRATION doctrine doc + CONSULT-DECISIONS ledger land in ONE commit (fix-it-twice).
- Push landed: `git rev-parse HEAD origin/main` returns two identical SHAs.
</verification>

<success_criteria>
- `grep -c 'SendMessage tier limitation' .planning/ORCHESTRATION.md` ≥ 2.
- The new ledger entry is the last `## ` entry in CONSULT-DECISIONS.md and is tagged
  `[OWNER]` dated 2026-07-18.
- Exactly four paths in the ratification commit (two doctrine files + PLAN + SUMMARY).
- Commit subject starts with the mandated string; body ends with the Co-Authored-By trailer.
- `HEAD` == `origin/main` after push.
</success_criteria>

<output>
After completion, create
`.planning/quick/260718-x7j-ratify-sendmessage-c2-tier-limitation-as/260718-x7j-SUMMARY.md`
(written and committed in Task 2).
</output>
