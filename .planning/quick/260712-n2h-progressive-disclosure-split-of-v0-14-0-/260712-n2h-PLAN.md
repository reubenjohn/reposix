---
phase: quick-260712-n2h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.14.0-phases/ROADMAP.md
  - .planning/milestones/v0.14.0-phases/ARCHIVE.md
autonomous: true
requirements:
  - QUICK-260712-N2H-ROADMAP-SPLIT
user_setup: []

must_haves:
  truths:
    - "p111-milestone-hygiene.sh exits 0 after the split (asserts A + B still green)"
    - "ROADMAP.md keeps every `### Phase N:` heading for phases 102-113 (all 12)"
    - "ROADMAP.md keeps the `**STATUS: ...**` terminal line inside each of Phase 103..109"
    - "ROADMAP.md still references Phase 113 (assert B)"
    - "Phase 112 DO-NOT-START stub block is preserved byte-for-byte in ROADMAP.md (unchanged)"
    - "Every original ROADMAP.md line survives verbatim across the union of the two output files (zero deletion)"
    - "ROADMAP.md is meaningfully smaller (target < 20000 B) and links to ARCHIVE.md"
    - "structure/no-loose-roadmap-or-requirements and structure/no-orphan-docs still exit 0"
  artifacts:
    - path: ".planning/milestones/v0.14.0-phases/ROADMAP.md"
      provides: "Lean phase index: top-matter + per-phase heading + terminal status + pointer"
      contains: "### Phase 113"
    - path: ".planning/milestones/v0.14.0-phases/ARCHIVE.md"
      provides: "Verbatim relocated per-phase detail bodies (Goal / Success criteria / Context anchor)"
      contains: "### Phase 103"
  key_links:
    - from: ".planning/milestones/v0.14.0-phases/ROADMAP.md"
      to: ".planning/milestones/v0.14.0-phases/ARCHIVE.md"
      via: "relative markdown link (top-level pointer + per-phase pointer lines)"
      pattern: "ARCHIVE\\.md"
---

<objective>
Progressive-disclosure split of `.planning/milestones/v0.14.0-phases/ROADMAP.md`
(currently 37764 B). v0.14.0 is a CLOSED milestone awaiting the owner tag. Relocate the
verbose completed-phase narrative into a NEW sibling `ARCHIVE.md` in the SAME directory,
leaving ROADMAP.md as a lean phase index: top-matter + every `### Phase N:` heading +
each phase's terminal `**STATUS: ...**` line + a one-line pointer to the archived detail.

Purpose: Shrink ROADMAP.md (target < 20000 B) for readability WITHOUT deleting any
information and WITHOUT breaking the `p111-milestone-hygiene.sh` gate that reads this
ROADMAP. This is pure relocation (verbatim), not summarization.

Output: A smaller `ROADMAP.md` + a new `ARCHIVE.md` holding the relocated bodies verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v0.14.0-phases/ROADMAP.md
@.planning/milestones/v0.8.0-phases/ARCHIVE.md
@quality/gates/agent-ux/p111-milestone-hygiene.sh

<interfaces>
<!-- Load-bearing facts the executor MUST honor. No codebase exploration needed. -->

STOP-LEVEL CONSTRAINTS (from the requesting orchestrator):
1. The gate `quality/gates/agent-ux/p111-milestone-hygiene.sh` READS this ROADMAP.md and
   has two asserts your split MUST keep green:
   - ASSERT A: for EACH of Phase 103,104,105,106,107,108,109 the block from
     `^### Phase N:` up to the next `^### Phase ` / `^## ` MUST contain a bolded terminal
     status line matching `\*\*(STATUS|Status)[:*].*(CLOSED|COMPLETE|SHIPPED|DONE|RATIFIED)`.
     => KEEP each `### Phase 103:` .. `### Phase 109:` heading AND its `**STATUS: ...**`
     paragraph in ROADMAP.md. Move ONLY the body below the status paragraph to ARCHIVE.md.
   - ASSERT B: ROADMAP.md must still contain a `Phase 113` / `P113` reference.
     => KEEP the `### Phase 113:` heading in ROADMAP.md.
2. DO NOT ALTER owner-gated content: Phase 112 "OD-4 launch-readiness SCOPE-BUT-DO-NOT-START
   stub" MUST keep its DO-NOT-START status and scope-stub content intact. SAFEST + REQUIRED
   here: keep Phase 112's ENTIRE block (lines 490-529, 2628 B) fully in ROADMAP.md UNCHANGED
   — do NOT archive any of it, do NOT reword it, do NOT "resolve" it.
3. ARCHIVE FILE LOCATION is EXACTLY `.planning/milestones/v0.14.0-phases/ARCHIVE.md`
   (SAME dir — matches the v0.8.0-phases/ARCHIVE.md convention). This path contains
   "phases"/"archive" so the `no-loose-roadmap-or-requirements` invariant does NOT flag it.
   Do NOT name it `ROADMAP-ARCHIVE.md`, do NOT put `ROADMAP` in the name, and do NOT place
   it at the milestones top level.
4. NO information deletion. Every archived phase-detail paragraph goes to ARCHIVE.md
   VERBATIM (byte-for-byte). ROADMAP.md keeps headings + status paragraphs + pointers.
5. Touch ONLY these two files. Never touch: quality/catalogs/code.json,
   .planning/phases/21-*, .planning/phases/22-*, scripts/demos, scripts/dev,
   quality/reports/verifications/docs-repro (foreign-work list).
6. DOC-ONLY: no cargo, no build, no test-suite. Do NOT `git push` (push is deferred to the
   normal phase-close cadence / owner). Committing the two touched files is expected
   ("uncommitted = didn't happen"), push is NOT.

VERIFIED FILE STRUCTURE (from a full read of the current ROADMAP.md, 571 lines / 37764 B):

- Top-matter = lines 1-66 (intro / thesis / ordering / recurring success criteria + a
  trailing `---`). STAYS in ROADMAP.md unchanged. First `### Phase ` heading is at line 67.
- Each phase block runs from its `### Phase N:` heading to the line before the next
  `### Phase ` heading (or EOF for 113).
- Within EVERY archived phase block there is exactly ONE unique body-start line:
  `^\*\*Goal:` (phases 102-111) OR `^\*\*Renumber rationale` (phase 113 has no Goal).
  This is the split boundary. `grep -nE '^\*\*Goal:|^\*\*Renumber rationale|^### Phase '`
  confirms one boundary per block. (Phase 112 also has a `**Goal:**`, but 112 is kept
  WHOLE so its boundary is irrelevant.)

PER-PHASE PLAN (boundary = first `**Goal:**` / `**Renumber rationale` after the heading):

  | Phase | Heading line | Body starts at | Retained head bytes | Disposition            |
  |-------|--------------|----------------|---------------------|------------------------|
  | 102   | 67           | 69  (Goal)     | 76  (heading only)  | archive body           |
  | 103   | 122          | 128 (Goal)     | 313 (head+STATUS)   | archive body           |
  | 104   | 163          | 169 (Goal)     | 316 (head+STATUS)   | archive body           |
  | 105   | 204          | 210 (Goal)     | 296 (head+STATUS)   | archive body           |
  | 106   | 244          | 250 (Goal)     | 357 (head+STATUS)   | archive body           |
  | 107   | 294          | 302 (Goal)     | 484 (head+STATUS)   | archive body           |
  | 108   | 329          | 349 (Goal)     | 1532 (head+STATUS*) | archive body           |
  | 109   | 385          | 392 (Goal)     | 436 (head+STATUS)   | archive body           |
  | 110   | 432          | 434 (Goal)     | 70  (heading only)  | archive body           |
  | 111   | 453          | 455 (Goal)     | 88  (heading only)  | archive body           |
  | 112   | 490          | —              | 2628 (whole block)  | KEEP WHOLE, unchanged  |
  | 113   | 530          | 535 (Renumber) | 207 (head+STATUS)   | archive body           |

  * Phase 108's "head" includes its long multi-line STATUS paragraph which carries the
    owner "DEFERRED-TO-v0.15.0" scope call (lines 331-343). Keep the WHOLE status paragraph
    (heading -> line before `**Goal:**`) in ROADMAP.md — it is terminal status, not verbose
    narrative. Phases 102, 110, 111 have NO status line in the original (the gate does not
    require one for them) — keep heading only; do NOT invent a status line for them.

  Retained (top-matter + heads + phase-112 whole) = 11382 B. After adding ~12 pointer lines
  + a top-level ARCHIVE pointer, ROADMAP.md lands ~12-13 KB (well under the 20 KB target).

EXACT PHASE TITLES (for ARCHIVE.md anchor headings + ROADMAP pointer text):
  102: D2 self-safe dark-factory hardening (HARD SERIALIZING GATE)
  103: Early cheap wins — doc-alignment grade/persist split + structure waiver OP-8 split
  104: GitHub-v09 helper-path 404 fix (S-260707-gh404)
  105: RBF-LR-03 rebase-recovery reconciliation redesign
  106: Waived tutorials reproduce (docs-repro/tutorial-replay + examples 01/02/04/05)
  107: RUSTSEC memmap2 + quinn-proto advisories in `Cargo.lock`
  108: `prune_oid_map` pagination-truncation data-loss hazard
  109: Carried RBF-FW-11 + quality-convergence write-contention
  110: +2 reservation Slot 1 — surprises absorption (OP-8)
  111: +2 reservation Slot 2 — good-to-haves polish + milestone close (OP-9)
  113: Lost-update shared-cursor guard (OUT-OF-BAND — shipped in v0.14.0)

BASELINE (confirmed GREEN before the split — the split must keep all three at exit 0):
  bash quality/gates/agent-ux/p111-milestone-hygiene.sh                                  -> PASS / exit 0
  python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-roadmap-or-requirements -> exit 0
  python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-orphan-docs                    -> exit 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scripted verbatim split — generate ARCHIVE.md + lean ROADMAP.md</name>
  <files>.planning/milestones/v0.14.0-phases/ARCHIVE.md, .planning/milestones/v0.14.0-phases/ROADMAP.md</files>
  <action>
Do NOT hand-edit line ranges (whitespace drift risk). Write a deterministic generator
script to the scratchpad dir (e.g. `<scratchpad>/split_roadmap.py` — do NOT commit the
script; it is a one-shot tool, not a `scripts/` artifact) that reads the CURRENT
working-tree `.planning/milestones/v0.14.0-phases/ROADMAP.md` and emits both output files
by pure relocation. Algorithm (verbatim, no rewording anywhere):

1. Read all lines of ROADMAP.md.
2. top_matter = every line before the first line matching `^### Phase ` (lines 1-66).
   Emit top_matter unchanged to the new ROADMAP.
3. Split the remainder into per-phase blocks: a block runs from a `^### Phase N:` line up
   to the line before the next `^### Phase ` line (last block runs to EOF).
4. For each block, parse N from the heading.
   - If N == 112: append the block VERBATIM to the new ROADMAP. Do NOT archive it. (Owner
     DO-NOT-START stub — untouched.)
   - Else (N in 102,103,104,105,106,107,108,109,110,111,113):
       boundary = index of the first line in the block matching `^\*\*Goal:` OR
                  `^\*\*Renumber rationale` (exactly one exists per block).
       head = block[:boundary]   # heading + blank + STATUS paragraph (verbatim)
       body = block[boundary:]   # Goal/Renumber .. end of block (verbatim)
       NEW ROADMAP gets: head  +  one pointer line  +  a trailing blank line.
       ARCHIVE gets:     the heading line (block[0])  +  a blank line  +  body (verbatim).
5. Pointer line format (single line, ends with a period so it renders as its own line):
   - phases 102-111:
     `> Detail (Goal / Requirements / Success criteria / Context anchor) archived → [ARCHIVE.md § Phase N](./ARCHIVE.md).`
   - phase 113 (no Goal):
     `> Detail (renumber rationale / what shipped / regression test / catalog row) archived → [ARCHIVE.md § Phase 113](./ARCHIVE.md).`
   (Bare `./ARCHIVE.md` link — .planning docs are not mkdocs-rendered, so a file link is
   sufficient and cannot break an anchor. The `§ Phase N` is human wayfinding.)
6. ARCHIVE.md file header (prepend, before the first archived phase heading) — mirror the
   v0.8.0-phases/ARCHIVE.md convention (title + status/shipped line + a back-pointer), e.g.:

     # v0.14.0 — Wave-2 hardening: archived phase detail

     **Status:** v0.14.0 CLOSED (awaiting owner tag). This file holds the VERBATIM
     relocated per-phase detail (Goal / Requirements / Success criteria / Context anchor,
     or narrative for Phase 113) for phases 102-111 + 113. It was split out of
     [ROADMAP.md](./ROADMAP.md) for progressive disclosure — ROADMAP.md retains the phase
     index (headings + terminal status + pointers back here). Phase 112 (the DO-NOT-START
     OD-4 stub) is kept whole in ROADMAP.md, not here. No content was summarized away;
     this is pure relocation.

     ---

7. Also add ONE top-level pointer to the NEW ROADMAP so the ROADMAP→ARCHIVE link is
   unmistakable: insert immediately AFTER the top-matter (i.e. right before the
   `### Phase 102:` heading) a blockquote:

     > **Archived detail:** completed-phase bodies (Goal / Success criteria / Context
     > anchor for P102-P111, P113) live in [ARCHIVE.md](./ARCHIVE.md); this ROADMAP keeps
     > the phase index (headings + terminal status + pointers). Phase 112's DO-NOT-START
     > stub is kept in full below.

Run the script (writes both files). This mechanical approach guarantees byte-for-byte
verbatim relocation of every body and every retained head.

Constraints recap: touch ONLY these two files; no cargo/build/test; no push; keep Phase
112 byte-identical; keep each Phase 103-109 `**STATUS:**` paragraph and every `### Phase N:`
heading (102-113) in ROADMAP.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v0.14.0-phases/ARCHIVE.md && [ "$(grep -c '^### Phase ' .planning/milestones/v0.14.0-phases/ROADMAP.md)" = "12" ] && echo OK</automated>
  </verify>
  <done>
ARCHIVE.md exists; new ROADMAP.md still has all 12 `### Phase ` headings (102-113); Phase
112 block unchanged; bodies relocated verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verification battery + commit (no push)</name>
  <files>.planning/milestones/v0.14.0-phases/ROADMAP.md, .planning/milestones/v0.14.0-phases/ARCHIVE.md</files>
  <action>
Run the full verification battery from the repo root and confirm EACH result. If any check
fails, fix the split (re-run the Task 1 generator with the correction) until all pass — do
NOT commit a red state. `SC=<scratchpad>` denotes the real absolute scratchpad path.

(a) GATE — asserts A + B still green (REQUIRED, authoritative):
    bash quality/gates/agent-ux/p111-milestone-hygiene.sh ; echo "exit=$?"
    => must print PASS and exit=0.

(b) STRUCTURE rows — no new RED:
    python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-roadmap-or-requirements ; echo "exit=$?"
    python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-orphan-docs ; echo "exit=$?"
    => both exit=0.

(c) SIZE — ROADMAP meaningfully smaller, both files reported:
    wc -c .planning/milestones/v0.14.0-phases/ROADMAP.md .planning/milestones/v0.14.0-phases/ARCHIVE.md
    => ROADMAP.md < 20000 B (target ~12-13 KB); ARCHIVE.md ~25-28 KB.

(d) LINK — ROADMAP links to ARCHIVE:
    grep -c 'ARCHIVE\.md' .planning/milestones/v0.14.0-phases/ROADMAP.md
    => >= 1 (expect ~13: top-level pointer + per-phase pointers).

(e) ZERO-DELETION (line accounting — proves pure relocation, nothing lost):
    Compare the ORIGINAL committed ROADMAP against the UNION of the two new files. Every
    original line must survive verbatim somewhere in the union (the only ADDED lines are
    pointer lines, the ARCHIVE header, and duplicated `### Phase N:` anchor headings —
    never removed lines):
      git show HEAD:.planning/milestones/v0.14.0-phases/ROADMAP.md > "$SC/orig.md"
      cat .planning/milestones/v0.14.0-phases/ROADMAP.md .planning/milestones/v0.14.0-phases/ARCHIVE.md > "$SC/union.md"
      comm -23 <(sort -u "$SC/orig.md") <(sort -u "$SC/union.md")
    => MUST print nothing (empty). Any output line is an original line missing from the
       union = content lost = FAIL.

(f) BODY-DRAIN sanity — the new ROADMAP retains no relocated body:
    grep -cE '^\*\*Goal:|^\*\*Renumber rationale' .planning/milestones/v0.14.0-phases/ROADMAP.md
    => exactly 1 (only Phase 112's `**Goal:**`, which is kept whole; zero `**Renumber` lines).

(g) PHASE-112 UNTOUCHED — the owner stub is byte-identical to the original:
    diff <(git show HEAD:.planning/milestones/v0.14.0-phases/ROADMAP.md | awk '/^### Phase 112:/{c=1} c&&/^### Phase 113:/{exit} c') \
         <(awk '/^### Phase 112:/{c=1} c&&/^### Phase 113:/{exit} c' .planning/milestones/v0.14.0-phases/ROADMAP.md)
    => empty diff (Phase 112 block unchanged).

After ALL checks pass, commit ONLY the two touched files (no push):
    git add .planning/milestones/v0.14.0-phases/ROADMAP.md .planning/milestones/v0.14.0-phases/ARCHIVE.md
    git commit -m "docs(v0.14.0): progressive-disclosure split — archive completed-phase detail into ARCHIVE.md

Relocate verbose P102-P111 + P113 phase bodies (Goal / Success criteria / Context
anchor) into a sibling ARCHIVE.md; ROADMAP.md keeps the phase index (headings +
terminal STATUS + pointers). Phase 112 DO-NOT-START stub kept whole. Pure relocation,
no content deleted. p111-milestone-hygiene + structure invariants stay GREEN."
Do NOT run `git push` — push is deferred to the normal phase-close cadence / owner.
  </action>
  <verify>
    <automated>bash quality/gates/agent-ux/p111-milestone-hygiene.sh && python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-roadmap-or-requirements && python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-orphan-docs && [ "$(wc -c < .planning/milestones/v0.14.0-phases/ROADMAP.md)" -lt 20000 ] && grep -q 'ARCHIVE\.md' .planning/milestones/v0.14.0-phases/ROADMAP.md && echo ALL_GREEN</automated>
  </verify>
  <done>
p111 gate exits 0 (asserts A + B green); both structure rows exit 0; ROADMAP.md < 20000 B
and links to ARCHIVE.md; zero-deletion sweep empty; Phase 112 byte-identical; two files
committed (no push).
  </done>
</task>

</tasks>

<verification>
Phase-level checks (all run in Task 2, all must hold before commit):
- `bash quality/gates/agent-ux/p111-milestone-hygiene.sh` exits 0 (asserts A: Phase
  103-109 terminal STATUS lines present; B: Phase 113 referenced).
- `python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-roadmap-or-requirements` exits 0.
- `python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-orphan-docs` exits 0.
- Zero-deletion `comm -23` sweep prints nothing (every original line preserved verbatim in
  the union of the two output files).
- Phase 112 block byte-identical to the pre-split original (empty diff).
- `wc -c` on both files: ROADMAP.md < 20000 B (down from 37764 B); ARCHIVE.md holds the
  relocated bodies. ARCHIVE.md exists and ROADMAP.md links to it.
</verification>

<success_criteria>
- ROADMAP.md is a lean phase index: top-matter intact; every `### Phase N:` heading
  (102-113) retained; Phases 103-109 each keep their terminal `**STATUS: ...**` line;
  Phase 112's DO-NOT-START stub kept whole and unchanged; each archived phase carries a
  one-line pointer to ARCHIVE.md.
- ARCHIVE.md (in the SAME `v0.14.0-phases/` dir, named exactly `ARCHIVE.md`) holds the
  verbatim relocated bodies of Phases 102-111 + 113 under matching `### Phase N:` headings.
- p111 hygiene gate + both structure freshness rows exit 0; zero content loss proven.
- ROADMAP.md < 20000 B. Only the two files_modified changed; no foreign paths touched; no
  push, no cargo, no build.
</success_criteria>

<output>
After completion, create
`.planning/quick/260712-n2h-progressive-disclosure-split-of-v0-14-0-/260712-n2h-SUMMARY.md`.

In the SUMMARY, additionally NOTE (for the orchestrator — do NOT act on it here, out of
scope): this index+archive shard convention (applied here to a CLOSED milestone) is exactly
the "shard convention" whose absence the `structure/file-size-limits` waiver note in
`quality/catalogs/freshness-invariants.json` cites as the reason milestone ROADMAP/REQUIREMENTS
splitting was deferred. The orchestrator may later decide whether to update that deferral note
and/or apply the same split to other closed-milestone ROADMAPs. This quick does NOT edit that
catalog waiver.
</output>
