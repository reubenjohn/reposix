---
quick_id: 260715-h1d
slug: scratch-repo-keep-policy
type: execute
wave: 1
depends_on: []
files_modified: [docs/reference/testing-targets.md]
autonomous: true
requirements: [QUICK-260715-h1d]
must_haves:
  truths:
    - "docs/reference/testing-targets.md documents the KEEP-policy for the reposix-scope-test-DELETEME scratch repo"
    - "The doc tells a future agent to force-push-reset (never delete) and to unarchive via gh api before first reuse"
    - "The new subsection sits inside the GitHub section, after the Cleanup blockquote and before the JIRA divider"
  artifacts:
    - path: "docs/reference/testing-targets.md"
      provides: "Scratch-repo KEEP-policy subsection under the GitHub section"
      contains: "### Scratch repo — `reposix-scope-test-DELETEME` (KEEP-policy)"
  key_links:
    - from: "docs/reference/testing-targets.md GitHub section"
      to: "reposix-scope-test-DELETEME reuse workflow"
      via: "gh api unarchive command"
      pattern: "gh api -X PATCH repos/reubenjohn/reposix-scope-test-DELETEME"
---

<objective>
Record the KEEP-policy for the GitHub scratch repo `reposix-scope-test-DELETEME`
as a new subsection in `docs/reference/testing-targets.md`, so successor agents
reset it via force-push (never delete) and unarchive it before reuse.

Purpose: The repo has drifted into an ARCHIVED state and the reuse contract lives
only in orchestration memory. Committing it to the testing-targets reference makes
the policy durable across sessions. This has been pending 5 rotations — it is a
small, well-scoped docs change.

Output: One new `###` subsection in the target doc, atomically committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/reference/testing-targets.md

<insertion_anchor>
<!-- Verified live 2026-07-15. Line numbers approximate — anchor on TEXT, not line #. -->
Target section: `## GitHub — `reubenjohn/reposix` issues` (~L141).
Structure: `### Env vars` → `### Rate-limit notes` → `### Cleanup` → owner-permission
blockquote (`> "TokenWorld is for testing — go crazy..."` + the parenthetical that it
"applies to `reubenjohn/reposix` issues", ~L169-171) → `---` divider (~L173) → `## JIRA`.

INSERT the new subsection AFTER that closing blockquote and BEFORE the `---` divider.
Do NOT touch the JIRA, Confluence, or any other section.
</insertion_anchor>

<grounding>
<!-- Live `gh api repos/reubenjohn/reposix-scope-test-DELETEME` result, 2026-07-15.
     The executor may cite these directly — do NOT re-hit GitHub. -->
{ "archived": true, "private": true, "pushed_at": "2026-07-14T03:51:16Z",
  "html_url": "https://github.com/reubenjohn/reposix-scope-test-DELETEME" }
</grounding>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the scratch-repo KEEP-policy subsection</name>
  <files>docs/reference/testing-targets.md</files>
  <action>
Insert the following new subsection into `docs/reference/testing-targets.md` AT the
insertion anchor above: after the GitHub `### Cleanup` owner-permission blockquote
(the block ending `> ...they are owner-controlled and safe to mutate during tests.)`)
and BEFORE the `---` divider that precedes `## JIRA`. Match the surrounding doc's tight,
imperative voice and its ~68-char line wrapping. Use the Edit tool with a precise anchor
(the blockquote's closing line + the `---`); do NOT rewrite the surrounding lines.

Exact content to insert (a blank line above and below, so the `---` divider is preserved):

```markdown
### Scratch repo — `reposix-scope-test-DELETEME` (KEEP-policy)

`reposix-scope-test-DELETEME` is a throwaway GitHub scratch repo
(private, under `reubenjohn/`) used for scope/init smoke tests. It is
a git *remote target*, NOT an issues target like the parent repo above.

**KEEP-policy: NEVER delete the repo.** Reset it via force-push
(`git push --force`) instead of deletion, so its URL/identity
(<https://github.com/reubenjohn/reposix-scope-test-DELETEME>) stays
stable across sessions and successor agents don't have to recreate it.

It is currently **ARCHIVED** on GitHub (verified `archived: true`,
`private: true`, last pushed `2026-07-14`). Archived repos reject
pushes. On first reuse, unarchive it via the GitHub API before running
tests:

```bash
gh api -X PATCH repos/reubenjohn/reposix-scope-test-DELETEME -f archived=false
```

Leave it unarchived for the session, or re-archive when done, per
session norms.
```

NOTE — the inner code fence above is a nested ```bash block. When you write it into the
Markdown file, it must render as a normal fenced code block inside the subsection (the
outer ```markdown fence here is only for this plan's readability; do NOT copy the outer
fence into the doc).

NOTICING (eager-fix candidate — same file, same GitHub section, <1h, no new dependency):
The `### Cleanup` subsection (~L166) claims "The Phase 36 cleanup automation will handle
this". The project is at v0.15.0 / P115 — Phase 36 is long past and no such automation
shipped, so this is a stale forward-reference (a lying-doc claim). If you confirm no
Phase 36 cleanup automation exists, eager-fix it in this SAME commit: replace the stale
promise with the present-tense manual-cleanup reality (e.g. drop the "Phase 36 cleanup
automation will handle this; for now" clause, keeping the manual-cleanup instruction and
its URL). If you cannot confirm within a couple minutes, leave it and flag it in the
SUMMARY instead — do not expand scope chasing it.
  </action>
  <verify>
    <automated>test "$(grep -c '^### Scratch repo — `reposix-scope-test-DELETEME` (KEEP-policy)$' docs/reference/testing-targets.md)" = "1" && test "$(grep -c 'gh api -X PATCH repos/reubenjohn/reposix-scope-test-DELETEME -f archived=false' docs/reference/testing-targets.md)" = "1" && git diff --name-only | grep -qx 'docs/reference/testing-targets.md' && test "$(git diff --name-only | wc -l)" = "1" && bash quality/gates/docs-build/mkdocs-strict.sh</automated>
  </verify>
  <done>
The new `### Scratch repo — ...` subsection exists exactly once, sits between the GitHub
Cleanup blockquote and the JIRA `---` divider, contains the `gh api ... archived=false`
unarchive command and the NEVER-delete/force-push-reset policy, and the mkdocs-strict
build passes. `git diff` touches ONLY `docs/reference/testing-targets.md` — the JIRA and
Confluence sections are byte-for-byte unchanged.
  </done>
</task>

</tasks>

<verification>
- `grep -A6 '^### Scratch repo' docs/reference/testing-targets.md` shows the KEEP-policy
  prose in the doc's imperative voice, and the `---`/`## JIRA` divider still follows it.
- `git diff docs/reference/testing-targets.md` is a pure addition (plus, optionally, the
  one-line Phase-36 noticing eager-fix) — no unrelated churn, no reflow of adjacent lines.
</verification>

<success_criteria>
- KEEP-policy subsection present, correctly placed, mkdocs-strict green.
- Only `docs/reference/testing-targets.md` staged and committed.
</success_criteria>

<execution_notes>
- STAGE TARGETED: `git add docs/reference/testing-targets.md` ONLY. NEVER `git add -A`/`git add .`
  (shared working tree — other agents' changes may be uncommitted).
- Commit ONLY the docs work product, atomically. Do NOT commit any `.planning/` artifacts
  (the orchestrator owns PLAN.md / SUMMARY.md / STATE.md).
- No `--no-verify` — pre-commit / pre-push hooks must run clean.
- Suggested commit message:
  `docs(testing-targets): record KEEP-policy for reposix-scope-test-DELETEME scratch repo`
</execution_notes>

<output>
After completion, create
`.planning/quick/260715-h1d-scratch-repo-keep-policy/260715-h1d-SUMMARY.md`.
</output>
