# SESSION-END-STATE — current contract

Auto-generated by `scripts/end-state.py init`. **Do not hand-edit;** run `init` to regenerate. The session is GREEN only when every claim below has status `PASS` per `scripts/end-state.py verdict`.

- Session ID: `5468ec00-693b-45ce-bfbd-b145d6382b9b`
- Session started at: `2026-04-27T05:16:54Z`
- Workspace version: `0.11.1`
- Schema version: `1`

## Claims

### ci-status

- **`ci/main-green-on-latest-completed`** — Most recent COMPLETED CI workflow run on origin/main must have conclusion=success. §3a.
  - verifier (shell): `test "$(gh run list --branch main --workflow CI --status completed --limit 1 --json conclusion --jq '.[0].conclusion')" = success`
  - status: `PASS`

### crate-publish

- **`crates-io/reposix-core-at-workspace-version`** — crates.io max_version of reposix-core must equal workspace version (0.11.1). §3b.
  - verifier (shell): `v=$(curl -s https://crates.io/api/v1/crates/reposix-core | grep -o '"max_version":"[^"]*"' | head -1 | sed 's/.*://;s/"//g'); test "$v" = "0.11.1"`
  - artifact: `.planning/verifications/crates-io/reposix-core.json`
  - status: `PASS`
- **`crates-io/reposix-cache-at-workspace-version`** — crates.io max_version of reposix-cache must equal workspace version (0.11.1). §3b.
  - verifier (shell): `v=$(curl -s https://crates.io/api/v1/crates/reposix-cache | grep -o '"max_version":"[^"]*"' | head -1 | sed 's/.*://;s/"//g'); test "$v" = "0.11.1"`
  - artifact: `.planning/verifications/crates-io/reposix-cache.json`
  - status: `PASS`
- **`crates-io/reposix-sim-at-workspace-version`** — crates.io max_version of reposix-sim must equal workspace version (0.11.1). §3b.
  - verifier (shell): `v=$(curl -s https://crates.io/api/v1/crates/reposix-sim | grep -o '"max_version":"[^"]*"' | head -1 | sed 's/.*://;s/"//g'); test "$v" = "0.11.1"`
  - artifact: `.planning/verifications/crates-io/reposix-sim.json`
  - status: `PASS`
- **`crates-io/reposix-github-at-workspace-version`** — crates.io max_version of reposix-github must equal workspace version (0.11.1). §3b.
  - verifier (shell): `v=$(curl -s https://crates.io/api/v1/crates/reposix-github | grep -o '"max_version":"[^"]*"' | head -1 | sed 's/.*://;s/"//g'); test "$v" = "0.11.1"`
  - artifact: `.planning/verifications/crates-io/reposix-github.json`
  - status: `PASS`
- **`crates-io/reposix-confluence-at-workspace-version`** — crates.io max_version of reposix-confluence must equal workspace version (0.11.1). §3b.
  - verifier (shell): `v=$(curl -s https://crates.io/api/v1/crates/reposix-confluence | grep -o '"max_version":"[^"]*"' | head -1 | sed 's/.*://;s/"//g'); test "$v" = "0.11.1"`
  - artifact: `.planning/verifications/crates-io/reposix-confluence.json`
  - status: `PASS`
- **`crates-io/reposix-jira-at-workspace-version`** — crates.io max_version of reposix-jira must equal workspace version (0.11.1). §3b.
  - verifier (shell): `v=$(curl -s https://crates.io/api/v1/crates/reposix-jira | grep -o '"max_version":"[^"]*"' | head -1 | sed 's/.*://;s/"//g'); test "$v" = "0.11.1"`
  - artifact: `.planning/verifications/crates-io/reposix-jira.json`
  - status: `PASS`
- **`crates-io/reposix-remote-at-workspace-version`** — crates.io max_version of reposix-remote must equal workspace version (0.11.1). §3b.
  - verifier (shell): `v=$(curl -s https://crates.io/api/v1/crates/reposix-remote | grep -o '"max_version":"[^"]*"' | head -1 | sed 's/.*://;s/"//g'); test "$v" = "0.11.1"`
  - artifact: `.planning/verifications/crates-io/reposix-remote.json`
  - status: `PASS`
- **`crates-io/reposix-cli-at-workspace-version`** — crates.io max_version of reposix-cli must equal workspace version (0.11.1). §3b.
  - verifier (shell): `v=$(curl -s https://crates.io/api/v1/crates/reposix-cli | grep -o '"max_version":"[^"]*"' | head -1 | sed 's/.*://;s/"//g'); test "$v" = "0.11.1"`
  - artifact: `.planning/verifications/crates-io/reposix-cli.json`
  - status: `PASS`

### doc-page-with-mermaid

- **`mermaid-renders/how-it-works/filesystem-layer`** — docs/how-it-works/filesystem-layer.md — playwright walk on a cache-cold navigation must show every <pre.mermaid> block has at least one <svg> child AND zero render-error console messages. §0.1 + §0.1.b.
  - verifier (artifact-json-exists): `python3 -c "import json,sys,pathlib; p=pathlib.Path('.planning/verifications/playwright/how-it-works/filesystem-layer.json'); sys.exit(2) if not p.exists() else None; d=json.loads(p.read_text()); mc=d.get('mermaid_count',0); sv=d.get('svg_counts',[]); ce=d.get('console_errors',[]); sys.exit(0 if (mc==0 or (len(sv)==mc and all(x>0 for x in sv))) and not ce else 1)"`
  - artifact: `.planning/verifications/playwright/how-it-works/filesystem-layer.json`
  - status: `PASS`
- **`mermaid-renders/how-it-works/git-layer`** — docs/how-it-works/git-layer.md — playwright walk on a cache-cold navigation must show every <pre.mermaid> block has at least one <svg> child AND zero render-error console messages. §0.1 + §0.1.b.
  - verifier (artifact-json-exists): `python3 -c "import json,sys,pathlib; p=pathlib.Path('.planning/verifications/playwright/how-it-works/git-layer.json'); sys.exit(2) if not p.exists() else None; d=json.loads(p.read_text()); mc=d.get('mermaid_count',0); sv=d.get('svg_counts',[]); ce=d.get('console_errors',[]); sys.exit(0 if (mc==0 or (len(sv)==mc and all(x>0 for x in sv))) and not ce else 1)"`
  - artifact: `.planning/verifications/playwright/how-it-works/git-layer.json`
  - status: `PASS`
- **`mermaid-renders/how-it-works/trust-model`** — docs/how-it-works/trust-model.md — playwright walk on a cache-cold navigation must show every <pre.mermaid> block has at least one <svg> child AND zero render-error console messages. §0.1 + §0.1.b.
  - verifier (artifact-json-exists): `python3 -c "import json,sys,pathlib; p=pathlib.Path('.planning/verifications/playwright/how-it-works/trust-model.json'); sys.exit(2) if not p.exists() else None; d=json.loads(p.read_text()); mc=d.get('mermaid_count',0); sv=d.get('svg_counts',[]); ce=d.get('console_errors',[]); sys.exit(0 if (mc==0 or (len(sv)==mc and all(x>0 for x in sv))) and not ce else 1)"`
  - artifact: `.planning/verifications/playwright/how-it-works/trust-model.json`
  - status: `PASS`
- **`mermaid-renders/index`** — docs/index.md — playwright walk on a cache-cold navigation must show every <pre.mermaid> block has at least one <svg> child AND zero render-error console messages. §0.1 + §0.1.b.
  - verifier (artifact-json-exists): `python3 -c "import json,sys,pathlib; p=pathlib.Path('.planning/verifications/playwright/index.json'); sys.exit(2) if not p.exists() else None; d=json.loads(p.read_text()); mc=d.get('mermaid_count',0); sv=d.get('svg_counts',[]); ce=d.get('console_errors',[]); sys.exit(0 if (mc==0 or (len(sv)==mc and all(x>0 for x in sv))) and not ce else 1)"`
  - artifact: `.planning/verifications/playwright/index.json`
  - status: `PASS`
- **`mermaid-renders/reference/git-remote`** — docs/reference/git-remote.md — playwright walk on a cache-cold navigation must show every <pre.mermaid> block has at least one <svg> child AND zero render-error console messages. §0.1 + §0.1.b.
  - verifier (artifact-json-exists): `python3 -c "import json,sys,pathlib; p=pathlib.Path('.planning/verifications/playwright/reference/git-remote.json'); sys.exit(2) if not p.exists() else None; d=json.loads(p.read_text()); mc=d.get('mermaid_count',0); sv=d.get('svg_counts',[]); ce=d.get('console_errors',[]); sys.exit(0 if (mc==0 or (len(sv)==mc and all(x>0 for x in sv))) and not ce else 1)"`
  - artifact: `.planning/verifications/playwright/reference/git-remote.json`
  - status: `PASS`

### freshness-invariant

- **`freshness/no-version-pinned-filenames`** — No version-pinned filenames (vN.N.N) outside CHANGELOG and .planning/milestones/v*-phases/. Catches §0.3-class drift.
  - verifier (command-empty-output): `find docs scripts -type f | grep -E 'v[0-9]+\.[0-9]+\.[0-9]+' | grep -v CHANGELOG || true`
  - status: `FAIL`
- **`freshness/install-leads-with-pkg-mgr/docs-index`** — docs/index.md hero must show a package-manager install command (brew/binstall/curl|sh/PowerShell-irm) BEFORE any 'git clone https://' or 'cargo build --release' source-compile snippet. Catches §0.2 drift. Bare prose 'git clone' (e.g. 'agent can git clone') does NOT trip the check — only URL-form source builds.
  - verifier (shell): `python3 -c "import re,sys; t=open('docs/index.md').read(); pm=re.search(r'(?im)(?:brew install|cargo binstall|curl[^\n]*\| ?sh|powershell[^\n]*irm)', t); src=[m.start() for m in re.finditer(r'(?im)git clone https?://|cargo build --release', t)]; first_src=min(src) if src else None; sys.exit(0 if pm and (first_src is None or pm.start() < first_src) else 1)"`
  - status: `FAIL`
- **`freshness/install-leads-with-pkg-mgr/README`** — README.md must show a package-manager install command BEFORE any 'git clone https://' or 'cargo build --release' source-compile snippet. §0.2.
  - verifier (shell): `python3 -c "import re,sys; t=open('README.md').read(); pm=re.search(r'(?im)(?:brew install|cargo binstall|curl[^\n]*\| ?sh|powershell[^\n]*irm)', t); src=[m.start() for m in re.finditer(r'(?im)git clone https?://|cargo build --release', t)]; first_src=min(src) if src else None; sys.exit(0 if pm and (first_src is None or pm.start() < first_src) else 1)"`
  - status: `FAIL`
- **`freshness/benchmarks-in-mkdocs-nav`** — Every docs/benchmarks/*.md must appear in mkdocs.yml nav OR in not_in_nav. No benchmark behind an absolute github URL. §0.4.
  - verifier (shell): `python3 -c "import sys,pathlib,re; y=open('mkdocs.yml').read(); missing=[p.name for p in pathlib.Path('docs/benchmarks').glob('*.md') if p.name not in y]; print('\n'.join(missing)); sys.exit(1 if missing else 0)"`
  - status: `PASS`
- **`freshness/no-loose-roadmap-or-requirements`** — No loose v*ROADMAP*.md / v*REQUIREMENTS*.md outside *phases/ or .planning/archive/. §0.5.
  - verifier (command-empty-output): `find .planning/milestones -maxdepth 2 \( -name '*ROADMAP*' -o -name '*REQUIREMENTS*' \) | grep -v phases | grep -v archive || true`
  - status: `FAIL`
- **`freshness/no-orphan-docs`** — Every docs/**/*.md must be in mkdocs.yml nav: OR in not_in_nav: OR a redirect_maps target. mkdocs --strict already enforces this; this row is a tripwire if --strict is ever softened.
  - verifier (shell): `bash scripts/check-docs-site.sh > /dev/null 2>&1`
  - status: `PASS`

---

_Verifier output is written to `.planning/verifications/_logs/<claim-id>.txt`. Artifact files (e.g. playwright JSON, crates-io JSON) are written under the matching `.planning/verifications/<category>/` subdir._
