# v0.11.0 doc-polish backlog

Generated by Phase 44 (`44-AUDIT.md`). Each item is a non-blocking finding from the cold-reader rubric. Promote into a v0.11.0 phase by copying line-items into a `PLAN.md`.

## README.md

- (Major, ESCALATE→Phase 45) v0.7-era Tier 1–5 demo blocks contain dead `reposix mount`/`reposix demo` commands; the body below the v0.9.0 quickstart needs a rewrite (Phase 45 owns).
- (Major) "v0.7.x — pre-FUSE-deletion" Quickstart should move into a `<details>` block.
- (Major) Architecture ASCII diagram still depicts FUSE + kernel VFS; replace with a v0.9.0 promisor-remote diagram.
- (Minor) "Honest scope" paragraph is v0.1–v0.7 era; refresh for v0.9.0.

## docs/index.md

- (Minor) "What it looks like underneath" 8-line paragraph would scan better as bullets.
- (Minor) "CI secret packs" reference lacks context for a cold reader; tighten.

## docs/concepts/mental-model-in-60-seconds.md

- (Minor) Section numbering vs "Three keys" — pre-existing structure is fine; consider explicit "Key 1/2/3" headings.

## docs/concepts/reposix-vs-mcp-and-sdks.md

- (Minor) MCP/SDK latency cells are characterized, not measured — add a row-level footnote marker.

## docs/tutorials/first-run.md

- (Major) Step 1 assumes the reader has cloned the repo; prepend an explicit `git clone … && cd reposix` line to deliver on the "five minutes from clone" promise.
- (Minor) `cargo run -p reposix-cli --` form vs `reposix init` — current callout is acceptable; could simplify.

## docs/how-it-works/filesystem-layer.md

- (Minor) FUSE/`fuser`/`/dev/fuse` mention in "Why partial clone" is Layer-3 permitted; consider trimming for cold-reader speed.
- (Minor) Mermaid diagram is dense (8 nodes); validate mobile rendering with playwright in Phase 45.

## docs/how-it-works/git-layer.md

- (Minor) Sequence diagram preamble is one-line; could expand by a sentence.
- (Minor) `option` capability described as "mostly cosmetic" — could note what flags it does pass.

## docs/how-it-works/trust-model.md

- (Major) Closing "Further reading" bullet to `.planning/research/v0.1-fuse-era/threat-model-and-critique.md` is a planning-tree path; it does not link in MkDocs. Either copy the file into the doc tree (with a "Historical" callout) or drop the reference.

## docs/guides/integrate-with-your-agent.md

- (Minor) Pattern 3 pseudo-code in a `text` block; could be valid Python with `subprocess.run` for copy-paste.

## docs/guides/troubleshooting.md

- (Minor) "Cache eviction" milestone tag (`v0.13.0`) should reconcile with the Future Requirements list.

## docs/guides/write-your-own-connector.md

- (Minor) Step 2 stub references `LinearCreds`/`translate(tainted)` that are not defined inline; acceptable for a sketch.

## docs/reference/simulator.md

- (Minor) `[Operating Principle](../research/agentic-engineering-reference.md)` — anchor missing; Phase 43 deferred anchor warnings.

## docs/reference/testing-targets.md

- (Minor) "Phase 36 wires three CI integration jobs" — Phase 36 shipped; tense polish.

## docs/reference/jira.md

- (Major) Section header "Limitations (Phase 28)" — drop the phase number for cold readers.
- (Minor) Frontmatter example uses ID `10001`; rest of docs use 4-digit IDs (`0001`). Normalize.

## docs/reference/confluence.md

- (Major) "FUSE mount layout (v0.4+)" describes the deleted FUSE tree (`pages/`, `tree/`, `comments/`). Either delete the section or reframe as historical with a callout.
- (Major) "Implements `IssueBackend` trait" — rename to `BackendConnector` (Phase 27 rename).
- (Minor) Demo links point at FUSE-era scripts (`06-mount-real-confluence.sh`); scripts need updating.

## docs/reference/http-api.md

- (Major) Closing paragraph compares helper's 4-digit form to FUSE's 11-digit form; FUSE side no longer exists. Drop the FUSE comparison.
- (Minor) Sim-side `audit_events` vs helper-side `audit_events_cache` cross-link is present but easy to conflate.

## docs/benchmarks/v0.9.0-latency.md

- (Minor) "Phase 36 wires …" — Phase 36 shipped; past tense.
- (Minor) Real-backend cells empty across all backends; clarify the path to fill them in.

## CHANGELOG.md `[v0.9.0]`

- (Minor) `fast-import` jargon — Layer 4 permits it; minor cold-reader trip.
- (Minor) Migration block uses `/tmp/m`; README quickstart uses `/tmp/reposix-demo`. Normalize.

## Self-improving infrastructure (OP-4) — overnight 2026-04-25 surfaced

- [ ] Add `mkdocs build --strict` to pre-push hook — would catch docs failures locally before CI burns 25 min on a red docs run. Surfaced by the gc/tokens push (`709a9ed → 9369276`) where two docs-only failures (banned-words "protocol-v2" + cross-tree relative links) cost 2 push cycles. Cost: requires Python + mkdocs-material in dev env (already needed for serve, just not required by hook today).
