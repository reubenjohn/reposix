---
phase: 59-docs-repro-dimension-agent-ux-thin-home-perf-relocate
plan: 04
type: execute
wave: 4
depends_on:
  - "59-01"
files_modified:
  - quality/gates/agent-ux/dark-factory.sh
  - scripts/dark-factory-test.sh
  - .github/workflows/ci.yml
  - quality/catalogs/agent-ux.json
autonomous: true
requirements:
  - SIMPLIFY-07
  - POLISH-AGENT-UX
must_haves:
  truths:
    - "scripts/dark-factory-test.sh is migrated to quality/gates/agent-ux/dark-factory.sh per SIMPLIFY-07. The new file preserves the existing 133-line script's behavior (sim path; v0.9.0 dark-factory regression invariant: helper stderr-teaching strings on conflict + blob-limit paths). The new file is functionally equivalent to the predecessor; a copy + path adjustments + a header-comment lineage block is the expected shape (NOT a rewrite)."
    - "scripts/dark-factory-test.sh becomes a thin shim (~5-10 lines) that exec's quality/gates/agent-ux/dark-factory.sh with the same args (default 'sim'). Per CLAUDE.md 'Local dev loop' section: `bash scripts/dark-factory-test.sh sim` MUST keep working for backward compatibility (developer muscle memory + any pre-existing CI workflow + the docs at CLAUDE.md). If the audit finds zero callers outside CLAUDE.md + the script itself, deletion is also acceptable -- but the shim is preferred per OP-5 reversibility."
    - ".github/workflows/ci.yml dark-factory job is updated to invoke the new path: `bash quality/gates/agent-ux/dark-factory.sh sim`. (If the workflow currently calls `bash scripts/dark-factory-test.sh sim`, the migration preserves the shim so the CI job keeps working without edit -- BUT we update the CI explicitly per OP-1 close-the-feedback-loop: do not rely on hidden shim chains for CI.)"
    - "quality/catalogs/agent-ux.json's row agent-ux/dark-factory-sim updates from status=NOT-VERIFIED to status=PASS after the runner re-runs the verifier in this wave. Wave A locked the verifier.script path (quality/gates/agent-ux/dark-factory.sh); Wave D ships the file."
    - "POLISH-AGENT-UX broaden-and-deepen: the migrated dark-factory.sh produces no regression vs the v0.9.0 baseline (the helper-teaching-strings invariant). If the sim path now FAILs (e.g. the helper changed and the invariant regressed), Wave D fixes the regression in-phase OR files a v0.12.1 carry-forward via MIGRATE-03. Per the prompt: 'any regression -> fix in same wave (or waive with reason).'"
    - "On Wave D close: cadence pre-pr runner exits 0 (agent-ux/dark-factory-sim row PASS); cadence pre-push runner unaffected (this row is not pre-push); the existing CI dark-factory job invokes the migrated path."
  artifacts:
    - path: "quality/gates/agent-ux/dark-factory.sh"
      provides: "The migrated dark-factory regression script (~133 lines preserved + a 5-15 line header-comment lineage block naming SIMPLIFY-07 + the v0.9.0 invariant). Functionally equivalent to scripts/dark-factory-test.sh sim path: starts a reposix-sim, runs `reposix init sim::demo <path>`, asserts the partial-clone working tree shape, asserts helper stderr emits the teaching strings on conflict + blob-limit paths. After the migration, the script also writes an artifact at quality/reports/verifications/agent-ux/dark-factory-sim.json with asserts_passed/failed populated (a small extension over the predecessor; the predecessor exited 0/1 only). Other backends (github / confluence / jira) are out of scope for this script -- they have ignored cargo tests per the existing dispatch."
      contains: "stderr-teaching"
    - path: "scripts/dark-factory-test.sh"
      provides: "Thin shim (<=10 lines): `exec bash $(dirname $0)/../quality/gates/agent-ux/dark-factory.sh \"$@\"`. Header comment names SIMPLIFY-07 lineage + cross-references CLAUDE.md 'Local dev loop' section. Keep this shim for OP-5 reversibility (developer muscle memory + any unaudited callers)."
      contains: "quality/gates/agent-ux/dark-factory.sh"
      max_lines: 15
    - path: ".github/workflows/ci.yml"
      provides: "Dark-factory job step updated to invoke `bash quality/gates/agent-ux/dark-factory.sh sim` directly (NOT via the shim). Anti-bloat: minimal patch (one line edit); do not refactor adjacent steps."
      contains: "quality/gates/agent-ux/dark-factory.sh"
    - path: "quality/catalogs/agent-ux.json"
      provides: "Row agent-ux/dark-factory-sim status flips NOT-VERIFIED -> PASS after the runner re-runs the verifier in this wave (Wave A locked the contract; Wave D verifies). last_verified populated with current-session ts. The runner does the flip; we do not hand-edit."
      contains: "PASS"
  key_links:
    - from: "scripts/dark-factory-test.sh (predecessor)"
      to: "quality/gates/agent-ux/dark-factory.sh (canonical home)"
      via: "SIMPLIFY-07 file migration; old path becomes shim"
      pattern: "SIMPLIFY-07"
    - from: ".github/workflows/ci.yml"
      to: "quality/gates/agent-ux/dark-factory.sh"
      via: "the CI dark-factory job invokes the new path explicitly per OP-1 close-the-feedback-loop"
      pattern: "agent-ux/dark-factory.sh"
    - from: "quality/runners/run.py --cadence pre-pr"
      to: "agent-ux/dark-factory-sim catalog row -> quality/gates/agent-ux/dark-factory.sh"
      via: "the runner discovers the row by cadence, invokes the verifier; status flips NOT-VERIFIED -> PASS on first successful run"
      pattern: "pre-pr"
---

<objective>
Wave D — SIMPLIFY-07 + POLISH-AGENT-UX. Migrate `scripts/dark-factory-test.sh` to `quality/gates/agent-ux/dark-factory.sh`. Validate the v0.9.0 dark-factory regression invariant still holds (no regression vs baseline). Update the CI workflow to invoke the new path explicitly. Flip the agent-ux/dark-factory-sim catalog row from NOT-VERIFIED to PASS via the runner.

Per `.planning/REQUIREMENTS.md`:
- SIMPLIFY-07: `scripts/dark-factory-test.sh` -> `quality/gates/agent-ux/dark-factory.sh`. The agent-ux dimension is a thin home; if dark-factory is the only gate at v0.12.0 close, document the dimension as "intentionally sparse — perf and security stubs land in v0.12.1." (The dimension README from Wave A already documents this.)
- POLISH-AGENT-UX: the migration runs end-to-end against the simulator; any regressions vs the v0.9.0 baseline are fixed in the same phase (cite commit).

Three deliverables:

1. **`quality/gates/agent-ux/dark-factory.sh`** — copy of `scripts/dark-factory-test.sh` (133 lines preserved) + a 5-15 line header-comment lineage block + a small extension that writes the runner-readable artifact at `quality/reports/verifications/agent-ux/dark-factory-sim.json`. Banned word: do NOT use `replace`; use `migrate to` / `rewrite as` / `alongside`. Anti-bloat: the predecessor is 133 lines; the new file is <=160 lines (predecessor + header lineage + artifact-write block).

2. **`scripts/dark-factory-test.sh`** — thin shim (<=10 lines + comments). `exec bash quality/gates/agent-ux/dark-factory.sh "$@"`. Per OP-5 reversibility, keep the shim (do NOT delete) — CLAUDE.md "Local dev loop" section explicitly documents `bash scripts/dark-factory-test.sh sim` as the developer command.

3. **`.github/workflows/ci.yml`** — the dark-factory job step updates to invoke `bash quality/gates/agent-ux/dark-factory.sh sim`. (NOT via the shim — invoke the canonical path directly per OP-1.)

Plus catalog status flip: `quality/catalogs/agent-ux.json` row updates NOT-VERIFIED -> PASS via the runner. The runner does the flip automatically; do not hand-edit.

**Pivot rules:**
- **The migration uncovers a regression in the v0.9.0 dark-factory invariant** (e.g. helper stderr no longer emits the teaching strings on conflict path): per POLISH-AGENT-UX, fix in-phase (cite commit + commit-trail to the helper code). If the fix is large (>50 LoC change to crates/reposix-remote/), file a v0.12.1 carry-forward via MIGRATE-03 + waive the row until v0.12.1.
- **The CI dark-factory job uses the legacy path AND that path's behavior changes mid-PR** (e.g. shim can't pass through env vars): keep the shim's exec verbatim (no env-var transformation); the shim is structurally equivalent to a hardlink for any sensible caller.
- **The cargo workspace must build for the test to run** (the script invokes reposix + reposix-sim + git-remote-reposix from target/debug or PATH): per CLAUDE.md "Build memory budget", do NOT parallelize Wave D with another cargo-touching wave. P59 has only one cargo-touching wave (Wave D); other waves are doc/script/Python-only.
- **The migrated script's artifact-write block must not break the predecessor's exit-code semantics**: the script exits 0 on PASS, 1 on FAIL. The artifact is a side effect; the runner reads it for asserts_passed/failed but the exit code remains authoritative for the runner's PASS/FAIL grade. Do not collapse exit-code branches when adding the artifact-write block.

**Output:** Wave D ships in 1-2 commits.
- Commit 1: `feat(p59): migrate dark-factory.sh to quality/gates/agent-ux/ (SIMPLIFY-07)` -- includes the 3 file edits.
- Commit 2 (optional): `chore(p59): runner re-runs agent-ux/dark-factory-sim PASS` -- catalog status flip via runner; only if the catalog row actually flipped (ie status changed from NOT-VERIFIED -> PASS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/catalogs/agent-ux.json
@quality/catalogs/code.json
@quality/catalogs/release-assets.json
@quality/runners/run.py
@quality/gates/agent-ux/README.md
@quality/gates/code/clippy-lint-loaded.sh
@quality/gates/code/check-fixtures.py
@quality/gates/release/installer-asset-bytes.py
@scripts/dark-factory-test.sh
@scripts/check_clippy_lint_loaded.sh
@scripts/check_fixtures.py
@.github/workflows/ci.yml
@.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-01-PLAN.md
@.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-01-SUMMARY.md
@.planning/phases/58-release-dimension-gates-code-absorption/58-03-PLAN.md
@CLAUDE.md

<interfaces>
**Migration shape (mirror P58 SIMPLIFY-04 / SIMPLIFY-05 closure pattern):**

P58 Wave C migrated `scripts/check_clippy_lint_loaded.sh` -> `quality/gates/code/clippy-lint-loaded.sh` and `scripts/check_fixtures.py` -> `quality/gates/code/check-fixtures.py`. Per P58 Wave E close + the Phase 58 verdict at `quality/reports/verdicts/p58/VERDICT.md`, both predecessors were DELETED (not shimmed). Reference: `quality/reports/verdicts/p58/VERDICT.md` lines 53-55 ("scripts/check_clippy_lint_loaded.sh DELETED (commit af140ed)").

**P59 SIMPLIFY-07 chooses SHIM over delete because:**
1. CLAUDE.md "Local dev loop" section names `bash scripts/dark-factory-test.sh sim` as a developer command. Deleting the path breaks that documentation.
2. The script accepts a backend arg (sim / github / confluence / jira); the github/confluence/jira branches print stderr messages naming the cargo test path. Future v0.12.1 work may extend those branches; preserving the predecessor as a shim signals "this entry point is still alive, just relocated".
3. OP-5 reversibility: a one-cycle parallel-migration window lets any unaudited caller (developer alias, niche workflow) surface before deletion. P63 SIMPLIFY-12 audit can delete then.

(Future P63 SIMPLIFY-12 may delete this shim. The Wave D shim has a `tracked_in: P63 SIMPLIFY-12` style comment.)

**`quality/gates/agent-ux/dark-factory.sh` shape:**

The new file is structurally `scripts/dark-factory-test.sh` + 2 additions:

1. Header lineage comment block (5-15 lines):
```bash
#!/usr/bin/env bash
# quality/gates/agent-ux/dark-factory.sh -- agent-ux dimension dark-factory regression.
#
# MIGRATED FROM: scripts/dark-factory-test.sh per SIMPLIFY-07 (P59).
# CATALOG ROW:   quality/catalogs/agent-ux.json -> agent-ux/dark-factory-sim
# CADENCE:       pre-pr (per CI dark-factory job; ~30s wall time)
# INVARIANT:     v0.9.0 dark-factory regression -- helper stderr-teaching
#                strings emit on conflict + blob-limit paths so a
#                stderr-reading agent can recover without prompt engineering.
#
# This file is functionally equivalent to its predecessor. The predecessor
# is preserved as a thin shim at scripts/dark-factory-test.sh per OP-5
# reversibility; CLAUDE.md "Local dev loop" continues to document the
# old path. P63 SIMPLIFY-12 audit may delete the shim.
```

2. Artifact-write block at the end (10-20 lines):
```bash
# Runner-readable artifact (Wave D extension; predecessor only exited 0/1).
ARTIFACT="quality/reports/verifications/agent-ux/dark-factory-sim.json"
mkdir -p "$(dirname "$ARTIFACT")"
if [[ $EXIT_CODE -eq 0 ]]; then
    PASSED='["dark-factory regression sim path exits 0", "helper stderr emits teaching strings on conflict + blob-limit paths (v0.9.0 invariant)", "no regression vs v0.9.0 baseline"]'
    FAILED='[]'
else
    PASSED='[]'
    FAILED='["dark-factory regression FAILed; see stderr for assertions failed"]'
fi
cat > "$ARTIFACT" <<EOF
{
  "ts": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
  "row_id": "agent-ux/dark-factory-sim",
  "exit_code": $EXIT_CODE,
  "asserts_passed": $PASSED,
  "asserts_failed": $FAILED
}
EOF
exit $EXIT_CODE
```

(The predecessor's body — between the header and the artifact block — is copied verbatim. Only the file-name + paths in error messages need adjustment; the v0.9.0 invariant code stays.)

**`scripts/dark-factory-test.sh` shim shape (post-Wave-D):**

```bash
#!/usr/bin/env bash
# scripts/dark-factory-test.sh -- migrated to quality/gates/agent-ux/dark-factory.sh per SIMPLIFY-07.
#
# This shim preserves the old path for OP-5 reversibility; CLAUDE.md
# "Local dev loop" continues documenting the old command. P63
# SIMPLIFY-12 audit may delete this shim.
exec bash "$(dirname "$0")/../quality/gates/agent-ux/dark-factory.sh" "$@"
```

(<=10 lines including the header comment.)

**.github/workflows/ci.yml dark-factory job edit:**

Locate the existing dark-factory job/step that calls `bash scripts/dark-factory-test.sh sim` (likely in the integration-tests or smoke job). Update the invocation to `bash quality/gates/agent-ux/dark-factory.sh sim` (canonical path). Anti-bloat: one-line edit; do not refactor adjacent steps. If the job is wrapped in a workflow_call / matrix step, edit only the run: line.

**Catalog status flip flow:**

After the new file lands + the shim is in place + CI is updated:
1. `python3 quality/runners/run.py --cadence pre-pr` discovers the agent-ux/dark-factory-sim row, invokes the verifier (the new path), parses the artifact, sets status=PASS.
2. The runner's idempotent-status-update logic (per `quality/runners/run.py` lines 95-99 + the P57 SURPRISES row 1 lesson) writes back the status flip to the catalog file IF AND ONLY IF the status changed.
3. If status was NOT-VERIFIED in Wave A and PASS now: the catalog file is updated; we commit the change.

The runner does the flip; we do NOT hand-edit the catalog.

**Sanity check before the migration:**

Run the predecessor `bash scripts/dark-factory-test.sh sim` to confirm the BEFORE state. If the predecessor FAILs, the migration must NOT mask the failure -- per the prompt: "any regression -> fix in same wave (or waive with reason)." Possible regressions:
- The helper stderr stopped emitting teaching strings (a v0.9.0 invariant slipped).
- The sim binary on PATH is the wrong version (the workspace cargo build is stale).
- The sim seed file is out of date.

If a real regression is found: stop the wave, fix the regression in-phase, cite the commit. If the regression is large: file a v0.12.1 carry-forward via MIGRATE-03 + waive the row.

**Cross-references:**

- P58 Wave C SIMPLIFY-04 + SIMPLIFY-05 closure (precedent for migrate-and-shim/delete decision): `.planning/phases/58-release-dimension-gates-code-absorption/58-03-PLAN.md`.
- `quality/gates/code/clippy-lint-loaded.sh` (P58 migration target; comparable shape — bash wrapper + artifact write).
- CLAUDE.md "Local dev loop" section -- the `bash scripts/dark-factory-test.sh sim` line is the source of the shim-preserve decision.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BEFORE-state baseline -- run predecessor</name>
  <files>(read-only; produces wave-summary input)</files>
  <action>Run `bash scripts/dark-factory-test.sh sim` and capture exit code + stderr. Confirm the v0.9.0 invariant (helper stderr-teaching strings emit on conflict + blob-limit paths) holds. If it FAILS, STOP the wave and investigate: this is the BEFORE state; if the predecessor is broken before migration, the migration must address the root cause OR file a v0.12.1 carry-forward + waiver. Capture the output for the wave summary. Also: `grep -REn 'dark-factory-test\.sh' .github/ scripts/ docs/ examples/ CLAUDE.md README.md` to enumerate every caller (informs Task 4 CI edit + the shim-vs-delete decision).</action>
  <verify>
    <automated>echo "=== predecessor BEFORE-state run ===" && bash scripts/dark-factory-test.sh sim 2>&1 | tee /tmp/dark-factory-before.txt | tail -20 && echo "=== predecessor exit: $? ===" && echo "=== caller audit ===" && grep -REn 'dark-factory-test\.sh' .github/ scripts/ docs/ examples/ CLAUDE.md README.md 2>/dev/null | tee /tmp/dark-factory-callers.txt</automated>
  </verify>
  <done>BEFORE-state captured; predecessor exit code recorded; caller list captured.</done>
</task>

<task type="auto">
  <name>Task 2: Create quality/gates/agent-ux/dark-factory.sh (migration target)</name>
  <files>quality/gates/agent-ux/dark-factory.sh</files>
  <action>Copy `scripts/dark-factory-test.sh` content verbatim, add the header lineage comment block (per `<interfaces>` § "header lineage comment block"), and append the artifact-write block at the end (per `<interfaces>` § "artifact-write block"). Adjust any internal path references that named the predecessor location. Make the file executable (chmod +x). Anti-bloat: <=160 lines (predecessor 133 + header 15 + artifact block 12). Banned word: do NOT use `replace`. Run `bash -n quality/gates/agent-ux/dark-factory.sh` to confirm syntax. Run the new file directly: `bash quality/gates/agent-ux/dark-factory.sh sim` and confirm the same exit code as the BEFORE state.</action>
  <verify>
    <automated>test -x quality/gates/agent-ux/dark-factory.sh || chmod +x quality/gates/agent-ux/dark-factory.sh && lines=$(wc -l < quality/gates/agent-ux/dark-factory.sh) && test "$lines" -le 160 || { echo "TOO LONG: $lines"; exit 1; } && bash -n quality/gates/agent-ux/dark-factory.sh && grep -q 'SIMPLIFY-07' quality/gates/agent-ux/dark-factory.sh && ! grep -q '\breplace\b' quality/gates/agent-ux/dark-factory.sh && bash quality/gates/agent-ux/dark-factory.sh sim 2>&1 | tail -10; echo "exit $?"</automated>
  </verify>
  <done>Migration target lands; <=160 lines; bash -n clean; SIMPLIFY-07 lineage in header; banned word check passes; running the new path produces the same exit code as the BEFORE state captured in Task 1; artifact at quality/reports/verifications/agent-ux/dark-factory-sim.json exists.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce scripts/dark-factory-test.sh to a thin shim</name>
  <files>scripts/dark-factory-test.sh</files>
  <action>Rewrite scripts/dark-factory-test.sh as a <=10 line shim per the `<interfaces>` § "shim shape" spec. The shim is `exec bash quality/gates/agent-ux/dark-factory.sh "$@"` plus the header lineage comment. Preserve executability. Confirm `bash scripts/dark-factory-test.sh sim` still runs and produces the same exit code (the shim transparently delegates).</action>
  <verify>
    <automated>lines=$(wc -l < scripts/dark-factory-test.sh) && test "$lines" -le 10 || { echo "shim too long: $lines"; exit 1; } && grep -q 'SIMPLIFY-07' scripts/dark-factory-test.sh && grep -q 'quality/gates/agent-ux/dark-factory.sh' scripts/dark-factory-test.sh && test -x scripts/dark-factory-test.sh && bash scripts/dark-factory-test.sh sim 2>&1 | tail -3; echo "exit $?"</automated>
  </verify>
  <done>Shim is <=10 lines; references the migration target; executable; running it still works (transparent delegation).</done>
</task>

<task type="auto">
  <name>Task 4: Update .github/workflows/ci.yml to invoke canonical path</name>
  <files>.github/workflows/ci.yml</files>
  <action>Locate the dark-factory job/step in `.github/workflows/ci.yml`. Update the `run:` line from `bash scripts/dark-factory-test.sh sim` (or whatever current invocation) to `bash quality/gates/agent-ux/dark-factory.sh sim`. Anti-bloat: single-line edit; do not refactor adjacent steps. Confirm the workflow file YAML is still valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml').read())"` (yaml is third-party; if not in env, use `python3 -c "import json; print('skip yaml validation')"` and document; OR use `gh workflow view ci.yml` if gh is available). If the job invokes the shim and the shim works, this edit is technically a "rename only"; do it anyway per OP-1 (close the feedback loop -- explicit > implicit).</action>
  <verify>
    <automated>grep -q 'quality/gates/agent-ux/dark-factory.sh' .github/workflows/ci.yml && echo "OK: ci.yml uses canonical path" && python3 -c "
try:
    import yaml
    yaml.safe_load(open('.github/workflows/ci.yml').read())
    print('OK: ci.yml is valid YAML')
except ImportError:
    print('SKIP: yaml not in env')
except Exception as e:
    print(f'FAIL: ci.yml invalid: {e}'); exit(1)
"</automated>
  </verify>
  <done>ci.yml invokes the canonical path; YAML still valid; no other workflow steps modified.</done>
</task>

<task type="auto">
  <name>Task 5: Run runner --cadence pre-pr; flip catalog row to PASS</name>
  <files>quality/catalogs/agent-ux.json, quality/reports/verifications/agent-ux/dark-factory-sim.json</files>
  <action>Run `python3 quality/runners/run.py --cadence pre-pr`. The runner discovers agent-ux/dark-factory-sim, invokes quality/gates/agent-ux/dark-factory.sh sim, reads the artifact written by the script, grades PASS/FAIL/PARTIAL based on exit code + asserts. If the row was NOT-VERIFIED in Wave A and is PASS now, the runner writes the status flip back to quality/catalogs/agent-ux.json (per the runner's idempotent-status-update path). Confirm the catalog has status: "PASS" + last_verified populated. If the runner exits non-zero (e.g. dark-factory failed): per POLISH-AGENT-UX, fix in-phase. If the fix is too large for v0.12.0 P59 scope, file a v0.12.1 carry-forward via MIGRATE-03 + attach a waiver to the catalog row (until 2026-07-26). Final commit: `feat(p59): migrate dark-factory.sh to quality/gates/agent-ux/ (SIMPLIFY-07 + POLISH-AGENT-UX)`.</action>
  <verify>
    <automated>python3 quality/runners/run.py --cadence pre-pr 2>&1 | tee /tmp/pre-pr.txt | tail -20 && python3 -c "import json; rows = json.loads(open('quality/catalogs/agent-ux.json').read())['rows']; df = [r for r in rows if r['id'] == 'agent-ux/dark-factory-sim']; assert len(df) == 1; status = df[0]['status']; print(f'agent-ux/dark-factory-sim status: {status}'); assert status in ('PASS', 'WAIVED'), f'expected PASS or WAIVED, got {status}'"</automated>
  </verify>
  <done>Runner exits 0 OR exits 1 only because of an unrelated row (not agent-ux/dark-factory-sim); catalog row status is PASS or WAIVED with documented carry-forward; artifact at quality/reports/verifications/agent-ux/dark-factory-sim.json exists with current-session ts.</done>
</task>

</tasks>

<verification>
After Wave D close:
- `quality/gates/agent-ux/dark-factory.sh` exists + executable + <=160 lines + SIMPLIFY-07 lineage in header + banned-word check passes.
- `scripts/dark-factory-test.sh` is a <=10 line shim that exec's the new path; running it still produces the same exit code.
- `.github/workflows/ci.yml` invokes `bash quality/gates/agent-ux/dark-factory.sh sim`.
- `quality/catalogs/agent-ux.json` row status is PASS (or WAIVED with documented v0.12.1 carry-forward if a regression was found).
- `python3 quality/runners/run.py --cadence pre-pr` exits 0 (no new RED rows introduced).
- `python3 quality/runners/run.py --cadence pre-push` still exits 0 (Wave B + C invariants preserved).
</verification>

<success_criteria>
1. Catalog-first per recurring criterion 1: Wave A's row.verifier.script path now points at extant file (Wave D honors the contract Wave A locked).
2. CLAUDE.md update is NOT in scope here -- Wave F.
3. Verifier-subagent dispatch is NOT in scope here -- Wave F.
4. SIMPLIFY-07 absorption: scripts/dark-factory-test.sh is a thin shim referencing the canonical path; CI invokes the canonical path explicitly.
5. POLISH-AGENT-UX broaden-and-deepen: no regression vs v0.9.0 baseline; if a regression was found, fixed in-phase OR waived with v0.12.1 carry-forward.
</success_criteria>

<pivot_rules>
- **Predecessor BEFORE-state FAILs:** the v0.9.0 invariant has slipped; do not migrate a broken script. Investigate root cause; either fix in-phase (cite commit) or waive the row + file v0.12.1 carry-forward via MIGRATE-03. Document in SURPRISES.md.
- **Migration uncovers a regression that BEFORE-state did not** (e.g. the artifact-write block introduces a bash quirk that breaks the test's exit-code path): revert the artifact-write block + ship without it for v0.12.0; the runner can grade by exit-code-only when no artifact is present (per `quality/runners/run.py` lines 215-220).
- **CI dark-factory job is in a different workflow file** (e.g. `.github/workflows/dark-factory.yml`): edit that file instead. The Task 4 search should find the canonical caller; if Task 1 found multiple, update each.
- **CLAUDE.md "Local dev loop" section's example needs updating:** wave F's CLAUDE.md update covers this; do NOT pre-edit CLAUDE.md in Wave D.
- **Cargo workspace must build for the test to run:** per CLAUDE.md "Build memory budget", do NOT parallelize Wave D with another cargo-touching wave. P59 has only one cargo-touching wave (this one); other waves are doc/script/Python-only.

Reference: `quality/SURPRISES.md`; P58 Wave C SIMPLIFY-04 + SIMPLIFY-05 precedent for migrate-and-shim-or-delete decisions.
</pivot_rules>

<risk_table>
| Risk | Likelihood | Mitigation |
|---|---|---|
| Predecessor BEFORE-state already broken | low-medium | Task 1 captures BEFORE-state explicitly; if broken, stop + investigate before any migration commits land |
| The artifact-write block introduces a bash quirk that breaks exit-code semantics | low | The artifact write is at the script's TAIL, after the original exit logic; preserve `EXIT_CODE` capture and the final `exit $EXIT_CODE` |
| A v0.9.0 invariant regression is present + the fix is large | medium-low | Waive the row + MIGRATE-03 v0.12.1 carry-forward; do not block P59 close |
| ci.yml has multiple dark-factory invocations (e.g. across jobs) | low-medium | grep finds all; update each |
| The shim path differs across OS (Linux vs macOS readlink semantics) | low | Use `dirname "$0"` (POSIX-compatible); avoid `readlink -f` |
| The runner does not flip status from NOT-VERIFIED to PASS because of an idempotency check (P57 SURPRISES row 1 lesson) | low | The flip IS a status change; the runner's `catalog_dirty()` returns True; the flip persists |
| Em-dash idempotency regression in the new file's header comment (P57 SURPRISES) | low | Use ASCII hyphens in the header lineage comment block |
</risk_table>

<output>
After completion, create `.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-04-SUMMARY.md` capturing:
- BEFORE-state predecessor exit code (from Task 1)
- AFTER-state migration target exit code (from Task 2 smoke run)
- Caller audit results (Task 1)
- ci.yml edit diff (Task 4 -- one-line)
- Final pre-pr runner state + catalog row status flip (PASS / WAIVED)
- Whether any regression was found + how it was handled
</output>
