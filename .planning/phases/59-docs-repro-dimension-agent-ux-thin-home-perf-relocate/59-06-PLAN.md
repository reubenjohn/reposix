---
phase: 59-docs-repro-dimension-agent-ux-thin-home-perf-relocate
plan: 06
type: execute
wave: 6
depends_on:
  - "59-01"
  - "59-02"
  - "59-03"
  - "59-04"
  - "59-05"
files_modified:
  - quality/catalogs/docs-reproducible.json
  - quality/catalogs/agent-ux.json
  - quality/reports/verifications/docs-repro/
  - quality/reports/verifications/agent-ux/
  - CLAUDE.md
  - quality/SURPRISES.md
  - .planning/STATE.md
  - quality/reports/verdicts/p59/VERDICT.md
autonomous: false
requirements:
  - POLISH-DOCS-REPRO
  - POLISH-AGENT-UX
  - QG-06
  - QG-07
must_haves:
  truths:
    - "POLISH-DOCS-REPRO closure: every catalog row in quality/catalogs/docs-reproducible.json is graded by re-running `python3 quality/runners/run.py --cadence pre-push` AND `--cadence post-release`. snippet-coverage row PASS (Wave B). example-03-claude-code-skill row PASS (Wave C manual-spec-check). The 4 example container rows (01, 02, 04, 05) + tutorial-replay row are PASS in a docker-equipped environment OR WAIVED with the documented Wave C waiver until 2026-05-12 (Wave F CI dispatch unblocks). The 2 benchmark-claim rows stay NOT-VERIFIED at v0.12.0 (kind: manual; freshness_ttl: 30d; Wave A spec) -- their P2 blast_radius means NOT-VERIFIED does NOT block GREEN per quality/runners/run.py compute_exit_code rules (only P0+P1 NOT-VERIFIED blocks)."
    - "POLISH-AGENT-UX closure: agent-ux/dark-factory-sim row PASS (Wave D). The v0.9.0 invariant is preserved (helper stderr-teaching strings emit on conflict + blob-limit paths). No regression vs baseline."
    - "ALL P0+P1 rows across the 3 P59 catalogs are PASS, WAIVED, or filed as v0.12.1 carry-forward. NO P0+P1 rows remain NOT-VERIFIED at Wave F close."
    - "QG-07 CLAUDE.md update: a new H3 subsection 'P59 -- Docs-repro + agent-ux + perf-relocate dimensions live' lands in CLAUDE.md (anti-bloat: <=80 added lines per the P58 Wave F precedent). Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail)."
    - "QG-06 verifier subagent dispatch: an unbiased subagent (Path A via Task tool if available; Path B in-session disclosure per P56/P57/P58 precedent if not) grades all catalog rows GREEN. Verdict at quality/reports/verdicts/p59/VERDICT.md."
    - "STATE.md cursor advances P58 SHIPPED -> P59 SHIPPED. Roadmap-Evolution entry appended at top (newest first); P58/P57/P56 entries preserved verbatim. progress frontmatter incremented (completed_phases 3 to 4; total_plans 16 to 22; completed_plans 16 to 22; percent 38 to 50)."
    - "SURPRISES.md is updated with any P59 pivot entries from Waves A through E (likely 1-3 entries: Wave C docker-absent waiver; Wave E Option B Python naming deviation; possibly Wave A pre-push catalog-shape gate extension). The 5 P56 + 3 P57 + 7 P58 entries from earlier phases are preserved verbatim. Anti-bloat: under 200 lines (currently 172; ~10-15 lines headroom for P59)."
    - "Phase-close commit lands all 4 docs updates (CLAUDE.md + SURPRISES.md + STATE.md + VERDICT.md) in the SAME PR per QG-07."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Append (50-80 lines) at the END of the existing P57 + P58 Quality Gates dimension subsections with the P59-specific extensions: docs-repro home (snippet-extract.py drift detector + container-rehearse.sh + tutorial-replay.sh + manual-spec-check.sh; 3 verifiers + 9 catalog rows including the new snippet-coverage row added by Wave B), agent-ux home (dark-factory.sh; 1 catalog row; intentionally sparse note + v0.12.1 stub), perf relocate (file-relocate-only at v0.12.0; 3 catalog stubs WAIVED until 2026-07-26 per MIGRATE-03; full gate logic v0.12.1), SIMPLIFY-06 + SIMPLIFY-07 + SIMPLIFY-11 absorption record (predecessors: scripts/repro-quickstart.sh, scripts/dark-factory-test.sh, scripts/{latency-bench.sh, bench_token_economy.py, test_bench_token_economy.py} -- shims or deleted per Wave C/D/E decisions). Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail)."
      contains: "docs-repro dimension"
      max_lines: 80
    - path: "quality/SURPRISES.md"
      provides: "Append any P59 entries from Waves A through E + this Wave F. 5 P56 + 3 P57 + 7 P58 entries preserved verbatim. Anti-bloat: under 200 lines (with ~10-15 lines headroom for P59 entries)."
      contains: "P59"
      max_lines: 200
    - path: ".planning/STATE.md"
      provides: "Roadmap-Evolution entry appended at top for P59 SHIPPED. Current Position cursor advances to P60. progress frontmatter incremented per the math above."
      contains: "P59 SHIPPED"
    - path: "quality/reports/verdicts/p59/VERDICT.md"
      provides: "Unbiased verifier subagent verdict (or in-session verdict with disclosure per P56/P57/P58 precedent). Top-line GREEN/RED, per-row table for the docs-repro 9 rows + agent-ux 1 row + perf 3 stub rows = 13 rows graded; QG-07 + SIMPLIFY-06 + SIMPLIFY-07 + SIMPLIFY-11 + POLISH-DOCS-REPRO + POLISH-AGENT-UX closure citations; recommendation."
      contains: "GREEN"
  key_links:
    - from: "CLAUDE.md (P59 append)"
      to: "quality/PROTOCOL.md"
      via: "cross-reference; CLAUDE.md does NOT duplicate runtime detail per anti-bloat rule"
      pattern: "quality/PROTOCOL.md"
    - from: "verifier subagent dispatch"
      to: "quality/reports/verdicts/p59/VERDICT.md"
      via: "Task tool dispatch with the prompt from quality/PROTOCOL.md verifier-subagent-prompt-template; subagent writes the verdict file with ZERO session context"
      pattern: "ZERO session context"
    - from: ".planning/STATE.md"
      to: "P60 agent (next session)"
      via: "Cursor advance + Roadmap-Evolution entry tells the next agent P59 GREEN + lists what they read at start of P60 (per quality/PROTOCOL.md Step 1 reading order)"
      pattern: "P59 SHIPPED"
---

<objective>
Wave F — phase close + POLISH broaden-and-deepen pass.

Per `.planning/REQUIREMENTS.md` § "Recurring success criteria across every phase (P56-P63)" criterion 5 ("Fix every RED row the dimension's gates flag") + per QG-06 + QG-07. Wave F is the broaden-and-deepen pass that locks all the dimension gates GREEN before the phase closes.

Five activities, in order:

1. **POLISH-DOCS-REPRO closure (Task 1):** run the full sweep locally (`pre-push` + `post-release` + `weekly`). For each RED/NOT-VERIFIED row in scope:
   - **Fix in-phase:** edit the source artifact (doc, script, or config) so the assertion passes. Cite the commit.
   - **Or waive:** for genuinely-deferred items, attach a 90d TTL waiver + dimension_owner + tracked_in MIGRATE-03 v0.12.1.
   - **Or file v0.12.1 carry-forward:** update MIGRATE-03 + new SURPRISES entry.

2. **POLISH-AGENT-UX closure (Task 2):** confirm agent-ux/dark-factory-sim PASS via runner --cadence pre-pr. If RED: fix in-phase OR file v0.12.1. The expected outcome (per Wave D): no regression vs v0.9.0 baseline; row is already PASS at Wave D close. Wave F just re-confirms.

3. **QG-07 CLAUDE.md update (Task 3):** append a P59 H3 subsection (50-80 lines) per the P58 Wave F precedent.

4. **QG-06 verifier subagent dispatch (Task 4):** dispatch an unbiased subagent (Path A via Task tool if available; Path B in-session disclosure per P56/P57/P58 precedent if not). Subagent reads the catalog rows + artifacts + CLAUDE.md diff with ZERO session context and grades GREEN/RED.

5. **STATE.md + SURPRISES.md updates (Task 5):** advance cursor; append any P59 pivot entries.

Plan is autonomous: false because the verifier-subagent dispatch in Task 4 is the human-or-checkpoint moment.

**Pivot rules:**
- **Path A Task tool unavailable in executor:** per P56/P57/P58 precedent, fall back to Path B (in-session verdict with disclosure block). The disclosure block names: (1) Evidence-only -- each PASS/WAIVED claim cites file+line OR runner output; (2) Catalog-rows-as-contract -- grade computed from row.expected.asserts <-> artifact asserts_passed/failed match, not narrative; (3) Refuse-GREEN-on-RED -- if any P0+P1 row outside {PASS, WAIVED}, mark RED; (4) Out-of-session re-grade should match.
- **A row is RED at Wave F entry:** Fix the underlying source. If too large for v0.12.0 P59 scope, waive + carry-forward via MIGRATE-03. Document in SURPRISES.md.
- **CLAUDE.md cap (anti-bloat):** if the P59 append pushes total CLAUDE.md beyond a manageable size, refactor only the new P57+P58+P59 subsections to be more concise; do NOT touch sections from earlier phases. Per `quality/PROTOCOL.md` § anti-bloat rules per surface, CLAUDE.md is bound only by its banned-words-lint and the per-section cross-reference-don't-duplicate rule.
- **A new pivot in this Wave F itself** (e.g. the verifier-subagent dispatch reveals a contract drift): append a final SURPRISES.md entry. Do NOT silently fix it.

**Output:** Wave F ships in 1-2 commits.
- Commit 1: `chore(p59): POLISH closure + CLAUDE.md update + STATE/SURPRISES advance + verifier verdict GREEN`.
- Commit 2 (optional): `chore(p59): in-phase fix for <RED row>` -- if Task 1 found a RED that needed fixing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.12.0/roadmap-and-rationale.md
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/catalogs/docs-reproducible.json
@quality/catalogs/agent-ux.json
@quality/catalogs/perf-targets.json
@quality/catalogs/freshness-invariants.json
@quality/catalogs/release-assets.json
@quality/catalogs/code.json
@quality/catalogs/orphan-scripts.json
@quality/runners/run.py
@quality/runners/verdict.py
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-06-PLAN.md
@.planning/phases/58-release-dimension-gates-code-absorption/58-06-PLAN.md
@.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-01-SUMMARY.md
@.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-02-SUMMARY.md
@.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-03-SUMMARY.md
@.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-04-SUMMARY.md
@.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-05-SUMMARY.md
@quality/reports/verdicts/p57/VERDICT.md
@quality/reports/verdicts/p58/VERDICT.md
@CLAUDE.md

<interfaces>
**CLAUDE.md update -- shape (per QG-07 + the P58 Wave F precedent):**

Anti-bloat: append a paragraph + tables; do NOT rewrite existing sections. The new content sits AFTER the existing P58 "Release dimension live" subsection, as a P59 docs-repro-dimension subsection.

Recommended new content (50-80 lines), inserted via H3 subsection right after the P58 subsection:

```markdown
### P59 -- Docs-repro + agent-ux + perf-relocate dimensions live (added 2026-04-XX)

**Docs-repro home:** `quality/gates/docs-repro/`. 3 verifiers + 1 manual-spec checker:

| Verifier | Catalog rows backed | Cadence |
|---|---|---|
| `snippet-extract.py` (3 modes: --list / --check / --write-template) | `docs-repro/snippet-coverage` | pre-push |
| `container-rehearse.sh <id>` | 4 example container rows + `docs-repro/tutorial-replay` | post-release |
| `tutorial-replay.sh` (promotion of `scripts/repro-quickstart.sh` per SIMPLIFY-06) | `docs-repro/tutorial-replay` | post-release |
| `manual-spec-check.sh <id>` | `docs-repro/example-03-claude-code-skill` | on-demand |

Cadence wiring: the post-release container rehearsals run via `quality-post-release.yml` (P58 stub already supports the cadence). Per the prompt's pivot rules: docker not installed -> NOT-VERIFIED with stderr warning + non-fatal exit. Per-snippet rehearsal cap: 60s OR move to post-release-only.

**Agent-ux home:** `quality/gates/agent-ux/`. Intentionally sparse at v0.12.0 -- the dark-factory regression is the only gate. Perf and security stubs land in v0.12.1 per MIGRATE-03.

| Verifier | Catalog row | Cadence |
|---|---|---|
| `dark-factory.sh sim` (migrated from `scripts/dark-factory-test.sh` per SIMPLIFY-07) | `agent-ux/dark-factory-sim` | pre-pr |

The v0.9.0 dark-factory invariant (helper stderr-teaching strings on conflict + blob-limit paths) is preserved verbatim; CI workflow `.github/workflows/ci.yml` invokes the canonical path explicitly.

**Perf home (relocate-only):** `quality/gates/perf/`. File-relocate only at v0.12.0; full gate logic deferred to v0.12.1 stub per MIGRATE-03. 3 catalog rows (`perf/latency-bench`, `perf/token-economy-bench`, `perf/headline-numbers-cross-check`) all WAIVED until 2026-07-26.

| Source script | Migrated to | Cadence | Status |
|---|---|---|---|
| `scripts/latency-bench.sh` | `quality/gates/perf/latency-bench.sh` | weekly | WAIVED v0.12.1 |
| `scripts/bench_token_economy.py` | `quality/gates/perf/bench_token_economy.py` | weekly | WAIVED v0.12.1 |
| `scripts/test_bench_token_economy.py` | `quality/gates/perf/test_bench_token_economy.py` | (test) | n/a |

`benchmarks/fixtures/*` stays in place (test inputs, not gates).

**SIMPLIFY-06/07/11 absorption record:**
- `scripts/repro-quickstart.sh` -> shim or deleted (decision in 59-03-SUMMARY.md).
- `scripts/dark-factory-test.sh` -> thin shim per OP-5; CLAUDE.md "Local dev loop" command still works.
- `scripts/{latency-bench.sh, bench_token_economy.py}` -> shims; `scripts/test_bench_token_economy.py` -> shim or deleted.

**Recovery patterns:**
- snippet drift detected: `python3 quality/gates/docs-repro/snippet-extract.py --write-template <derived-id>` then paste output into `quality/catalogs/docs-reproducible.json`.
- container rehearsal RED: read the artifact at `quality/reports/verifications/docs-repro/<row-id>.json`; the `stderr` field has the docker output.
- dark-factory regression RED: run `bash quality/gates/agent-ux/dark-factory.sh sim` locally with `set -x` to see the helper stderr; the v0.9.0 invariant is the teaching-strings assertion.

**Cross-references:** `quality/gates/docs-repro/README.md`, `quality/gates/agent-ux/README.md`, `quality/gates/perf/README.md`, `quality/PROTOCOL.md` (runtime contract), `MIGRATE-03` v0.12.1 carry-forward (perf full implementation).
```

(Adjust the dates + commit refs at execution time.)

**SURPRISES.md update:**

Re-read `quality/SURPRISES.md`. Likely P59 entries (depending on what happened in Waves A-E):
- Wave A: catalog-first commit clean -> likely NO surprise. (If pre-push catalog-shape gate needed allow-list extension: 1 entry.)
- Wave B: snippet-extract.py + drift detector. Possible surprises: pivot from per-block tracking to allow-list mode if >50 blocks (1 entry); fenced-block extractor false-positive (1 entry).
- Wave C: container-rehearse + tutorial-replay. Likely surprise: docker-absent waiver in dev env (1 entry).
- Wave D: dark-factory migration. Possible surprises: v0.9.0 invariant regression (1 entry); ci.yml multi-caller (rare).
- Wave E: perf relocate. Likely surprise: Option B underscore decision + catalog row correction (1 entry); pytest discovery tweak (1 entry); benchmark fixture path resolution (1 entry).

If zero surprises across all waves: add a single confirmation entry per the P57/P58 Wave F precedent stating phase shipped without pivots, with summary of what landed clean.

Total expected new entries: 1-5. Anti-bloat cap: 200 lines (currently 172; headroom 28 lines).

Order: APPEND AT THE END. Do NOT touch P56/P57/P58 entries.

**STATE.md cursor advance:**

Frontmatter:
- last_updated: current RFC3339.
- last_activity: P59-shipped one-liner naming the 3 dimensions + the SIMPLIFY-06/07/11 closures.
- progress: total_phases 8 (unchanged); completed_phases 3 -> 4; total_plans 16 -> 22 (+6 P59 plans); completed_plans 16 -> 22; percent 38 -> 50.

Per the P57/P58 Wave F W-4 lesson: read frontmatter at runtime, increment, do NOT hard-code the math.

Roadmap-Evolution entry: prepend at TOP of `## Accumulated Context` -> `### Roadmap Evolution` (newest first per the file's existing convention). The entry is ~30-50 lines summarizing Wave A through F outcomes.

Current Position: advance from P58 SHIPPED to P59 SHIPPED. Cursor text matches the structure of P57+P58 cursor advances at the existing STATE.md.

**QG-06 verifier subagent dispatch -- Path A vs Path B:**

**Path A (preferred):** dispatch via the Task tool with `subagent_type: "general-purpose"` and the prompt from `quality/PROTOCOL.md` § "Verifier subagent prompt template" (filled with phase number 59). The subagent reads catalog rows + artifacts + CLAUDE.md diff with ZERO session context; emits verdict to `quality/reports/verdicts/p59/<ts>.md`.

**Path B (fallback per P56/P57/P58 precedent):** in-session verdict with the disclosure block, written by the Wave F executor with the following constraints:
1. **Evidence-only.** Each PASS/WAIVED claim cites a specific file + line range OR a runner output line.
2. **Catalog-rows-as-contract.** The grade is computed from `row.expected.asserts` <-> artifact `asserts_passed/failed` match, not from narrative.
3. **Refuse-GREEN-on-RED.** If any P0+P1 row had `status` outside `{PASS, WAIVED}` after the final runner sweep, mark this file RED.
4. **Out-of-session re-grade should match.** A future Path A subagent reading the same catalog files + artifacts + CLAUDE.md diff at this commit SHA should produce the same verdict.

**Verdict file structure (ports the P58 VERDICT.md shape):**

- Top-line: GREEN/RED + ts + phase + badge.
- Disclosure block (Path B only).
- Top-line summary table: catalog rows graded, runner cadence exit codes, badge state, QG-07/QG-09 status.
- Per-dimension per-row tables: docs-repro (9 rows) + agent-ux (1 row) + perf (3 stub rows).
- CLAUDE.md QG-07 citations: `git diff main...HEAD -- CLAUDE.md` confirms the new H3 subsection appears.
- Recurring criteria 1-5 evidence (catalog-first, CLAUDE.md, verifier dispatch, SIMPLIFY absorption, RED-row fixes).
- Carry-forwards: enumerate any rows WAIVED with their `tracked_in` + `until` + reason.
- Recommendation: GREEN -> P60 may begin; RED -> phase does NOT close.

**STATE.md frontmatter math (per P58 Wave F W-4 lesson):**

Use a runtime-read script (do NOT hard-code):

```python
import yaml
from pathlib import Path
text = Path(".planning/STATE.md").read_text()
parts = text.split("---", 2)
fm = yaml.safe_load(parts[1])
fm["progress"]["completed_phases"] += 1  # 3 -> 4
fm["progress"]["completed_plans"] += 6  # P59 has 6 plans
fm["progress"]["total_plans"] += 6  # 16 -> 22
fm["progress"]["percent"] = round(fm["progress"]["completed_phases"] / fm["progress"]["total_phases"] * 100)
# Write back; preserve trailing newline
```

(Or `python3` snippet inline; the executor decides on the tooling. The KEY is: read at runtime, increment, write back. Do NOT hand-edit the YAML keys to specific numbers.)

**Cross-references:**

- `quality/reports/verdicts/p58/VERDICT.md` -- the structural template for this file (Path B disclosure block; per-row tables; QG citations; recurring criteria evidence).
- `quality/reports/verdicts/p57/VERDICT.md` -- earlier precedent.
- `.planning/phases/58-release-dimension-gates-code-absorption/58-06-PLAN.md` -- this plan's structural template (Wave F shape).
- `quality/PROTOCOL.md` § "Verifier subagent prompt template" -- the Task-dispatch prompt verbatim (used for Path A).
- `quality/PROTOCOL.md` § "SURPRISES.md format" -- entry template.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POLISH-DOCS-REPRO + POLISH-AGENT-UX broaden-and-deepen sweep</name>
  <files>(catalog status flips via runner; conditional in-phase fixes)</files>
  <action>Run the full sweep:
1. `python3 quality/runners/run.py --cadence pre-push` -- expect exit 0.
2. `python3 quality/runners/run.py --cadence pre-pr` -- expect exit 0 (agent-ux/dark-factory-sim PASS).
3. `python3 quality/runners/run.py --cadence post-release` -- expect exit 0 (4 example container rows + tutorial-replay row PASS or WAIVED + manual-spec-check row PASS + 2 benchmark-claim rows manual at v0.12.0).
4. `python3 quality/runners/run.py --cadence weekly` -- expect exit 0 (3 perf rows WAIVED + release rows PASS).

For any RED P0+P1 row: triage. (a) Fix in-phase via the underlying source. (b) Or attach 90d waiver + dimension_owner + tracked_in MIGRATE-03 v0.12.1 + SURPRISES.md entry. (c) Or update MIGRATE-03 + carry-forward.

Capture the AFTER-state runner output for each cadence into the wave summary.</action>
  <verify>
    <automated>set +e; for c in pre-push pre-pr post-release weekly; do echo "=== cadence: $c ==="; python3 quality/runners/run.py --cadence $c 2>&1 | tail -10; echo "--- exit $? ---"; done; echo "DONE: see exit codes above"</automated>
  </verify>
  <done>4 cadences run; AFTER-state captured for each; any RED rows triaged + actioned per (a)/(b)/(c) above.</done>
</task>

<task type="auto">
  <name>Task 2: QG-07 CLAUDE.md update -- append P59 H3 subsection</name>
  <files>CLAUDE.md</files>
  <action>Append a new H3 subsection AFTER the existing P58 "Release dimension live" subsection (or wherever the P58 Wave F append placed it; locate it via `grep -n 'P58' CLAUDE.md`). Use the template in `<interfaces>` § "CLAUDE.md update". Anti-bloat: <=80 added lines per the P58 Wave F precedent. Banned word: do NOT use `replace`; use `migrate to` / `rewrite as` / `alongside`. Cross-reference quality/PROTOCOL.md (does NOT duplicate runtime detail). Verify total line growth + banned-words-lint.</action>
  <verify>
    <automated>git diff HEAD -- CLAUDE.md | grep -c '^+' | xargs -I{} test {} -lt 90 && grep -q 'P59 -- Docs-repro' CLAUDE.md && ! grep -q '\breplace\b' CLAUDE.md && bash scripts/banned-words-lint.sh CLAUDE.md 2>&1 | tail -5</automated>
  </verify>
  <done>CLAUDE.md gains the P59 H3 subsection; <=80 added lines; banned-words-lint passes; cross-references quality/PROTOCOL.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: QG-06 verifier subagent dispatch (Path A or Path B)</name>
  <files>quality/reports/verdicts/p59/VERDICT.md</files>
  <action>Dispatch the unbiased verifier subagent that grades ALL catalog rows for P59 with ZERO session context, per quality/PROTOCOL.md § "Verifier subagent prompt template".

1. **Try Path A first:** dispatch a Task tool agent with `subagent_type: "general-purpose"` and the verbatim prompt from `quality/PROTOCOL.md` § "Verifier subagent prompt template" (substituting phase=59). The subagent reads catalog rows + artifacts + CLAUDE.md diff and writes the verdict to `quality/reports/verdicts/p59/<ISO_TIMESTAMP>.md`.

2. **If Path A fails (Task tool unavailable in this executor):** Path B fallback per P56/P57/P58 precedent. Author the verdict in-session with the disclosure block (verbatim from `<interfaces>` § "Path B fallback"), citing each PASS/WAIVED with file+line OR runner output line, and naming all 4 disclosure constraints. Write to `quality/reports/verdicts/p59/VERDICT.md`.

3. The verdict MUST include: top-line GREEN/RED + 13-row per-dimension table (9 docs-repro + 1 agent-ux + 3 perf) + QG-07 CLAUDE.md citations + recurring-criteria evidence + carry-forwards table + recommendation.

4. **Phase does NOT close on RED.** If the verifier grades RED, return to Task 1 + fix in-phase OR file v0.12.1 carry-forward. Do not advance to Task 4 until GREEN.

This task is a checkpoint:human-verify because the verdict is the phase-close grade — the executor pauses at this step until the verdict is reviewed. Resume signal: type "approved" if verdict GREEN; or describe RED rows to be fixed before resume.</action>
  <verify>
    <automated>test -f quality/reports/verdicts/p59/VERDICT.md && grep -E '^\*\*Verdict: GREEN\*\*|^Verdict: GREEN' quality/reports/verdicts/p59/VERDICT.md && grep -c 'docs-repro/\|agent-ux/\|perf/' quality/reports/verdicts/p59/VERDICT.md | awk '{ if ($1 < 13) {print "FEWER than 13 row citations: " $1; exit 1} else {print "OK: " $1 " row citations"} }'</automated>
  </verify>
  <done>quality/reports/verdicts/p59/VERDICT.md exists; top-line GREEN; per-row table cites all 13 catalog rows (9 docs-repro + 1 agent-ux + 3 perf); QG-07 + recurring-criteria evidence present; recommendation states "P60 may begin".</done>
</task>

<task type="auto">
  <name>Task 4: STATE.md cursor advance + SURPRISES.md append + final phase-close commit</name>
  <files>.planning/STATE.md, quality/SURPRISES.md</files>
  <action>Per the `<interfaces>` STATE.md frontmatter math: read frontmatter at runtime, increment counters (completed_phases 3->4, completed_plans 16->22, total_plans 16->22, percent 38->50), write back. Append a Roadmap-Evolution entry at the TOP of `### Roadmap Evolution` summarizing Wave A through F outcomes (~30-50 lines). Advance Current Position from "P58 SHIPPED" to "P59 SHIPPED -- Docs-repro + agent-ux + perf-relocate dimensions live, ...". Update `last_activity` + `last_updated`. Append any P59 SURPRISES entries from Waves A-E (Wave F itself unlikely to add unless the verifier dispatch surfaces a contract drift). Final commit: `chore(p59): POLISH closure + CLAUDE.md update + STATE/SURPRISES advance + verifier verdict GREEN` -- includes Tasks 1-4 docs updates + the verdict file.</action>
  <verify>
    <automated>python3 -c "import yaml; from pathlib import Path; text = Path('.planning/STATE.md').read_text(); parts = text.split('---', 2); fm = yaml.safe_load(parts[1]); print(f'completed_phases: {fm[\"progress\"][\"completed_phases\"]}'); assert fm['progress']['completed_phases'] == 4, 'expected 4'; assert fm['progress']['percent'] == 50, 'expected 50'" && grep -q 'P59 SHIPPED' .planning/STATE.md && wc -l quality/SURPRISES.md | awk '{ if ($1 > 200) {print \"TOO LONG: \" $1; exit 1} else {print \"SURPRISES.md lines: \" $1 \" (under 200 cap)\"} }' && test -f quality/reports/verdicts/p59/VERDICT.md && grep -q 'GREEN' quality/reports/verdicts/p59/VERDICT.md</automated>
  </verify>
  <done>STATE.md frontmatter + Roadmap-Evolution entry + Current Position advanced; SURPRISES.md within 200-line cap with P59 entries appended; verdict file GREEN; final commit lands.</done>
</task>

</tasks>

<verification>
After Wave F close (P59 phase-close grade):
- `python3 quality/runners/run.py --cadence pre-push` exit 0.
- `python3 quality/runners/run.py --cadence pre-pr` exit 0.
- `python3 quality/runners/run.py --cadence post-release` exit 0 (any docker-absent waivers documented).
- `python3 quality/runners/run.py --cadence weekly` exit 0 (3 perf rows WAIVED).
- `quality/reports/verdicts/p59/VERDICT.md` exists + GREEN + cites all 13 catalog rows + QG-07 + SIMPLIFY-06/07/11 + POLISH-DOCS-REPRO + POLISH-AGENT-UX closures.
- `git diff main...HEAD -- CLAUDE.md` shows the new P59 H3 subsection.
- `quality/SURPRISES.md` <=200 lines.
- `.planning/STATE.md` frontmatter has completed_phases=4, percent=50.
- All P0+P1 catalog rows across docs-reproducible.json + agent-ux.json + perf-targets.json are PASS, WAIVED, or filed as v0.12.1 carry-forward.
</verification>

<success_criteria>
1. **Catalog-first** -- PASS. Wave A committed the contract (3 catalogs + 3 READMEs) before any verifier code; Waves B-E cite row IDs.
2. **CLAUDE.md update in same PR (QG-07)** -- PASS. Wave F lands the H3 subsection in this commit chain.
3. **Verifier-subagent dispatch on phase close (QG-06)** -- PASS. Path A or Path B; verdict at `quality/reports/verdicts/p59/VERDICT.md`.
4. **SIMPLIFY absorption** -- PASS. SIMPLIFY-06 (repro-quickstart.sh shim/delete) + SIMPLIFY-07 (dark-factory.sh migrated; old path shim) + SIMPLIFY-11 (perf-dim file relocate; old paths shims).
5. **Fix every RED row (broaden-and-deepen)** -- PASS. Wave F is this pass. All P0+P1 rows PASS, WAIVED with documented reason, or filed as v0.12.1 carry-forward.
</success_criteria>

<pivot_rules>
- **Path A Task tool unavailable:** Path B per P56/P57/P58 precedent. Disclosure block + 4 constraints in the verdict.
- **A row is RED at Wave F entry:** triage with (fix / waive / carry-forward); document in SURPRISES.md.
- **CLAUDE.md hits an unmanageable size:** refactor only the new P57+P58+P59 subsections to be more concise; do NOT touch sections from earlier phases.
- **A pivot happens during Wave F itself** (e.g. verifier-dispatch reveals contract drift): append a final SURPRISES.md entry; do NOT silently fix.
- **STATE.md frontmatter math hard-coded by an executor mistake** (per P57 Wave F W-4 lesson): re-run the math at runtime via Python; do NOT hand-type numbers.
- **The verifier verdict goes RED on a row Wave A pre-emptively waived** (e.g. perf-targets.json rows now report a problem we did not anticipate): the waiver makes the runner grade WAIVED, not RED; if the verifier subagent disagrees with the waiver-block, the disagreement is the contract drift. Append SURPRISES.md + either tighten the waiver block or accept the verdict.

Reference: `quality/SURPRISES.md` (12 entries: 5 P56 + 3 P57 + 7 P58); P58 Wave F precedent in `quality/reports/verdicts/p58/VERDICT.md`.
</pivot_rules>

<risk_table>
| Risk | Likelihood | Mitigation |
|---|---|---|
| Wave F verifier subagent grades RED on a row Wave E thought was PASS | low-medium | Wave F Task 1 runs the full sweep first; any RED triggers Task 3 NOT to advance; in-phase fix loop |
| Docker-absent waivers (Wave C) bleed past their 2026-05-12 expiry without CI dispatch unblocking | medium | The waiver `tracked_in` field names "Wave F CI dispatch in docker-equipped GH runner"; if Wave F cannot run that dispatch (e.g. GH Actions queue full), document in SURPRISES + extend waiver to 2026-06-12 with explicit reason |
| CLAUDE.md append exceeds 80 lines | medium | Track count during Task 2; trim cross-references to one-liners; let `quality/PROTOCOL.md` carry the runtime detail |
| Path A Task dispatch unavailable AND Path B disclosure block not authored carefully | low | Use the verbatim disclosure block from P58 VERDICT.md as the structural template; copy + adjust dates/citations |
| STATE.md frontmatter increment uses wrong formula | low | Use the runtime-read script in `<interfaces>` -- read fm["progress"], increment, write back; do NOT hand-type |
| SURPRISES.md cap of 200 lines is exceeded by the cumulative P56+P57+P58+P59 entries | low | Currently 172 lines; ~10-15 lines of P59 entries lands ~185; if exceeded, archive oldest 50 entries to `quality/SURPRISES-archive-2026-Q2.md` per anti-bloat rule |
| The verdict file is GREEN but a downstream P60 agent finds a row was actually RED | low | The verifier subagent's catalog-rows-as-contract rule + the runner's exit-code check ensure GREEN means GREEN; future divergence is a contract-drift bug worth a SURPRISES entry |
| The SIMPLIFY-06 + SIMPLIFY-07 + SIMPLIFY-11 absorptions are listed in CLAUDE.md but the actual file states drift (e.g. shims accidentally deleted by a later-wave commit) | low | Task 1 runs `ls -la scripts/{repro-quickstart.sh, dark-factory-test.sh, latency-bench.sh, bench_token_economy.py, test_bench_token_economy.py}` to confirm the post-Wave-E file states; verdict reflects ground truth |
</risk_table>

<output>
After completion, create `.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-06-SUMMARY.md` capturing:
- Wave F sweep AFTER-state (4 cadences exit codes + per-row status)
- CLAUDE.md added line count + commit ref
- Verdict path (Path A vs Path B) + verdict file path
- SURPRISES.md final line count + new entry count
- STATE.md progress values (completed_phases 4, percent 50)
- Recommendation: P60 ready to begin
</output>
