---
phase: 59-docs-repro-dimension-agent-ux-thin-home-perf-relocate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/catalogs/docs-reproducible.json
  - quality/catalogs/agent-ux.json
  - quality/catalogs/perf-targets.json
  - quality/gates/docs-repro/README.md
  - quality/gates/agent-ux/README.md
  - quality/gates/perf/README.md
autonomous: true
requirements:
  - DOCS-REPRO-04
  - SIMPLIFY-06
  - SIMPLIFY-07
  - SIMPLIFY-11
must_haves:
  truths:
    - "A committed catalog set defines GREEN for P59 BEFORE any verifier code lands (catalog-first per quality/PROTOCOL.md Step 3 and recurring criterion 1 from REQUIREMENTS.md)."
    - "quality/catalogs/docs-reproducible.json carries 8 seed rows: 4 example/0[1,2,4,5]-*/run rows (container kind, post-release cadence; the run.sh / run.py wrappers that exercise sim end-to-end), 1 example/03-claude-code-skill row (manual kind, freshness_ttl 90d; markdown agent spec, not auto-runnable), 1 docs-repro/tutorial-replay row (container kind, post-release cadence; promotion target for scripts/repro-quickstart.sh per SIMPLIFY-06 + DOCS-REPRO-03), 2 benchmark-claim rows (benchmark-claim/8ms-cached-read + benchmark-claim/89.1-percent-token-reduction; manual kind, ci-warning, freshness_ttl 30d; ported from .planning/docs_reproducible_catalog.json)."
    - "quality/catalogs/agent-ux.json carries 1 seed row: agent-ux/dark-factory-sim (kind: container or mechanical depending on whether docker is required; cadence: pre-pr; the SIMPLIFY-07 promotion target for scripts/dark-factory-test.sh)."
    - "quality/catalogs/perf-targets.json carries 3 seed rows (all status=NOT-VERIFIED with a 90-day v0.12.1 waiver): perf/latency-bench (latency-bench.sh sim relocate target), perf/token-economy-bench (bench_token_economy.py relocate target), perf/headline-numbers-cross-check (the cross-check that compares bench output against headline copy in docs/index.md + README.md — full implementation deferred to v0.12.1 per MIGRATE-03)."
    - "Three dimension READMEs land alongside the catalogs: quality/gates/docs-repro/README.md (verifier table for snippet-extract.py + container-rehearse.sh + tutorial-replay.sh; conventions; pivot rules), quality/gates/agent-ux/README.md (intentionally sparse — perf and security stubs land in v0.12.1; documents the dark-factory.sh migration source), quality/gates/perf/README.md (perf-relocate file move; full gate logic deferred to v0.12.1 stub per MIGRATE-03; cross-references SIMPLIFY-11). All three READMEs are <=50 lines per anti-bloat (per quality/PROTOCOL.md anti-bloat rules)."
    - "ALL rows in all three catalogs are status=NOT-VERIFIED in this commit. The verifier files do not yet exist (Waves B-E ship them); the catalog locks the contract for the unbiased verifier in Wave F."
    - "An ad-hoc preflight check is also committed in the same commit: scripts/check-quality-catalogs.py is amended (or a new helper added) so that the existing pre-push catalog-shape gate parses the new files. This avoids breaking pre-push during P59 development."
  artifacts:
    - path: "quality/catalogs/docs-reproducible.json"
      provides: "8 docs-repro-dimension rows: docs-repro/example-01-shell-loop, docs-repro/example-02-python-agent, docs-repro/example-04-conflict-resolve, docs-repro/example-05-blob-limit-recovery (kind: container; cadence: post-release; verifier.script: quality/gates/docs-repro/container-rehearse.sh; verifier.args: [<row-id>]; container: ubuntu:24.04), docs-repro/example-03-claude-code-skill (kind: manual; cadence: on-demand; freshness_ttl: 90d; status: NOT-VERIFIED; comment: 'markdown agent spec for Claude Code skill; not auto-runnable'), docs-repro/tutorial-replay (kind: container; cadence: post-release; verifier.script: quality/gates/docs-repro/tutorial-replay.sh; freshness_ttl: null; SIMPLIFY-06 promotion target), benchmark-claim/8ms-cached-read (kind: manual; cadence: weekly; freshness_ttl: 30d; ports row from .planning/docs_reproducible_catalog.json:303), benchmark-claim/89.1-percent-token-reduction (kind: manual; cadence: weekly; freshness_ttl: 30d; ports row from .planning/docs_reproducible_catalog.json:331). Every row has the unified schema (id, dimension=docs-repro, cadence, kind, sources, command, expected, verifier, artifact, status=NOT-VERIFIED, last_verified=null, freshness_ttl, blast_radius, owner_hint, waiver=null)."
      contains: "docs-repro/example-01-shell-loop"
    - path: "quality/catalogs/agent-ux.json"
      provides: "1 agent-ux-dimension row: agent-ux/dark-factory-sim. cadence: pre-pr (per SIMPLIFY-07 — dark-factory-test.sh is currently invoked from CI on every PR via .github/workflows/ci.yml dark-factory job; preserve that cadence). kind: mechanical (the sim path runs against the local cargo workspace; no container required; the SIMPLIFY-07 migration target). verifier.script: quality/gates/agent-ux/dark-factory.sh. verifier.args: [sim]. expected.asserts: ['exit 0', 'stderr-teaching-strings-on-stderr asserted (the v0.9.0 dark-factory invariant)', 'no regression vs v0.9.0 baseline (per POLISH-AGENT-UX)']. blast_radius: P1. status=NOT-VERIFIED."
      contains: "agent-ux/dark-factory-sim"
    - path: "quality/catalogs/perf-targets.json"
      provides: "3 perf-dimension stub rows, all status=NOT-VERIFIED with a 90-day waiver tracked_in MIGRATE-03 v0.12.1: perf/latency-bench (relocate target for scripts/latency-bench.sh; future verifier in quality/gates/perf/latency-bench.sh; cadence: weekly), perf/token-economy-bench (relocate target for scripts/bench_token_economy.py; future verifier in quality/gates/perf/bench-token-economy.py; cadence: weekly), perf/headline-numbers-cross-check (full v0.12.1 implementation; the verifier compares headline numbers in docs/index.md + README.md against bench fixtures; cadence: weekly). All three rows have a waiver block: until=2026-07-26T00:00:00Z, reason='v0.12.1 stub per MIGRATE-03 — file-relocate only at v0.12.0; full gate logic deferred', dimension_owner='v0.12.0 P59 — see SURPRISES.md', tracked_in='MIGRATE-03 v0.12.1'. Every row has blast_radius P2 (perf is non-blocking at v0.12.0)."
      contains: "perf/latency-bench"
    - path: "quality/gates/docs-repro/README.md"
      provides: "Dimension home for docs-repro. <=50 lines. Sections: verifier table (3 rows: snippet-extract.py | container-rehearse.sh | tutorial-replay.sh), conventions (stdlib-only Python, ubuntu:24.04 default container, post-release cadence for container kind, ci_friendly: true, docker required gracefully NOT-VERIFIED if absent), pivot rules (lifted from prompt: container time budget >15min/release -> drop multi-persona; >50 fenced blocks -> allow-list mode; per-snippet rehearsal >60s -> post-release cadence only)."
      contains: "snippet-extract.py"
      max_lines: 50
    - path: "quality/gates/agent-ux/README.md"
      provides: "Dimension home for agent-ux. <=50 lines. Sections: verifier table (1 row: dark-factory.sh), 'intentionally sparse' note (perf + security stubs land in v0.12.1 per MIGRATE-03), source-script lineage (scripts/dark-factory-test.sh -> quality/gates/agent-ux/dark-factory.sh per SIMPLIFY-07; v0.9.0 dark-factory invariant preserved; CI job preserved at pre-pr cadence)."
      contains: "intentionally sparse"
      max_lines: 50
    - path: "quality/gates/perf/README.md"
      provides: "Dimension home for perf. <=50 lines. Sections: file-relocate-only callout (per SIMPLIFY-11 P59 scope: file moves only; full gate logic v0.12.1 stub per MIGRATE-03), source-script lineage (scripts/{bench_token_economy.py,test_bench_token_economy.py,latency-bench.sh} -> quality/gates/perf/{bench-token-economy.py,test-bench-token-economy.py,latency-bench.sh}; benchmarks/fixtures/* stays as test inputs), waiver explanation (3 rows in perf-targets.json carry 90d waivers tracked_in MIGRATE-03 v0.12.1)."
      contains: "file-relocate-only"
      max_lines: 50
  key_links:
    - from: "quality/catalogs/docs-reproducible.json"
      to: "quality/gates/docs-repro/{snippet-extract.py, container-rehearse.sh, tutorial-replay.sh} (Waves B + C)"
      via: "row.verifier.script field path"
      pattern: "quality/gates/docs-repro"
    - from: "quality/catalogs/agent-ux.json"
      to: "quality/gates/agent-ux/dark-factory.sh (Wave D)"
      via: "row.verifier.script field path"
      pattern: "quality/gates/agent-ux/dark-factory.sh"
    - from: "quality/catalogs/perf-targets.json"
      to: "quality/gates/perf/{latency-bench.sh, bench-token-economy.py} (Wave E file-relocate)"
      via: "row.verifier.script field path; the waiver makes the row WAIVED until v0.12.1"
      pattern: "quality/gates/perf"
    - from: "All 3 catalogs"
      to: "Wave F unbiased verifier subagent"
      via: "row.expected.asserts is the contract the verifier grades against; locked in this commit BEFORE Waves B-E land code"
      pattern: "expected"
    - from: ".planning/docs_reproducible_catalog.json (DEPRECATED seed)"
      to: "quality/catalogs/docs-reproducible.json (target)"
      via: "row port: tutorial/first-run-end-to-end -> docs-repro/tutorial-replay; benchmark-claim rows preserve id; install rows already migrated to release-assets.json by P58 (do NOT re-port)"
      pattern: "DEPRECATED seed"
---

<objective>
Wave A — the **catalog-first commit**. Per `quality/PROTOCOL.md` § "Per-phase protocol" Step 3 and recurring criterion 1 from `.planning/REQUIREMENTS.md` § "Recurring success criteria across every phase (P56-P63)": every phase's FIRST commit MUST write the catalog rows that define this phase's end-state contract. This wave does NOT write any verifier code — it only declares "here is what GREEN means for P59" so the unbiased verifier subagent in Wave F reads a contract that existed BEFORE the implementation landed.

Six deliverables ship in this commit:

1. **`quality/catalogs/docs-reproducible.json`** — 8 docs-repro rows. Five `examples/0[1-5]-*/` rows (4 container-kind for the auto-runnable shell/python wrappers + 1 manual-kind for the markdown skill spec, per the prompt's special-case for `examples/03-claude-code-skill/`). One `docs-repro/tutorial-replay` row (the SIMPLIFY-06 + DOCS-REPRO-03 promotion target for `scripts/repro-quickstart.sh`). Two benchmark-claim rows ported from the DEPRECATED `.planning/docs_reproducible_catalog.json` seed (the 5 install rows already moved to `release-assets.json` by P58 — do NOT re-port; the install rows have lineage tags pointing back to release-assets per the deprecation note in the seed file). The 3 quickstart/troubleshooting/repro rows from the seed are NOT ported because they have no concrete verifier yet (deferred to Wave B `snippet-extract.py` which will emit candidate rows for any uncatalogued snippet).

2. **`quality/catalogs/agent-ux.json`** — 1 row: `agent-ux/dark-factory-sim`. cadence: `pre-pr` (preserves the existing `ci.yml` dark-factory job cadence per CLAUDE.md "Local dev loop"). kind: `mechanical` (sim path; cargo workspace; no docker required). The SIMPLIFY-07 migration target.

3. **`quality/catalogs/perf-targets.json`** — 3 stub rows, all status=NOT-VERIFIED + 90-day waivers tracked_in MIGRATE-03 v0.12.1. Per the prompt: "P59 ships file move only; gate logic deferred to v0.12.1 per MIGRATE-03". The waivers convert the rows to status=WAIVED at runner time so they do not block GREEN.

4. **`quality/gates/docs-repro/README.md`** — dimension home, <=50 lines. Verifier table, conventions, pivot rules.

5. **`quality/gates/agent-ux/README.md`** — dimension home, <=50 lines. "Intentionally sparse — perf and security stubs land in v0.12.1" note.

6. **`quality/gates/perf/README.md`** — dimension home, <=50 lines. "File-relocate-only at v0.12.0; full gate logic v0.12.1 stub per MIGRATE-03" callout.

Why ship the README stubs in Wave A? Because the catalog rows reference these directories; the unbiased verifier in Wave F reads the README to confirm dimension scope. Anchoring all three READMEs in Wave A means the dimension contracts are complete before the verifier code exists.

Plus housekeeping: ensure the existing pre-push `scripts/check-quality-catalogs.py` (or `scripts/check_quality_catalogs.py` — verify which exists) parses the new catalog files without erroring (catalog shape passes existing gate). If the existing checker hard-errors on `docs-repro` / `agent-ux` / `perf` dimension values, extend its valid-dimension allow-list in the same commit. Anti-bloat: minimal patch — append values to a list, do not refactor.

Purpose: lock the contract. After this commit, downstream waves (B/C/D/E/F) cannot quietly drift the asserts. If Wave B's `snippet-extract.py` does not satisfy a row's `expected.asserts`, Wave F refuses to grade GREEN.

Output: 6 files committed in a single commit titled `docs(p59): catalog-first commit — DOCS-REPRO-04 + SIMPLIFY-06/07/11 stubs + dim READMEs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/v0.12.0/naming-and-architecture.md
@.planning/research/v0.12.0/roadmap-and-rationale.md
@.planning/docs_reproducible_catalog.json
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/catalogs/README.md
@quality/catalogs/release-assets.json
@quality/catalogs/freshness-invariants.json
@quality/catalogs/orphan-scripts.json
@quality/catalogs/code.json
@quality/runners/run.py
@quality/runners/verdict.py
@quality/reports/verdicts/p58/VERDICT.md
@quality/gates/release/README.md
@quality/gates/code/README.md
@quality/gates/structure/README.md
@scripts/repro-quickstart.sh
@scripts/dark-factory-test.sh
@scripts/check-quality-catalogs.py
@scripts/check_quality_catalogs.py
@examples/README.md
@examples/01-shell-loop/run.sh
@examples/02-python-agent/run.py
@examples/04-conflict-resolve/run.sh
@examples/05-blob-limit-recovery/run.sh
@examples/03-claude-code-skill/RUN.md
@CLAUDE.md

<interfaces>
**Catalog row schema (verbatim from `quality/catalogs/README.md`; same shape as P58 release-assets rows):**

```jsonc
{
  "id":            "docs-repro/<slug>",                    // <dimension>/<slug> — stable, never reused
  "dimension":     "docs-repro",                            // for agent-ux: "agent-ux"; for perf: "perf"
  "cadence":       "post-release",                          // pre-push|pre-pr|weekly|pre-release|post-release|on-demand
  "kind":          "container",                             // mechanical|container|asset-exists|subagent-graded|manual
  "sources":       ["docs/tutorials/first-run.md:51-166"],
  "command":       "bash examples/01-shell-loop/run.sh",   // null for non-runnable rows
  "expected":      { "asserts": ["..."] },
  "verifier":      {
    "script":      "quality/gates/docs-repro/container-rehearse.sh",
    "args":        ["docs-repro/example-01-shell-loop"],
    "timeout_s":   180,
    "container":   "ubuntu:24.04"                           // null for kind != "container"
  },
  "artifact":      "quality/reports/verifications/docs-repro/example-01-shell-loop.json",
  "status":        "NOT-VERIFIED",                          // Wave A; Waves B-F update
  "last_verified": null,
  "freshness_ttl": null,                                    // "30d" / "90d" for manual rows
  "blast_radius":  "P0|P1|P2",
  "owner_hint":    "...",
  "waiver":        null                                     // {until, reason, dimension_owner, tracked_in} or null
}
```

**Catalog file wrapper format** (per P58 precedent in `quality/catalogs/release-assets.json`):

```jsonc
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "comment": "Quality Gates catalog -- docs-repro dimension. Schema spec: quality/catalogs/README.md. Verifier impl: quality/gates/docs-repro/. Reports: quality/reports/verifications/docs-repro/.",
  "dimension": "docs-repro",
  "rows": [ /* 8 rows */ ]
}
```

**The 8 docs-reproducible.json rows in order (Group 1: examples 5 rows; Group 2: tutorial-replay 1 row; Group 3: benchmark-claims 2 rows):**

**Group 1 -- examples/0[1-5]-*/run rows (5 rows; SIMPLIFY-06 part 2 contract).**

The five examples are different shapes per `examples/README.md`:
- `01-shell-loop`: bash run.sh, ~10s
- `02-python-agent`: python run.py, ~15s
- `03-claude-code-skill`: markdown agent-prompt.md (no run script; Claude Code skill spec)
- `04-conflict-resolve`: bash run.sh, ~20s
- `05-blob-limit-recovery`: bash run.sh, ~15s

**Row 1 -- `docs-repro/example-01-shell-loop`:**
- dimension: `"docs-repro"`, cadence: `"post-release"` (container rehearsal cost-budget post-release per OP-1; not pre-push), kind: `"container"`
- sources: `["examples/01-shell-loop/run.sh", "examples/README.md:6"]`
- command: `"bash examples/01-shell-loop/run.sh"`
- expected.asserts:
  - `"in a fresh ubuntu:24.04 container with cargo + python3 + curl pre-installed and the workspace cargo build complete, bash examples/01-shell-loop/run.sh exits 0"`
  - `"the run leaves a partial-clone working tree at /tmp/reposix-example-01"`
  - `"the example pushes a commit and the simulator's audit log shows a helper_push_* row"`
- verifier.script: `"quality/gates/docs-repro/container-rehearse.sh"`, args: `["docs-repro/example-01-shell-loop"]`, timeout_s: `300`, container: `"ubuntu:24.04"`
- artifact: `"quality/reports/verifications/docs-repro/example-01-shell-loop.json"`
- status: `"NOT-VERIFIED"`, last_verified: `null`, freshness_ttl: `null`
- blast_radius: `"P1"`, owner_hint: `"if RED: examples/01-shell-loop/run.sh has a regression OR the simulator/cli/helper changed and the example was not updated; check examples/01-shell-loop/expected-output.md and the binary on PATH preamble"`, waiver: `null`

**Row 2 -- `docs-repro/example-02-python-agent`:** same shape as Row 1 against `examples/02-python-agent/run.py`.
- command: `"python3 examples/02-python-agent/run.py"`
- expected.asserts: `["in a fresh ubuntu:24.04 container, python3 examples/02-python-agent/run.py exits 0", "the agent finds 'database' in some issue body and applies a severity label", "git push reports success"]`
- verifier.args: `["docs-repro/example-02-python-agent"]`
- blast_radius: `"P1"`, owner_hint: same shape

**Row 3 -- `docs-repro/example-03-claude-code-skill`:**
- kind: `"manual"` (markdown agent-prompt; not auto-runnable in container per the prompt's special-case)
- cadence: `"on-demand"`
- freshness_ttl: `"90d"` (per the prompt: 90-day TTL because manual)
- command: `null`
- expected.asserts: `["examples/03-claude-code-skill/RUN.md exists and references .claude/skills/reposix-agent-flow", "examples/03-claude-code-skill/agent-prompt.md exists and parses as markdown", "this row is graded by manual verifier-subagent dispatch (not auto-runnable)"]`
- verifier.script: `"quality/gates/docs-repro/manual-spec-check.sh"` (very thin; asserts the two files exist + parse). If we decide manual-spec-check is overkill, leave script null and document the row explicitly as manual-grade-only.
- verifier.args: `["docs-repro/example-03-claude-code-skill"]`
- blast_radius: `"P2"`, owner_hint: `"if RED: examples/03-claude-code-skill/{RUN.md, agent-prompt.md} got out of sync with .claude/skills/reposix-agent-flow"`, waiver: `null`

**Row 4 -- `docs-repro/example-04-conflict-resolve`:** same shape as Row 1 against `examples/04-conflict-resolve/run.sh`.
- command: `"bash examples/04-conflict-resolve/run.sh"`
- expected.asserts: `["bash examples/04-conflict-resolve/run.sh exits 0 in ubuntu:24.04 container", "the second agent observes 'fetch first' from helper stderr and recovers via git pull --rebase", "after recovery, both edits are merged"]`
- verifier.args: `["docs-repro/example-04-conflict-resolve"]`
- blast_radius: `"P1"`, owner_hint: `"if RED: push-time conflict detection regressed; check the helper export-path conflict logic"`

**Row 5 -- `docs-repro/example-05-blob-limit-recovery`:** same shape as Row 1 against `examples/05-blob-limit-recovery/run.sh`.
- command: `"bash examples/05-blob-limit-recovery/run.sh"`
- expected.asserts: `["bash examples/05-blob-limit-recovery/run.sh exits 0 in ubuntu:24.04 container", "the agent observes the blob-limit error from helper stderr and recovers via git sparse-checkout", "after recovery, the targeted issue blob is materialized while siblings stay sparse"]`
- verifier.args: `["docs-repro/example-05-blob-limit-recovery"]`
- blast_radius: `"P1"`, owner_hint: `"if RED: blob-limit error message regressed OR sparse-checkout recovery path regressed"`

**Group 2 -- tutorial-replay (1 row; SIMPLIFY-06 part 1 + DOCS-REPRO-03).**

**Row 6 -- `docs-repro/tutorial-replay`:**
- dimension: `"docs-repro"`, cadence: `"post-release"`, kind: `"container"`
- sources: `["docs/tutorials/first-run.md:51-166", "scripts/repro-quickstart.sh", "README.md:56-70"]`
- command: `"bash quality/gates/docs-repro/tutorial-replay.sh"` (the post-Wave-C path; Wave A locks the contract; Wave C ships the file)
- expected.asserts: ports the existing scripts/repro-quickstart.sh assertions:
  - `"step 2: simulator starts and listens on 127.0.0.1:7878"`
  - `"step 3: reposix init sim::demo /tmp/repo exits 0 and configures partial-clone"`
  - `"step 4: git checkout -B main refs/reposix/origin/main exits 0"`
  - `"step 5: issues/0001.md exists with title: frontmatter"`
  - `"step 7: git push reports [new branch] main -> main"`
  - `"step 8: audit_events_cache has a row with op LIKE 'helper_push_%'"`
- verifier.script: `"quality/gates/docs-repro/tutorial-replay.sh"`, args: `[]`, timeout_s: `300`, container: `"ubuntu:24.04"`
- artifact: `"quality/reports/verifications/docs-repro/tutorial-replay.json"`
- blast_radius: `"P0"` (tutorial is the headline conversion path per .planning/docs_reproducible_catalog.json:251), owner_hint: `"if RED: docs/tutorials/first-run.md or one of the binary build steps regressed; check scripts/repro-quickstart.sh predecessor for the canonical assertions"`, waiver: `null`

**Group 3 -- benchmark-claims (2 rows; ports from .planning/docs_reproducible_catalog.json).**

**Row 7 -- `benchmark-claim/8ms-cached-read`:** ports row from `.planning/docs_reproducible_catalog.json:303`.
- dimension: `"docs-repro"`, cadence: `"weekly"`, kind: `"manual"` (the benchmark itself runs via cron in `.github/workflows/bench-latency-cron.yml`; this row tracks freshness + headline-cross-check delta — full automation is v0.12.1 SIMPLIFY-11 perf cross-check)
- freshness_ttl: `"30d"`
- sources: `["docs/index.md:14", "README.md:21", ".planning/docs_reproducible_catalog.json:303"]`
- command: `null` (claim is grader-evaluated against bench output)
- expected.asserts:
  - `"docs/benchmarks/latency.md frontmatter last_measured_at is < 30 days old"`
  - `"the cached-read p50 cell value matches the headline 8 ms to within +/-2ms"`
- verifier.script: `null` (manual at v0.12.0; full automation per perf/headline-numbers-cross-check stub in perf-targets.json)
- verifier.args: `[]`, timeout_s: `60`, container: `null`
- artifact: `"quality/reports/verifications/docs-repro/benchmark-claim-8ms-cached-read.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P2"` (per source row), waiver: `null`

**Row 8 -- `benchmark-claim/89.1-percent-token-reduction`:** ports row from `.planning/docs_reproducible_catalog.json:331`.
- dimension: `"docs-repro"`, cadence: `"weekly"`, kind: `"manual"`
- freshness_ttl: `"30d"`
- sources: `["docs/index.md:13", "README.md:23", ".planning/docs_reproducible_catalog.json:331"]`
- command: `null`
- expected.asserts:
  - `"headline 89.1% is greppable from docs/benchmarks/token-economy.md"`
  - `"the comparison fixture file referenced from token-economy.md exists"`
- verifier.script: `null`, blast_radius: `"P2"`, waiver: `null`

**The 1 agent-ux.json row:**

**Row 1 -- `agent-ux/dark-factory-sim`:**
- dimension: `"agent-ux"`, cadence: `"pre-pr"` (preserves CLAUDE.md "Local dev loop" section "Dark-factory regression"), kind: `"mechanical"` (the script runs against the local cargo workspace + sim)
- sources: `["scripts/dark-factory-test.sh", "CLAUDE.md (Local dev loop section)"]`
- command: `"bash quality/gates/agent-ux/dark-factory.sh sim"`
- expected.asserts:
  - `"bash quality/gates/agent-ux/dark-factory.sh sim exits 0 against the local cargo workspace"`
  - `"the helper's stderr-teaching strings emit on the conflict and blob-limit paths (the v0.9.0 dark-factory invariant)"`
  - `"reposix init sim::demo <path> configures partial-clone correctly"`
  - `"no regression vs v0.9.0 baseline (POLISH-AGENT-UX broaden-and-deepen)"`
- verifier.script: `"quality/gates/agent-ux/dark-factory.sh"`, args: `["sim"]`, timeout_s: `120`, container: `null`
- artifact: `"quality/reports/verifications/agent-ux/dark-factory-sim.json"`
- status: `"NOT-VERIFIED"`, last_verified: `null`, freshness_ttl: `null`
- blast_radius: `"P1"`, owner_hint: `"if RED: scripts/dark-factory-test.sh (pre-migration) or quality/gates/agent-ux/dark-factory.sh (post-migration) finds the helper teaching strings missing -- inspect helper export-path / fetch-blob-limit code"`, waiver: `null`

**The 3 perf-targets.json stub rows (all WAIVED until 2026-07-26):**

**Common waiver block** (used in all 3 perf rows):

```jsonc
"waiver": {
  "until":           "2026-07-26T00:00:00Z",
  "reason":          "v0.12.1 stub per MIGRATE-03 -- file-relocate only at v0.12.0; full gate logic deferred (latency cross-check + token-economy bench cross-check + headline-numbers cross-check require new verifier code that did not fit in v0.12.0 P59 scope per the planner authority limits in REQUIREMENTS.md)",
  "dimension_owner": "v0.12.0 P59 -- see SURPRISES.md (will be appended at Wave A close if pivoted)",
  "tracked_in":      "MIGRATE-03 v0.12.1 (perf-dimension full implementation per REQUIREMENTS.md § Out of Scope)"
}
```

**Row 1 -- `perf/latency-bench`:**
- dimension: `"perf"`, cadence: `"weekly"`, kind: `"mechanical"`
- sources: `["scripts/latency-bench.sh", ".github/workflows/bench-latency-cron.yml"]`
- command: `"bash quality/gates/perf/latency-bench.sh sim"`
- expected.asserts: `["bash quality/gates/perf/latency-bench.sh sim exits 0", "the bench output (markdown table) is greppable from docs/benchmarks/latency.md", "[v0.12.1] the latency cells in docs/benchmarks/latency.md match the bench output to within +/-2ms"]`
- verifier.script: `"quality/gates/perf/latency-bench.sh"`, args: `["sim"]`, timeout_s: `300`, container: `null`
- artifact: `"quality/reports/verifications/perf/latency-bench.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P2"`, waiver: <common block>

**Row 2 -- `perf/token-economy-bench`:**
- dimension: `"perf"`, cadence: `"weekly"`, kind: `"mechanical"`
- sources: `["scripts/bench_token_economy.py", "scripts/test_bench_token_economy.py"]`
- command: `"python3 quality/gates/perf/bench-token-economy.py"`
- expected.asserts: `["python3 quality/gates/perf/bench-token-economy.py exits 0", "the bench output is greppable from docs/benchmarks/token-economy.md", "[v0.12.1] the headline 89.1% in docs/index.md + README.md matches the bench output"]`
- verifier.script: `"quality/gates/perf/bench-token-economy.py"`, args: `[]`, timeout_s: `120`, container: `null`
- artifact: `"quality/reports/verifications/perf/token-economy-bench.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P2"`, waiver: <common block>

**Row 3 -- `perf/headline-numbers-cross-check`:**
- dimension: `"perf"`, cadence: `"weekly"`, kind: `"mechanical"`
- sources: `["docs/index.md:13-14", "README.md:21,23", "docs/benchmarks/latency.md", "docs/benchmarks/token-economy.md"]`
- command: `"python3 quality/gates/perf/headline-numbers-cross-check.py"` (file does not exist at v0.12.0; v0.12.1 ships it)
- expected.asserts: `["[v0.12.1] every headline number in docs/index.md + README.md is greppable from a benchmark fixture file", "[v0.12.1] the cross-check fails RED if any headline drifts from its bench source"]`
- verifier.script: `"quality/gates/perf/headline-numbers-cross-check.py"`, args: `[]`, timeout_s: `30`, container: `null`
- artifact: `"quality/reports/verifications/perf/headline-numbers-cross-check.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P2"`, waiver: <common block>

**Three dimension READMEs (anti-bloat <=50 lines each):**

`quality/gates/docs-repro/README.md` shape (model on `quality/gates/release/README.md`):
- H1: `# quality/gates/docs-repro/`
- one-line description: "Verifiers backing `quality/catalogs/docs-reproducible.json` (8 rows, docs-repro dimension)."
- verifier table: `snippet-extract.py` | rows it backs | cadence (pre-push: drift detector); `container-rehearse.sh` | rows it backs (4 example container rows + tutorial-replay) | cadence (post-release); `tutorial-replay.sh` | docs-repro/tutorial-replay | post-release
- conventions section: stdlib-only Python (argparse/json/pathlib/subprocess/urllib.request/datetime/sys/hashlib/re); ubuntu:24.04 default container; pre-installs `curl ca-certificates`; ci_friendly: true; docker required (gracefully NOT-VERIFIED if absent — does not crash the runner); per-verifier line caps (snippet-extract.py <=250 lines; container-rehearse.sh <=150 lines)
- pivot rules section (lift verbatim from prompt): >15min/release container budget -> drop multi-persona; ubuntu-only first; >50 fenced blocks -> allow-list mode; per-snippet rehearsal >60s -> post-release cadence only

`quality/gates/agent-ux/README.md` shape:
- H1: `# quality/gates/agent-ux/`
- "Intentionally sparse at v0.12.0 — the dark-factory regression is the only gate. Perf and security stubs land in v0.12.1 per `MIGRATE-03`."
- verifier table: `dark-factory.sh` | `agent-ux/dark-factory-sim` | pre-pr
- source-script lineage: `scripts/dark-factory-test.sh` -> `quality/gates/agent-ux/dark-factory.sh` per SIMPLIFY-07 (Wave D); v0.9.0 dark-factory invariant preserved verbatim; CI job at `.github/workflows/ci.yml` continues invoking the migrated path
- cross-references: `quality/PROTOCOL.md`, `quality/SURPRISES.md`, CLAUDE.md "Local dev loop" section

`quality/gates/perf/README.md` shape:
- H1: `# quality/gates/perf/`
- "**File-relocate only at v0.12.0**" callout; "Full gate logic deferred to v0.12.1 stub per `MIGRATE-03`."
- source-script lineage: `scripts/latency-bench.sh` -> `quality/gates/perf/latency-bench.sh`; `scripts/bench_token_economy.py` -> `quality/gates/perf/bench-token-economy.py`; `scripts/test_bench_token_economy.py` -> `quality/gates/perf/test-bench-token-economy.py`; `benchmarks/fixtures/*` stays in place (test inputs, not gates)
- waiver explanation: 3 catalog rows in `perf-targets.json` carry a 90-day waiver tracked_in MIGRATE-03 v0.12.1; expiry is 2026-07-26
- cross-references: `quality/catalogs/perf-targets.json`, `MIGRATE-03` carry-forward in REQUIREMENTS.md

**Pre-push catalog-shape gate**

The existing `scripts/check-quality-catalogs.py` (or `check_quality_catalogs.py`) parses every `quality/catalogs/*.json` file at pre-push time per P57 Wave B. Verify it accepts the 3 new dimension values (`docs-repro`, `agent-ux`, `perf`). If the script hard-codes a valid-dimension allow-list, append the missing values. Anti-bloat: the patch is a single-line list extension.

**Existing catalog precedents to mirror**

Use `quality/catalogs/release-assets.json` as the structural template (15 rows with the unified schema). Use `quality/catalogs/code.json` for waiver block format (the 2 cargo-test-pass + cargo-fmt-clean WAIVED rows already wear the same waiver-block shape proposed here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 3 catalogs in a single atomic commit</name>
  <files>quality/catalogs/docs-reproducible.json, quality/catalogs/agent-ux.json, quality/catalogs/perf-targets.json</files>
  <action>Create all three catalog files using the unified schema documented in `quality/catalogs/README.md`. Mirror the structural style of `quality/catalogs/release-assets.json` (P58 reference). Use the row specifications in the `<interfaces>` block above verbatim. EVERY row has `status: "NOT-VERIFIED"` and `last_verified: null` in this commit (the runner sets PASS/FAIL/PARTIAL after Waves B-E ship the verifiers; Wave F re-runs the full sweep). The 3 perf rows wear the common waiver block (until=2026-07-26, tracked_in=MIGRATE-03 v0.12.1) — when the runner sees the in-effect waiver it grades the row WAIVED, not NOT-VERIFIED, so the rows do not block GREEN. Do NOT write a verifier file in this commit. Do NOT extend `scripts/check-quality-catalogs.py` here unless it hard-errors on `docs-repro` / `agent-ux` / `perf` dimension values — if it does, append the values to the allow-list in this same commit (do not refactor the script).</action>
  <verify>
    <automated>python3 -c "import json; [json.loads(open(p).read()) for p in ['quality/catalogs/docs-reproducible.json','quality/catalogs/agent-ux.json','quality/catalogs/perf-targets.json']]" && python3 -c "import json; rows=[r for f in ['quality/catalogs/docs-reproducible.json','quality/catalogs/agent-ux.json','quality/catalogs/perf-targets.json'] for r in json.loads(open(f).read())['rows']]; assert len(rows)==12, f'expected 12 rows, got {len(rows)}'; assert all(r['status']=='NOT-VERIFIED' for r in rows), 'every row must be NOT-VERIFIED in Wave A'; print(f'OK: {len(rows)} rows committed; all NOT-VERIFIED')" && python3 quality/runners/run.py --cadence pre-push 2>&1 | tail -5</automated>
  </verify>
  <done>3 catalog files parse as JSON; 12 rows total (8 docs-repro + 1 agent-ux + 3 perf); every row is NOT-VERIFIED; the 3 perf rows wear the documented waiver block; pre-push runner does not crash on the new files.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 3 dimension READMEs (docs-repro / agent-ux / perf)</name>
  <files>quality/gates/docs-repro/README.md, quality/gates/agent-ux/README.md, quality/gates/perf/README.md</files>
  <action>Create all three README files following the `<interfaces>` shape spec above. Use `quality/gates/release/README.md` as the structural template. Each file is <=50 lines. The docs-repro README documents the 3 verifiers Waves B-C will ship + conventions + pivot rules verbatim from the prompt. The agent-ux README documents "intentionally sparse" + the dark-factory.sh migration source. The perf README documents "file-relocate only at v0.12.0" + the SIMPLIFY-11 source-script lineage + the v0.12.1 carry-forward via MIGRATE-03. Banned word check: do NOT use `replace`; use `migrate to` / `rewrite as` / `alongside` per the prompt + CLAUDE.md banned-words rule.</action>
  <verify>
    <automated>for f in quality/gates/docs-repro/README.md quality/gates/agent-ux/README.md quality/gates/perf/README.md; do test -s "$f" || { echo "MISSING: $f"; exit 1; }; lines=$(wc -l < "$f"); test "$lines" -le 50 || { echo "TOO LONG ($lines): $f"; exit 1; }; grep -l '\breplace\b' "$f" && { echo "BANNED WORD in $f"; exit 1; } || true; done && echo OK</automated>
  </verify>
  <done>3 READMEs exist; each <=50 lines; no banned word `replace`; each references its catalog file + verifier list + conventions/pivot rules per the spec.</done>
</task>

<task type="auto">
  <name>Task 3: Verify pre-push gate parses the new catalogs (extend allow-list if needed)</name>
  <files>scripts/check-quality-catalogs.py, scripts/check_quality_catalogs.py</files>
  <action>Locate the existing pre-push catalog-shape gate (one of the two filenames above; verify which exists at this commit). Run it. If it hard-errors on the new `docs-repro`, `agent-ux`, or `perf` dimension values (because of a hard-coded allow-list), extend the allow-list with the missing values (single-line edit). Anti-bloat: do NOT refactor; do NOT add new logic. If the gate already accepts arbitrary dimension strings, no edit needed; document the no-op in the task summary. Re-run pre-push to confirm GREEN.</action>
  <verify>
    <automated>(test -f scripts/check-quality-catalogs.py && python3 scripts/check-quality-catalogs.py) || (test -f scripts/check_quality_catalogs.py && python3 scripts/check_quality_catalogs.py) || true; bash scripts/hooks/pre-push 2>&1 | tail -10</automated>
  </verify>
  <done>Pre-push gate exits 0 OR documents which row(s) the gate flagged; if the gate flagged the new dimension values, the allow-list is extended in this same commit (no separate commit).</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + sanity sweep</name>
  <files>(commit-only; no new files written here)</files>
  <action>Commit all 6 files in a single atomic commit. Title: `docs(p59): catalog-first commit -- DOCS-REPRO-04 + SIMPLIFY-06/07/11 stubs + dim READMEs`. Body cites the catalog row counts (8 docs-repro + 1 agent-ux + 3 perf = 12 rows; 3 perf rows WAIVED until 2026-07-26 per MIGRATE-03 v0.12.1) and references this plan file. Run the full pre-push runner sweep one more time to confirm no regression introduced.</action>
  <verify>
    <automated>git log -1 --format='%s' | grep -F 'docs(p59)' && git diff HEAD~1 --name-only | sort | grep -E '^(quality/catalogs/(docs-reproducible|agent-ux|perf-targets)\.json|quality/gates/(docs-repro|agent-ux|perf)/README\.md)$' | wc -l | grep -q '^6$' && python3 quality/runners/run.py --cadence pre-push && echo "OK: Wave A clean"</automated>
  </verify>
  <done>Single commit lands all 6 files; pre-push runner exit 0; commit subject names the catalog-first contract; no other files modified except the optional 1-line allow-list patch in `scripts/check-quality-catalogs.py`.</done>
</task>

</tasks>

<verification>
After Wave A close:
- `python3 quality/runners/run.py --cadence pre-push` exits 0.
- `python3 quality/runners/run.py --cadence post-release` shows the 5 new container-kind rows in scope but graded NOT-VERIFIED (because Wave A did not ship verifiers); the runner does not crash. The 3 perf rows show as WAIVED.
- `git diff HEAD~1 --name-only` lists exactly the 6 files (or 7 if the catalog-shape gate allow-list patch is included).
- The unbiased verifier subagent in Wave F can read every catalog row's `expected.asserts` and grade against the artifacts produced by Waves B-E.
</verification>

<success_criteria>
1. Catalog-first per recurring criterion 1: 3 catalog files + 3 dimension READMEs land in a single atomic commit BEFORE any verifier code (Waves B-E).
2. CLAUDE.md update is NOT in scope for Wave A — it lands in Wave F per QG-07 (the same-PR rule applies at phase close, not per-wave).
3. Verifier-subagent dispatch is NOT in scope for Wave A — Wave F does it.
4. SIMPLIFY absorption is contracted in Wave A (the catalog rows reference the migration targets); the actual file moves land in Waves C/D/E.
5. RED-row fix is NOT in scope for Wave A — the 3 perf rows are pre-emptively WAIVED so they cannot grade RED; everything else is NOT-VERIFIED, which Waves B-E will resolve.
</success_criteria>

<pivot_rules>
- **If `scripts/check-quality-catalogs.py` is structurally hostile to the new dimension values** (e.g. it does dimension-specific switch logic, not a simple allow-list): document the obstacle as a SURPRISES.md entry; ship the catalogs anyway with status NOT-VERIFIED; mark the pre-push gate flag as a known-issue carry-forward to Wave B (which can refactor the checker as part of the snippet-extract.py work).
- **If the JSON for the 3 perf waivers triggers an em-dash idempotency regression** (per `quality/SURPRISES.md` 2026-04-27 P57 Wave B entries): use literal ASCII hyphens in the waiver-text strings; the SURPRISES journal explains the encoding gotcha.
- **If extending the catalog file count breaks the runner's catalog-discovery pattern** (e.g. P58 hard-coded the catalog file list rather than globbing `quality/catalogs/*.json`): inspect `discover_catalogs()` in `quality/runners/run.py`. If hard-coded: document in SURPRISES + extend the list as a separate commit BEFORE this Wave A commit. If globbed: no action needed (P57 design used glob per `quality/runners/run.py:67-70` per memory of P57 Wave B).

Reference: `quality/SURPRISES.md` (12 entries: 5 P56 + 3 P57 + 7 P58); `.planning/phases/58-release-dimension-gates-code-absorption/58-01-PLAN.md` (Wave A precedent for catalog-first).
</pivot_rules>

<risk_table>
| Risk | Likelihood | Mitigation |
|---|---|---|
| Catalog row drift between Wave A spec and Wave B/C/D/E implementation | medium | Wave A locks `expected.asserts` verbatim from the prompt; Waves B-E read this plan file at start; Wave F's unbiased verifier cross-checks against the catalog |
| Pre-push catalog-shape gate hard-codes dimension allow-list and rejects `docs-repro`/`agent-ux`/`perf` | low-medium | Task 3 inspects + extends the allow-list in the same commit; if the gate is structurally hostile, the pivot rule kicks in |
| The `examples/03-claude-code-skill` row design (manual + ttl=90d) is awkward — the runner currently does not enforce ttl for manual rows | low | The row's `kind: "manual"` + `freshness_ttl: "90d"` documents the contract; the runner-side ttl-enforcement comes in v0.12.1 SUBJ-* work; the catalog row is forward-compatible. Wave F verifier accepts NOT-VERIFIED + manual + ttl as the spec'd contract |
| Catalog file count exceeds an unstated runner glob pattern | low | Inspect `discover_catalogs()` in Task 4 verify; if hard-coded list found, extend in same commit |
| Em-dash idempotency regression (P57 SURPRISES row 1) | low | Mitigation: keep all em-dash characters in catalog comments + waiver reasons as literal ASCII hyphens; do NOT use unicode em-dashes inside JSON strings |
| The 3 perf-targets rows are pre-emptively WAIVED and may look like silent-downgrade — the planner authority limit only allows split / waiver under specific conditions | low | The waiver is explicitly justified by the planner-authority-limit "context cost" rule (full perf cross-check verifier requires ~250 lines of new Python that did not fit in v0.12.0 P59 scope) AND the dependency-conflict rule (the cross-check needs latency-bench output + token-economy bench output with stable schemas, both v0.12.1 work). Tracked_in MIGRATE-03 v0.12.1 makes the carry-forward explicit |
</risk_table>

<output>
After completion, create `.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-01-SUMMARY.md` capturing:
- 3 catalog files committed with row counts (8 + 1 + 3)
- 3 dimension READMEs committed (with line counts confirming <=50 each)
- Whether the pre-push catalog-shape gate needed an allow-list extension
- Any SURPRISES.md entry appended (if pivot rules fired)
- Pre-push + post-release runner outputs after Wave A close
</output>
