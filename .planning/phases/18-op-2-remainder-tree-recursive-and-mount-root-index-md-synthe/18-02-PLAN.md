---
phase: 18
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - CHANGELOG.md
  - scripts/dev/test-tree-index.sh
autonomous: true
requirements:
  - INDEX-01
  - INDEX-02

must_haves:
  truths:
    - "Full workspace test suite passes with no regressions"
    - "CHANGELOG.md records Phase 18 under [Unreleased] with both features listed"
    - "A dev script demonstrates tree-level and mount-root `_INDEX.md` reads (mirrors Phase 15's test-bucket-index.sh)"
    - "Phase 18 SUMMARY.md captures implementation decisions for future agents"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Phase 18 feature entries under [Unreleased]"
      contains: "tree/<subdir>/_INDEX.md"
    - path: "scripts/dev/test-tree-index.sh"
      provides: "Executable demo + smoke test for both new synthetic files"
  key_links:
    - from: "scripts/dev/test-tree-index.sh"
      to: "reposix-fuse mount"
      via: "cargo run -p reposix-fuse + fusermount3"
      pattern: "cat.*_INDEX.md"
---

<objective>
Finalize Phase 18: workspace-wide green-gauntlet pass, CHANGELOG update, a dev smoke-test script for the two new `_INDEX.md` levels, and the phase SUMMARY.

Purpose: Ensure the implementation is fully integrated and documented before the phase is declared done. The smoke script gives future agents and developers a concrete, executable proof of the feature.

Output: Passing `cargo test --workspace` + `cargo clippy --workspace`, a CHANGELOG entry, `scripts/dev/test-tree-index.sh`, and `18-SUMMARY.md`.
</objective>

<execution_context>
@/home/reuben/.claude/get-shit-done/workflows/execute-plan.md
@/home/reuben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/reuben/workspace/reposix/.planning/PROJECT.md
@/home/reuben/workspace/reposix/.planning/ROADMAP.md
@/home/reuben/workspace/reposix/.planning/STATE.md
@/home/reuben/workspace/reposix/.planning/phases/18-op-2-remainder-tree-recursive-and-mount-root-index-md-synthe/18-01-SUMMARY.md
@/home/reuben/workspace/reposix/.planning/phases/15-dynamic-index-md-synthesized-in-fuse-bucket-directory-op-2-p/15-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: workspace-wide test gate + CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <action>
    **Test gate:**
    Run the full workspace test suite and clippy:
    ```bash
    cargo test --workspace --quiet
    cargo clippy --workspace --all-targets -- -D warnings
    cargo fmt --all --check
    ```
    Fix any failures before touching CHANGELOG. Common issues to expect:
    - Non-exhaustive match warnings if any FUSE callback has a `match InodeKind::classify(...)` that didn't get the new arms — add them.
    - Clippy lint `clippy::pedantic` on new functions — suppress specific lints with `#[allow(...)]` and a rationale comment if genuinely not fixable.

    **CHANGELOG update:**
    Read `CHANGELOG.md` and locate the `[Unreleased]` section. Add under `### Added`:

    ```markdown
    - **OP-2 remainder (Phase 18):** `mount/tree/<subdir>/_INDEX.md` — recursive subtree sitemap
      synthesized via DFS from `TreeSnapshot`; YAML frontmatter + pipe-table with `depth | name | target`
      columns; all descendants listed (full DFS, not just direct children). Inode allocated dynamically
      from `AtomicU64` in the reserved `7..=0xFFFF` range, one per tree directory.
    - **OP-2 remainder (Phase 18):** `mount/_INDEX.md` — whole-mount overview listing `.gitignore`,
      `<bucket>/` (with issue count), and `tree/` (when hierarchy is active). `ROOT_INDEX_INO = 6`.
      Completes OP-2 started in Phase 15; agents can now `cat` any level of the mount hierarchy.
    ```

    Keep existing entries intact. Do not promote `[Unreleased]` to a version tag (that is a user gate).
  </action>
  <verify>
    <automated>cargo test --workspace --quiet 2>&1 | tail -5 && cargo clippy --workspace --all-targets -- -D warnings 2>&1 | grep -c "^error" || true</automated>
  </verify>
  <done>
    - `cargo test --workspace --quiet` exits 0 with no test failures
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0 with no errors
    - `cargo fmt --all --check` exits 0
    - CHANGELOG.md `[Unreleased]` section contains both Phase 18 feature bullets
  </done>
</task>

<task type="auto">
  <name>Task 2: dev smoke script + phase SUMMARY</name>
  <files>scripts/dev/test-tree-index.sh</files>
  <action>
    **Smoke script** at `scripts/dev/test-tree-index.sh`:

    Mirror `scripts/dev/test-bucket-index.sh` (Phase 15). The script must:
    1. Start the simulator (`cargo run -p reposix-sim -- --port 7778` in the background, wait for it to be ready via `curl --retry`)
    2. Mount the FUSE filesystem on a temp dir (`cargo run -p reposix-fuse -- <mount> --origin http://127.0.0.1:7778 --project demo`)
    3. Seed at least two Confluence-style pages with a parent/child relationship so `tree/` is populated (POST two issues via curl: one with no parent, one with `parent_id` set to the first)
    4. Trigger a readdir refresh by running `ls <mount>/pages/` (forces `refresh_issues` → tree snapshot build)
    5. Assert `cat <mount>/_INDEX.md` succeeds and output contains `kind: mount-index`
    6. Find a tree subdirectory with `ls <mount>/tree/` and assert `cat <mount>/tree/<first-entry>/_INDEX.md` succeeds and output contains `kind: tree-index`
    7. Assert that `touch <mount>/_INDEX.md` fails (EROFS check)
    8. Assert that `echo x > <mount>/tree/<first-entry>/_INDEX.md` fails (EROFS check)
    9. Unmount cleanly with `fusermount3 -u <mount>` (or `fusermount -u` on older systems)
    10. Print `PASS` on success; non-zero exit on any assertion failure

    Use `set -euo pipefail` and a `cleanup()` trap for the unmount.

    The script is a manual/CI convenience tool; it does NOT need to be wired into `cargo test`. Make it executable (`chmod +x`).

    **SUMMARY** — write `.planning/phases/18-op-2-remainder-tree-recursive-and-mount-root-index-md-synthe/18-SUMMARY.md` using the GSD summary template (`@/home/reuben/.claude/get-shit-done/templates/summary.md`). Key fields to fill:

    - `phase`: 18
    - `name`: OP-2 remainder — tree-recursive and mount-root `_INDEX.md` synthesis
    - `status`: complete
    - `requirements_closed`: INDEX-01, INDEX-02
    - `new_constants`: ROOT_INDEX_INO=6, TREE_INDEX_ALLOC_START=7, TREE_INDEX_ALLOC_END=0xFFFF
    - `new_functions`: render_tree_index, render_mount_root_index, tree_dir_index_ino, tree_dir_index_bytes_or_render, mount_root_index_bytes_or_render, synthetic_file_attr
    - `new_struct_fields`: mount_root_index_bytes, tree_dir_index_cache, tree_index_inodes, tree_index_ino_reverse, tree_index_alloc
    - `test_count_delta`: +6 (or the actual count from Wave A)
    - `decisions`: full DFS for tree-level index (not shallow); Option B inode strategy (AtomicU64 + DashMap, separate from InodeRegistry); tree/ shows `—` for count in mount-root index
    - `handoff_items`: OP-2 now fully closed at all three levels (bucket Phase 15, tree-subdir Phase 18, mount-root Phase 18)

    After writing the SUMMARY, update `.planning/STATE.md` cursor to: "Phase 18 SHIPPED. Phase 19 (OP-1 remainder) is next."
  </action>
  <verify>
    <automated>test -f /home/reuben/workspace/reposix/scripts/dev/test-tree-index.sh && test -x /home/reuben/workspace/reposix/scripts/dev/test-tree-index.sh && echo "script exists and is executable"</automated>
  </verify>
  <done>
    - `scripts/dev/test-tree-index.sh` exists, is executable, and passes `bash -n` (no syntax errors)
    - `18-SUMMARY.md` exists in the phase directory and covers all key decisions and new exports
    - `STATE.md` cursor updated to Phase 18 SHIPPED
    - No new git-tracked files were forgotten (check `git status`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke script → live simulator | Script POSTs issues to the simulator to seed tree data; simulator is localhost only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-W2-01 | Tampering | `test-tree-index.sh` — seeded issue titles in `cat` output | accept | Script only asserts frontmatter keys (`kind: tree-index`), not title content. Simulator is localhost; no real data exposure. |
| T-18-W2-02 | DoS | Script leaves simulator or FUSE mount running on failure | mitigate | `trap cleanup EXIT` in the script ensures `fusermount3 -u` and `kill $SIM_PID` run even on non-zero exit |
</threat_model>

<verification>
After both tasks complete:

```bash
# Final full-workspace gate
cargo test --workspace --quiet
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --all --check

# Smoke script syntax check
bash -n scripts/dev/test-tree-index.sh

# CHANGELOG presence
grep -c "kind: mount-index\|tree-recursive\|OP-2 remainder" CHANGELOG.md || grep "Phase 18" CHANGELOG.md

# SUMMARY presence
ls .planning/phases/18-op-2-remainder-tree-recursive-and-mount-root-index-md-synthe/18-SUMMARY.md
```
</verification>

<success_criteria>
- `cargo test --workspace --quiet` exits 0 — no regressions across the full workspace
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0
- `cargo fmt --all --check` exits 0
- CHANGELOG `[Unreleased]` has both Phase 18 feature bullets
- `scripts/dev/test-tree-index.sh` exists, is executable, and is syntax-valid
- `18-SUMMARY.md` written with key decisions and exports documented
- `STATE.md` cursor updated to Phase 18 SHIPPED
</success_criteria>

<output>
Phase 18 complete. The SUMMARY is at:
`.planning/phases/18-op-2-remainder-tree-recursive-and-mount-root-index-md-synthe/18-SUMMARY.md`

OP-2 is now fully closed:
- Phase 15: `mount/<bucket>/_INDEX.md` (bucket level)
- Phase 18: `mount/tree/<subdir>/_INDEX.md` (tree-dir level) + `mount/_INDEX.md` (mount-root level)

Next: Phase 19 (OP-1 remainder).
</output>
