---
task_id: 90-07
phase: 90
wave: E
type: mechanical + docs
execution_mode: top-level
req_ids: [SC7-CLAUDE.md, SC8-close, D90-01, D90-06, D90-11]
effort: S (3-4h)
model_tier: sonnet
depends_on: [90-01, 90-02, 90-03, 90-04, 90-05, 90-06]
files_modified:
  - .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md   # status updates
  - .planning/milestones/v0.13.0-phases/ROADMAP.md            # P91 amendment (D90-01/-06)
  - .planning/milestones/v0.13.0-phases/GOOD-TO-HAVES.md      # drain 90-04's candidate entries (D90-12 item 4)
  - CLAUDE.md                                                 # § Quality Gates revision (SC7)
  - quality/reports/verdicts/p90/VERDICT.md                   # phase close (SC8)
---

# Task 90-07 — Intake drain + ROADMAP amendment + CLAUDE.md + phase close

## Goal

Land the phase-close deliverables: SURPRISES-INTAKE status updates for the items
P90 disposed, the ROADMAP P91 amendment (QL-001 + sanctioned-target litmus
criterion), the CLAUDE.md § Quality Gates revision (SC7), then push + verifier
dispatch (SC8). **This is the LAST wave — depends on all prior waves committed.**

## Subtask A — SURPRISES-INTAKE status updates

Update these entries (leave the rest OPEN for their owning phases):

| entry (timestamp / severity) | new STATUS |
|---|---|
| `2026-07-04 05:10` Codex HIGH (verifier-not-found preserves PASS) | **RESOLVED** — cite 90-02c commit (RBF-FW-07a). |
| `2026-07-04 05:10` all-three-legs HIGH (minted_at backdate hole) | **RESOLVED** — cite 90-02a commit (minted_at anchor). |
| `2026-07-04 05:10` independent+coordinator MED (skip demotes+persists) | **RESOLVED** — cite 90-02d commit (RBF-FW-07b). |
| `2026-07-04 05:10` Claude MED (sanctioned-target membership residual) | **ROUTED-P91** — D90-06; the litmus body asserts membership. Annotate, do NOT resolve. |
| `2026-07-04 06:20` BLOCKER (QL-001 path-shape) | **ROUTED-P91** — D90-01; annotate with the ROADMAP P91 amendment pointer. |
| `2026-07-03 21:35` LOW (`NOT_VERIFIED` underscore typo) | **RESOLVED-c0d5459** — D90-11 / R2 § B.2 (already fixed by convergence; close, do NOT re-fix). |
| `2026-07-03 11:00` HIGH (waiver cliff 2026-07-26) | **RESOLVED** — cite 90-05 waiver-disposition commit (per-waiver renewals + tracked_in repoints). |
| `2026-07-03 11:05` HIGH (5 EXPIRED docs-repro waivers) | **RESOLVED** — cite 90-05 renewal commit. |
| `2026-07-03 11:10` MED (quality-weekly RED 2 weeks) + `2026-07-04 05:30` MED (weekly never green: 2 verifier-less P2 rows) | **DEFERRED-P95/P97** — the north-star fix (write the 2 verifier scripts) is P95/P97 per the entries' own homes; annotate with the RAISE § 1 pointer. Do NOT soften verdict.py (explicitly rejected). |
| `2026-07-04 05:40` LOW (whole-file-hash STALE brittleness) | **RESOLVED** if 90-04/90-07 rebind cleared the 3 rows; else **DEFERRED-P95** (design look at line-anchored bindings). |
| `2026-07-04 08:30` MED (5 MISSING_TEST rows) | **RESOLVED** — cite 90-06 commit (real tests + rebind + un-waive). |

For any other intake entry found already-landed during the phase, apply D90-11
(RESOLVED + SHA, do not re-fix).

**GOOD-TO-HAVES drain (D90-12 item 4):** collect the candidate entries 90-04
handed over in its TLDR (e.g. the `verdict.py` `_freshness` import cleanup, if
not eager-fixed) and write them to GOOD-TO-HAVES.md here — 90-02 and 90-07 are
the only writers of that file (single-writer discipline; 90-04 must not write
it from Wave B).

## Subtask B — ROADMAP P91 amendment (D90-01 + D90-06)

Amend the ROADMAP Phase 91 entry (`ROADMAP.md:171-187`):
1. **Name QL-001** with its **six sharpened acceptance criteria** (D90-01; source
   the criteria from the intake `2026-07-04 06:20` entry's numbered list — canonical
   path-shape across `builder.rs`/`refresh.rs`/`fast_import.rs`/`diff.rs` +
   `issue_id_from_path`; `issues/*.md` planner filter; stream-parser LF peek-and-
   consume; REAL `git push` regression whose green depends on all landing
   together; all four path sites grep-verifiable to one canonical spelling).
2. **Add the sanctioned-target litmus-body criterion** (D90-06): P91's litmus
   verifier MUST assert the resolved real-backend target is one of the sanctioned
   three (`docs/reference/testing-targets.md`) and fail loud otherwise.
3. Note the `agent-ux/real-git-push-e2e` waiver (2026-07-31) dies with the P91
   fix (not renewed — D90-01).

> **R-5 caution:** ROADMAP.md is under `.planning/`, NOT `docs/` — unlikely to be
> a docs-alignment source. Confirm no doc-alignment row cites it before push.

## Subtask C — CLAUDE.md § Quality Gates revision (SC7 — REVISE, don't append)

Per the CLAUDE.md-stays-current rule, **revise existing sections** to reflect the
new state (do NOT append a narrative):
- **9-dimensions table, agent-ux row:** the cell currently reads "dark-factory
  regression (sim arm + DVCS third arm)…" — add `+ test-name-vs-asserts` (R2
  noticing 7). The agent-ux dimension now also owns the honesty-triage gate.
- **§ Quality Gates taxonomy:** document the new honesty rules concisely with
  progressive disclosure: `minted_at` (write-once audit-cutoff anchor),
  `coverage_kind` (transport/perf rows; hard-new/RAISE-legacy split),
  `transport_claim` tri-state opt-out, missing-verifier→NOT-VERIFIED, env-skip-
  preserves, shell-subprocess transcript-runtime, F-K4b asserts-congruence, the
  test-name gate, the absorption-honesty + milestone-adversarial dispatches.
  Cite R1/R2/PROTOCOL for depth — keep CLAUDE.md ≤40k chars (route detail to
  PROTOCOL.md which 90-04 expanded).
- **Subagent delegation / kinds:** note `agent-ux/dvcs-third-arm` is now
  `mechanical`; `subjective/dvcs-cold-reader` is dispatch-wired.

## Subtask D — Phase close (SC8)

1. Ensure `python3 quality/runners/run.py --cadence pre-push` is **honestly
   green** (all 9 P90 rows PASS; the 5 MISSING_TEST rows BOUND; no STALE
   docs-alignment rows — rebind any doc-edit drift per R-5).
2. `git push origin main` (BEFORE the verifier dispatch, per push-cadence rule;
   no `--no-verify`).
3. Dispatch the **unbiased verifier subagent** (PROTOCOL.md § "Verifier subagent
   prompt template") to grade the 8 ROADMAP SCs from artifacts with zero session
   context. Write `quality/reports/verdicts/p90/VERDICT.md`. **SC2 grades
   against 90-02f's per-expected-assert congruence implementation (confirmed
   shipping per D90-12 item 1 — defer was rejected):** the verifier checks the
   p86 F6 regression fixture goes RED and the zero-false-RED full-catalog
   acceptance held. A missing or global-overlap-only implementation is RED.
4. **STATE.md cursor advance is the COORDINATOR's action at close (note only** —
   this task does not edit STATE.md; flag the coordinator to advance the cursor
   to P91 after the verdict is GREEN).

## Verification

```bash
python3 quality/runners/run.py --cadence pre-push 2>&1 | tail -20     # honestly green (SC8)
grep -c 'RESOLVED\|ROUTED-P91\|DEFERRED-P95' .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md
grep -q 'QL-001' .planning/milestones/v0.13.0-phases/ROADMAP.md       # P91 amendment (D90-01)
grep -q 'test-name-vs-asserts' CLAUDE.md                              # dimension cell updated
wc -c CLAUDE.md   # <= 40000
test -f quality/reports/verdicts/p90/VERDICT.md
```

## Acceptance criteria

- [ ] Intake entries updated per Subtask A (RESOLVED w/ SHA, ROUTED-P91, DEFERRED-P95/P97 as disposed); underscore typo closed RESOLVED-c0d5459 (D90-11).
- [ ] ROADMAP P91 names QL-001 + 6 criteria + sanctioned-target litmus criterion (D90-01/-06).
- [ ] CLAUDE.md § Quality Gates REVISED (not appended); agent-ux cell + honesty rules; ≤40k chars.
- [ ] `--cadence pre-push` honestly green.
- [ ] Pushed to origin (no `--no-verify`); verdict at `quality/reports/verdicts/p90/VERDICT.md`; SC2 graded against 90-02f's p86-F6 fixture + zero-false-RED evidence (D90-12 item 1).
- [ ] 90-04's GOOD-TO-HAVES candidate entries drained into GOOD-TO-HAVES.md (D90-12 item 4).
- [ ] STATE cursor advance flagged to coordinator (not done here).

## Must NOT touch

STATE.md (coordinator advances at close). Any `quality/runners/*` or gate scripts
(prior waves own; this wave only pushes them green). QL-001 product code (P91).

## Two-channel rule

Sub-subagent writes the intake diff + ROADMAP diff + CLAUDE.md diff + the
pre-push green tail to disk; returns ≤300-word TLDR (entries disposed, SCs green,
verdict path, SC2 ruling, coordinator STATE-advance reminder, surprises).
