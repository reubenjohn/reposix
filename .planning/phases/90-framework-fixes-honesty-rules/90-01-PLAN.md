---
task_id: 90-01
phase: 90
wave: A
type: mechanical
execution_mode: top-level
req_ids: [RBF-FW-06, RBF-FW-07, RBF-FW-08, RBF-FW-09, RBF-FW-10, RBF-FW-12]
effort: S (2-3h)
model_tier: haiku (mechanical JSON; sonnet if audit-paragraph drafting stalls)
depends_on: []
must_be_first_commit: true
files_modified:
  - quality/catalogs/freshness-invariants.json
  - quality/catalogs/agent-ux.json
  - quality/reports/raise-list-p90.md   # NEW (skeleton)
---

# Task 90-01 — Catalog-first: mint the P90 honesty rows NOT-VERIFIED + RAISE skeleton

## Goal (SC7)

Mint the P90 GREEN-contract rows **before any implementation lands**, per
`quality/PROTOCOL.md` Step 3 + CLAUDE.md "Catalog-first rule". Every subsequent
commit (90-02..90-07) cites a row id from this set. Every new row carries:
`minted_at` (RFC3339, write-once — D90-03), `claim_vs_assertion_audit` (≥50
chars — inherited P89 rule), `cadences`, `blast_radius`, `status: NOT-VERIFIED`,
`last_verified: null`. Meta rows whose text contains transport keywords carry
`transport_claim: false` (P90-D1). Also lay down the RAISE-LIST skeleton file so
90-05 fills a committed structure.

## Rows to mint

Read wrappers first (`head -20` each) to mirror `$schema`/`comment`/`dimension`/
`rows` conventions. Confirm `freshness-invariants.json` is `"dimension":
"structure"`.

### A. `quality/catalogs/freshness-invariants.json` (dimension: structure) — 6 rows

Verifier for the runner-behavior rows is a thin bash wrapper running the python
unit tests (mirror `structure/claim-vs-assertion-audit-required` → runs
`python3 -m unittest`). Multiple rows MAY share one wrapper with distinct
`expected.asserts`.

1. **`structure/coverage-kind-required`** (RBF-FW-06)
   - `kind: mechanical`; `cadences: ["pre-push","pre-pr"]`; `blast_radius: P1`
   - `transport_claim: false` (this row's asserts name transport keywords)
   - `sources`: `["quality/runners/_audit_field.py","quality/catalogs/README.md"]`
   - `expected.asserts`: `["is_transport_or_perf_row honors transport_claim tri-state (true→transport, false→suppress, absent→regex over comment+id in dims perf/agent-ux/security)","a transport-detected row minted >= P90 lacking coverage_kind AND lacking a compliant waiver → SystemExit at load","a legacy (pre-cutoff) transport row lacking coverage_kind loads clean (RAISE-only)","non-transport mechanical row without coverage_kind loads clean (no over-fire)"]`
   - `verifier.script`: `quality/gates/structure/runner-honesty-semantics.sh` (NEW; ships 90-02)
   - `claim_vs_assertion_audit`: explain how `test_audit_field.TestCoverageKind` would fail if the split (hard-new / RAISE-legacy) or the `transport_claim` tri-state were broken. (≥50 chars; draft ~250.)

2. **`structure/minted-at-write-once`** (D90-03 / cross-AI H2)
   - `kind: mechanical`; `cadences: ["pre-push","pre-pr"]`; `blast_radius: P0`
   - `expected.asserts`: `["when minted_at present it is the sole audit-cutoff anchor (immutable; a hand-edited last_verified no longer moves the cutoff)","a row with minted_at >= P90_MINT_CUTOFF (2026-07-05Z) and no minted_at is rejected only via the lv>=cutoff guard (null-lv legacy rows NOT forced)","full-catalog load sweep succeeds at landing (no existing row trips the reject)"]`
   - `verifier.script`: `quality/gates/structure/runner-honesty-semantics.sh`
   - `claim_vs_assertion_audit`: falsification via `test_audit_field.TestMintedAt` (the backdate row with minted_at 2026-06 + backdated lv 2026-04 must FAIL). Note the P90-D2 residual.

3. **`structure/verifier-missing-demotes`** (RBF-FW-07a / cross-AI H4)
   - `kind: mechanical`; `cadences: ["pre-push"]`; `blast_radius: P1`
   - `expected.asserts`: `["a PASS row whose verifier.script is absent flips to NOT-VERIFIED (never preserves PASS)","the artifact carries error='verifier not found at <path>' distinguishing a deploy glitch from a real FAIL","_verifier_missing transient flag is stripped before persist"]`
   - `verifier.script`: `quality/gates/structure/runner-honesty-semantics.sh`
   - `claim_vs_assertion_audit`: falsification via the new missing-verifier test class.

4. **`structure/skip-fail-closed-with-history`** (RBF-FW-07b / cross-AI M8, **amended D90-04 per D90-12 item 2**)
   - `kind: mechanical`; `cadences: ["pre-push"]`; `blast_radius: P0`
   - `transport_claim: false`
   - `expected.asserts`: `["an env-gated skip flips and persists status NOT-VERIFIED for ALL pre-release-real-backend rows including the P0 litmus (fail-closed, OD-2-safe)","the prior real grade is preserved in last_real_grade + last_real_verified write-history fields (written only when prior status was PASS/FAIL/PARTIAL)","the skip artifact/report carries skip_reason: env-missing so the churn is explained","a second cred-less run after the flip leaves the catalog byte-identical (idempotent)"]`
   - `verifier.script`: `quality/gates/structure/runner-honesty-semantics.sh`
   - `claim_vs_assertion_audit`: MUST match the **fail-closed-with-history** contract (NOT preserve wording): falsification via `test_realbackend.TestRunRowEnvGateIntegration` — if the claim ("a cred-less run can never ride a stale real PASS; history + skip_reason explain the demotion") were false, the prior-PASS+scrubbed-env test would find status still PASS (skip-as-pass channel open), or find NOT-VERIFIED with no `last_real_grade`/`skip_reason` (silent ground-truth loss — M8's actual complaint).

5. **`structure/shell-subprocess-transcript-runtime`** (RBF-FW-08 / cross-AI M6)
   - `kind: mechanical`; `cadences: ["pre-push"]`; `blast_radius: P1`
   - `transport_claim: false`
   - `expected.asserts`: `["a kind:shell-subprocess row computing PASS on exit 0 is downgraded to FAIL unless artifact.transcript_path names an existing file containing an argv: line","non-shell-subprocess rows are unaffected (no over-fire)","the live agent-ux/kind-shell-subprocess-worked-example row still PASSes (its verifier emits a real transcript)"]`
   - `verifier.script`: `quality/gates/structure/runner-honesty-semantics.sh`
   - `claim_vs_assertion_audit`: falsification via `test_realbackend` _run_synthetic extension.

6. **`structure/asserts-congruence-grade-time`** (F-K4b / **ROADMAP SC2** — confirmed shipping per D90-12 item 1)
   - `kind: mechanical`; `cadences: ["pre-push"]`; `blast_radius: P1`
   - `transport_claim: false`
   - `expected.asserts`: `["when BOTH expected.asserts and verifier-written asserts_passed are non-empty, EVERY expected.asserts entry must map to >=1 asserts_passed entry via normalized per-pair token matching; any unmatched expected assert blocks the PASS flip and asserts_failed names the unmatched entries","when asserts_passed is empty (verifier wrote none) the check is a no-op (backward-compat with the 91 mechanical rows)","the p86 F6 shape (9 expected vs 17 passed, heavy shared vocabulary, 2 expected uncovered) goes RED","zero false RED across all existing catalog rows against their current artifacts"]`
   - `verifier.script`: `quality/gates/structure/runner-honesty-semantics.sh`
   - `claim_vs_assertion_audit`: falsification via `TestAssertsCongruence` — if the claim ("every expected assert must be covered by a passed assert") were false (e.g. only global vocabulary overlap were checked), the p86 F6 regression fixture would grade PASS and the test would fail.
   - **`comment` records:** "SC2/F-K4b; per-expected-assert design ruled by D90-12 item 1 (defer-to-P95 rejected)."

### B. `quality/catalogs/agent-ux.json` (dimension: agent-ux) — 3 rows

7. **`agent-ux/test-name-vs-asserts`** (RBF-FW-09 / F-K8)
   - `kind: mechanical`; `cadences: ["pre-push"]`; `blast_radius: P1`; `timeout_s: 15`
   - `transport_claim: false` (its asserts LITERALLY contain the push/fetch regex)
   - `sources`: `["quality/gates/agent-ux/test-name-vs-asserts.sh","quality/reports/raise-list-p90.md"]`
   - `expected.asserts`: `["a Rust #[test] fn whose name matches (real|dark_factory|round_?trip|end_to_end|e2e|push|fetch) whose body neither drives a git push/fetch subprocess nor a non-127.0.0.1 host nor is #[ignore] real-backend-gated is RAISEd","the committed expected-RAISE baseline (R2 § C.2, 4 entries) matches → exit 0","a NEW un-baselined dishonest name → exit 1","per-line // test-name-honesty: ok — <reason> marker exempts, and a bare marker with no reason itself RAISEs"]`
   - `verifier.script`: `quality/gates/agent-ux/test-name-vs-asserts.sh` (NEW; ships 90-03)
   - `artifact`: `quality/reports/verifications/agent-ux/test-name-vs-asserts.json`
   - `claim_vs_assertion_audit`: falsification via the 4-row baseline going empty/mismatched.

8. **`agent-ux/absorption-honesty-template-present`** (RBF-FW-10 / F-K5, consumed by P96)
   - `kind: mechanical`; `cadences: ["pre-push","pre-pr"]`; `blast_radius: P1`
   - `sources`: `["quality/dispatch/absorption-honesty-spot-check.md","quality/PROTOCOL.md","PRACTICES.md"]`
   - `expected.asserts`: `["quality/dispatch/absorption-honesty-spot-check.md exists","its content sha256 matches the committed hash (content-hash binding, not mere existence)","it contains the four F-K5 clauses verbatim: sample EVERY no-intake phase / author != milestone orchestrator / rubric 'walk one critical example end-to-end mentally — does it work?' / content-hash binding"]`
   - `verifier.script`: `quality/gates/agent-ux/absorption-honesty-template-present.sh` (NEW; ships 90-04)
   - `artifact`: `quality/reports/verifications/agent-ux/absorption-honesty-template-present.json`
   - `claim_vs_assertion_audit`: falsification if the template is deleted or its hash drifts or a clause is missing.

9. **`agent-ux/milestone-adversarial-pass`** (RBF-FW-12 / D90-09)
   - `kind: mechanical`; `cadences: ["pre-release-real-backend"]`; `blast_radius: P0`
   - `sources`: `["quality/dispatch/milestone-adversarial.md","quality/runners/verdict.py"]`
   - `expected.asserts`: `["milestone-close verdict blocks GREEN (forces red) when quality/reports/verifications/adversarial/<milestone>.json is absent","blocks GREEN when the artifact reports >=1 failed row audit","the gate can only DARKEN (green->red), never lighten — composes with compute_color/compute_exit_code"]`
   - `verifier.script`: `quality/gates/agent-ux/milestone-adversarial-pass.sh` (NEW; ships 90-04; thin bash running `python3 -m unittest quality.runners.test_verdict` adversarial cases)
   - `artifact`: `quality/reports/verifications/agent-ux/milestone-adversarial-pass.json`
   - `claim_vs_assertion_audit`: falsification via `test_verdict` absent-artifact + failed-row cases.

> **Artifact-path note (D90-09 vs R1):** the adversarial *subagent* writes
> `quality/reports/verifications/adversarial/<version>.json` (D90-09 path). R1 §
> FW-12 wrote `.../milestone-adversarial/<version>.json`. **Use D90-09's path**
> (`adversarial/`) — D90 is binding. 90-04's verdict.py hook reads that path.

### C. RAISE-LIST skeleton — `quality/reports/raise-list-p90.md` (NEW)

Create with section headers ONLY (90-05 fills the content), so the file exists
catalog-first and the gate/detector have a committed target:

```markdown
# RAISE LIST — P90 (seeds P92 / P94 / P95)
> Generated by walking the P90 honesty gates + validators over the live catalog.
> Structured, decision-ready for downstream planners. Dispositions are advisory
> to the owning phase; P90 does NOT land the fixes (F-class boundary, D90-01/-05).

## 1. Transport/perf rows — coverage_kind dispositions   <!-- 90-05 fills from R2 § A -->
## 2. Dishonest tests — test-name-vs-asserts baseline     <!-- 90-05 fills from R2 § C -->
## 3. Magic-fixture hazards (path-shape schism)           <!-- 90-05 fills from R2 § E -->
## 4. Subagent-graded migration record                    <!-- 90-05 fills from 90-03 -->
## 5. Waiver dispositions                                 <!-- 90-05 fills from R2 § D -->
```

## Steps

1. Read both catalog wrappers; confirm dimensions.
2. Append 6 rows to `freshness-invariants.json`, 3 to `agent-ux.json`.
3. `python3 -m json.tool` each catalog → `/dev/null` (trailing-comma guard).
4. `python3 quality/runners/run.py --cadence pre-push --dimension structure,agent-ux --dry-run 2>&1 | tail -30` — confirm all 9 rows surface NOT-VERIFIED. **Note:** the runner's `_audit_field` validator ships in 90-02; until then `claim_vs_assertion_audit`/`minted_at`/`coverage_kind` fields are silently accepted at load. This dry-run MUST pass (rows load).
5. Write the RAISE skeleton.
6. Commit (catalog-first). Message cites all 9 row ids + RBF-FW mapping + the P90-D1 `transport_claim` note + the D90-12 rulings (row 4 fail-closed-with-history; row 6 per-expected-assert congruence).

## Verification

```bash
python3 -m json.tool quality/catalogs/freshness-invariants.json >/dev/null
python3 -m json.tool quality/catalogs/agent-ux.json >/dev/null
python3 quality/runners/run.py --cadence pre-push --dimension structure,agent-ux --dry-run \
  | grep -cE '(coverage-kind-required|minted-at-write-once|verifier-missing-demotes|skip-fail-closed-with-history|shell-subprocess-transcript-runtime|asserts-congruence-grade-time|test-name-vs-asserts|absorption-honesty-template-present|milestone-adversarial-pass)' \
  | awk '$1 == 9 {exit 0} {exit 1}'
test -f quality/reports/raise-list-p90.md
```

## Acceptance criteria

- [ ] Both catalogs parse; 9 new row ids present.
- [ ] All 9 rows: `status: NOT-VERIFIED`, `last_verified: null`, `minted_at` present (RFC3339, `2026-07-04T…Z` or the actual landing date), `claim_vs_assertion_audit` ≥50 chars.
- [ ] The 5 meta rows that name transport keywords carry `transport_claim: false`.
- [ ] Row 6 `comment` cites the D90-12 item 1 ruling (per-expected-assert design; defer rejected).
- [ ] Row 4 asserts + audit text carry the fail-closed-with-history contract (amended D90-04), NOT preserve wording.
- [ ] RAISE skeleton exists with the 5 section headers.
- [ ] No implementation file added in this commit (first-commit-of-phase contract).

## Must NOT touch

Any `quality/runners/*.py`, any `quality/gates/**` script, verdict/PROTOCOL/
CLAUDE docs. This wave is pure catalog JSON + one skeleton markdown.

## Two-channel rule

Sub-subagent writes the full diff + json.tool output + dry-run tail to disk;
returns ≤300-word TLDR (row ids + dry-run exit + surprises filed).
