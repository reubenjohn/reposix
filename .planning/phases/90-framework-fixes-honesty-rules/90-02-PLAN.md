---
task_id: 90-02
phase: 90
wave: B
type: unit
execution_mode: top-level
req_ids: [RBF-FW-06, RBF-FW-07, RBF-FW-08, F-K4b-SC2, minted_at]
effort: M (5-6h)
model_tier: sonnet (R1 gives file:line + test plans; opus not warranted)
depends_on: [90-01]
files_modified:
  - quality/runners/_audit_field.py
  - quality/runners/_realbackend.py
  - quality/runners/run.py            # in-place branch edits ONLY (over-cap)
  - quality/runners/test_audit_field.py
  - quality/runners/test_realbackend.py
  - quality/gates/structure/runner-honesty-semantics.sh   # NEW thin wrapper
  - quality/catalogs/README.md         # schema rows: minted_at, coverage_kind, transport_claim
  - GOOD-TO-HAVES.md                   # file the run.py line-count gate (XS)
---

# Task 90-02 — Runner honesty semantics (python; NO cargo, NO verdict.py)

## Goal

Land the runner/validator honesty changes per R1 § 2. **Net-new decision logic
goes in helpers** (`_audit_field.py`, `_realbackend.py`) — `run.py` gets ONLY
the ~two in-place branch edits R1 specifies, because run.py is already 429/350.
verdict.py is untouched here (P90-D3 — 90-04 owns it). All tests are python
`unittest` — **zero cargo**.

## Anti-bloat accounting (state final line counts in the wave report)

| file | before | delta | after | cap | note |
|---|---|---|---|---|---|
| run.py | 429 | ~+22 (07a ~6, 07b fail-closed+history ~14, helper call-sites ~2) | ~451 | 350 | **still over** — remediation filed to GOOD-TO-HAVES (line-count gate, XS) |
| _audit_field.py | 75 | ~+75 (minted_at anchor ~15, coverage_kind detect+block ~25, transcript_evidence_ok ~18, per-pair asserts_congruent ~17) | ~150 | — | new logic home; per-pair congruence (D90-12) costs ~+10 over the rejected global-overlap sketch |
| _realbackend.py | 98 | ~0 | 98 | — | no change (skip logic edits land in run.py branch) |

**Honesty note (do NOT skip):** run.py exits P90 over-cap. The wave report MUST
state the final `wc -l quality/runners/run.py` number and confirm the
GOOD-TO-HAVES entry (a `structure` `wc -l` gate on run.py/verdict.py) is filed.
Silently ignoring the cap is the exact dishonesty P90 kills.

## Subtasks

### 90-02a — `minted_at` write-once anchor (D90-03; row `structure/minted-at-write-once`)
- `_audit_field.py`: add `P90_MINT_CUTOFF = "2026-07-05T00:00:00Z"` (P90-D2 —
  **bump if 90-01 landed later; the constant must be ≥ the 90-01 commit date**).
  Rewrite the anchor at `_audit_field.py:39-42` per R1: `minted_at` present → it
  is the sole `is_new` anchor; absent → today's null-or-`last_verified`
  heuristic. Add the reject: `if minted is None and lv is not None and
  parse_rfc3339(lv) >= parse_rfc3339(P90_MINT_CUTOFF): SystemExit(...)`.
- README schema: add `minted_at` row (after `last_verified`).
- Tests (`test_audit_field.TestMintedAt`, R1's 4 cases): pre-cutoff mint +
  backdated lv + no audit → PASS; **post-cutoff mint + backdated lv + no audit →
  FAIL (the H2 fix)**; minted_at absent + post-cutoff lv → FAIL "lacks
  minted_at"; minted_at absent + null lv → NOT forced (5-legacy-row guard).

### 90-02b — `coverage_kind` load-time validation (RBF-FW-06; row `structure/coverage-kind-required`)
- `_audit_field.py`: add `is_transport_or_perf_row(row) -> bool` implementing the
  **P90-D1 tri-state**: `transport_claim is True` → True; `transport_claim is
  False` → False (regex suppressed); else regex `_TRANSPORT_RE` (R1's pattern)
  over `comment` + `id` **only** (NOT `expected.asserts` — avoids meta-row
  self-match, R-3), gated to `dimension in {perf, agent-ux, security}`.
- Enforcement split (D90-05): row minted ≥ P90 (via minted_at/cutoff) that is
  transport AND lacks `coverage_kind: real-backend` AND lacks a compliant
  `WAIVED + until_date` → `SystemExit`. Legacy transport rows → **RAISE-only**
  (NOT a load concern; 90-05's walker emits them — validator stays narrow).
- Add `coverage_kind` enum (`real-backend`|`sim-only`|`mechanical`|`manual`) +
  `transport_claim` to README schema.
- Tests (`test_audit_field.TestCoverageKind`, R1's 5 cases + one P90-D1 case:
  `transport_claim: false` on a regex-hitting row → NOT transport → loads clean).

### 90-02c — RBF-FW-07a missing-verifier ⇒ NOT-VERIFIED (row `structure/verifier-missing-demotes`)
- `run.py:252-254`: replace the status-preserving conditional with unconditional
  `row["status"] = "NOT-VERIFIED"; row["_verifier_missing"] = True`. Keep the
  existing artifact `error` field (run.py:243). Add `_verifier_missing` to the
  transient-strip list (run.py:409-411) and the summary branch (run.py:398-399).
- Tests (new `test_realbackend.TestVerifierMissing` or `test_run_missing_verifier.py`,
  R1's 3 cases): PASS+missing→NOT-VERIFIED w/ error; NOT-VERIFIED+missing stays;
  `_verifier_missing` stripped before persist. **Backward-compat: empirically 0
  green rows flip (R1 § 1a — the 5 missing-script rows are all WAIVED).**

### 90-02d — RBF-FW-07b env-skip fail-closed WITH preserved history (row `structure/skip-fail-closed-with-history`)
> **AMENDED per D90-04 (post plan-check, D90-12 item 2).** The original blanket
> skip-preserve would let a future post-P91 real PASS on the P0 litmus row
> survive a cred-less milestone-close — the skip-as-pass channel OD-2 forbids.
> R1 § FW-07b's preserve mechanism is SUPERSEDED; keep only its artifact-channel
> and transient-strip conventions.
- `run.py` skip branch (~183-194): on env-gated skip, current `status` flips
  (and persists) **NOT-VERIFIED — fail-closed, OD-2-safe, for ALL
  pre-release-real-backend rows including the P0 litmus.** The prior REAL grade
  is preserved in write-history fields persisted on the row:
  `last_real_grade: <prior status>` + `last_real_verified: <prior
  last_verified>` (written only when prior status ∈ PASS/FAIL/PARTIAL — never
  overwritten by a subsequent skip). The skip artifact/report carries an
  explicit `skip_reason: env-missing` marker so the churn is explained, not
  silent. `catalog_dirty` WILL fire on the first PASS→NOT-VERIFIED flip — that
  is now intended, honest behavior (churn explained by the marker + history
  fields); subsequent cred-less re-runs are idempotent (already NOT-VERIFIED →
  no further churn).
- README schema: add `last_real_grade` + `last_real_verified` rows (nullable;
  written by the runner on env-gated skip only).
- Tests (`test_realbackend.TestRunRowEnvGateIntegration` ext — D90-12-mandated
  cases):
  - **prior PASS + scrubbed env → status NOT-VERIFIED AND `last_real_grade ==
    "PASS"` + `last_real_verified` preserved AND artifact has `skip_reason:
    env-missing`** (the required regression per amended D90-04).
  - **litmus P0 prior PASS + scrubbed env at milestone-close → NOT-VERIFIED →
    verdict RED** (OD-2: a cred-less close can never ride a stale real PASS).
  - prior NOT-VERIFIED (the pre-P91 SLOT state) → stays NOT-VERIFIED, no
    history fields written (nothing real to preserve).
  - second scrubbed run after the flip → catalog byte-identical (idempotence).

### 90-02e — RBF-FW-08 runtime transcript enforcement (row `structure/shell-subprocess-transcript-runtime`)
- `_audit_field.py`: add `transcript_evidence_ok(row, artifact, repo_root) ->
  (bool, str)` per R1 (checks `transcript_path` present → file exists → contains
  `argv:` line). Co-locate next to `_has_transcript_contract` with a docstring
  naming the **load-vs-runtime split** (R1 noticing item 6).
- `run.py` after `map_exit_code_to_status` (~322): if `kind == shell-subprocess`
  and `status == PASS`, gate on `transcript_evidence_ok(row, artifact,
  repo_root)`; on fail → `status = FAIL` + append to `asserts_failed`. Pass the
  `repo_root` param (not the module global — synthetic tests).
- **OP-1 pre-landing check (risk R-4):** run
  `bash quality/gates/agent-ux/shell-subprocess-example.sh` for REAL and confirm
  its artifact carries `transcript_path` → existing file with an `argv:` line
  BEFORE landing the gate. Capture the transcript path in the wave report.
- Tests (`test_realbackend` _run_synthetic ext, R1's 5 cases).

### 90-02f — F-K4b asserts-congruence at grade time (ROADMAP SC2; row `structure/asserts-congruence-grade-time`)
> **CONFIRMED SHIPPING per D90-12 item 1** (plan-check GO-WITH-FIXES; defer-to-
> P95 considered and REJECTED). Design is **per-expected-assert congruence**,
> NOT zero-global-overlap — the global-overlap strawman grades the p86 F6 shape
> PASS dishonestly, the exact failure class P90 exists to kill.
- `_audit_field.py`: add `asserts_congruent(expected_asserts, asserts_passed) ->
  (ok: bool, unmatched: list[str])`. **Per-pair matching:** EVERY
  `expected.asserts` entry must map to ≥1 `asserts_passed` entry via normalized
  per-pair token matching (lowercase, split on non-alnum, drop stopwords; a
  pair matches when the expected entry's significant tokens are sufficiently
  covered by that single passed entry). **Any unmatched expected assert blocks
  the PASS flip.** Fires only when BOTH lists are non-empty; empty
  `asserts_passed` (the 91 mechanical rows that write none) → no-op
  (backward-compat).
- `run.py` grade path (after status computed, before persist): for a would-be
  PASS with non-empty `expected.asserts`, if unmatched entries exist →
  `status = FAIL` + `asserts_failed.append("F-K4b: expected assert(s) not
  covered by any asserts_passed entry: <unmatched>")`.
- Tests (`test_audit_field.TestAssertsCongruence`):
  - **p86 F6 regression fixture (REQUIRED per D90-12):** 9-item expected vs
    17-item passed, heavy shared vocabulary, two expected asserts uncovered →
    MUST go RED (the case zero-global-overlap misses).
  - every expected entry covered by some passed entry → PASS.
  - asserts_passed empty → no-op.
  - single unmatched expected entry among otherwise-matching lists → RED, and
    `asserts_failed` names exactly the unmatched entries.
  - **Acceptance criterion: zero false RED across ALL existing catalog rows
    when graded against their current artifacts** (full-catalog grade smoke;
    tune the per-pair coverage threshold against the live mechanical rows
    until this holds — threshold value + tuning evidence go in the wave
    report).

### 90-02g — thin verifier wrapper + README + GOOD-TO-HAVES
- `quality/gates/structure/runner-honesty-semantics.sh` (NEW): `set -euo
  pipefail`; runs `python3 -m unittest quality.runners.test_audit_field
  quality.runners.test_realbackend`; writes the standard artifact JSON; exit 0
  on all-pass. Backs rows 1-6 of 90-01.
- README schema: `minted_at`, `coverage_kind`, `transport_claim` rows.
- `GOOD-TO-HAVES.md`: file "XS — structure `wc -l` gate on run.py/verdict.py so
  the ≤350/≤400 caps are checked, not aspirational (R1 noticing 1)."

## Verification

```bash
python3 -m unittest quality.runners.test_audit_field quality.runners.test_realbackend -v
bash quality/gates/structure/runner-honesty-semantics.sh
# Full-catalog load sweep — P90-D2 acceptance: no existing row trips minted_at/coverage_kind:
python3 quality/runners/run.py --cadence pre-push --dry-run 2>&1 | tail -5   # must NOT SystemExit
bash quality/gates/agent-ux/shell-subprocess-example.sh   # R-4: confirm real transcript emitted
wc -l quality/runners/run.py quality/runners/_audit_field.py                 # report the numbers
```

## Acceptance criteria

- [ ] All new unittest classes pass; existing runner tests still green.
- [ ] Full-catalog `load_catalog()` sweep succeeds at landing (P90-D2 / R-3 guard).
- [ ] Live `shell-subprocess-example.sh` still PASSes with a real transcript (R-4).
- [ ] **90-02f: zero false RED across ALL existing catalog rows against current artifacts** (D90-12 acceptance criterion); p86 F6 fixture goes RED.
- [ ] **90-02d: prior-PASS + scrubbed env → NOT-VERIFIED with `last_real_grade=PASS` recorded + `skip_reason: env-missing`; litmus-P0 milestone-close case → verdict RED** (amended D90-04).
- [ ] run.py final line count reported; GOOD-TO-HAVES line-count-gate entry filed.
- [ ] Rows `structure/{coverage-kind-required,minted-at-write-once,verifier-missing-demotes,skip-fail-closed-with-history,shell-subprocess-transcript-runtime,asserts-congruence-grade-time}` flip NOT-VERIFIED → PASS.

## Must NOT touch

`quality/runners/verdict.py` (90-04 owns it — P90-D3). Any catalog row block
(90-01 minted them; edits are 90-03/90-05). Any cargo/Rust file. The
`test-name-vs-asserts.sh` gate (90-03).

## Two-channel rule

Sub-subagent writes full diffs + unittest -v output + the load-sweep tail + the
`wc -l` numbers + the shell-subprocess transcript path to disk; returns ≤300-word
TLDR (per-subtask pass/fail + final run.py LOC + R-4 status + the 90-02f
threshold tuning evidence + surprises).
