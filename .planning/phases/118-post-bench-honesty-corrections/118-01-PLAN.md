---
phase: 118-post-bench-honesty-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/milestones/audits/2026-07-12-reality-check.md
autonomous: true
requirements: [DOCS-07, DOCS-09]

must_haves:
  truths:
    - "PROJECT.md's Context section no longer cites the FUSE-era `/mnt/jira/PROJ-123.md` path — it uses a git-native `cat issues/<id>.md` example"
    - "PROJECT.md's Context section no longer cites the disputed unevidenced `~2k vs ~150k` (≈75×) ratio — it cites a figure that traces to `docs/benchmarks/token-economy.md` (P115's live-session measurement) and links/names that doc so it is evidenced"
    - "PROJECT.md does NOT resurrect the RETIRED synthetic `89.1%` count-tokens-over-fixture figure (it was superseded by P115's live re-measurement; reintroducing it re-creates the exact defect this phase closes)"
    - "Each of the five stale `stalled/uncut owner-gated tag` premises in the reality-check audit carries a dated SUPERSEDED annotation; the original 2026-07-12 sentences are preserved verbatim (history intact, not rewritten)"
    - "The audit annotation states the current truth: v0.13.0, v0.13.1, v0.14.0 are ALL cut as git tags AND published as GitHub releases (v0.14.0 = Latest), and the archival cascade has since run"
    - "No code, cargo, or behavior change — only the two `.planning/*.md` prose files are touched; Arc D scope is not re-opened and no literal tag is cut (the tags already exist)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "corrected Context 'Why this exists' bullet — git-native path + evidenced live figure + citation"
      contains: "token-economy"
    - path: ".planning/milestones/audits/2026-07-12-reality-check.md"
      provides: "dated SUPERSEDED annotations over the stalled-tag premise (original text preserved)"
      contains: "SUPERSEDED"
  key_links:
    - from: ".planning/PROJECT.md"
      to: "docs/benchmarks/token-economy.md"
      via: "the corrected 'Why this exists' bullet cites/links the canonical benchmark doc so the figure is no longer unevidenced"
      pattern: "token-economy"
    - from: ".planning/milestones/audits/2026-07-12-reality-check.md"
      to: "the P118 dated master banner"
      via: "each of the 5 stale-premise homes carries an inline SUPERSEDED marker pointing at the dated correction"
      pattern: "SUPERSEDED"
---

<objective>
Correct two dishonest/stale claims flagged by the 2026-07-12 reality-check, now that P115 (BENCH-01) has re-measured the real figures:

1. **SC1 / DOCS-07** — `.planning/PROJECT.md:49`'s Context bullet cites a FUSE-era path (`/mnt/jira/PROJ-123.md`) and an unevidenced `~2k vs ~150k` (≈75×) token ratio that contradicts the project's own measurement by an order of magnitude. Re-cite it to a git-native path and a figure that traces to `docs/benchmarks/token-economy.md`.
2. **SC2 / DOCS-09** — the reality-check audit repeats a "cut the two stalled/uncut owner-gated tags (v0.13.0, v0.14.0)" premise in five places. That premise is now STALE: `git tag -l` shows v0.13.0, v0.13.1, v0.14.0 all cut, and they are published GitHub releases (v0.14.0 = Latest). Annotate each occurrence as SUPERSEDED with a dated note — preserving the historical audit text, not rewriting it.

Purpose: These are the last two honesty corrections gating the v0.15.0 "Floor" — a launch-readiness milestone cannot ship over a headline figure the project's own benchmark contradicts, or over an audit that reads as if the release tags are still stuck.
Output: Two edited `.planning/*.md` prose files, two atomic commits. **Zero code. Zero cargo. Zero behavior change.**

**PROSE-ONLY INVARIANT (hard STOP).** This phase touches ONLY `.planning/PROJECT.md` and `.planning/milestones/audits/2026-07-12-reality-check.md`. If executing either task appears to require a code change, a new figure not present in `docs/benchmarks/token-economy.md`, a literal `git tag` cut, or an edit outside those two files → STOP, do NOT do it, and file to `.planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md` with severity + sketch. Re-opening Arc D scope or cutting a tag is out of scope by construction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Exact correction targets + the ONLY sanctioned figure source. Do NOT explore the codebase; everything you need is here. -->

=== SC1 target — `.planning/PROJECT.md:49` (verbatim, the "Why this exists" bullet) ===
> - **Why this exists.** From `docs/research/agentic-engineering-reference.md`: MCP burns 100k+ tokens on schema discovery before the first useful operation. POSIX is in the model's pre-training. A `cat /mnt/jira/PROJ-123.md` operation is ~2k tokens of context vs ~150k for the equivalent MCP-mediated read.

Two defects in that last sentence:
 (a) `/mnt/jira/PROJ-123.md` is a FUSE-era mount path — reposix is now git-native (no FUSE); the real UX is `cat issues/<id>.md` inside a git checkout.
 (b) `~2k tokens of context vs ~150k` (≈75×) is unevidenced and contradicts the project's own measurement by ~an order of magnitude (flagged in `.planning/milestones/audits/2026-07-12-reality-check.md`).
Note: the FIRST clause of this bullet ("MCP burns 100k+ tokens on schema discovery before the first useful operation") is an accurate characterization of MCP tool-loading overhead and is fine to KEEP — the defect is the specific `~2k vs ~150k` single-op ratio and the FUSE path.

=== ONLY sanctioned figure source — `docs/benchmarks/token-economy.md` (P115, measured 2026-07-16, 6 live agentic sessions, REAL live GitHub-MCP server as the MCP arm; CI-reproducible via `bench_token_economy.py --offline`) ===
Canonical headline table (identical task = read 3 GitHub issues + edit 1 + push, median-of-3 per arm):
| Axis | reposix | GitHub MCP | reposix advantage |
| Output tokens | 1,213 | 21,171 | ~94.3% fewer |
| Cache-creation tokens | 19,068 | 56,129 | ~66.0% fewer |
| Total input-context tokens | 244,556 | 550,219 | ~55.6% smaller |
| Cost per session (USD) | $0.2076 | $0.8278 | ~74.9% cheaper |
The BOUND, doc-alignment-GREEN headline rows are `output-reduction-94-percent` (~94% fewer output tokens) and `cost-reduction-75-percent` (~75% cheaper). The `~55.6% smaller total input-context` axis is the closest direct analog to the original "tokens of context" phrasing.

=== RETIRED — do NOT resurrect ===
The synthetic `89.1%` "count_tokens over a static fixture" figure (and its 85.5% GitHub / 76.4% Confluence siblings, and the reposix-531-tokens-vs-MCP-4,883 pair) is RETIRE_PROPOSED in `quality/catalogs/doc-alignment.json` — P115 T6 replaced it with the live four-axis reframe above precisely because it was synthetic/unevidenced. DOCS-07's literal "(or the CI-verified 89.1% figure)" clause was written BEFORE P115 landed; the operative branch now is DOCS-07's "(or BENCH-01's re-measured value)" = the live figures above. Citing 89.1% would re-introduce the exact defect this phase exists to purge. Those 11 RETIRE_PROPOSED rows are a separate HUMAN-GATED batch — do NOT touch `doc-alignment.json`.

=== SC2 targets — five stale-premise homes in `.planning/milestones/audits/2026-07-12-reality-check.md` (verbatim) ===
 L336-339 (§2 Lane 5 bloat map): "the archival cascade is convention-blocked on the un-pushed owner-gated tags (v0.13.0, v0.14.0) ... *symptom of stalled tag cuts*, not neglect. Cutting the two tags unblocks the 21 loose phase dirs."
 L409-411 (§3 Exhibit E): "bloat is partly stalled-tag fallout ... two owner-gated tags (v0.13.0, v0.14.0) are uncut, so 21 dead phase dirs sit in live .planning/phases/ ..."
 L465-467 (§4 punch-list): "... Also: cut the two stalled tags."
 L482-485 (§4 Option ii): "convention-driven gradual archival: cut the two stalled tags → /gsd-cleanup moves the 21 dead phase dirs ..."
 L549-550 (§5 open question #2): "**Tag cuts.** v0.13.0/v0.14.0 tags are owner-gated and stalled; they block the archival cascade ... Cut both now?"
Companion (RECOMMENDED, same banner): L547-548 (§5 open question #1) asks "is the v1.0 headline the honest-but-synthetic 89.1%, or do you fund a live-MCP-server re-measurement first?" — RESOLVED: P115 funded the live re-measurement (`docs/benchmarks/token-economy.md`). The master banner may note this in the same breath.

=== CURRENT TRUTH (the annotation must state this) ===
`git tag -l` → v0.13.0, v0.13.1, v0.14.0 all exist; v0.14.0 is the public GitHub "Latest" release. Archival cascade HAS run: P78-94 → `.planning/milestones/v0.13.0-phases/`, P102/105/106/110-113 → `.../v0.14.0-phases/`; neither range remains in live `.planning/phases/`. PROJECT.md:3-8 already states this correctly and names the ADDENDUM the "canonical record."

=== Doc-alignment safety (fix-twice) ===
`quality/catalogs/doc-alignment.json` binds `docs/**` and `README.md` lines — grep confirms ZERO bindings on `.planning/PROJECT.md` or on `reality-check`. So no rebind is owed. The executor MUST still run the confirmation grep (Task verify) — a passing grep is the artifact, not an assumption.

=== Gate scope ===
Both edited files are under `.planning/` — NOT `docs/**` and not `mkdocs.yml`. The docs-build trio (`mkdocs-strict.sh` / `mermaid-renders.sh` / `structure/banned-words.sh`) does NOT apply. The `pre-commit` hook runs automatically; the phase-close `git push` + `post-push` CI-green check are mandatory (see <verification>).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-cite PROJECT.md's token figure to P115's live benchmark (SC1 / DOCS-07)</name>
  <files>.planning/PROJECT.md</files>
  <action>Rewrite ONLY the final sentence of the "Why this exists" bullet at `.planning/PROJECT.md:49` (keep the "MCP burns 100k+ tokens on schema discovery" clause — it is accurate). The replacement sentence MUST:
  (1) use a git-native example — `cat issues/<id>.md` inside a git checkout (NOT `/mnt/jira/PROJ-123.md`, NOT any `/mnt/...` FUSE path);
  (2) state a figure that traces to `docs/benchmarks/token-economy.md` — RECOMMENDED: lead with the BOUND canonical headline (~94% fewer output tokens, 1,213 vs 21,171; ~75% cheaper per session, $0.21 vs $0.83) measured against a live GitHub backend in P115; you MAY additionally cite the ~56% smaller total input-context axis as the direct analog to the old "tokens of context" phrasing;
  (3) name/link `docs/benchmarks/token-economy.md` (and optionally "P115") in-line so the figure is EVIDENCED, not free-floating.
  You MUST NOT write `89.1%`, `~2k`, `~150k`, `531 tokens`, or `4,883` — the 89.1%/531/4,883 synthetic figures are RETIRED (see <interfaces> "do NOT resurrect"); use DOCS-07's "BENCH-01's re-measured value" branch = the live headline. Do NOT touch any other line of PROJECT.md (line 50's "FUSE + git-remote-helper" describes the historical `initial-report.md` accurately and is OUT of P118 scope — see NOTICED). Prose only; final wording is yours within these guardrails. Reference DOCS-07 in the commit body.
  Then COMMIT (targeted stage, no `git add -A`, no `--no-verify`): `docs(118): re-cite PROJECT.md token figure to P115 live benchmark (DOCS-07)`.</action>
  <verify>
    <automated>F=.planning/PROJECT.md; echo "== defects gone =="; for bad in '/mnt/jira' '150k' '~2k' '89.1' '4,883' '531 tokens'; do n=$(grep -Fc "$bad" "$F"); [ "$n" -eq 0 ] && echo "PASS: no '$bad'" || echo "FAIL: '$bad' still in PROJECT.md ($n)"; done; echo "== git-native + evidenced =="; grep -q 'issues/' "$F" && echo "PASS: git-native path present" || echo "FAIL: no git-native issues/ path"; grep -q 'token-economy' "$F" && echo "PASS: cites token-economy.md" || echo "FAIL: no benchmark citation"; echo "== fix-twice (expect 0 bindings) =="; grep -c 'PROJECT.md' quality/catalogs/doc-alignment.json | { read n; [ "$n" -eq 0 ] && echo "PASS: no doc-alignment binding on PROJECT.md — no rebind owed" || echo "REVIEW: $n PROJECT.md bindings — rebind in THIS commit"; }</automated>
  </verify>
  <done>PROJECT.md:49 uses `cat issues/<id>.md`, cites a `docs/benchmarks/token-economy.md`-traceable figure with an in-line reference, contains none of `/mnt/jira` `150k` `~2k` `89.1` `4,883` `531 tokens`; doc-alignment grep confirms 0 PROJECT.md bindings; commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate the stale tag-cut premise as SUPERSEDED (SC2 / DOCS-09)</name>
  <files>.planning/milestones/audits/2026-07-12-reality-check.md</files>
  <action>Annotate — do NOT rewrite — the five stalled-tag premises listed in <interfaces> (L336-339, L409-411, L465-467, L482-485, L549-550). Preserve every original 2026-07-12 sentence VERBATIM (this is a historical audit; git history + the ADDENDUM are the canonical Arc D record). Add:
  (a) ONE dated master banner near the TOP of the document (immediately after the title/date preamble, ~line 8, before `## §1`) — a blockquote note: `> **UPDATE 2026-07-17 (P118 · DOCS-09) — STALE-TAG PREMISE SUPERSEDED.**` stating the current truth: v0.13.0, v0.13.1, v0.14.0 are ALL cut as git tags AND published GitHub releases (v0.14.0 = "Latest"; verified `git tag -l` + `gh release list`); the archival cascade has since run (P78-94 → `v0.13.0-phases/`, P102-113 → `v0.14.0-phases/`, neither range remains in live `.planning/phases/`); the "stalled/uncut owner-gated tags block the archival" framing recurring below (name the five line homes) is RESOLVED; the original text is preserved as the historical record — do NOT act on it. This is a PROSE correction, NOT a literal tag cut (the tags already exist).
  (b) A one-line inline marker at EACH of the five homes, e.g. `> **[SUPERSEDED 2026-07-17 — P118/DOCS-09: tags cut; see the top-of-doc note.]**`, placed adjacent to (not replacing) the stale sentence.
  RECOMMENDED (same banner, ownership touch): note that §5 open-question #1 (L547-548, "honest-but-synthetic 89.1% vs fund a live re-measurement") is RESOLVED — P115 funded the live re-measurement (`docs/benchmarks/token-economy.md`).
  Do NOT edit `.planning/ROADMAP.md`, `.planning/PROJECT.md`, `quality/catalogs/doc-alignment.json`, or any code. Reference DOCS-09 in the commit body.
  Then COMMIT (targeted stage, no `git add -A`, no `--no-verify`): `docs(118): annotate stale tag-cut premise as superseded (DOCS-09)`.</action>
  <verify>
    <automated>F=.planning/milestones/audits/2026-07-12-reality-check.md; echo "== annotations present (>=5 inline + 1 banner = >=6) =="; n=$(grep -Fc 'SUPERSEDED' "$F"); [ "$n" -ge 6 ] && echo "PASS: $n SUPERSEDED markers" || echo "FAIL: only $n SUPERSEDED markers (need >=6: banner + 5 homes)"; grep -Fc '2026-07-17' "$F" | { read d; [ "$d" -ge 1 ] && echo "PASS: dated ($d)" || echo "FAIL: undated"; }; echo "== current truth stated =="; grep -q 'Latest' "$F" && grep -Fq 'v0.13.1' "$F" && echo "PASS: banner names releases incl v0.13.1" || echo "FAIL: banner missing release truth"; echo "== history preserved (original stale sentences NOT deleted) =="; s=$(grep -Fc 'stalled tag' "$F"); u=$(grep -Fc 'uncut' "$F"); echo "original-text markers still present: 'stalled tag'=$s 'uncut'=$u (both must be >=1 — annotation, not rewrite)"; [ "$s" -ge 1 ] && [ "$u" -ge 1 ] && echo "PASS: original text preserved" || echo "FAIL: original audit text was rewritten/removed"</automated>
  </verify>
  <done>The audit carries a dated 2026-07-17 master banner + an inline SUPERSEDED marker at each of the five homes (>=6 total); the banner states all three tags are cut+released (names v0.13.1, "Latest"); the original "stalled tag"/"uncut" sentences are still present verbatim; commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark doc → planning prose | figures copied from `docs/benchmarks/token-economy.md` into PROJECT.md — a committed, CI-reproducible source, not tainted backend bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Information disclosure | figures cited in PROJECT.md | accept | Figures come from the committed, offline-reproducible `token-economy.md` (no secrets, no private backend body content — captures are scrubbed); no tainted remote byte is routed into the doc. |
| T-118-02 | Tampering (truth-rot) | audit annotation | mitigate | Original audit text is preserved verbatim (grep-verified in Task 2); corrections are additive dated banners, so the historical record cannot be silently rewritten. |
</threat_model>

<verification>
Goal-backward — what must be TRUE for a verifier to grade GREEN:

- **SC1 (DOCS-07):** `grep -F '/mnt/jira' .planning/PROJECT.md` = 0 AND `grep -F '150k' .planning/PROJECT.md` = 0 AND `grep -F '89.1' .planning/PROJECT.md` = 0 (retired figure not resurrected) AND `grep 'issues/' .planning/PROJECT.md` ≥ 1 (git-native path) AND `grep 'token-economy' .planning/PROJECT.md` ≥ 1 (evidenced). The cited figure must be locatable in `docs/benchmarks/token-economy.md` (94.3% / 74.9% / 55.6% headline table).
- **SC2 (DOCS-09):** the audit has ≥6 `SUPERSEDED` markers (1 banner + 5 homes), a `2026-07-17` date, a banner naming v0.13.1 + "Latest", AND still contains the original `stalled tag` / `uncut` sentences (annotation, not rewrite).
- **SC3 (prose-only invariant):** `git diff --name-only <phase-base>..HEAD` lists ONLY `.planning/PROJECT.md` and `.planning/milestones/audits/2026-07-12-reality-check.md` — NO `*.rs`, NO `crates/**`, NO `Cargo.*`, NO `docs/**`, NO `quality/catalogs/doc-alignment.json`, NO literal git tag created. Verifier check: `git diff --name-only HEAD~2..HEAD | grep -Ev '^\.planning/(PROJECT\.md|milestones/audits/2026-07-12-reality-check\.md)$'` returns nothing.
- **Fix-twice:** `grep -c 'PROJECT.md' quality/catalogs/doc-alignment.json` = 0 and `grep -c 'reality-check' quality/catalogs/doc-alignment.json` = 0 (no binding → no rebind owed; a passing grep is the artifact).
- **Phase close (mandatory):** `git push origin main` (pre-push hook runs automatically) BEFORE the verifier dispatch, THEN `python3 quality/runners/run.py --cadence post-push --persist` — the `code/ci-green-on-main` (P0) probe must show main's NEWEST `ci.yml` run concluded success. Never open P119 over a red main.
- **OWNERSHIP CHARTER:** report a Noticing section (see NOTICED below); an empty one from doc-touching work is a red flag.
</verification>

<success_criteria>
SC1: PROJECT.md's Context "Why this exists" bullet uses a git-native `cat issues/<id>.md` example and an evidenced figure traceable to `docs/benchmarks/token-economy.md` (P115 live re-measurement); the FUSE path and the unevidenced `~2k vs ~150k` ratio are gone; the retired synthetic `89.1%` is NOT resurrected.
SC2: All five stalled-tag premises in the 2026-07-12 reality-check are annotated SUPERSEDED with a dated 2026-07-17 note stating v0.13.0/v0.13.1/v0.14.0 are all tagged + released; original audit text preserved verbatim.
SC3: Prose-only — exactly two `.planning/*.md` files changed across two atomic commits; no code, no cargo, no behavior change, no Arc D re-open, no literal tag cut.
</success_criteria>

<output>
After completion, create `.planning/phases/118-post-bench-honesty-corrections/118-01-SUMMARY.md`. Commit with targeted staging only (never `git add -A`/`.`), no `--no-verify`, trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

RAISE LIST (surface to coordinator/verifier before/with the summary):
- R1 — DOCS-07 literal text says "(or the CI-verified 89.1% figure)"; that figure is now RETIRED-synthetic. This plan reads DOCS-07's "(or BENCH-01's re-measured value)" branch as operative. If a verifier expects a literal `89.1%`, escalate — do NOT resurrect it.
- R2 — PROJECT.md:50 ("architecture deep-dive on FUSE + git-remote-helper") and PROJECT.md:41 ("FUSE on Linux only") retain FUSE framing but are OUT of P118 scope (P118 = the token figure + tag premise only). Left intentionally; flag as NOTICED, do not fix here.
- R3 — §5 open-question #1 (audit L547-548, the "89.1% vs re-measure" fork) is resolved by P115; annotated as a companion note in Task 2's banner. If the verifier wants it as a hard SC, escalate (currently RECOMMENDED, not required).
</output>
