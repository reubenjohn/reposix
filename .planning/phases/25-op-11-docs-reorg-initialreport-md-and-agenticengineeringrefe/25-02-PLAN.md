---
plan: 25-02
wave: 2
phase: 25
title: "v0.7.0 version bump, CHANGELOG promotion, STATE.md cursor, SUMMARY"
depends_on: [25-01]
files_modified:
  - Cargo.toml
  - Cargo.lock
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-SUMMARY.md
autonomous: true
requirements_addressed: []

must_haves:
  truths:
    - "Cargo workspace version is 0.7.0"
    - "CHANGELOG.md has a [v0.7.0] section (promoted from [Unreleased])"
    - "STATE.md cursor marks Phase 25 shipped and points at Phase 26"
    - "ROADMAP.md Phase 25 plan list shows both plans checked"
    - "ROADMAP.md Phase 25 Goal/Requirements text is accurate (not copy-pasted labels text from Phase 19)"
    - "25-SUMMARY.md exists with artifacts produced and decisions made"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace version = 0.7.0"
      contains: "version = \"0.7.0\""
    - path: "CHANGELOG.md"
      provides: "v0.7.0 release entry"
      contains: "[v0.7.0]"
    - path: ".planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-SUMMARY.md"
      provides: "phase completion record"
  key_links:
    - from: "Cargo.toml workspace version"
      to: "0.7.0"
      via: "version field"
      pattern: "version = \"0.7.0\""
    - from: "CHANGELOG.md"
      to: "[v0.7.0]"
      via: "promotion from [Unreleased]"
      pattern: "\\[v0\\.7\\.0\\]"
---

<objective>
Bump the workspace Cargo version from 0.6.0 to 0.7.0, promote the CHANGELOG [Unreleased] section to [v0.7.0], update STATE.md and ROADMAP.md to mark Phase 25 shipped, and write the phase SUMMARY.

Purpose: Complete the v0.7.0 milestone release bookkeeping — docs reorg was the last phase in the 21–25 block.
Output: Cargo.toml at 0.7.0, Cargo.lock regenerated, CHANGELOG promoted, STATE.md cursor at Phase 26, SUMMARY written.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/CONTEXT.md
@.planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargo version bump + CHANGELOG promotion</name>
  <files>Cargo.toml, Cargo.lock, CHANGELOG.md</files>
  <read_first>
    - Cargo.toml (lines 1–20 — find `[workspace.package]` version field, currently `version = "0.6.0"`)
    - CHANGELOG.md (lines 1–30 — find `## [Unreleased]` section and its contents; note the comparison URL at the bottom uses `v0.5.0...HEAD` and needs to be updated)
  </read_first>
  <action>
    Step 1 — Bump Cargo.toml workspace version. In `[workspace.package]`, change:
    ```
    version = "0.6.0"
    ```
    to:
    ```
    version = "0.7.0"
    ```
    Only edit this single field. Do not touch any per-crate version overrides.

    Step 2 — Regenerate Cargo.lock by running:
    ```bash
    cargo check --workspace
    ```
    This updates Cargo.lock to reflect the new workspace version without a full build.

    Step 3 — Promote CHANGELOG.md `[Unreleased]` → `[v0.7.0]`. The current CHANGELOG has:
    ```
    ## [Unreleased]
    ```
    at the top. Replace this heading with:
    ```
    ## [v0.7.0] — 2026-04-16
    ```
    Insert a new blank `## [Unreleased]` section above it (so future work has a target).

    Then find the comparison URL at the bottom of CHANGELOG.md — currently:
    ```
    [Unreleased]: https://github.com/reubenjohn/reposix/compare/v0.5.0...HEAD
    ```
    Replace it with two lines:
    ```
    [Unreleased]: https://github.com/reubenjohn/reposix/compare/v0.7.0...HEAD
    [v0.7.0]: https://github.com/reubenjohn/reposix/compare/v0.6.0...v0.7.0
    ```

    If the `[v0.7.0]` section body (promoted from Unreleased) contains Phase 21–24 entries, keep them all — they are all v0.7.0 work. If the Unreleased section is empty or sparse, add a minimum entry:
    ```markdown
    ### Changed
    - Moved `InitialReport.md` and `AgenticEngineeringReference.md` to `docs/research/` (OP-11); root stubs contain visible redirect notes. Updated all cross-references in `CLAUDE.md`, `README.md`, and `.planning/research/threat-model-and-critique.md`. Added `Research` section to mkdocs.yml nav.
    - Workspace version bumped `0.6.0 → 0.7.0`.
    ```
  </action>
  <verify>
    <automated>
      cd /home/reuben/workspace/reposix && grep -c 'version = "0.7.0"' Cargo.toml && grep -c '\[v0\.7\.0\]' CHANGELOG.md
    </automated>
    Expected: first line prints 1 (Cargo.toml has the new version), second prints 1 ([v0.7.0] entry exists in CHANGELOG).
  </verify>
  <acceptance_criteria>
    - `grep 'version = "0.7.0"' Cargo.toml` exits 0
    - `grep '\[v0\.7\.0\]' CHANGELOG.md` exits 0
    - `grep '\[Unreleased\]' CHANGELOG.md` exits 0 (blank new Unreleased section exists at top)
    - `grep 'v0\.6\.0\.\.\.v0\.7\.0' CHANGELOG.md` exits 0 (comparison URL added)
    - `test -f Cargo.lock` exits 0 (Cargo.lock exists and was regenerated)
    - `cargo check --workspace 2>&1 | grep -c "^error"` = 0
  </acceptance_criteria>
  <done>Cargo.toml at 0.7.0, Cargo.lock regenerated, CHANGELOG promoted with v0.7.0 entry and correct comparison URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ROADMAP.md Phase 25 entry + update STATE.md + write 25-SUMMARY.md</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-SUMMARY.md
  </files>
  <read_first>
    - .planning/STATE.md (full — need current cursor and milestone fields)
    - .planning/ROADMAP.md (lines 647–660 — Phase 25 section; note the Goal/Requirements text is WRONG: it currently says labels overlay from Phase 19 copy-paste; this must be corrected)
    - @$HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
  </read_first>
  <action>
    Step 1 — Fix the stale Phase 25 Goal/Requirements text in ROADMAP.md. The Phase 25 section currently has an incorrect Goal and Requirements (copy-pasted from Phase 19 labels work). Replace the entire Phase 25 body — from `**Goal:**` through the `Plans:` list — with:

    ```markdown
    **Goal:** Move `InitialReport.md` and `AgenticEngineeringReference.md` to `docs/research/`, update all cross-references in `CLAUDE.md`, `README.md`, and `.planning/research/`, add a Research section to mkdocs.yml nav, and bump workspace to v0.7.0.
    **Requirements**: none (docs-only phase)
    **Depends on:** Phase 24
    **Plans:** 2 plans complete

    Plans:
    - [x] 25-01-PLAN.md — cross-ref audit, root clutter cleanup, mkdocs.yml nav
    - [x] 25-02-PLAN.md — v0.7.0 version bump, CHANGELOG promotion, STATE.md, SUMMARY
    ```

    Step 2 — Update STATE.md. Find the `## Current Position` section and make these changes:
    - `Cursor:` line → update to: `**Phase 25 SHIPPED. OP-11 docs reorg complete — InitialReport.md and AgenticEngineeringReference.md moved to docs/research/; cross-refs updated; mkdocs.yml nav includes Research section; v0.7.0 workspace version bumped. Next: Phase 26 (docs clarity overhaul).**`
    - `Status:` → `Phase complete — ready for Phase 26`
    - `last_updated` → `"2026-04-16T00:00:00.000Z"`

    Also add a Roadmap Evolution bullet to `## Accumulated Context` → `### Roadmap Evolution`:
    ```
    - **Phase 25 SHIPPED (2026-04-16):** OP-11 docs reorg — InitialReport.md → docs/research/initial-report.md, AgenticEngineeringReference.md → docs/research/agentic-engineering-reference.md; root stubs have visible markdown redirect notes; CLAUDE.md, README.md, threat-model-and-critique.md cross-refs updated; mkdocs.yml Research nav section added; v0.7.0 workspace version bump + CHANGELOG promotion.
    ```

    Step 3 — Write `25-SUMMARY.md` using the summary template. Key fields:
    - Phase: 25
    - Goal: OP-11 docs reorg — move InitialReport.md and AgenticEngineeringReference.md to docs/research/
    - Status: SHIPPED
    - Plans executed: 25-01, 25-02
    - Artifacts produced:
      - `docs/research/initial-report.md` (pre-existing from earlier commit — confirmed in place)
      - `docs/research/agentic-engineering-reference.md` (pre-existing from earlier commit — confirmed in place)
      - `InitialReport.md` (root stub with visible markdown blockquote redirect)
      - `AgenticEngineeringReference.md` (root stub with visible markdown blockquote redirect)
      - `mkdocs.yml` — Research nav section added between Decisions and Reference
      - `CLAUDE.md` — cross-refs updated to docs/research/ paths
      - `.planning/research/threat-model-and-critique.md` — ~15 bare cross-refs updated to docs/research/ paths
      - `CHANGELOG.md` — [v0.7.0] promoted from [Unreleased]
      - `Cargo.toml` — version 0.6.0 → 0.7.0
      - `Cargo.lock` — regenerated
    - Decisions made:
      - Root stub format: visible markdown blockquote redirect (not invisible HTML comment)
      - mkdocs.yml Research section inserted between Decisions and Reference in the nav
    - Tests: no Rust code changes; `cargo check --workspace` green
    - Next: Phase 26 (docs clarity overhaul)
  </action>
  <verify>
    <automated>
      cd /home/reuben/workspace/reposix && grep -c "Phase 25 SHIPPED" .planning/STATE.md && test -f .planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-SUMMARY.md && echo "SUMMARY exists"
    </automated>
    Expected: prints 1, then "SUMMARY exists".
  </verify>
  <acceptance_criteria>
    - `grep -q "Phase 25 SHIPPED" .planning/STATE.md` exits 0
    - `grep -q "Phase 26" .planning/STATE.md` exits 0 (cursor points at Phase 26)
    - `grep -q "25-01-PLAN.md" .planning/ROADMAP.md` exits 0
    - `grep -q "25-02-PLAN.md" .planning/ROADMAP.md` exits 0
    - `grep -q "LABEL-01" .planning/ROADMAP.md | grep -q "Phase 25"` — this must NOT match (stale labels text removed)
    - `test -f .planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-SUMMARY.md` exits 0
    - 25-SUMMARY.md contains `Status: SHIPPED`
  </acceptance_criteria>
  <done>ROADMAP.md Phase 25 section corrected and plan list marked complete; STATE.md cursor updated to Phase 26; 25-SUMMARY.md written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → planning docs | All changes are to docs/planning files; no trust boundary crossings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Tampering | Cargo.toml version | accept | Version bump is docs-level bookkeeping; no security surface |
</threat_model>

<verification>
After both tasks complete:
1. `grep 'version = "0.7.0"' Cargo.toml` → match
2. `grep '\[v0\.7\.0\]' CHANGELOG.md` → match
3. `grep -q "Phase 25 SHIPPED" .planning/STATE.md` → exits 0
4. `cargo check --workspace 2>&1 | grep -c "^error"` → 0
5. `test -f .planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-SUMMARY.md` → exits 0
6. `grep -q "LABEL-01" .planning/ROADMAP.md` confirms LABEL-01 only appears in Phase 19 section, not Phase 25
</verification>

<success_criteria>
- Workspace at v0.7.0 with regenerated Cargo.lock
- CHANGELOG [v0.7.0] entry exists with Phase 21-25 work
- STATE.md cursor points at Phase 26
- ROADMAP.md Phase 25 goal text is correct (not stale labels copy-paste)
- ROADMAP.md Phase 25 plan list fully checked
- 25-SUMMARY.md written
</success_criteria>

<output>
After completion, the file `.planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-SUMMARY.md` IS the output artifact for this plan — it is written by Task 2, not after.
</output>
