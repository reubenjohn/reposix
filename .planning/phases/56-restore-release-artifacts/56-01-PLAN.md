---
phase: 56-restore-release-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/docs_reproducible_catalog.json
  - .planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md
autonomous: true
requirements:
  - RELEASE-01
  - RELEASE-02
  - RELEASE-03
must_haves:
  truths:
    - "A committed catalog defines what GREEN looks like for the 5 install paths BEFORE any release.yml change lands."
    - "Each install row carries asserts the verifier subagent in Wave D will read with zero session context."
    - "The catalog's schema-version + temporary-home decision is recorded so P57 can migrate it cleanly."
  artifacts:
    - path: ".planning/docs_reproducible_catalog.json"
      provides: "Refreshed install rows with current asserts; schema_status flips DRAFT→ACTIVE-V0; new fields phase=p56 and last_verified_at:null on the 5 install rows."
      contains: "install/curl-installer-sh"
    - path: ".planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md"
      provides: "One-page rationale for keeping the catalog in .planning/ vs. creating quality/catalogs/release-assets.json early; documents the P57 migration plan."
      contains: "temporary-home"
  key_links:
    - from: ".planning/docs_reproducible_catalog.json"
      to: "Wave D verifier subagent"
      via: "expected_outcome.asserts is the contract the verifier grades against"
      pattern: "expected_outcome"
    - from: ".planning/docs_reproducible_catalog.json"
      to: ".github/workflows/release.yml (Wave B)"
      via: "remediation_hint field tells Wave B what to edit"
      pattern: "remediation_hint"
---

<objective>
Wave A — the **catalog-first commit**. Refresh the 5 install rows in `.planning/docs_reproducible_catalog.json` so they are the contract Wave D's verifier subagent reads. NO release.yml edits in this plan; this commit only declares "here's what GREEN means for P56."

Per `.planning/research/v0.12.0-autonomous-execution-protocol.md` Step 3: every phase's FIRST commit MUST write the catalog rows that define this phase's end-state contract. The verifier subagent reads catalog rows that exist BEFORE the implementation lands, so the contract cannot be retro-fitted to whatever the agent shipped.

Purpose: lock the asserts. After this commit, the agent cannot quietly shrink scope (e.g., "ship curl path only, defer ps1") without an explicit waiver row.

Output: a refreshed `docs_reproducible_catalog.json` with 5 install rows pinned to phase=p56 + a one-page `56-01-CATALOG-NOTE.md` documenting the temporary-home decision so P57 has clean migration instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/v0.12.0-install-regression-diagnosis.md
@.planning/research/v0.12.0-autonomous-execution-protocol.md
@.planning/research/v0.12.0-roadmap-and-rationale.md
@.planning/docs_reproducible_catalog.json
@CLAUDE.md

<interfaces>
The catalog file already has a `claims[]` array with 5 install rows (`install/curl-installer-sh`, `install/powershell-installer-ps1`, `install/cargo-binstall`, `install/homebrew`, `install/build-from-source`). Each row's existing fields are documented in the `field_glossary` block at the top of the file. We are NOT inventing new fields — we are:
1. Adding `phase: "p56"` to the 5 install rows (preserves traceability when P57 migrates them).
2. Setting `last_verified_at: null` on rows that currently say `"2026-04-27T15:59:00Z"` (the old timestamp is stale; Wave C will overwrite with the real verification time).
3. Tightening `expected_outcome.asserts` for the curl/ps1/binstall/homebrew rows to match the validation steps in `v0.12.0-install-regression-diagnosis.md` (the asserts there are the canonical contract).
4. Flipping the file's top-level `schema_status` from `"DRAFT — not wired into pre-push or CI yet; pending owner review"` to `"ACTIVE-V0 — P56 verifier reads this as the install-path contract; P57 will migrate to quality/catalogs/install-paths.json with the unified schema (id/dimension/cadence/kind/sources/verifier/artifact/status/freshness_ttl/waiver/blast_radius/owner_hint)"`.

The unified-schema fields land in P57 (per `.planning/research/v0.12.0-naming-and-architecture.md`). For P56 we deliberately do NOT shoehorn unified fields into v0 — the current schema is sufficient for the verifier subagent + Wave B implementor, and the P57 migration is one-shot rather than a half-half mess.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 56-01-A: Write the temporary-home rationale doc</name>
  <files>.planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md</files>
  <action>
Create a ≤80-line markdown doc explaining:
1. **Why the catalog rows live in `.planning/docs_reproducible_catalog.json` for P56 (not `quality/catalogs/release-assets.json`).** Reasons (cite REQUIREMENTS.md `## v0.12.0` § "Release dimension — close the immediate breakage" and the prompt's constraint paragraph): (a) `quality/` does not exist yet — P57 ships the framework skeleton (QG-01); creating a one-row `quality/catalogs/` outside that scaffolded directory pre-empts the framework's own first commit and risks shape-drift. (b) the v0 catalog already has the 5 install rows with field semantics that map 1:1 onto the unified schema (sources↔sources, verifier↔verifier, expected_outcome.asserts↔verifier.expected_asserts, blast_radius↔blast_radius, automation_status↔cadence). (c) P57 has explicit absorption work for this file already: see ROADMAP.md Phase 57 / Phase 59 entries — Phase 59 ports the DRAFT seed to `quality/catalogs/docs-reproducible.json`; Phase 58 ships `quality/catalogs/install-paths.json` (the install rows split out at that point). Forcing a half-step in P56 doubles the migration churn.
2. **What P57/P58 must do to migrate cleanly.** Concrete checklist: (a) the 5 install rows split — copy to `quality/catalogs/install-paths.json` with unified-schema fields (P58 SIMPLIFY work); the docs-tutorial + benchmark + troubleshooting rows stay in `docs_reproducible_catalog.json` and migrate to `quality/catalogs/docs-reproducible.json` (P59 DOCS-REPRO-04). (b) preserve the `phase: "p56"` provenance tag on every migrated row so audit-trail "which phase declared this contract first" is recoverable.
3. **Pivot rule for Wave B (Option A → Option B).** Restate the diagnosis-doc rule verbatim with the empty-bullet format: "If `${{ github.ref }}` substitutions in `release.yml` cannot be cleanly mapped from `reposix-cli-v0.11.3` to a `version=0.11.3` for archive filenames (current `reposix-${TAG}-${target}.tar.gz` would become `reposix-reposix-cli-v0.11.3-…`), pivot to Option B per `.planning/research/v0.12.0-install-regression-diagnosis.md` § 'Option B'. Append the pivot to `quality/SURPRISES.md` (Wave D creates the file)."
4. **Anti-bloat note.** "This file is single-purpose. Do not grow it. P57 deletes it as part of the migration close-out."

End with a one-line: "Cross-references: REQUIREMENTS.md ## v0.12.0 RELEASE-01..03; ROADMAP.md ## Phase 56; .planning/research/v0.12.0-install-regression-diagnosis.md."
  </action>
  <verify>
    <automated>test -f .planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md &amp;&amp; wc -l .planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md | awk '{exit ($1 &gt; 80) ? 1 : 0}'</automated>
  </verify>
  <done>
File exists, is ≤80 lines, contains all four sections, names "Option A" + "Option B" + "SURPRISES.md" verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 56-01-B: Refresh the 5 install rows in docs_reproducible_catalog.json</name>
  <files>.planning/docs_reproducible_catalog.json</files>
  <action>
Edit `.planning/docs_reproducible_catalog.json` IN PLACE. JSON is hand-written and pretty; preserve indentation (2 spaces) and field ordering of existing rows.

**Step 1 — Top-level schema_status flip.** Replace the existing `"schema_status": "DRAFT — not wired into pre-push or CI yet; pending owner review"` with: `"schema_status": "ACTIVE-V0 — P56 verifier reads this as the install-path contract; P57 will migrate the install rows to quality/catalogs/install-paths.json with the unified schema (id/dimension/cadence/kind/sources/verifier/artifact/status/freshness_ttl/waiver/blast_radius/owner_hint), the docs/tutorial/benchmark rows to quality/catalogs/docs-reproducible.json"`.

**Step 2 — For each of the 5 install rows** (`install/curl-installer-sh`, `install/powershell-installer-ps1`, `install/cargo-binstall`, `install/homebrew`, `install/build-from-source`):
   a. Add a `"phase": "p56"` field immediately after the existing `"id"` field. (For build-from-source, the row stays at status: PASS but still gets phase tag for traceability.)
   b. Set `"last_verified_at": null` on the 4 broken/stale rows (curl, powershell, cargo-binstall, homebrew). (Build-from-source keeps its `"implicit via ci.yml \`test\` job last green run"` note.)

**Step 3 — Tighten `expected_outcome.asserts` per row** to match `.planning/research/v0.12.0-install-regression-diagnosis.md` § "Validation steps (the catalog row's `expected.asserts` for RELEASE-01)":

   - **install/curl-installer-sh** asserts (replace existing list with):
     ```
     "GET https://github.com/reubenjohn/reposix/releases/latest/download/reposix-installer.sh returns HTTP 200",
     "the response body is &gt;= 1024 bytes and starts with '#!'",
     "in a fresh ubuntu:24.04 container with curl installed, the documented one-liner exits 0",
     "after the one-liner, `command -v reposix` exits 0",
     "after the one-liner, `command -v git-remote-reposix` exits 0",
     "`reposix --version` prints the latest workspace version (matches the GH release tag's version component)"
     ```

   - **install/powershell-installer-ps1** asserts (replace with):
     ```
     "GET https://github.com/reubenjohn/reposix/releases/latest/download/reposix-installer.ps1 returns HTTP 200",
     "the response body is &gt;= 1024 bytes",
     "container-rehearsal deferred to v0.12.1 windows runner; for v0.12.0 the asset-existence + min-bytes check is the contract"
     ```
     (The container-rehearsal half is explicitly deferred — `.planning/research/v0.12.0-roadmap-and-rationale.md` § "Compression options" calls out windows runners as v0.12.1 cost. Do NOT add a windows-rehearse assert. Wave D's verifier reads asset-existence only.)

   - **install/cargo-binstall** asserts (replace with):
     ```
     "in a fresh rust:1.82-slim container with cargo-binstall installed, `cargo binstall --no-confirm reposix-cli reposix-remote` exits 0",
     "stdout/stderr show binstall resolving to a prebuilt GitHub release binary, NOT a source-build fallback (assert by greppping for 'Downloading' against a github.com/reubenjohn/reposix/releases/download URL; failure mode: assert FAILS if the log contains the string 'Falling back' or 'compiling reposix' against a crates.io source path)",
     "after install, `command -v reposix` and `command -v git-remote-reposix` both exit 0",
     "`reposix --version` matches the latest crates.io max_version of reposix-cli"
     ```

   - **install/homebrew** asserts (replace with):
     ```
     "GET https://api.github.com/repos/reubenjohn/homebrew-reposix/contents/Formula/reposix.rb returns HTTP 200",
     "the formula's `version \"X.Y.Z\"` string equals the latest reposix-cli release tag's version component (parsed from the GH release tag, e.g. reposix-cli-v0.11.3 → 0.11.3)",
     "the formula's three sha256 strings are non-empty 64-hex-char strings (no template literals or empty quotes)",
     "container-rehearsal deferred to v0.12.1 macos runner per cost-conscious decision; for v0.12.0 the formula-version check is the contract"
     ```

   - **install/build-from-source** asserts (keep mostly as-is, but reword for clarity — replace existing list with):
     ```
     "in the current git checkout, `cargo build --release --workspace --bins` exits 0",
     "target/release/reposix exists and is executable",
     "target/release/git-remote-reposix exists and is executable",
     "covered by ci.yml `test` job — no separate Wave D verifier action needed"
     ```

**Step 4 — Update `failure_mode` and `remediation_hint` on the 4 broken rows.** Each row's `remediation_hint` should now read: `"P56 Wave B fixes .github/workflows/release.yml to fire on `reposix-cli-v*` tags (Option A); pivot to Option B (release-plz mirror tag) if Option A breaks the version derivation. See .planning/research/v0.12.0-install-regression-diagnosis.md."`. The `failure_mode` strings stay (they accurately describe the current state until Wave C verifies).

**Step 5 — Validate the JSON.** Run `python3 -c "import json; json.load(open('.planning/docs_reproducible_catalog.json'))"` and confirm exit 0. If JSON is malformed, fix immediately — Wave D's subagent reads this file and a parse failure breaks the contract.

**Step 6 — Do NOT touch:** the 5 non-install rows (`tutorial/first-run-end-to-end`, `quickstart/index-after-one-commit`, `quickstart/readme-five-line`, `benchmark-claim/8ms-cached-read`, `benchmark-claim/89.1-percent-token-reduction`, `troubleshooting/sparse-checkout-recovery`). Those belong to P59 (DOCS-REPRO-04). P56 only touches the 5 install rows + the top-level schema_status string.

Per CLAUDE.md `Operating Principles` OP-6 (ground truth): the catalog file is the contract; if any row's asserts are vague, Wave D's subagent will refuse to grade GREEN. Be specific.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('.planning/docs_reproducible_catalog.json')); install_rows=[c for c in d['claims'] if c['id'].startswith('install/')]; assert len(install_rows)==5, f'expected 5 install rows, got {len(install_rows)}'; assert all(c.get('phase')=='p56' for c in install_rows), 'every install row needs phase: p56'; assert d['schema_status'].startswith('ACTIVE-V0'), 'schema_status must flip to ACTIVE-V0'; print('catalog OK', len(install_rows), 'install rows tagged p56')"</automated>
  </verify>
  <done>
JSON parses; 5 install rows have `"phase": "p56"`; top-level `schema_status` starts with `"ACTIVE-V0"`; the 4 broken rows have `last_verified_at: null`; install/curl-installer-sh's `expected_outcome.asserts` contains "container" + "command -v reposix" + ">= 1024 bytes"; non-install rows untouched.
  </done>
</task>

<task type="auto">
  <name>Task 56-01-C: Catalog-first commit (single commit, no implementation yet)</name>
  <files>.planning/docs_reproducible_catalog.json, .planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md</files>
  <action>
Stage and commit ONLY these two files (no `release.yml` change yet — that's Wave B). Commit message:

```
docs(p56): catalog-first commit — refresh install-row contract for RELEASE-01..03

Refreshes the 5 install rows in .planning/docs_reproducible_catalog.json
so they are the GREEN-criterion contract that Wave D's verifier subagent
reads with zero session context. Per v0.12.0 autonomous-execution
protocol step 3: catalog rows MUST land BEFORE the implementation, so
the contract cannot be retro-fitted.

Changes:
- Top-level schema_status: DRAFT → ACTIVE-V0 with P57 migration plan inline.
- 5 install rows tagged with phase: "p56" for traceability through P57/P58
  catalog migration.
- expected_outcome.asserts tightened on curl/powershell/cargo-binstall/
  homebrew rows to match the validation steps in
  .planning/research/v0.12.0-install-regression-diagnosis.md
  (asset-existence + min-bytes + container-rehearsal where ci-friendly).
- Windows + macOS container rehearsals explicitly deferred to v0.12.1
  per cost-conscious decision in
  .planning/research/v0.12.0-roadmap-and-rationale.md.
- New .planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md
  documents why the catalog stays in .planning/ (vs. creating a
  one-row quality/catalogs/release-assets.json before the framework
  exists) + the P57/P58 migration checklist.

NO release.yml edit in this commit — that is Wave B per PLAN 56-02.
The verifier subagent (Wave D, PLAN 56-04) reads the catalog as the
contract; the implementation has 24h to land before the contract
becomes a stale DRAFT.

Refs: RELEASE-01, RELEASE-02, RELEASE-03
```

**Critical:** do NOT include any `release.yml` or `release-plz.yml` edits in this commit. If the working tree has any other staged changes, abort and ask the orchestrator (something is wrong with the wave isolation).

Run pre-push hook locally before push: `git push` will trigger `scripts/end-state.py verify` + the freshness invariants. Confirm GREEN before continuing to Wave B.
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format: | grep -E 'docs_reproducible_catalog\.json|56-01-CATALOG-NOTE\.md' | wc -l | awk '$1==2 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
HEAD commit changes exactly two files (`.planning/docs_reproducible_catalog.json` and `.planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md`); commit message body cites RELEASE-01..03; pre-push hook GREEN; no `release.yml`/`release-plz.yml` in HEAD's tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → committed JSON | Hand-edit of catalog file; risk of malformed JSON breaking downstream tooling. |
| catalog ↔ verifier subagent | Subagent reads JSON; vague/missing asserts let agent declare GREEN on partial work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | `.planning/docs_reproducible_catalog.json` | mitigate | Task 56-01-B Step 5 runs `json.load()` immediately after edit; commit hook also catches malformed JSON if `scripts/end-state.py` references the file. |
| T-56-02 | Repudiation | catalog row provenance | mitigate | Per-row `"phase": "p56"` field — every assert is traceable to the phase that declared it; survives P57/P58 migration. |
| T-56-03 | Information disclosure | none — catalog file is public-by-design (committed to repo) | accept | No PII or secrets; install URLs are already documented in README.md. |
| T-56-04 | Denial of service | breaks Wave D verifier dispatch | mitigate | Wave D's verifier subagent reads the JSON; Task 56-01-B Step 5 + Task 56-01-C verify-gate guarantee well-formed JSON before commit pushes. |
| T-56-05 | Spoofing | n/a — no auth boundary in this wave | accept | All work is local-edit + commit; no remote API calls in Wave A. |
| T-56-06 | Elevation of privilege | n/a | accept | Pure file-edit work; no script execution surfaces. |
</threat_model>

<verification>
After this plan ships, the following must be true:

1. `git log -1 --name-only` lists exactly 2 files: `.planning/docs_reproducible_catalog.json` + `.planning/phases/56-restore-release-artifacts/56-01-CATALOG-NOTE.md`.
2. `python3 -c "import json; json.load(open('.planning/docs_reproducible_catalog.json'))"` exits 0.
3. `jq '[.claims[] | select(.id | startswith("install/"))] | length' .planning/docs_reproducible_catalog.json` returns `5`.
4. `jq '[.claims[] | select(.id | startswith("install/"))] | map(.phase) | unique' .planning/docs_reproducible_catalog.json` returns `["p56"]`.
5. `jq '.schema_status' .planning/docs_reproducible_catalog.json` starts with `"ACTIVE-V0"`.
6. The pre-push hook (`scripts/end-state.py verify`) is GREEN — no freshness-invariant regressions from the JSON edit.
</verification>

<success_criteria>
The catalog-first commit lands. Three downstream consumers can now do their work:

- **Wave B (PLAN 56-02)** reads the `remediation_hint` field on each install row to know which file to edit (`.github/workflows/release.yml`).
- **Wave C (PLAN 56-03)** reads `expected_outcome.asserts` to know what to verify against the freshly-cut release.
- **Wave D (PLAN 56-04)** reads the same `expected_outcome.asserts` as the unbiased contract; the verifier subagent grades PASS only if every assert is met.

If Wave A is skipped or partial, downstream waves have no anchor — the verifier subagent at phase close will refuse to grade GREEN, and RELEASE-01..03 stay `planning`.
</success_criteria>

<output>
After completion, create `.planning/phases/56-restore-release-artifacts/56-01-SUMMARY.md` with: commit SHA, files changed, JSON validation output, the 5 install row IDs, the schema_status string, the pivot rule reference, and a one-line "ready for Wave B" handoff.
</output>
