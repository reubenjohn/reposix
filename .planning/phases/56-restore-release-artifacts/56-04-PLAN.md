---
phase: 56-restore-release-artifacts
plan: 04
type: execute
wave: 4
depends_on:
  - "56-01"
  - "56-02"
  - "56-03"
files_modified:
  - CLAUDE.md
  - .planning/STATE.md
  - quality/SURPRISES.md
  - .planning/docs_reproducible_catalog.json
  - quality/reports/verdicts/p56/
autonomous: false
requirements:
  - RELEASE-01
  - RELEASE-02
  - RELEASE-03
must_haves:
  truths:
    - "CLAUDE.md gains a 'v0.12.0 Quality Gates — forward pointer' section in the same wave the phase ships, satisfying QG-07 (mandatory CLAUDE.md update per phase)."
    - "quality/SURPRISES.md is created and seeded with at minimum the latest-pointer caveat and the cargo-binstall metadata follow-up if applicable."
    - "An unbiased verifier subagent (zero session context) grades each of the 5 install rows AND the QG-07 CLAUDE.md update AND the SURPRISES.md presence — and writes a verdict to quality/reports/verdicts/p56/<ts>.md."
    - "The verdict is GREEN before the phase is declared closed; if RED, the phase loops back to fix the failing row(s)."
    - "STATE.md cursor is advanced to position Phase 56 as shipped; the install rows in the catalog flip from FAIL/PARTIAL to PASS (timestamps from Wave C JSON evidence)."
  artifacts:
    - path: "CLAUDE.md"
      provides: "New section pointing at v0.12.0 Quality Gates research bundle + forward-pointer to quality/PROTOCOL.md (lands in P57); short note that release.yml fires on `reposix-cli-v*` tags too."
      contains: "v0.12.0 Quality Gates — forward pointer"
    - path: "quality/SURPRISES.md"
      provides: "Append-only journal seeded with the P56 latest-pointer caveat + any pivot or follow-up items from Waves A-C."
      contains: "P56"
    - path: ".planning/docs_reproducible_catalog.json"
      provides: "Install rows flipped to status: PASS (or PARTIAL for cargo-binstall if the prebuilt fall-back issue persists), with last_verified_at timestamps from the Wave C JSON evidence."
      contains: "PASS"
    - path: ".planning/STATE.md"
      provides: "Cursor advanced to 'Phase 56 SHIPPED'; accumulated-context entry for the v0.12.0 P56 close."
      contains: "Phase 56 SHIPPED"
    - path: "quality/reports/verdicts/p56/"
      provides: "Unbiased subagent's verdict markdown — scored table per catalog row, CLAUDE.md update grade, SURPRISES.md presence grade, file:line citations, GREEN/RED."
      contains: "verdict"
  key_links:
    - from: "Wave C verification JSON files"
      to: "verifier subagent's per-row grading"
      via: "subagent reads .planning/verifications/p56/install-paths/*.json with zero session context"
      pattern: "verifications/p56"
    - from: "CLAUDE.md update"
      to: "QG-07 phase-close requirement"
      via: "git diff main...HEAD -- CLAUDE.md must show this phase's contributions"
      pattern: "v0.12.0"
    - from: "verifier subagent verdict"
      to: "STATE.md cursor advance + REQUIREMENTS.md status flip"
      via: "phase only closes on GREEN; STATE.md update happens after the verdict file is written"
      pattern: "GREEN"
---

<objective>
Wave D — phase close. Three artifacts close P56:

1. **CLAUDE.md update** in the same PR (QG-07): new section pointing forward to v0.12.0 Quality Gates, noting the `reposix-cli-v*` tag-glob convention, and the install-path catalog reference.
2. **quality/SURPRISES.md creation** (QG-05): the file does not yet exist (P57 originally owns the framework skeleton including SURPRISES.md; per the prompt's constraint paragraph, P56 may seed it early). Seed with the latest-pointer caveat + any pivot or follow-up notes from Waves A-C.
3. **Unbiased verifier subagent dispatch** (QG-06): a fresh subagent with zero session context reads the catalog rows + Wave C evidence files + CLAUDE.md diff + SURPRISES.md presence and writes a graded verdict to `quality/reports/verdicts/p56/<ts>.md`. Phase only closes on GREEN.

Per `.planning/research/v0.12.0-autonomous-execution-protocol.md` § "The one principle that everything else follows from": the executing agent's word is not the verdict. The subagent dispatched here is the gate.

Output: CLAUDE.md updated, quality/SURPRISES.md seeded, the catalog rows flipped to PASS, STATE.md cursor advanced, verdict file written, RELEASE-01..03 marked shipped in REQUIREMENTS.md (only after verdict is GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/56-restore-release-artifacts/56-01-PLAN.md
@.planning/phases/56-restore-release-artifacts/56-02-PLAN.md
@.planning/phases/56-restore-release-artifacts/56-03-PLAN.md
@.planning/phases/56-restore-release-artifacts/56-01-SUMMARY.md
@.planning/phases/56-restore-release-artifacts/56-02-SUMMARY.md
@.planning/phases/56-restore-release-artifacts/56-03-SUMMARY.md
@.planning/research/v0.12.0-autonomous-execution-protocol.md
@.planning/research/v0.12.0-roadmap-and-rationale.md
@.planning/docs_reproducible_catalog.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 56-04-A: Update CLAUDE.md with the v0.12.0 forward pointer (QG-07)</name>
  <files>CLAUDE.md</files>
  <action>
Per QG-07 (`.planning/REQUIREMENTS.md` ## v0.12.0): every phase that introduces a new file, convention, gate, or operational rule MUST update CLAUDE.md in the same PR. Anti-bloat: append a paragraph + code reference; cross-reference `quality/PROTOCOL.md` (lands in P57) rather than duplicating runtime detail.

**Add a new section at the END of CLAUDE.md** (immediately before the existing "Quick links" section), titled `## v0.12.0 Quality Gates — forward pointer`. Content:

```
The v0.12.0 milestone supersedes ad-hoc `scripts/check-*.sh` and the
conflated `scripts/end-state.py` with a dimension-tagged Quality
Gates system at `quality/{gates,catalogs,reports,runners}/`. **The
framework itself ships in P57** — until then, the catalog format
lives in `.planning/docs_reproducible_catalog.json` (ACTIVE-V0; P57
migrates it to the unified schema). Read
`.planning/research/v0.12.0-{vision-and-mental-model,
naming-and-architecture, roadmap-and-rationale,
autonomous-execution-protocol}.md` before working on any v0.12.0
phase. Read `quality/SURPRISES.md` (append-only pivot journal — seeded
in P56) at the start of every phase to avoid repeating dead ends.

P56 contribution (this phase): `.github/workflows/release.yml` now
fires on **both** `v*` AND `reposix-cli-v*` tag globs (Option A from
`.planning/research/v0.12.0-install-regression-diagnosis.md`),
because release-plz cuts per-crate tags. Archive filenames use the
`v${version}` form (e.g. `reposix-v0.11.3-x86_64-unknown-linux-musl.tar.gz`)
regardless of which tag triggered the workflow; the GH Release object
still uses the actual tag name (e.g. `reposix-cli-v0.11.3`) so
release-plz's existing release object is the one that gets assets
attached. **If you edit release.yml,** preserve the
`release_tag`/`version`/`version_tag` distinction in the `plan` job's
outputs — collapsing them re-introduces the regression. The
install-path contract lives in
`.planning/docs_reproducible_catalog.json` until P58 splits it into
`quality/catalogs/install-paths.json`.

Known issue tracked under MIGRATE-03 (v0.12.1 carry-forward):
GitHub's `releases/latest/download/...` pointer follows release
recency, and release-plz cuts per-crate releases. If a non-cli
per-crate release publishes after the cli release, the latest
pointer moves and `releases/latest/download/reposix-installer.sh`
404s again until the next cli release. **Long-term recovery:**
either (a) make release.yml's `gh release create` invoke `--latest`
to pin the pointer to the cli release, or (b) configure release-plz
to publish reposix-cli last in its per-crate sequence.

Cross-references:
- `.planning/REQUIREMENTS.md` ## v0.12.0 (active milestone scope)
- `.planning/ROADMAP.md` ## v0.12.0 Quality Gates (PLANNING) (Phases 56-63)
- `quality/PROTOCOL.md` (lands P57 — autonomous-mode runtime contract)
- `quality/SURPRISES.md` (append-only pivot journal; ≤200 lines, archives at 200)
```

**Also extend the existing `## Operating Principles (project-specific)` section** with a new principle 7 (added after the existing principle 6, NOT replacing any). Content:

```
7. **Phase-close means catalog-row PASS.** v0.12.0 introduces the
   "verifier subagent grades GREEN" gate per phase close (QG-06).
   No phase ships on the executing agent's word; an unbiased subagent
   reads the catalog rows + the Wave-N verification artifacts with
   zero session context and writes a verdict to
   `quality/reports/verdicts/p<N>/<ts>.md`. **The verdict is the
   contract** — if RED, the phase loops back to fix the failing rows
   rather than negotiating the catalog down.
```

**Anti-bloat check:** the addition is one new section (~30 lines) + one new principle (~8 lines). Do NOT rewrite existing CLAUDE.md sections; do NOT remove anything. The existing "Threat model" section, the "Build memory budget" section, the "Subagent delegation rules" section all stay intact.

**Banned-words check:** run `bash scripts/banned-words-lint.sh`. The script's repo config scopes to `docs/` (per the script header line 7: "scan Layer 1 + Layer 2"). CLAUDE.md is repo-root, not docs/, so it is NOT scanned. But run the lint anyway as a sanity check that the addition didn't break anything else. Must exit 0.

**Mkdocs check:** run `bash scripts/check-docs-site.sh` only if any docs/ file changed (none should have). Skip if untouched.
  </action>
  <verify>
    <automated>grep -q "v0.12.0 Quality Gates . forward pointer" CLAUDE.md &amp;&amp; grep -q "reposix-cli-v\*" CLAUDE.md &amp;&amp; grep -q "release_tag" CLAUDE.md &amp;&amp; grep -q "Phase-close means catalog-row PASS" CLAUDE.md &amp;&amp; bash scripts/banned-words-lint.sh</automated>
  </verify>
  <done>
CLAUDE.md has the new "v0.12.0 Quality Gates — forward pointer" section before "Quick links"; principle 7 added under Operating Principles (project-specific); banned-words lint passes; "release_tag" + "reposix-cli-v*" + "Phase-close means catalog-row PASS" all greppable.
  </done>
</task>

<task type="auto">
  <name>Task 56-04-B: Create quality/SURPRISES.md and seed P56 entries (QG-05)</name>
  <files>quality/SURPRISES.md</files>
  <action>
The directory `quality/` does not yet exist. Create it now (P57 will scaffold the rest of the layout — `quality/{gates,catalogs,reports,runners}/`; P56 only needs `SURPRISES.md` and `reports/verdicts/p56/` for Wave D's purposes).

```bash
mkdir -p quality/reports/verdicts/p56
```

Create `quality/SURPRISES.md` with:

```
# quality/SURPRISES.md — append-only pivot journal

Per `.planning/research/v0.12.0-autonomous-execution-protocol.md`
§ "SURPRISES.md format". Append one line per unexpected obstacle +
one line per resolution. **Required reading for the next phase
agent.** The next agent does NOT repeat investigations of things
already journaled here.

Format: `YYYY-MM-DD P<N>: <what happened> — <one-line resolution>`

Anti-bloat: ≤200 lines. When it crosses, archive oldest 50 to
`quality/SURPRISES-archive-YYYY-QN.md` and start fresh.

Seeded by P56 (Wave D). P57 takes ownership when the framework skeleton ships.

---

2026-04-27 P56: GitHub's `releases/latest/download/...` pointer
follows release recency, but release-plz cuts per-crate releases.
A non-cli per-crate release published after the cli release moves
the latest pointer and re-breaks `releases/latest/download/reposix-installer.sh`
until the next cli release. — Tracked under MIGRATE-03 (v0.12.1
carry-forward). Recovery options: (a) `gh release create --latest`
to pin the pointer to the cli release in release.yml, or (b)
configure release-plz to publish reposix-cli last. P58 may add a
catalog row that monitors `releases/latest/download/reposix-installer.sh`
HTTP status as a weekly drift detector.
```

**If Wave B chose Option B** (pivot from Option A), append a SECOND entry by reading `.planning/phases/56-restore-release-artifacts/56-02-PIVOT-DRAFT.md` and merging its one-liner. Content example:

```
2026-04-27 P56: pivoted from Option A (extend release.yml tag-glob)
to Option B (release-plz post-publish mirror tag) because <one-line
reason from PIVOT-DRAFT>. — release.yml unchanged; release-plz.yml
gained a "Mirror workspace tag" step gated on
contains(steps.release-plz.outputs.releases_created, 'reposix-cli').
```

**If Task 56-03-D recorded cargo-binstall as PARTIAL** (prebuilt fall-back issue), append a THIRD entry:

```
2026-04-27 P56: cargo binstall fell back to source build despite the
release.yml fix because [package.metadata.binstall].pkg-url in
crates/reposix-cli/Cargo.toml templates against an old archive name.
— Catalog row marked PARTIAL (blast_radius P1, "works just slow").
Tracked under v0.12.1 carry-forward; fix is a one-line edit to the
binstall pkg-url template once Wave B's Option A version_tag rename
settles. P58's release dimension may want a binstall-resolves-to-prebuilt
gate.
```

**Verify the file is well-formed.** Run:
```bash
test -f quality/SURPRISES.md
wc -l quality/SURPRISES.md
grep -c '^2026-' quality/SURPRISES.md
```
Expected: file exists; ≤200 lines; ≥1 entry (latest-pointer); ≤3 entries (latest-pointer + optional pivot + optional binstall-PARTIAL).
  </action>
  <verify>
    <automated>test -f quality/SURPRISES.md &amp;&amp; grep -q "P56:" quality/SURPRISES.md &amp;&amp; grep -q "latest pointer\|releases/latest\|latest-pointer\|latest pointer" quality/SURPRISES.md &amp;&amp; awk 'END{exit ($0 ~ /^$/ &amp;&amp; NR &lt;= 200) ? 0 : (NR &lt;= 200 ? 0 : 1)}' quality/SURPRISES.md</automated>
  </verify>
  <done>
File exists; contains the file-level header explaining format + anti-bloat rule; contains at least the P56 latest-pointer entry; if applicable, contains the Option-B pivot entry; if applicable, contains the cargo-binstall PARTIAL entry; ≤200 lines.
  </done>
</task>

<task type="auto">
  <name>Task 56-04-C: Flip the install rows in docs_reproducible_catalog.json from FAIL/PARTIAL to PASS</name>
  <files>.planning/docs_reproducible_catalog.json</files>
  <action>
Now that Wave C produced PASS evidence for each install path, update the catalog rows to reflect the verified state.

For each of the 5 install rows in `.planning/docs_reproducible_catalog.json`:

1. **install/curl-installer-sh:** read `.planning/verifications/p56/install-paths/curl-installer-sh.json`. If `status==PASS`:
   - Set `"status": "PASS"`.
   - Set `"last_verified_at"` to the JSON's `verified_at` value.
   - Update `"failure_mode": null`.
   - Keep `"remediation_hint"` for archival reference (do NOT clear — it's documentation of the fix that landed; future agents reading this row see "fixed by" rather than empty).
   - Set `"automation_status": "manual-only"` (P58 will wire it into weekly cadence; for now the verification was manual-via-this-phase).

2. **install/powershell-installer-ps1:** read `.planning/verifications/p56/install-paths/powershell-installer-ps1.json`. Same update shape; `last_verified_at` from the JSON; `failure_mode: null`. Note in `automation_status` that container-rehearsal half is deferred to v0.12.1.

3. **install/cargo-binstall:** read `.planning/verifications/p56/install-paths/cargo-binstall.json`. If `status==PASS`, flip to PASS as above. **If status==PARTIAL** (prebuilt fall-back), keep `"status": "PARTIAL"` but update `"last_verified_at"` and `"failure_mode": "binstall resolves but falls back to source build because [package.metadata.binstall].pkg-url template in crates/reposix-cli/Cargo.toml mismatches the new v${version_tag}-prefixed archive names; v0.12.1 carry-forward"`.

4. **install/homebrew:** read `.planning/verifications/p56/install-paths/homebrew.json`. Set status, `last_verified_at`, clear `failure_mode`.

5. **install/build-from-source:** read `.planning/verifications/p56/install-paths/build-from-source.json`. Already PASS at start; just refresh `last_verified_at` from the ci.yml run timestamp.

**Validate JSON parse:**
```bash
python3 -c "import json; d=json.load(open('.planning/docs_reproducible_catalog.json')); rows=[c for c in d['claims'] if c['id'].startswith('install/')]; statuses={c['id']:c['status'] for c in rows}; print(statuses)"
```
Expected output (Option A clean, no binstall pivot needed): all 5 PASS. Acceptable variant: cargo-binstall PARTIAL.

**Do NOT touch the 5 non-install rows** (`tutorial/...`, `quickstart/...`, `benchmark-claim/...`, `troubleshooting/...`). Those belong to P59.

**Do NOT change the top-level `schema_status`** — it stays "ACTIVE-V0 — P56 verifier reads…" until P57 migrates the file.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('.planning/docs_reproducible_catalog.json')); rows=[c for c in d['claims'] if c['id'].startswith('install/')]; assert len(rows)==5; bad=[c for c in rows if c['status'] not in ('PASS','PARTIAL')]; assert not bad, f'install rows still FAIL/NOT-VERIFIED: {[c[\"id\"] for c in bad]}'; assert all(c.get('last_verified_at') for c in rows), 'every install row needs last_verified_at populated'; print({c['id']:c['status'] for c in rows})"</automated>
  </verify>
  <done>
JSON parses; all 5 install rows have status PASS or PARTIAL; all 5 have non-null last_verified_at; the top-level schema_status remains "ACTIVE-V0".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 56-04-D: Dispatch unbiased verifier subagent (QG-06)</name>
  <files>quality/reports/verdicts/p56/&lt;ISO_TIMESTAMP&gt;.md</files>
  <action>
Per `.planning/research/v0.12.0-autonomous-execution-protocol.md` Step 7 — dispatch a `general-purpose` subagent (the orchestrator's Task tool) with **zero session context**. The subagent reads:

1. `.planning/REQUIREMENTS.md` ## v0.12.0 — finds RELEASE-01..03 mapped to P56.
2. `.planning/docs_reproducible_catalog.json` — finds the 5 install rows tagged `phase: "p56"`.
3. `.planning/verifications/p56/install-paths/*.json` — reads each Wave-C evidence file with no narrative context.
4. `.planning/verifications/p56/release-fire-evidence.md` — confirms the workflow run actually fired.
5. `git diff main...HEAD -- CLAUDE.md` — confirms the QG-07 mandatory CLAUDE.md update.
6. `quality/SURPRISES.md` — confirms the file exists and contains P56 entries.

The subagent grades:
- **Per-row** (each install row): PASS / FAIL / PARTIAL / NOT-VERIFIED. Compares the catalog row's `expected_outcome.asserts` against the corresponding evidence JSON. The catalog row says "after the one-liner, `command -v reposix` exits 0"; the JSON's `asserts.command_v_reposix_exit_zero` field is `true` → PASS.
- **CLAUDE.md update (QG-07):** confirms the diff includes a "v0.12.0" reference + the principle 7 update + the release.yml release_tag/version_tag note. If absent → block GREEN.
- **SURPRISES.md (QG-05):** confirms file exists with at least the P56 latest-pointer entry. If absent → block GREEN.

The subagent's verdict markdown lands at `quality/reports/verdicts/p56/&lt;ts&gt;.md`. The phase does NOT close on RED.

**Subagent dispatch prompt (copy-paste, do not editorialize):**

```
You have ZERO context from this session. You are the unbiased verifier
for v0.12.0 Phase 56 (RELEASE-01..03: restore release artifacts).

Inputs:
1. .planning/REQUIREMENTS.md ## v0.12.0 — find requirements assigned to Phase 56 (RELEASE-01, RELEASE-02, RELEASE-03).
2. .planning/docs_reproducible_catalog.json — find the 5 rows where id starts with "install/" and phase == "p56".
3. .planning/verifications/p56/install-paths/*.json — Wave C evidence per row.
4. .planning/verifications/p56/release-fire-evidence.md — workflow-fire evidence.
5. CLAUDE.md — confirm this phase's contributions are documented (per QG-07).
6. quality/SURPRISES.md — confirm the file exists and contains P56 entries (per QG-05).

For each catalog row in scope:
1. Read row.expected_outcome.asserts (the contract declared in PLAN 56-01).
2. Read the corresponding .planning/verifications/p56/install-paths/<id_slug>.json (the evidence Wave C produced).
3. Compare each assert against the evidence file's asserts block. If the
   evidence file says assert==true, that assert PASSes. If the evidence
   file is missing an assert that the catalog row required, the row is
   PARTIAL (or FAIL if the missing assert is P0/P1 blast_radius).
4. Grade the row: PASS / FAIL / PARTIAL / NOT-VERIFIED.

For CLAUDE.md (QG-07):
1. Run `git diff main...HEAD -- CLAUDE.md` (orchestrator provides the
   diff; if you do not have it, ask). Confirm the diff includes:
   (a) a new section titled "v0.12.0 Quality Gates — forward pointer"
   (b) a new principle 7 about phase-close = catalog-row PASS
   (c) a reference to release_tag/version/version_tag distinction
   (d) a reference to MIGRATE-03 v0.12.1 carry-forward for the latest pointer
2. If any of (a)-(c) is absent: phase is NOT done — block the GREEN verdict.

For quality/SURPRISES.md (QG-05):
1. Confirm the file exists at quality/SURPRISES.md.
2. Confirm it contains at least one entry tagged "P56:" with the
   latest-pointer caveat.
3. If file is absent: phase is NOT done — block GREEN.

Verdict rules (verbatim from autonomous-execution-protocol.md):
- Refuse to grade GREEN unless every P0+P1 catalog row is PASS or WAIVED
  AND CLAUDE.md is updated AND SURPRISES.md exists.
- Cargo-binstall PARTIAL is acceptable (blast_radius P1; the row's note
  says "works just slow") — does NOT block GREEN, but call it out.
- All P0 rows must be PASS (or WAIVED with a TTL ≤90d): curl-installer-sh
  is P0 (every linux/mac user hits it); powershell-installer-ps1 is P0
  for windows users; homebrew is P0 for mac users.

Output: a scored markdown table + per-row evidence citations (quote
specific JSON keys from the verification files) at
quality/reports/verdicts/p56/<UTC-ISO-timestamp>.md.

Header format:
  # P56 verdict — <UTC ISO>
  - Verdict: GREEN | RED | YELLOW
  - Catalog rows graded: <count>
  - QG-07 CLAUDE.md update: PASS | FAIL
  - QG-05 SURPRISES.md presence: PASS | FAIL

Then the per-row table with columns: row_id, blast_radius, asserts_total,
asserts_passing, status, evidence_citation (file:line or JSON-key path).

Then a short "Recommendation" section: GREEN → phase closes; YELLOW →
non-blocking issues to track in v0.12.1; RED → phase loops.
```

**Run the dispatch.** Use the orchestrator's Task tool with subagent_type=`general-purpose`. Wait for the verdict file to land at `quality/reports/verdicts/p56/&lt;ts&gt;.md`.

**Read the verdict.** If GREEN: proceed to Task 56-04-E (commit + STATE.md update). If YELLOW with binstall-PARTIAL only: also proceed (the row is P1, partial is documented). If RED: do NOT proceed — return to the failing row, fix it (re-run the affected install path's container rehearsal, refresh the JSON evidence, update the catalog row's status), then re-dispatch a fresh subagent for a second grading.
  </action>
  <verify>
    <automated>ls quality/reports/verdicts/p56/*.md 2&gt;/dev/null | head -1 | xargs -I{} grep -lE "^- Verdict: (GREEN|YELLOW)" {} | wc -l | awk '$1==1 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
A verdict file exists at `quality/reports/verdicts/p56/<ts>.md` with header `Verdict: GREEN` (or `YELLOW` with documented binstall-PARTIAL only). Owner has typed "GREEN" or "YELLOW" to confirm the verdict is acceptable. RED verdict means phase loops and Task 56-04-E does not run.
  </done>
  <resume-signal>Type "GREEN", "YELLOW", or describe the RED issues</resume-signal>
</task>

<task type="auto">
  <name>Task 56-04-E: Update STATE.md, REQUIREMENTS.md status, commit + push the close</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, CLAUDE.md, quality/SURPRISES.md, quality/reports/verdicts/p56/, .planning/docs_reproducible_catalog.json</files>
  <action>
**This task ONLY runs after Task 56-04-D's verdict is GREEN (or YELLOW with documented binstall-PARTIAL).** If RED, the loop must close first.

**Step 1 — Update REQUIREMENTS.md status table.** In `.planning/REQUIREMENTS.md` ## v0.12.0 § Traceability, change:
```
| RELEASE-01 | P56 | planning |
| RELEASE-02 | P56 | planning |
| RELEASE-03 | P56 | planning |
```
to:
```
| RELEASE-01 | P56 | shipped |
| RELEASE-02 | P56 | shipped |
| RELEASE-03 | P56 | shipped |
```

In the "Active" § "Release dimension — close the immediate breakage" block, change the three checkbox items from `[ ]` to `[x]`:
```
- [x] **RELEASE-01**: Restore the curl/PowerShell installer URLs ...
- [x] **RELEASE-02**: Homebrew tap formula auto-updates with each release ...
- [x] **RELEASE-03**: `cargo binstall reposix-cli reposix-remote` resolves to a prebuilt binary ...
```

**Step 2 — Update STATE.md.** Update `## Current Position`:
```yaml
Phase: 57
Plan: —
Cursor: P56 SHIPPED 2026-04-27. Release pipeline restored — release.yml fires on `reposix-cli-v*` tags AND `v*` tags (Option A); a fresh reposix-cli-v0.11.3 GH Release shipped with all 7 expected assets. 5 install paths verified end-to-end (curl: ubuntu:24.04 rehearsal PASS; ps1: asset-existence PASS, container deferred v0.12.1; cargo-binstall: rust:1.82-slim rehearsal <PASS|PARTIAL — note source-fallback>; homebrew: tap formula version + sha256 PASS, macos rehearsal deferred v0.12.1; build-from-source: ci.yml-covered PASS). CLAUDE.md gained "v0.12.0 Quality Gates — forward pointer" section + new operating principle 7 (phase-close = catalog-row PASS). quality/SURPRISES.md created and seeded with the latest-pointer caveat (releases/latest/download/... follows release recency; release-plz cuts per-crate releases; non-cli releases published after cli move the pointer — tracked MIGRATE-03 v0.12.1). Verdict: GREEN per quality/reports/verdicts/p56/<ts>.md from unbiased subagent. Next: P57 (Quality Gates skeleton + structure dimension migration). Gate-state precondition for P57: read quality/reports/verdicts/p56/<ts>.md to confirm GREEN, read quality/SURPRISES.md, read this entry, then `/gsd-plan-phase 57`.
Status: shipped (P56)
Last activity: 2026-04-27 -- P56 closed with verifier subagent GREEN verdict; install paths restored end-to-end.
```

Append to `## Accumulated Context` § Roadmap Evolution (insert at the top of the list, above the existing 2026-04-27 v0.12.0 scaffold entry):

```
- **P56 SHIPPED (2026-04-27, v0.12.0):** RELEASE-01..03 closed.
  release.yml's `on.push.tags` now matches both `v*` and `reposix-cli-v*`
  globs (Option A from .planning/research/v0.12.0-install-regression-diagnosis.md);
  a fresh `reposix-cli-v0.11.3` GH Release ships with 7 assets (5 platform
  archives + 2 installers + SHA256SUMS). 5 install paths verified
  (`.planning/verifications/p56/install-paths/*.json`): curl PASS in
  ubuntu:24.04 rehearsal, powershell PASS asset-existence (windows runner
  v0.12.1), cargo-binstall <PASS|PARTIAL>, homebrew PASS tap formula
  version + sha256s (macos runner v0.12.1), build-from-source PASS via
  ci.yml. CLAUDE.md gained v0.12.0 forward-pointer section + new
  operating principle 7 (phase-close = catalog-row PASS). quality/SURPRISES.md
  created and seeded with the latest-pointer caveat (release-plz cuts
  per-crate releases; non-cli releases shift the latest pointer; tracked
  MIGRATE-03 v0.12.1). Verifier subagent verdict GREEN at
  quality/reports/verdicts/p56/<ts>.md. Next: P57 framework skeleton.
```

Update the progress block:
```yaml
progress:
  total_phases: 8
  completed_phases: 1
  total_plans: 4
  completed_plans: 4
  percent: 12
```

**Step 3 — Write 56-PHASE-SUMMARY.md.** Create `.planning/phases/56-restore-release-artifacts/56-PHASE-SUMMARY.md` with:
- One-paragraph what-shipped (release.yml fix, all 5 install paths verified, CLAUDE.md update, SURPRISES.md created, verdict GREEN)
- Wave A/B/C/D commit SHAs
- The 5 install row statuses
- Pointer to quality/reports/verdicts/p56/<ts>.md
- "Carry-forward to v0.12.1" section: latest-pointer fix, cargo-binstall metadata fix (if PARTIAL), windows + macos container rehearsals
- "Lessons for P57" section: catalog-first commit worked; verifier subagent dispatch worked; SURPRISES.md created early was a good pivot vs. waiting for P57.

**Step 4 — Stage + commit.** Single commit for the close:
```
chore(p56): close phase — RELEASE-01..03 SHIPPED, verdict GREEN

Wave D — phase close.

Updates:
- CLAUDE.md: new "v0.12.0 Quality Gates — forward pointer" section + new
  operating principle 7 (phase-close = catalog-row PASS).
- quality/SURPRISES.md: created and seeded with the latest-pointer
  caveat (and Option-B-pivot / cargo-binstall-PARTIAL entries if
  applicable). P57 takes ownership when the framework skeleton ships.
- .planning/docs_reproducible_catalog.json: 5 install rows flipped to
  PASS (or PARTIAL for cargo-binstall) with last_verified_at from
  Wave C JSON evidence.
- .planning/REQUIREMENTS.md: RELEASE-01..03 traceability rows flipped
  to "shipped"; checkboxes ticked.
- .planning/STATE.md: cursor advanced to Phase 57; accumulated-context
  entry for P56 close.
- .planning/phases/56-restore-release-artifacts/56-PHASE-SUMMARY.md:
  phase-level summary written.
- quality/reports/verdicts/p56/<ts>.md: unbiased subagent verdict GREEN
  (committed in this same wave so the verdict is the artifact, per
  QG-06).

Refs: RELEASE-01, RELEASE-02, RELEASE-03 (all shipped)
```

Run pre-push hook (`scripts/end-state.py verify` + freshness invariants + banned-words). Must be GREEN. Push.

**Step 5 — Tag the verdict (do NOT tag the release).** No git tag for the phase close — phases close via the verdict markdown, not a tag. Release-plz already cut `reposix-cli-v0.11.3` (Wave C); that's the only tag for P56.
  </action>
  <verify>
    <automated>grep -q "RELEASE-01.*shipped" .planning/REQUIREMENTS.md &amp;&amp; grep -q "P56 SHIPPED" .planning/STATE.md &amp;&amp; grep -q "Phase: 57" .planning/STATE.md &amp;&amp; test -f .planning/phases/56-restore-release-artifacts/56-PHASE-SUMMARY.md &amp;&amp; ls quality/reports/verdicts/p56/*.md | wc -l | awk '$1 &gt;= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
REQUIREMENTS.md shows RELEASE-01..03 as `shipped`; STATE.md cursor names Phase 57 next; 56-PHASE-SUMMARY.md exists; ≥1 verdict file exists in quality/reports/verdicts/p56/; pre-push hook GREEN; commit pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed catalog ↔ verifier subagent | Subagent reads catalog + evidence files; if either is missing the verdict is RED. |
| CLAUDE.md update ↔ next-phase agent | Next-phase agent reads CLAUDE.md as bible; missing or wrong content here = ungrounded next agent (the QG-07 anti-pattern). |
| SURPRISES.md ↔ next-phase agent | Same. The journal is required reading per the protocol. |
| verifier subagent ↔ executing agent | Subagent has zero context, refuses to be "talked out of RED." This boundary is the one principle of the v0.12.0 framework. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-17 | Spoofing | executing agent attempts to grade itself GREEN despite RED catalog rows | mitigate | Task 56-04-D dispatches a fresh subagent with zero context; the orchestrator (this plan's runner) does NOT write the verdict file directly. |
| T-56-18 | Tampering | catalog row status flipped to PASS without backing evidence | mitigate | Task 56-04-C requires reading the corresponding JSON evidence file; the verifier subagent in Task 56-04-D re-checks the alignment between catalog asserts and evidence contents. |
| T-56-19 | Repudiation | "I forgot to update CLAUDE.md" / "QG-07 was deferred" | mitigate | Task 56-04-D's subagent runs `git diff main...HEAD -- CLAUDE.md` and refuses GREEN if absent. Task 56-04-A's verify gate runs the same grep. Two layers. |
| T-56-20 | Information disclosure | none — all artifacts are committed-by-design | accept | No PII/secrets in the catalog, evidence files, or verdict markdown. |
| T-56-21 | DoS | verifier subagent times out or returns inconclusive | mitigate | The subagent prompt is structured (per-row scoring rubric); it can grade NOT-VERIFIED for individual rows without timing out. If the subagent itself errors, dispatch a second one — same prompt, same inputs, fresh context. |
| T-56-22 | EoP | none — Wave D doesn't change auth or grant new privileges | accept | All git pushes use the developer's existing credentials; the workflow_run that fired in Wave C used GITHUB_TOKEN scoped to the repo. |
</threat_model>

<verification>
After this plan ships, the following are persistent:

1. **CLAUDE.md** has the new "v0.12.0 Quality Gates — forward pointer" section + principle 7. `grep -q "v0.12.0 Quality Gates . forward pointer" CLAUDE.md` succeeds.
2. **quality/SURPRISES.md** exists with at least the P56 latest-pointer entry. `grep -q "P56:" quality/SURPRISES.md` succeeds.
3. **catalog rows** for the 5 install paths are PASS or PARTIAL with `last_verified_at` populated.
4. **quality/reports/verdicts/p56/<ts>.md** exists and contains "GREEN" in the verdict header.
5. **.planning/REQUIREMENTS.md** shows RELEASE-01..03 as "shipped".
6. **.planning/STATE.md** cursor names Phase 57 as next.
7. **56-PHASE-SUMMARY.md** exists and pointers to the verdict file + the install-path JSON evidence.
8. **Pre-push hook** GREEN (no freshness regressions; banned-words clean).
</verification>

<success_criteria>
P56 closes with the unbiased subagent's GREEN verdict on file. The catalog rows the phase declared in Wave A are now backed by Wave C evidence and graded PASS by Wave D's subagent. CLAUDE.md is updated in the same PR, satisfying QG-07. quality/SURPRISES.md is seeded for P57's first read.

Three downstream consumers can now proceed:
- **P57's framework-skeleton work** can read `quality/SURPRISES.md` for P56's latest-pointer caveat and the catalog-first-commit lessons.
- **P58's release dimension** has its primary regression class (the curl-installer URL going dark) fully documented + tracked.
- **The user** has working install paths: `curl … | sh`, `irm … | iex`, `cargo binstall`, `brew install`, and `cargo install` all land working binaries on PATH.

If any of the 5 install rows is FAIL after the verdict, P56 does NOT close. Loop back, fix the row, re-verify, re-dispatch. Per CLAUDE.md OP-1: don't declare done without seeing green.
</success_criteria>

<output>
After completion, the artifact at `.planning/phases/56-restore-release-artifacts/56-PHASE-SUMMARY.md` is the phase-level summary (written in Task 56-04-E Step 3). The verifier subagent's verdict is the contract artifact at `quality/reports/verdicts/p56/<ts>.md`.

The next phase agent reads, in order: STATE.md cursor → 56-PHASE-SUMMARY.md → quality/SURPRISES.md → quality/reports/verdicts/p56/<ts>.md → CLAUDE.md (via grep) → then `/gsd-plan-phase 57`.
</output>
