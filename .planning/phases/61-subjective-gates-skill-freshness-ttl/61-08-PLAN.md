---
phase: 61-subjective-gates-skill-freshness-ttl
plan: 08
type: execute
wave: 8
depends_on:
  - "61-01"
  - "61-02"
  - "61-03"
  - "61-04"
  - "61-05"
  - "61-06"
  - "61-07"
files_modified:
  - CLAUDE.md
  - quality/SURPRISES.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - quality/reports/verdicts/p61/VERDICT.md
autonomous: false
requirements:
  - QG-06
  - QG-07
must_haves:
  truths:
    - "QG-07 CLAUDE.md update -- a new H3 subsection 'P61 -- Subjective gates skill + freshness TTL' lands in CLAUDE.md (anti-bloat: <=80 added lines per the P58 + P59 + P60 precedent). Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail). Cites: subjective dimension home (3 rubrics: cold-reader-hero-clarity / install-positioning / headline-numbers-sanity); reposix-quality-review skill (.claude/skills/reposix-quality-review/ scaffold + 3 rubric impls + 2 dispatchers); freshness-TTL extension in run.py + verdict.py + test_freshness.py; quality-pre-release.yml workflow + release.yml soft-gate doc; POLISH-SUBJECTIVE Wave G outcomes (3 rubrics dispatched + findings triaged + catalog flipped); v0.12.1 MIGRATE-03 carry-forwards (auto-dispatch on STALE rows; hard-gate chaining via composite workflow; any P2-or-deferred findings)."
    - "QG-06 verifier subagent dispatch: an unbiased subagent (Path A via Task tool if available; Path B in-session disclosure per P56/P57/P58/P59/P60 precedent if not) grades all P61-touched catalog rows GREEN. Verdict at quality/reports/verdicts/p61/VERDICT.md. The verdict template is verbatim from quality/PROTOCOL.md § 'Verifier subagent prompt template'."
    - "ALL P0+P1 rows across the P61-touched catalogs (subjective for the 3 new rows) are PASS, WAIVED-extended for v0.12.1, or filed as carry-forward via MIGRATE-03. NO P0+P1 rows remain NOT-VERIFIED at Wave H close. NO row remains in the Wave A short-lived (until 2026-05-15) waiver state."
    - ".planning/REQUIREMENTS.md traceability table updated -- SUBJ-01, SUBJ-02, SUBJ-03, POLISH-SUBJECTIVE all flip from `planning` to `shipped (P61)`. 4 requirement IDs land at `shipped`."
    - ".planning/STATE.md cursor advances P60 SHIPPED -> P61 SHIPPED. Roadmap-Evolution entry appended at top (newest first); P60/P59/P58/P57/P56 entries preserved verbatim. progress frontmatter incremented (completed_phases 5 -> 6; total_plans 30 -> 38; completed_plans 30 -> 38; percent 63 -> 75)."
    - "quality/SURPRISES.md is updated with any P61 pivot entries from Waves A-G (likely 2-5 entries: Wave A short-lived waiver pattern (familiar), Wave B parse_duration extraction if anti-bloat triggered, Wave C cross-import or path-arithmetic fix, Wave D doc-clarity-review parse drift if hit, Wave E rubric prompt jargon iteration, Wave F GH Actions chaining limitation (already filed as v0.12.1 MIGRATE-03 carry-forward; SURPRISES entry just confirms), Wave G subagent variance + any in-phase findings fixed). The 3 P57 + 7 P58 + 6 P59 + N P60 entries from earlier phases preserved verbatim. Anti-bloat: under 200 lines via archive rotation if needed (current 244 post-P60; P61 entries push to ~250-260 -> archive rotation per quality/PROTOCOL.md anti-bloat). Likely action: archive next 50 oldest entries (P57's 3 + P58's 7 + half of P59 = ~13 entries... not 50; defer rotation OR rotate fewer-than-50 with a header note per the P59 precedent that established quarterly rotation)."
    - "Phase-close commit lands all 5 docs updates (CLAUDE.md + SURPRISES.md + STATE.md + REQUIREMENTS.md + VERDICT.md) in the SAME PR per QG-07."
    - "The Wave H verifier subagent reads the catalog rows + the artifacts + the CLAUDE.md diff with ZERO session context and grades GREEN/RED. The 4 disclosure constraints (Path B fallback) are honored verbatim per P56/P57/P58/P59/P60 precedent: (1) evidence-only; (2) catalog-rows-as-contract; (3) refuse-GREEN-on-RED; (4) out-of-session re-grade should match."
    - "Plan is autonomous: false because the verifier-subagent dispatch in Task 4 is the human-or-checkpoint moment per P59/P60 precedent."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Append (60-80 lines) at the END of the existing P57 + P58 + P59 + P60 Quality Gates dimension subsections with the P61-specific extensions: subjective dimension home (3 rubrics + .claude/skills/reposix-quality-review/ scaffold + 3 rubric prompts + 2 dispatchers); freshness-TTL extension (parse_duration + is_stale in run.py; STALE sub-table in verdict.py; test_freshness.py 8-test pytest); quality-pre-release.yml workflow live (triggers on tag-push + dispatch); release.yml soft-gate doc + v0.12.1 hard-gate carry-forward; POLISH-SUBJECTIVE Wave G outcomes (3 rubrics dispatched + findings triaged + catalog flipped); v0.12.1 MIGRATE-03 carry-forwards filed. ALSO: update CLAUDE.md 'Cold-reader pass on user-facing surfaces' section to point at the new skill + the rubric catalog. ALSO: update CLAUDE.md 'Subagent delegation rules' table (around line 247 in the current state) to add a row for 'Dispatching subjective rubrics' -> 'reposix-quality-review skill (after P61 ships)'. Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail)."
      contains: "subjective dimension"
      max_lines: 80
    - path: "quality/SURPRISES.md"
      provides: "Append any P61 entries from Waves A-G + this Wave H. P57+P58+P59+P60 entries preserved verbatim. Anti-bloat: archive rotation per the precedent if line count > 240."
      contains: "P61"
      max_lines: 270
    - path: ".planning/STATE.md"
      provides: "Roadmap-Evolution entry appended at top for P61 SHIPPED. Current Position cursor advances to P62. progress frontmatter incremented per the math: completed_phases 5 -> 6; total_plans 30 -> 38 (P56=4 + P57=6 + P58=6 + P59=6 + P60=8 + P61=8 = 38); completed_plans 30 -> 38; percent 63 -> 75 (6/8 = 75%)."
      contains: "P61 SHIPPED"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table updated: SUBJ-01, SUBJ-02, SUBJ-03, POLISH-SUBJECTIVE flip planning -> shipped (P61). MIGRATE-03 row gains v0.12.1 carry-forward entries (auto-dispatch on STALE; hard-gate chaining; any P2 findings deferred from Wave G). Per-phase requirement counts in the footer paragraph updated: P61=4 requirements, all shipped at Wave H."
      contains: "shipped (P61)"
    - path: "quality/reports/verdicts/p61/VERDICT.md"
      provides: "Unbiased verifier subagent verdict (or in-session verdict with disclosure block per P56/P57/P58/P59/P60 precedent). Top-line GREEN/RED. Per-row table for the 3 P61-touched rows graded. QG-07 + SUBJ-01..03 + POLISH-SUBJECTIVE closure citations. Recurring-criteria evidence (criteria 1-5). Recommendation: 'GREEN. P62 may begin.' OR specific RED with required-action."
      contains: "GREEN"
  key_links:
    - from: "CLAUDE.md (P61 append)"
      to: "quality/PROTOCOL.md"
      via: "cross-reference; CLAUDE.md does NOT duplicate runtime detail per anti-bloat rule"
      pattern: "quality/PROTOCOL.md"
    - from: "verifier subagent dispatch"
      to: "quality/reports/verdicts/p61/VERDICT.md"
      via: "Task tool dispatch with the prompt from quality/PROTOCOL.md § 'Verifier subagent prompt template'; subagent writes the verdict file with ZERO session context"
      pattern: "ZERO session context"
    - from: ".planning/STATE.md"
      to: "P62 agent (next session)"
      via: "Cursor advance + Roadmap-Evolution entry tells the next agent P61 GREEN + lists what they read at start of P62 (per quality/PROTOCOL.md Step 1 reading order)"
      pattern: "P61 SHIPPED"
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "Wave H verifier subagent"
      via: "the subagent verifies the 4 requirement IDs flipped to shipped; if any flip is unverified, refuses GREEN"
      pattern: "shipped (P61)"
    - from: "CLAUDE.md 'Subagent delegation rules' update"
      to: ".claude/skills/reposix-quality-review/SKILL.md"
      via: "the new row in the delegation table tells future agents WHEN to use the new skill (subjective rubric grading; pre-release verification; on-demand rubric re-grade)"
      pattern: "reposix-quality-review skill"
---

<objective>
Wave H — phase close + CLAUDE.md QG-07 + SURPRISES.md update + STATE.md cursor + REQUIREMENTS.md traceability + verifier subagent dispatch.

Per `.planning/REQUIREMENTS.md` § "Recurring success criteria across every phase (P56-P63)" criteria 2 + 3 ('CLAUDE.md update in the same PR (QG-07)' + 'Unbiased verifier-subagent dispatch on phase close (QG-06)'). Wave H is the phase-close ritual that locks the dimension contract for P62's executor + grades P61 GREEN.

Five activities, in order:

1. **QG-07 CLAUDE.md update (Task 1)**: append a P61 H3 subsection (60-80 lines) per the P58 + P59 + P60 precedent. Plus 2 small in-place updates to existing CLAUDE.md sections: 'Cold-reader pass on user-facing surfaces' (point at new skill); 'Subagent delegation rules' table (add reposix-quality-review row).

2. **STATE.md cursor advance (Task 2)**: roadmap-evolution entry + Current Position cursor + progress frontmatter increment.

3. **REQUIREMENTS.md traceability (Task 3)**: 4 requirement IDs flip planning -> shipped (P61) in the table; MIGRATE-03 row gains v0.12.1 carry-forward entries; per-phase footer paragraph updated.

4. **QG-06 verifier subagent dispatch (Task 4)**: dispatch an unbiased subagent (Path A via Task tool if available; Path B in-session disclosure per P56/P57/P58/P59/P60 precedent if not). Subagent grades GREEN/RED. Verdict at quality/reports/verdicts/p61/VERDICT.md.

5. **SURPRISES.md updates + atomic commit (Task 5)**: append any P61 pivot entries; commit all 5 docs updates in the same PR.

Plan is `autonomous: false` because the verifier-subagent dispatch is the human-or-checkpoint moment per P59/P60 precedent.

**Pivot rules**:

- **Path A Task tool unavailable in executor**: per P56/P57/P58/P59/P60 precedent, fall back to Path B (in-session verdict with disclosure block). The disclosure block names: (1) Evidence-only -- each PASS/WAIVED claim cites file+line OR runner output; (2) Catalog-rows-as-contract -- grade computed from row.expected.asserts <-> artifact asserts_passed/failed match, not narrative; (3) Refuse-GREEN-on-RED -- if any P0+P1 row outside {PASS, WAIVED-extended}, mark RED; (4) Out-of-session re-grade should match.

- **A row is RED at Wave H entry**: Wave G should have caught it. If a regression slipped through, fix BEFORE dispatching the verifier. The verifier's contract is "the catalog at this commit SHA grades GREEN".

- **CLAUDE.md anti-bloat (P57 + P58 + P59 + P60 + P61 = 5 H3 subsections under the Quality Gates parent section)**: each subsection is <=80 lines per precedent; total Quality Gates section is now ~330-380 lines. If banned-words-lint catches the addition, fix the offending word in the new subsection (likely `replace`). MIGRATE-02 in P63 does the cohesion pass that audits cross-refs across all 5 subsections.

- **SURPRISES.md crossing 240 lines** (current 244 post-P60): per `quality/PROTOCOL.md` § anti-bloat rules per surface, archive oldest 50 entries to `quality/SURPRISES-archive-2026-Q2.md` (P59 already created this archive). At Wave H, decide: if SURPRISES.md is at 250-270 lines after P61 entries land, do another archive rotation (next 50 oldest -> archive). If at 245-250 lines (P61 added 1-6 entries), defer rotation to P62 (trivially over cap; not worth the rotation overhead).

- **A new pivot in this Wave H itself** (e.g. the verifier-subagent dispatch reveals a contract drift): append a final SURPRISES.md entry. Do NOT silently fix it.

**Output**: Wave H ships in 1-2 commits.
- Commit 1: `chore(p61): POLISH closure + CLAUDE.md update + STATE/SURPRISES/REQUIREMENTS advance + verifier verdict GREEN`.
- Commit 2 (optional): `chore(p61): in-phase fix for <regression>` -- ONLY IF Wave H found a regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.12.0-roadmap-and-rationale.md
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/SURPRISES-archive-2026-Q2.md
@quality/catalogs/subjective-rubrics.json
@quality/catalogs/freshness-invariants.json
@quality/catalogs/code.json
@quality/catalogs/release-assets.json
@quality/catalogs/docs-reproducible.json
@quality/catalogs/agent-ux.json
@quality/catalogs/perf-targets.json
@quality/catalogs/orphan-scripts.json
@quality/catalogs/docs-build.json
@quality/runners/run.py
@quality/runners/verdict.py
@quality/reports/verdicts/p60/VERDICT.md
@quality/reports/verdicts/p59/VERDICT.md
@quality/reports/verifications/subjective/cold-reader-hero-clarity.json
@quality/reports/verifications/subjective/install-positioning.json
@quality/reports/verifications/subjective/headline-numbers-sanity.json
@.planning/phases/60-docs-build-migration/60-08-PLAN.md
@CLAUDE.md
@.planning/phases/61-subjective-gates-skill-freshness-ttl/61-01-PLAN.md
@.planning/phases/61-subjective-gates-skill-freshness-ttl/61-02-PLAN.md
@.planning/phases/61-subjective-gates-skill-freshness-ttl/61-03-PLAN.md
@.planning/phases/61-subjective-gates-skill-freshness-ttl/61-04-PLAN.md
@.planning/phases/61-subjective-gates-skill-freshness-ttl/61-05-PLAN.md
@.planning/phases/61-subjective-gates-skill-freshness-ttl/61-06-PLAN.md
@.planning/phases/61-subjective-gates-skill-freshness-ttl/61-07-PLAN.md

<interfaces>
**CLAUDE.md P61 H3 subsection shape (model on the existing P57/P58/P59/P60 subsections in CLAUDE.md):**

```markdown
### P61 -- Subjective gates skill + freshness-TTL (added 2026-04-27)

The Quality Gates subjective dimension shipped in P61 with 3 rubrics catalogued in `quality/catalogs/subjective-rubrics.json` (cross-dimension; subagent-graded kind). Rubrics + cadences:

| Rubric | Implementation skill | Cadence | freshness_ttl |
|---|---|---|---|
| `subjective/cold-reader-hero-clarity` | `doc-clarity-review` (existing global skill) | pre-release | 30d |
| `subjective/install-positioning` | `reposix-quality-review` (P61 ships at `.claude/skills/reposix-quality-review/`) | pre-release | 30d |
| `subjective/headline-numbers-sanity` | `reposix-quality-review` | weekly | 30d |

**reposix-quality-review skill (SUBJ-02 closure)**: NEW skill at `.claude/skills/reposix-quality-review/` (4 scaffold files + 3 rubric prompts + 2 dispatchers; ~700 LOC total scaffold + impl). The ONLY skill change pre-approved for v0.12.0 per `.planning/research/v0.12.0-open-questions-and-deferrals.md` line 124. Invocation: `bash .claude/skills/reposix-quality-review/dispatch.sh --rubric <id>` OR `--all-stale` OR `--force`. Path A (Task tool from Claude session) preferred; Path B (claude -p subprocess) fallback.

**Freshness-TTL extension (SUBJ-03 closure)**: `quality/runners/run.py` gained `parse_duration(s: str) -> timedelta` + `is_stale(row: dict, now: datetime) -> bool` + STALE-counts-as-NOT-VERIFIED integration in `run_row` + `[STALE]` label in `print_row_summary`. `quality/runners/verdict.py` gained STALE sub-table in the NOT-VERIFIED rollup section. `quality/runners/test_freshness.py` ships 8 pytest tests covering parse_duration + is_stale + STALE+WAIVED interaction + STALE+blast_radius semantics.

**Pre-release cadence wiring (SUBJ-03 closure)**: `.github/workflows/quality-pre-release.yml` (NEW; <=80 LOC; triggers on tag-push v* + reposix-cli-v* + workflow_dispatch; runs `python3 quality/runners/run.py --cadence pre-release`; emits ::error:: with dispatcher invocation on failure). `.github/workflows/release.yml` gained a comment block documenting parallel-execution contract; hard-gate chaining via composite workflow OR workflow_run trigger is v0.12.1 carry-forward (filed as MIGRATE-03 entry; per P56 SURPRISES row 1 GH-Actions cross-workflow chaining limitation).

**POLISH-SUBJECTIVE broaden-and-deepen (Wave G)**: 3 rubrics dispatched in parallel via SKILL.md Path A (Task tool); 3 real artifacts persisted at `quality/reports/verifications/subjective/<slug>.json`. Findings triage: <COUNT> P0/P1 fixed in-phase (cite specific source-file edits); <COUNT> P2 deferred to v0.12.1 MIGRATE-03 carry-forward (cite); catalog rows flipped from WAIVED -> PASS or WAIVED-extended-to-2026-07-26. Specific outcomes: <FILL IN from Wave G summary>.

**v0.12.1 MIGRATE-03 carry-forwards (P61)**:
- Auto-dispatch on STALE rows from CI: requires Anthropic API access from GH Actions runner; deferred to v0.12.1.
- Hard-gate chaining release.yml -> quality-pre-release.yml: requires composite workflow OR workflow_run trigger; deferred to v0.12.1.
- Any P2 findings deferred from Wave G (cite specifics).

**Cross-references**: `quality/PROTOCOL.md` § verifier-subagent prompt template + § anti-bloat rules; `quality/SURPRISES.md` § P61 entries (parse_duration extraction if any; subagent variance findings); `quality/gates/subjective/README.md` (dimension home + rubric conventions); `.planning/research/v0.12.0-open-questions-and-deferrals.md` Q2 (rubric variance handling).
```

The above is ~60 lines including the table + the closure paragraphs. Within the <=80 cap.

**ALSO update existing CLAUDE.md sections (in-place):**

1. **"Cold-reader pass on user-facing surfaces" section** (find via grep): add a sentence pointing at the new skill:

```diff
+ The cold-reader pass is now operationalized via the `reposix-quality-review`
+ skill at `.claude/skills/reposix-quality-review/`: invoke
+ `bash .claude/skills/reposix-quality-review/dispatch.sh --rubric subjective/cold-reader-hero-clarity`
+ to grade README.md + docs/index.md hero clarity automatically. The catalog
+ row at `quality/catalogs/subjective-rubrics.json` enforces "rubric was checked
+ within 30d" via the freshness-TTL gate (P61 SUBJ-03).
```

2. **"Subagent delegation rules" section** (find the table around line ~247): add a row:

```diff
| Cold-reader pass on user-facing surfaces | doc-clarity-review skill |
+ | Dispatching subjective rubrics (cold-reader, install-positioning, headline-numbers) | reposix-quality-review skill (after P61 ships) |
```

(The PROTOCOL.md § "Skill dispatch — when to spawn subagents" already lists `reposix-quality-review` in the table at row 11; CLAUDE.md should mirror this.)

**STATE.md update shape (Task 2):**

Append at the TOP of `## Accumulated Context` -> `### Roadmap Evolution`:

```markdown
- **P61 SHIPPED (2026-04-27, v0.12.0):** Subjective gates skill + freshness-TTL enforcement. `quality/catalogs/subjective-rubrics.json` ships 3 seed rubrics (cold-reader-hero-clarity P1 pre-release, install-positioning P0 pre-release, headline-numbers-sanity P2 weekly; each freshness_ttl=30d). `.claude/skills/reposix-quality-review/` NEW skill (the ONLY skill change pre-approved for v0.12.0 per .planning/research/v0.12.0-open-questions-and-deferrals.md line 124): SKILL.md (<=180 LOC; --rubric/--all-stale/--force/usage modes); dispatch.sh entry point (<=125 LOC; routes per rubric ID); lib/catalog.py + lib/persist_artifact.py (<=100 LOC each; canonical artifact JSON shape); 3 rubric prompts (cold-reader-hero-clarity.md jargon-free <=80 LOC consumed by doc-clarity-review subprocess; install-positioning.md <=80 LOC inline-dispatch; headline-numbers-sanity.md <=120 LOC inline-dispatch); 2 dispatchers (lib/dispatch_cold_reader.sh wraps doc-clarity-review; lib/dispatch_inline_subagent.sh routes install-positioning + headline-numbers via Path A Task / Path B claude -p). **SUBJ-03 freshness-TTL closure**: quality/runners/run.py gains parse_duration + is_stale + STALE-counts-as-NOT-VERIFIED integration (~50 LOC; final 380 LOC under 390 cap); quality/runners/verdict.py gains STALE sub-table (~30 LOC); quality/runners/test_freshness.py ships 8 pytest tests (parse_duration + is_stale + STALE+WAIVED + STALE+blast_radius). `.github/workflows/quality-pre-release.yml` NEW workflow (<=80 LOC; triggers on tag-push v* + reposix-cli-v* + workflow_dispatch). `.github/workflows/release.yml` gains parallel-execution comment block + v0.12.1 hard-gate carry-forward note. **POLISH-SUBJECTIVE Wave G**: 3 rubrics dispatched in parallel via SKILL.md Path A; 3 real artifacts persisted at quality/reports/verifications/subjective/<slug>.json; <COUNT> P0/P1 findings fixed in-phase (cite); <COUNT> P2 deferred to v0.12.1 MIGRATE-03; catalog rows flipped from WAIVED -> {PASS, WAIVED-extended-to-2026-07-26}. CLAUDE.md gained "P61 -- Subjective gates skill + freshness-TTL" H3 subsection (~60 lines under 80 cap; banned-words clean) + 2 in-place updates ("Cold-reader pass" section + "Subagent delegation rules" table). SURPRISES.md gained <COUNT> P61 entries (3 P57 + 7 P58 + 6 P59 + N P60 + N P61 = ~16+N entries; archive rotation per P59/P60 precedent if >240 lines). Verifier subagent verdict GREEN at `quality/reports/verdicts/p61/VERDICT.md` (Path B in-session disclosure per P56/P57/P58/P59/P60 precedent if Task tool unavailable; 4 disclosure constraints honored). 3 catalog rows graded; all P0+P1 PASS or WAIVED-extended with documented carry-forwards. Carry-forwards: auto-dispatch on STALE from CI (v0.12.1; needs Anthropic API access in GH Actions runner); hard-gate chaining release.yml -> quality-pre-release.yml (v0.12.1; needs composite workflow); Wave G P2 findings (cite). Next: P62 -- repo-org-gaps cleanup (ORG-01 + POLISH-ORG).
```

Update Current Position cursor:

```markdown
Phase: 62
Plan: -
Cursor: P61 SHIPPED -- Subjective gates skill + freshness-TTL enforcement. 3 rubrics catalogued; reposix-quality-review skill scaffold + 3 rubric impls + 2 dispatchers; runner freshness-TTL extension; pre-release workflow live; POLISH-SUBJECTIVE Wave G dispatch + findings triage complete; CLAUDE.md gained P61 H3 subsection. Runner state: pre-push exit 0 / pre-pr exit 0 / weekly exit 0 / pre-release exit 0 / post-release exit 0. Verifier verdict GREEN at `quality/reports/verdicts/p61/VERDICT.md`. Carry-forwards: auto-dispatch on STALE (v0.12.1 MIGRATE-03); hard-gate chaining (v0.12.1 MIGRATE-03); Wave G P2 findings if any. Next: P62 repo-org-gaps cleanup (ORG-01 + POLISH-ORG). Gate-state precondition for P62: read `quality/reports/verdicts/p61/VERDICT.md` to confirm GREEN, read `quality/SURPRISES.md`, read this entry, then `/gsd-plan-phase 62`.
Status: shipped (P61)
Last activity: 2026-04-27 -- P61 closed with GREEN verdict at `quality/reports/verdicts/p61/VERDICT.md`. 3 catalog rows graded; all P0+P1 PASS or WAIVED-extended. Subjective dimension complete. CLAUDE.md / SURPRISES.md / STATE.md / REQUIREMENTS.md / verdict updates landed in Wave H. Next: P62.
```

Update progress frontmatter:
```yaml
progress:
  total_phases: 8
  completed_phases: 6  # was 5
  total_plans: 38      # was 30 (P56=4 + P57=6 + P58=6 + P59=6 + P60=8 + P61=8 = 38)
  completed_plans: 38  # was 30
  percent: 75          # was 63; (6/8 = 75%)
```

**REQUIREMENTS.md traceability flips (Task 3):**

Update the per-phase row entries:
```
| SUBJ-01 | P61 | shipped (P61) |
| SUBJ-02 | P61 | shipped (P61) |
| SUBJ-03 | P61 | shipped (P61) |
| POLISH-SUBJECTIVE | P61 | shipped (P61) |
```

Update MIGRATE-03 row to add v0.12.1 carry-forward entries (append to the existing MIGRATE-03 cell content):
```
+ Plus (added 2026-04-27 from P61 Wave F + Wave G):
+ (e) Auto-dispatch on STALE rows from CI: needs Anthropic API access from GH Actions runner; deferred to v0.12.1.
+ (f) Hard-gate chaining release.yml -> quality-pre-release.yml: needs composite workflow OR workflow_run trigger; deferred to v0.12.1.
+ (g) Subjective rubric Wave G P2 findings: <list specifics from Wave G summary>.
```

Update the per-phase requirement counts paragraph footer:
```
P61=4 requirements, all SHIPPED (SUBJ-01, SUBJ-02, SUBJ-03, POLISH-SUBJECTIVE)
```

**Verifier subagent prompt (Task 4) -- copy verbatim from quality/PROTOCOL.md § "Verifier subagent prompt template":**

```
You have ZERO context from this session. You are the unbiased verifier for v0.12.0 Phase 61.

Inputs:
1. .planning/REQUIREMENTS.md ## v0.12.0 -- find requirements assigned to Phase 61
2. quality/catalogs/*.json -- find rows tagged phase=61 or whose verifier ships in this phase
3. CLAUDE.md -- confirm this phase's contributions are documented (per QG-07)
4. quality/SURPRISES.md -- confirm any pivots are journaled

For each catalog row in scope:
1. Read row.verifier.script and row.expected.asserts
2. If row.kind == "container", run quality/gates/docs-repro/container-rehearse.sh <row.id>
3. Else, invoke row.verifier.script with row.verifier.args
4. Compare row.artifact contents against row.expected.asserts
5. Grade: PASS (all asserts met) | FAIL (any P0+P1 assert unmet) | PARTIAL (P2 unmet only) | NOT-VERIFIED (no artifact dated this session)

For CLAUDE.md (per QG-07):
1. git diff main...HEAD -- CLAUDE.md
2. Confirm this phase's new files/conventions/gates appear in the diff
3. If absent: phase is NOT done -- block the GREEN verdict

For SURPRISES.md:
1. Read the file; confirm any pivot in the phase has a corresponding entry
2. If a pivot is mentioned in commits but absent from SURPRISES.md: ungrounded pivot -- flag

Output a scored markdown table with file:line citations. Write to quality/reports/verdicts/p61/<ISO_TIMESTAMP>.md.
Refuse to grade GREEN unless every P0+P1 catalog row is PASS or WAIVED AND CLAUDE.md is updated.
```

Path A: dispatch via Task tool (general-purpose subagent). Path B fallback: in-session verdict authored by Wave H executor with the 4 disclosure constraints honored verbatim.

**SURPRISES.md anti-bloat decision (Task 5):**

Read `quality/SURPRISES.md` line count post-P61-entries-appended:
- If <=240 lines: leave as-is (current 244 minus 4 over the 240 trigger; P61 entries push to ~250-260; defer rotation).
- If 241-270 lines: defer rotation to P62 (marginal).
- If >270 lines: archive next 50 oldest to `quality/SURPRISES-archive-2026-Q2.md` (P59 created the archive; P60 deferred rotation).

Most likely outcome: 250-260 lines post-P61; defer rotation per the precedent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QG-07 CLAUDE.md update -- append P61 H3 subsection + 2 in-place updates</name>
  <files>CLAUDE.md</files>
  <action>(1) Append a new H3 subsection at the END of the existing P57+P58+P59+P60 Quality Gates subsection chain. Title: `### P61 -- Subjective gates skill + freshness-TTL (added 2026-04-27)`. Body per the `<interfaces>` shape spec: rubric table (3 rows + skill mapping); reposix-quality-review skill summary (SUBJ-02); freshness-TTL extension summary (SUBJ-03); pre-release workflow + release.yml soft-gate doc; POLISH-SUBJECTIVE Wave G outcomes with COUNTS from Wave G's summary; v0.12.1 MIGRATE-03 carry-forwards. <=80 added lines. Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail). (2) In-place update to "Cold-reader pass on user-facing surfaces" section: add ~5-line paragraph pointing at the new skill + the rubric catalog. (3) In-place update to "Subagent delegation rules" table: add a row for "Dispatching subjective rubrics" -> "reposix-quality-review skill (after P61 ships)". Banned-words check: do NOT use `replace`; use `migrate to`, `swap`, `supplant by`, `rewrite as` per the prompt + CLAUDE.md banned-words rule. Run `bash scripts/banned-words-lint.sh --all` post-edit (or whatever lint exists; might be moved to quality/gates/structure/banned-words.sh per P57).</action>
  <verify>
    <automated>added_lines=$(git diff main -- CLAUDE.md 2>/dev/null | grep -E '^\+' | wc -l); test "$added_lines" -le 95 || { echo "TOO LONG ($added_lines added; cap 95 = 80 H3 + 5 cold-reader + 1 row + buffer)"; exit 1; }; grep -E '\breplace\b' CLAUDE.md | head -5 && { echo "BANNED in CLAUDE.md"; exit 1; } || echo "OK no banned word"; grep -F 'P61' CLAUDE.md | head -3 && grep -F 'subjective dimension' CLAUDE.md && grep -F 'reposix-quality-review' CLAUDE.md && echo "OK P61 subsection + skill citation present"</automated>
  </verify>
  <done>P61 H3 subsection appended (<=80 lines); 2 in-place updates landed (cold-reader pointer + delegation table row); banned-words clean; cites SUBJ-01/02/03 + POLISH-SUBJECTIVE + reposix-quality-review skill + v0.12.1 carry-forwards.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md cursor advance + roadmap-evolution entry</name>
  <files>.planning/STATE.md</files>
  <action>(1) Append a new bullet at the TOP of `## Accumulated Context > ### Roadmap Evolution` per the `<interfaces>` STATE.md shape. Cite Wave G outcomes: COUNT of P0/P1 fixed in-phase; COUNT of P2 deferred to v0.12.1; specific findings (if any). P60+P59+P58+P57+P56 entries preserved verbatim. (2) Update `## Current Position` cursor: Phase 62; Cursor cites P61 SHIPPED with the runner state line + the carry-forwards list; Last activity: 2026-04-27 P61 closed with GREEN verdict. (3) Update progress frontmatter: completed_phases 5 -> 6; total_plans 30 -> 38; completed_plans 30 -> 38; percent 63 -> 75.</action>
  <verify>
    <automated>grep -F 'P61 SHIPPED' .planning/STATE.md | head -3 && grep -F 'Phase: 62' .planning/STATE.md && grep -F 'completed_phases: 6' .planning/STATE.md && grep -F 'total_plans: 38' .planning/STATE.md && grep -F 'percent: 75' .planning/STATE.md && echo "OK STATE.md cursor advanced"</automated>
  </verify>
  <done>Roadmap-Evolution entry appended; Current Position advanced to P62; progress frontmatter incremented; P60/P59/P58/P57/P56 entries preserved.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md traceability table flips + MIGRATE-03 amend</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>(1) Update the traceability table: flip 4 rows from `planning` -> `shipped (P61)`: SUBJ-01, SUBJ-02, SUBJ-03, POLISH-SUBJECTIVE. (2) Append v0.12.1 carry-forward entries to the existing MIGRATE-03 row text: (e) auto-dispatch on STALE rows from CI; (f) hard-gate chaining release.yml -> quality-pre-release.yml; (g) Wave G P2 findings (cite specifics). (3) Update the per-phase requirement counts paragraph footer: P61 entry from `planning` to `all SHIPPED (SUBJ-01, SUBJ-02, SUBJ-03, POLISH-SUBJECTIVE)`. Banned-words: no `replace`.</action>
  <verify>
    <automated>grep -c 'shipped (P61)' .planning/REQUIREMENTS.md | grep -E '^([4-9]|[0-9]{2,})$' || { echo "FAIL: <4 shipped (P61) entries"; grep 'shipped (P61)' .planning/REQUIREMENTS.md; exit 1; }; grep -F 'SUBJ-01' .planning/REQUIREMENTS.md | grep -F 'shipped (P61)' && grep -F 'SUBJ-02' .planning/REQUIREMENTS.md | grep -F 'shipped (P61)' && grep -F 'SUBJ-03' .planning/REQUIREMENTS.md | grep -F 'shipped (P61)' && grep -F 'POLISH-SUBJECTIVE' .planning/REQUIREMENTS.md | grep -F 'shipped (P61)' && grep -E 'auto-dispatch.*STALE' .planning/REQUIREMENTS.md && echo "OK 4 requirements flipped + MIGRATE-03 carry-forwards appended"</automated>
  </verify>
  <done>4 requirement IDs flipped; MIGRATE-03 row gains v0.12.1 carry-forward entries; per-phase footer updated; banned-words clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: QG-06 verifier subagent dispatch (Path A or Path B)</name>
  <what-built>The Wave H executor needs to dispatch the unbiased verifier subagent (Path A via Task tool if available; Path B in-session disclosure per P56/P57/P58/P59/P60 precedent if not). The verifier reads catalog rows + artifacts + CLAUDE.md diff with ZERO session context and grades GREEN/RED. Verdict at quality/reports/verdicts/p61/VERDICT.md.</what-built>
  <how-to-verify>
    1. Confirm Task tool availability: if `Task({subagent_type: 'general-purpose', ...})` is callable, use Path A. Otherwise, use Path B.
    2. **Path A**: Dispatch the subagent with the verbatim prompt from `quality/PROTOCOL.md` § "Verifier subagent prompt template" (replace `<N>` with `61` and `<ISO_TIMESTAMP>` with the current UTC timestamp). The subagent writes the verdict file. Wait for it to return.
    3. **Path B (Wave H executor authors verdict in-session with 4 disclosure constraints honored)**: Author `quality/reports/verdicts/p61/VERDICT.md` per the P60 Wave H precedent (`quality/reports/verdicts/p60/VERDICT.md` is the template). Include disclosure block + per-row table for the 3 P61-touched rows + recurring-criteria evidence + recommendation.
    4. Confirm the verdict file exists at `quality/reports/verdicts/p61/VERDICT.md` AND grades GREEN.
    5. If RED: do NOT proceed. Investigate per Wave G escalation rules; fix the RED + re-grade.
    6. Type "approved -- verdict GREEN" to resume.
  </how-to-verify>
  <resume-signal>Type "approved -- verdict GREEN" or describe the RED + required action</resume-signal>
</task>

<task type="auto">
  <name>Task 5: SURPRISES.md update + atomic Wave H commit</name>
  <files>quality/SURPRISES.md, (commit only)</files>
  <action>(1) Read `quality/SURPRISES.md`. Append P61 pivot entries from Waves A-G + this Wave H. Likely entries: Wave A short-lived waiver pattern (3 rows; familiar); Wave B parse_duration extraction if anti-bloat triggered; Wave C cross-import path arithmetic; Wave D doc-clarity-review parse format if hit; Wave E rubric prompt jargon iteration if hit; Wave F GH Actions chaining limitation (already filed as v0.12.1 MIGRATE-03 carry-forward; SURPRISES entry just confirms); Wave G subagent variance + any in-phase findings fixed; Wave H verifier dispatch path (A or B). (2) Anti-bloat: count lines post-edit. If <=240, leave as-is. If 241-270, defer rotation to P62. If >270, archive next 50 oldest entries. (3) Commit Tasks 1-4 + this update in 1 atomic commit titled `chore(p61): POLISH closure + CLAUDE.md update + STATE/SURPRISES/REQUIREMENTS advance + verifier verdict GREEN`. Body cites: P61 4 requirements shipped; runner GREEN across 5 cadences; verdict GREEN; CLAUDE.md QG-07 + 2 in-place updates; SURPRISES.md update + line count; STATE.md cursor advance.</action>
  <verify>
    <automated>grep -F 'P61' quality/SURPRISES.md | head -3 && lines=$(wc -l < quality/SURPRISES.md) && echo "SURPRISES.md lines: $lines"; if [ "$lines" -gt 270 ]; then echo "WARN: anti-bloat -- rotate to archive"; fi; test -f quality/reports/verdicts/p61/VERDICT.md && grep -F 'GREEN' quality/reports/verdicts/p61/VERDICT.md | head -1 && git log -1 --format='%s' | grep -F 'p61' && echo "OK Wave H clean"</automated>
  </verify>
  <done>SURPRISES.md updated with P61 entries; anti-bloat decision documented; verdict file exists + grades GREEN; commit lands all 5 docs updates (CLAUDE.md + SURPRISES.md + STATE.md + REQUIREMENTS.md + VERDICT.md) in the same commit; pre-push hook self-verifies (which is the proof Wave A-G + this commit all work).</done>
</task>

</tasks>

<verification>
After Wave H close:
- CLAUDE.md gains P61 H3 subsection (<=80 added lines; banned-words clean) + 2 in-place updates.
- .planning/STATE.md cursor advanced to P62; progress frontmatter incremented.
- .planning/REQUIREMENTS.md traceability table has 4 rows flipped to shipped (P61); MIGRATE-03 row gains v0.12.1 carry-forwards.
- quality/SURPRISES.md gains P61 entries (~3-7); anti-bloat decision documented.
- quality/reports/verdicts/p61/VERDICT.md exists + grades GREEN.
- All 5 runner cadences exit 0 (post-Wave-G).
- Pre-push hook self-verifies on the Wave H commit (no --no-verify).
- The next agent reading .planning/STATE.md knows P61 is SHIPPED + reads the verdict + reads the SURPRISES entries to know any P61 pivots.
- v0.12.0 milestone progress: 6/8 phases (75%); 38/38 plans within shipped phases.
</verification>

<success_criteria>
1. **Catalog-first criterion 1:** N/A for Wave H (Wave A locked the contract).
2. **CLAUDE.md update (criterion 2 / QG-07):** PASS -- P61 H3 subsection landed in same PR; <=80 lines for new section; 2 in-place updates landed; banned-words clean.
3. **Verifier-subagent dispatch (criterion 3 / QG-06):** PASS via Path A (preferred) or Path B (precedent fallback). Verdict GREEN at `quality/reports/verdicts/p61/VERDICT.md`.
4. **SUBJ-01..03 + POLISH-SUBJECTIVE closure (criterion 4):** all 4 requirement IDs flipped to shipped (P61). Closure cited in CLAUDE.md + SUMMARY.
5. **Fix every RED row (criterion 5):** Wave G's broaden-and-deepen handles. Wave H confirms zero P0+P1 RED at phase close via the verifier subagent verdict.
</success_criteria>

<pivot_rules>
- **Path A Task tool unavailable** in Wave H executor: per P56/P57/P58/P59/P60 precedent, fall back to Path B (in-session verdict with disclosure block). The disclosure constraints are NON-NEGOTIABLE: evidence-only, catalog-rows-as-contract, refuse-GREEN-on-RED, out-of-session-re-grade-match. Author the verdict per the `quality/reports/verdicts/p60/VERDICT.md` template (P60 Wave H's verdict is the most-recent precedent).
- **A row is RED at Wave H entry**: Wave G should have caught it. If a regression slipped through, fix BEFORE dispatching the verifier. Do NOT bypass the verifier with `--no-verify` on commits.
- **CLAUDE.md banned-words-lint catches a hit**: identify the offending word in the new P61 subsection. Most common: `replace` (use `migrate to`, `swap`, `supplant by`, `rewrite as`). Fix and re-run lint.
- **SURPRISES.md crossing 270 lines** post-P61-entries: archive the next 50 oldest entries to `quality/SURPRISES-archive-2026-Q2.md` (which already exists from P59 Wave F). Document the rotation in a header note.
- **A new pivot in Wave H itself** (e.g. the verifier reveals a contract drift): append a final SURPRISES.md entry. Do NOT silently fix it -- the verifier saw the catalog at this commit SHA and graded; if a fix is needed, that's a follow-up commit.
- **The verifier subagent grades RED on something Wave G missed** (e.g. a P0+P1 row that Wave G didn't address): per `quality/PROTOCOL.md` § "When stuck rules", append SURPRISES + dispatch a `gsd-phase-researcher` to investigate + propose 2-3 paths forward. Pause via STATE.md cursor update if needed.

Reference: `quality/reports/verdicts/p60/VERDICT.md` (P60 Wave H verdict template -- Path B disclosure shape); `.planning/phases/60-docs-build-migration/60-08-PLAN.md` (Wave H structure precedent for this Wave H); `quality/PROTOCOL.md` § "Verifier subagent prompt template" (verbatim verifier prompt); CLAUDE.md "Subagent delegation rules" (Path A vs Path B precedent).
</pivot_rules>

<risk_table>
| Risk | Likelihood | Mitigation |
|---|---|---|
| Path A Task tool unavailable -> Path B fallback authored by Wave H executor | medium-high (matches P56/P57/P58/P59/P60 pattern) | The 4 disclosure constraints + the P60 verdict template provide the structure; out-of-session re-grade by a future Path A subagent should produce identical verdict |
| CLAUDE.md banned-words-lint catches `replace` in the new subsection | medium (word is common; humans default to it) | Pivot rule + Task 1 verify gate catches; fix-and-retry |
| SURPRISES.md crosses 270 lines post-P61 entries | medium | Pivot rule: archive 50 oldest to existing 2026-Q2 archive; precedent in P59 Wave F |
| Wave G sweep missed a RED row that Wave H verifier subagent surfaces | low (Wave G's recurring criterion 5 is explicit; runner sweep catches everything) | Pause Wave H; fix in a separate commit; re-dispatch verifier; document in SURPRISES |
| Pre-push hook (P60 Wave E rewrite) blocks Wave H commit because it found a NEW RED | low (Wave G ensured GREEN) | If a regression on Wave H commit only: investigate which Wave H file broke it (most likely CLAUDE.md banned-words); fix and retry; do NOT --no-verify |
| .planning/STATE.md frontmatter math wrong (e.g. progress.percent off) | low | Math in the spec: 6/8 = 75% |
| .planning/STATE.md `total_plans` count wrong (P56=4, P57=6, P58=6, P59=6, P60=8, P61=8 = 38) | low | Verify in the spec |
| The Wave H executor accidentally bypasses the verifier dispatch (autonomous: false intentionally pauses) | medium-high (executors love to autopilot) | The plan is `autonomous: false` + Task 4 is `checkpoint:human-verify`; if Path A unavailable, Path B disclosure is non-negotiable -- the Wave H executor cannot ship without authoring the disclosure + verdict |
| Anti-bloat overflow on CLAUDE.md (P57+P58+P59+P60+P61 H3 subsections accumulate) | low (each is <=80 lines; total ~330-380 lines under the Quality Gates section is acceptable; MIGRATE-02 in P63 audits) | P63 MIGRATE-02 cohesion pass handles cross-link audit + dedup |
| Wave H accidentally re-runs Wave G dispatchers and overwrites artifacts (artifacts then have wrong ts) | low | Wave H does NOT touch dispatchers; only docs + commit; verify gate confirms only the 5 docs files in commit diff |
| MIGRATE-03 v0.12.1 carry-forward entries collide with existing P56-P60 carry-forwards | low | Append-only; cite source phase + wave |
</risk_table>

<output>
After completion, create `.planning/phases/61-subjective-gates-skill-freshness-ttl/61-08-SUMMARY.md` capturing:
- CLAUDE.md added line count (<=80 for new H3 + ~5 for cold-reader update + 1 for delegation table row).
- STATE.md cursor advanced to P62 (verified).
- REQUIREMENTS.md 4 traceability flips + MIGRATE-03 amend (verified).
- SURPRISES.md P61 entry count + line count (and any archive rotation if exceeded).
- Verdict file path + GREEN status.
- Path A vs Path B used.
- Per-row grade table for the 3 P61-touched rows.
- v0.12.1 MIGRATE-03 carry-forward summary: auto-dispatch on STALE; hard-gate chaining; Wave G P2 findings (if any).
- Recommendation: "P62 may begin per `quality/reports/verdicts/p61/VERDICT.md`."
- Phase-close commit SHA + commit message.
</output>
</content>
</invoke>