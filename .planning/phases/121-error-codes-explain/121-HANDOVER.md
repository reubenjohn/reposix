## PHASE
P121 "Error codes + `reposix explain <code>` — Rust-compiler-grade code namespace" (milestone v0.15.0 "Floor"). C1 phase-coordinator (opus tier) relief handover. Requirement UX-02. Plan: `/home/reuben/workspace/reposix/.planning/phases/121-error-codes-explain/121-PLAN.md`.

## CURRENT STATE (all committed on `main`, NOT pushed — successor owns push cadence)
- W0: `6d8c69f2` (plan commit + M1 two-tier-framing fix + ROADMAP `.planning/ROADMAP.md` Plans:1), `cab82d15` (catalog-first gate — `quality/gates/agent-ux/rpx_registry_check.py` + row `agent-ux/rpx-codes-registry` NOT-VERIFIED + `rpx-codes-registry.sh`).
- W1: `30c518da` (`crates/reposix-core/src/codes.rs` — 24 rustc-grade entries + `[RPX-xxxx]` inline render in `errmsg.rs` (tag on headline's FIRST line, multi-line-safe) + pinning-test transition `code_slot_renders_nothing`→`code_renders_tag_and_nudge` + `teach_coded()`), `ab789edc` (reconciliation+intake).
- W2: `57fe43bf` (registry completed 24→31: minted RPX-0204 sync bound-tree URL, 0307 non-UTF8 path, 0308-0311 the four `init --since` rewind sites, 0506 UNFILTERED_FETCH), `99d9668c` (`reposix explain` subcommand — `crates/reposix-cli/src/explain.rs`, `Explain { code: Option<String>, --list }` in main.rs, output mirrors `rustc --explain`, registry-driven `--list`, unknown→RPX-0900 teach exit 1; `explain::run` is SYNC not async — deliberate, a pure static reader; registry-driven tests in `crates/reposix-cli/tests/explain.rs`).
- W3: `c0cdda5e` (CLI wiring — 24 sites via `.code(ids::…)`/`teach_coded`), `4420670c` (helper wiring — 6 reposix-remote sites), `53a6282d` (eager-fixed pre-existing rust-1.96 clippy blocker in explain.rs), `60971f58` (flipped `agent-ux/rpx-codes-registry` NOT-VERIFIED→PASS).
- **Status:** Registry COMPLETE = 31 codes. `reposix explain` ships. Gate `agent-ux/rpx-codes-registry` = PASS, `python3 quality/gates/agent-ux/rpx_registry_check.py` exits 0 (legs: forward + M3-all-syntaxes-both-crates + M2-converse + leg-d unknown-code; `--self-test` green). Cargo clippy(`--workspace --all-targets -D warnings`)/test/fmt all clean. Tree clean. **Environment note:** nextest is ABSENT — `cargo test` is the documented fallback. ONE cargo invocation machine-wide, FOREGROUND only.
- SC1 proven live: `reposix init 'not-a-spec' …`→stderr `… [RPX-0001]` + `Explain: reposix explain RPX-0001`; `reposix log /tmp`→`[RPX-0301]`+nudge.

## SUCCESS CRITERIA (grade goal-backward at close)
- SC1: every user-facing CLI + helper error emits a `[RPX-xxxx]` code — proven by extended `errors_teach_recovery` suites + real error-path grep on both binaries.
- SC2: `reposix explain <code>` prints non-empty cause + fix + copy-paste recovery for EVERY registry code — per-code assertions in `tests/explain.rs` + the gate.
- SC3: `reposix explain` output shape matches `rustc --explain E0308` (code header + extended body + fix section) — `rustc_parity_of_shape` test.
- SC4: committed `agent-ux/rpx-codes-registry` gate asserts registry completeness+uniqueness+format + unknown-code-teaches; `--list`/no-arg enumerates.
- SC5: `reposix explain` + RPX namespace documented (error-codes.md in nav + cli.md section), mkdocs-strict + banned-words green, CLAUDE.md revised.
- OP-2 threat cut: registry + code slot + explain output are `&'static str`-only (no tainted byte reaches them).

## REMAINING WORK (in order)
### 1. W3.5 — helper-error correctness completion (sonnet executor). REQUIRED for honest SC1/SC2 close — two absorbed W3 noticings:
   (a) **RPX-0601 semantic stretch.** `crates/reposix-remote/src/bus_handler.rs:461` emits a mirror UNREACHABLE/misconfigured error but is coded RPX-0601, whose registry title is "malformed reposix bus URL" — reachability ≠ malformed-syntax, so `reposix explain RPX-0601` gives a WRONG explanation for that real error path. FIX: mint a new 06xx code (next free, e.g. RPX-0602) "mirror unreachable/misconfigured" with rustc-grade prose (teach cause+fix+alternative+copy-paste recovery) in `codes.rs` + re-point bus_handler.rs:461 to it. (The "registry frozen" note from W3 was a W3-internal tidiness constraint, NOT a project rule — lifting it for a correctness fix is correct and expected.)
   (b) **Helper const-string SC1 gap (gate blind-spot).** RPX-0503 (`BLOB_LIMIT_EXCEEDED_FMT`), RPX-0506 (`UNFILTERED_FETCH_HINT`), RPX-0504/0505 (write_loop diag lines) are REGISTERED but their raw const-string emissions do NOT carry the `[RPX-xxxx]` tag — they don't route through `teach`/`Teach`, so the M2 gate leg (teach-shaped sites only) never flagged them = a real gate blind spot. BLOB_LIMIT is documented in root CLAUDE.md as a load-bearing AGENT-FACING recovery error, so per SC1 it MUST carry its code in output. FIX per code: determine whether the emission is a HUMAN-FACING error message (shown to the user on failure) or a GIT-PROTOCOL status line that git itself parses. If human-facing → add the `[RPX-xxxx]` tag + update the pinning integration test that asserts that exact stderr string (same pattern as W1's errmsg pinning-test transition) + STRENGTHEN `rpx_registry_check.py` to also verify these non-teach helper emission forms carry their code (close the blind spot). If protocol-only (git parses it, NOT user-facing) → that code is not a user-facing error code: REMOVE it from the registry (no mis-registered/dead entries). HARD CAUTION: do NOT add `[RPX-xxxx]` to any stderr line git parses as protocol — it breaks the helper; if unsure whether a line is protocol vs human-facing, treat as protocol and escalate. Keep checker exit 0 + gate PASS honest after changes.
   Verify: real error-path trigger for the blob-limit path shows its code; cargo `-p reposix-remote` + `-p reposix-core` green; checker exit 0.

### 2. W4 — docs + fix-twice + bookkeeping (sonnet executor):
   - NEW `docs/reference/error-codes.md` (documenting the RPX namespace + `reposix explain` usage; 80%-use-case-first) + add to `mkdocs.yml` nav. MUST pass BOTH `bash quality/gates/docs-build/mkdocs-strict.sh` AND `bash quality/gates/structure/banned-words.sh` (fix-twice: run BOTH, or `python3 quality/runners/run.py --cadence pre-push`; a docs-build-only sweep once leaked a plumbing term).
   - `docs/reference/cli.md`: add the `explain` subcommand to the Commands block; update `cli.rs::help_lists_all_subcommands` "15 subcommands" comment/ground-count (15→16, now that explain is the 16th) AND rebind the `doc-alignment` binding for any reworded line in the SAME commit (avoid STALE_DOCS_DRIFT — see root CLAUDE.md fix-twice cont'd). [W2 noticing 1]
   - CLAUDE.md fix-twice: document the RPX error-code namespace + `reposix explain` convention + how to add a new code (which registry, the gate) in the relevant CLAUDE.md (root or `crates/`), so the next agent's session reads the tightened rule.
   - P120 roadmap-staleness [W0 noticing 3]: `/home/reuben/workspace/reposix/.planning/ROADMAP.md` ~L298 shows P120 `0/TBD | Not started` despite P120 CLOSED GREEN — fix to P120's actual closed state. (The earlier P120-edit ban was scoped to W0 bookkeeping; this correction is now in-scope as a noticed lying-doc fix.)

### 3. CODE-REVIEW — dispatch gsd-code-reviewer over the FULL P121 diff. SPECIFICALLY: verify each `.code()` maps to the SEMANTICALLY-CORRECT registry entry (a mis-map is a silent UX bug the gate cannot catch — spot-check every site→code pairing); explain-output quality (rustc-grade: teaches fix + alternative + copy-paste recovery); NO secret/credential in any error or explain string (cred-hygiene doctrine). Fix findings or file per OP-8.

### 4. PUSH cadence — `git fetch origin && git rebase origin/main` BEFORE `git push origin main` (concurrent sessions push to main). After push: `python3 quality/runners/run.py --cadence post-push --persist`; the `code/ci-green-on-main` (P0) probe must show main's NEWEST `ci.yml` run concluded success. If your push's run is in-flight: `gh run watch <id> --exit-status` (background) then re-confirm green. NEVER open the next phase over a red main.

### 5. VERIFY — dispatch gsd-verifier → `quality/reports/verdicts/p121/VERDICT.md`, graded goal-backward vs SC1-SC5. RED → loop back to fix (don't close); if unresolvable → report to L0. GREEN → close.

### 6. CLOSE (on GREEN) — advance `/home/reuben/workspace/reposix/.planning/STATE.md`: `completed_phases` 7→8, `percent` 47→53, cursor "P121 CLOSED GREEN … next = P122". Commit VERDICT+STATE, push (fetch-rebase first), re-confirm `code/ci-green-on-main`. Then report done to L0.

## OPEN INTAKE / TRACKED (already durable)
- P121-W1-01 (SURPRISES-INTAKE) — code-allocation undercount map; now FULLY CONSUMED (registry complete). RPX-0304 correctly left unallocated (gc `--strategy` is a working value_enum).
- GTH-V15-68 (GOOD-TO-HAVES) — `codes.rs` oversized-`.rs`; waived.
- File-size: `init.rs` ~40k + 4 `reposix-remote` src files 35-42k over the 20k `*.rs` soft ceiling — WAIVED until 2026-08-08; in the oversize-`.rs` class (GTH-V15-65/68); `crates/**/*.rs` fall outside the blocking gate glob. No action this phase.
- W3 noticing 1 (RPX-0601) is being ABSORBED into W3.5(a) above — not deferred.

## NON-NEGOTIABLES (embed OD-3 charter verbatim in every dispatch)
ONE cargo invocation machine-wide, FOREGROUND only (never run_in_background), prefer `-p <crate>`, jobs=2 (VM OOM risk). Leaf test setup (init/sim-seed/git config/commit) in a `/tmp` clone with `cd` in the SAME Bash invocation — never the shared repo (PreToolUse hook enforces). Uncommitted = didn't happen; commit before ending any turn. No `--no-verify`. Tainted-by-default (codes are static `&'static str`; no remote byte into the code slot/explain output). DO NOT touch the launch-animation E1 publish (GTH-V15-37, owner-PENDING) or implement GTH-V15-67 (cred-hygiene allow-marker). Understand intention over literal plan execution.

OD-3 ownership charter (embed verbatim in every executor/reviewer/verifier dispatch): (1) acceptance criteria are the floor — done = "I'd defend this in review as excellent"; (2) noticing is a deliverable — name what you notice near the work; empty noticing from code work = red flag; (3) eager-fix (<1h, no new dep) or file to `.planning/milestones/v0.15.0-phases/{SURPRISES-INTAKE,GOOD-TO-HAVES}.md` — never silently skip; (4) verify against reality — run the subcommand, hit the error path; (5) north star — Rust-compiler-grade UX: every explain entry teaches the fix, suggests the alternative, gives a copy-paste recovery command (exemplar `crates/reposix-cli/src/init.rs::refuse_existing_repo_root`).

## ESCALATION STATUS
No owner-decision open. Namespace format `RPX-xxxx` is ROADMAP-ratified (do not re-litigate). Appending the code + Explain-nudge to error output is the intended ROADMAP behavior, NOT a breaking change to escalate. STOP-AND-ESCALATE to L0 only if W3.5(b) reveals a genuine breaking change to git-parsed protocol output, or if the verifier REDs on something unresolvable.
