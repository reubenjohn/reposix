---
phase: 122-remote-init-hardening
plan: 01
type: execute
wave: 1
model_tier: sonnet
depends_on: []
files_modified:
  - quality/catalogs/agent-ux.json
autonomous: true
requirements: [DRAIN-07, DRAIN-08, DRAIN-09]

must_haves:
  truths:
    - "The phase's GREEN contract for all three carry-forwards (SC1/SC2/SC3) exists in the catalog BEFORE any implementation commit."
    - "The unbiased verifier can grade SC1, SC2, and SC3 from catalog rows that predate the impl."
  artifacts:
    - path: "quality/catalogs/agent-ux.json"
      provides: "Two new rows (import-parent-resolve-fails-loud, init-refuses-nested-in-shared-tree) + extended stateless-connect asserts on rebase-recovery-reconciles"
      contains: "import-parent-resolve-fails-loud"
  key_links:
    - from: "quality/catalogs/agent-ux.json rows"
      to: "quality/gates/agent-ux/*.sh verifiers (authored in later waves)"
      via: "verifier.script paths"
      pattern: "import-parent-resolve-fails-loud|init-refuses-nested-in-shared-tree"
---

<objective>
Catalog-first commit for P122. Write the GREEN-contract catalog rows for all three
carry-forwards BEFORE any implementation lands, so the unbiased verifier grades rows that
predate the code (quality/PROTOCOL.md Step 3). NO cargo, NO code — pure JSON.

Purpose: The catalog-first rule (quality/CLAUDE.md) requires the phase's FIRST commit to
define what GREEN looks like. This plan is that commit.
Output: `quality/catalogs/agent-ux.json` gains two new rows and one extended row.
Model tier: sonnet (mechanical JSON authoring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-remote-init-hardening/RESEARCH.md
@quality/catalogs/README.md
@quality/CLAUDE.md

Non-negotiables (apply to EVERY P122 plan):
- ONE cargo invocation machine-wide, FOREGROUND-only (never run_in_background/detached),
  jobs=2 (`CARGO_BUILD_JOBS=2` is committed), prefer `-p <crate>`. (This plan runs no cargo.)
- Commit before ending any turn (uncommitted = didn't happen). Never `--no-verify`.
- Tainted-by-default: every remote byte (sim included) is attacker-influenced; egress only
  via `REPOSIX_ALLOWED_ORIGINS`; the sim is the default backend.
- Leaf isolation: any `reposix init`/sim-seed/`git config`/`git commit` runs in a /tmp
  clone with `cd /tmp/...` in the SAME Bash invocation (PreToolUse leaf-isolation-guard.sh
  exits 2 otherwise). (This plan does none of those.)
- Never hand-pick gates before a push — run `python3 quality/runners/run.py --cadence
  pre-push` (full cadence). P117/P121 both shipped avoidable 2-push cascades from
  hand-picked sweeps.
- Phase-close `git push origin main` is deferred to the coordinator. Do NOT push per-wave.
  All four P122 plans land locally, then ONE phase-close push — this is WHY the transiently
  unmet rows (the SC2/SC3 rows below AND the SC1 stateless-connect asserts) never reach CI.
  LOAD-BEARING: catalog-first is safe here ONLY because the coordinator never pushes
  mid-phase; a per-wave push would strand these rows as a false-RED on main. Do NOT push
  until all four plans have landed.

Ownership charter (OD-3 — the floor is excellence, not "plan executed"):
1. Acceptance criteria are the floor, not the ceiling: "done" = "I'd defend this in review
   as excellent," not "plan executed."
2. Noticing is a deliverable — every code-touching report names what it noticed nearby
   (lying doc claims, tests that don't assert what their names promise, teaching-free error
   messages, dead code, stale comments, missing edge cases). An empty noticing section from
   code-touching work is a red flag.
3. Eager-fix or file — never silently skip: <1h + no new dependency → fix in place; else →
   `.planning/milestones/v0.15.0-phases/{SURPRISES-INTAKE,GOOD-TO-HAVES}.md` with severity + sketch.
4. Verify against reality — run the binary, hit the path, render the page; a claim without a
   committed artifact isn't done.
5. North star — Rust-compiler-grade UX: every user-facing error (1) teaches the fix,
   (2) suggests the alternative, (3) gives a copy-paste recovery command. Exemplar:
   `crates/reposix-cli/src/init.rs::refuse_existing_repo_root`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two NEW catalog rows (SC2 + SC3)</name>
  <files>quality/catalogs/agent-ux.json</files>
  <action>
Append these two rows to the `rows` array of `quality/catalogs/agent-ux.json`. Use exactly
these shapes (schema: quality/catalogs/README.md). `minted_at` is set to today
(2026-07-17T00:00:00Z), write-once. `transport_claim: false` (these are corruption-defense /
helper-robustness rows, NOT transport claims — the explicit false suppresses the
description-regex that would otherwise force `coverage_kind: real-backend`). Specifically for
the SC2 helper row: its assert is unit-level `resolve_import_parent` exit-code semantics (a
bare `cargo test -p reposix-remote` bin-target check), NOT a transport-layer / real-backend
claim, so `transport_claim: false` is correct — record this rationale in 122-02-SUMMARY.

SC2 row (DRAIN-08, GTH-V15-05):
{
  "id": "agent-ux/import-parent-resolve-fails-loud",
  "dimension": "agent-ux",
  "cadences": ["on-demand"],
  "kind": "mechanical",
  "sources": ["crates/reposix-remote/src/main.rs:450-469", "crates/reposix-remote/src/main.rs:422"],
  "expected": { "asserts": [
    "cargo test -p reposix-remote (bare per-crate, bin-target unit tests) exits 0 for the resolve_import_parent regression tests",
    "resolve_import_parent returns Ok(None) ONLY for spawn-success + exit-1 + empty-stdout (genuine ref-absent first-fetch parentless seed)",
    "resolve_import_parent returns Err (loud) on a NON-absence git failure (spawn failure or a non-1 non-zero exit), proven by an injected fake-git shim that exits 128",
    "resolve_import_parent returns Err (loud, RPX-0508) on an anomalous exit-0-with-empty-stdout (git rev-parse --verify --quiet always prints an oid on success, so empty stdout is a broken-git signal, not a benign absence) rather than silently degrading to the parentless overlay",
    "the loud non-absence failure emits the RPX-0508 coded teaching instead of silently degrading to the parentless overlay"
  ] },
  "verifier": { "script": "quality/gates/agent-ux/import-parent-resolve-fails-loud.sh", "args": [], "timeout_s": 180 },
  "artifact": "quality/reports/verifications/agent-ux/import-parent-resolve-fails-loud.json",
  "status": "NOT-VERIFIED",
  "last_verified": null,
  "minted_at": "2026-07-17T00:00:00Z",
  "coverage_kind": "mechanical",
  "transport_claim": false,
  "blast_radius": "P1",
  "owner_hint": "crates/reposix-remote/src/main.rs::resolve_import_parent — distinguish ref-absent from non-absence git failure",
  "claim_vs_assertion_audit": "If resolve_import_parent still degraded to Ok(None) on a non-absence git failure, the injected exit-128 fake-git shim test would observe Ok(None) / a parentless overlay instead of the asserted Err + RPX-0508 teaching, so the assertion would fail — that falsifies the description claim that non-absence failures now error loudly rather than silently.",
  "waiver": null
}

SC3 row (DRAIN-09, GTH-V15-06):
{
  "id": "agent-ux/init-refuses-nested-in-shared-tree",
  "dimension": "agent-ux",
  "cadences": ["on-demand"],
  "kind": "mechanical",
  "sources": ["crates/reposix-cli/src/init.rs:135-168", "crates/reposix-cli/src/init.rs:266-350", ".claude/hooks/leaf-isolation-guard.sh:170-184"],
  "expected": { "asserts": [
    "cargo test -p reposix-cli (the P122 init-refusal regression tests) exits 0",
    "reposix init into a fresh subdir nested inside a NON-/tmp git working tree is refused with the RPX-0406 coded teaching that names reposix attach and prints a copy-paste recovery",
    "reposix init into a fresh subdir under a /tmp throwaway clone SUCCEEDS (the sanctioned dark-factory flow is preserved)",
    "a symlink/.. path that canonicalizes back into a non-/tmp git working tree is still refused (realpath canonicalization holds, mirroring leaf-isolation-guard.sh is_safe)",
    "the worktree-shared-config self-check fires: when git init binds the target to a SHARED git-dir (git -C <path> rev-parse --absolute-git-dir != <path>/.git, e.g. GIT_DIR injected by a subprocess/worktree bypass), reposix init aborts with RPX-0406 BEFORE any git config write reaches the shared config (test case (e))",
    "reposix attach against an existing checkout still succeeds (attach adoption is not regressed)"
  ] },
  "verifier": { "script": "quality/gates/agent-ux/init-refuses-nested-in-shared-tree.sh", "args": [], "timeout_s": 240 },
  "artifact": "quality/reports/verifications/agent-ux/init-refuses-nested-in-shared-tree.json",
  "status": "NOT-VERIFIED",
  "last_verified": null,
  "minted_at": "2026-07-17T00:00:00Z",
  "coverage_kind": "mechanical",
  "transport_claim": false,
  "blast_radius": "P0",
  "owner_hint": "crates/reposix-cli/src/init.rs — add nested-in-worktree refusal (RPX-0406) mirroring leaf-isolation-guard.sh is_safe; init only, never attach",
  "claim_vs_assertion_audit": "If reposix init did NOT refuse a fresh subdir nested inside a non-/tmp git working tree, the nested-in-non-tmp-repo test would observe a successful init (the shared-tree corruption vector left open) instead of the asserted RPX-0406 refusal — that falsifies the description claim; the /tmp-succeeds and attach-succeeds asserts independently falsify any over-broad refusal that broke the dark-factory flow or attach adoption. If the worktree-shared-config self-check were absent or a no-op, case (e) — which forces absolute-git-dir != <path>/.git via a GIT_DIR-injected shared store — would observe a completed init (config writes reaching the shared config) instead of the asserted pre-config RPX-0406 abort, falsifying the self-check claim.",
  "waiver": null
}

Validate the file parses: `python3 -m json.tool quality/catalogs/agent-ux.json >/dev/null`.
  </action>
  <verify>
    <automated>python3 -m json.tool quality/catalogs/agent-ux.json >/dev/null && python3 -c "import json;d=json.load(open('quality/catalogs/agent-ux.json'));ids={r['id'] for r in d['rows']};assert 'agent-ux/import-parent-resolve-fails-loud' in ids and 'agent-ux/init-refuses-nested-in-shared-tree' in ids; print('OK')"</automated>
  </verify>
  <done>Both new rows present, JSON parses, ids resolvable.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the SC1 rebase-recovery-reconciles row with stateless-connect asserts</name>
  <files>quality/catalogs/agent-ux.json</files>
  <action>
Find the existing `agent-ux/rebase-recovery-reconciles` row. Do NOT change its `minted_at`,
`cadences`, `kind`, `blast_radius`, or `status`. APPEND these two entries to its
`expected.asserts` array (they encode the DRAIN-07 stateless-connect coverage the gate will
satisfy in Wave 4):

  "on git >= 2.34 the gate ALSO runs both drift scenarios via the real stateless-connect path (protocol.version UNSET) — no bare TODO-skip remains on modern git"
  "the stateless-connect legs emit a deterministic per-scenario verdict: both converge via the documented `git pull --rebase && git push`, OR the divergence is captured as a labelled, filed known-divergence (never a silent skip or a faked green)"

Also EXTEND (do not replace) the row's `claim_vs_assertion_audit` string with a sentence:
"On git >= 2.34 the gate now exercises the stateless-connect read path (protocol.version
unset), so a regression that left it as a bare TODO-skip, or faked a green without
adjudicating convergence, would fail these asserts."

CAUTION (ordering, load-bearing): these asserts are transiently UNMET until Wave 4 lands the
gate impl. That is safe ONLY because the phase-close push is deferred (see Non-negotiables).
Do NOT run `run.py --persist` on the agent-ux catalog and do NOT push until Wave 4's gate
impl is in place. Note this in the SUMMARY.
  </action>
  <verify>
    <automated>python3 -c "import json;d=json.load(open('quality/catalogs/agent-ux.json'));r=[x for x in d['rows'] if x['id']=='agent-ux/rebase-recovery-reconciles'][0];a=' '.join(r['expected']['asserts']);assert 'stateless-connect' in a and 'TODO' in a; print('OK')"</automated>
  </verify>
  <done>SC1 row carries the two stateless-connect asserts; minted_at unchanged; JSON parses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog JSON → verifier | The catalog is the GREEN contract; a malformed or overclaiming row would let a false-green pass. This plan is the only trusted authoring point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-06 | Tampering | catalog rows | mitigate | Rows written with concrete `expected.asserts` that a downstream verifier must satisfy against a real artifact; `claim_vs_assertion_audit` explains falsifiability so an overclaiming assert can't hide. |
| T-122-07 | Repudiation | catalog-first ordering | accept | Catalog committed as the phase's FIRST commit (predates impl); the git log is the audit trail. No secret data in this file. |
</threat_model>

<verification>
- `python3 -m json.tool quality/catalogs/agent-ux.json` exits 0.
- The three rows (2 new + 1 extended) grade-ready with concrete asserts.
- Do NOT run `run.py --persist` for agent-ux in this wave (rows are intentionally unmet
  until later waves ship the verifiers/impl).
</verification>

<success_criteria>
Two new agent-ux rows present; the SC1 row extended with stateless-connect asserts; JSON
valid; no cargo run; no push.
</success_criteria>

<output>
After completion, create `.planning/phases/122-remote-init-hardening/122-01-SUMMARY.md`
noting the transient-unmet-SC1 ordering caveat and any noticing.
</output>
