---
phase: 122-remote-init-hardening
plan: 04
type: execute
wave: 4
model_tier: opus
depends_on: ["122-01"]
files_modified:
  - quality/gates/agent-ux/rebase-recovery-reconciles.sh
  - quality/catalogs/agent-ux.json
  - crates/CLAUDE.md
autonomous: true
requirements: [DRAIN-07]

must_haves:
  truths:
    - "On git >= 2.34 the rebase-recovery gate ALSO exercises the real stateless-connect read path (protocol.version unset) for both drift scenarios — no bare TODO-skip remains on modern git."
    - "Each stateless-connect scenario emits a deterministic verdict: convergence (PASS) OR a labelled, filed known-divergence — never a silent skip or a faked green."
    - "The legacy import-path legs still pass (old-git support is preserved)."
  artifacts:
    - path: "quality/gates/agent-ux/rebase-recovery-reconciles.sh"
      provides: "modern-git stateless-connect legs (protocol.version unset) with per-scenario adjudication"
      contains: "stateless-connect"
  key_links:
    - from: "rebase-recovery-reconciles.sh stateless-connect legs"
      to: "agent-ux/rebase-recovery-reconciles expected.asserts"
      via: "asserts_passed strings that token-map the SC1 asserts (F-K4b congruence)"
      pattern: "stateless-connect"
---

<objective>
Close GTH-V15-04 / DRAIN-07 and resolve P105 PLAN §5. RBF-LR-03's rebase-recovery fix
(`bd5b9cb`, disjoint import namespace `refs/reposix-import/*`) is proven only on the legacy
`import` path: the gate force-sets `protocol.version=0` for every git subprocess, so even on
CI's modern git (ubuntu-latest, ≥2.34) the `stateless-connect` (protocol-v2) READ path is
never exercised — it only logs a TODO. Extend the gate so that on git ≥2.34 it ALSO runs
both drift scenarios via the real stateless-connect path and adjudicates each deterministically.

P105 §5 (the exact open item, verbatim in RESEARCH.md): "Does the `stateless-connect`
(protocol-v2) fetch path — used by git ≥ 2.34 — ALSO break, or does it already fast-forward
off the cache's chained history? … If stateless-connect already works, the `import` fix
still ships (old-git support) and the gate must force the `import` path to keep guarding it.
If stateless-connect ALSO breaks, that is a SECOND fix site (cache-side) — file it, do not
silently expand this lane."

REALITY CHECK (verify against reality, OP-1): this VM's git is now **2.50.1** (the gate's
"only 2.25.1 installed" comments are STALE) — you can exercise the stateless-connect path
LOCALLY, in the gate's own /tmp workspace, before CI ever runs. Do so; do not guess.

Purpose: prove BOTH fetch paths reconcile on modern git, or gate+file the divergence.
Output: gate extended, catalog asserts satisfied (or divergence documented + filed), §5 resolved.
Model tier: opus (subtle git protocol-v2 vs v0 divergence + a branch-on-reality judgment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/122-remote-init-hardening/RESEARCH.md
@.planning/milestones/v0.14.0-phases/105-rbf-lr-03-rebase-recovery/105-PLAN.md

Non-negotiables (apply to EVERY P122 plan):
- ONE cargo invocation machine-wide, FOREGROUND-only (never run_in_background/detached),
  jobs=2, prefer `-p <crate>`. The gate builds/uses the workspace `reposix` binary — if a
  build is needed, `cargo build -p reposix-cli -p reposix-remote -p reposix-sim` FOREGROUND,
  one cargo at a time.
- Commit before ending any turn. Never `--no-verify`.
- Tainted-by-default: every remote byte (sim included) is attacker-influenced; the gate
  drives the sim (default backend). Keep egress to the allowlist; the transcript emits
  env_keys NAMES only (`cut -d= -f1` — reuse the existing pattern / transcript.sh helper).
- Leaf isolation: the gate manages its OWN throwaway RUN_DIR — do not point it at the shared
  tree. Any manual repro of `reposix init`/git config runs in a /tmp clone with `cd /tmp/...`
  in the SAME Bash invocation.
- Never hand-pick gates before a push — run `python3 quality/runners/run.py --cadence
  pre-push` (full cadence). Phase-close push is the coordinator's; do NOT push per-wave.
  This plan lands LAST in the phase, so it also un-strands the transient SC1 asserts that
  122-01 committed (they become satisfied here).

Ownership charter (OD-3):
1. Acceptance criteria are the floor, not the ceiling: "done" = "I'd defend this in review
   as excellent," not "plan executed."
2. Noticing is a deliverable — name what you notice nearby. An empty noticing section from
   code-touching work is a red flag.
3. Eager-fix or file — never silently skip: <1h + no new dependency → fix in place; else →
   `.planning/milestones/v0.15.0-phases/{SURPRISES-INTAKE,GOOD-TO-HAVES}.md` with severity + sketch.
4. Verify against reality — RUN the gate on this VM's git 2.50.1; a claim without a committed
   transcript/artifact isn't done.
5. North star — Rust-compiler-grade UX (applies to the gate's own stderr/verdict messaging).

<interfaces>
From quality/gates/agent-ux/rebase-recovery-reconciles.sh (current shape):
- import-path forcing at ~157-159: `export GIT_CONFIG_COUNT/KEY_0/VALUE_0` = protocol.version=0
  for ALL git subprocesses. This is what MUST be lifted (or scoped) for the stateless-connect legs.
- Scenario A (peer push drift), Scenario B (external REST PATCH), Scenario C (SoT delete) —
  each runs `git pull --rebase origin main && git push origin main`, checks convergence,
  FATAL_RE / REFLOCK_RE guards, transcript logging via `tlog`.
- STATELESS-CONNECT block at ~478-491: currently only logs a TODO on modern git, a SKIP on old.
- `pass()`/`fail()`/`finish()`; artifact at quality/reports/verifications/agent-ux/rebase-recovery-reconciles.json.
The row `agent-ux/rebase-recovery-reconciles` (agent-ux.json) already carries — from 122-01 —
two SC1 asserts requiring the stateless-connect legs + a deterministic per-scenario verdict.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add modern-git stateless-connect legs (protocol.version unset) with per-scenario adjudication</name>
  <files>quality/gates/agent-ux/rebase-recovery-reconciles.sh</files>
  <action>
Extend the gate so that when `git --version` is ≥ 2.34 it runs the two drift scenarios
(reuse the Scenario A peer-push and Scenario B external-REST helpers) a SECOND time WITHOUT
the `protocol.version=0` forcing (unset GIT_CONFIG_COUNT/KEY_0/VALUE_0 for these legs, or run
them in a subshell that does not export the forcing), so git selects the real
stateless-connect (protocol-v2) READ path. Requirements:
- Keep the existing import-path legs intact (they guard old-git support — do NOT delete the
  forcing for those; the row asserts BOTH paths).
- For each stateless-connect scenario, assert convergence exactly as the import legs do:
  `git pull --rebase origin main && git push origin main` exits 0, the edit reaches the SoT,
  and NO `fatal: error while running fast-import` / `does not contain` (FATAL_RE) and NO
  `cannot lock ref 'refs/reposix/origin/main'` (REFLOCK_RE) on the recovery path.
- Replace the bare TODO at ~485-491 with a real, deterministic adjudication (see Task 2 for
  the divergence branch). Update the stale header comments that claim "only git 2.25.1 is
  installed" — this VM is 2.50.1 now (fix-twice honesty).
- Emit `pass "..."` strings that TOKEN-MATCH the SC1 `expected.asserts` in agent-ux.json
  (F-K4b congruence, `agent-ux/test-name-vs-asserts`): one for "stateless-connect legs run on
  modern git (no bare TODO-skip)" and one for "deterministic per-scenario verdict (converge
  OR labelled known-divergence)".
- Update the transcript (`tlog`) with the git version, whether the forcing was applied per
  leg, and the per-scenario recovery logs.
Run it locally: `bash quality/gates/agent-ux/rebase-recovery-reconciles.sh` on git 2.50.1
(FOREGROUND; the gate builds/uses the workspace binary). Read the transcript to learn the
actual stateless-connect outcome.
  </action>
  <verify>
    <automated>bash quality/gates/agent-ux/rebase-recovery-reconciles.sh; echo "exit=$?"; grep -qi 'stateless-connect' quality/reports/transcripts/rebase-recovery-reconciles-*.txt 2>/dev/null && grep -Eqi 'protocol.version (unset|2)|stateless.connect.*(PASS|converge|DIVERGENCE)' quality/reports/transcripts/rebase-recovery-reconciles-*.txt && echo LEGS_RAN</automated>
  </verify>
  <done>On git ≥2.34 the gate exercises stateless-connect for both scenarios; import legs still pass; transcript records the real per-scenario outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Adjudicate the outcome — converge (PASS) or document+gate+file the divergence</name>
  <files>quality/gates/agent-ux/rebase-recovery-reconciles.sh, quality/catalogs/agent-ux.json, crates/CLAUDE.md</files>
  <action>
Based on the LOCAL run (Task 1), take exactly ONE branch — verify against reality, do not guess:

BRANCH A — stateless-connect CONVERGES (the P105 §5 hypothesis; git owns ref placement via
protocol-v2 + remote.origin.fetch so it likely fast-forwards):
- Finalize the stateless-connect PASS asserts; the gate exits 0 having proven BOTH paths on
  modern git. Confirm the two SC1 `expected.asserts` (122-01) each map to an emitted
  `asserts_passed` string. Add a one-line note to crates/CLAUDE.md (or the gate header) that
  the modern-git stateless-connect read path is now proven, not just the import path.

BRANCH B — stateless-connect ALSO BREAKS (a SECOND, cache-side fix site — per P105 §5 "file
it, do not silently expand this lane"):
- Do NOT attempt the cache-side fix here (out of this lane's charter, and > this phase).
- File a HIGH row to `.planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md` naming the
  cache-side second fix site with the repro (the failing stateless-connect scenario + the
  exact error line) and a fix-sketch.
- Make the gate DOCUMENT the divergence deterministically: a labelled known-divergence guard
  (e.g. the stateless-connect leg records the specific error as an EXPECTED-DIVERGENCE with
  the SURPRISES-INTAKE ref) so the row's "deterministic per-scenario verdict … OR the
  divergence is captured as a labelled, filed known-divergence" assert is satisfied — NOT a
  silent skip, NOT a faked green, NOT a hard-RED of the whole gate (the import path still
  guards old-git and passes). Keep the import legs green; the stateless-connect leg is a
  documented, tracked known-divergence until the cache-side phase.
- Journal the decision in `quality/SURPRISES.md` (one line, per PROTOCOL Step 9).

Either branch: ensure the row's `claim_vs_assertion_audit` (extended in 122-01) still holds,
and that the gate's exit code reflects the real state (0 when the contract is met — including
the documented-divergence contract in Branch B).
  </action>
  <verify>
    <automated>bash quality/gates/agent-ux/rebase-recovery-reconciles.sh; test $? -eq 0 && python3 -c "import json;d=json.load(open('quality/catalogs/agent-ux.json'));r=[x for x in d['rows'] if x['id']=='agent-ux/rebase-recovery-reconciles'][0];print('asserts:',len(r['expected']['asserts']))"</automated>
  </verify>
  <done>The gate exits 0 with the SC1 contract met (convergence proven, or a labelled+filed known-divergence); §5 is resolved in code + (if Branch B) SURPRISES-INTAKE + SURPRISES.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate → live sim (remote bytes) | The gate drives the sim; sim bytes are attacker-influenced (seed data). The gate must not route them into an outbound side-effect outside its throwaway workspace. |
| gate transcript → committed artifact | The transcript records env; a value leak would exfiltrate secrets into git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-05 | Information disclosure | gate transcript env_keys | mitigate | env_keys emits variable NAMES only (`env | cut -d= -f1`) — reuse the existing pattern / transcript.sh helper; no `=value` pairs. |
| T-122-09 | Tampering (false green) | stateless-connect adjudication | mitigate | Deterministic per-scenario verdict; a divergence is a labelled, filed known-divergence (never a silent skip / faked green); import legs stay green to guard old-git. |
| T-122-10 | Repudiation | §5 resolution | accept | Outcome journaled in the transcript + (Branch B) SURPRISES-INTAKE + SURPRISES.md; git log is the audit trail. |
</threat_model>

<verification>
- `bash quality/gates/agent-ux/rebase-recovery-reconciles.sh` exits 0 on git 2.50.1 (and, at
  phase close, on CI ubuntu-latest via the pre-pr cadence).
- The two SC1 `expected.asserts` each map to an emitted `asserts_passed` (F-K4b congruent).
- Stale "git 2.25.1 only" header comments corrected.
- `python3 quality/runners/run.py --cadence pre-push` clean (do NOT hand-pick gates).
- Branch B only: SURPRISES-INTAKE HIGH row + SURPRISES.md line present.
</verification>

<success_criteria>
The rebase-recovery gate exercises the real stateless-connect read path on git ≥2.34 and
adjudicates each drift scenario deterministically (convergence proven, or a labelled+filed
known-divergence), while the legacy import legs still pass — P105 §5 resolved against reality.
</success_criteria>

<output>
After completion, create `.planning/phases/122-remote-init-hardening/122-04-SUMMARY.md`
recording which branch (A/B) the reality run took, and a Noticing section (OD-3 #2).
</output>
