---
phase: 57-quality-gates-skeleton-structure-dimension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/catalogs/README.md
  - quality/catalogs/freshness-invariants.json
  - quality/catalogs/orphan-scripts.json
  - quality/PROTOCOL.md
  - quality/SURPRISES.md
autonomous: true
requirements:
  - QG-01
  - QG-03
  - QG-04
  - QG-05
  - QG-08
  - STRUCT-01
  - BADGE-01
  - SIMPLIFY-01
must_haves:
  truths:
    - "A committed catalog set defines GREEN for P57 BEFORE any runner / verifier code lands."
    - "PROTOCOL.md exists as the single-page autonomous-mode runtime contract every later phase reads at start (≤500 lines)."
    - "SURPRISES.md ownership formally transfers from P56 to P57 (header note + line count ≤200)."
    - "freshness-invariants.json carries 9 rows: 6 migrated freshness invariants + QG-08 scope row + BADGE-01 row + structure/banned-words row (B-4 fix: moved into Wave A so Wave C does not amend the catalog mid-phase). All rows status=NOT-VERIFIED with verifier.script paths pointing at where the verifier WILL ship in Wave C."
    - "QG-08 row's expected.asserts make 'top-level REQUIREMENTS.md / ROADMAP.md scope = active milestone + index ONLY' a mechanical predicate the runner can grade."
  artifacts:
    - path: "quality/catalogs/README.md"
      provides: "The unified catalog schema spec (every row carries id/dimension/cadence/kind/sources/command/expected/verifier/artifact/status/last_verified/freshness_ttl/blast_radius/owner_hint/waiver) + the boundary doc with scripts/catalog.py (different domain — keep separate per SIMPLIFY-03 initial assessment) + the catalog-status legend (PASS / FAIL / PARTIAL / NOT-VERIFIED / WAIVED)."
      contains: "blast_radius"
    - path: "quality/catalogs/freshness-invariants.json"
      provides: "9 catalog rows: freshness/no-version-pinned-filenames, freshness/install-leads-with-pkg-mgr/docs-index, freshness/install-leads-with-pkg-mgr/README, freshness/benchmarks-in-mkdocs-nav, freshness/no-loose-roadmap-or-requirements, freshness/no-orphan-docs, structure/banned-words (SIMPLIFY-01 contract; verifier wrapper ships Wave C), structure/top-level-requirements-roadmap-scope (QG-08), structure/badges-resolve (BADGE-01 P57 stub: list rows only — verifier ships in P60)."
      contains: "structure/top-level-requirements-roadmap-scope"
    - path: "quality/catalogs/orphan-scripts.json"
      provides: "Empty array `[]` placeholder + schema header. Populated as needed when a script genuinely cannot be absorbed."
      contains: '"$schema"'
    - path: "quality/PROTOCOL.md"
      provides: "Autonomous-mode runtime contract. Adapted from .planning/research/v0.12.0-autonomous-execution-protocol.md. Sections: (1) The one principle, (2) Failure modes the protocol protects against, (3) Per-phase protocol Steps 1-9, (4) Waiver protocol with TTL, (5) SURPRISES.md format, (6) Skill-dispatch table, (7) When-stuck rules, (8) Anti-bloat rules per surface, (9) Verifier subagent prompt template, (10) Cross-references."
      contains: "catalog-first"
      max_lines: 500
    - path: "quality/SURPRISES.md"
      provides: "Existing 5 P56 entries preserved verbatim. Header gains a `## Ownership` block: 'P56 seeded; P57 takes ownership 2026-04-27'. ≤200-line cap re-asserted."
      contains: "P57 takes ownership"
      max_lines: 200
  key_links:
    - from: "quality/catalogs/freshness-invariants.json"
      to: "quality/gates/structure/freshness-invariants.py (Wave C)"
      via: "row.verifier.script field path"
      pattern: "quality/gates/structure"
    - from: "quality/catalogs/freshness-invariants.json"
      to: "Wave F verifier subagent"
      via: "row.expected.asserts is the contract the unbiased verifier grades against"
      pattern: "expected"
    - from: "quality/PROTOCOL.md"
      to: "Wave B runner (run.py + verdict.py)"
      via: "PROTOCOL.md §runner-contract documents the runner's inputs/outputs; Wave B implements that contract"
      pattern: "run.py --cadence"
    - from: "quality/PROTOCOL.md"
      to: "Wave F unbiased verifier dispatch"
      via: "PROTOCOL.md §verifier-subagent-prompt-template is the prompt Wave F copies"
      pattern: "ZERO session context"
---

<objective>
Wave A — the **catalog-first commit**. Per `.planning/research/v0.12.0-autonomous-execution-protocol.md` Step 3: every phase's FIRST commit MUST write the catalog rows that define this phase's end-state contract. This wave does NOT write any verifier code — it only declares "here is what GREEN means for P57" so the unbiased verifier subagent in Wave F reads a contract that existed BEFORE the implementation landed.

Three deliverables ship in this commit:

1. **`quality/catalogs/README.md`** — the unified catalog schema spec (D2 / D11 from `v0.12.0-decisions-log.md`), the SIMPLIFY-03 boundary statement (per-FILE planning catalog `scripts/catalog.py` ≠ per-CHECK quality verdict `quality/runners/verdict.py` — keep separate), and the catalog-status legend.

2. **`quality/catalogs/freshness-invariants.json`** — 9 rows: the 6 migrated freshness invariants from `scripts/end-state.py` + the QG-08 row enforcing top-level REQUIREMENTS.md/ROADMAP.md scope + the BADGE-01 P57 stub + the SIMPLIFY-01 `structure/banned-words` row (B-4 fix: lands in Wave A so Wave C never amends the catalog mid-phase). Every row has `status: NOT-VERIFIED`, `verifier.script` pointing at where the verifier WILL ship in Wave C, and concrete `expected.asserts` so the verifier subagent in Wave F can grade against a specific predicate (not narrative).

3. **`quality/PROTOCOL.md`** — the autonomous-mode runtime contract adapted from the research doc. ≤500 lines (anti-bloat per QG-03). Every later phase agent reads this at start of phase per Step 1 of the per-phase protocol. The same file documents the verifier subagent prompt template (QG-06) so Wave F's dispatch is mechanical.

Plus housekeeping: `quality/catalogs/orphan-scripts.json` opens as `[]`; `quality/SURPRISES.md` gains an `## Ownership` block transferring custody from P56 to P57.

Purpose: lock the contract. After this commit, downstream waves (B/C/D/E/F) cannot quietly drift the asserts. If Wave C's verifier code does not satisfy a row's `expected.asserts`, Wave F refuses to grade GREEN.

Output: 5 files committed in a single commit titled `docs(p57): catalog-first commit — QG-01/QG-03/QG-05/QG-08/STRUCT-01 contract + PROTOCOL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.12.0-naming-and-architecture.md
@.planning/research/v0.12.0-autonomous-execution-protocol.md
@.planning/research/v0.12.0-vision-and-mental-model.md
@.planning/research/v0.12.0-decisions-log.md
@.planning/verifications/p56/VERDICT.md
@quality/SURPRISES.md
@scripts/end-state.py
@CLAUDE.md

<interfaces>
The 6 freshness invariants exist today in `scripts/end-state.py` `bootstrap_claims()` between approximately line 144 and line 281. Each is a dict with shape:

```python
{
  "id": "freshness/no-version-pinned-filenames",
  "category": "freshness-invariant",
  "description": "...",
  "verifier": {
    "type": "command-empty-output" | "shell" | "artifact-json-exists",
    "command": "<bash one-liner>",
  },
  "artifact": None | "<path>",
  ...
}
```

The migration to the unified schema (per `.planning/research/v0.12.0-naming-and-architecture.md` § "Catalog schema") maps the fields:

| end-state.py v0 | unified schema | mapping |
|---|---|---|
| `id` | `id` | becomes `<dimension>/<slug>` form (already is) |
| `category` | `dimension` | `freshness-invariant` → `structure` |
| `verifier.type` | `kind` | `shell` / `command-empty-output` → `mechanical`; `artifact-json-exists` → `mechanical` (artifact-existence is still mechanical) |
| `verifier.command` | `verifier.script` + `verifier.args` | command body extracted into `quality/gates/structure/freshness-invariants.py` (Wave C); P57 Wave A row carries `verifier.script: "quality/gates/structure/freshness-invariants.py"` and `verifier.args: ["--row-id", "<id>"]`. The actual command body is preserved in the python file Wave C ships. |
| `description` | `description` | one-line rationale |
| (new) | `cadence` | all 6 freshness rows = `pre-push` |
| (new) | `sources` | one or two `file:line-range` refs to where the invariant is described in CLAUDE.md / docs / etc. |
| (new) | `expected` | `{ asserts: ["..."] }` — concrete shell predicates |
| (new) | `artifact` | `quality/reports/verifications/structure/<slug>.json` |
| (new) | `status` | `NOT-VERIFIED` (Wave A) → updated by runner (Wave C/D) |
| (new) | `last_verified` | `null` (Wave A) → RFC3339 timestamp (Wave C/D) |
| (new) | `freshness_ttl` | `null` for mechanical rows (re-verified every pre-push) |
| (new) | `blast_radius` | per-row P0/P1/P2 — see specifics below |
| (new) | `owner_hint` | one-liner naming the file most likely to need editing on FAIL |
| (new) | `waiver` | `null` (no waivers at Wave A) |

**Catalog file format.** A JSON object with shape:

```jsonc
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "comment": "Quality Gates catalog — structure dimension. Schema spec: quality/catalogs/README.md. Verifier impl: quality/gates/structure/. Reports: quality/reports/verifications/structure/.",
  "dimension": "structure",
  "rows": [ <row1>, <row2>, ... ]
}
```

(Per `v0.12.0-naming-and-architecture.md` § "Per-dimension catalog files" the file IS the dimension's catalog; the wrapper object lets us hang metadata without polluting per-row fields.)

**The 9 rows (Wave A scope; B-4 fix expanded from 8 to 9):**

1. **`structure/no-version-pinned-filenames`** — blast_radius P1 — sources: `CLAUDE.md:Freshness invariants` + `scripts/end-state.py:144-164` — owner_hint: "any new doc/script that hardcodes a version in its filename outside CHANGELOG.md and *-phases/" — expected.asserts: `["find docs scripts -type f | grep -E 'v[0-9]+\\.[0-9]+\\.[0-9]+' | grep -v CHANGELOG returns empty"]`
2. **`structure/install-leads-with-pkg-mgr-docs-index`** — blast_radius P0 — sources: `docs/index.md:hero` + `CLAUDE.md:Freshness invariants` — owner_hint: "docs/index.md hero section" — expected.asserts: `["pkg-mgr command (brew install | cargo binstall | curl|sh | powershell-irm) appears BEFORE any 'git clone https://' or 'cargo build --release' source-compile snippet in docs/index.md"]`
3. **`structure/install-leads-with-pkg-mgr-readme`** — blast_radius P0 — sources: `README.md:hero` — same shape as #2 against `README.md`
4. **`structure/benchmarks-in-mkdocs-nav`** — blast_radius P1 — sources: `docs/benchmarks/*.md` + `mkdocs.yml:nav` — owner_hint: "mkdocs.yml `nav:`" — expected.asserts: `["every docs/benchmarks/*.md filename appears in mkdocs.yml"]`
5. **`structure/no-loose-roadmap-or-requirements`** — blast_radius P1 — sources: `.planning/milestones/` + `CLAUDE.md:GSD planning org is regular` — owner_hint: "any *ROADMAP*.md / *REQUIREMENTS*.md at .planning/milestones/ top-level (must be inside *-phases/)" — expected.asserts: `["find .planning/milestones -maxdepth 2 -name '*ROADMAP*' -o -name '*REQUIREMENTS*' | grep -v phases | grep -v archive returns empty"]`
6. **`structure/no-orphan-docs`** — blast_radius P1 — sources: `mkdocs.yml:nav` — owner_hint: "mkdocs.yml `nav:` or `not_in_nav:` or `redirect_maps:`" — expected.asserts: `["bash scripts/check-docs-site.sh exits 0 (mkdocs --strict + orphan-doc check)"]`
7. **`structure/top-level-requirements-roadmap-scope`** (NEW — QG-08) — blast_radius P1 — sources: `.planning/REQUIREMENTS.md` + `.planning/ROADMAP.md` + `CLAUDE.md:GSD planning org is regular` — owner_hint: ".planning/REQUIREMENTS.md and .planning/ROADMAP.md must contain ONLY the active milestone (currently v0.12.0) + a 'Previously validated' index pointing at .planning/milestones/v0.X.0-phases/{REQUIREMENTS,ROADMAP}.md" — expected.asserts:
    - `"top-level .planning/REQUIREMENTS.md contains exactly one '## v0.12.0 Requirements' section + at most one '## v0.X.X' index/cross-reference paragraph; no '## v0.11.0' / '## v0.10.0' / '## v0.9.0' top-level sections"`
    - `"top-level .planning/ROADMAP.md contains exactly one '## v0.12.0 Quality Gates' phases section + at most one historical-milestones index paragraph; no '## v0.11.0 Polish & Reproducibility (PLANNING)' / '## v0.10.0 Docs & Narrative Shine (PLANNING)' / '## v0.9.0 Architecture Pivot' top-level sections"`
    - **Note:** `.planning/REQUIREMENTS.md` already complies (POLISH2-21 extraction). `.planning/ROADMAP.md` does NOT comply — it still carries v0.11.0/v0.10.0/v0.9.0 sections. Wave D fixes ROADMAP.md per POLISH-STRUCT.
8. **`structure/badges-resolve`** (NEW — BADGE-01 P57 stub) — blast_radius P2 (does not gate milestone) — sources: `README.md:badges` + `docs/index.md:badges` — owner_hint: "any badge URL in README.md or docs/index.md whose HEAD response is not HTTP 200 with content-type matching 'image'" — cadence: `pre-push` AND `weekly` — kind: `asset-exists` — expected.asserts: `["P57 stub: row exists with verifier.script pointing at quality/gates/docs-build/badges-resolve.py (P60 ships the verifier); status: NOT-VERIFIED is acceptable until P60 lands; this row is the contract anchor."]` — waiver: `{until: "2026-07-25T00:00:00Z", reason: "verifier ships in P60 per BADGE-01 traceability; row exists in P57 to anchor the contract", dimension_owner: "v0.12.0 P60 docs-build dimension owner", tracked_in: "BADGE-01 / P60"}`
9. **`structure/banned-words`** (NEW — SIMPLIFY-01 contract; B-4 fix moved this row from Wave C into Wave A) — blast_radius P1 — dimension `structure`, cadence `pre-push`, kind `mechanical` — sources: `["docs/.banned-words.toml", "scripts/banned-words-lint.sh"]` — expected.asserts: `["bash quality/gates/structure/banned-words.sh exits 0 (no banned-word violations in docs/)"]` — verifier.script: `"quality/gates/structure/banned-words.sh"`, args: `[]`, timeout_s: `30` — artifact: `"quality/reports/verifications/structure/banned-words.json"` — owner_hint: `"any docs/ file using banned terms (replace, FUSE, fusermount, kernel, partial-clone, etc. — see docs/.banned-words.toml for the full matrix)"` — waiver: `null`. The verifier wrapper (`quality/gates/structure/banned-words.sh`) ships in Wave C; the row anchors the contract from P57 onward.

**PROTOCOL.md content** is taken near-verbatim from `.planning/research/v0.12.0-autonomous-execution-protocol.md` with the following edits:

- §opening note "Future home" is removed (we ARE the future home now).
- §"Cross-references" updates: bullets that point at `.planning/research/v0.12.0-*.md` files are kept (those are still the source-of-truth research docs); bullet pointing at "future home" is replaced by "this file is `quality/PROTOCOL.md`."
- New §at-the-top: "Reading order for an agent picking up a phase: STATE.md → ROADMAP.md (this phase's entry) → REQUIREMENTS.md (this phase's req IDs) → THIS FILE → quality/SURPRISES.md → previous phase's verdict at quality/reports/verdicts/p<N-1>/<latest>.md."
- §"Anti-bloat rules per surface" gains a row for itself: `quality/PROTOCOL.md ≤500 lines; new rules go here only when they apply to MORE than one dimension; dimension-specific rules go in quality/gates/<dim>/README.md`.
- ≤500 lines total. The research doc is ~244 lines so there is plenty of headroom; do NOT pad.

**SURPRISES.md ownership block** is a new `## Ownership` H2 inserted immediately after the existing intro paragraph (line ~10), before the first `2026-04-27 P56:` entry. Content: `P56 seeded this file (5 entries) at phase close. P57 takes ownership 2026-04-27. Anti-bloat: ≤200 lines. Future surprise entries continue below in `YYYY-MM-DD P<N>: <obstacle> — <resolution>` format.` The 5 existing P56 entries are preserved verbatim. Total file stays ≤200 lines (currently 57; plenty of room).

**`quality/catalogs/README.md`** content sections:

1. **What is a catalog?** — One paragraph: "A catalog is the data layer of a Quality Gates dimension. Each row is one (gate, verifier, expected-outcome) triple. The runner reads catalogs, runs the verifiers, and writes artifacts. The verdict.py script reads artifacts and emits the human-readable verdict."
2. **Unified schema** — The full schema table from `v0.12.0-naming-and-architecture.md` § "Catalog schema" (every field, every required-or-not, every note).
3. **Per-dimension catalog files** — The table from the same research doc § "Per-dimension catalog files".
4. **Status legend** — `PASS` / `FAIL` / `PARTIAL` / `NOT-VERIFIED` / `WAIVED` semantics.
5. **Boundary with `scripts/catalog.py` (SIMPLIFY-03)** — One paragraph: per-FILE planning catalog (which files in this v0.X session are KEEP/TODO/DONE/REVIEW/DELETE/REFACTOR; reads `.planning/v0.11.1-catalog.json` etc.) is a different domain from per-CHECK quality verdict (which gates are GREEN/RED). Initial assessment: keep separate; do NOT fold. SIMPLIFY-03 closes with this README boundary doc + leaving `scripts/catalog.py` in place. The two systems may share a verifier-runner abstraction in v0.13.x if cohesion emerges, but not in v0.12.0.
6. **Cross-references** — pointers to PROTOCOL.md + the v0.12.0 research bundle.

≤200 lines total.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 57-01-A: Write quality/catalogs/README.md (schema spec + SIMPLIFY-03 boundary)</name>
  <files>quality/catalogs/README.md</files>
  <action>
Create `quality/catalogs/README.md` (≤200 lines) with the 6 sections enumerated in `<interfaces>` above. Use the full schema table from `.planning/research/v0.12.0-naming-and-architecture.md` § "Catalog schema" verbatim (the schema is locked per D2 from `v0.12.0-decisions-log.md`; do NOT invent fields or rename).

**Section ordering** (matches the user's natural reading flow):
1. What is a catalog? (1 paragraph)
2. Unified schema (table — every field with required/optional + notes)
3. Per-dimension catalog files (table mapping dimension → file)
4. Status legend (PASS / FAIL / PARTIAL / NOT-VERIFIED / WAIVED)
5. SIMPLIFY-03 boundary statement (per-FILE planning catalog vs per-CHECK quality verdict; keep separate; rationale cites that the two systems answer DIFFERENT questions — `scripts/catalog.py` answers "what files in this session are TODO?", `quality/runners/verdict.py` answers "what gates are GREEN?")
6. Cross-references (PROTOCOL.md + .planning/research/v0.12.0-*.md bundle)

**SIMPLIFY-03 closure language** (Section 5) MUST include verbatim:
- "Initial assessment per SIMPLIFY-03: keep `scripts/catalog.py` separate; do NOT fold."
- "Rationale: per-FILE catalog and per-CHECK catalog answer different questions; conflating them would degrade both."
- "If a verifier-runner cohesion emerges in v0.13.x or later, this boundary may be revisited."

Per the prompt's constraint: SIMPLIFY-03 ships the boundary documentation, NOT a deletion. `scripts/catalog.py` stays in place.

**Anti-bloat header** (top of file): one-line YAML-like comment block:
```
<!-- quality/catalogs/README.md — schema spec for Quality Gates catalogs. -->
<!-- ≤200 lines. New schema fields require a v0.X.Y migration; do not extend ad-hoc. -->
```

Cross-reference pattern at the bottom:
```
## Cross-references

- `quality/PROTOCOL.md` — autonomous-mode runtime contract that consumes these catalogs.
- `.planning/research/v0.12.0-naming-and-architecture.md` — schema design rationale.
- `.planning/research/v0.12.0-vision-and-mental-model.md` — dimension/cadence/kind taxonomy.
- `.planning/research/v0.12.0-decisions-log.md` D2 (gates as umbrella term) and D11 (per-milestone REQUIREMENTS scoping).
```
  </action>
  <verify>
    <automated>test -f quality/catalogs/README.md && lc=$(wc -l &lt; quality/catalogs/README.md) && [ "$lc" -le 200 ] && grep -q "Unified schema" quality/catalogs/README.md && grep -q "SIMPLIFY-03" quality/catalogs/README.md && grep -q "scripts/catalog.py" quality/catalogs/README.md && grep -q "PASS.*FAIL.*PARTIAL.*NOT-VERIFIED.*WAIVED" quality/catalogs/README.md</automated>
  </verify>
  <done>
File exists, ≤200 lines, contains all 6 sections, SIMPLIFY-03 boundary statement uses the verbatim phrases above, every catalog-schema field from `v0.12.0-naming-and-architecture.md` § "Catalog schema" is present in the schema table.
  </done>
</task>

<task type="auto">
  <name>Task 57-01-B: Write quality/catalogs/freshness-invariants.json (8 rows)</name>
  <files>quality/catalogs/freshness-invariants.json</files>
  <action>
Create `quality/catalogs/freshness-invariants.json` with the wrapper-object format documented in `<interfaces>`:

```jsonc
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "comment": "Quality Gates catalog — structure dimension. Schema spec: quality/catalogs/README.md. Verifier impl: quality/gates/structure/. Reports: quality/reports/verifications/structure/.",
  "dimension": "structure",
  "rows": [ /* 8 rows */ ]
}
```

**Each of the 8 rows** has the exact field list from `v0.12.0-naming-and-architecture.md` § "Catalog schema":
`id`, `dimension`, `cadence`, `kind`, `sources`, `command` (null for non-docs-repro/install rows), `expected`, `verifier` (with `script`, `args`, `timeout_s`, `container`), `artifact`, `status`, `last_verified`, `freshness_ttl`, `blast_radius`, `owner_hint`, `waiver`.

**The 9 rows in order** (B-4 fix added row 9; use the exact ids from `<interfaces>` above):

Row 1 — `structure/no-version-pinned-filenames`:
- dimension: `"structure"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `["CLAUDE.md:Freshness invariants", "scripts/end-state.py:144-164"]`
- command: `null`, expected.asserts: `["find docs scripts -type f | grep -E 'v[0-9]+\\.[0-9]+\\.[0-9]+' | grep -v CHANGELOG returns empty stdout"]`
- verifier: `{ "script": "quality/gates/structure/freshness-invariants.py", "args": ["--row-id", "structure/no-version-pinned-filenames"], "timeout_s": 30, "container": null }`
- artifact: `"quality/reports/verifications/structure/no-version-pinned-filenames.json"`
- status: `"NOT-VERIFIED"`, last_verified: `null`, freshness_ttl: `null`
- blast_radius: `"P1"`, owner_hint: `"any new doc/script that hardcodes a version in its filename outside CHANGELOG.md and *-phases/"`, waiver: `null`

Row 2 — `structure/install-leads-with-pkg-mgr-docs-index`:
- dimension/cadence/kind same as Row 1
- sources: `["docs/index.md:hero", "CLAUDE.md:Freshness invariants"]`
- expected.asserts: `["pkg-mgr command (brew install | cargo binstall | curl|sh | powershell-irm) appears BEFORE any 'git clone https://' or 'cargo build --release' source-compile snippet in docs/index.md"]`
- verifier.script same; args = `["--row-id", "structure/install-leads-with-pkg-mgr-docs-index"]`
- artifact: `"quality/reports/verifications/structure/install-leads-with-pkg-mgr-docs-index.json"`
- blast_radius: `"P0"`, owner_hint: `"docs/index.md hero section — install path order"`

Row 3 — `structure/install-leads-with-pkg-mgr-readme`: same as Row 2 against `README.md`. blast_radius `"P0"`.

Row 4 — `structure/benchmarks-in-mkdocs-nav`:
- sources: `["docs/benchmarks/", "mkdocs.yml:nav"]`
- expected.asserts: `["every docs/benchmarks/*.md filename appears in mkdocs.yml"]`
- blast_radius: `"P1"`, owner_hint: `"mkdocs.yml `nav:` block — benchmarks subsection"`

Row 5 — `structure/no-loose-roadmap-or-requirements`:
- sources: `[".planning/milestones/", "CLAUDE.md:GSD planning org is regular"]`
- expected.asserts: `["find .planning/milestones -maxdepth 2 ( -name '*ROADMAP*' -o -name '*REQUIREMENTS*' ) | grep -v phases | grep -v archive returns empty stdout"]`
- blast_radius: `"P1"`, owner_hint: `"any *ROADMAP*.md / *REQUIREMENTS*.md at .planning/milestones/ top-level (must be inside *-phases/)"`

Row 6 — `structure/no-orphan-docs`:
- sources: `["mkdocs.yml:nav"]`
- expected.asserts: `["bash scripts/check-docs-site.sh exits 0 (mkdocs --strict + orphan-doc check)"]`
- verifier.timeout_s: `60` (mkdocs build is slower)
- blast_radius: `"P1"`, owner_hint: `"mkdocs.yml `nav:` or `not_in_nav:` or `redirect_maps:`"`

Row 7 — `structure/top-level-requirements-roadmap-scope` (QG-08):
- dimension: `"structure"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `[".planning/REQUIREMENTS.md", ".planning/ROADMAP.md", "CLAUDE.md:GSD planning org is regular"]`
- command: `null`
- expected.asserts:
  - `"top-level .planning/REQUIREMENTS.md contains exactly one '## v0.12.0 Requirements' section; the file does NOT contain '## v0.11.0' / '## v0.10.0' / '## v0.9.0' / '## v0.8.0' as top-level H2 sections (these belong inside .planning/milestones/v0.X.0-phases/REQUIREMENTS.md)"`
  - `"top-level .planning/ROADMAP.md contains exactly one '## v0.12.0 Quality Gates (PLANNING)' section; the file does NOT contain '## v0.11.0 Polish & Reproducibility (PLANNING)' / '## v0.10.0 Docs & Narrative Shine (PLANNING)' / '## v0.9.0 Architecture Pivot' as top-level H2 sections (these belong inside .planning/milestones/v0.X.0-phases/ROADMAP.md)"`
  - `"the historical milestones index paragraph (one paragraph at the top with relative links to .planning/milestones/v0.X.0-phases/{REQUIREMENTS,ROADMAP}.md) is permitted in both files"`
- verifier.script: `"quality/gates/structure/freshness-invariants.py"`, args: `["--row-id", "structure/top-level-requirements-roadmap-scope"]`, timeout_s: `15`
- artifact: `"quality/reports/verifications/structure/top-level-requirements-roadmap-scope.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P1"`
- owner_hint: `"if RED: move historical sections from .planning/{REQUIREMENTS,ROADMAP}.md to .planning/milestones/v0.X.0-phases/{REQUIREMENTS,ROADMAP}.md per POLISH-STRUCT (P57 Wave D)"`
- waiver: `null` — this row IS the QG-08 contract; waivers would defeat its purpose.

Row 8 — `structure/badges-resolve` (BADGE-01 P57 stub):
- dimension: `"structure"`, cadence: `"pre-push"`, kind: `"asset-exists"`
- sources: `["README.md:badges", "docs/index.md:badges"]`
- command: `null`
- expected.asserts: `["P57 stub: row exists with verifier.script pointing at quality/gates/docs-build/badges-resolve.py (P60 ships the verifier per BADGE-01 traceability); the row anchors the contract from P57 onward; status: NOT-VERIFIED is acceptable until P60 ships the verifier."]`
- verifier.script: `"quality/gates/docs-build/badges-resolve.py"`, args: `["--row-id", "structure/badges-resolve"]`, timeout_s: `30`
- artifact: `"quality/reports/verifications/structure/badges-resolve.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P2"` (does not gate milestone close)
- owner_hint: `"P60 docs-build dimension ships the verifier; for P57 the row's existence is the contract"`
- waiver: `{ "until": "2026-07-25T00:00:00Z", "reason": "verifier ships in P60 per BADGE-01 traceability; P57 anchors the row in the catalog so the gate exists from skeleton onward", "dimension_owner": "v0.12.0 P60 docs-build dimension owner", "tracked_in": "BADGE-01 / P60" }`

Row 9 — `structure/banned-words` (SIMPLIFY-01 contract; B-4 fix moved this row into Wave A):
- dimension: `"structure"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `["docs/.banned-words.toml", "scripts/banned-words-lint.sh"]`
- command: `null`
- expected.asserts: `["bash quality/gates/structure/banned-words.sh exits 0 (no banned-word violations in docs/)"]`
- verifier.script: `"quality/gates/structure/banned-words.sh"`, args: `[]`, timeout_s: `30`, container: `null`
- artifact: `"quality/reports/verifications/structure/banned-words.json"`
- status: `"NOT-VERIFIED"`, last_verified: `null`, freshness_ttl: `null`
- blast_radius: `"P1"`, owner_hint: `"any docs/ file using banned terms (replace, FUSE, fusermount, kernel, partial-clone, etc. — see docs/.banned-words.toml for the full matrix)"`, waiver: `null`

The verifier wrapper at `quality/gates/structure/banned-words.sh` ships in Wave C (57-03 Task C). Wave A only ships the row.

**Validate the JSON immediately after write.** Run `python3 -c "import json; d=json.load(open('quality/catalogs/freshness-invariants.json')); assert d['dimension']=='structure'; assert len(d['rows'])==9; ids={r['id'] for r in d['rows']}; assert 'structure/top-level-requirements-roadmap-scope' in ids; assert 'structure/badges-resolve' in ids; assert 'structure/banned-words' in ids; print('catalog OK', len(d['rows']), 'rows')"`. If JSON is malformed, fix immediately — Wave B's runner reads this file and a parse failure breaks the contract.

**Field-ordering convention.** Within each row, order fields as the `<interfaces>` field-mapping table lists (`id` first, then `dimension`, `cadence`, `kind`, `sources`, `command`, `expected`, `verifier`, `artifact`, `status`, `last_verified`, `freshness_ttl`, `blast_radius`, `owner_hint`, `waiver`). Pretty-print with 2-space indent.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('quality/catalogs/freshness-invariants.json')); assert d['dimension']=='structure'; assert len(d['rows'])==9, f'expected 9 rows after B-4 fix, got {len(d[chr(34)+chr(114)+chr(111)+chr(119)+chr(115)+chr(34)])}'; ids={r['id'] for r in d['rows']}; need={'structure/no-version-pinned-filenames','structure/install-leads-with-pkg-mgr-docs-index','structure/install-leads-with-pkg-mgr-readme','structure/benchmarks-in-mkdocs-nav','structure/no-loose-roadmap-or-requirements','structure/no-orphan-docs','structure/top-level-requirements-roadmap-scope','structure/badges-resolve','structure/banned-words'}; assert ids==need, f'expected {sorted(need)}, got {sorted(ids)}'; assert all(r['status']=='NOT-VERIFIED' for r in d['rows']); assert all(r['cadence']=='pre-push' for r in d['rows']); badge_row=next(r for r in d['rows'] if r['id']=='structure/badges-resolve'); assert badge_row['waiver'] is not None and badge_row['waiver']['tracked_in']=='BADGE-01 / P60'; banned_row=next(r for r in d['rows'] if r['id']=='structure/banned-words'); assert banned_row['waiver'] is None and banned_row['blast_radius']=='P1'; print('catalog OK', len(d['rows']))"</automated>
  </verify>
  <done>
JSON parses; wrapper has dimension="structure"; **9 rows** present with the correct ids (B-4 fix expanded from 8 to 9); every row has status=NOT-VERIFIED; QG-08 row has 3 expected.asserts naming v0.11.0/v0.10.0/v0.9.0 historical sections; BADGE-01 row has waiver pointing at P60; structure/banned-words row has blast_radius=P1, waiver=null, verifier.script wired to quality/gates/structure/banned-words.sh.
  </done>
</task>

<task type="auto">
  <name>Task 57-01-C: Write quality/catalogs/orphan-scripts.json (empty placeholder)</name>
  <files>quality/catalogs/orphan-scripts.json</files>
  <action>
Create `quality/catalogs/orphan-scripts.json` as a near-placeholder with one temporary waiver row per the W-2 plan-check fix:

```jsonc
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "comment": "Quality Gates catalog — orphan-scripts waivers. Each row documents a script that genuinely cannot be absorbed into a quality/gates/<dim>/ home and explains why. SIMPLIFY-12 (P63) audits this file at milestone close. Ideally empty.",
  "dimension": "meta",
  "rows": [
    {
      "id": "release/crates-io-max-version",
      "dimension": "release",
      "cadence": "pre-push",
      "kind": "mechanical",
      "sources": ["scripts/end-state.py:bootstrap_claims (crates-io-max-version)"],
      "command": null,
      "expected": { "asserts": ["covered by scripts/end-state.py during the parallel-migration window; will be migrated to quality/gates/release/ in P58"] },
      "verifier": { "script": "scripts/end-state.py", "args": ["verdict"], "timeout_s": 60, "container": null },
      "artifact": null,
      "status": "WAIVED",
      "last_verified": null,
      "freshness_ttl": null,
      "blast_radius": "P1",
      "owner_hint": "P58 SIMPLIFY-04/05 ships the migrated check at quality/gates/release/crates-io-max-version.py; until then this row documents the temporary coverage gap",
      "waiver": {
        "until": "2026-05-15T00:00:00Z",
        "reason": "W-2 plan-check fix: parallel-migration parity is documentary, not behavioral; the crates.io max-version check is currently in scripts/end-state.py and will move to release-dim in P58 (SIMPLIFY-04/05). Documents the temporary coverage gap so the verifier subagent knows the regression class is still gated.",
        "dimension_owner": "v0.12.0 P58 release dimension owner",
        "tracked_in": "SIMPLIFY-04/05 P58"
      }
    }
  ]
}
```

**Critical:** the single waiver row above is the W-2 documented coverage gap. P57 has no other orphan scripts to declare. If P58/P59/P60 needs to orphan a script, those phases append rows here per SIMPLIFY-12 P63 audit. The waiver row schema mirrors the unified catalog row but populates `waiver` with the orphan rationale + TTL.

This file's existence proves the catalog directory is in place; the single seeded row documents the parallel-migration coverage gap so the Wave F verifier subagent grades it as known/expected, not a surprise.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('quality/catalogs/orphan-scripts.json')); assert d['dimension']=='meta'; assert len(d['rows'])==1; r=d['rows'][0]; assert r['id']=='release/crates-io-max-version'; assert r['status']=='WAIVED'; assert r['waiver']['tracked_in']=='SIMPLIFY-04/05 P58'; assert r['waiver']['until']=='2026-05-15T00:00:00Z'; print('orphan-scripts W-2 waiver OK')"</automated>
  </verify>
  <done>
File exists, parses as JSON, has dimension=meta, has 1 W-2 waiver row (`release/crates-io-max-version` until 2026-05-15, tracked_in `SIMPLIFY-04/05 P58`).
  </done>
</task>

<task type="auto">
  <name>Task 57-01-D: Write quality/PROTOCOL.md (autonomous-mode runtime contract)</name>
  <files>quality/PROTOCOL.md</files>
  <action>
Create `quality/PROTOCOL.md` (≤500 lines) by adapting `.planning/research/v0.12.0-autonomous-execution-protocol.md`. Use the research doc as the source-of-truth content; apply ONLY the edits enumerated in the `<interfaces>` block:

1. Strip the `> **Future home.**` opening admonition.
2. Insert §at-the-top "Reading order" block immediately after the §"The one principle that everything else follows from" section: a numbered list naming `STATE.md → ROADMAP.md (this phase) → REQUIREMENTS.md (this phase's req IDs) → THIS FILE → quality/SURPRISES.md → previous phase's verdict at quality/reports/verdicts/p<N-1>/<latest>.md`.
3. §"Anti-bloat rules per surface" — the existing table already references PROTOCOL.md but as the "future" file; update to present-tense and confirm the ≤500-line cap on this file itself.
4. §"Cross-references" — keep the `.planning/research/v0.12.0-*.md` bullets; remove any "future home" reference; note that this file (PROTOCOL.md) is the runtime contract, the research bundle is the design rationale.

**Verbatim sections that MUST appear** (cite by section header):

- "## The one principle that everything else follows from"
- "## Failure modes the protocol protects against" (with the 7-row table: Drift / Half-done / Silent downgrade / Lost context / Bloat / Cascading break / Ungrounded next agent)
- "## Per-phase protocol (the agent reads this at start of EVERY phase)" with §§ Step 1 — Read context, Step 2 — Verify precondition, Step 3 — Catalog-first commit, Step 4 — Implementation commits, Step 5 — CLAUDE.md update (MANDATORY), Step 6 — Run the verifier, Step 7 — Dispatch unbiased verifier subagent, Step 8 — Update STATE.md + commit, Step 9 — If anything pivoted, append to SURPRISES.md
- "## Waiver protocol" (with the 4-rule list: waivers expire / `until` ≤ 90 days / `reason` must be specific / `tracked_in` should point at a v0.X.Y carry-forward)
- "## SURPRISES.md format" (with the YYYY-MM-DD P<N>: pattern + 2 examples)
- "## Skill dispatch — when to spawn subagents" (the 8-row table)
- "## When stuck rules" (5-step procedure)
- "## Anti-bloat rules per surface" (table — gains a self-row for PROTOCOL.md ≤500 lines)
- "## Verifier subagent prompt template" (verbatim copy-paste-ready prompt)
- "## Cross-references"

**The Verifier subagent prompt template MUST appear unchanged** because Wave F copies it verbatim — any edit risks Wave F's verifier behaving differently from what every later phase expects.

**Line budget self-check.** After writing, run `wc -l quality/PROTOCOL.md`. If >500, abort and trim non-load-bearing prose (NOT the verbatim sections). Target: 350-450 lines (research doc is ~244 lines so PROTOCOL.md sits ~250-300 after the edits + reading-order block).
  </action>
  <verify>
    <automated>test -f quality/PROTOCOL.md && lc=$(wc -l &lt; quality/PROTOCOL.md) && [ "$lc" -le 500 ] && grep -q "The one principle that everything else follows from" quality/PROTOCOL.md && grep -q "Catalog-first commit" quality/PROTOCOL.md && grep -q "Reading order" quality/PROTOCOL.md && grep -q "Verifier subagent prompt template" quality/PROTOCOL.md && grep -q "ZERO session context\|ZERO context from this session" quality/PROTOCOL.md && grep -q "Waiver protocol" quality/PROTOCOL.md && grep -q "Anti-bloat rules per surface" quality/PROTOCOL.md && echo "PROTOCOL.md OK lines=$lc"</automated>
  </verify>
  <done>
File exists, ≤500 lines, contains every section listed above, the Verifier subagent prompt template is verbatim with "ZERO session context" or "ZERO context from this session", "Reading order" block names STATE.md/ROADMAP.md/REQUIREMENTS.md/PROTOCOL.md/SURPRISES.md/previous verdict in order, anti-bloat table has a row for PROTOCOL.md ≤500 lines.
  </done>
</task>

<task type="auto">
  <name>Task 57-01-E: Update quality/SURPRISES.md ownership block (P56 → P57 handoff)</name>
  <files>quality/SURPRISES.md</files>
  <action>
Edit `quality/SURPRISES.md` (currently 57 lines per the prompt's `<13>` reference; verified). Insert a new `## Ownership` H2 block immediately after the existing intro paragraph (which ends `…P57 takes ownership when the framework skeleton ships.`) and BEFORE the first `2026-04-27 P56:` entry.

**Block content (verbatim — preserves the existing P56 entries below):**

```markdown
## Ownership

P56 seeded this file at phase close (5 entries; commit `87cd1c3`). **P57 takes ownership 2026-04-27** as part of the Quality Gates skeleton landing. From P57 onward, this file is referenced by `quality/PROTOCOL.md` § "SURPRISES.md format" as the canonical pivot journal.

Anti-bloat: ≤200 lines (currently {N}). When the file crosses 200 lines, archive the oldest 50 entries to `quality/SURPRISES-archive-YYYY-QN.md` and start fresh — see `quality/PROTOCOL.md` § "Anti-bloat rules per surface" for the rotation rule.

Format: `YYYY-MM-DD P<N>: <obstacle> — <one-line resolution>`. **Required reading for every phase agent at start of phase.** The next agent does NOT repeat investigations of things already journaled here.
```

Replace `{N}` with the file's actual line count after the edit (use `wc -l` on the edited file). The 5 existing P56 entries (dated 2026-04-27) MUST be preserved verbatim — do NOT renumber, summarize, or compress them.

**Cross-check:** After the edit, the file's structure should be:
1. Title `# quality/SURPRISES.md — append-only pivot journal`
2. Intro paragraph (existing, ends "P57 takes ownership when the framework skeleton ships.")
3. NEW `## Ownership` block (above)
4. `---` separator
5. 5 P56 entries verbatim

**Anti-bloat verify.** `wc -l quality/SURPRISES.md` after edit MUST be ≤200. Currently 57; expect ~70-75 after this edit.
  </action>
  <verify>
    <automated>test -f quality/SURPRISES.md && lc=$(wc -l &lt; quality/SURPRISES.md) && [ "$lc" -le 200 ] && grep -q "## Ownership" quality/SURPRISES.md && grep -q "P57 takes ownership 2026-04-27" quality/SURPRISES.md && [ "$(grep -c '^2026-04-27 P56:' quality/SURPRISES.md)" -eq 5 ] && echo "SURPRISES.md OK lines=$lc P56-entries=5"</automated>
  </verify>
  <done>
File ≤200 lines, has new `## Ownership` H2 block, names "P57 takes ownership 2026-04-27" verbatim, all 5 original P56 entries preserved (count == 5).
  </done>
</task>

<task type="auto">
  <name>Task 57-01-F: Catalog-first commit (single commit, no implementation yet)</name>
  <files>quality/catalogs/README.md, quality/catalogs/freshness-invariants.json, quality/catalogs/orphan-scripts.json, quality/PROTOCOL.md, quality/SURPRISES.md</files>
  <action>
Stage and commit ONLY these 5 files (no `quality/runners/`, `quality/gates/`, `scripts/end-state.py`, or pre-push-hook edits — those are Wave B / C / D / F).

**Commit message** (HEREDOC):

```
docs(p57): catalog-first commit — QG-01 / QG-03 / QG-05 / QG-08 / STRUCT-01 / SIMPLIFY-01 contract + PROTOCOL.md

Lands the GREEN-criterion contract for P57 BEFORE any runner / verifier
code ships. Per .planning/research/v0.12.0-autonomous-execution-protocol.md
Step 3: every phase's first commit MUST write the catalog rows that
define this phase's end-state contract, so the unbiased verifier subagent
in Wave F reads a contract that existed BEFORE the implementation.

Files:
- quality/catalogs/README.md (NEW) — unified catalog schema spec +
  SIMPLIFY-03 boundary (per-FILE catalog scripts/catalog.py vs
  per-CHECK quality verdict — keep separate; rationale documented).
- quality/catalogs/freshness-invariants.json (NEW) — 9 rows
  (B-4 fix: SIMPLIFY-01 row moved into Wave A so Wave C does NOT
  amend the catalog mid-phase):
  6 migrated freshness invariants (no-version-pinned-filenames,
  install-leads-with-pkg-mgr-{docs-index,readme},
  benchmarks-in-mkdocs-nav, no-loose-roadmap-or-requirements,
  no-orphan-docs) + structure/top-level-requirements-roadmap-scope
  (QG-08 — enforces top-level REQUIREMENTS.md/ROADMAP.md scope =
  active milestone + index ONLY) + structure/badges-resolve
  (BADGE-01 P57 stub — verifier ships in P60) +
  structure/banned-words (SIMPLIFY-01 contract; verifier wrapper
  ships Wave C). All rows status=NOT-VERIFIED, verifier.script
  paths point at where the Wave C python module / shell wrapper
  land.
- quality/catalogs/orphan-scripts.json (NEW) — seeded with one
  temporary waiver row (W-2 plan-check fix): release/crates-io-
  max-version (until 2026-05-15, tracked_in SIMPLIFY-04/05 P58) so
  the parallel-migration coverage gap is documented before Wave B's
  runner ever sees the catalog. Future orphan rows append here per
  SIMPLIFY-12 P63 audit.
- quality/PROTOCOL.md (NEW) — single-page autonomous-mode runtime
  contract adapted from .planning/research/v0.12.0-autonomous-
  execution-protocol.md. ≤500 lines. Documents: catalog-first rule,
  9-step per-phase protocol, waiver protocol with TTL, SURPRISES.md
  format, skill-dispatch table, anti-bloat rules per surface,
  verifier subagent prompt template (Wave F copies verbatim).
- quality/SURPRISES.md — adds `## Ownership` block transferring
  custody from P56 to P57. 5 P56 entries preserved verbatim.

NO runner code, NO verifier code, NO pre-push hook edit, NO
scripts/end-state.py shrink in this commit — those are Wave B/C/D/F
per .planning/phases/57-quality-gates-skeleton-structure-dimension/
57-{02,03,04,06}-PLAN.md.

The verifier subagent (Wave F, PLAN 57-06) reads this catalog as the
contract; the runner + verifier impl have until Wave C close to land
or the contract becomes stale DRAFT.

Refs: QG-01, QG-03, QG-04, QG-05, QG-08, STRUCT-01, BADGE-01, SIMPLIFY-01, SIMPLIFY-03 + W-2 (orphan-scripts.json waiver row for SIMPLIFY-04/05 P58)
```

**Critical guardrails:**
- Do NOT include any `quality/runners/`, `quality/gates/`, `scripts/end-state.py`, or `scripts/hooks/pre-push` edits in this commit. If the working tree has any other staged changes, abort and ask the orchestrator (something is wrong with wave isolation).
- Pre-push hook will trigger (existing chain: credential scan + `scripts/end-state.py verdict` warning + `scripts/check-docs-site.sh` IF docs/ touched + cargo fmt + cargo clippy). The new files are under `quality/` not `docs/`, so docs-site check skips. Cargo fmt + clippy still run; expect GREEN (no rust changes).
- If `scripts/end-state.py verdict` warning fires (non-blocking — current chain prints `! SESSION-END-STATE verdict is RED (non-fatal warning)` if any claim is RED), proceed regardless. The WARNING does not block the push; the FATAL gate is fmt + clippy + docs-site IF docs/ touched.
- `scripts/banned-words-lint.sh` is NOT yet wired into pre-push (per current pre-push body). It will be after Wave C (SIMPLIFY-01). For Wave A, manually verify clean: `bash scripts/banned-words-lint.sh` exits 0 against the new files. The banned word `replace` in PROTOCOL.md is fine if used inside the verbatim research-doc text — but inspect `quality/PROTOCOL.md` for any new occurrences. If found, rewrite to `migrate to` / `for the 80%` / `rewrite as` / `complement` / `alongside` per CLAUDE.md.

After commit lands, run `git log -1 --name-only --pretty=format:` and verify the diff lists exactly these 5 files.
  </action>
  <verify>
    <automated>git log -1 --name-only --pretty=format: | sort | tr -s '\n' | grep -E 'quality/catalogs/README\.md|quality/catalogs/freshness-invariants\.json|quality/catalogs/orphan-scripts\.json|quality/PROTOCOL\.md|quality/SURPRISES\.md' | wc -l | awk '$1==5 {exit 0} {exit 1}' &amp;&amp; bash scripts/banned-words-lint.sh</automated>
  </verify>
  <done>
HEAD commit changes exactly 5 files (the 5 listed above); commit message body cites QG-01 / QG-03 / QG-04 / QG-05 / QG-08 / STRUCT-01 / BADGE-01 / SIMPLIFY-03; pre-push hook GREEN; `bash scripts/banned-words-lint.sh` exits 0; no `quality/runners/`, `quality/gates/`, `scripts/end-state.py`, or pre-push hook in HEAD's tree.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → committed JSON | Hand-edit of catalog files; risk of malformed JSON breaking Wave B's runner. |
| catalog ↔ verifier subagent (Wave F) | Subagent reads JSON; vague/missing asserts let agent declare GREEN on partial work. |
| PROTOCOL.md ↔ every later phase agent | If PROTOCOL.md is wrong/incomplete, every P58–P63 agent reads bad guidance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-01 | Tampering | `quality/catalogs/freshness-invariants.json` | mitigate | Task 57-01-B verify-block runs `json.load()` immediately after edit; commit hook also catches malformed JSON via `scripts/end-state.py verdict` warning. |
| T-57-01-02 | Repudiation | catalog row provenance | mitigate | Per-row `sources` field cites file:line refs; survives P58/later migration since the field is in the unified schema. |
| T-57-01-03 | Information disclosure | none — catalogs + PROTOCOL.md are public-by-design | accept | No PII or secrets; everything is design state. |
| T-57-01-04 | Denial of service | breaks Wave B runner / Wave F verifier dispatch | mitigate | Task 57-01-B + 57-01-D verify-blocks guarantee parseable JSON + ≤500-line PROTOCOL.md before commit pushes. |
| T-57-01-05 | Spoofing | n/a — no auth boundary in this wave | accept | All work is local-edit + commit; no remote API calls in Wave A. |
| T-57-01-06 | Elevation of privilege | n/a | accept | Pure file-edit work; no script execution surfaces. |
| T-57-01-07 | Tampering | PROTOCOL.md drift from research-doc verbatim sections | mitigate | Task 57-01-D verify-block greps for the load-bearing strings ("ZERO session context", "Catalog-first commit", "Waiver protocol"); the verbatim Verifier subagent prompt template is the contract Wave F copies. |
</threat_model>

<verification>
After this plan ships:

1. `git log -1 --name-only --pretty=format:` lists exactly 5 files: `quality/catalogs/README.md`, `quality/catalogs/freshness-invariants.json`, `quality/catalogs/orphan-scripts.json`, `quality/PROTOCOL.md`, `quality/SURPRISES.md`.
2. `python3 -c "import json; d=json.load(open('quality/catalogs/freshness-invariants.json')); assert len(d['rows'])==9"` exits 0 (B-4 fix: 9 rows; SIMPLIFY-01 row moved to Wave A).
3. `python3 -c "import json; d=json.load(open('quality/catalogs/orphan-scripts.json')); assert len(d['rows'])==1 and d['rows'][0]['id']=='release/crates-io-max-version'"` exits 0 (W-2 waiver row present).
4. `wc -l quality/PROTOCOL.md` returns ≤500.
5. `wc -l quality/SURPRISES.md` returns ≤200.
6. `wc -l quality/catalogs/README.md` returns ≤200.
7. `grep -c 'structure/top-level-requirements-roadmap-scope' quality/catalogs/freshness-invariants.json` returns ≥1 (QG-08 row present).
8. `grep -c 'BADGE-01 / P60' quality/catalogs/freshness-invariants.json` returns 1 (BADGE-01 row's waiver tracked_in).
9. `bash scripts/banned-words-lint.sh` exits 0 against the new files (no banned-word leakage from research-doc adaptation).
10. Pre-push hook GREEN (cargo fmt + clippy clean — no rust changes; docs-site check skipped — no docs/ changes).
</verification>

<success_criteria>
The catalog-first commit lands. Three downstream consumers can now do their work:

- **Wave B (PLAN 57-02)** reads `quality/PROTOCOL.md` § "runner contract" + the catalog rows' `verifier.script` field paths to know what file paths the runner expects to invoke.
- **Wave C (PLAN 57-03)** reads `expected.asserts` per row to know what each verifier function must check; the python module at `quality/gates/structure/freshness-invariants.py` ships exactly those checks.
- **Wave D (PLAN 57-04)** reads the `structure/top-level-requirements-roadmap-scope` row's expected.asserts to know what POLISH-STRUCT must fix; the QG-08 row IS the contract for which historical sections move out of `.planning/ROADMAP.md`.
- **Wave F (PLAN 57-06)** reads PROTOCOL.md § "Verifier subagent prompt template" verbatim to dispatch the unbiased verifier subagent for phase close.

If Wave A is skipped or partial (e.g., catalog has fewer than 8 rows, or PROTOCOL.md is missing the verifier-prompt template), every downstream wave operates without an anchor, and the verifier subagent at phase close will refuse to grade GREEN. The recurring success criteria #1 (catalog-first) is non-negotiable for v0.12.0.

**Recurring criteria for this phase (per REQUIREMENTS.md lines ~194-199):**
1. **Catalog-first commit** — Wave A IS this rule; this PLAN's commit IS the FIRST P57 commit; subsequent plan commits cite the row id (`Refs: structure/no-version-pinned-filenames`).
2. **CLAUDE.md update in same PR (QG-07)** — Wave F PLAN 57-06 lands the CLAUDE.md update; this Wave's scope is catalog only (catalog-first means catalog FIRST, not catalog only — CLAUDE.md update lands at phase close in Wave F per `.planning/research/v0.12.0-autonomous-execution-protocol.md` Step 5).
3. **Verifier-subagent dispatch on phase close (QG-06)** — Wave F dispatches; verdict at `quality/reports/verdicts/p57/<ts>.md`.
4. **SIMPLIFY absorption** — Wave A SHIPS SIMPLIFY-03 closure (boundary doc in catalogs/README.md); SIMPLIFY-01 (banned-words migration) lands in Wave C; SIMPLIFY-02 (end-state.py shrink) lands in Wave C.
5. **Fix every RED row the dimension's gates flag** — Wave A does NOT yet have a runner to surface RED rows; the runner ships Wave B; the first RED scan happens in Wave C; POLISH-STRUCT fixes ship in Wave D. Wave A's contribution is laying down the contract that Wave D fixes against.
</success_criteria>

<pivot_rules>
Per `.planning/research/v0.12.0-roadmap-and-rationale.md` § "Phase 57 — Quality Gates skeleton" pivot rule:

> If pre-push perf regresses (the runner discovery + composition adds >10s vs the old script chain), keep `scripts/end-state.py` as the pre-push entry, run new system in parallel for parity-checking, hard-cut in P63. **Do not silently downgrade** — file a waiver row + SURPRISES journal entry.

Wave A does NOT touch the runner or pre-push hook, so this pivot does not apply yet. **Wave A pivot rules:**

- **If `quality/PROTOCOL.md` exceeds 500 lines** after applying the research-doc edits: trim non-load-bearing prose; do NOT touch the verbatim Verifier subagent prompt template. If still >500, append a `2026-04-27 P57: PROTOCOL.md hit 500-line ceiling — moved <section> to quality/gates/<dim>/README.md` entry to `quality/SURPRISES.md` and split.
- **If the QG-08 row's expected.asserts are too vague** (i.e., a future verifier can't write a deterministic predicate): tighten by naming the exact regex / grep pattern. Do not ship vague asserts — the Wave F verifier subagent refuses to grade vague asserts GREEN.
- **If JSON validation fails on `quality/catalogs/freshness-invariants.json`**: fix immediately (fields missing commas, unbalanced quotes). Do NOT commit malformed JSON; Wave B's runner depends on a parseable file.
- **If `bash scripts/banned-words-lint.sh` flags PROTOCOL.md** for a banned word imported from the research doc: rewrite the offending sentence using `migrate to` / `for the 80%` / `rewrite as` / `complement` / `alongside`. Append SURPRISES.md entry naming the offending sentence.

Append any pivot to `quality/SURPRISES.md` per PROTOCOL.md Step 9. Format: `YYYY-MM-DD P57: <obstacle> — <resolution>`.
</pivot_rules>

<risk_table>
| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| `quality/PROTOCOL.md` adaptation introduces banned word `replace` from research-doc text | medium | low (banned-words-lint catches it) | Task 57-01-F verify-block runs `bash scripts/banned-words-lint.sh`; pre-push lint not yet wired but manual run catches it |
| QG-08 row's expected.asserts grade as PASS today (REQUIREMENTS.md is clean, ROADMAP.md is NOT) — agent may try to "fix" the asserts to make them pass | medium | high (would defeat the QG-08 contract) | Task 57-01-B explicitly notes "the QG-08 row's asserts MUST grade RED for ROADMAP.md as it stands today; that RED is what POLISH-STRUCT in Wave D fixes". The contract is the asserts; the FIX is Wave D's move of historical sections. |
| JSON malformation breaks Wave B runner | low | high (blocks all of P57) | Task 57-01-B verify-block + Task 57-01-F sanity grep before commit |
| PROTOCOL.md exceeds 500-line cap | low | medium (anti-bloat rule violation) | Task 57-01-D verify-block + line budget self-check during write |
| BADGE-01 row's waiver `until: 2026-07-25T00:00:00Z` (90 days from 2026-04-27) is interpreted as "P60 must ship by then or row goes RED" | low | low (waiver mechanics work as designed) | The 90-day cap matches PROTOCOL.md §"Waiver protocol" rule "until must be ≤ 90 days from last_verified"; explicitly cite this in the row's reason field |
</risk_table>

<output>
After completion, create `.planning/phases/57-quality-gates-skeleton-structure-dimension/57-01-SUMMARY.md` with: commit SHA, files changed (5 paths), JSON validation output for `freshness-invariants.json` + `orphan-scripts.json`, the 8 row IDs in `freshness-invariants.json`, PROTOCOL.md line count, SURPRISES.md line count, the pivot rule reference, and a one-line "ready for Wave B" handoff naming `quality/runners/run.py + quality/runners/verdict.py + pre-push wiring (alongside, NOT migrate-from)`.
</output>
