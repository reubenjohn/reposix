---
phase: 57-quality-gates-skeleton-structure-dimension
plan: 02
type: execute
wave: 2
depends_on:
  - "57-01"
files_modified:
  - quality/gates/code/.gitkeep
  - quality/gates/docs-build/.gitkeep
  - quality/gates/docs-repro/.gitkeep
  - quality/gates/release/.gitkeep
  - quality/gates/structure/.gitkeep
  - quality/gates/agent-ux/.gitkeep
  - quality/gates/perf/.gitkeep
  - quality/gates/security/.gitkeep
  - quality/reports/verifications/.gitkeep
  - quality/reports/verdicts/.gitkeep
  - quality/runners/run.py
  - quality/runners/verdict.py
  - quality/runners/__init__.py
  - scripts/hooks/pre-push
autonomous: true
requirements:
  - QG-01
  - QG-02
  - QG-09
must_haves:
  truths:
    - "All 8 dimension directories under quality/gates/ exist (placeholders kept by .gitkeep) so each later phase has a fully-scoped home from day one."
    - "quality/runners/run.py is implementable as stdlib-only Python, cross-platform (linux + macOS), discovers catalog rows by cadence, runs verifiers within timeout, writes per-row JSON artifacts to row.artifact, and exits non-zero if any P0+P1 row is RED."
    - "quality/runners/verdict.py reads artifacts, emits markdown verdict + shields.io endpoint badge JSON to quality/reports/badge.json (QG-09 P57 emit scope), AND supports a `session-end` positional subcommand that rolls up across every cadence and writes a stable SESSION-VERDICT.md copy (the path the Wave C scripts/end-state.py shim invokes)."
    - "scripts/hooks/pre-push runs the new runner ALONGSIDE the existing scripts/end-state.py chain (parallel-migration rule per D4); both produce verdicts; neither replaces the other in this phase."
    - "Pre-push perf budget: new runner adds ≤10s overhead vs old chain. If exceeded, file waiver row + SURPRISES.md entry per pivot rule."
  artifacts:
    - path: "quality/gates/{code,docs-build,docs-repro,release,structure,agent-ux,perf,security}/.gitkeep"
      provides: "8 dimension placeholder directories with .gitkeep comments naming the eventual content."
      contains: ".gitkeep"
    - path: "quality/reports/verifications/.gitkeep + quality/reports/verdicts/.gitkeep"
      provides: "Reports tree placeholder. Wave C writes first artifacts; Wave F writes first verdict."
      contains: ".gitkeep"
    - path: "quality/runners/run.py"
      provides: "Single entry point: python3 quality/runners/run.py --cadence <C>. Stdlib only. Implements runner contract from quality/PROTOCOL.md."
      exports: ["main", "discover_catalogs", "run_row", "is_in_scope"]
      contains: "argparse"
    - path: "quality/runners/verdict.py"
      provides: "Verdict generator: python3 quality/runners/verdict.py [--cadence <C> | --phase <N>]. Reads artifacts, emits markdown verdict + shields.io endpoint badge JSON."
      exports: ["main", "compute_status_counts", "emit_markdown_verdict", "emit_shields_endpoint_json"]
      contains: "schemaVersion"
    - path: "scripts/hooks/pre-push"
      provides: "Updated pre-push body. New quality/runners/run.py --cadence pre-push invocation runs ALONGSIDE the existing chain. Both emit verdicts; neither blocks the other. Hard-cut deferred to P60 (SIMPLIFY-10)."
      contains: "quality/runners/run.py --cadence pre-push"
  key_links:
    - from: "quality/runners/run.py"
      to: "quality/catalogs/*.json"
      via: "discover_catalogs() globs for *.json under quality/catalogs/"
      pattern: "quality/catalogs"
    - from: "quality/runners/run.py"
      to: "quality/gates/<dim>/<verifier>"
      via: "row.verifier.script field — runner invokes via subprocess"
      pattern: "verifier"
    - from: "quality/runners/run.py"
      to: "quality/reports/verifications/<dim>/<row-id>.json"
      via: "row.artifact field — runner writes JSON output"
      pattern: "artifact"
    - from: "quality/runners/verdict.py"
      to: "quality/reports/badge.json"
      via: "shields.io endpoint format emit — QG-09 P57 scope"
      pattern: "schemaVersion"
    - from: "quality/runners/verdict.py session-end"
      to: "quality/reports/verdicts/session-end/SESSION-VERDICT.md"
      via: "session-end subcommand rolls up every cadence + writes a stable convenience copy that scripts/end-state.py shim (Wave C) reads"
      pattern: "SESSION-VERDICT.md"
    - from: "scripts/hooks/pre-push"
      to: "quality/runners/run.py"
      via: "shell-out alongside scripts/end-state.py warning chain (parallel migration per D4)"
      pattern: "quality/runners/run.py --cadence pre-push"
---

<objective>
Wave B — directory skeleton + runner. Wave A landed the contract; Wave B builds the framework that consumes it: 8 dimension placeholder directories, the reports tree, the runner (run.py), the verdict generator (verdict.py), and pre-push hook wiring that runs the new runner ALONGSIDE the old chain.

**Critical constraint — parallel migration per D4 (`v0.12.0-decisions-log.md`):** the new system runs SIDE-BY-SIDE with the old. The existing `scripts/end-state.py verdict` warning + cargo fmt + cargo clippy + check-docs-site + check-mermaid-renders pipeline stays UNCHANGED. The new `quality/runners/run.py --cadence pre-push` is added as a SEPARATE invocation. Both produce verdicts; neither replaces the other. Hard-cut to runner-only is P60 (SIMPLIFY-10), NOT this phase.

**Pre-push perf budget:** the new runner adds ≤10s overhead. If exceeded, file a waiver + SURPRISES.md entry per pivot rule from `v0.12.0-roadmap-and-rationale.md` § Phase 57. Do NOT silently downgrade.

**QG-09 P57 emit scope:** `quality/runners/verdict.py` writes `quality/reports/badge.json` in shields.io endpoint format. P60 publishes it via mkdocs; P57's job is just the JSON emit. Color thresholds: `brightgreen` if all P0+P1 rows are PASS or WAIVED; `yellow` if any P2 row is RED; `red` if any P0+P1 row is RED.

**Stdlib-only Python.** No `pip install`. Cross-platform: linux + macOS. Windows out of scope.

Output: 14 files committed across 1-2 commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.12.0-naming-and-architecture.md
@.planning/research/v0.12.0-decisions-log.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-01-PLAN.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-01-SUMMARY.md
@quality/PROTOCOL.md
@quality/catalogs/README.md
@quality/catalogs/freshness-invariants.json
@scripts/hooks/pre-push
@CLAUDE.md

<interfaces>
**Runner contract** (from `.planning/research/v0.12.0-naming-and-architecture.md` § "Runner contract"):

1. Discover catalog files under quality/catalogs/ (glob *.json, skip orphan-scripts.json + README.md).
2. For each row: filter by `row.cadence == C AND (row.waiver is null OR row.waiver.until > now())`.
3. Sort by blast_radius (P0 first).
4. Invoke `row.verifier.script` with `row.verifier.args` under `row.verifier.timeout_s`. Capture stdout/stderr; write JSON to `row.artifact` with shape `{ts, exit_code, stdout, stderr, asserts_passed: [...], asserts_failed: [...]}`. Update `row.status` (0=PASS, 1=FAIL, 2=PARTIAL, timeout=FAIL). Update `row.last_verified`.
5. Write the catalog file back (preserve field order).
6. Exit 0 if all P0+P1 rows are PASS or WAIVED; exit 1 otherwise. P2 RED logs warning but doesn't block.

**Critical Wave B behavior — verifier-not-found handling.** Wave B's runner runs against Wave A's catalogs which point at not-yet-shipped Wave C verifier scripts. The runner MUST gracefully report `NOT-VERIFIED` (status) when the script does not exist on disk; it MUST NOT crash. Artifact for missing-verifier rows: `{ts, row_id, exit_code: null, error: "verifier not found at <path>", asserts_passed: [], asserts_failed: []}`.

**WAIVED handling.** If `row.waiver` is in scope (already filtered through `is_in_scope`), set status `WAIVED` and skip the subprocess. Artifact: `{ts, row_id, exit_code: 0, waiver: <row.waiver>, asserts_passed: [], asserts_failed: []}`.

**Verdict.py contract:**

1. Read every `row.artifact` for rows in scope.
2. Compute counts by status.
3. Write `quality/reports/verdicts/<cadence-or-phase>/<ts>.md` with: top-line GREEN/RED, status counts table, list of FAIL rows with owner_hint, list of WAIVED rows with reason+until, list of NOT-VERIFIED with last_verified age + freshness_ttl.
4. Write `quality/reports/badge.json` (shields.io endpoint format) — QG-09 P57 emit.
5. Exit 0 iff verdict is GREEN.
6. **`session-end` positional subcommand** — when `sys.argv[1] == "session-end"`, run a thin wrapper that calls verdict.py's existing rollup logic across EVERY cadence and writes (a) a timestamped report at `quality/reports/verdicts/session-end/<ts>.md` and (b) a stable copy at `quality/reports/verdicts/session-end/SESSION-VERDICT.md`. Per `.planning/research/v0.12.0-naming-and-architecture.md` lines 149+161 architecture spec. Anti-bloat: this is a thin wrapper, NOT new grading logic.

**Badge JSON schema** (per D13 + REQUIREMENTS.md QG-09):

```jsonc
{ "schemaVersion": 1, "label": "quality gates", "message": "47/50 GREEN", "color": "brightgreen" }
```

Color thresholds: `brightgreen` if all P0+P1 rows are PASS or WAIVED; `yellow` if any P2 RED but no P0+P1 RED; `red` if any P0+P1 RED. Message numerator = (P0+P1 rows in scope with status PASS or WAIVED); denominator = (P0+P1 rows in scope, total). P2 rows excluded from numerator/denominator (they affect color but not the count).

**Pre-push hook wiring** (parallel migration per D4). The current pre-push body has 6 blocks:
1. Credential scan (lines ~63-122, fatal)
2. Catalog coverage warning (lines ~136-145, non-fatal)
3. SESSION-END-STATE verdict warning (lines ~147-159, non-fatal)
4. Docs-site validation (lines ~161-194, fatal IF docs touched)
5. Cargo fmt (fatal)
6. Cargo clippy (fatal)

**Wave B addition:** insert a new block between block 3 (SESSION-END-STATE warning) and block 4 (docs-site). The new block runs `python3 quality/runners/run.py --cadence pre-push` as a NON-FATAL warning. Body:

```bash
# --- Quality Gates runner (parallel-migration window — non-fatal warning) ----
# Per .planning/research/v0.12.0-decisions-log.md D4 (parallel-then-cut), the
# new quality/runners/run.py runs ALONGSIDE scripts/end-state.py for two
# pre-push cycles. P60 (SIMPLIFY-10) hard-cuts pre-push to a single
# `quality/runners/run.py --cadence pre-push` invocation. Until then, this
# block is a warning that lets us compare verdicts.
if [ -f quality/runners/run.py ]; then
  log "${YELLOW}->${NC} running quality/runners/run.py --cadence pre-push (parallel migration; warning only)..."
  if ! python3 quality/runners/run.py --cadence pre-push 2>&1 | sed 's/^/  /'; then
    printf '%b\n' "${YELLOW}!${NC} quality/runners/run.py --cadence pre-push reported RED (non-fatal warning until P60)" >&2
  else
    log "${GREEN}OK${NC} quality/runners/run.py --cadence pre-push GREEN."
  fi
fi
```

Critical: the new block is non-fatal. The old chain (cargo fmt + clippy + docs-site) STAYS FATAL. Wave B does NOT change which checks block; it only adds the new runner as a warning so verdicts can be compared during Wave C/D/E without bricking pushes.

**Stdlib-only Python imports allowed:** `argparse, json, pathlib, subprocess, datetime, sys, os`. NO `requests, yaml, click, rich`.

**Cross-platform constraints:**
- Use `pathlib.Path` for path ops.
- Use `subprocess.run([..., shell=False])` — never `shell=True`.
- Use `sys.executable` for python invocation (works under venv).
- For shell verifiers: use `["bash", path]`.

**Module constants for both runners:**
- `REPO_ROOT = Path(__file__).resolve().parent.parent.parent`
- `CATALOG_DIR = REPO_ROOT / "quality" / "catalogs"`
- `REPORTS_DIR = REPO_ROOT / "quality" / "reports"`
- `BLAST_RADIUS_ORDER = {"P0": 0, "P1": 1, "P2": 2}`

**Output line budget:**
- `run.py` target 150-300 lines (cap 350 in verify).
- `verdict.py` target 200-350 lines (cap 400 in verify).
- `__init__.py` empty (just creates the package marker).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 57-02-A: Create directory skeleton (8 .gitkeep + 2 reports placeholders)</name>
  <files>quality/gates/code/.gitkeep, quality/gates/docs-build/.gitkeep, quality/gates/docs-repro/.gitkeep, quality/gates/release/.gitkeep, quality/gates/structure/.gitkeep, quality/gates/agent-ux/.gitkeep, quality/gates/perf/.gitkeep, quality/gates/security/.gitkeep, quality/reports/verifications/.gitkeep, quality/reports/verdicts/.gitkeep</files>
  <action>
Create 10 `.gitkeep` files. Each is a single-line `# ...` comment naming the eventual content per `v0.12.0-naming-and-architecture.md` § "Directory layout":

- `quality/gates/code/.gitkeep` — `# code dimension — clippy, fmt, cargo nextest. Mostly references ci.yml. P58 SIMPLIFY-04 + SIMPLIFY-05 land here.`
- `quality/gates/docs-build/.gitkeep` — `# docs-build dimension — mkdocs-strict.sh, mermaid-renders.sh, link-resolution.py, badges-resolve.py. P60 SIMPLIFY-08 + BADGE-01 land here.`
- `quality/gates/docs-repro/.gitkeep` — `# docs-repro dimension — snippet-extract.py, container-rehearse.sh, tutorial-replay.sh. P59 SIMPLIFY-06 lands here.`
- `quality/gates/release/.gitkeep` — `# release dimension — gh-assets-present.py, brew-formula-current.py, crates-io-max-version.py, installer-asset-bytes.py. P58 RELEASE-04 lands here.`
- `quality/gates/structure/.gitkeep` — `# structure dimension — freshness-invariants.py (the migrated end-state.py freshness rows), banned-words.sh. P57 Wave C ships these.`
- `quality/gates/agent-ux/.gitkeep` — `# agent-ux dimension — dark-factory.sh. P59 SIMPLIFY-07. Intentionally sparse for v0.12.0.`
- `quality/gates/perf/.gitkeep` — `# perf dimension — token-economy-bench.py, latency-bench.sh. P59 SIMPLIFY-11 file-relocate; v0.12.1 stub-to-real per MIGRATE-03.`
- `quality/gates/security/.gitkeep` — `# security dimension — allowlist-enforcement.py, audit-immutability-test. v0.12.1 carry-forward per MIGRATE-03.`
- `quality/reports/verifications/.gitkeep` — `# Per-row artifact dir. Runner writes <dim>/<slug>.json under here. .gitkeep keeps the empty tree in git.`
- `quality/reports/verdicts/.gitkeep` — `# Per-cadence + per-phase verdict dir. verdict.py writes <cadence-or-phase>/<ts>.md under here.`

Use the Write tool for each file.
  </action>
  <verify>
    <automated>for d in code docs-build docs-repro release structure agent-ux perf security; do test -f quality/gates/$d/.gitkeep || { echo "missing: quality/gates/$d/.gitkeep"; exit 1; }; done && test -f quality/reports/verifications/.gitkeep && test -f quality/reports/verdicts/.gitkeep && echo "10 .gitkeep files OK"</automated>
  </verify>
  <done>
8 dimension `.gitkeep` files exist under `quality/gates/`; 2 `.gitkeep` files exist under `quality/reports/`; each contains a one-line `# ...` comment naming the eventual content.
  </done>
</task>

<task type="auto">
  <name>Task 57-02-B: Implement quality/runners/run.py + __init__.py</name>
  <files>quality/runners/run.py, quality/runners/__init__.py</files>
  <action>
Create `quality/runners/__init__.py` as an empty file.

Create `quality/runners/run.py` implementing the runner contract from `<interfaces>`. Structure:

**Header docstring** (verbatim shape):

```python
#!/usr/bin/env python3
"""Quality Gates runner — single entry point for cadence-tagged gates.

Per .planning/research/v0.12.0-naming-and-architecture.md § runner-contract.
Stdlib only. Cross-platform (linux + macOS).
Anti-bloat: this file does not grow beyond the runner contract. Per-dimension
verifiers live under quality/gates/<dim>/.

Usage:
  python3 quality/runners/run.py --cadence <pre-push|pre-pr|weekly|pre-release|post-release|on-demand>

Exit codes:
  0 — every P0+P1 row in scope is PASS or WAIVED.
  1 — any P0+P1 row in scope is FAIL/PARTIAL/NOT-VERIFIED.
"""
```

**Module constants** (defined at top after imports):
- `REPO_ROOT`, `CATALOG_DIR`, `REPORTS_DIR`, `BLAST_RADIUS_ORDER` per `<interfaces>`.

**Top-level functions** (each documented):
- `now_iso() -> str` — return current UTC time as RFC3339 with `Z` suffix.
- `parse_rfc3339(s: str) -> datetime` — parse RFC3339 with or without `Z`; return tz-aware datetime.
- `discover_catalogs() -> list[Path]` — glob `CATALOG_DIR / "*.json"`. Skip files whose stem is `orphan-scripts` or whose name is `README.md`. Return sorted.
- `load_catalog(path: Path) -> dict` — `json.load`; verify wrapper has `dimension` + `rows` keys; raise on parse failure with file path in error.
- `save_catalog(path: Path, data: dict) -> None` — `json.dump(..., indent=2, sort_keys=False)` + trailing newline. Preserve top-level field order: `$schema`, `comment`, `dimension`, `rows`.
- `is_in_scope(row: dict, cadence: str, now: datetime) -> bool` — `row.cadence == cadence` AND (`row.waiver is None` OR `parse_rfc3339(row.waiver["until"]) > now`).
- `sort_by_blast_radius(rows: list[dict]) -> list[dict]` — stable sort by `BLAST_RADIUS_ORDER.get(row.get("blast_radius"), 99)`.
- `run_row(row: dict, repo_root: Path) -> dict` — invoke verifier, write artifact, return updated row dict. Logic:
  - **WAIVED case:** `row["waiver"]` is in scope. Set status `WAIVED`. Write artifact `{ts, row_id, exit_code: 0, waiver: row["waiver"], asserts_passed: [], asserts_failed: []}`. Skip subprocess.
  - **Verifier-not-found case:** `(repo_root / row["verifier"]["script"]).exists()` is False. Set status `NOT-VERIFIED`. Write artifact `{ts, row_id, exit_code: null, error: "verifier not found at <path>", asserts_passed: [], asserts_failed: []}`. Skip subprocess. **This path is critical for Wave B** — Wave A's catalogs point at not-yet-shipped Wave C verifiers.
  - **Subprocess case:** detect language by extension. `.py` → `[sys.executable, str(repo_root / row["verifier"]["script"]), *row["verifier"]["args"]]`. `.sh` → `["bash", str(repo_root / row["verifier"]["script"]), *row["verifier"]["args"]]`. Run with `timeout=row["verifier"].get("timeout_s", 30)`. On `TimeoutExpired`: status `FAIL`, artifact gets `timed_out: true`. Map exit 0→PASS, 1→FAIL, 2→PARTIAL.
  - Write artifact JSON to `repo_root / row["artifact"]`; mkdir parents.
  - Return row dict with updated `status` + `last_verified`.
- `compute_exit_code(rows: list[dict]) -> int` — exit 0 iff every P0+P1 row's status is PASS or WAIVED. Print warning for P2 RED but don't change exit.
- `print_row_summary(row: dict, elapsed_s: float) -> None` — print `[STATUS]   row.id  (Pn, X.XXs)  -> owner_hint` style line to stdout.
- `main() -> int` — argparse with `--cadence` required (choices: pre-push, pre-pr, weekly, pre-release, post-release, on-demand). Run the discover/filter/sort/run/save pipeline. Print summary line. Return exit code.

**Output format on stdout** (when run):

```
quality/runners/run.py --cadence pre-push
  catalog: freshness-invariants.json (8 rows; 8 in scope)
    [NOT-VERIFIED] structure/install-leads-with-pkg-mgr-readme  (P0) -> verifier not found at quality/gates/structure/freshness-invariants.py
    [NOT-VERIFIED] structure/install-leads-with-pkg-mgr-docs-index  (P0) -> verifier not found
    [NOT-VERIFIED] structure/no-version-pinned-filenames  (P1) -> verifier not found
    ...
    [WAIVED] structure/badges-resolve  (P2, until 2026-07-25) -> verifier ships in P60
summary: 0 PASS, 0 FAIL, 0 PARTIAL, 1 WAIVED, 7 NOT-VERIFIED -> exit=1 (P0+P1 NOT-VERIFIED treated as RED)
```

**Self-test before commit.** Run `python3 quality/runners/run.py --cadence pre-push`. Expected:
- Exit 1 (NOT-VERIFIED rows treated as RED for P0+P1).
- Stdout shows 8 rows: 7 NOT-VERIFIED + 1 WAIVED.
- 8 artifact files written under `quality/reports/verifications/structure/`.
- Catalog rows updated: `status: NOT-VERIFIED` (or WAIVED for badge row), non-null `last_verified`.

This proves Wave B's runner correctly reports "no verifiers yet" without crashing.

Target line count: 150-300 (cap 350 in verify).
  </action>
  <verify>
    <automated>test -f quality/runners/run.py && test -f quality/runners/__init__.py && python3 -c "import ast; ast.parse(open('quality/runners/run.py').read())" && lc=$(wc -l < quality/runners/run.py) && [ "$lc" -le 350 ] && python3 quality/runners/run.py --help 2>&1 | grep -q "cadence" && echo "run.py syntax OK lines=$lc"</automated>
  </verify>
  <done>
`run.py` exists, parses as valid Python, ≤350 lines, `--help` mentions `--cadence`. Self-test (running with `--cadence pre-push`) does NOT crash; exits 1 (rows NOT-VERIFIED treated as RED for P0+P1); writes 8 artifact files under `quality/reports/verifications/structure/`; catalog rows are updated to `status: NOT-VERIFIED` (or WAIVED for badge row) with non-null `last_verified`. `__init__.py` exists (empty).
  </done>
</task>

<task type="auto">
  <name>Task 57-02-C: Implement quality/runners/verdict.py (markdown + shields.io badge JSON)</name>
  <files>quality/runners/verdict.py</files>
  <action>
Create `quality/runners/verdict.py` implementing the verdict contract from `<interfaces>`.

**Header docstring:**

```python
#!/usr/bin/env python3
"""Quality Gates verdict — collate artifacts -> markdown verdict + shields.io badge JSON.

Per .planning/research/v0.12.0-naming-and-architecture.md § runner-contract.
Stdlib only. Reads quality/catalogs/*.json + quality/reports/verifications/<dim>/*.json,
writes quality/reports/verdicts/<cadence-or-phase>/<ts>.md + quality/reports/badge.json.

QG-09 P57 emit scope: badge.json in shields.io endpoint format. P60 publishes
it via mkdocs as docs/badge.json -> https://reubenjohn.github.io/reposix/badge.json.

Usage:
  python3 quality/runners/verdict.py --cadence pre-push
  python3 quality/runners/verdict.py --phase 57
  python3 quality/runners/verdict.py session-end   # roll up across every cadence; emit SESSION-VERDICT.md
  python3 quality/runners/verdict.py            # all cadences

Exit codes:
  0 — verdict is GREEN.
  1 — verdict is RED.
"""
```

**Module constants** (same as run.py): `REPO_ROOT`, `CATALOG_DIR`, `REPORTS_DIR`, plus `VERDICTS_DIR = REPORTS_DIR / "verdicts"` and `BADGE_PATH = REPORTS_DIR / "badge.json"`.

**Top-level functions:**
- `now_iso() -> str` — RFC3339 UTC.
- `now_iso_filename() -> str` — filesystem-safe variant (`2026-04-27T18-34-00Z`, replacing `:` with `-`).
- `discover_catalogs() -> list[Path]` — same as run.py (copy is fine; Wave B prioritizes small-and-correct over dedup).
- `load_artifact(row: dict, repo_root: Path) -> dict | None` — read `repo_root / row["artifact"]` if exists; return parsed JSON or None.
- `compute_status_counts(rows: list[dict]) -> dict[str, int]` — count by status across PASS/FAIL/PARTIAL/WAIVED/NOT-VERIFIED.
- `compute_color(rows: list[dict]) -> str` — apply 3-rule color logic from `<interfaces>`: brightgreen if all P0+P1 rows in scope are PASS or WAIVED; yellow if any P2 RED but no P0+P1 RED; red if any P0+P1 RED.
- `compute_badge_message(rows: list[dict]) -> str` — `f"{numerator}/{denominator} GREEN"`. Numerator = P0+P1 rows in scope with status in {PASS, WAIVED}. Denominator = P0+P1 rows in scope, total. P2 rows excluded from both.
- `emit_markdown_verdict(rows_with_artifacts: list[dict], cadence_or_phase: str, out_path: Path) -> None` — write the markdown verdict per the structure below.
- `emit_shields_endpoint_json(rows: list[dict], out_path: Path) -> None` — write `{schemaVersion: 1, label: "quality gates", message, color}` JSON.
- `emit_session_end_rollup(repo_root: Path, args: list[str]) -> int` — runs the verdict rollup across EVERY cadence (pre-push, pre-pr, weekly, pre-release, post-release, on-demand) reading the most recent artifact per row, composes a single combined markdown verdict, writes it to `quality/reports/verdicts/session-end/<ts>.md`, and ALSO writes/overwrites a stable convenience copy at `quality/reports/verdicts/session-end/SESSION-VERDICT.md` (a copy, NOT a symlink — windows compat + git portability). The rollup is a thin wrapper around the existing per-cadence rollup logic; no new grading rules — anti-bloat. Reads optional `--phase <N>` argument from `args` to scope to a phase if supplied; otherwise covers all rows currently in any catalog.
- `main() -> int` — argparse with `session-end` as a POSITIONAL subcommand (handled BEFORE the optional flags) plus `--cadence` (optional) OR `--phase` (optional) flags. Dispatch:
  - `sys.argv[1] == "session-end"` → call `emit_session_end_rollup(REPO_ROOT, sys.argv[2:])`.
  - `--cadence X` → write to `quality/reports/verdicts/<cadence>/<ts>.md`.
  - `--phase N` → write to `quality/reports/verdicts/p<N>/<ts>.md`.
  - Neither → write to `quality/reports/verdicts/all/<ts>.md`.
  Compute verdict, write markdown + badge, return exit code.

**Markdown verdict structure:**

```markdown
# Quality Gates verdict — <cadence or phase> — <ts>

- Verdict: **<GREEN|RED>**
- Generated at: `<RFC3339>`
- Badge: `quality/reports/badge.json` (shields.io endpoint format)

## Status counts

| status | count |
|---|---|
| PASS | <N> |
| FAIL | <N> |
| PARTIAL | <N> |
| WAIVED | <N> |
| NOT-VERIFIED | <N> |

## Failures (if any)

- `<row.id>` (P<n>, dim=<dim>) — <row.owner_hint>
  - artifact: `<row.artifact>`
  - asserts_failed: <list>

## Waivers (if any)

- `<row.id>` — `<row.waiver.reason>` (until `<row.waiver.until>`, tracked_in `<row.waiver.tracked_in>`)

## NOT-VERIFIED (if any)

- `<row.id>` (P<n>, dim=<dim>) — last_verified: `<row.last_verified or "never">`, freshness_ttl: `<row.freshness_ttl or "(mechanical)">`

## PASS (collapsed)

<details><summary>N PASS rows</summary>

- `<row.id>` (P<n>, dim=<dim>)

</details>

---
*Generated by `python3 quality/runners/verdict.py`. See `quality/PROTOCOL.md` § "runner contract".*
```

**Verdict path:**
- `--cadence X`: `quality/reports/verdicts/<cadence>/<ts>.md`.
- `--phase N`: `quality/reports/verdicts/p<N>/<ts>.md`.
- `session-end` subcommand: `quality/reports/verdicts/session-end/<ts>.md` PLUS a stable copy at `quality/reports/verdicts/session-end/SESSION-VERDICT.md` (overwritten each invocation; this is the path the `scripts/end-state.py` shim from Wave C reads).
- Neither: `quality/reports/verdicts/all/<ts>.md`.

**`session-end` subcommand semantics** (per `.planning/research/v0.12.0-naming-and-architecture.md` lines 149+161 architecture spec — "session-end mode runs across every cadence + emits SESSION-VERDICT.md"):

The subcommand runs the verdict rollup across EVERY cadence (reading the latest per-row artifact regardless of which cadence produced it) and emits one combined markdown report. **It is a thin wrapper** around verdict.py's existing rollup logic — it does NOT re-implement grading; it iterates `for cadence in CADENCES: collate_rows(cadence)` and unions the results. Anti-bloat: ≤30 lines added to verdict.py for this subcommand.

The stable `SESSION-VERDICT.md` copy exists so that `scripts/end-state.py` (the Wave C shim) can pass `session-end` through and the caller gets a deterministic path. Wave C's shim invokes `python3 quality/runners/verdict.py session-end` (passing through `sys.argv[2:]` for any optional `--phase N` scoping).

**Badge JSON** at `quality/reports/badge.json`:

```json
{ "schemaVersion": 1, "label": "quality gates", "message": "0/7 GREEN", "color": "red" }
```

(Wave B self-test result: 0 PASS in P0+P1, 7 P0+P1 rows in scope, 1 P2 WAIVED. Numerator/denominator counts P0+P1 only. Color is `red` because 7 P0+P1 NOT-VERIFIED rows.)

**Self-test.** Run `python3 quality/runners/verdict.py --phase 57`. Expected:
- Writes `quality/reports/verdicts/p57/<ts>.md` with 7 NOT-VERIFIED rows + 1 WAIVED.
- Writes `quality/reports/badge.json` with `color: red`, `message: "0/7 GREEN"`.
- Exit code 1.

**Self-test for `session-end`.** Run `python3 quality/runners/verdict.py session-end`. Expected:
- Writes `quality/reports/verdicts/session-end/<ts>.md` (timestamped).
- Writes `quality/reports/verdicts/session-end/SESSION-VERDICT.md` (stable copy; same content).
- Same exit-code semantics as cadence/phase modes (0 GREEN, 1 RED).
- Wave B run state: exit 1 (rows still NOT-VERIFIED until Wave C ships verifiers).

Target line count: 200-350 (cap 400 in verify).
  </action>
  <verify>
    <automated>test -f quality/runners/verdict.py && python3 -c "import ast; ast.parse(open('quality/runners/verdict.py').read())" && lc=$(wc -l < quality/runners/verdict.py) && [ "$lc" -le 400 ] && python3 quality/runners/verdict.py --phase 57 || true; test -f quality/reports/badge.json && python3 -c "import json; d=json.load(open('quality/reports/badge.json')); assert d['schemaVersion']==1; assert d['label']=='quality gates'; assert 'message' in d; assert d['color'] in ('brightgreen','yellow','red'); print('badge.json OK', d)" && ls quality/reports/verdicts/p57/ | head -3 && python3 quality/runners/verdict.py session-end || true; test -f quality/reports/verdicts/session-end/SESSION-VERDICT.md && echo "session-end OK"</automated>
  </verify>
  <done>
`verdict.py` exists, parses as valid Python, ≤400 lines. Self-test writes `quality/reports/verdicts/p57/<ts>.md` + `quality/reports/badge.json`. Badge JSON has correct schema (`schemaVersion: 1`, `label: "quality gates"`, valid `color`). Markdown verdict has all 5 sections (Status counts, Failures, Waivers, NOT-VERIFIED, PASS). The `session-end` positional subcommand is wired and writes both a timestamped report and the stable `SESSION-VERDICT.md` copy.
  </done>
</task>

<task type="auto">
  <name>Task 57-02-D: Wire pre-push hook to invoke runner alongside old chain</name>
  <files>scripts/hooks/pre-push</files>
  <action>
Edit `scripts/hooks/pre-push`. Insert the new "Quality Gates runner" block per `<interfaces>` between the existing "SESSION-END-STATE verdict (non-fatal warning)" block (currently at lines ~147-159) and the "Docs-site validation" block (currently at lines ~161-194).

**Exact insertion point:** find the line `# --- Docs-site validation -------------------------------------------------` and insert the new block IMMEDIATELY BEFORE it.

**Insertion content** (exact):

```bash
# --- Quality Gates runner (parallel-migration window — non-fatal warning) ----
# Per .planning/research/v0.12.0-decisions-log.md D4 (parallel-then-cut), the
# new quality/runners/run.py runs ALONGSIDE scripts/end-state.py for two
# pre-push cycles. P60 (SIMPLIFY-10) hard-cuts pre-push to a single
# `quality/runners/run.py --cadence pre-push` invocation. Until then, this
# block is a warning that lets us compare verdicts.
if [ -f quality/runners/run.py ]; then
  log "${YELLOW}->${NC} running quality/runners/run.py --cadence pre-push (parallel migration; warning only)..."
  if ! python3 quality/runners/run.py --cadence pre-push 2>&1 | sed 's/^/  /'; then
    printf '%b\n' "${YELLOW}!${NC} quality/runners/run.py --cadence pre-push reported RED (non-fatal warning until P60)" >&2
  else
    log "${GREEN}OK${NC} quality/runners/run.py --cadence pre-push GREEN."
  fi
fi

```

**Critical guardrails:**
- The new block is NON-FATAL (no `exit 1`). The existing chain (cargo fmt + clippy + docs-site IF touched) STAYS FATAL and unchanged.
- Use the existing color constants `${YELLOW}`, `${GREEN}`, `${NC}` defined at the top of the file. Use the existing `log()` helper for non-fatal-info lines and `printf '%b\n' ... >&2` for warnings (matching the existing style).
- Do NOT change `set -euo pipefail` (already present).
- Do NOT touch the credential scan, catalog coverage, SESSION-END-STATE, docs-site, mermaid-renders, fmt, or clippy blocks. ONLY insert the new block at the marked location.

**Test the hook locally:** run `bash scripts/hooks/test-pre-push.sh` if it exists; otherwise simulate by running `bash scripts/hooks/pre-push < /dev/null` and confirming it does NOT crash and that the new "Quality Gates runner" line appears in stderr output. The hook will print warnings about NOT-VERIFIED rows (expected — Wave C hasn't shipped yet); these are non-fatal.

**Performance check:** time `python3 quality/runners/run.py --cadence pre-push` standalone. Expected: <2s (the runner's only work is reading 1 catalog file, 8 rows, all marked NOT-VERIFIED → quick subprocess-skip path → write 8 small artifacts). If >2s, the runner has an O(N²) bug; investigate before committing.

**If pre-push perf budget breached** (>10s overhead vs the old chain): per the Pivot Rules block below, file a waiver row in `quality/catalogs/orphan-scripts.json` documenting the overage, append a `2026-04-27 P57: ` line to `quality/SURPRISES.md`, and proceed with the parallel migration anyway. The old chain still gates pushes.
  </action>
  <verify>
    <automated>grep -q "Quality Gates runner (parallel-migration window" scripts/hooks/pre-push && grep -q "quality/runners/run.py --cadence pre-push" scripts/hooks/pre-push && bash -n scripts/hooks/pre-push && echo "pre-push hook syntax OK; new block present"</automated>
  </verify>
  <done>
`scripts/hooks/pre-push` has a new "Quality Gates runner" block between SESSION-END-STATE and Docs-site blocks; the block invokes `python3 quality/runners/run.py --cadence pre-push` non-fatally; existing fatal blocks (fmt, clippy, docs-site, mermaid-renders, credential scan) are unchanged; `bash -n` (syntax check) passes.
  </done>
</task>

<task type="auto">
  <name>Task 57-02-E: Commit Wave B (skeleton + runners + pre-push wiring)</name>
  <files>quality/gates/code/.gitkeep, quality/gates/docs-build/.gitkeep, quality/gates/docs-repro/.gitkeep, quality/gates/release/.gitkeep, quality/gates/structure/.gitkeep, quality/gates/agent-ux/.gitkeep, quality/gates/perf/.gitkeep, quality/gates/security/.gitkeep, quality/reports/verifications/.gitkeep, quality/reports/verdicts/.gitkeep, quality/runners/run.py, quality/runners/verdict.py, quality/runners/__init__.py, scripts/hooks/pre-push</files>
  <action>
Stage and commit all 14 files (10 .gitkeep + 3 runners + pre-push edit). Plus, the runner self-tests in Task B + C will have written:
- 8 artifacts under `quality/reports/verifications/structure/` (one per row)
- 1 verdict under `quality/reports/verdicts/p57/<ts>.md`
- 1 badge at `quality/reports/badge.json`
- Updated `quality/catalogs/freshness-invariants.json` (status fields → NOT-VERIFIED/WAIVED, last_verified set)

The artifacts are runtime byproducts. Decide commit scope:

**Option 1 (recommended):** Commit the 14 source files. Do NOT commit the artifacts (they regenerate on every runner invocation). Update `.gitignore` to exclude `quality/reports/verifications/structure/` AND `quality/reports/verdicts/` AND `quality/reports/badge.json`. Ship the .gitkeep placeholders so the directory tree exists in git but artifacts don't.

**Option 2 (alternative):** Commit the artifacts as P57 first-run baseline. Useful for diff'ing future runs. Cost: every pre-push will leave dirty status on these files.

**Choose Option 1.** Add to `.gitignore`:

```
# Quality Gates runtime artifacts (per-run; regenerate on every runner invocation)
quality/reports/verifications/*/*.json
quality/reports/verdicts/*/*.md
quality/reports/badge.json
```

Also revert the catalog status mutations from the self-test (Wave B's runner mutated catalog rows; Wave A's commit had `status: NOT-VERIFIED` + `last_verified: null`; the self-test set `last_verified` to a real timestamp). Use `git checkout quality/catalogs/freshness-invariants.json` to revert the catalog to Wave A's pristine state. Wave C's first runner invocation will populate the timestamps for real.

**Two-commit option (cleaner trail):**
- Commit 1: `chore(p57): scaffold quality/gates/* + reports tree + runners (QG-01, QG-02)` — 13 files (10 .gitkeep + 3 runners) + .gitignore update.
- Commit 2: `chore(p57): wire pre-push to invoke quality/runners/run.py alongside old chain (D4 parallel migration)` — 1 file (pre-push edit).

**One-commit alternative:** combine into a single commit. Either is acceptable; two-commit is cleaner because the pre-push edit is one logical unit and easier to revert if the parallel-migration window proves it's a regression.

**Use two-commit form.** Commit 1 message:

```
chore(p57): scaffold quality/gates/* + reports tree + runners (QG-01, QG-02, QG-09 emit)

Lands the framework skeleton that consumes Wave A's catalog contract:
- quality/gates/{code,docs-build,docs-repro,release,structure,agent-ux,perf,security}/
  with .gitkeep comments naming eventual content per
  .planning/research/v0.12.0-naming-and-architecture.md § "Directory layout".
- quality/reports/{verifications,verdicts}/ with .gitkeep placeholders.
- quality/runners/run.py — single entry point; cadence-tagged gate runner;
  stdlib-only Python; cross-platform (linux + macOS); gracefully handles
  not-yet-shipped Wave C verifiers via NOT-VERIFIED status.
- quality/runners/verdict.py — markdown verdict + shields.io endpoint badge
  JSON (QG-09 P57 emit scope; P60 publishes via mkdocs).
- .gitignore — exclude per-run artifacts (verifications/*.json, verdicts/*.md,
  badge.json) since they regenerate on every runner invocation.

Wave B self-test (`python3 quality/runners/run.py --cadence pre-push` +
`python3 quality/runners/verdict.py --phase 57`):
- 8 catalog rows visited; 7 NOT-VERIFIED (verifiers ship Wave C),
  1 WAIVED (BADGE-01 row, until 2026-07-25).
- Exit codes correctly 1 (RED) — proves the runner reports correctly without
  crashing on missing verifiers.

NO Wave C verifier code yet (that lands in 57-03 PLAN). NO scripts/end-state.py
shrink yet (Wave C). NO POLISH-STRUCT fixes (Wave D).

Refs: QG-01, QG-02, QG-09 (P57 emit), STRUCT-01 prep
```

Commit 2 message:

```
chore(p57): wire pre-push to invoke quality/runners/run.py alongside old chain

Per .planning/research/v0.12.0-decisions-log.md D4 (parallel-then-cut migration),
the new runner runs SIDE-BY-SIDE with scripts/end-state.py for two pre-push
cycles. Both produce verdicts; neither blocks the other. Hard-cut to runner-only
is P60 (SIMPLIFY-10).

Pre-push body now has 7 blocks (was 6):
1. Credential scan (fatal) — unchanged
2. Catalog coverage warning (non-fatal) — unchanged
3. SESSION-END-STATE verdict warning (non-fatal) — unchanged
4. NEW: Quality Gates runner warning (non-fatal) — `quality/runners/run.py --cadence pre-push`
5. Docs-site validation (fatal IF docs touched) — unchanged
6. Cargo fmt (fatal) — unchanged
7. Cargo clippy (fatal) — unchanged

The new block is non-fatal so it cannot brick a push during Wave C/D/E
implementation. Pre-push perf budget: ≤10s overhead. Wave B self-test
measured ~0.5s standalone; well within budget.

Pivot rule per .planning/research/v0.12.0-roadmap-and-rationale.md § P57:
if perf regresses, file waiver + SURPRISES.md entry; do NOT silently downgrade.

Refs: QG-02, D4 parallel-migration, SIMPLIFY-10 (P60 hard-cut)
```

**Pre-commit checks:**
- `bash scripts/banned-words-lint.sh` — clean.
- `python3 -c "import ast; ast.parse(open('quality/runners/run.py').read())"` — valid Python.
- `python3 -c "import ast; ast.parse(open('quality/runners/verdict.py').read())"` — valid Python.
- `bash -n scripts/hooks/pre-push` — valid bash.
- Self-test (one more time with the reverted catalog): `python3 quality/runners/run.py --cadence pre-push && python3 quality/runners/verdict.py --phase 57` — runs without crashing; exit 1 expected.

**Push.** The new pre-push block runs against itself (recursive — pre-push hook invokes the runner which Wave B just shipped). Expected: NOT-VERIFIED warnings print, push proceeds (non-fatal).
  </action>
  <verify>
    <automated>git log --oneline -3 | head -3 && git log -1 --name-only --pretty=format: | head -25 && bash scripts/banned-words-lint.sh && python3 -c "import ast; ast.parse(open('quality/runners/run.py').read()); ast.parse(open('quality/runners/verdict.py').read())" && bash -n scripts/hooks/pre-push && grep -q "quality/reports/verifications" .gitignore && grep -q "quality/reports/verdicts" .gitignore && grep -q "quality/reports/badge.json" .gitignore</automated>
  </verify>
  <done>
2 commits land (or 1 combined): commit 1 ships 10 .gitkeep + 3 runner files + .gitignore update; commit 2 ships pre-push edit. `bash scripts/banned-words-lint.sh` clean. Both python files parse. `bash -n scripts/hooks/pre-push` clean. `.gitignore` excludes per-run artifacts. Self-test runs without crashing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog JSON → runner | Runner deserializes user-controlled JSON; risk of malformed catalog crashing pre-push. |
| runner → verifier subprocess | Runner shells out; risk of command injection if verifier paths/args contain shell metacharacters. |
| pre-push hook → developer git push | Adding a non-fatal block must NOT brick legitimate pushes during the migration window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-57-02-01 | Tampering | malformed catalog JSON | mitigate | Runner uses `json.load` with try/except; raises with file path; runner exits 1 (RED) but does NOT crash pre-push (the new block is non-fatal). |
| T-57-02-02 | Elevation of privilege | command injection via row.verifier.script | mitigate | Runner uses `subprocess.run([..., shell=False])`. Catalog rows are committed-via-PR (reviewed). The verifier path is treated as a path, not a shell string — `pathlib.Path` resolution + `shell=False` prevents injection even on hostile catalog input. |
| T-57-02-03 | Denial of service | runaway verifier (infinite loop) | mitigate | `subprocess.run(timeout=row.verifier.timeout_s)`. Default 30s; max 60s per current rows. TimeoutExpired → status FAIL + artifact `timed_out: true`. |
| T-57-02-04 | Information disclosure | artifact JSON leaks subprocess stdout/stderr | accept | Catalogs + verifiers are public; no secrets expected in stdout. If a future verifier needs to handle secrets, it must redact in its own output. |
| T-57-02-05 | Repudiation | runner mutates catalog status without audit trail | mitigate | Each artifact carries `ts: <RFC3339>`; the artifact is the audit. Catalog mutations are committed (or git-ignored — see Task E). |
| T-57-02-06 | Spoofing | n/a — no auth boundary | accept | Pure local-file-edit + subprocess work. No remote calls in Wave B (verifiers are stdlib + local commands). BADGE-01 row's verifier (HEAD URL check) ships in P60, not Wave B. |
</threat_model>

<verification>
After this plan ships:

1. `for d in code docs-build docs-repro release structure agent-ux perf security; do test -f quality/gates/$d/.gitkeep || exit 1; done` — exit 0.
2. `test -f quality/runners/run.py && test -f quality/runners/verdict.py && test -f quality/runners/__init__.py` — exit 0.
3. `python3 -c "import ast; ast.parse(open('quality/runners/run.py').read()); ast.parse(open('quality/runners/verdict.py').read())"` — exit 0.
4. `python3 quality/runners/run.py --help` — exits 0, mentions `--cadence`.
5. `python3 quality/runners/run.py --cadence pre-push` — runs without crashing; reports 7 NOT-VERIFIED + 1 WAIVED; exit 1.
6. `python3 quality/runners/verdict.py --phase 57` — writes `quality/reports/verdicts/p57/<ts>.md` + `quality/reports/badge.json`.
7. `python3 -c "import json; d=json.load(open('quality/reports/badge.json')); assert d['schemaVersion']==1 and d['label']=='quality gates' and d['color'] in ('brightgreen','yellow','red')"` — exit 0.
8. `grep -q "Quality Gates runner (parallel-migration window" scripts/hooks/pre-push && bash -n scripts/hooks/pre-push` — exit 0.
9. `bash scripts/banned-words-lint.sh` — exit 0.
10. Pre-push hook GREEN (cargo fmt + clippy clean — no rust changes; docs-site check skipped).
</verification>

<success_criteria>
The framework skeleton is in place and the runner can be invoked. Three downstream consumers can now do their work:

- **Wave C (PLAN 57-03)** ships the verifier scripts under `quality/gates/structure/freshness-invariants.py` + `banned-words.sh`. With Wave B's runner already in place, Wave C just drops in the verifiers and re-runs the runner — rows flip from NOT-VERIFIED to PASS/FAIL/PARTIAL.
- **Wave D (PLAN 57-04)** runs the runner against the QG-08 row to identify which historical sections need to move from `.planning/ROADMAP.md` to `.planning/milestones/v0.X.0-phases/ROADMAP.md`. The runner surfaces the FAIL; Wave D fixes it (POLISH-STRUCT).
- **Wave F (PLAN 57-06)** dispatches the unbiased verifier subagent which re-runs `python3 quality/runners/run.py --cadence pre-push` and `python3 quality/runners/verdict.py --phase 57` from a zero-context vantage. The verdict file from this self-run is the phase-close grade.

If Wave B is skipped or partial (e.g., the runner crashes on missing verifiers, or pre-push perf regresses without a documented waiver), Wave C cannot demonstrate progress through the runner, and Wave F has no functional dispatch target.

**Recurring criteria for this phase:**

1. **Catalog-first** — Wave B does NOT add catalog rows; it consumes Wave A's contract. The catalog-first rule was satisfied by Wave A's commit.
2. **CLAUDE.md update in same PR (QG-07)** — deferred to Wave F (final phase-close commit). Wave B's commits cite the QG-* requirement IDs but do not edit CLAUDE.md (lower-noise commit history).
3. **Verifier-subagent dispatch on phase close (QG-06)** — Wave F.
4. **SIMPLIFY absorption** — Wave B does not absorb scripts (that's Wave C — banned-words + end-state shrink). Wave B builds the framework that Wave C absorbs into.
5. **Fix every RED row** — Wave B's runner reveals that the entire P57 catalog is NOT-VERIFIED today (because verifiers don't exist). Wave C fixes that by shipping the verifiers; Wave D fixes the underlying drift in `.planning/ROADMAP.md` per POLISH-STRUCT.
</success_criteria>

<pivot_rules>
Per `.planning/research/v0.12.0-roadmap-and-rationale.md` § Phase 57:

> If pre-push perf regresses (>10s overhead from new runner + composition vs old script chain), keep `scripts/end-state.py` as pre-push entry, run new system in parallel for parity-checking, hard-cut in P63. **File waiver row + SURPRISES journal entry. Do NOT silently downgrade.**

**Wave B specifics:**

- **If `python3 quality/runners/run.py --cadence pre-push` adds >10s** to pre-push: keep the old chain as pre-push gate (Wave B already ships this — the new block is non-fatal). Add a row to `quality/catalogs/orphan-scripts.json` with `id: "perf/pre-push-runner-overhead"`, `reason: "runner added <X>s; investigation deferred to P60 hard-cut"`, `until: "2026-07-25T00:00:00Z"`. Append `2026-04-27 P57: pre-push runner overhead exceeded 10s budget; tracked in orphan-scripts.json` to `quality/SURPRISES.md`.
- **If runner crashes on Wave A catalog** (any row not handled by the not-found / WAIVED / subprocess paths): debug + fix in this same wave. Do NOT commit a crashing runner.
- **If verdict.py emits malformed badge.json**: P60 mkdocs publish will fail. Validate `python3 -c "import json; json.load(open('quality/reports/badge.json'))"` before commit.
- **If banned-words-lint.sh flags any new file** (e.g., docstrings using "replace"): rewrite using `migrate to` / `for the 80%` / `rewrite as` / `complement` / `alongside`. Append SURPRISES.md entry.

Append any pivot to `quality/SURPRISES.md` per PROTOCOL.md Step 9. Format: `YYYY-MM-DD P57: <obstacle> — <resolution>`.
</pivot_rules>

<risk_table>
| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| Runner crashes on missing verifier (Wave A's catalog points at not-yet-shipped Wave C scripts) | medium | high (would block Wave C development cycle) | Task 57-02-B explicitly handles `verifier-not-found` via `NOT-VERIFIED` status; self-test validates this path works |
| Pre-push perf overhead >10s | low | medium (would force pivot to P60 hard-cut earlier) | Wave B self-test measures <2s with no verifiers; Wave C verifiers add ~0.5s each at most (mechanical shell ops) — total budget <8s expected |
| `subprocess.run` shell-injection via row.verifier.script field | low | high (RCE in pre-push hook) | `shell=False` + `pathlib.Path` resolution; catalogs reviewed via PR |
| Catalog mutation during self-test commits dirty state | medium | low (annoying diff) | Task 57-02-E reverts catalog with `git checkout` before commit; Wave C overwrites for real |
| `.gitignore` rule too broad — accidentally ignores legitimate `.gitkeep` placeholders | low | medium (next agent confused by missing dirs in checkout) | Use `quality/reports/verifications/*/*.json` (slash-slash) not `quality/reports/verifications/**` so .gitkeep files are NOT ignored |
| Banned word in PROTOCOL.md → blocks pre-push | low | low | Wave A already greenlit; if Wave B adds a banned word in commit messages or runner docstrings, banned-words-lint catches before commit |
</risk_table>

<output>
After completion, create `.planning/phases/57-quality-gates-skeleton-structure-dimension/57-02-SUMMARY.md` with: commit SHAs (1 or 2), files changed (14 paths), self-test output (run.py + verdict.py against Wave A catalog), badge.json content, perf measurement (`time python3 quality/runners/run.py --cadence pre-push`), pre-push hook diff summary (block insertion location), and a one-line "ready for Wave C" handoff naming `quality/gates/structure/freshness-invariants.py + banned-words.sh + scripts/end-state.py shrink to ≤30-line shim`.
</output>
