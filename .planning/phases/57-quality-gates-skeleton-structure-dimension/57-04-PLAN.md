---
phase: 57-quality-gates-skeleton-structure-dimension
plan: 04
type: execute
wave: 4
depends_on:
  - "57-01"
  - "57-02"
  - "57-03"
files_modified:
  - .planning/ROADMAP.md
  - .planning/milestones/v0.11.0-phases/ROADMAP.md
  - .planning/milestones/v0.10.0-phases/ROADMAP.md
  - .planning/milestones/v0.9.0-phases/ROADMAP.md
autonomous: false
requirements:
  - POLISH-STRUCT
  - QG-08
must_haves:
  truths:
    - "After Wave D, `python3 quality/runners/run.py --cadence pre-push` produces ZERO RED rows in the structure dimension. Every catalog row in scope is PASS or WAIVED."
    - ".planning/ROADMAP.md contains exactly one '## v0.12.0 Quality Gates (PLANNING)' H2 section + an index paragraph linking to historical *-phases/ROADMAP.md files. Historical milestone H2 sections (v0.11.0, v0.10.0, v0.9.0) are MOVED OUT to per-milestone files inside *-phases/."
    - "Each historical section is preserved verbatim in its target *-phases/ROADMAP.md (no content loss). v0.10.0-phases/ROADMAP.md and v0.9.0-phases/ROADMAP.md ALREADY EXIST with curated archive content (per B-1 plan-check fix); v0.11.0-phases/ROADMAP.md is NEW (created in Task A from OLD top-level v0.11.0 section)."
    - "POLISH-STRUCT also audits the OTHER freshness invariants: any RED on no-version-pinned-filenames / install-pkg-mgr / benchmarks-in-mkdocs / no-loose-roadmap / no-orphan-docs gets fixed in this same wave (cite commit). If unfixable: 90d-TTL waiver + SURPRISES.md."
    - "User checkpoint: this is the ONE chunky move in P57 (~480 lines of ROADMAP.md content moves). Owner reviews before commit per checkpoint:human-verify."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Top-level ROADMAP scoped to active milestone (v0.12.0) + a historical-milestones index paragraph. Down from ~704 lines to ~250 lines (estimate)."
      contains: "## v0.12.0 Quality Gates"
      max_lines: 300
    - path: ".planning/milestones/v0.11.0-phases/ROADMAP.md"
      provides: "Per-milestone ROADMAP file holding the v0.11.0 phase entries (Phases 50-55) verbatim from the OLD top-level ROADMAP.md."
      contains: "Phase 50:"
    - path: ".planning/milestones/v0.10.0-phases/ROADMAP.md"
      provides: "Already exists per the milestone directory listing. Wave D verifies it carries Phases 40-45; if not, populates from the OLD top-level ROADMAP.md."
      contains: "Phase 40:"
    - path: ".planning/milestones/v0.9.0-phases/ROADMAP.md"
      provides: "Already exists per the milestone directory listing with 207 lines of curated archive content (SHIPPED 2026-04-24 marker; B-1 plan-check fix). Wave D verifies its content via unique-content grep + min line count; preserves verbatim. Do NOT overwrite."
      contains: "SHIPPED 2026-04-24"
  key_links:
    - from: "verify_top_level_requirements_roadmap_scope"
      to: ".planning/ROADMAP.md after Wave D"
      via: "Wave D's edit removes the historical H2 sections; the verifier re-runs and reports PASS"
      pattern: "## v0.12.0"
    - from: "OLD .planning/ROADMAP.md content"
      to: ".planning/milestones/v0.X.0-phases/ROADMAP.md"
      via: "Verbatim move (no content loss); each historical section becomes the body of its per-milestone file"
      pattern: "Phase \\d+:"
---

<objective>
Wave D — POLISH-STRUCT, the structure-dimension RED-fix pass per the **owner directive (recurring criterion #5)**: "I'm really hoping that after this milestone the codebase is pristine and high quality across all the dimensions." Wave C's runner surfaced RED rows; Wave D fixes them (or waives with TTL + SURPRISES entry).

**The dominant RED today: QG-08 on `.planning/ROADMAP.md`.** ROADMAP.md is currently ~704 lines, with these top-level H2 sections per `grep -n '^## ' .planning/ROADMAP.md`:
- `## Milestones` (line 3)
- `## Phases` (line 18)
- `## v0.12.0 Quality Gates (PLANNING)` (line 20) — KEEP
- `## v0.11.0 Polish & Reproducibility (PLANNING)` (line 200) — MOVE
- `## v0.10.0 Docs & Narrative Shine (PLANNING)` (line 313) — MOVE
- `## v0.9.0 Architecture Pivot — Git-Native Partial Clone` (line 445) — MOVE
- `## Backlog` (line 684) — KEEP (or move to `.planning/BACKLOG.md` if owner approves; default: keep)

Three historical milestone sections need to move to per-milestone `ROADMAP.md` files inside `.planning/milestones/v0.X.0-phases/`:
- v0.11.0 → `.planning/milestones/v0.11.0-phases/ROADMAP.md` (file does NOT exist; create).
- v0.10.0 → `.planning/milestones/v0.10.0-phases/ROADMAP.md` (**file ALREADY EXISTS** with curated archive content per `ls .planning/milestones/v0.10.0-phases/`; verify with W-1 unique-content marker `SHIPPED 2026-04-25` + min line count ≥90; preserve verbatim).
- v0.9.0 → `.planning/milestones/v0.9.0-phases/ROADMAP.md` (**file ALREADY EXISTS** with 207 curated archive lines per B-1 plan-check fix; verify with unique-content marker `SHIPPED 2026-04-24` + line count ≥90; preserve verbatim — do NOT overwrite).

**Move scope: ~480 lines of content move from top-level ROADMAP.md to per-milestone files. This is the chunky move flagged in the prompt.** Owner has explicitly approved P57 carrying it; pivot rule below covers what to do if it proves too chunky.

**Other RED audits.** Wave C's runner output flags any other RED rows. Wave D fixes them OR waives with 90d TTL + SURPRISES.md. Specifically check:
- `structure/no-version-pinned-filenames` — any new doc with a version in its filename outside CHANGELOG / *-phases/?
- `structure/install-leads-with-pkg-mgr-{docs-index,readme}` — Wave 4-A from P56 already verified `README.md` — should be PASS. `docs/index.md` should also be PASS per POLISH2 work. **If RED: fix the install order.**
- `structure/benchmarks-in-mkdocs-nav` — every `docs/benchmarks/*.md` in `mkdocs.yml`?
- `structure/no-loose-roadmap-or-requirements` — any loose `*ROADMAP*.md` / `*REQUIREMENTS*.md` outside `*-phases/` or `archive/`?
- `structure/no-orphan-docs` — `bash scripts/check-docs-site.sh` exits 0?

This wave has a `checkpoint:human-verify` task for the owner to review the ROADMAP.md move before commit (the move is the chunkiest single edit in P57; owner-on-the-loop sanity-check guards against accidental content loss).

**Plan is `autonomous: false`** because of the human-verify checkpoint.

Output: 4 files committed across 2-3 commits + 1 owner-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/v0.12.0-decisions-log.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-01-PLAN.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-03-PLAN.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-03-PARITY.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-03-SUMMARY.md
@quality/PROTOCOL.md
@quality/catalogs/freshness-invariants.json
@quality/reports/verifications/structure/top-level-requirements-roadmap-scope.json
@quality/runners/run.py
@quality/gates/structure/freshness-invariants.py
@CLAUDE.md

<interfaces>
**ROADMAP.md current top-level structure** (per `grep -n '^## '` at planning time):

```
3:  ## Milestones
18: ## Phases
20: ## v0.12.0 Quality Gates (PLANNING)        [KEEP — active]
200:## v0.11.0 Polish & Reproducibility (PLANNING)        [MOVE]
313:## v0.10.0 Docs & Narrative Shine (PLANNING)          [MOVE]
445:## v0.9.0 Architecture Pivot — Git-Native Partial Clone   [MOVE]
684:## Backlog                                          [KEEP]
```

**Move plan:**

1. **v0.11.0 section** (lines 200-312, ~112 lines): copy verbatim to `.planning/milestones/v0.11.0-phases/ROADMAP.md`. Add a 5-line preamble at the top of the new file explaining "this file extracted from .planning/ROADMAP.md on 2026-04-27 per QG-08 + POLISH-STRUCT (P57 Wave D); content is verbatim from the OLD top-level."

2. **v0.10.0 section** (lines 313-444, ~131 lines): the file `.planning/milestones/v0.10.0-phases/ROADMAP.md` already exists per `ls`. **Verify** its content matches the OLD top-level v0.10.0 section. If they differ:
   - If the existing file is MORE detailed (e.g., extracted earlier with additional notes): keep the existing file; remove the OLD top-level section; document divergence in commit message.
   - If the existing file is LESS detailed: replace its body with the OLD top-level content + the same 5-line preamble.
   - If they match: just remove the OLD top-level section.

3. **v0.9.0 section** (lines 445-683, ~238 lines): copy verbatim to `.planning/milestones/v0.9.0-phases/ROADMAP.md`. Same 5-line preamble.

4. **Top-level ROADMAP.md** (after the moves):
   - Lines 1-19: keep (`## Milestones` + `## Phases` headers).
   - Lines 20-199: keep (v0.12.0 active milestone).
   - REPLACE the historical sections with a single ~10-line `## Previously planned milestones` index block:
     ```markdown
     ## Previously planned milestones

     Per CLAUDE.md §0.5 / Workspace layout, each shipped/historical milestone's
     ROADMAP.md lives inside its `*-phases/` directory. Top-level ROADMAP.md
     holds ONLY the active milestone (currently v0.12.0) + this index.

     - **v0.11.0** Polish & Reproducibility — `.planning/milestones/v0.11.0-phases/ROADMAP.md` (Phases 50–55, SHIPPED 2026-04-25 → 2026-04-27).
     - **v0.10.0** Docs & Narrative Shine — `.planning/milestones/v0.10.0-phases/ROADMAP.md` (Phases 40–45, SHIPPED 2026-04-25).
     - **v0.9.0** Architecture Pivot — `.planning/milestones/v0.9.0-phases/ROADMAP.md` (Phases 31–36, SHIPPED 2026-04-24).
     - v0.8.0 and earlier — see `.planning/milestones/v0.X.0-phases/ARCHIVE.md` per the POLISH2-21 condensation.
     ```
   - Lines 684-704 (`## Backlog`): keep at the end.

   Final top-level ROADMAP.md target: ~210-250 lines (down from 704). The QG-08 verifier flips to PASS.

**5-line preamble for each new per-milestone ROADMAP.md:**

```markdown
# vX.Y.0 ROADMAP — [milestone name]

> Extracted from `.planning/ROADMAP.md` on 2026-04-27 per **QG-08 + POLISH-STRUCT** (P57 Wave D).
> Content below is verbatim from the OLD top-level ROADMAP.md historical section for vX.Y.0.
> Active-milestone ROADMAP work lives at `.planning/ROADMAP.md`; this file is historical.

---

[OLD top-level vX.Y.0 section content, verbatim]
```

**Avoid content loss.** The move is verbatim. After the move, `git diff` should show:
- 4 new lines + ~480 deleted lines from `.planning/ROADMAP.md`.
- ~485 new lines added to `.planning/milestones/v0.X.0-phases/ROADMAP.md` files.
- Net: roughly zero (some preamble overhead).

A 5-line preamble per new file × 3 files = 15 lines of overhead. Plus the new index paragraph at top-level (~10 lines). Total net delta: ~25 lines added.

**Side checks during the move:**

1. **Cross-references that pointed at the moved sections.** Run `grep -rn '\.planning/ROADMAP.md.*v0\.[0-9]\.[0-9]\|v0\.[0-9]\.[0-9].*\.planning/ROADMAP.md' --include='*.md'` to find docs that link into specific historical sections of the OLD top-level ROADMAP.md. If any are found: update the link to point at the new per-milestone file. Note: anchor links (`#phase-N`) may break — fix them too.

2. **`scripts/end-state.py` references.** The OLD bootstrap_claims had a `freshness/no-loose-roadmap-or-requirements` row checking `find .planning/milestones -maxdepth 2`. The new milestone ROADMAP.md files are INSIDE `*-phases/` dirs (depth 3+), so they don't trip the existing predicate. Sanity check: re-run `python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-roadmap-or-requirements` after the move; should still PASS.

3. **CLAUDE.md §0.5 convention.** The convention already exists in CLAUDE.md (lines 47-72 per the embedded `claudeMd` in this prompt). Wave D does NOT change CLAUDE.md (Wave F handles all CLAUDE.md updates per QG-07). Wave D's job is just to enforce the convention by moving content; the rule is already documented.

**Owner checkpoint** (the chunky-move sanity check):

After Tasks A + B + C land the per-milestone files and the top-level edit, the checkpoint task asks the owner to:
- `git diff .planning/ROADMAP.md` — confirm only the 3 historical sections + the new index block change; no other content drift.
- `git diff .planning/milestones/v0.11.0-phases/ROADMAP.md` (and v0.10.0, v0.9.0) — confirm content is verbatim from the OLD top-level (allowing for the 5-line preamble).
- `python3 quality/gates/structure/freshness-invariants.py --row-id structure/top-level-requirements-roadmap-scope` — confirm exits 0 (PASS).
- Approve the move OR escalate (e.g., "wait, the v0.10.0 file already had X — restore it; I need a different merge strategy").

**Pivot rule (chunky-move overage).** Per the prompt: "If POLISH-STRUCT fixes are too chunky (e.g., REQUIREMENTS.md scope split moves >2000 lines), do them as a separate sub-wave with explicit commit boundaries; if STILL too chunky, defer to a P57.5 or v0.12.1 with explicit waiver + tracked carry-forward."

Current move scope: ~480 lines of ROADMAP.md content + 3 new files. Well below 2000 lines, well within P57 scope. Proceed normally.

If during execution the move blows up (e.g., owner reviews and wants a different merge strategy for v0.10.0): pivot to a sub-wave structure (commit each milestone move separately), each with its own owner-verify, and append SURPRISES.md.

**Other RED audit checklist** (from `<objective>`):

For each row, run the verifier and inspect output:
```bash
for row_id in structure/no-version-pinned-filenames \
              structure/install-leads-with-pkg-mgr-docs-index \
              structure/install-leads-with-pkg-mgr-readme \
              structure/benchmarks-in-mkdocs-nav \
              structure/no-loose-roadmap-or-requirements \
              structure/no-orphan-docs ; do
  python3 quality/gates/structure/freshness-invariants.py --row-id "$row_id"
  echo "$row_id exit=$?"
done
```

For any RED:
- **Tier 1 — fixable in this wave:** fix the underlying drift, cite commit, the verifier flips to PASS automatically on next runner invocation.
- **Tier 2 — fixable but big:** if the fix is >100 LOC or touches another phase's scope, file a v0.12.1 carry-forward via MIGRATE-03; waive with 90d TTL.
- **Tier 3 — unfixable:** likely a verifier bug. Document in SURPRISES.md; consider a 90d-TTL waiver while the verifier is being debugged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 57-04-A: Create .planning/milestones/v0.11.0-phases/ROADMAP.md (verbatim move)</name>
  <files>.planning/milestones/v0.11.0-phases/ROADMAP.md</files>
  <action>
Read `.planning/ROADMAP.md` lines 200-312 (the v0.11.0 section + its `### Phase NN:` subsections). The exact line range is determined by `grep -n '^## ' .planning/ROADMAP.md`:
- Section starts at line 200 (`## v0.11.0 Polish & Reproducibility (PLANNING)`)
- Section ends at line 312 (the line BEFORE `## v0.10.0 Docs & Narrative Shine (PLANNING)` at 313).

Use `sed -n '200,312p' .planning/ROADMAP.md > /tmp/v0.11.0-section.md` to extract the exact slice.

Create `.planning/milestones/v0.11.0-phases/ROADMAP.md` with:

```markdown
# v0.11.0 ROADMAP — Polish & Reproducibility

> Extracted from `.planning/ROADMAP.md` on 2026-04-27 per **QG-08 + POLISH-STRUCT** (P57 Wave D).
> Content below is verbatim from the OLD top-level ROADMAP.md historical section for v0.11.0.
> Active-milestone ROADMAP work lives at `.planning/ROADMAP.md`; this file is historical.

---

<contents of /tmp/v0.11.0-section.md verbatim, EXCEPT the leading H2 line is downshifted to H1 in the preamble title above; instead of duplicating the H2, start the verbatim section at the FIRST `### Phase NN:` line>
```

**Concretely:** the new file's body after the `---` separator should start with the first `### Phase NN:` heading from the OLD section (likely `### Phase 50: Hygiene & Cleanup Wave (v0.11.0)`), preserving the H3-and-below hierarchy verbatim. Do NOT include the OLD `## v0.11.0 Polish & Reproducibility (PLANNING)` H2 in the body — that's superseded by the H1 in the preamble. All other content is preserved.

**Verbatim test:** after writing, `diff <(sed -n '200,312p' .planning/ROADMAP.md | tail -n +2) <(sed -n '7,$p' .planning/milestones/v0.11.0-phases/ROADMAP.md)` should show only minor differences (preamble-related). The phase entries themselves match line-for-line.

(Don't worry about getting the diff to be exactly empty; the goal is "no content was dropped or modified" — the preamble + H1 vs H2 are the expected differences.)

Sanity-check: `wc -l .planning/milestones/v0.11.0-phases/ROADMAP.md` should be ~115-120 lines (113 from extract + 7 preamble).

**Existing file check.** `.planning/milestones/v0.11.0-phases/REQUIREMENTS.md` already exists. The new `ROADMAP.md` should sit alongside it.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v0.11.0-phases/ROADMAP.md && grep -q "Phase 50" .planning/milestones/v0.11.0-phases/ROADMAP.md && grep -q "Phase 55" .planning/milestones/v0.11.0-phases/ROADMAP.md && grep -q "Extracted from" .planning/milestones/v0.11.0-phases/ROADMAP.md && lc=$(wc -l < .planning/milestones/v0.11.0-phases/ROADMAP.md) && [ "$lc" -ge 100 ] && [ "$lc" -le 200 ] && echo "v0.11.0 ROADMAP.md OK lines=$lc"</automated>
  </verify>
  <done>
File exists, contains Phase 50 + Phase 55 entries, has the "Extracted from" preamble, lines between 100-200 (verbatim move + 7-line preamble).
  </done>
</task>

<task type="auto">
  <name>Task 57-04-B: Verify or create .planning/milestones/v0.10.0-phases/ROADMAP.md</name>
  <files>.planning/milestones/v0.10.0-phases/ROADMAP.md</files>
  <action>
The file `.planning/milestones/v0.10.0-phases/ROADMAP.md` ALREADY EXISTS per `ls .planning/milestones/v0.10.0-phases/`. **Verify** before any edit:

1. Read the existing file.
2. Read OLD top-level ROADMAP.md lines 313-444 (the v0.10.0 section).
3. Compare:
   - **If the existing file already contains the OLD top-level v0.10.0 phase entries** (Phases 40-45) verbatim or substantially: ALL GOOD — do not touch the file. Wave D's responsibility is just removing the OLD top-level section in Task D.
   - **If the existing file is empty / stub / different content**: replace its body with the OLD top-level v0.10.0 section (verbatim) + the 5-line preamble.

**If a divergence is detected:** decide based on which is more authoritative:
- Existing file may have been written first (extracted earlier) and contain MORE detail, with the top-level being a later abbreviated version.
- OLD top-level may be more recent (still being maintained) while existing file is stale.

Default: **prefer the OLD top-level content** (more recent and verbose). If the existing file appears richer (rare), open a checkpoint asking the owner; otherwise overwrite.

**If the existing file is sound** (matches OLD top-level): no edit. Document the verification in 57-04-SUMMARY.md.

**If overwriting**: same preamble as Task A:

```markdown
# v0.10.0 ROADMAP — Docs & Narrative Shine

> Extracted from `.planning/ROADMAP.md` on 2026-04-27 per **QG-08 + POLISH-STRUCT** (P57 Wave D).
> Content below is verbatim from the OLD top-level ROADMAP.md historical section for v0.10.0.
> Active-milestone ROADMAP work lives at `.planning/ROADMAP.md`; this file is historical.

---

<verbatim content from OLD lines 313-444, starting at first `### Phase 40:` heading>
```

Sanity-check: `wc -l .planning/milestones/v0.10.0-phases/ROADMAP.md` should be ~135-150 lines after Wave D (131 from extract + 7 preamble + perhaps existing additional content).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v0.10.0-phases/ROADMAP.md && grep -q "Phase 40" .planning/milestones/v0.10.0-phases/ROADMAP.md && grep -q "Phase 45\|Phase 4[1-5]" .planning/milestones/v0.10.0-phases/ROADMAP.md && grep -q "SHIPPED 2026-04-25" .planning/milestones/v0.10.0-phases/ROADMAP.md && [ "$(wc -l < .planning/milestones/v0.10.0-phases/ROADMAP.md)" -ge 90 ] && echo "v0.10.0 ROADMAP.md OK (W-1 unique-content marker + min line count)"</automated>
  </verify>
  <done>
File exists, contains Phase 40 + at least one of Phase 41-45 entries; W-1 unique-content marker `SHIPPED 2026-04-25` is present; line count ≥90 (currently 99 per planning-time `ls -la`). If the file already existed with rich content, it was preserved; if stub-only or absent rich content, populated from OLD top-level.
  </done>
</task>

<task type="auto">
  <name>Task 57-04-C: Verify or populate .planning/milestones/v0.9.0-phases/ROADMAP.md (B-1 fix: file exists)</name>
  <files>.planning/milestones/v0.9.0-phases/ROADMAP.md</files>
  <action>
**B-1 plan-check fix.** The file `.planning/milestones/v0.9.0-phases/ROADMAP.md` ALREADY EXISTS with 207 lines of curated archive content (`SHIPPED 2026-04-24`, motivation, key design decisions, Phases 31–36). The original plan said `(file does NOT exist; create)` — that was wrong.

**Verify-before-edit, mirroring Task B's pattern:**

1. Read the existing file.
2. Run the unique-content marker check:
   ```bash
   if [ -f .planning/milestones/v0.9.0-phases/ROADMAP.md ] && \
      grep -q "SHIPPED 2026-04-24" .planning/milestones/v0.9.0-phases/ROADMAP.md && \
      [ "$(wc -l < .planning/milestones/v0.9.0-phases/ROADMAP.md)" -ge 90 ]; then
       echo "EXISTING archive content present — preserve verbatim, do NOT overwrite"
       PRESERVE=1
   else
       echo "Stub or missing — populate from OLD top-level v0.9.0 section"
       PRESERVE=0
   fi
   ```

3. **If `PRESERVE=1` (the expected case at planning time, since the file is 207 lines with the SHIPPED marker):**
   - Do NOT touch the file.
   - Wave D's Task D will REMOVE the OLD top-level v0.9.0 section from `.planning/ROADMAP.md` per the existing plan; the per-milestone file already holds the archive.
   - Document the preservation in 57-04-SUMMARY.md ("v0.9.0-phases/ROADMAP.md pre-existed with 207 curated archive lines; preserved verbatim per B-1").

4. **If `PRESERVE=0` (stub-only or absent):** populate from OLD top-level v0.9.0 section per the original (pre-fix) plan, with the 5-line preamble. This branch is unlikely at planning time but kept for resilience if someone reverts the file.

**The decision is symmetric with Task B's handling of v0.10.0-phases/ROADMAP.md.** Both pre-existing per-milestone files are preserved; Wave D's job is just removing the OLD top-level sections so QG-08 flips to PASS.

**Do NOT** run `sed -n '445,683p' .planning/ROADMAP.md > /tmp/v0.9.0-section.md` and overwrite — that would clobber the existing archive content.

Sanity-check: `wc -l .planning/milestones/v0.9.0-phases/ROADMAP.md` should be ≥90 (currently 207). The unique-content marker `SHIPPED 2026-04-24` should be present.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v0.9.0-phases/ROADMAP.md && grep -q "Phase 31" .planning/milestones/v0.9.0-phases/ROADMAP.md && grep -q "SHIPPED 2026-04-24" .planning/milestones/v0.9.0-phases/ROADMAP.md && lc=$(wc -l < .planning/milestones/v0.9.0-phases/ROADMAP.md) && [ "$lc" -ge 90 ] && echo "v0.9.0 ROADMAP.md preserved OK lines=$lc"</automated>
  </verify>
  <done>
File exists with the "SHIPPED 2026-04-24" unique-content marker AND ≥90 lines, contains Phase 31 reference. If pre-existing rich content was found, it was preserved verbatim per B-1; if stub-only, it was populated from OLD top-level.
  </done>
</task>

<task type="auto">
  <name>Task 57-04-D: Edit .planning/ROADMAP.md — remove historical sections, add index block</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Edit `.planning/ROADMAP.md` to remove lines 200-683 (the 3 historical milestone sections) and replace with the ~10-line `## Previously planned milestones` index block per `<interfaces>`.

**Sequence:**

1. Re-read the OLD line numbers via `grep -n '^## ' .planning/ROADMAP.md` (line numbers may have drifted slightly during planning; use the live numbers).
2. Construct the new content:
   - Lines 1-199 unchanged (active v0.12.0).
   - **Replace lines 200-683 with the index block** (per `<interfaces>`):

```markdown
## Previously planned milestones

Per CLAUDE.md §0.5 / Workspace layout, each shipped/historical milestone's
ROADMAP.md lives inside its `*-phases/` directory. Top-level ROADMAP.md
holds ONLY the active milestone (currently v0.12.0) + this index.

- **v0.11.0** Polish & Reproducibility — `.planning/milestones/v0.11.0-phases/ROADMAP.md` (Phases 50–55, SHIPPED 2026-04-25 → 2026-04-27).
- **v0.10.0** Docs & Narrative Shine — `.planning/milestones/v0.10.0-phases/ROADMAP.md` (Phases 40–45, SHIPPED 2026-04-25).
- **v0.9.0** Architecture Pivot — `.planning/milestones/v0.9.0-phases/ROADMAP.md` (Phases 31–36, SHIPPED 2026-04-24).
- v0.8.0 and earlier — see `.planning/milestones/v0.X.0-phases/ARCHIVE.md` per the POLISH2-21 condensation (8 archives, v0.1.0 → v0.8.0).
```

   - Lines 684+ (`## Backlog` section onward) preserved verbatim.

3. Final structure:
```
[Lines 1-19] ## Milestones / ## Phases (preamble)
[Lines 20-199] ## v0.12.0 Quality Gates (PLANNING) — active; ALL phases 56-63 here
[NEW ~10 lines] ## Previously planned milestones — index pointing at *-phases/ROADMAP.md
[Original lines 684+, now starting at ~210] ## Backlog
```

Expected line count after edit: ~220-240 lines (was 704). The QG-08 verifier should now PASS on ROADMAP.md.

**Use `sed` or Python script** for the deletion + insertion (NOT manual hand-editing — too risky for 480-line move). Recommendation:

```bash
# Backup
cp .planning/ROADMAP.md /tmp/ROADMAP.md.bak

# Find the exact line where v0.11.0 section starts and Backlog section starts
v11_start=$(grep -n '^## v0\.11\.0 ' .planning/ROADMAP.md | head -1 | cut -d: -f1)
backlog_start=$(grep -n '^## Backlog' .planning/ROADMAP.md | head -1 | cut -d: -f1)

# Construct the new file via head + index block + tail
{
  head -n $((v11_start - 1)) .planning/ROADMAP.md
  cat <<'EOF'
## Previously planned milestones

Per CLAUDE.md §0.5 / Workspace layout, each shipped/historical milestone's
ROADMAP.md lives inside its `*-phases/` directory. Top-level ROADMAP.md
holds ONLY the active milestone (currently v0.12.0) + this index.

- **v0.11.0** Polish & Reproducibility — `.planning/milestones/v0.11.0-phases/ROADMAP.md` (Phases 50–55, SHIPPED 2026-04-25 → 2026-04-27).
- **v0.10.0** Docs & Narrative Shine — `.planning/milestones/v0.10.0-phases/ROADMAP.md` (Phases 40–45, SHIPPED 2026-04-25).
- **v0.9.0** Architecture Pivot — `.planning/milestones/v0.9.0-phases/ROADMAP.md` (Phases 31–36, SHIPPED 2026-04-24).
- v0.8.0 and earlier — see `.planning/milestones/v0.X.0-phases/ARCHIVE.md` per the POLISH2-21 condensation (8 archives, v0.1.0 → v0.8.0).

EOF
  tail -n +$backlog_start .planning/ROADMAP.md
} > /tmp/ROADMAP.md.new

# Sanity check before overwriting
diff /tmp/ROADMAP.md.bak /tmp/ROADMAP.md.new | head -20  # should show 480-line removal + 10-line addition
mv /tmp/ROADMAP.md.new .planning/ROADMAP.md
```

**Sanity checks after edit:**
- `wc -l .planning/ROADMAP.md` returns ≤300.
- `grep -c '^## v0\.[0-9]' .planning/ROADMAP.md` returns 1 (just `## v0.12.0`).
- `grep -c '^### Phase' .planning/ROADMAP.md` returns ~15-20 (just v0.12.0 phases + backlog phase 999.1, NOT the 18+ historical phases).
- **W-6: Backlog line preservation.** `[ "$(grep -c '^## Backlog' .planning/ROADMAP.md)" -eq 1 ]` returns 0 (exactly one `## Backlog` heading; the move did not duplicate or drop it).
- `python3 quality/gates/structure/freshness-invariants.py --row-id structure/top-level-requirements-roadmap-scope` exits 0.

**Cross-reference fix.** Run `grep -rn '\.planning/ROADMAP\.md' --include='*.md' --include='*.py' --include='*.sh' --include='*.toml' --include='*.json'` and inspect the hits. Any link that points specifically at a v0.11.0/v0.10.0/v0.9.0 section (e.g., `.planning/ROADMAP.md#phase-50`) needs updating to the new per-milestone file. Most references will be generic (`.planning/ROADMAP.md` → still valid); only deep links break. Document any updates in the commit message.
  </action>
  <verify>
    <automated>lc=$(wc -l < .planning/ROADMAP.md) && [ "$lc" -le 300 ] && [ "$(grep -c '^## v0\.[0-9]' .planning/ROADMAP.md)" -eq 1 ] && grep -q "## Previously planned milestones" .planning/ROADMAP.md && [ "$(grep -c '^## Backlog' .planning/ROADMAP.md)" -eq 1 ] && python3 quality/gates/structure/freshness-invariants.py --row-id structure/top-level-requirements-roadmap-scope; echo "QG-08 exit=$?"</automated>
  </verify>
  <done>
`.planning/ROADMAP.md` is ≤300 lines, contains exactly one `## v0.[0-9]` H2 section (v0.12.0), has the new index block, preserves `## Backlog` section. The QG-08 verifier exits 0 (PASS).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 57-04-E: Owner verifies the chunky ROADMAP.md move</name>
  <what-built>
Wave D moved ~480 lines of content from `.planning/ROADMAP.md` to 3 per-milestone files inside `.planning/milestones/v0.X.0-phases/ROADMAP.md`. The owner reviews before commit because:
- This is the chunkiest single edit in P57.
- Content loss would be hard to recover from once committed.
- The `.planning/milestones/v0.10.0-phases/ROADMAP.md` file pre-existed; verify Wave D's handling preserved it correctly.
- The Tasks A-D ran in sequence; Wave D's PARITY against the QG-08 verifier flips RED→PASS only after this final task closes.
  </what-built>
  <how-to-verify>
1. **Inspect the diffs:**
   ```bash
   git diff .planning/ROADMAP.md | head -50
   git diff .planning/ROADMAP.md | tail -50
   git diff --stat .planning/milestones/
   ```
   Expected: top-level ROADMAP.md drops ~480 lines + adds ~10 (index block). Per-milestone files added ~480 lines combined (preamble + verbatim section).

2. **Verify content integrity** (no accidental edits to historical phases):
   ```bash
   git show HEAD:.planning/ROADMAP.md | sed -n '/^## v0\.11\.0/,/^## v0\.10\.0/p' > /tmp/old-v11.md
   diff /tmp/old-v11.md <(tail -n +7 .planning/milestones/v0.11.0-phases/ROADMAP.md) | head -20
   ```
   (Adjust the diff to skip the H2-vs-H1 difference; the rest should be empty or trivial whitespace.)

3. **Run the QG-08 verifier:**
   ```bash
   python3 quality/gates/structure/freshness-invariants.py --row-id structure/top-level-requirements-roadmap-scope
   echo "exit=$?"  # expected: 0
   cat quality/reports/verifications/structure/top-level-requirements-roadmap-scope.json | python3 -m json.tool | head -20
   ```
   Expected: `asserts_passed` lists both REQUIREMENTS.md + ROADMAP.md as clean; `asserts_failed` is empty; exit 0.

4. **Run the full structure-dim runner:**
   ```bash
   python3 quality/runners/run.py --cadence pre-push
   ```
   Expected: 9 rows in scope (post-Wave-C). 8 PASS (6 freshness + banned-words + QG-08) + 1 WAIVED (BADGE-01). Exit 0.

5. **Cross-reference fix audit** — Wave D's edit might have broken any deep-link to the OLD ROADMAP.md sections. Run:
   ```bash
   grep -rn '\.planning/ROADMAP\.md#' --include='*.md' --include='*.py' --include='*.sh' || echo "no deep links — clean"
   ```
   If any matches: discuss whether Wave D should patch them or defer to a follow-up.

6. **Decide:**
   - **Approve:** type "approved" → Wave D proceeds to Task F (commit + Other-RED audit).
   - **Issue:** describe the issue (e.g., "v0.10.0 file pre-existed and Wave D overwrote it incorrectly — restore from `git show HEAD~3:.planning/milestones/v0.10.0-phases/ROADMAP.md`").
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 57-04-F: Other-RED audit + commit Wave D</name>
  <files>.planning/ROADMAP.md, .planning/milestones/v0.11.0-phases/ROADMAP.md, .planning/milestones/v0.10.0-phases/ROADMAP.md, .planning/milestones/v0.9.0-phases/ROADMAP.md, quality/SURPRISES.md (if any RED found below)</files>
  <action>
**Step 1 — Other-RED audit.** Run each of the OTHER 6 freshness verifiers + banned-words and record results:

```bash
for row_id in structure/no-version-pinned-filenames \
              structure/install-leads-with-pkg-mgr-docs-index \
              structure/install-leads-with-pkg-mgr-readme \
              structure/benchmarks-in-mkdocs-nav \
              structure/no-loose-roadmap-or-requirements \
              structure/no-orphan-docs \
              structure/banned-words ; do
  python3 quality/gates/structure/freshness-invariants.py --row-id "$row_id" 2>/dev/null
  echo "$row_id exit=$?"
done
```

(Note: `structure/banned-words` dispatches to the wrapper script, not the python module — but the `freshness-invariants.py` dispatch should handle it OR the runner does. If the python dispatch table doesn't include `structure/banned-words`, just run `bash quality/gates/structure/banned-words.sh; echo "banned-words exit=$?"`.)

**Step 2 — For each row that exits non-zero (RED):**

- **Tier 1 (fixable, small):** fix the underlying drift, cite commit. Re-run verifier; should now PASS.
- **Tier 2 (fixable, big):** if fix is >100 LOC or touches another phase: file v0.12.1 carry-forward via MIGRATE-03 update; waive with 90d TTL on the row in `quality/catalogs/freshness-invariants.json` (set `waiver: {until: "2026-07-25T00:00:00Z", reason: "...", dimension_owner: "...", tracked_in: "MIGRATE-03"}`). Append SURPRISES.md.
- **Tier 3 (verifier bug):** likely a regex / path issue in the verifier code. Document in SURPRISES.md with the specific failure mode; fix in this same wave OR file 90d-TTL waiver while debugging.

**Expected state.** Per `.planning/STATE.md` last_activity, P56 SHIPPED with all 5 install paths verified; the freshness invariants for `install-leads-with-pkg-mgr-{readme,docs-index}` should be PASS already (POLISH2 work). The other freshness rows should also be PASS per the existing `scripts/end-state.py verdict` GREEN history. **Most likely outcome: all 6 freshness + banned-words PASS in this audit; only QG-08 was RED, fixed in Tasks A-E.**

If everything passes: skip to Step 3 (commit). If anything is RED: fix in Tier 1/2/3 framework, document, commit.

**Step 3 — Commit Wave D.**

Multiple commits depending on what ran in this wave:

**Commit 1** (Tasks A-D — the chunky ROADMAP.md move):

```
docs(p57): POLISH-STRUCT — move historical ROADMAP sections to *-phases/ (QG-08 fix)

Wave C surfaced QG-08 RED on .planning/ROADMAP.md — top-level still
contained v0.11.0/v0.10.0/v0.9.0 historical sections (~480 lines).
Per CLAUDE.md §0.5 / Workspace layout, each shipped milestone's ROADMAP.md
lives inside its *-phases/ directory.

Files:
- .planning/ROADMAP.md — historical sections REMOVED; new
  ## Previously planned milestones index block added pointing at the
  per-milestone files. Active milestone v0.12.0 unchanged. ## Backlog
  preserved.
- .planning/milestones/v0.11.0-phases/ROADMAP.md (NEW) — verbatim move
  of OLD lines 200-312 (Phases 50-55).
- .planning/milestones/v0.10.0-phases/ROADMAP.md — verified existing
  content; <preserved as-is | populated from OLD lines 313-444>.
- .planning/milestones/v0.9.0-phases/ROADMAP.md (NEW) — verbatim move
  of OLD lines 445-683 (Phases 27-36 incl. SUPERSEDED Phase 30).

Top-level ROADMAP.md: 704 → ~230 lines.
QG-08 verifier (structure/top-level-requirements-roadmap-scope) flips
RED → PASS:
  asserts_passed:
    - .planning/REQUIREMENTS.md scope is clean (no v0.8/9/10/11 H2 sections)
    - .planning/ROADMAP.md scope is clean (no historical milestone H2 sections)
  asserts_failed: []

Refs: POLISH-STRUCT, QG-08
```

**Commit 2** (if Other-RED audit found anything fixable; else skip):

```
fix(p57): POLISH-STRUCT — fix structure-dim RED on <row_id>

<short description of the fix>. Verifier flips RED → PASS:
  exit 0; asserts_passed: [...]

Refs: POLISH-STRUCT
```

**Commit 3** (if waivers / SURPRISES.md entries needed):

```
docs(p57): POLISH-STRUCT — waive <row_id> until <date> (tier 2 deferral)

<row_id>'s fix is >100 LOC / touches another phase / etc.; deferred via
90d-TTL waiver. SURPRISES.md updated. MIGRATE-03 carry-forward updated.

Refs: POLISH-STRUCT, MIGRATE-03 (v0.12.1 carry-forward)
```

**Pre-commit:**
- `bash scripts/banned-words-lint.sh` — clean.
- `python3 quality/runners/run.py --cadence pre-push` — exit 0 (every P0+P1 row PASS or WAIVED). If still RED on something other than QG-08: address per Step 2.
- `python3 quality/runners/verdict.py --phase 57` — verdict written; check the markdown.

**Push.** Pre-push hook fires:
- New runner block (non-fatal): expects all rows PASS/WAIVED → no warning.
- Old shim (`scripts/end-state.py verdict`): delegates to verdict.py → same outcome.
- cargo fmt + clippy: clean (no Rust changes).
- check-docs-site: clean (no docs/ changes).
- check-mermaid-renders: clean (no docs/ changes).
- credential scan: clean.

Push GREEN.
  </action>
  <verify>
    <automated>git log --oneline -3 | head -3 && python3 quality/runners/run.py --cadence pre-push; echo "runner exit=$?" && python3 quality/runners/verdict.py --phase 57; echo "verdict exit=$?" && [ "$(grep -c '^## v0\.[0-9]' .planning/ROADMAP.md)" -eq 1 ] && bash scripts/banned-words-lint.sh && echo "Wave D commits OK"</automated>
  </verify>
  <done>
1-3 commits land. `.planning/ROADMAP.md` ≤300 lines with only v0.12.0 H2; per-milestone ROADMAP.md files exist with verbatim historical content. Runner exits 0 on `--cadence pre-push` (every P0+P1 row PASS or WAIVED). QG-08 RED is fixed. Other-RED audit completed: any RED found is either fixed (cite commit) or waived with 90d TTL + SURPRISES.md entry. `bash scripts/banned-words-lint.sh` clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical milestone content → per-milestone files | Wave D moves ~480 lines verbatim; risk of accidental content loss / typo introduction. |
| QG-08 verifier ↔ Wave D edit | The verifier MUST flip RED → PASS after the edit; if not, Wave D's work didn't actually fix the contract. |
| owner approval gate | Owner reviews the move before commit; protects against unrecoverable misbehavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-57-04-01 | Tampering | accidental content drift during sed/script-driven move | mitigate | Tasks A-C use `sed -n` extraction (line-bounded) + verbatim copy; Task E owner-verify checkpoint inspects `git diff` before commit. |
| T-57-04-02 | Repudiation | "Where did this content go?" — an agent in 6 months can't find Phase 50 | mitigate | Top-level ROADMAP.md `## Previously planned milestones` index block names every per-milestone file with a relative link. The 5-line preamble at the top of each per-milestone file documents the extraction date + cause. |
| T-57-04-03 | Information disclosure | n/a — content is public | accept | All planning docs are public-by-design. |
| T-57-04-04 | Denial of service | a partial move leaves ROADMAP.md in inconsistent state mid-PR | mitigate | The whole move is one or two commits; if Task D finishes mid-edit, `git status` is dirty until Task F commits. The owner-verify checkpoint sits BEFORE the commit (not after) so an aborted run leaves the working tree dirty + recoverable, not committed-broken. |
| T-57-04-05 | Repudiation | broken deep links to OLD ROADMAP.md sections | mitigate | Task D Step "Cross-reference fix" greps for deep links; any are patched in the same commit or documented in commit message. |
| T-57-04-06 | Spoofing | n/a | accept | Pure file-edit work. |
| T-57-04-07 | Tampering | a Wave D commit accidentally touches an unrelated file | mitigate | Task F pre-commit step inspects `git status` before staging; only the 4 ROADMAP.md files (+ SURPRISES.md if RED found) should be in the commit. |
</threat_model>

<verification>
After this plan ships:

1. `wc -l .planning/ROADMAP.md` returns ≤300.
2. `grep -c '^## v0\.[0-9]' .planning/ROADMAP.md` returns 1 (only v0.12.0).
3. `grep -q '## Previously planned milestones' .planning/ROADMAP.md` succeeds.
4. `grep -q '## Backlog' .planning/ROADMAP.md` succeeds (preserved).
5. `test -f .planning/milestones/v0.11.0-phases/ROADMAP.md && grep -q "Phase 50" .planning/milestones/v0.11.0-phases/ROADMAP.md` — exit 0.
6. `test -f .planning/milestones/v0.10.0-phases/ROADMAP.md && grep -q "Phase 40" .planning/milestones/v0.10.0-phases/ROADMAP.md` — exit 0.
7. `test -f .planning/milestones/v0.9.0-phases/ROADMAP.md && grep -q "Phase 31" .planning/milestones/v0.9.0-phases/ROADMAP.md` — exit 0.
8. `python3 quality/gates/structure/freshness-invariants.py --row-id structure/top-level-requirements-roadmap-scope` — exit 0 (QG-08 PASS).
9. `python3 quality/runners/run.py --cadence pre-push` — exit 0 (every P0+P1 row PASS or WAIVED).
10. `python3 quality/runners/verdict.py --phase 57` — verdict GREEN; markdown emitted; badge.json `color: brightgreen`.
11. `bash scripts/banned-words-lint.sh` — exit 0 (no banned words leaked into Wave D files).
12. Other-RED audit: every freshness row + banned-words row exits 0 (PASS), OR any RED is documented in SURPRISES.md with a 90d-TTL waiver in the catalog.
</verification>

<success_criteria>
After Wave D, the structure dimension is ALL-GREEN-OR-WAIVED. The QG-08 contract is enforced by code (a future agent who adds `## v0.13.0` to top-level ROADMAP.md trips the verifier).

**Recurring criteria for this phase:**

1. **Catalog-first** — Wave D consumes Wave A's catalog (no new rows added in this wave; the QG-08 row drove the work).
2. **CLAUDE.md update in same PR (QG-07)** — deferred to Wave F.
3. **Verifier-subagent dispatch on phase close (QG-06)** — Wave F.
4. **SIMPLIFY absorption** — N/A in Wave D (closures happened in Waves A + C).
5. **Fix every RED row** — **THIS IS WAVE D'S CORE MANDATE.** Per the recurring criterion #5: "the milestone is not about instrumenting the codebase — it is about leaving the codebase pristine." Wave D fixes the QG-08 RED + audits all other freshness rows. Every RED at end of Wave D is either FIXED (cite commit), WAIVED (90d TTL + SURPRISES.md + MIGRATE-03), or escalated to the owner.

**Wave F handoff (next):** Wave F dispatches the unbiased verifier subagent which re-runs the runner from a zero-context vantage. Expected verdict: GREEN. Wave F also lands the CLAUDE.md update (QG-07) covering the new dimension/cadence/kind taxonomy + meta-rule extension.

**If Wave D's checkpoint goes RED** (owner identifies content loss, regression in v0.10.0 file, broken deep link Wave D didn't catch): Wave D pivots to a sub-wave structure (per the chunky-move pivot rule). Each sub-wave commits one milestone move + its own owner-verify, with explicit SURPRISES.md entries.
</success_criteria>

<pivot_rules>
**Wave D pivot rules:**

- **If the Wave D ROADMAP.md edit accidentally drops content** (Task E owner-verify catches): revert via `git checkout .planning/ROADMAP.md`. Re-run Tasks A-D one section at a time, with owner-verify after each. Append `2026-04-27 P57: ROADMAP.md move dropped content on first attempt — pivot to one-section-per-commit with owner-verify` to SURPRISES.md.
- **If Other-RED audit finds RED on multiple freshness rows beyond QG-08**: each is individually scoped (Tier 1/2/3 per `<interfaces>`). Tier 2 / Tier 3 use waivers + SURPRISES.md per the wave's design. Do NOT silently downgrade.
- **If `.planning/milestones/v0.10.0-phases/ROADMAP.md` already had richer content than the OLD top-level**: Task B documents the divergence + preserves the existing file. Do NOT overwrite without owner approval.
- **If a deep link grep finds many broken links** (>10): scope them as a follow-up commit (still in Wave D scope) OR escalate to the owner if any are user-facing (e.g., docs/index.md links to a specific Phase). Append SURPRISES.md.
- **If the chunky move proves too chunky in practice** (e.g., Task A alone takes >2h or trips multiple owner-verifies): pivot to deferring v0.9.0 (the largest section, ~238 lines) to a P57.5 mini-phase with explicit waiver. Update MIGRATE-03 to add this carry-forward. Append SURPRISES.md. The QG-08 verifier MUST be GREEN by Wave D close — if v0.9.0 deferral leaves it RED, the deferral approach won't work; rewrite the QG-08 expected.asserts to allow partial scope (e.g., "no v0.10/11 sections" only, with v0.9.0 explicitly waived).

Append any pivot to `quality/SURPRISES.md` per PROTOCOL.md Step 9.
</pivot_rules>

<risk_table>
| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| Accidental content drift during 480-line move | medium | high (ROADMAP.md is canonical historical record) | Tasks A-C use line-bounded `sed -n`; Task E owner-verify checkpoint inspects diff before commit |
| `.planning/milestones/v0.10.0-phases/ROADMAP.md` pre-existing content gets clobbered | medium | medium (loss of curated extraction) | Task B explicitly checks existing content first; preserves richer existing file |
| QG-08 verifier asserts don't actually flip after move (regex bug, edge case) | low | high (Wave D would re-RED) | Verifier was tested in Wave C; Task E checkpoint re-runs after move and reports exit code |
| Deep links to OLD `.planning/ROADMAP.md#phase-50` break docs/index.md or other user-facing pages | low | medium | Task D step grep finds them; fix in same commit or document |
| Other-RED audit finds many RED rows that are individually small but combine to a chunky fix | medium | medium | Tier 2 / Tier 3 framework allows waivers with TTL; per-row SURPRISES.md; MIGRATE-03 carry-forward updates |
| Owner unavailable for checkpoint approval | low | medium (Wave D blocks) | Plan is autonomous: false; agent waits for owner approval. If owner is offline >1h, append SURPRISES.md "stuck on owner-verify" + STOP per PROTOCOL.md "When stuck rules" |
| The 5-line preamble inadvertently changes the rendered nav structure of an mkdocs site (if the per-milestone files were ever included in nav) | low | low | The per-milestone files are NOT in mkdocs.yml nav; they are planning docs only |
</risk_table>

<output>
After completion, create `.planning/phases/57-quality-gates-skeleton-structure-dimension/57-04-SUMMARY.md` with: commit SHAs (1-3), files changed (4 ROADMAP files + optional SURPRISES.md), `wc -l` for top-level ROADMAP.md (before/after), the QG-08 verifier output (asserts_passed list), the Other-RED audit results (status per row), the owner-verify checkpoint approval timestamp + signal, and a one-line "ready for Wave E" handoff naming SIMPLIFY-03 audit closure status (Wave A already shipped the boundary doc; Wave E confirms `scripts/catalog.py` stays in place + verifies SIMPLIFY-03 row in catalog if any).
</output>
