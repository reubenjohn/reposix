---
phase: 57-quality-gates-skeleton-structure-dimension
plan: 06
type: execute
wave: 6
depends_on:
  - "57-01"
  - "57-02"
  - "57-03"
  - "57-04"
  - "57-05"
files_modified:
  - CLAUDE.md
  - quality/SURPRISES.md
  - .planning/STATE.md
  - quality/reports/verdicts/p57/VERDICT.md
autonomous: false
requirements:
  - QG-06
  - QG-07
must_haves:
  truths:
    - "CLAUDE.md gains a new section 'v0.12.0 Quality Gates — dimension/cadence/kind taxonomy' (or similar) documenting: the 8 dimensions, the 6 cadences, the 5 kinds, the catalog-first rule, the unbiased verifier subagent dispatch, the meta-rule extension ('fix the issue, update CLAUDE.md, AND tag the dimension'), and cross-references to quality/PROTOCOL.md."
    - "quality/SURPRISES.md gains any P57 entries that arose during Waves B/C/D/E (parity divergences, chunky-move pivots, audit findings). The 5 P56 entries + Ownership block from Wave A are preserved verbatim."
    - ".planning/STATE.md cursor advances from 'P56 SHIPPED' to 'P57 SHIPPED — Quality Gates skeleton + structure dimension landed'. The Roadmap-Evolution entry is appended (NOT a rewrite)."
    - "An unbiased verifier subagent is dispatched per QG-06 with the prompt template from quality/PROTOCOL.md § 'Verifier subagent prompt template'. The verdict lands at quality/reports/verdicts/p57/VERDICT.md. Phase does NOT close on RED."
    - "Phase-close commit lands all 3 docs updates + the verdict file. CLAUDE.md update is in the SAME PR per QG-07."
  artifacts:
    - path: "CLAUDE.md"
      provides: "New section appended with: dimension/cadence/kind taxonomy table, the catalog-first / verifier-subagent / mandatory-CLAUDE-update rules from PROTOCOL.md, the meta-rule extension. Cross-references to quality/PROTOCOL.md (NOT duplicating runtime detail per anti-bloat). Optional minor edit to existing 'Subagent delegation rules' section adding QG-06 verifier pattern."
      contains: "dimension/cadence/kind"
    - path: "quality/SURPRISES.md"
      provides: "Append any P57 entries collected from Waves B/C/D/E (likely 0-3 entries: parity divergence if any, chunky-move pivot if any, audit-discovery if any). 5 P56 entries + ownership block preserved."
      contains: "P57"
      max_lines: 200
    - path: ".planning/STATE.md"
      provides: "Roadmap-Evolution entry appended for P57 SHIPPED. Current Position cursor advances to P58. Last activity timestamp updated."
      contains: "P57 SHIPPED"
    - path: "quality/reports/verdicts/p57/VERDICT.md"
      provides: "Unbiased verifier subagent verdict (or in-session verdict with disclosure per P56 precedent). Top-line GREEN/RED, per-row table, QG-07 + QG-08 closure citations, recommendation."
      contains: "GREEN\\|RED"
  key_links:
    - from: "CLAUDE.md (new Quality Gates section)"
      to: "quality/PROTOCOL.md"
      via: "cross-reference; CLAUDE.md does NOT duplicate runtime detail per anti-bloat rule"
      pattern: "quality/PROTOCOL.md"
    - from: "verifier subagent dispatch"
      to: "quality/reports/verdicts/p57/VERDICT.md"
      via: "Task tool dispatch with the prompt from PROTOCOL.md § verifier-subagent-prompt-template; subagent writes the verdict file"
      pattern: "ZERO session context"
    - from: ".planning/STATE.md"
      to: "P58 agent (next session)"
      via: "Cursor advance + Roadmap-Evolution entry tells the next agent P57 GREEN + lists what they read at start of P58"
      pattern: "P57 SHIPPED"
---

<objective>
Wave F — phase close. Land the CLAUDE.md update (QG-07), append any P57 SURPRISES entries (QG-05 maintenance), advance the STATE.md cursor, and dispatch the unbiased verifier subagent (QG-06) whose verdict at `quality/reports/verdicts/p57/VERDICT.md` is the P57 phase-close grade.

**Per `.planning/research/v0.12.0-autonomous-execution-protocol.md` Steps 5-9:**
- Step 5: CLAUDE.md update is MANDATORY in the same PR. Wave F lands it.
- Step 6: Run the verifier (`quality/runners/run.py --cadence pre-push` + `verdict.py --phase 57`). Wave D already showed all rows GREEN; Wave F re-runs to confirm.
- Step 7: Dispatch unbiased verifier subagent. Per the P56 precedent, the dispatch may need to fall back to "verifier-context disclosure" if the executing agent's toolset doesn't include Task dispatch — see P56's verdict for the pattern.
- Step 8: Update STATE.md.
- Step 9: Append SURPRISES.md if any P57 wave pivoted.

**Plan is `autonomous: false`** because the verifier subagent dispatch is the human-or-checkpoint moment — the orchestrator (or owner) initiates the dispatch; the executor confirms the verdict.

Output: 4 files committed in 1-2 commits + verifier dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.12.0-autonomous-execution-protocol.md
@.planning/research/v0.12.0-decisions-log.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-01-PLAN.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-01-SUMMARY.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-02-SUMMARY.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-03-SUMMARY.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-03-PARITY.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-04-SUMMARY.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-05-SUMMARY.md
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-05-SIMPLIFY-03-AUDIT.md
@.planning/verifications/p56/VERDICT.md
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/catalogs/README.md
@quality/catalogs/freshness-invariants.json
@quality/runners/run.py
@quality/runners/verdict.py
@CLAUDE.md

<interfaces>
**CLAUDE.md update — what to add (per QG-07 + the prompt's Wave F notes):**

A new section appended to CLAUDE.md. Anti-bloat per PROTOCOL.md §"Anti-bloat rules per surface":
- Append a paragraph + cross-reference; do NOT rewrite existing sections.
- Cross-reference `quality/PROTOCOL.md` for runtime detail rather than duplicating.
- The new section sits AFTER the existing "v0.12.0 Quality Gates — phase log" section that P56 added (per `.planning/verifications/p56/VERDICT.md` "QG-07 — CLAUDE.md update" section confirming P56's CLAUDE.md update).

**Recommended new section content** (≤80 lines added to CLAUDE.md; cross-reference-heavy):

```markdown
## Quality Gates — dimension/cadence/kind taxonomy (added P57)

The v0.12.0 Quality Gates framework lives at `quality/`. Runtime contract:
`quality/PROTOCOL.md`. Every quality check (gate) sits at one
`(dimension, cadence, kind)` coordinate.

**8 dimensions** — the regression classes the project has:

| Dimension | Checks | Home |
|---|---|---|
| code | clippy, fmt, cargo nextest | `quality/gates/code/` (P58) |
| docs-build | mkdocs strict, mermaid renders, link resolve, badges resolve | `quality/gates/docs-build/` (P60) |
| docs-repro | snippet extract, container rehearse, tutorial replay | `quality/gates/docs-repro/` (P59) |
| release | gh assets present, brew formula current, crates.io max version, installer bytes | `quality/gates/release/` (P58) |
| structure | freshness invariants, banned words, top-level scope (QG-08) | `quality/gates/structure/` (P57 — shipped) |
| agent-ux | dark-factory regression | `quality/gates/agent-ux/` (P59) |
| perf | latency, token economy | `quality/gates/perf/` (P59 file-relocate; v0.12.1 stub→real) |
| security | allowlist enforcement, audit immutability | `quality/gates/security/` (v0.12.1 carry-forward) |

**6 cadences** — when the gate runs:

`pre-push` (local, every push, <60s, blocking) · `pre-pr` (PR CI, <10min, blocking) · `weekly` (cron, alerting) · `pre-release` (on tag, <15min, blocking) · `post-release` (after assets ship, alerting) · `on-demand` (manual / subagent).

**5 kinds** — how the gate is verified:

`mechanical` (deterministic shell + asserts) · `container` (fresh ubuntu/alpine + post-conditions) · `asset-exists` (HEAD/GET URL + min-bytes) · `subagent-graded` (rubric-driven subagent) · `manual` (human-only with TTL freshness).

**Adding a new gate** is one catalog row + one verifier in the right dimension dir. The runner discovers + composes by tag. No new top-level script, no new pre-push wiring.

**Catalog-first rule.** Every phase's FIRST commit writes the catalog rows that define this phase's GREEN contract; subsequent commits cite the row id. The verifier subagent reads catalog rows that exist BEFORE the implementation lands.

**Verifier subagent dispatch (QG-06).** Phase close MUST dispatch an unbiased subagent that grades catalog rows from artifacts with zero session context. The executing agent does NOT get to talk the verifier out of RED.

**Mandatory CLAUDE.md update per phase (QG-07).** This rule itself. Each phase introducing a new file/convention/gate updates CLAUDE.md in the same PR.

**Meta-rule extension** (when an owner catches a quality miss): fix the issue, update CLAUDE.md, AND **tag the dimension**. The dimension tag tells the next agent which catalog file to add the row to + which `quality/gates/<dim>/` directory the verifier belongs in. This makes ad-hoc fixes structural.

**Cross-references** (do NOT duplicate runtime detail here):
- `quality/PROTOCOL.md` — autonomous-mode runtime contract (~400 lines).
- `quality/catalogs/README.md` — unified catalog schema spec.
- `quality/SURPRISES.md` — append-only pivot journal.
- `.planning/research/v0.12.0-naming-and-architecture.md` — design rationale.
- `.planning/research/v0.12.0-vision-and-mental-model.md` — why this exists.

**Per-dimension owner_hint** when an owner catches a miss: see PROTOCOL.md § "Failure modes the protocol protects against" for the routing rule. The catalog row is the contract; the verifier is the code; the artifact is the audit; the verdict is the grade.
```

**Optional edit to existing CLAUDE.md "Subagent delegation rules" section** (lower-priority; only if the section is currently silent on QG-06): append a one-liner `- QG-06 verifier subagent dispatch on every phase close — see quality/PROTOCOL.md § "Verifier subagent prompt template" for the verbatim copy-paste prompt.` to the existing bullet list.

**SURPRISES.md updates.** Re-read `quality/SURPRISES.md` and check whether Waves B/C/D/E added entries already (they may have, per their own pivot rules). If new entries from Wave F itself (e.g., the verifier subagent dispatch reveals an unexpected RED): append them. Format per PROTOCOL.md: `YYYY-MM-DD P57: <obstacle> — <resolution>`. Anti-bloat: ≤200 lines.

**STATE.md cursor advance** — append a Roadmap-Evolution entry similar to P56's:

```markdown
- **P57 SHIPPED (2026-04-27, v0.12.0):** Quality Gates skeleton + structure
  dimension migration. Files landed: `quality/{gates,catalogs,reports,
  runners}/` with `quality/PROTOCOL.md` (autonomous-mode runtime contract,
  ~400 lines) + `quality/SURPRISES.md` ownership transferred from P56.
  `quality/catalogs/freshness-invariants.json` carries 9 rows (6 freshness
  invariants migrated from `scripts/end-state.py` + structure/banned-words
  + structure/top-level-requirements-roadmap-scope (QG-08) + structure/
  badges-resolve (BADGE-01 P57 stub, waived until 2026-07-25 — verifier
  ships P60)). `quality/runners/{run,verdict}.py` are stdlib-only Python
  cross-platform, runner contract per `v0.12.0-naming-and-architecture.md`
  § "Runner contract". `quality/runners/verdict.py` emits
  `quality/reports/badge.json` in shields.io endpoint format (QG-09 P57
  scope; P60 publishes via mkdocs). `quality/gates/structure/freshness-
  invariants.py` ships the 6+1 verifier functions; `quality/gates/structure/
  banned-words.sh` wraps `scripts/banned-words-lint.sh --all`
  (SIMPLIFY-01 closure via Approach A wrapper). `scripts/end-state.py`
  reduced 734 lines → ≤30-line shim that delegates to
  `python3 quality/runners/verdict.py` (STRUCT-02 + SIMPLIFY-02).
  POLISH-STRUCT (Wave D) moved ~480 lines of historical milestone
  content from `.planning/ROADMAP.md` to `.planning/milestones/v0.X.0-
  phases/ROADMAP.md` (3 files: v0.11.0, v0.10.0, v0.9.0); top-level
  ROADMAP.md ~704 → ~230 lines, QG-08 verifier flips RED → PASS.
  SIMPLIFY-03 closed via Wave A boundary doc + Wave E audit memo
  (`scripts/catalog.py` left in place per the audit). Pre-push hook
  gains the new runner block alongside `scripts/end-state.py` chain
  (parallel migration per D4); hard-cut deferred to P60 SIMPLIFY-10.
  CLAUDE.md gains "Quality Gates — dimension/cadence/kind taxonomy"
  section (QG-07). Verifier subagent verdict GREEN at
  `quality/reports/verdicts/p57/VERDICT.md`. Carry-forwards: BADGE-01
  P60, SIMPLIFY-04..10 P58/P59/P60, MIGRATE-01..03 P63. Next: P58
  release dimension gates + code-dimension absorption.
```

The Current Position cursor:

```yaml
Phase: 58
Plan: —
Cursor: P57 SHIPPED — Quality Gates skeleton + structure dimension shipped...
Status: shipped (P57)
Last activity: 2026-04-27 -- P57 closed with GREEN verdict at quality/reports/verdicts/p57/VERDICT.md...
```

Update the `progress` block frontmatter:
- `completed_phases: 1` → `2`
- `total_plans: 4` → `10` (P56's 4 plans + P57's 6 plans)
- `completed_plans: 4` → `10`
- `percent: 12` → `25` (2/8 phases = 25%)

**Verifier subagent dispatch (QG-06).** Per `quality/PROTOCOL.md` § "Verifier subagent prompt template" + the P56 precedent:

The orchestrator (or the owner) dispatches a `general-purpose` subagent via the Task tool with the prompt from PROTOCOL.md. The subagent reads:
1. `.planning/REQUIREMENTS.md ## v0.12.0` — find requirements assigned to P57.
2. `quality/catalogs/*.json` — find rows tagged with P57's verifier scripts (or with `last_verified` post Wave A's commit).
3. `CLAUDE.md` — confirm P57's contributions are documented (QG-07).
4. `quality/SURPRISES.md` — confirm any pivots are journaled.

For each catalog row in scope:
1. Re-run the verifier.
2. Compare actual artifact against expected.asserts.
3. Grade PASS / FAIL / PARTIAL / NOT-VERIFIED.

The subagent writes `quality/reports/verdicts/p57/VERDICT.md` with file:line citations.

**Fallback per P56 precedent.** If the executing agent's toolset doesn't include Task dispatch (see P56's `.planning/verifications/p56/VERDICT.md` "Verifier-context disclosure"), the executing agent writes the verdict in-session BUT explicitly discloses:
- which evidence sources it consulted (catalog files, artifact JSONs, runner output, parity report);
- that it did NOT re-interpret evidence narratively;
- that an out-of-session subagent dispatch should match the verdict exactly.

**Verdict structure** (per P56 precedent in `.planning/verifications/p56/VERDICT.md`):

```markdown
# P57 verdict — <RFC3339 ts>

- Verdict: **GREEN | RED**
- Catalog rows graded: <N>
- QG-07 CLAUDE.md update: PASS | FAIL
- QG-05 SURPRISES.md presence: PASS | FAIL (already shipped Wave A; expect PASS)
- QG-06 verifier dispatch: PASS (this verdict file IS the QG-06 evidence)
- QG-08 top-level-requirements-roadmap-scope row: PASS (after Wave D move)
- POLISH-STRUCT closure: PASS (no remaining RED in structure dim)
- SIMPLIFY-01/02/03 closure: PASS

> **Verifier-context disclosure** (or "subagent dispatch" if Task tool available)

## Per-row table

| row_id | blast_radius | asserts_total | asserts_passing | status | evidence_citation |
|---|---|---|---|---|---|
| structure/no-version-pinned-filenames | P1 | 1 | 1 | PASS | quality/reports/verifications/structure/no-version-pinned-filenames.json (asserts_passed: ['find docs scripts | grep ... empty'], asserts_failed: []) |
| structure/install-leads-with-pkg-mgr-docs-index | P0 | 1 | 1 | PASS | ...json |
| ... (all 9 rows) ... |

## QG-07 — CLAUDE.md update

Citations from `git diff main...HEAD -- CLAUDE.md`:
- New section "Quality Gates — dimension/cadence/kind taxonomy" present.
- Cross-references quality/PROTOCOL.md.
- Meta-rule extension documented.
- bash scripts/banned-words-lint.sh clean.

QG-07 status: **PASS**.

## QG-05 — quality/SURPRISES.md presence + ownership

- File exists, ≤200 lines.
- Wave A's Ownership block present.
- 5 P56 entries preserved verbatim.
- 0-N P57 entries appended (Waves B-F pivots, if any).

QG-05 status: **PASS**.

## QG-08 — top-level scope

- `.planning/REQUIREMENTS.md` has 1 H2 section (`## v0.12.0 Requirements`).
- `.planning/ROADMAP.md` has 1 H2 section for active milestone (`## v0.12.0 Quality Gates (PLANNING)`).
- 3 historical milestone ROADMAP.md files exist under `.planning/milestones/v0.X.0-phases/`.
- Verifier `verify_top_level_requirements_roadmap_scope` exits 0.

QG-08 status: **PASS**.

## SIMPLIFY-01 / SIMPLIFY-02 / SIMPLIFY-03 closure

- SIMPLIFY-01: `quality/gates/structure/banned-words.sh` wrapper exists; bash invocation passes; `scripts/banned-words-lint.sh` unchanged + invoked by wrapper.
- SIMPLIFY-02: `scripts/end-state.py` is ≤30 lines (`wc -l` returns ≤30); `verdict` subcommand delegates to `python3 quality/runners/verdict.py`.
- SIMPLIFY-03: `scripts/catalog.py` exists; `quality/catalogs/README.md` § SIMPLIFY-03 boundary documented; audit memo at `.planning/phases/57-quality-gates-skeleton-structure-dimension/57-05-SIMPLIFY-03-AUDIT.md` confirms.

## Recommendation

**GREEN** — phase closes. P57 has fulfilled QG-01..09 (P57 emit scope), STRUCT-01, STRUCT-02, SIMPLIFY-01..03, BADGE-01 (P57 stub, waived), POLISH-STRUCT.

Non-blocking carry-forwards (none block GREEN):
- BADGE-01 verifier ships in P60 (waiver until 2026-07-25).
- SIMPLIFY-04..10 ship in P58/P59/P60 per traceability table.
- MIGRATE-01..03 ship in P63.

Next: P58 — release dimension gates + code-dimension absorption.
```

**Pre-push hook impact.** The committed CLAUDE.md + STATE.md + SURPRISES.md + verdict.md trigger pre-push:
- credential scan: clean.
- catalog coverage warning: existing scripts/catalog.py reports state per its own catalog (orthogonal to P57; non-fatal).
- SESSION-END-STATE warning (now via shim): delegates to verdict.py; verdict GREEN → no warning.
- new runner block: runs against the structure-dim catalog; all rows PASS or WAIVED → no warning.
- docs-site: not touched (no docs/ changes).
- mermaid-renders: not touched.
- cargo fmt + clippy: clean.

Pre-push GREEN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 57-06-A: Update CLAUDE.md (QG-07 — Quality Gates dimension/cadence/kind taxonomy section)</name>
  <files>CLAUDE.md</files>
  <action>
Edit `CLAUDE.md`. Append the new section per `<interfaces>` after the existing "v0.12.0 Quality Gates — phase log" section that P56 added. Per P56's verdict (`.planning/verifications/p56/VERDICT.md` § "QG-07 — CLAUDE.md update"), that section is greppable as `v0.12.0 Quality Gates . phase log`.

**W-5 precondition (plan-check fix):** before any edit, assert P56's prerequisite section exists. If it does NOT, the insertion-point assumption is broken and the agent must escalate, not silently fail:

```bash
grep -q "## v0.12.0 Quality Gates" CLAUDE.md || die "P56 prerequisite missing: CLAUDE.md does not contain '## v0.12.0 Quality Gates' section. Investigate before editing — Wave F's insertion point assumes P56's phase-log section exists. Possible causes: P56 verdict was rolled back, CLAUDE.md was rewritten, or the section name changed."
```

(In a bash script, define `die() { echo "FATAL: $*" >&2; exit 1; }` near the top, or use `|| { echo ...; exit 1; }` inline.)

**Find the insertion point:** locate the end of the existing "v0.12.0 Quality Gates — phase log" section (the last line before the next H2 `## ...` heading). Insert the new section after a blank line.

**Exact insertion content** (per `<interfaces>` recommended new section, paraphrasable but maintain the table structures verbatim — those are load-bearing for future agents).

**Style notes:**
- Use H2 (`##`) for the section header.
- Tables are `| col | col |` GFM with `|---|---|` separators.
- Keep ≤80 added lines (anti-bloat per PROTOCOL.md §"Anti-bloat rules per surface" — CLAUDE.md is a project guide, not a textbook).
- The cross-references list at the bottom MUST point at `quality/PROTOCOL.md` (the runtime detail home), `quality/catalogs/README.md`, `quality/SURPRISES.md`, and the 2 research docs.
- Use the banned-words-allowed list (`migrate to`, `for the 80%`, etc.); do NOT use `replace`.

**Optional edit:** if the existing CLAUDE.md "Subagent delegation rules" section does not mention QG-06, append a single bullet: `- QG-06 verifier subagent dispatch on every phase close — see quality/PROTOCOL.md § "Verifier subagent prompt template" for the verbatim copy-paste prompt.`

**Self-check:**
- `wc -l CLAUDE.md` — note the new total; document in 57-06-SUMMARY.md.
- `bash scripts/banned-words-lint.sh` — exit 0.
- `grep -q "dimension/cadence/kind" CLAUDE.md` — exit 0.
- `grep -q "quality/PROTOCOL.md" CLAUDE.md` — exit 0.
- `grep -q "fix the issue, update CLAUDE.md, AND tag the dimension" CLAUDE.md` — exit 0 (the meta-rule extension verbatim).
  </action>
  <verify>
    <automated>grep -q "dimension/cadence/kind" CLAUDE.md && grep -q "quality/PROTOCOL.md" CLAUDE.md && grep -q "tag the dimension" CLAUDE.md && grep -q "Catalog-first" CLAUDE.md && bash scripts/banned-words-lint.sh && lc=$(wc -l < CLAUDE.md) && echo "CLAUDE.md OK lines=$lc"</automated>
  </verify>
  <done>
CLAUDE.md gains the "Quality Gates — dimension/cadence/kind taxonomy" section: 8-dim table, 6-cadence list, 5-kind list, catalog-first rule, verifier-subagent rule, mandatory-CLAUDE-update rule, meta-rule extension verbatim ("fix the issue, update CLAUDE.md, AND tag the dimension"), cross-references to quality/PROTOCOL.md. `bash scripts/banned-words-lint.sh` clean.
  </done>
</task>

<task type="auto">
  <name>Task 57-06-B: Append any P57 wave-pivot SURPRISES (QG-05 maintenance)</name>
  <files>quality/SURPRISES.md</files>
  <action>
Re-read `quality/SURPRISES.md`. Read each Wave's SUMMARY.md (57-02, 57-03, 57-04, 57-05) and identify any pivots that should have already been captured. If they were captured during their own waves: skip. If not: append now.

**Likely candidates** (depending on what actually happened):
- Wave B: pre-push perf overage (probably did NOT happen, but if it did, append per pivot rules).
- Wave C: parity divergence (probably did NOT happen on the 6 freshness rows; QG-08 RED was expected, not a pivot).
- Wave D: chunky-move owner-verify outcome — if owner approved on first pass, NO surprise. If owner asked for revisions, DID happen → append.
- Wave E: SIMPLIFY-03 audit conclusion — if it confirmed Wave A, NOT a surprise. If it revealed actual overlap, DID happen.

**Format per PROTOCOL.md:** `YYYY-MM-DD P57: <one-line obstacle> — <one-line resolution>`.

**If zero pivots happened:** add a single confirmation entry at the end of the existing P56 entries:

```
2026-04-27 P57: phase shipped without pivots — POLISH-STRUCT closed cleanly,
parity confirmed on 6 freshness rows, SIMPLIFY-01/02/03 absorbed via the
Approach A wrappers + Wave A boundary doc. — All catalog rows GREEN or WAIVED;
verdict at quality/reports/verdicts/p57/VERDICT.md.
```

This single "no-pivots" entry IS valuable — it documents that P57 ran clean (the next agent reading SURPRISES.md sees that P57 didn't introduce dead-end paths to avoid). Anti-bloat: 1-3 lines max for this entry.

**Line cap.** `wc -l quality/SURPRISES.md` ≤ 200. Currently ~75 lines after Wave A's Ownership block + 5 P56 entries; adding 1-5 P57 entries keeps us well under cap.

**Order.** Append AT THE END (chronological order). Do NOT touch the P56 entries above.
  </action>
  <verify>
    <automated>test -f quality/SURPRISES.md && lc=$(wc -l < quality/SURPRISES.md) && [ "$lc" -le 200 ] && [ "$(grep -c '^2026-04-27 P57:' quality/SURPRISES.md)" -ge 1 ] && [ "$(grep -c '^2026-04-27 P56:' quality/SURPRISES.md)" -eq 5 ] && echo "SURPRISES.md OK lines=$lc P56=5 P57>=1"</automated>
  </verify>
  <done>
File ≤200 lines. ≥1 P57 entry appended (either real pivots or the clean-run confirmation). 5 P56 entries preserved verbatim. Wave A Ownership block intact.
  </done>
</task>

<task type="auto">
  <name>Task 57-06-C: Run the full runner + verdict.py once more for the close-of-phase artifact set</name>
  <files>quality/reports/verdicts/p57/VERDICT.md (will be created by the runner OR by Task D's verifier subagent dispatch — see notes), quality/catalogs/freshness-invariants.json (status fields update from re-run)</files>
  <action>
**This task generates the in-session pre-flight verdict** so Task D (verifier subagent dispatch) has fresh artifacts to grade.

```bash
# Run the runner — updates catalog row statuses + writes per-row artifacts
python3 quality/runners/run.py --cadence pre-push
# Run verdict.py with --phase 57 — writes the markdown verdict + badge.json
python3 quality/runners/verdict.py --phase 57
```

Expected:
- Runner exits 0 (every P0+P1 row PASS or WAIVED — Wave D fixed QG-08).
- 9 artifacts updated under `quality/reports/verifications/structure/`.
- Catalog rows update: status PASS or WAIVED, last_verified set to now.
- verdict.py writes `quality/reports/verdicts/p57/<ts>.md` (or `pre-push/<ts>.md` depending on flag — the `--phase 57` form writes to `p57/`).
- `quality/reports/badge.json` updated: `color: brightgreen`, `message: "8/8 GREEN"` (8 P0+P1 rows in scope, all PASS or WAIVED; the BADGE-01 P2 row excluded from numerator/denominator per QG-09 rules).

**Sanity-check the badge:**

```bash
python3 -c "import json; d=json.load(open('quality/reports/badge.json')); print(d)"
# expect: {'schemaVersion': 1, 'label': 'quality gates', 'message': '<N>/<M> GREEN', 'color': 'brightgreen'}
```

**If the runner exits non-zero** (any RED row): STOP. Investigate. The owner-verify checkpoint in Wave D was supposed to confirm GREEN before reaching Wave F. If RED here, something regressed. Possibilities:
- A Wave A/B/C/D/E commit accidentally re-introduced a freshness drift.
- The QG-08 verifier has a bug; ROADMAP.md change broke something Wave D didn't expect.
- The runner has a bug (Wave B oversight).

Per PROTOCOL.md "When stuck rules": file SURPRISES.md `2026-04-27 P57 Wave F: regression detected after Wave D — <symptom>` and pause for owner input.

**If the runner exits 0** (expected): the artifacts are fresh + correct; Task D's verifier subagent reads them.
  </action>
  <verify>
    <automated>python3 quality/runners/run.py --cadence pre-push; runner_exit=$?; python3 quality/runners/verdict.py --phase 57; verdict_exit=$?; python3 -c "import json; d=json.load(open('quality/reports/badge.json')); assert d['color']=='brightgreen', f'expected brightgreen, got {d[\"color\"]} ({d})'; print('badge:', d)" && echo "runner=$runner_exit verdict=$verdict_exit (both expected 0)" && [ "$runner_exit" -eq 0 ] && [ "$verdict_exit" -eq 0 ]</automated>
  </verify>
  <done>
Runner exits 0; verdict.py exits 0; badge.json has `color: brightgreen`. Per-row artifacts under `quality/reports/verifications/structure/` are timestamped now. Catalog rows updated with PASS/WAIVED status + non-null last_verified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 57-06-D: Dispatch unbiased verifier subagent (QG-06) — or in-session verdict with disclosure (P56 fallback)</name>
  <what-built>
Wave F has assembled the phase-close evidence:
- All P57 catalog rows graded by `quality/runners/run.py --cadence pre-push` (Task C).
- CLAUDE.md updated with the dimension/cadence/kind taxonomy section (Task A; QG-07).
- SURPRISES.md updated with any P57 pivots (Task B; QG-05 maintenance).
- Per-row artifacts at `quality/reports/verifications/structure/*.json` are fresh.

This task dispatches the unbiased verifier subagent per QG-06 + PROTOCOL.md § "Verifier subagent prompt template". The subagent reads zero session context — only the catalog rows + artifacts + CLAUDE.md diff — and writes `quality/reports/verdicts/p57/VERDICT.md`.

**Two execution paths** (per P56 precedent in `.planning/verifications/p56/VERDICT.md`):

**Path A — Task tool dispatch** (preferred). The orchestrator (or owner) dispatches a `general-purpose` subagent via the Task tool with the prompt template from `quality/PROTOCOL.md` § "Verifier subagent prompt template". The subagent reads + grades + writes the verdict.

**Path B — In-session verdict with disclosure** (P56 fallback when Task dispatch is unavailable). The Wave F executor writes the verdict in-session BUT explicitly discloses:
- which evidence sources it consulted (catalog files, artifact JSONs, runner output, parity report);
- that it did NOT re-interpret evidence narratively;
- that an out-of-session subagent dispatch should match this verdict exactly.

The disclosure pattern is verbatim from P56's verdict file (lines 14-26 of `.planning/verifications/p56/VERDICT.md` — copy-paste with P57 substitutions).
  </what-built>
  <how-to-verify>
1. **Choose the path:**
   - If running under an orchestrator that has Task tool access: Path A (preferred).
   - If running in a session where Task dispatch is not possible: Path B with disclosure.

2. **Path A — Task dispatch.** Dispatch a `general-purpose` subagent with the verbatim PROTOCOL.md § "Verifier subagent prompt template" + P57 substitutions:
   - `<N>` → 57.
   - Inputs section keeps the 4 sources verbatim.
   - Output: scored markdown table with file:line citations to `quality/reports/verdicts/p57/VERDICT.md`.

   The subagent's prompt MUST refuse to grade GREEN unless every P0+P1 catalog row is PASS or WAIVED AND CLAUDE.md is updated AND quality/SURPRISES.md is current.

3. **Path B — in-session verdict with disclosure.** The executor writes `quality/reports/verdicts/p57/VERDICT.md` per the structure in `<interfaces>` above. Use the P56 disclosure block verbatim. Cite EXACT artifact paths + line ranges; do NOT narrate.

4. **Read the verdict file:**
   ```bash
   cat quality/reports/verdicts/p57/VERDICT.md
   ```
   Expected: `Verdict: **GREEN**`. 9 catalog rows graded (8 PASS + 1 WAIVED). QG-07 PASS. QG-05 PASS. QG-06 PASS (this verdict file IS the QG-06 evidence). QG-08 PASS. POLISH-STRUCT PASS. SIMPLIFY-01/02/03 PASS.

5. **Decide:**
   - **GREEN approved:** type "approved" → Wave F proceeds to Task E (STATE.md cursor advance + commit).
   - **RED:** the verdict file describes the RED rows. Either (a) fix in this same wave (loop back to Wave D for the structure-dim row, or appropriate Wave for the cause), or (b) escalate via SURPRISES.md + STOP per PROTOCOL.md "When stuck rules". Phase does NOT close on RED.
  </how-to-verify>
  <resume-signal>Type "approved" (after reading the GREEN verdict file) or describe the RED rows.</resume-signal>
</task>

<task type="auto">
  <name>Task 57-06-E: Update STATE.md cursor + commit Wave F (phase close)</name>
  <files>.planning/STATE.md, CLAUDE.md, quality/SURPRISES.md, quality/reports/verdicts/p57/VERDICT.md</files>
  <action>
**Step 1 — Update `.planning/STATE.md`:**

Edit the frontmatter and the body per `<interfaces>` STATE.md cursor advance:
- Frontmatter `last_updated` → current RFC3339.
- Frontmatter `last_activity` → P57-shipped one-liner.
- Frontmatter `progress`: **W-4 plan-check fix — read current frontmatter at runtime, then increment.** Do NOT hard-code the before/after numbers. Sample script:

  ```python
  import yaml, sys
  from pathlib import Path
  text = Path(".planning/STATE.md").read_text()
  parts = text.split("---", 2)
  fm = yaml.safe_load(parts[1])
  prog = fm["progress"]
  prog["completed_phases"] = int(prog["completed_phases"]) + 1
  prog["completed_plans"] = int(prog["completed_plans"]) + 6  # P57 ships 6 plans
  prog["total_plans"] = int(prog["total_plans"]) + 6
  prog["percent"] = int(round(100 * prog["completed_phases"] / int(prog["total_phases"])))
  fm["last_updated"] = "<current-RFC3339>"
  fm["last_activity"] = "<P57-shipped-one-liner>"
  parts[1] = "\n" + yaml.safe_dump(fm, sort_keys=False, default_flow_style=False) + "\n"
  Path(".planning/STATE.md").write_text("---".join(parts))
  ```

  This is resilient if STATE.md was already advanced (e.g., a previous Wave F retry); it always reads the current values rather than assuming `completed_phases: 1` → `2`. The original hard-coded math (`1` → `2`, `4` → `10`, `12` → `25`) was fragile per the W-4 plan-check feedback.

- Body `## Accumulated Context > ### Roadmap Evolution`: APPEND a new bullet at the TOP of the list (above the existing P56 bullet) per `<interfaces>` template. Do NOT delete the P56 bullet.
- Body `## Current Position`: update Phase to 58, Cursor + Status + Last activity to P57-shipped state.

**Step 2 — Stage Wave F's 4 files:**

```bash
git add CLAUDE.md quality/SURPRISES.md .planning/STATE.md quality/reports/verdicts/p57/VERDICT.md
```

**Step 3 — Commit. Single combined commit** (or 2 commits if you prefer separating the verdict from the docs update):

```
chore(p57): phase close — CLAUDE.md QG-07 + SURPRISES.md + STATE.md + verdict GREEN

Closes P57 — Quality Gates skeleton + structure dimension migration.
The unbiased verifier subagent's verdict at
quality/reports/verdicts/p57/VERDICT.md grades all 9 catalog rows
GREEN (8 PASS + 1 WAIVED) and confirms QG-05 / QG-06 / QG-07 / QG-08
closure.

Files:
- CLAUDE.md — new "Quality Gates — dimension/cadence/kind taxonomy"
  section appended (QG-07). Cross-references quality/PROTOCOL.md
  for runtime detail (anti-bloat). Meta-rule extension documented:
  "fix the issue, update CLAUDE.md, AND tag the dimension."
  Subagent-delegation-rules section gains a QG-06 bullet.
- quality/SURPRISES.md — appended <N> P57 entries for any wave
  pivots; 5 P56 entries + Wave A Ownership block preserved.
  ≤200 lines.
- .planning/STATE.md — Roadmap-Evolution gains a P57 SHIPPED entry
  at the top; Current Position cursor advances to P58. Progress
  frontmatter updated (2/8 phases = 25%).
- quality/reports/verdicts/p57/VERDICT.md — verifier verdict GREEN.
  Cites file:line for each of 9 catalog rows + QG-07 + QG-05 +
  QG-08 + SIMPLIFY-01/02/03 closure.

P57 ships (in addition to this commit's docs):
- quality/{PROTOCOL.md, SURPRISES.md, catalogs/, gates/, reports/, runners/}
- quality/runners/{run.py, verdict.py, __init__.py}
- quality/catalogs/{README.md, freshness-invariants.json, orphan-scripts.json}
- quality/gates/structure/{freshness-invariants.py, banned-words.sh}
- 8 .gitkeep files for the dimension placeholder dirs
- scripts/end-state.py reduced 734 → ≤30 line shim
- scripts/hooks/pre-push gains the parallel-migration runner block
- 3 per-milestone ROADMAP.md files (.planning/milestones/v0.X.0-phases/)
- .planning/ROADMAP.md scoped to v0.12.0 + index (~704 → ~230 lines)

Carry-forwards (none block GREEN):
- BADGE-01 verifier ships in P60 (waiver until 2026-07-25).
- SIMPLIFY-04..10 ship in P58/P59/P60.
- MIGRATE-01..03 ship in P63.

Refs: QG-01..09, STRUCT-01..02, SIMPLIFY-01..03, BADGE-01 (P57 stub),
POLISH-STRUCT, D4 parallel-migration
```

**Step 4 — Pre-commit checks:**

```bash
bash scripts/banned-words-lint.sh                                      # clean
python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"  # frontmatter parses
python3 quality/runners/run.py --cadence pre-push                       # exit 0
python3 quality/runners/verdict.py --phase 57                          # exit 0
```

**Step 5 — Push.** The pre-push hook fires:
- new runner block (non-fatal warning): all rows GREEN/WAIVED → no warning.
- old shim (delegates to verdict.py): GREEN → no warning.
- cargo fmt + clippy + docs-site: clean (no Rust/docs changes).

Pre-push passes. P57 ships.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" | head -1 && grep -q "P57 SHIPPED" .planning/STATE.md && grep -q "Phase: 58" .planning/STATE.md && grep -q "GREEN" quality/reports/verdicts/p57/VERDICT.md && python3 quality/runners/run.py --cadence pre-push; echo "runner=$?" && bash scripts/banned-words-lint.sh && echo "phase close commit OK"</automated>
  </verify>
  <done>
Single commit (or 2) lands. STATE.md cursor on Phase 58. CLAUDE.md has the new section. SURPRISES.md ≥1 P57 entry. Verdict GREEN. Runner GREEN. `bash scripts/banned-words-lint.sh` clean. Push GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| executing agent's word ↔ verifier subagent's verdict | The whole framework's purpose is to break this boundary; Wave F dispatches the subagent OR uses the P56 disclosure pattern when Task tool unavailable. |
| CLAUDE.md update ↔ next agent's session | If CLAUDE.md is missing the dimension/cadence/kind taxonomy, the P58 agent operates ungrounded — recurring miss pattern. |
| STATE.md cursor ↔ next agent's first action | The next agent's first read is STATE.md; if cursor is wrong, the agent operates on stale context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-57-06-01 | Tampering | Wave F executor talks the verdict GREEN despite a real RED | mitigate | Path A subagent dispatch is the preferred path; Path B requires explicit disclosure block per P56 precedent. The verdict file itself cites file:line evidence — re-grade-able. |
| T-57-06-02 | Repudiation | "Where is P57's contract proof?" | mitigate | The verdict file at `quality/reports/verdicts/p57/VERDICT.md` is the audit. STATE.md cross-references it. CLAUDE.md cross-references PROTOCOL.md which cross-references this verdict pattern. |
| T-57-06-03 | Denial of service | verifier subagent unavailable → phase blocked | mitigate | Path B fallback per P56 precedent; the executing agent writes the verdict with the disclosure block. |
| T-57-06-04 | Tampering | CLAUDE.md update accidentally drops content | mitigate | Task A is APPEND-only (new section after existing P56 phase log section); pre-commit grep confirms the new section IS present without inspecting deletions. |
| T-57-06-05 | Information disclosure | n/a | accept | All updates are public-by-design. |
| T-57-06-06 | Spoofing | n/a | accept | No auth boundary. |
| T-57-06-07 | Tampering | STATE.md frontmatter parsing drift | mitigate | Pre-commit `yaml.safe_load` check on the frontmatter ensures the file remains parseable by gsd-tools. |
</threat_model>

<verification>
After this plan ships:

1. `grep -q "dimension/cadence/kind" CLAUDE.md` — exit 0.
2. `grep -q "tag the dimension" CLAUDE.md` — exit 0 (meta-rule extension verbatim).
3. `grep -q "quality/PROTOCOL.md" CLAUDE.md` — exit 0 (cross-reference present).
4. `[ $(grep -c '^2026-04-27 P57:' quality/SURPRISES.md) -ge 1 ]` — exit 0.
5. `[ $(wc -l < quality/SURPRISES.md) -le 200 ]` — exit 0.
6. `grep -q "P57 SHIPPED" .planning/STATE.md` — exit 0.
7. `grep -q "Phase: 58" .planning/STATE.md` — exit 0.
8. `python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"` — exit 0.
9. `test -f quality/reports/verdicts/p57/VERDICT.md && grep -q "Verdict: \*\*GREEN\*\*" quality/reports/verdicts/p57/VERDICT.md` — exit 0.
10. `python3 quality/runners/run.py --cadence pre-push` — exit 0 (runner sees all rows GREEN or WAIVED).
11. `python3 -c "import json; d=json.load(open('quality/reports/badge.json')); assert d['color']=='brightgreen'"` — exit 0.
12. `bash scripts/banned-words-lint.sh` — exit 0.
13. The HEAD commit lists 4 files (CLAUDE.md + STATE.md + SURPRISES.md + VERDICT.md). The commit message references QG-01..09, STRUCT-01..02, SIMPLIFY-01..03, BADGE-01, POLISH-STRUCT.
</verification>

<success_criteria>
P57 closes GREEN. Every recurring criterion satisfied:

1. **Catalog-first** — Wave A landed the contract before any implementation; subsequent waves cited row IDs.
2. **CLAUDE.md update in same PR (QG-07)** — **Wave F lands it.** New "Quality Gates — dimension/cadence/kind taxonomy" section appended; cross-references PROTOCOL.md; meta-rule extension documented.
3. **Verifier-subagent dispatch on phase close (QG-06)** — **Wave F dispatches** (Path A) or writes the in-session verdict with explicit disclosure (Path B). Verdict file at `quality/reports/verdicts/p57/VERDICT.md`.
4. **SIMPLIFY absorption** — Closed in Waves A (SIMPLIFY-03) + C (SIMPLIFY-01 + SIMPLIFY-02). All 3 in-scope SIMPLIFY-* requirements ship.
5. **Fix every RED row** — Wave D fixed the QG-08 RED on `.planning/ROADMAP.md`. The other 6 freshness rows + banned-words are GREEN by Wave F. The BADGE-01 row is WAIVED with 90d TTL pointing at P60.

P57's scope (per traceability table in REQUIREMENTS.md `## v0.12.0`):
- QG-01..09 (P57 emit/scaffold scope) ✓
- STRUCT-01, STRUCT-02 ✓
- SIMPLIFY-01, SIMPLIFY-02, SIMPLIFY-03 ✓
- POLISH-STRUCT ✓
- BADGE-01 (P57 stub; P60 ships verifier) ✓ via WAIVED row

15 requirements scoped; 15 closed (BADGE-01 closed-by-WAIVED until P60). Per-phase requirement count from REQUIREMENTS.md: P57=15. Match.

**P58 handoff:** the next agent reads `.planning/STATE.md` cursor, then `quality/reports/verdicts/p57/VERDICT.md`, then `quality/SURPRISES.md`, then `quality/PROTOCOL.md`, then `.planning/ROADMAP.md` § Phase 58 entry. P58 builds the release dimension that would have caught the RELEASE-01 regression class within 7 days; P58 also folds SIMPLIFY-04 + SIMPLIFY-05 (the smaller code-dimension scripts).
</success_criteria>

<pivot_rules>
**Wave F pivot rules:**

- **If the verifier subagent grades RED**: phase does NOT close. Per PROTOCOL.md "Step 7", the executing agent does NOT get to talk the verifier out of RED. Either:
  - Fix the underlying RED rows (loop back to Wave C/D as appropriate); re-run runner; re-dispatch verifier.
  - File 90d-TTL waiver per the waiver protocol; re-dispatch verifier; if subagent accepts the waiver as principled, GREEN; if not, escalate.
  - Append `2026-04-27 P57 Wave F: verifier RED on <row_id> — <resolution>` to SURPRISES.md.
- **If Task tool dispatch unavailable** (P56 precedent): use Path B in-session verdict with full disclosure block. Mark the verdict file's preamble explicitly. The orchestrator can re-grade out-of-session.
- **If CLAUDE.md update inadvertently uses banned words**: rewrite using `migrate to` / `for the 80%` / `rewrite as` / `complement` / `alongside`. Append SURPRISES.md.
- **If pre-push hook GREEN regresses** (e.g., the new runner block flips RED on a row it was GREEN on in Wave D): the new runner block is non-fatal so push proceeds, but the warning is a signal — investigate before phase close. Most likely cause: Wave F's docs edit accidentally tripped the "no banned words" / "install path order" / "no orphan docs" check. Fix in this same wave.
- **If STATE.md frontmatter parse fails after edit** (e.g., dangling colon, bad indentation): Wave F MUST fix before commit. gsd-tools reads STATE.md frontmatter for `/gsd-progress` and similar; a parse failure breaks every subsequent slash-command.

Append any pivot to `quality/SURPRISES.md` per PROTOCOL.md Step 9.
</pivot_rules>

<risk_table>
| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| Verifier subagent dispatch unavailable (Task tool not in agent toolset) | medium | low (Path B fallback covers it; same precedent as P56 verdict) | Plan explicitly accommodates Path B with disclosure block |
| Wave F's CLAUDE.md edit lands a banned word from research-doc copy | low | low | banned-words-lint runs in pre-commit verify |
| STATE.md frontmatter edit corrupts YAML | medium | high (breaks /gsd-progress + every read of STATE.md) | Pre-commit `yaml.safe_load` check |
| Verifier sees a RED that Wave D's checkpoint missed | low | medium (forces a Wave D loop-back) | Wave D's checkpoint already required `python3 quality/runners/run.py --cadence pre-push` exit 0; same check happens in Task C |
| The verdict file generated by an in-session Path B verdict lacks sufficient citations to be re-graded out-of-session | medium | medium (out-of-session re-grade may diverge) | Path B template explicitly cites file:line for every claim per P56 precedent; no narrative |
| New CLAUDE.md content tips line cap or duplicates prior content | low | low | Wave F appends only; existing sections untouched |
| Pre-push hook's check-mermaid-renders gate fires (false positive — the new files don't touch mermaid) | very low | low | Hook only runs check-mermaid-renders if docs/ touched; Wave F doesn't touch docs/ |
</risk_table>

<output>
After completion, create `.planning/phases/57-quality-gates-skeleton-structure-dimension/57-06-SUMMARY.md` with: commit SHA(s), files changed (4), CLAUDE.md line count delta, SURPRISES.md P57 entries appended (count + summary), STATE.md progress numbers (before/after), verdict file path + GREEN/RED status, runner final exit code, badge.json color, recurring-criteria 1-5 closure summary, and a one-line "P57 SHIPPED — next: P58 release dimension" handoff.
</output>
