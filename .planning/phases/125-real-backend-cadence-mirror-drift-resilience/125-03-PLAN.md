---
phase: 125-real-backend-cadence-mirror-drift-resilience
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - docs/reference/testing-targets.md
  - quality/catalogs/doc-alignment.json
autonomous: true
requirements: [DRAIN-02]
must_haves:
  truths:
    - "docs/reference/testing-targets.md names scripts/refresh-tokenworld-mirror.sh as the mirror-refresh pre-step for the pre-release-real-backend cadence (it currently has ZERO mention of that script — the concrete SC1 doc gap)."
    - "The doc cross-references the self-reconciling litmus (Plan 02 / SC2) as the reason the manual pre-step is usually unnecessary — a faster manual escape hatch, not a hard prerequisite."
    - "The doc-alignment walk gate passes; any new binding for the added claim was minted via the tool, never hand-edited into doc-alignment.json."
  artifacts:
    - path: "docs/reference/testing-targets.md"
      provides: "A mirror-refresh pre-step section naming refresh-tokenworld-mirror.sh + the self-heal cross-reference"
      contains: "refresh-tokenworld-mirror.sh"
  key_links:
    - from: "docs/reference/testing-targets.md"
      to: "scripts/refresh-tokenworld-mirror.sh"
      via: "named copy-paste command in the new pre-step section"
      pattern: "bash scripts/refresh-tokenworld-mirror.sh"
    - from: "docs/reference/testing-targets.md"
      to: "quality/gates/agent-ux/lib/litmus-flow.sh (self-heal, Plan 02)"
      via: "cross-reference that the self-reconciling litmus makes the pre-step usually unnecessary"
      pattern: "self-reconcil"
---

<objective>
SC1 / DRAIN-02: document the mandatory mirror-refresh pre-step for the
`pre-release-real-backend` cadence.

`docs/reference/testing-targets.md` currently has ZERO reference to
`scripts/refresh-tokenworld-mirror.sh` (verified by grep — this is the concrete SC1 doc
gap). The 2026-07-16 owner ruling already blesses that script as "the manual op-recovery
(incl. the documented litmus pre-step)." SC1's clause is either/or ("a documented
mandatory pre-step ... OR a self-reconciling litmus") — Plan 02 delivers the
self-reconciling litmus, and RESEARCH.md Open Question 2 recommends documenting the
pre-step regardless (cheap; a faster manual escape hatch for an operator who wants to
pre-warm state without running the full litmus), cross-referencing the self-heal as the
"you shouldn't usually need this" caveat.

Purpose: give a human operator a named, copy-paste mirror-refresh command AND make clear
(a) when to reach for it and (b) that Plan 02's self-heal makes it usually unnecessary.

Output: a new pre-step section in testing-targets.md + any doc-alignment binding the walk
gate requires (minted via tool).

WAVE 2 rationale: this plan is the sole remaining doc-alignment.json writer; it lands after
Plan 01 (which may rebind a troubleshooting.md row) to avoid a same-wave catalog-file
clobber, and after Plan 02 so the self-heal cross-reference describes a shipped mechanism.

CRITICAL DISTINCTION (do not blur): `refresh-tokenworld-mirror.sh` converges the external
mirror clone through the bus/backend path — it is NOT `reposix sync --reconcile` (which
rebuilds only the local cache and does NOT converge the external mirror; the script's own
header says it deliberately does NOT use `--reconcile` for mirror convergence). The doc
must not conflate the two.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-real-backend-cadence-mirror-drift-resilience/125-RESEARCH.md
@.planning/phases/125-real-backend-cadence-mirror-drift-resilience/125-PATTERNS.md
</context>

<load_bearing_constraint>
- doc-alignment.json is MINT-ONLY. Never hand-edit it. Add/rebind bindings via
  `reposix-quality doc-alignment bind ...` or `/reposix-quality-refresh
  docs/reference/testing-targets.md` (quality/CLAUDE.md — a hand-edit breaks the whole
  catalog at load time in serde/validation layers `jq` won't catch).
- Verification walks MUST use the wrapper `bash quality/gates/docs-alignment/walk.sh`
  (grades against a `/tmp` copy) — NEVER the raw `reposix-quality doc-alignment walk`
  subcommand (which mutates the committed catalog's counters as a side effect).
- Do NOT recommend `reposix sync --reconcile` as the mirror-refresh command — that is the
  local-cache path. The mirror-refresh command is `bash scripts/refresh-tokenworld-mirror.sh`.
- This is a mkdocs-tracked docs change: it must also pass
  `bash quality/gates/docs-build/mkdocs-strict.sh` and the structure `banned-words` gate.
  When in doubt run `python3 quality/runners/run.py --cadence pre-push` rather than
  hand-picking gates (fix-twice P117 W3: a docs-build-only local sweep once let a
  banned-words leak reach a push).
</load_bearing_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Add the mirror-refresh pre-step section to testing-targets.md</name>
  <files>docs/reference/testing-targets.md</files>
  <read_first>
    - docs/reference/testing-targets.md (the `## Pre-flight verification` section at
      lines 23-46 — copy its structure: heading → one-sentence rationale → fenced bash
      block → outcome; and the `> **Milestone-close cadence self-sources .env.**`
      blockquote at lines 271-280 — copy its `>` callout shape and closing-sentence
      pattern; and the `#### Sacrificial editable page` subsection at lines 110-125 which
      already documents 2818063 + `confluence_tokenworld.py restore`)
    - scripts/refresh-tokenworld-mirror.sh (its header "THE MECHANISM" section — so the doc
      describes accurately what the script does: fetch the backend-materialized tree and
      overlay it onto the mirror; it does NOT use `reposix sync --reconcile`)
    - quality/gates/agent-ux/lib/litmus-flow.sh (Plan 02's self-heal — the mechanism the
      cross-reference points at)
  </read_first>
  <action>
    Add a new section to testing-targets.md documenting the mirror-refresh pre-step. Place
    it as a new top-level section mirroring `## Pre-flight verification`'s position (a
    sibling to it near the top), OR as a subsection under `## Confluence — TokenWorld space`
    after the `#### Sacrificial editable page` subsection — pick whichever reads best given
    the surrounding structure; the top-level sibling is preferred for discoverability.

    Title it `## Mirror-refresh pre-step (GitHub-mirror drift)`. Content, in this order
    (match the existing section's heading → rationale → fenced block → note shape):

    1. One-sentence rationale: the milestone-close vision-litmus and the
       `pre-release-real-backend` cadence clone a plain-git GitHub mirror; the litmus's own
       prior push can re-stale that mirror, so a second same-day run can hit a stale-base
       rebase conflict. The named pre-step refreshes the mirror clone to backend-current.

    2. The copy-paste command in a fenced bash block:
       ```bash
       bash scripts/refresh-tokenworld-mirror.sh
       ```

    3. A one-line description of WHAT it does (accurate to the script header): it fetches
       the backend-materialized `pages/` tree and overlays it onto the mirror so the mirror
       matches the SoT — it converges the EXTERNAL mirror, which `reposix sync --reconcile`
       does NOT do (that command rebuilds only the local cache). Do not conflate the two.

    4. A `>` callout (copy the `> **...**` shape from the .env callout at lines 271-280)
       stating the self-heal caveat: as of P125, the litmus SELF-RECONCILES the mirror
       clone before its marker edit (`quality/gates/agent-ux/lib/litmus-flow.sh` self-heal,
       DRAIN-12), so you usually do NOT need to run this pre-step manually — it remains a
       faster escape hatch to pre-warm/verify mirror state without a full litmus run
       (mirrors how `reposix sync --reconcile` is documented as "safe to run any time" even
       though most flows shouldn't need it).

    Keep the existing `## Pre-flight verification` section (which runs
    `preflight-real-backends.sh`) intact — this new section is complementary, not a
    replacement.

    Then run `bash quality/gates/docs-build/mkdocs-strict.sh` and `bash
    quality/gates/structure/banned-words.sh` — both must pass (the section uses only
    user-facing capability language; avoid any Layer-1 plumbing term the banned-words gate
    blocks). Commit `docs(125-03): document refresh-tokenworld-mirror pre-step for real-backend cadence`.
  </action>
  <verify>
    <automated>grep -q "bash scripts/refresh-tokenworld-mirror.sh" docs/reference/testing-targets.md && grep -qi "self-reconcil" docs/reference/testing-targets.md && bash quality/gates/docs-build/mkdocs-strict.sh >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Mirror-refresh pre-step" docs/reference/testing-targets.md` succeeds
    - `grep -q "bash scripts/refresh-tokenworld-mirror.sh" docs/reference/testing-targets.md` succeeds (the named command — previously ZERO mentions)
    - `grep -qi "self-reconcil" docs/reference/testing-targets.md` succeeds (the SC2 cross-reference caveat)
    - The doc does NOT present `reposix sync --reconcile` as the mirror-refresh command: `grep -A2 "Mirror-refresh pre-step" docs/reference/testing-targets.md` does not put `reposix sync --reconcile` as the refresh command (it may only appear in the distinction-drawing sentence)
    - The existing `## Pre-flight verification` section is unchanged (`grep -q "bash scripts/preflight-real-backends.sh" docs/reference/testing-targets.md` still succeeds)
    - `bash quality/gates/docs-build/mkdocs-strict.sh` exits 0 AND `bash quality/gates/structure/banned-words.sh` exits 0
  </acceptance_criteria>
  <done>testing-targets.md documents the `refresh-tokenworld-mirror.sh` pre-step with the self-heal cross-reference; mkdocs-strict + banned-words pass.</done>
</task>

<task type="auto">
  <name>Task 2: Pass the doc-alignment walk gate (mint any required binding)</name>
  <files>docs/reference/testing-targets.md, quality/catalogs/doc-alignment.json</files>
  <read_first>
    - docs/reference/testing-targets.md (the section added in Task 1)
    - quality/CLAUDE.md § "Docs-alignment dimension" (the mint-only rule + walk-wrapper rule)
    - quality/catalogs/doc-alignment.json (to see the existing testing-targets.md bindings'
      shape — 22 rows already bind this doc; DO NOT hand-edit)
  </read_first>
  <action>
    Run `bash quality/gates/docs-alignment/walk.sh`. Two outcomes:

    - PASS clean: the new pre-step prose is an operational instruction, not a falsifiable
      product claim, and both alignment_ratio and coverage_ratio stay above floor. No
      binding needed — done. (This is the likely outcome; an "run this script before X"
      instruction is not an eligible testable claim.)

    - BLOCK (coverage_ratio dropped, or the walker flags the new line as an unbound eligible
      claim): mint the binding via the TOOL, never by hand. Either
      `/reposix-quality-refresh docs/reference/testing-targets.md` (single-doc fan-out) or
      the specific `reposix-quality doc-alignment bind ...` verb, binding the new claim to
      the appropriate artifact — the natural test target is
      `quality/gates/agent-ux/milestone-close-vision-litmus.sh` (the litmus whose self-heal
      the pre-step describes) or a structure/grep check that `refresh-tokenworld-mirror.sh`
      is named in the doc. Whatever you bind, re-run `bash quality/gates/docs-alignment/walk.sh`
      until it exits 0, and commit the minted doc-alignment.json in this SAME commit as any
      corresponding doc wording (P117 W3 rebind-in-same-commit precedent).

    NOTE the slash-command constraint: `/reposix-quality-refresh` and
    `/reposix-quality-backfill` are TOP-LEVEL only (depth-2 fan-out is unreachable inside
    gsd-executor). If the executor is running under gsd-executor and a mint is required, use
    the direct `reposix-quality doc-alignment bind` binary verb (available via Bash), not
    the slash command; if that too is unavailable in-context, checkpoint and surface that
    the mint must run from a fresh top-level session (matching the documented
    orchestration-shaped-phase escape hatch in .planning/CLAUDE.md).

    Commit (only if doc-alignment.json changed) `chore(125-03): bind testing-targets mirror-refresh claim (minted)`.
  </action>
  <verify>
    <automated>bash quality/gates/docs-alignment/walk.sh >/dev/null 2>&1 && echo WALK-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `bash quality/gates/docs-alignment/walk.sh` exits 0
    - If `quality/catalogs/doc-alignment.json` changed, `git log --oneline -1 -- quality/catalogs/doc-alignment.json` shows a mint-tool commit (not a hand-edit) and it rode with the doc change
    - No enum/shape corruption: `python3 -c "import json; json.load(open('quality/catalogs/doc-alignment.json'))"` succeeds (structural sanity; the real guard is that the mint tool wrote it)
  </acceptance_criteria>
  <done>The doc-alignment walk gate exits 0; any binding was minted via the tool, not hand-edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented command → operator shell | a doc that shows a real-backend command could leak credentials if it interpolated them into the command line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-03 | Information Disclosure | documented `refresh-tokenworld-mirror.sh` pre-step command | accept | The documented command is `bash scripts/refresh-tokenworld-mirror.sh` with NO credential arguments on the command line — the script reads creds from env (`ATLASSIAN_*` / `REPOSIX_ALLOWED_ORIGINS`), so the doc echoes no secret. Consistent with the existing `preflight-real-backends.sh` / `.env` self-source docs in this same file. No new secret-bearing surface. |
| T-125-03b | Tampering | doc-alignment.json corruption via hand-edit | mitigate | Task 2 forbids hand-editing the catalog; any binding is minted via `reposix-quality`/refresh (acceptance-criteria checked), preventing the whole-catalog-load-break class (quality/CLAUDE.md P117 W2). |
</threat_model>

<verification>
- `grep` gates confirm the named `refresh-tokenworld-mirror.sh` command + the self-heal cross-reference.
- `bash quality/gates/docs-build/mkdocs-strict.sh`, `bash quality/gates/structure/banned-words.sh`, and `bash quality/gates/docs-alignment/walk.sh` all exit 0.
- The mirror-refresh command is NOT conflated with `reposix sync --reconcile`.
</verification>

<success_criteria>
SC1 (DRAIN-02): a documented mandatory mirror-refresh pre-step
(`scripts/refresh-tokenworld-mirror.sh`) is present in testing-targets.md, cross-referencing
Plan 02's self-reconciling litmus as the reason it is usually unnecessary — closing the
concrete SC1 doc gap (the script previously had zero mentions in that doc).
</success_criteria>

<output>
After completion, create `.planning/phases/125-real-backend-cadence-mirror-drift-resilience/125-03-SUMMARY.md`
</output>
