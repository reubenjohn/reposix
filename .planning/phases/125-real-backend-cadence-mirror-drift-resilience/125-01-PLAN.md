---
phase: 125-real-backend-cadence-mirror-drift-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/reposix-remote/tests/push_conflict.rs
  - crates/reposix-remote/src/write_loop.rs
  - docs/guides/troubleshooting.md
autonomous: true
requirements: [DRAIN-12]
must_haves:
  truths:
    - "The helper's mirror-lag reject hint recommends `reposix sync --reconcile` (not the no-op bare `reposix sync` that per sync.rs's own doc comment does nothing)."
    - "The helper's mirror-lag reject hint warns that on a `reposix attach` (Pattern-C) tree a bare `git pull`/`git rebase` reads the stale origin mirror, and points at a remote-explicit rebase against the SoT-backed bus remote."
    - "The pinned literal substring `git pull --rebase` survives verbatim in write_loop.rs; all existing pinned-substring Rust tests + doc-alignment rows stay green (nothing removed or reworded)."
    - "A new Rust regression test drives the mirror-lag-ref-populated branch (via write_mirror_synced_at + a stale-base push) and asserts the augmented hint content."
  artifacts:
    - path: "crates/reposix-remote/src/write_loop.rs"
      provides: "Corrected mirror-lag reject hint (--reconcile fix + additive Pattern-C remote-explicit line)"
      contains: "reposix sync --reconcile"
    - path: "crates/reposix-remote/tests/push_conflict.rs"
      provides: "Regression test asserting the augmented mirror-lag hint content"
      contains: "write_mirror_synced_at"
    - path: "docs/guides/troubleshooting.md"
      provides: "Additive Pattern-C clarification on the Bus-remote fetch-first recovery"
  key_links:
    - from: "crates/reposix-remote/tests/push_conflict.rs"
      to: "crates/reposix-remote/src/write_loop.rs"
      via: "write_mirror_synced_at populates refs/mirrors/<sot>-synced-at, then a stale-base push enters the mirror-lag branch"
      pattern: "write_mirror_synced_at"
    - from: "crates/reposix-remote/src/write_loop.rs"
      to: "docs/concepts/dvcs-topology.md:90"
      via: "code phrasing aligned to the already-correct documented example (fix-twice)"
      pattern: "reposix sync --reconcile"
---

<objective>
SC3 / DRAIN-12: correct the git-remote-reposix helper's mirror-drift teaching string.

Two concrete bugs live in the mirror-lag-ref-populated reject hint in
`write_loop.rs` (RESEARCH.md Pitfall 2 + Pitfall 4):
  (a) the hint recommends bare `reposix sync` — which per `sync.rs`'s own doc comment
      does NOTHING (it just prints a hint pointing at `--reconcile`; exits 0) — instead
      of the correct `reposix sync --reconcile` shown in the already-correct doc example
      at `docs/concepts/dvcs-topology.md:90`. The code drifted from the doc.
  (b) the hint does not warn that on a `reposix attach` (Pattern-C) tree, a bare
      `git pull`/`git rebase` reads the ORIGIN MIRROR by default (which may itself be
      stale), so the "recovery" is a near-no-op against a stale mirror.

Purpose: a stale-mirror-clone operator following the helper's hint verbatim currently
gets pointed at a no-op (`reposix sync` without `--reconcile`) and a bare `git rebase`
that reads the stale mirror — the exact failure the DRAIN-02 second-run false-negative
depends on. Correct BOTH, additively, without disturbing the load-bearing pinned
substring.

Output: a corrected write_loop.rs hint block, a new Rust regression test asserting the
augmented content, and one additive fix-twice clarification in troubleshooting.md.

HARD CONSTRAINT (see <load_bearing_constraint> below): the literal substring
`git pull --rebase` must survive verbatim — ≥12 doc-alignment rows + 3 Rust test files
pin it. This plan ADDS guidance; it never removes or rewords that phrase. This plan does
NOT touch the bus push's mirror fan-out algorithm (GTH-V15-38 "Option C" is out of scope
for this milestone per the 2026-07-16 owner ruling).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-real-backend-cadence-mirror-drift-resilience/125-RESEARCH.md
@.planning/phases/125-real-backend-cadence-mirror-drift-resilience/125-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase; use directly, no exploration. -->

Cache mirror-ref API (crates/reposix-cache/src/mirror_refs.rs):
```rust
pub fn write_mirror_synced_at(&self, sot_host: &str, ts: DateTime<Utc>) -> Result<String>  // :155
pub fn read_mirror_synced_at(&self, sot_host: &str) -> Result<Option<DateTime<Utc>>>       // :227
```

write_loop.rs mirror-lag branch fires ONLY when `read_mirror_synced_at(backend_name)`
returns `Some` — so the new test must call `write_mirror_synced_at("sim", <ts>)` on a
Cache opened in the SAME `REPOSIX_CACHE_DIR` the subprocess uses, before driving the push.

Cache build + mirror-ref populate precedent (crates/reposix-remote/tests/bus_precheck_b.rs:110-115):
```rust
let cache_root = tempfile::tempdir().expect("cache_root");
let _env = CacheDirGuard::new(cache_root.path());
let backend: Arc<dyn BackendConnector> = sim_backend(&server);
let cache = Cache::open(backend, "sim", project).expect("Cache::open");
cache.sync().await.expect("seed sync (warm cache cursor)");
drop(cache);   // release the lock before the subprocess reopens the same dir
```

Redaction convention (crates/reposix-remote/src/bus_handler.rs:453-458) — apply ONLY if
the final hint interpolates an actual remote name/URL (see <load_bearing_constraint>):
```rust
let safe_url = crate::backend_dispatch::redact_userinfo(mirror_url);
let safe_stderr = crate::backend_dispatch::redact_userinfo(stderr.trim());
```
</interfaces>
</context>

<load_bearing_constraint>
- The literal substring `git pull --rebase` MUST survive verbatim in write_loop.rs. Do
  NOT touch the RPX-0505 diag at write_loop.rs:197-200 (`"...Run: git pull --rebase..."`).
  Pinned by ≥12 doc-alignment rows + `push_conflict.rs:198`, `bus_precheck_b.rs:191`,
  `agent_flow.rs:188`. Your edits ADD lines only.
- Do NOT rewrite the bus push's mirror fan-out (bus_handler.rs `push_mirror` /
  mirror-egress). That is GTH-V15-38 "Option C", NOT sanctioned this milestone.
- Do NOT recommend `reposix sync --reconcile` as a MIRROR-drift remedy in any new prose —
  `--reconcile` rebuilds only the LOCAL cache, never the external GitHub mirror. It is the
  correct fix for the LOCAL-CACHE staleness the hint's own sentence addresses ("refresh
  your cache against the SoT"); the Pattern-C line is what addresses the mirror. Keep the
  two concepts distinct exactly as `docs/concepts/dvcs-topology.md:90` does.
- `write_loop.rs` is NOT a bound `source` in doc-alignment.json (verified: its single
  appearance is in a `rationale` prose field; the pinned rows bind `main.rs` + test
  files). Editing write_loop.rs therefore does not drift any doc-alignment source hash.
  troubleshooting.md IS a bound doc — Task 3 handles its walk-gate check.
</load_bearing_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Add the failing (RED) mirror-lag-hint regression test — catalog-first</name>
  <files>crates/reposix-remote/tests/push_conflict.rs</files>
  <read_first>
    - crates/reposix-remote/tests/push_conflict.rs (the sibling test
      `stale_base_push_emits_fetch_first_and_writes_no_rest` at lines 106-217 is the exact
      analog — reuse `sample_issue`, `render_with_overrides`, `one_file_export`, the
      wiremock `MockServer` setup, and the `Command::cargo_bin("git-remote-reposix")` drive)
    - crates/reposix-remote/tests/bus_precheck_b.rs (lines 98-201 — the
      `Cache::open` + `cache.sync().await` + `drop(cache)` precedent, and the imports:
      `Cache`, `CacheDirGuard`, `sim_backend`, `BackendConnector`, `Arc`)
    - crates/reposix-remote/src/write_loop.rs (lines 189-224 — the mirror-lag branch the
      test must drive; note it fires ONLY when `read_mirror_synced_at(backend_name)` is Some)
    - crates/reposix-cache/src/mirror_refs.rs (line 155 — `write_mirror_synced_at` signature)
  </read_first>
  <action>
    Add ONE new `#[tokio::test]` fn to push_conflict.rs (sibling to
    `stale_base_push_emits_fetch_first_and_writes_no_rest`). Name it
    `mirror_lag_reject_hint_recommends_reconcile_and_remote_explicit_rebase`.

    Directly above the `fn` signature (within the 6-line window per quality/CLAUDE.md
    "Marker placement window"), copy the honesty-marker shape from this file's line 110:
    `// test-name-honesty: ok — drives helper export via stdin against wiremock; genuine mirror-lag-hint coverage`

    Setup (the ONE new step vs the sibling test — populate the mirror-lag ref so the
    branch under test fires):
    1. Build the wiremock `MockServer` and mount the same GET mocks the sibling test
       uses so the backend has issue 2 at version=2 (copy lines 112-133 verbatim; the
       strict PATCH/POST/DELETE `.expect(0)` mocks at 135-152 are OPTIONAL here — you may
       keep them to also prove no writes fire).
    2. Create the per-test cache dir the SAME way the subprocess will consume it:
       `let cache_dir = tempfile::tempdir().expect("cache_dir");`
       `let cache_path = cache_dir.path().to_path_buf();`
    3. Pre-build + pre-populate the mirror-lag ref (this is the load-bearing new step):
       open a `Cache` on that dir with backend host `"sim"` and project `"demo"`, warm it
       (`cache.sync().await`), then call
       `cache.write_mirror_synced_at("sim", chrono::Utc::now() - chrono::Duration::minutes(5))`,
       then `drop(cache)` to release the lock — mirroring bus_precheck_b.rs:110-115. Use
       backend host `"sim"` (NOT `"confluence"`; the URL scheme below is
       `reposix::{server.uri()}/projects/demo`, a sim single-backend push).
    4. Drive the stale-base push exactly as the sibling test does (lines 154-181): a
       `render_with_overrides(2, "issue 2", "edited body\n", 1, 2)` blob →
       `one_file_export("issues/2.md", ...)` → `Command::cargo_bin("git-remote-reposix")`
       with args `["origin", &url]`, `.env("REPOSIX_CACHE_DIR", &cache_path)`,
       `.write_stdin(...)`, run under `spawn_blocking`.

    Assertions (the RED contract — these FAIL against current write_loop.rs, which emits
    bare `reposix sync` + `git rebase` with no `--reconcile` and no Pattern-C line):
    ```rust
    // Prove the mirror-lag branch actually fired (guards against a mis-setup
    // false-negative that looks like a missing-impl failure):
    assert!(
        stderr.contains("last synced from"),
        "mirror-lag branch did not fire — refs/mirrors/<sot>-synced-at was not populated: {stderr}"
    );
    // SC3 bug (a): the corrected --reconcile flag on the cache-refresh hint:
    assert!(
        stderr.contains("reposix sync --reconcile"),
        "stderr still recommends the no-op bare `reposix sync` (missing --reconcile): {stderr}"
    );
    // SC3 bug (b): the Pattern-C remote-explicit augmentation:
    assert!(
        stderr.contains("git pull --rebase <reposix-remote-name> main")
            || stderr.contains("reposix attach"),
        "stderr missing the Pattern-C bare-pull-reads-stale-mirror augmentation hint: {stderr}"
    );
    // Pin survives (this must stay TRUE both before and after — never removed):
    assert!(
        stderr.contains("git pull --rebase"),
        "pinned `git pull --rebase` substring vanished: {stderr}"
    );
    ```
    Match the existing `stderr.contains(...)` + descriptive-panic pattern used throughout
    this file (lines 189-216).

    Add any missing imports (`Cache`, `CacheDirGuard`, `sim_backend`, `BackendConnector`,
    `std::sync::Arc`, `chrono`) — cross-reference bus_precheck_b.rs's import block for the
    exact paths. If a helper like `sim_backend`/`CacheDirGuard` lives in a shared test
    module not visible to push_conflict.rs, replicate the minimal inline equivalent rather
    than adding a cross-file `mod` — keep the test self-contained.

    Commit RED with message `test(125-01): add failing mirror-lag augmented-hint regression`.
    This is the phase's catalog-first artifact for SC3 — the failing test is committed
    BEFORE the impl that makes it pass (per quality/CLAUDE.md catalog-first rule).
  </action>
  <verify>
    <automated>cargo test -p reposix-remote mirror_lag_reject_hint_recommends_reconcile_and_remote_explicit_rebase -- --exact 2>&1 | grep -qE "FAILED|panicked|assertion" && echo RED-as-expected</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mirror_lag_reject_hint_recommends_reconcile_and_remote_explicit_rebase" crates/reposix-remote/tests/push_conflict.rs` returns `1`
    - `grep -q "write_mirror_synced_at" crates/reposix-remote/tests/push_conflict.rs` succeeds
    - `grep -q "reposix sync --reconcile" crates/reposix-remote/tests/push_conflict.rs` succeeds (assertion present)
    - `grep -q "test-name-honesty: ok" crates/reposix-remote/tests/push_conflict.rs` succeeds within 6 lines above the new fn
    - The new test FAILS when run against the unmodified write_loop.rs (RED committed)
    - No existing assertion in this file was removed (the sibling test's `git pull --rebase` + `[RPX-0505]` asserts are untouched)
  </acceptance_criteria>
  <done>The new test exists, compiles, and FAILS (RED) against current write_loop.rs, committed as the catalog-first first commit.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the write_loop.rs mirror-lag hint (GREEN)</name>
  <files>crates/reposix-remote/src/write_loop.rs</files>
  <read_first>
    - crates/reposix-remote/src/write_loop.rs (lines 189-225 — the exact block to edit)
    - docs/concepts/dvcs-topology.md (line 90 — the already-correct `reposix sync --reconcile`
      phrasing the code must align TO; this is the fix-twice source of truth)
    - crates/reposix-remote/src/bus_handler.rs (lines 453-458 — redaction convention, only
      relevant if you choose to thread a real remote name; see action)
  </read_first>
  <action>
    Edit the `if let Ok(Some(synced_at)) = c.read_mirror_synced_at(backend_name)` arm
    (write_loop.rs lines 208-219). Two edits, both ADDITIVE-or-align, neither touching
    line 197-200's pinned `Run: git pull --rebase` RPX-0505 diag.

    EDIT (a) — Pitfall 2, align the cache-refresh hint to the doc. REPLACE the current
    line 216-218 diag:
    ```rust
    crate::diag(&format!(
        "hint: run `reposix sync` to update local cache from {backend_name} directly, then `git rebase`",
    ));
    ```
    WITH the doc's already-correct phrasing (dvcs-topology.md:90 / troubleshooting.md:241),
    which also ADDS another instance of the pinned `git pull --rebase` substring (safe):
    ```rust
    crate::diag(
        "hint: run `reposix sync --reconcile` to refresh your cache against the SoT, then `git pull --rebase`",
    );
    ```
    (This is a plain `&str` — the interpolated `{backend_name}` is intentionally dropped to
    match the doc's canonical wording; there is nothing dynamic left to interpolate.)

    EDIT (b) — Pitfall 4, ADD a NEW `crate::diag` call immediately AFTER edit (a)'s line,
    still inside the same `if let Ok(Some(synced_at)) = ...` arm (so it fires only on the
    post-first-sync Pattern-C shape). Use the generic-placeholder wording — it interpolates
    NOTHING dynamic, so no redaction is needed and no `apply_writes` signature change is
    required (RESEARCH.md Assumption A3 / Open Question 1 — the safe default):
    ```rust
    crate::diag(
        "hint: if this tree was created via `reposix attach`, a bare `git pull`/`git rebase` \
         reads the ORIGIN MIRROR by default (which may itself be stale) — rebase against the \
         SoT-backed remote explicitly, e.g. `git pull --rebase <reposix-remote-name> main`",
    );
    ```
    DO NOT thread the actual bus-remote name/URL into this string unless you also route it
    through `crate::backend_dispatch::redact_userinfo(...)` first (bus_handler.rs:453-458)
    — but the generic `<reposix-remote-name>` placeholder is the recommended, lower-risk
    default and needs no redaction because no remote byte is interpolated.

    Leave the `"hint: your origin (GH mirror) was last synced from {sot} at {ts} ..."`
    diag (lines 211-215) unchanged. Leave the RPX-0505 diag (197-200) unchanged.

    Run `cargo test -p reposix-remote` — the Task-1 test now passes (GREEN) and every
    existing pinned-substring test (`push_conflict.rs:198`, `bus_precheck_b.rs:191`) still
    passes. Commit GREEN with `fix(125-01): teach --reconcile + Pattern-C remote-explicit rebase in mirror-lag hint`.
  </action>
  <verify>
    <automated>cargo test -p reposix-remote 2>&1 | tail -5 | grep -qE "test result: ok" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reposix sync --reconcile" crates/reposix-remote/src/write_loop.rs` returns `1`
    - `grep -c "reposix sync\`" crates/reposix-remote/src/write_loop.rs` shows NO bare `reposix sync` without `--reconcile` remaining (the old no-op hint is gone)
    - `grep -q "reads the ORIGIN MIRROR by default" crates/reposix-remote/src/write_loop.rs` succeeds (Pattern-C line added)
    - `grep -q "Run: git pull --rebase" crates/reposix-remote/src/write_loop.rs` still succeeds (RPX-0505 pinned diag intact at line ~198)
    - `cargo test -p reposix-remote` full pass (Task-1 test GREEN; `push_conflict` + `bus_precheck_b` + `bus_precheck_a` all green)
    - bus_handler.rs mirror fan-out / mirror-egress code is unchanged: `git diff --stat crates/reposix-remote/src/bus_handler.rs` shows no change
  </acceptance_criteria>
  <done>write_loop.rs recommends `reposix sync --reconcile` + the Pattern-C remote-explicit rebase; the pinned substring survives; `cargo test -p reposix-remote` is fully green.</done>
</task>

<task type="auto">
  <name>Task 3: Fix-twice clarification in troubleshooting.md + walk-gate check</name>
  <files>docs/guides/troubleshooting.md</files>
  <read_first>
    - docs/guides/troubleshooting.md (lines 233-283 — the `### Bus-remote fetch first
      rejection` section; the additive note goes here)
    - quality/gates/agent-ux/lib/litmus-flow.sh (lines 94-99 — the canonical prose
      explaining WHY the bus remote must be named explicitly on a Pattern-C tree; your
      new sentence echoes this reasoning)
    - crates/reposix-remote/src/write_loop.rs (the hint you edited in Task 2 — the doc
      note must be consistent with the shipped hint wording)
  </read_first>
  <action>
    Per the fix-twice meta-rule, the doc correction rides in the SAME plan as the code fix.
    The existing recovery block (troubleshooting.md:246-257) shows a BARE `git pull --rebase`
    as "the" recovery. That is correct for a Pattern-B (`reposix init`) tree but misleads a
    Pattern-C (`reposix attach`) tree, where `git pull` reads the stale origin mirror
    (dvcs-topology.md:152). Add ONE additive clarification — do NOT rewrite the section, do
    NOT reword the existing bare `git pull --rebase` recovery lines (they stay for Pattern-B).

    Append a new `>` note immediately after the existing v0.14.0 blockquote (after line 279,
    before line 281's "On conflict, resolve..."), wording:
    > **Pattern-C (`reposix attach`) trees — name the bus remote explicitly.** On an attach
    > tree, git's fetch still reads `origin` (the plain-git mirror), which can lag the SoT;
    > a bare `git pull --rebase` there reconciles against the *stale mirror*, not the
    > backend. Rebase against your SoT-backed bus remote by name, e.g.
    > `git pull --rebase <reposix-remote-name> main && git push <reposix-remote-name> main`
    > — the same remote-explicit recovery the milestone-close litmus uses on purpose
    > (`quality/gates/agent-ux/lib/litmus-flow.sh:94-99`).

    This is purely additive (introduces new prose; it does not alter any existing bound
    line). After the edit, run the doc-alignment walk gate. If — and only if — the walk
    gate reports a drift/rebind on a troubleshooting.md-bound row, re-run the bind via the
    MINT TOOL in this SAME commit (`reposix-quality doc-alignment bind ...` or
    `/reposix-quality-refresh docs/guides/troubleshooting.md`) — NEVER hand-edit
    doc-alignment.json (quality/CLAUDE.md: rows are mint-only). If the walk gate passes
    clean (expected for a purely additive note), no doc-alignment.json change is needed.

    Commit with `docs(125-01): clarify Pattern-C remote-explicit rebase in troubleshooting`.
  </action>
  <verify>
    <automated>bash quality/gates/docs-alignment/walk.sh >/dev/null 2>&1 && grep -q "Pattern-C" docs/guides/troubleshooting.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Pattern-C (\`reposix attach\`) trees — name the bus remote explicitly" docs/guides/troubleshooting.md` succeeds
    - `grep -q "git pull --rebase <reposix-remote-name> main" docs/guides/troubleshooting.md` succeeds
    - The existing bare `git pull --rebase` recovery lines (246-257) are unchanged (`grep -q "git pull --rebase                 # fetch reconciles" docs/guides/troubleshooting.md` still succeeds)
    - `bash quality/gates/docs-alignment/walk.sh` exits 0
    - If doc-alignment.json was touched, it was via the mint tool (git blame shows a `reposix-quality`/refresh mint, not a hand-edit) and rode in this same commit
  </acceptance_criteria>
  <done>troubleshooting.md carries the additive Pattern-C clarification, the walk gate passes, and any binding change was minted (not hand-edited) in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper stderr → operator terminal / logs | stderr is a documented exfiltration leg (root CLAUDE.md § Threat model); every remote byte is attacker-influenced, so any URL/remote-name echoed into a hint is a potential credential-leak surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Information Disclosure | `write_loop.rs` mirror-lag hint (Task 2) | mitigate | The shipped default wording interpolates NOTHING dynamic — the corrected `--reconcile` line and the Pattern-C line are both static `&str`; the pre-existing `"last synced from {sot} at {ts}"` line interpolates only `backend_name` (a static config host string, e.g. `sim`/`confluence`) + a timestamp, never a URL or remote-name resolved from a bus URL. No credential can reach stderr. IF a future executor threads an actual git remote name/URL into the hint (RESEARCH.md Assumption A3), it MUST route through `crate::backend_dispatch::redact_userinfo` first (bus_handler.rs:453-458, `crates/CLAUDE.md` § "Credential redaction before ANY error/stderr echo") — this is why the generic `<reposix-remote-name>` placeholder is the mandated default. |
| T-125-02 | Tampering | pinned `git pull --rebase` substring / bus fan-out | accept | Out-of-scope changes (rewording the pin, rewriting the fan-out) are structurally prevented by the acceptance criteria (`grep` gates + `cargo test -p reposix-remote` pinned-substring tests + a `git diff --stat` no-change check on bus_handler.rs). No new attack surface introduced. |
</threat_model>

<verification>
- `cargo test -p reposix-remote` full pass (new augmented-hint test GREEN; all pinned-substring tests green).
- `grep` gates in each task's acceptance_criteria confirm the `--reconcile` fix, the Pattern-C line, and the surviving pin.
- `bash quality/gates/docs-alignment/walk.sh` exits 0.
- `git diff --stat crates/reposix-remote/src/bus_handler.rs` shows no change (Option C untouched).
</verification>

<success_criteria>
SC3 (DRAIN-12): the helper's `git pull --rebase` teaching string is corrected for the
mirror-drift case specifically — it recommends `reposix sync --reconcile` (not the no-op
bare form) and warns that a Pattern-C attach tree's bare pull reads the stale mirror,
while the pinned `git pull --rebase` substring survives verbatim and a new regression
test asserts the augmented content.
</success_criteria>

<output>
After completion, create `.planning/phases/125-real-backend-cadence-mirror-drift-resilience/125-01-SUMMARY.md`
</output>
