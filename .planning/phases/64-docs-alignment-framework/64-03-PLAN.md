---
phase: 64-docs-alignment-framework
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - quality/PROTOCOL.md
  - quality/runners/run.py
  - .githooks/pre-push
  - scripts/hooks/pre-push
  - CLAUDE.md
  - quality/SURPRISES.md
  - quality/reports/verdicts/p64/VERDICT.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/milestones/v0.12.0-phases/ARCHIVE.md
autonomous: true
requirements:
  - DOC-ALIGN-05
  - DOC-ALIGN-07
must_haves:
  truths:
    - "quality/PROTOCOL.md gains a new top-level section 'Two project-wide principles' between the existing 'Runtime contract' section and the 'Failure modes' section. The section MUST contain Principle A (Subagents propose with citations; tools validate and mint) and Principle B (Tools fail loud, structured, agent-resolvable) verbatim from .planning/research/v0.12.0-docs-alignment-design/02-architecture.md § 'Two project-wide principles', with the cross-tool examples enumerated as bullet lists. ≤80 added lines total. Banned-words clean."
    - "quality/runners/run.py gains a new gate row entry in its registry pointing at the docs-alignment dimension. The gate is invoked at cadence=pre-push by shelling to `target/release/reposix-quality walk` (or `target/debug/reposix-quality walk` if release not built; the runner detects). Exit code -> PASS/FAIL/PARTIAL per the existing exit-code mapping. The gate's summary stderr (when non-zero) is forwarded to the runner's stderr verbatim so the slash-command hint reaches the user."
    - ".githooks/pre-push (and the chained scripts/hooks/pre-push if separate) is unchanged structurally — the existing chain through python3 quality/runners/run.py --cadence pre-push picks up the new gate via the registry. Verify by running `bash scripts/test-pre-push.sh` if it exists, or by manually invoking the hook script with a synthetic ref input. Hook tests PASS."
    - "CLAUDE.md gains:
      (a) A new row in the dimensions matrix under 'Quality Gates — dimension/cadence/kind taxonomy': `docs-alignment` | claims have tests; hash drift detection | `quality/gates/docs-alignment/` (P64 — shipped).
      (b) The 'Orchestration-shaped phases run at top-level, not under /gsd-execute-phase' note from .planning/research/v0.12.0-docs-alignment-design/03-execution-modes.md § 'CLAUDE.md addition' — appended under the existing 'Subagent delegation rules' section. (NOTE: an earlier version of this note was committed in 7abd43c when the phase was scoped; verify whether re-writing or supplementing is needed. If the earlier text already matches the design doc, leave it; otherwise reconcile.)
      (c) A NEW H3 subsection under 'v0.12.0 Quality Gates — milestone shipped 2026-04-27' (or under the per-phase contribution log if structured that way) titled 'P64 -- Docs-alignment dimension framework + skill (added 2026-04-28)'. Strict ≤40 added lines; banned-words clean. Summarizes: the new crate, the empty-state catalog, the 3 freshness rows, the skill files, the 2 project-wide principles in PROTOCOL.md, the runner integration, and the verifier dispatch path."
    - "Total CLAUDE.md size after this plan ≤ 40 KB (project hard cap enforced by ~/.git-hooks/pre-commit). If addition pushes over: archive an older H3 subsection to .planning/milestones/v0.12.0-phases/ARCHIVE.md per the existing pattern + leave a 1-line 'See ARCHIVE.md for archived P56-P60 contribution log' marker in CLAUDE.md."
    - "quality/SURPRISES.md gets ≥1 new entry capturing P64 pivots if any (e.g. syn 2.x impl-block traversal pattern, runner registry plumbing surprises, walker exit-code semantics). At least 1 entry MUST appear; if no real surprises, append a short 'P64 -- no significant surprises; design bundle held' entry naming the design bundle as the reason."
    - "quality/reports/verdicts/p64/VERDICT.md is written by an in-session Path B verifier dispatch following the project precedent (P56-P63). Verdict format follows the existing precedent at quality/reports/verdicts/p61/VERDICT.md. Required fields: phase, mode, dispatched_via='P64-Path-B-in-session', date, verdict (GREEN/RED), per-row grades for each P64 catalog row (3 freshness + the doc-alignment summary block + skill scaffolding row + binary surface row + clippy/fmt/test row), 4-disclosure block (no Task tool, in-session orchestrator, gsd-executor depth-1 limitation, owner-confirms-on-resume). The VERDICT MUST be GREEN before the phase closes. RED -> address findings, re-run, re-grade."
    - ".planning/REQUIREMENTS.md updates: DOC-ALIGN-01..07 are flipped from [ ] to [x] with date stamp 2026-04-28. Existing [x] requirements untouched."
    - ".planning/STATE.md updated: progress.completed_phases bumps from 8 to 9 (P56-P64); progress.percent recomputes (9/10 = 90); pending_phases drops P64 entry (only P65 remains). last_activity field gains a P64 stanza summarizing what shipped. status remains in-progress (P65 still pending)."
    - "Workspace-wide cargo gates run ONCE in this plan's verification step: cargo check --workspace; cargo clippy --workspace --all-targets -- -D warnings; cargo fmt --all -- --check; cargo test --workspace (this is the LAST cargo invocation; respects memory budget by being end-of-plan only)."
    - "Banned word 'replace' MUST NOT appear in any new string anywhere in this plan's diff. Banned-words sweep before commit."
    - "Plan 64-03 ships in TWO atomic commits:
      Commit A: `feat(reposix-quality): runner integration + hook wiring + PROTOCOL.md two principles`
        - quality/runners/run.py + .githooks/pre-push if needed + quality/PROTOCOL.md.
        Validation: pre-push hook test PASS; runner --cadence pre-push exit 0; new doc-alignment gate row PASS or NOT-VERIFIED-acceptable.
      Commit B: `docs(p64): CLAUDE.md update + SURPRISES + REQUIREMENTS flip + STATE + verifier verdict`
        - CLAUDE.md + SURPRISES.md + REQUIREMENTS.md flips + STATE.md + verdict file + ARCHIVE.md if size budget required it.
        Validation: CLAUDE.md ≤40KB; banned-words clean; VERDICT.md GREEN; all 6 cadences green via runner."
  artifacts:
    - path: "quality/PROTOCOL.md"
      max_lines: 700
      provides: "Adds the two-principles section (≤80 lines)."
    - path: "quality/runners/run.py"
      max_lines: 450
      provides: "Adds 1 gate registry entry + per-gate exit-code forwarding for the docs-alignment walker stderr."
    - path: ".githooks/pre-push"
      provides: "Verify no structural change needed; hook chain already invokes the runner."
    - path: "scripts/hooks/pre-push"
      provides: "Same — verify no structural change needed."
    - path: "CLAUDE.md"
      max_lines_added: 50
      provides: "New dimension-matrix row + (possibly already-shipped) orchestration-shaped phases note + 40-line P64 H3 subsection."
    - path: "quality/SURPRISES.md"
      provides: "≥1 P64 entry."
    - path: "quality/reports/verdicts/p64/VERDICT.md"
      provides: "GREEN verifier verdict (Path B)."
    - path: ".planning/REQUIREMENTS.md"
      provides: "DOC-ALIGN-01..07 flipped to shipped."
    - path: ".planning/STATE.md"
      provides: "Progress 8 -> 9 phases; percent 80 -> 90; P64 entry."
    - path: ".planning/milestones/v0.12.0-phases/ARCHIVE.md"
      provides: "If CLAUDE.md size budget required archiving; otherwise unchanged."
  key_links:
    - from: "quality/runners/run.py"
      to: "target/{release,debug}/reposix-quality"
      via: "subprocess from runner gate dispatch"
      pattern: "reposix-quality walk"
    - from: "CLAUDE.md (P64 H3)"
      to: ".planning/research/v0.12.0-docs-alignment-design/"
      via: "in-text reference"
      pattern: "design bundle"
verification:
  - "cargo build -p reposix-quality --release   # ensure target/release/reposix-quality exists for runner"
  - "python3 quality/runners/run.py --cadence pre-push   # exit 0; new docs-alignment gate present"
  - "python3 quality/runners/run.py --cadence pre-pr   # exit 0"
  - "bash scripts/test-pre-push.sh   # if present; otherwise skip with note"
  - "wc -c CLAUDE.md   # total size ≤ 40960 bytes"
  - "bash scripts/banned-words-lint.sh quality/PROTOCOL.md quality/runners/run.py CLAUDE.md quality/SURPRISES.md quality/reports/verdicts/p64/VERDICT.md .planning/REQUIREMENTS.md .planning/STATE.md"
  - "cargo check --workspace   # FINAL workspace-wide check; one-shot at end of plan"
  - "cargo clippy --workspace --all-targets -- -D warnings   # FINAL"
  - "cargo fmt --all -- --check   # FINAL"
canonical_refs:
  - .planning/research/v0.12.0-docs-alignment-design/02-architecture.md
  - .planning/research/v0.12.0-docs-alignment-design/03-execution-modes.md
  - .planning/research/v0.12.0-docs-alignment-design/05-p64-infra-brief.md
  - quality/PROTOCOL.md
  - quality/runners/run.py
  - quality/reports/verdicts/p61/VERDICT.md
  - CLAUDE.md
---

# Plan 64-03: Runner integration + hook + PROTOCOL.md principles + CLAUDE.md + verifier dispatch

## Goal

Wire the new binary into the runner + hook + protocol + project memory, then dispatch the in-session Path B verifier to grade P64. Phase closes on GREEN verdict.

## Steps

1. **Runner integration commit (Commit A).** Add the docs-alignment gate row to `quality/runners/run.py`'s registry. Confirm cadence=pre-push invocation works. Update `quality/PROTOCOL.md` with the two principles. Run pre-push and pre-pr cadences end-to-end; both exit 0. Banned-words clean. Commit A.

2. **CLAUDE.md update + verifier dispatch (Commit B).** Add the dimension-matrix row + P64 H3 subsection. If CLAUDE.md size > 40KB, archive an older subsection to ARCHIVE.md. Append a SURPRISES.md entry. Flip DOC-ALIGN-01..07 in REQUIREMENTS.md. Update STATE.md cursor.

3. **Verifier dispatch (Path B in-session).** With ZERO session context, re-read the catalog rows defining P64's GREEN contract + the artifacts produced by Plans 01 + 02 + this plan's commits. Grade each catalog row PASS/FAIL with rationale. Write the verdict to `quality/reports/verdicts/p64/VERDICT.md` with the 4-disclosure block. If GREEN, commit B and close P64. If RED, address findings, re-grade.

4. **Final workspace cargo gates.** ONE workspace-wide check + clippy + fmt + test. All clean. (This is the LAST cargo invocation in P64.)

## Out of scope

- P65 backfill execution (top-level mode, separate phase).
- Closing any MISSING_TEST or RETIRE_PROPOSED row (v0.12.1).

## Risk + mitigation

- **CLAUDE.md size cap** → archive proactively; the size cap is hard-enforced by the personal pre-commit hook.
- **Verifier RED on first pass** → expected and recoverable; the executor loops back, addresses findings, re-runs walk + tests, re-dispatches.
- **Runner registry shape mismatch** → read 1 existing entry verbatim before adding; copy the exact field shape.
