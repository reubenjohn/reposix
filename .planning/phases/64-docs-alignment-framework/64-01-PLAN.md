---
phase: 64-docs-alignment-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/catalogs/doc-alignment.json
  - quality/catalogs/freshness-invariants.json
  - quality/catalogs/README.md
  - quality/gates/structure/freshness-invariants.py
  - quality/gates/docs-alignment/README.md
autonomous: true
requirements:
  - DOC-ALIGN-04
  - DOC-ALIGN-06
must_haves:
  truths:
    - "P64's FIRST commit writes the catalog rows that define this phase's GREEN contract per quality/PROTOCOL.md Step 3 (catalog-first rule). No crate code, no hook wiring, no PROTOCOL.md/CLAUDE.md prose lands until Wave 1 ships the contracts."
    - "quality/catalogs/doc-alignment.json is created with the empty-state shape: schema_version='1.0', summary={claims_total:0, claims_bound:0, claims_missing_test:0, claims_retire_proposed:0, claims_retired:0, alignment_ratio:1.0, floor:0.50, trend_30d:'+0.00', last_walked:null}, rows:[]. JSON parses; pretty-printed; trailing newline."
    - "quality/catalogs/freshness-invariants.json gains 3 new rows under rows[]: structure/doc-alignment-catalog-present, structure/doc-alignment-summary-block-valid, structure/doc-alignment-floor-not-decreased. Each row has id (kebab-case), kind=mechanical, sources=['quality/catalogs/doc-alignment.json'] for first two and 'git log' commitlog implicit for third, expected.asserts naming the canonical assertion (file exists+parses / summary keys present + alignment_ratio recomputable / floor field never decreases between commits), verifier.script=quality/gates/structure/freshness-invariants.py with --check <slug> dispatch, blast_radius=P0, status=NOT-VERIFIED until Wave 1 verifier branches land + run, waiver=null. cadence=pre-push for the first two; cadence=weekly for the third."
    - "quality/gates/structure/freshness-invariants.py grows 3 new check branches matching the 3 new row IDs. Branch contracts: doc-alignment-catalog-present checks the file exists + json.loads succeeds; doc-alignment-summary-block-valid checks the summary dict has the 9 required keys AND alignment_ratio == claims_bound / max(1, claims_total - claims_retired) (within 0.001 epsilon for float compare); doc-alignment-floor-not-decreased uses subprocess('git', 'log', '-p', '--', file) to walk every commit-touch and assert the floor field never decreased. Each branch follows the existing `--check <slug>` dispatch pattern + emits stderr message naming the relevant slash command on failure (/reposix-quality-refresh for present/valid; /reposix-quality-backfill for floor — though the floor check is not row-actionable, it's an audit signal)."
    - "quality/gates/docs-alignment/README.md is NEW (≤80 lines). Dimension home: 1-line summary; quick-start invocation; row table from quality/catalogs/doc-alignment.json (empty initially — show schema); cross-references to quality/PROTOCOL.md, quality/catalogs/README.md, the design bundle at .planning/research/v0.12.0-docs-alignment-design/, and the skill home at .claude/skills/reposix-quality-doc-alignment/. Mirrors the layout of quality/gates/{structure,docs-build,docs-repro,perf,subjective,code,release}/README.md."
    - "quality/catalogs/README.md gains a 'docs-alignment dimension' subsection under the existing per-dimension headers (≤30 added lines). Documents the row schema (id format, claim, source f:l-l, source_hash, test, test_body_hash, last_verdict states, last_run, last_extracted, last_extracted_by) plus the row-state machine (BOUND, MISSING_TEST, STALE_DOCS_DRIFT, STALE_TEST_DRIFT, STALE_TEST_GONE, TEST_MISALIGNED, RETIRE_PROPOSED, RETIRE_CONFIRMED) and the summary block + floor_waiver shape. Cross-refs .planning/research/v0.12.0-docs-alignment-design/02-architecture.md as source of truth."
    - "Banned word 'replace' MUST NOT appear in any new string. Banned-words sweep via `bash scripts/banned-words-lint.sh` (or canonical `quality/gates/structure/banned-words.sh`) over changed paths before commit lands."
    - "Skill files at .claude/skills/reposix-quality-doc-alignment/{SKILL.md, refresh.md, backfill.md, prompts/extractor.md, prompts/grader.md} and the two thin slash-command skills .claude/skills/reposix-quality-{refresh,backfill}/SKILL.md were preflight-written by the orchestrator. Wave 1 includes them in the catalog-first commit."
    - "ALL Wave 1 changes ship in ONE commit titled `docs(p64): catalog-first -- doc-alignment seed + 3 freshness rows + skill scaffolding (preflight)`. Commit message cites DOC-ALIGN-04, DOC-ALIGN-06, references the design bundle."
  artifacts:
    - path: "quality/catalogs/doc-alignment.json"
      provides: "NEW file. Empty-state schema + summary block + zero rows. Becomes the active catalog for the docs-alignment dimension."
    - path: "quality/catalogs/freshness-invariants.json"
      provides: "Three new rows (catalog-present / summary-block-valid / floor-not-decreased) appended under rows[]. Existing rows untouched."
    - path: "quality/catalogs/README.md"
      provides: "Adds the 'docs-alignment dimension' subsection (≤30 added lines) per progressive-disclosure rule."
    - path: "quality/gates/structure/freshness-invariants.py"
      provides: "Three new --check branches matching the new row slugs. Uses the existing dispatch + JSON-summary pattern."
    - path: "quality/gates/docs-alignment/README.md"
      provides: "NEW dimension home (≤80 lines). Mirrors the layout of every other quality/gates/<dim>/README.md."
    - path: ".claude/skills/reposix-quality-doc-alignment/{SKILL.md,refresh.md,backfill.md,prompts/extractor.md,prompts/grader.md}"
      provides: "Pre-written by orchestrator; included in the catalog-first commit. Normative inputs to subsequent waves (Waves 2-5 implement the binary surface those skills invoke)."
    - path: ".claude/skills/reposix-quality-refresh/SKILL.md"
      provides: "Pre-written by orchestrator; thin slash-command entry that delegates to the umbrella skill."
    - path: ".claude/skills/reposix-quality-backfill/SKILL.md"
      provides: "Pre-written by orchestrator; thin slash-command entry that delegates to the umbrella skill."
  key_links:
    - from: "quality/catalogs/freshness-invariants.json (3 new rows)"
      to: "quality/gates/structure/freshness-invariants.py (3 new --check branches)"
      via: "row.verifier.script"
      pattern: "freshness-invariants.py --check <row-slug>"
    - from: "quality/catalogs/doc-alignment.json (empty catalog)"
      to: "Wave 2+ binary subcommand surface"
      via: "binary reads/writes the catalog at this path"
      pattern: "quality/catalogs/doc-alignment.json"
verification:
  - "python3 -c \"import json; d=json.load(open('quality/catalogs/doc-alignment.json')); assert d['schema_version']=='1.0'; assert d['summary']['floor']==0.5; assert d['rows']==[]; print('PASS')\""
  - "python3 quality/gates/structure/freshness-invariants.py --check structure/doc-alignment-catalog-present"
  - "python3 quality/gates/structure/freshness-invariants.py --check structure/doc-alignment-summary-block-valid"
  - "python3 quality/runners/run.py --cadence pre-push  # exits 0; new rows graded PASS"
  - "bash scripts/banned-words-lint.sh quality/catalogs/doc-alignment.json quality/catalogs/freshness-invariants.json quality/gates/docs-alignment/README.md quality/gates/structure/freshness-invariants.py quality/catalogs/README.md .claude/skills/reposix-quality-doc-alignment/ .claude/skills/reposix-quality-refresh/ .claude/skills/reposix-quality-backfill/"
canonical_refs:
  - .planning/research/v0.12.0-docs-alignment-design/02-architecture.md
  - .planning/research/v0.12.0-docs-alignment-design/05-p64-infra-brief.md
  - quality/PROTOCOL.md
  - quality/catalogs/README.md
  - quality/gates/structure/freshness-invariants.py
---

# Plan 64-01: Catalog-first commit — seed + 3 freshness rows + skill scaffolding

## Goal

Land the catalog-first commit per the v0.12.0 QG-06 rule before any binary code is written. This commit defines the GREEN contract that subsequent waves must satisfy.

## Steps

1. **Read source-of-truth bundle.** Re-read `02-architecture.md` § "Catalog row schema" and § "Hash semantics" + `05-p64-infra-brief.md` § "Catalog rows P64 ships". This plan's contract derives directly from those sections.

2. **Write `quality/catalogs/doc-alignment.json`.** Empty-state shape per the brief. Pretty-printed (2-space indent), trailing newline.

3. **Append 3 rows to `quality/catalogs/freshness-invariants.json`.** Read the file; append 3 new objects to `rows`; preserve existing rows verbatim; preserve summary metadata; pretty-printed JSON. Each row's schema follows the existing pattern in the file.

4. **Extend `quality/gates/structure/freshness-invariants.py`.** Add 3 new `--check <slug>` branches. Wire them through the existing dispatch table at the bottom of the file. Each branch is ≤30 lines.

5. **Write `quality/gates/docs-alignment/README.md`** as the new dimension home. Mirror `quality/gates/structure/README.md` shape: 1-line summary, quick-start invocation, row table (empty for now), cross-references.

6. **Update `quality/catalogs/README.md`** with the docs-alignment dimension subsection.

7. **Verify.** Run the verification commands listed above; all must exit 0 before commit.

8. **Banned-words sweep.** `bash scripts/banned-words-lint.sh <changed-paths>` exits 0.

9. **Commit.** Single commit per the title in `must_haves.truths`. Include all skill files (preflight) + catalog rows + dimension README + structure verifier extensions in this commit.

## Out of scope (later waves)

- Crate skeleton (Wave 2)
- Subcommand implementations (Waves 2–4)
- Hash binary (Wave 4)
- Hook wiring (Wave 5)
- PROTOCOL.md / CLAUDE.md updates (Waves 5–6)
- Verifier dispatch (Wave 6)
