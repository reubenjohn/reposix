---
phase: 91-attach-sync-real-backend-wiring
plan: 05
type: execute
wave: 5
depends_on: [91-01, 91-02, 91-03, 91-04]
files_modified:
  - quality/gates/agent-ux/milestone-close-vision-litmus.sh
autonomous: false
# RBF-A-05 primary owner is 91-04 (PLAN-CHECK SF-3); listed here ONLY as the real-mirror analog
# (litmus prep per D91-07) — the verifier grades RBF-A-05 against 91-04's dvcs-third-arm populate.
requirements: ["RBF-A-05 (real-mirror analog; primary owner 91-04)"]
must_haves:
  truths:
    - "The litmus script asserts the resolved target is one of the sanctioned three, hard-FAIL otherwise (D90-06)"
    - "Env-unset -> exit 75; creds-present-but-unreachable / non-sanctioned -> hard-FAIL (not 75) per OD-2"
    - "The script drives vanilla-clone + reposix attach confluence::TokenWorld + edit + push + dual-audit assert (coordinator-verified — requires creds; executor verifies the env-unset exit-75 path only)"
    - "A shell-subprocess transcript is emitted via lib/transcript.sh"
    - "The mirror repo (reubenjohn/reposix-tokenworld-mirror) is verified present/populated"
  artifacts:
    - path: "quality/gates/agent-ux/milestone-close-vision-litmus.sh"
      provides: "real litmus body per D91-06 8 steps (no longer an unconditional exit-75 stub)"
  key_links:
    - from: "quality/gates/agent-ux/milestone-close-vision-litmus.sh"
      to: "docs/reference/testing-targets.md sanctioned targets"
      via: "in-body target assertion (the D90-06 proof obligation)"
      pattern: "TokenWorld|reubenjohn/reposix|KAN"
---

<objective>
Replace the unconditional exit-75 stub `milestone-close-vision-litmus.sh` with the real litmus body (D91-06,
8 steps) — the single highest-leverage deliverable of P91 (until it stops exiting 75, the P0
milestone-close-vision-litmus-real-backend row can never be anything but NOT-VERIFIED, blocking every future
milestone close per OD-2). Leave the substrate ready: mirror repo verified/populated, script executable,
transcript lib wired. The ACTUAL fresh-agent T2 run is the COORDINATOR's phase gate (D91-06/D91-07) — NOT an
executor task; do NOT flip the catalog row's status.

Purpose: this script is what the coordinator's REOPEN gate mechanically anchors on.
Output: a real, sanctioned-target-asserting litmus script + verified mirror substrate.
</objective>

<ownership_charter>
Binding: (1) floor not ceiling; (2) noticing is a deliverable; (3) eager-fix <1h else file; (4) verify against
reality; (5) polish for adoption.
</ownership_charter>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/91-attach-sync-real-backend-wiring/91-DECISIONS.md   (D91-06, D91-07)
@.planning/phases/91-attach-sync-real-backend-wiring/91-RESEARCH-framework.md   (§ B(b), § B(c) 8 steps, § A(c) exit codes)
@docs/reference/testing-targets.md   (sanctioned targets + cleanup convention)

<interfaces>
Current stub (quality/gates/agent-ux/milestone-close-vision-litmus.sh): 24 executable lines, writes a
substrate_not_landed NOT-VERIFIED artifact and `exit 75` unconditionally. Keep its exit-code discipline as the
template but replace the body.

The 8-step body to write (D91-06 / framework B(c)):
1. Resolve + assert sanctioned target IN-BODY: Confluence tenant reuben-john space TokenWorld / GitHub
   reubenjohn/reposix / JIRA TEST|$JIRA_TEST_PROJECT|$REPOSIX_JIRA_PROJECT (KAN per owner). Hard-FAIL (non-75)
   on anything else — THIS is the D90-06 proof obligation, must not be prose-only.
2. Preflight: `bash scripts/preflight-real-backends.sh` — exit 0 required. exit 1/2 → this row exits 75, BUT at
   milestone-close, creds-present-yet-unreachable leans hard-RED "substrate exists, cannot execute" (OD-2) —
   comment this distinction explicitly in the script.
3. Fresh-agent commands: git clone the GH mirror → cd → reposix attach confluence::TokenWorld --remote-name
   reposix → edit a page md → git commit → git push reposix main.
4. 5 pass boxes asserted mechanically: vanilla clone obtained; attach modified git config
   (extensions.partialClone=reposix, remote.reposix.url present); edit+commit succeeded; git push succeeded
   (real helper round-trip, not synthetic stream); server-side change confirmed via REST (reposix list ...).
5. Dual-table audit assert: query BOTH audit_events_cache AND audit_events for this run's rows (OP-3).
6. HIGH-friction hard-FAIL: if the documented happy path DISAGREES with binary behavior (T2 F6/F7 shape),
   FAIL loud — do NOT downgrade to NOT-VERIFIED (OD-2).
7. Transcript: write quality/reports/transcripts/milestone-close-vision-litmus-real-backend-<RFC3339>.txt via
   quality/gates/agent-ux/lib/transcript.sh (argv + env var NAMES only + cwd + exit_code + stdout/stderr).
8. Cleanup: kind=test labels swept; NEVER delete pages 7766017/7798785. Env unset → exit 75 stays legitimate.

Constraints: push path works on git 2.25 (export capability — 91-02 proved real pushes run); any step requiring
helper stateless-connect FETCH must degrade explicitly, not fake. Exit codes (framework A(c)): 0→PASS,
2→PARTIAL, 75→NOT-VERIFIED, else→FAIL. Never exit 1 for a recoverable precondition (would overwrite the honest
75). Do NOT add a waiver to the row (OD-2 SystemExit). Do NOT change the row's status or asserts — the existing
asserts already match this body.

Mirror substrate (D91-07): verify reubenjohn/reposix-tokenworld-mirror exists and is populated (gh api /
git ls-remote). If empty/stale, populating it is in-scope litmus prep (also the real analog of RBF-A-05's
populated mirror). This is a real-backend GH operation — sanctioned target, kind=test etiquette.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite milestone-close-vision-litmus.sh with the real 8-step body (D91-06)</name>
  <files>quality/gates/agent-ux/milestone-close-vision-litmus.sh</files>
  <action>
Replace the unconditional exit-75 stub with the 8-step body above. Keep the env-unset → exit-75 path (runner
env-gate short-circuits before this, but defensive). Assert the sanctioned target in-body (step 1, hard-FAIL on
non-sanctioned). Drive the real vanilla-clone + attach + edit + push flow (step 3), assert the 5 pass boxes
(step 4) + dual-table audit (step 5), hard-FAIL on HIGH friction (step 6), emit the transcript (step 7), clean
up with the protected-page guard (step 8). Comment the OD-2 exit-code distinctions inline. Make executable.
Do NOT touch the catalog row.
  </action>
  <verify>bash quality/gates/agent-ux/milestone-close-vision-litmus.sh; echo "exit=$?"  (with no creds: exit 75, honest NOT-VERIFIED); shellcheck if available</verify>
  <done>Script no longer unconditionally exits 75 when creds ARE set; asserts sanctioned target; emits transcript; env-unset still exits 75; row untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The rewritten litmus script + verified mirror substrate.</what-built>
  <how-to-verify>
The executor CANNOT run the real TokenWorld flow without the owner's creds + non-loopback allowlist, and MUST
NOT self-grade the REOPEN gate (D91-07). Coordinator/owner action required BEFORE phase close:
1. Confirm reubenjohn/reposix-tokenworld-mirror exists + is populated (the executor reports its verification;
   if it populated the mirror, confirm the kind=test etiquette was honored and 7766017/7798785 untouched).
2. With creds + REPOSIX_ALLOWED_ORIGINS=https://reuben-john.atlassian.net set, the COORDINATOR runs the litmus
   script AND dispatches a fresh dark-factory-style subprocess agent to execute the T2-attach flow against
   TokenWorld; grade the transcript for HIGH frictions (documented happy path disagrees with binary behavior).
3. ≥1 HIGH ⇒ phase REOPENS (fix, re-run). This gate runs AFTER 91-06's push + CI green, per 91-06's ordering.
  </how-to-verify>
  <resume-signal>Coordinator confirms mirror substrate verified + acknowledges the T2 REOPEN gate is theirs to run post-close.</resume-signal>
</task>

</tasks>

<verification>
- `bash quality/gates/agent-ux/milestone-close-vision-litmus.sh` exits 75 without creds (honest); does NOT
  unconditionally exit 75 when creds are present (it proceeds to the real flow).
- Mirror repo existence/population verified (report the gh api / git ls-remote evidence).
- Transcript path wiring present; no waiver added to the row; row status unchanged (NOT-VERIFIED).
</verification>

<success_criteria>
Litmus stub replaced with a real sanctioned-target-asserting body; substrate ready; the REOPEN gate is
mechanically anchored and left for the coordinator to execute.
</success_criteria>

<out_of_scope>
Do NOT flip the milestone-close-vision-litmus-real-backend row status or add a waiver (OD-2). Do NOT run the
fresh-agent T2 grade yourself (coordinator's phase gate). Do NOT touch pages 7766017/7798785. NO REQUIREMENTS/
CLAUDE flips (91-06).
</out_of_scope>

<what_to_notice>
- If preflight-real-backends.sh's exit-code semantics don't cleanly map to the OD-2 75-vs-hard-FAIL distinction,
  note the ambiguity in the script comments + SUMMARY.
- If the helper stateless-connect FETCH is needed for step 3's server-side confirm and it can't run on git 2.25,
  document the explicit degradation (REST read via `reposix list` instead) — do NOT fake it.
- If the mirror repo is empty and populating it is larger than expected, checkpoint to coordinator rather than
  improvising a large seed.
</what_to_notice>

<commit>
- feat(agent-ux): real milestone-close vision-litmus body — sanctioned-target assert + T2 flow + dual audit (D91-06)
Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
</commit>

<output>
Create `.planning/phases/91-attach-sync-real-backend-wiring/91-05-SUMMARY.md` — include mirror-substrate
verification evidence and the OD-2 exit-code decisions made in the script.
</output>
