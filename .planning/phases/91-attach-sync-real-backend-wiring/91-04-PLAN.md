---
phase: 91-attach-sync-real-backend-wiring
plan: 04
type: execute
wave: 4
depends_on: [91-01, 91-02, 91-03]
files_modified:
  - quality/gates/agent-ux/dark-factory/dvcs-third-arm.sh
  - quality/catalogs/agent-ux.json
  - crates/reposix-confluence/tests/contract.rs
  - docs/reference/testing-targets.md
  - .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md
autonomous: true
requirements: [RBF-A-05]
must_haves:
  truths:
    - "dvcs-third-arm reconciliation report names non-zero counts in >=3 cases (matched/no_id/backend_deleted/mirror_lag)"
    - "contract_confluence_live_hierarchy self-seeds its parent+child and does not depend on ambient live state"
    - "testing-targets.md documents pages 7766017/7798785 as protected durable fixtures"
  artifacts:
    - path: "quality/gates/agent-ux/dark-factory/dvcs-third-arm.sh"
      provides: "NON-EMPTY work tree + populated bare mirror exercising the 5 reconciliation cases"
    - path: "crates/reposix-confluence/tests/contract.rs"
      provides: "self-seeding hierarchy test (create parent+child, assert, teardown)"
    - path: "docs/reference/testing-targets.md"
      provides: "protected-fixture note (never delete 7766017/7798785)"
  key_links:
    - from: "quality/gates/agent-ux/dark-factory/dvcs-third-arm.sh"
      to: "reconciliation walk counts"
      via: "seeded content shaped to each of the 5 cases"
      pattern: "matched=[1-9]"
---

<objective>
Close p86 F13 (RBF-A-05): make the `dvcs-third-arm` harness populate a NON-EMPTY work tree + bare mirror so the
reconciliation walk exercises matched / no_id / backend_deleted / mirror_lag / duplicate-id cases with non-zero,
case-specific counts (not the current shape-only all-zeros regex) — and rewrite the catalog row's asserts so a
buggy reconciliation can no longer hide behind an all-zeros report. Also make the fragile Confluence
live-hierarchy test self-seeding (D91-08) and document the protected durable-fixture pages.

Purpose: today `dvcs-third-arm.sh:131,134` git-init empty repos and assert `matched=0 no_id=0 ...` — vacuous.
Output: honest reconciliation coverage + a self-sufficient hierarchy test + a committed protected-fixture note.
</objective>

<ownership_charter>
Binding: (1) floor not ceiling; (2) noticing is a deliverable; (3) eager-fix <1h else file; (4) verify against
reality; (5) polish for adoption.
</ownership_charter>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/91-attach-sync-real-backend-wiring/91-DECISIONS.md   (D91-08)
@.planning/phases/91-attach-sync-real-backend-wiring/91-RESEARCH-framework.md   (§ B(a) RBF-A-05, § 2.6)
@.planning/phases/91-attach-sync-real-backend-wiring/91-RESEARCH-code.md   (§ 2.3 reconciliation 5 cases, § 2.6)

<interfaces>
dvcs-third-arm.sh (current empty-tree shape):
- `git init --quiet "$WORK_REPO"` (:131), `git init --bare --quiet "$MIRROR_BARE"` (:134) — no content.
- `spawn_sim seeded` already at :55 — inspect what `seeded` populates; align mirror content to those records'
  frontmatter `id`. Reconciliation regex assert at :169 is shape-only (accepts all-zeros).
RBF-A-05 requires: seed the sim, write matching issues/<id>.md files (frontmatter id) into WORK_REPO before
git init/commit, push into MIRROR_BARE as the vanilla-clone baseline; deliberately omit an id from one file
(no-id case); delete/close one record backend-side after seeding (backend-deleted); advance sim cursor without
updating mirror (mirror-lag); optionally two files sharing an id (duplicate-id hard-abort). Then assert
case-specific non-zero counts. NOTE: the harness's build_and_resolve_bins runs `cargo build --workspace --bins`
— this is the ONE workspace build in P91; it is unavoidable and must run with NO other cargo process active.

Reconciliation 5 cases (reposix-cache/src/reconciliation.rs, module doc :4-15, code :94-229):
matched (:203-227) / backend-deleted+OrphanPolicy (:166-189) / no-id warn+skip (:117-127) / duplicate-id hard
abort zero-rows (:136-144) / mirror-lag count-only (:191-196). Note 91-03 may have adjusted ForkAsNew/Abort —
use the post-91-03 behavior.

catalog row agent-ux/dvcs-third-arm (agent-ux.json:992-1039): rewrite expected.asserts so the reconciliation
assert requires non-vacuous counts (matched>=1 AND at least two other non-zero cases); add minted_at
"2026-07-05T00:00:00Z"; update claim_vs_assertion_audit to explain why an all-zeros report now FAILS. Row is
`pre-pr` cadence (NOT pre-push) so editing it won't break local pre-push. Framework NOTICED #3: fold the
already-weak assert honesty into the same edit.

Confluence hierarchy (D91-08, research §2.6): contract.rs:750-797 (contract_confluence_live_hierarchy),
#[ignore] + skip_if_no_env!, read-only assert on live state. create_record supports parentId (lib.rs:280-290).
Make it self-seeding: create parent+child via create_record(parentId) labeled kind=test, assert hierarchy,
delete in teardown. Recommend the "verify durable fixture pair 7766017/7798785 still exists by get_record, else
create fresh kind=test pair" hybrid (avoids space clutter) — planner's explicit call: prefer the hybrid.
NEVER delete 7766017/7798785.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate dvcs-third-arm work tree + mirror to exercise the 5 reconciliation cases (RBF-A-05)</name>
  <files>quality/gates/agent-ux/dark-factory/dvcs-third-arm.sh, quality/catalogs/agent-ux.json</files>
  <action>
Inspect what `spawn_sim seeded` populates. Before `git init`, write issues/<id>.md files into WORK_REPO with
frontmatter ids: some matching seeded records (matched), one with no id field (no-id), one whose backend record
is deleted/closed after seeding (backend-deleted), leave one seeded record with no local file (mirror-lag).
Commit + push into MIRROR_BARE as the vanilla-clone baseline. Run reposix attach; assert the reconciliation
report names case-specific NON-ZERO counts in >=3 cases (e.g. `matched=1 no_id=1 backend_deleted=1 mirror_lag=1`),
not the shape-only regex. Rewrite the catalog row's reconciliation assert + claim_vs_assertion_audit accordingly;
add minted_at. Keep the TokenWorld leg substrate-gap-deferred behind REPOSIX_DARK_FACTORY_REAL_TOKENWORLD=1.
  </action>
  <verify>bash quality/gates/agent-ux/dark-factory.sh dvcs-third-arm 2>&1 | tail -15  (exits 0; report shows non-zero counts)</verify>
  <done>Harness populates content; reconciliation report has non-zero counts in >=3 cases; row asserts rewritten + minted_at; exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Make contract_confluence_live_hierarchy self-seeding (D91-08) + document protected fixtures</name>
  <files>crates/reposix-confluence/tests/contract.rs, docs/reference/testing-targets.md, .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md</files>
  <action>
Rewrite contract_confluence_live_hierarchy (contract.rs:750-797) as the hybrid: at test start, get_record(7766017)
+ get_record(7798785); if present, assert the parent/child hierarchy against them (read-only, safe); if missing,
create_record a fresh parent+child pair with parentId, labeled kind=test, assert, and delete BOTH in teardown.
NEVER delete 7766017/7798785. Add to docs/reference/testing-targets.md an explicit "Protected durable fixtures —
NEVER delete" note naming pages 7766017 (parent) / 7798785 (child), space REPOSIX (id 360450), label
reposix-durable-fixture, and why (framework NOTICED #2 — the constraint currently lives only in oral tradition).
Flip the hierarchy SURPRISES-INTAKE entry (line ~200) to RESOLVED with SHA.
  </action>
  <verify>cargo test -p reposix-confluence --no-run 2>&1 | tail -5 (compiles); cargo test -p reposix-confluence contract_confluence_live_hierarchy 2>&1 | tail -5 (default-skip without creds); bash quality/gates/docs-build/mkdocs-strict.sh 2>&1 | tail -5</verify>
  <done>Test self-seeds (hybrid), compiles, default-skips; testing-targets.md documents protected pages; mkdocs strict passes; intake RESOLVED.</done>
</task>

</tasks>

<verification>
- `bash quality/gates/agent-ux/dark-factory.sh dvcs-third-arm` exits 0 with non-zero reconciliation counts.
- `cargo test -p reposix-confluence` compiles + default-skips cleanly; clippy/fmt clean.
- `bash quality/gates/docs-build/mkdocs-strict.sh` + `mermaid-renders.sh` pass (docs/ touched).
- catalog loads clean; dvcs-third-arm row shows non-vacuous asserts + minted_at.
</verification>

<success_criteria>
dvcs-third-arm exercises the reconciliation cases for real (closes p86 F13); hierarchy test self-sufficient;
protected fixtures documented in a committed artifact.
</success_criteria>

<out_of_scope>
NO real `git push reposix main` from the harness (that stays P92 — the harness asserts attach + agent-UX
surface only). NO litmus (91-05). NO REQUIREMENTS/CLAUDE flips (91-06). Do NOT touch pages 7766017/7798785.
</out_of_scope>

<what_to_notice>
- If `seeded` doesn't produce enough records to shape all 5 cases, note what it produces and whether the
  duplicate-id hard-abort case can be exercised without aborting the whole harness run (it should be a separate
  sub-invocation that expects non-zero exit).
- If the dvcs-third-arm row's OTHER asserts (teaching strings) are now stale after 91-03's token scrub, fix them here.
- If testing-targets.md's generic cleanup section (:73-79) still invites deleting the durable fixtures, tighten it.
</what_to_notice>

<commit>
Sketch:
- test(agent-ux): populate dvcs-third-arm work tree + mirror, non-zero reconciliation counts (RBF-A-05, p86 F13)
- test(confluence): self-seeding hierarchy test + document protected durable fixtures (D91-08)
Each: Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
</commit>

<output>
Create `.planning/phases/91-attach-sync-real-backend-wiring/91-04-SUMMARY.md`.
</output>
