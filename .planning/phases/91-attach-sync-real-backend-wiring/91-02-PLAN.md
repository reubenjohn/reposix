---
phase: 91-attach-sync-real-backend-wiring
plan: 02
type: execute
wave: 2
depends_on: [91-01]
files_modified:
  - crates/reposix-core/src/path.rs
  - crates/reposix-cache/src/builder.rs
  - crates/reposix-cli/src/refresh.rs
  - crates/reposix-remote/src/fast_import.rs
  - crates/reposix-remote/src/diff.rs
  - crates/reposix-remote/src/main.rs
  - crates/reposix-remote/src/precheck.rs
  - crates/reposix-remote/tests/push_conflict.rs
  - crates/reposix-remote/tests/protocol.rs
  - crates/reposix-remote/tests/bus_write_happy.rs
  - crates/reposix-remote/tests/bus_write_sot_fail.rs
  - crates/reposix-remote/tests/bus_write_mirror_fail.rs
  - crates/reposix-remote/tests/bus_write_post_precheck_409.rs
  - crates/reposix-remote/tests/bus_write_audit_completeness.rs
  - crates/reposix-remote/tests/perf_l1.rs
  - quality/catalogs/agent-ux.json
  - quality/gates/agent-ux/ql-001-canonical-path.sh
  - .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md
autonomous: true
requirements: [QL-001]
must_haves:
  truths:
    - "A push of the full seeded tree in issues/<id>.md shape produces zero spurious Create/Delete"
    - "A no-op push writes zero backend mutations (BUG-3 stream-parser fix)"
    - "A tree with .reposix/ metadata pushes without invalid-blob rejection (issues/*.md filter)"
    - "All four path-shape sites produce one canonical spelling, grep-verifiable"
  artifacts:
    - path: "crates/reposix-core/src/path.rs"
      provides: "record_path(id) + shared issue_id_from_path (issues/<id>.md, padding-agnostic)"
      contains: "pub fn record_path"
    - path: "crates/reposix-remote/src/diff.rs"
      provides: "plan() filters to issues/*.md, deletes-win reconciliation, uses core helper"
    - path: "crates/reposix-remote/src/fast_import.rs"
      provides: "peek-one-byte LF consume (not read_line), M/D reconciliation"
  key_links:
    - from: "crates/reposix-remote/src/diff.rs"
      to: "reposix_core::path::issue_id_from_path"
      via: "shared canonical parser"
      pattern: "reposix_core::path::(record_path|issue_id_from_path)"
    - from: "crates/reposix-remote/src/precheck.rs"
      to: "reposix_core::path::issue_id_from_path"
      via: "conflict precheck no longer silently skips real issues/<id>.md paths"
      pattern: "issue_id_from_path"
---

<objective>
Land the QL-001 fix (D90-01, D91-01, D91-02, D91-10): canonicalize the record path to `issues/<id>.md` unpadded
across all producer/consumer sites through ONE shared reposix-core helper; fix the fast-import stream-parser
peek-LF over-consumption (BUG-3) and the M/D reconciliation (BUG-2); re-key the ~10 masking fixtures so they go
RED-if-the-bug-returns; make refresh.rs regenerate deterministically (D91-10); retire the real-git-push-e2e
waiver + re-add pre-pr; flip the ql-001 verifier to real asserts.

Purpose: the push diff planner cannot round-trip a genuinely git-produced push today (spurious Create+Delete on
every push, silent conflict-precheck bypass on real trees). This is a hard dependency of SC-6 (litmus).
Output: canonical path helper + parser fix + honest regression fixtures + real box-local proof.
</objective>

<ownership_charter>
Binding: (1) floor not ceiling; (2) noticing is a deliverable; (3) eager-fix <1h else file; (4) verify against
reality — a claim without an artifact is not done; (5) polish for adoption.
</ownership_charter>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/91-attach-sync-real-backend-wiring/91-DECISIONS.md   (D91-01, D91-02, D91-10)
@.planning/phases/91-attach-sync-real-backend-wiring/91-RESEARCH-code.md   (§ 1.1–1.5 — verified line inventory)
@quality/reports/raise-list-p90.md   (§ 3 — magic-fixture hazards, the masking tests)

<interfaces>
Verified line-cited targets (drift-corrected against 32ba856 — RE-VERIFY before citing in the PR, code shifts):

Existing foundation to BUILD ON (do not write a sixth parser):
- `crates/reposix-core/src/path.rs:57-72` — `validate_record_filename(name)` already padding-agnostic
  (parse::<u64> on `<digits>.md`, final component only, no `issues/` prefix handling).
- `crates/reposix-core/src/path.rs:139-149` — `slug_or_fallback` doc references the "11-digit padded"
  convention (Confluence `pages/`); double-check it is cosmetic-only before ruling out of scope.

Producer/consumer sites to route through the new helper:
- `crates/reposix-cache/src/builder.rs:90` — `format!("{}.md", issue.id.0)` ALREADY canonical (issues/<id>.md
  unpadded); make it call record_path but this is a no-op/adopt, NOT a behavior change. Tree nesting :131-138.
- `crates/reposix-cli/src/refresh.rs:120` — `format!("{:011}.md", …)` 11-pad PRODUCER change → unpadded.
  Bucket resolution :102-105. Write loop :112-124 NEVER deletes stale files → D91-10 wipe-and-rewrite.
- `crates/reposix-remote/src/fast_import.rs:63` — emit side `M 100644 :{} {:04}.md` → record_path.
- `crates/reposix-remote/src/diff.rs:106` — prior-key `format!("{:04}.md", i.id.0)` → record_path.
- `crates/reposix-remote/src/diff.rs:74-77` — `issue_id_from_path` (bare-only) → delete, use core.
- `crates/reposix-remote/src/main.rs:433-436` — QL-157 duplicate `issue_id_from_path` → DELETE, use core.
- `crates/reposix-remote/src/precheck.rs:151-152` — `let Some(id)=issue_id_from_path(path) else {continue}`
  silently skips real paths → route through core so real issues/<id>.md paths are checked.

The two parser bugs (fast_import.rs):
- `:156-157` — `let _ = r.read_line(&mut maybe_nl)` over-consumes an entire line. Fix: peek ONE byte via
  BufRead::fill_buf()+consume(1); consume only if 0x0A. Applies to BOTH blob-data and commit-message-data
  (same handler). This is BUG-3 (swallows the lowest-mark M-line after the commit-message data block).
- `:173-186` — M-insert (`out.tree.insert`, :181) never removed on a later D (:185). Fix at plan()-time
  (diff.rs:169-191 reconciliation loop): filter to issues/*.md before frontmatter::parse (BUG-2, non-issue
  blobs like .reposix/fetched_at.txt reject the push) AND make deletes win over tree for any path in both.

Fixture re-key inventory (raise-list §3 + research §1.2) — re-key to issues/<id>.md so RED-if-bug-returns:
- diff.rs:284 (unchanged_push_emits_no_patches), diff.rs:311 (extra_trailing_newline_is_a_noop)
- push_conflict.rs:154 (one_file_export "0002.md"→"issues/2.md")
- protocol.rs:67,85 (doc + `M 100644 :1 0001.md`)
- bus_write_happy.rs:251, bus_write_sot_fail.rs:245, bus_write_mirror_fail.rs:213,
  bus_write_post_precheck_409.rs:228, bus_write_audit_completeness.rs:214
- perf_l1.rs:311 + its `no_op_tree_export` helper — GREP the helper definition first (research NOTICED #9).
- diff.rs:233-264 cap tests (five/six_deletes_*) do NOT need re-keying (empty parsed.tree) but keep passing
  as a regression signal that cap semantics are unchanged.
- bulk_delete_cap.rs — OPEN THE FILE, grep for bare shape before declaring inventory closed (NOTICED #8).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical record_path + issue_id_from_path to reposix-core; route all 7 sites</name>
  <files>crates/reposix-core/src/path.rs, crates/reposix-cache/src/builder.rs, crates/reposix-cli/src/refresh.rs, crates/reposix-remote/src/fast_import.rs, crates/reposix-remote/src/diff.rs, crates/reposix-remote/src/main.rs, crates/reposix-remote/src/precheck.rs</files>
  <behavior>
    - record_path(42) == "issues/42.md" (unpadded).
    - issue_id_from_path("issues/42.md") == Some(42); issue_id_from_path("issues/00000000042.md") == Some(42)
      (padding-agnostic, builds on validate_record_filename); == None for "pages/x.md", ".reposix/y.txt", "foo.md".
    - After routing: diff.rs prior-key, fast_import emit, refresh producer, builder all spell id 42 identically.
  </behavior>
  <action>
Add `pub fn record_path(id: u64) -> String` ("issues/{id}.md") and `pub fn issue_id_from_path(path: &str)
-> Option<u64>` (strip "issues/" prefix, reuse validate_record_filename for the padding-agnostic stem parse,
return None for non-issues/*.md). Add unit tests in path.rs for the cases above. Then route: delete diff.rs:74-77
and main.rs:433-436 duplicates and import the core fn; change diff.rs:106, fast_import.rs:63, refresh.rs:120,
builder.rs:90 to use record_path; route precheck.rs:151 through the core fn (real paths now parse, not skip).
Per D91-01: after this, no `format!("{:04}.md"`/`format!("{:011}.md"` record-path construction survives outside
reposix-core (fixtures excepted, re-keyed in Task 3).
  </action>
  <verify>cargo test -p reposix-core path 2>&1 | tail -5  (new unit tests pass)</verify>
  <done>Core helper + tests pass; all 7 sites route through it; the two duplicates deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix fast_import peek-LF (BUG-3) + plan() issues/*.md filter & deletes-win (BUG-2); refresh wipe-and-rewrite (D91-10)</name>
  <files>crates/reposix-remote/src/fast_import.rs, crates/reposix-remote/src/diff.rs, crates/reposix-cli/src/refresh.rs</files>
  <behavior>
    - A commit-message `data N` block followed immediately by `M 100644 :1 issues/1.md` no longer swallows the
      M-line: parsed.tree contains issues/1.md.
    - plan() over a tree containing `.reposix/fetched_at.txt` + `issues/1.md` ignores the non-issue path (no
      invalid-blob reject) and only diffs issues/*.md.
    - A stream that adds then deletes the same path yields a net delete (deletes win).
    - refresh over a tree pre-seeded with stale `issues/00000000042.md` regenerates `issues/42.md` and removes
      the stale-padded duplicate; never touches non-record files.
  </behavior>
  <action>
fast_import.rs:156-157 → peek one byte (fill_buf/consume(1)), consume only if 0x0A. diff.rs plan() (:169-191)
→ filter to issues/*.md before frontmatter::parse; make the deletes vec authoritative over tree for shared
paths. refresh.rs → before writing the bucket dir, remove any issues/*.md whose stem parses to a record id but
whose spelling differs from record_path(id) (D91-10); leave non-record files untouched. Add a refresh test with
a pre-seeded stale-padded file.
  </action>
  <verify>cargo test -p reposix-remote fast_import 2>&1 | tail -5 && cargo test -p reposix-cli refresh 2>&1 | tail -5</verify>
  <done>Parser fix + filter + deletes-win + refresh regeneration tests all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-key masking fixtures to issues/<id>.md + add RED-if-bug-returns regression tests</name>
  <files>crates/reposix-remote/src/diff.rs, crates/reposix-remote/tests/push_conflict.rs, crates/reposix-remote/tests/protocol.rs, crates/reposix-remote/tests/bus_write_happy.rs, crates/reposix-remote/tests/bus_write_sot_fail.rs, crates/reposix-remote/tests/bus_write_mirror_fail.rs, crates/reposix-remote/tests/bus_write_post_precheck_409.rs, crates/reposix-remote/tests/bus_write_audit_completeness.rs, crates/reposix-remote/tests/perf_l1.rs</files>
  <action>
Re-key every fixture in the § interfaces inventory from bare `000N.md` to canonical `issues/N.md`. FIRST grep
`no_op_tree_export`'s definition and open `bulk_delete_cap.rs` (NOTICED #8/#9) — re-key or confirm-clean. Add
dedicated regression tests that satisfy QL-001 criteria 1-4 at the diff::plan / parse_export_stream layer
(box-independent, git-2.34-free per D91-02): full-seeded-tree push → zero Deletes; no-op push → zero mutations;
.reposix/ metadata present → no invalid-blob reject; canonical tree round-trips one edit as one PATCH.
PROVE RED-IF-BUG-RETURNS: git-stash the Task 1/2 fix, run the re-keyed tests, confirm they FAIL, unstash.
Paste the actual `cargo test` FAIL output (test names + assertion lines) VERBATIM into the SUMMARY — a
checkable artifact for the verifier subagent, not a prose "confirmed RED" (PLAN-CHECK SF-2; a re-key that
stays green with the bug is worthless).
  </action>
  <verify>cargo test -p reposix-remote 2>&1 | tail -8  (all pass with fix; documented RED without fix)</verify>
  <done>Fixtures canonical; new regression tests pass with fix, fail without; SUMMARY contains the pasted cargo-test FAIL output (SF-2 artifact); bulk_delete_cap + no_op_tree_export audited.</done>
</task>

<task type="auto">
  <name>Task 4: Flip ql-001 verifier to real asserts; retire real-git-push-e2e waiver + re-add pre-pr</name>
  <files>quality/gates/agent-ux/ql-001-canonical-path.sh, quality/catalogs/agent-ux.json, .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md</files>
  <action>
ql-001-canonical-path.sh: enable the real asserts (remove the exit-75 short-circuit) — grep proves zero stray
padding outside core (use `grep -v '^#'` hygiene; exclude tests/ where fixtures legitimately name issues/N.md),
core helper single-source, main.rs duplicate gone; assert the new regression test fn names exist. Exit 0 on
pass. Flip the ql-001 row status to PASS with a real last_verified.
real-git-push-e2e row (agent-ux.json:1339): REMOVE the waiver block; do NOT add minted_at — the row keeps
legacy status per D91-11 (minted 2026-07-04, before the cutoff; stamping 2026-07-05 would be the exact
backdate/postdate dodge the write-once field guards against — PLAN-CHECK MF-2); set transport_claim:false (it drives the LOCAL sim — declaring real-backend would be F-K4a false, A(a) nuance);
re-add "pre-pr" to cadences (owner_hint + D-CONV-1 mandate). Run the script locally: on THIS box (git 2.25.1)
it legitimately exits 75/NOT-VERIFIED — that is the honest environment gap, NOT a failure (D91-02); the e2e
PASS proof is CI (ubuntu-latest, git ≥2.34, pre-pr job). Do NOT fabricate a PASS.
SURPRISES-INTAKE.md: flip the QL-001 BLOCKER entry STATUS to RESOLVED with this plan's commit SHA.
  </action>
  <verify>bash quality/gates/agent-ux/ql-001-canonical-path.sh; echo "exit=$?" (0); bash quality/gates/agent-ux/real-git-push-e2e.sh; echo "exit=$?" (75 on this box — honest); python3 quality/runners/run.py --cadence on-demand 2>&1 | tail -3 (loads clean)</verify>
  <done>ql-001 row PASS (real grep asserts); real-git-push-e2e waiver gone, pre-pr restored, exits 75 locally (documented), catalog loads clean; intake entry RESOLVED.</done>
</task>

</tasks>

<verification>
- `cargo test -p reposix-core`, `-p reposix-remote`, `-p reposix-cli` (refresh), `-p reposix-cache` (builder) pass.
- `cargo clippy -p reposix-remote --all-targets -- -D warnings` clean; `cargo fmt --all -- --check` clean.
- ql-001 verifier exits 0; grep confirms zero stray padding outside reposix-core (tests/ excepted).
- real-git-push-e2e exits 75 locally (git 2.25.1) — recorded as the expected environment gap.
- Documented proof that re-keyed fixtures go RED against the unfixed planner.
</verification>

<success_criteria>
QL-001 six criteria satisfied at the cargo layer (box-independent) + real-git-push-e2e waiver retired + pre-pr
restored; grep-verifiable single canonical spelling; fixtures are honest regression gates.
</success_criteria>

<out_of_scope>
NO attach/sync wiring (91-03). NO lib split. Do NOT touch the litmus row or dvcs-third-arm row. Do NOT attempt
a git ≥2.34 build (optional/timeboxed per D91-02 — if attempted, ≤30min, non-gating, never at budget cost).
If slug_or_fallback (path.rs:139) turns out load-bearing for Confluence pages/, STOP and note — do not silently
rewrite the Confluence page-naming convention here.
</out_of_scope>

<what_to_notice>
- Confirm builder.rs:90 is truly already canonical (no behavior change) — if the tree-nesting at :131-138 has
  drifted, cite the real lines.
- If the peek-LF fix reveals other read_line over-consumption in the same handler, note it.
- If any bus_write_*/perf_l1 assert only passed BECAUSE of the bug shape (not just the path string), that is a
  deeper masking — call it out; the re-key may expose a real second bug.
- Watch for precheck.rs now surfacing conflicts on real trees that were previously silently skipped — this is
  correct but may change the observable behavior of existing conflict tests; reconcile honestly.
</what_to_notice>

<commit>
Sketch (multiple commits acceptable, one per task or logically grouped):
- feat(core): canonical record_path + shared issue_id_from_path (issues/<id>.md); route 7 sites (QL-001 BUG-1)
- fix(remote): peek-one-byte LF + issues/*.md plan filter + deletes-win (QL-001 BUG-2/BUG-3); refresh regen (D91-10)
- test(remote): re-key masking fixtures to canonical shape + RED-if-bug-returns regressions (QL-001)
- docs(91): retire real-git-push-e2e waiver, re-add pre-pr, flip ql-001 row PASS; QL-001 intake RESOLVED
Each: Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
</commit>

<output>
Create `.planning/phases/91-attach-sync-real-backend-wiring/91-02-SUMMARY.md` — include the RED-without-fix
observation, the local exit-75 note, and any deeper masking noticed.
</output>
