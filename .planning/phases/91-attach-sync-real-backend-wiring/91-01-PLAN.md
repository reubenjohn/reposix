---
phase: 91-attach-sync-real-backend-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/catalogs/agent-ux.json
  - quality/gates/agent-ux/ql-001-canonical-path.sh
autonomous: true
requirements: [QL-001, RBF-A-01, RBF-A-02, RBF-A-04]
must_haves:
  truths:
    - "Catalog load (run.py) succeeds with the 2 new rows present"
    - "Both new rows read NOT-VERIFIED with minted_at + coverage_kind set correctly"
  artifacts:
    - path: "quality/catalogs/agent-ux.json"
      provides: "2 new rows: ql-001-canonical-path-shape, attach-sync-real-backend"
    - path: "quality/gates/agent-ux/ql-001-canonical-path.sh"
      provides: "box-independent grep+cargo-name verifier skeleton (exits 75 until 91-02 lands)"
  key_links:
    - from: "quality/catalogs/agent-ux.json"
      to: "run.py load_catalog / _audit_field.validate_row"
      via: "coverage_kind + minted_at + claim_vs_assertion_audit fields"
      pattern: "coverage_kind|minted_at"
---

<objective>
Mint the P91 GREEN contract: two NEW catalog rows, status NOT-VERIFIED, BEFORE any implementation lands
(catalog-first rule, CLAUDE.md § Quality Gates). This plan contains ZERO Rust/implementation edits — only the
catalog rows and a verifier skeleton that legitimately exits 75 until 91-02 fills it in.

Purpose: subsequent plans (91-02, 91-03) cite these row ids; the verifier subagent at phase close reads rows
that existed BEFORE the code.
Output: 2 catalog rows + 1 verifier skeleton script.
</objective>

<ownership_charter>
Binding on this executor: (1) acceptance criteria are the floor, not the ceiling — done = you'd defend this in
review as excellent; (2) noticing is a deliverable — report what you noticed near your work; (3) eager-fix <1h
+ no new dependency, else file to SURPRISES-INTAKE/GOOD-TO-HAVES with severity+sketch; (4) verify against
reality — a claim without an artifact is not done; (5) north star: polish for adoption.
</ownership_charter>

<context>
@.planning/phases/91-attach-sync-real-backend-wiring/91-DECISIONS.md   (D91-11 — catalog honesty)
@.planning/phases/91-attach-sync-real-backend-wiring/91-RESEARCH-framework.md   (§ A(a)/A(b) — row schema)
@quality/catalogs/README.md   (unified row schema, lines 20-49)

<interfaces>
Reference row shapes to mirror (read, do not edit here):
- `quality/catalogs/agent-ux.json:1301-1337` — milestone-close-vision-litmus row (real-backend, waiver:null,
  shell-subprocess, claim_vs_assertion_audit shape — the model for row #2).
- `quality/catalogs/agent-ux.json:1382-1418` — test-name-vs-asserts row (mechanical, minted_at,
  transport_claim:false — the model for row #1).
- `quality/catalogs/agent-ux.json:43-77` — reposix-attach-against-vanilla-clone (agent-ux _provenance_note
  hand-edit convention; bind verb does not cover agent-ux dim).

Load-time gates that WILL SystemExit on a malformed row (`_audit_field.py`):
- minted_at required for last_verified/minted_at ≥ 2026-07-05 (P90_MINT_CUTOFF); add proactively.
- coverage_kind required when the transport regex matches id/comment for {perf,agent-ux,security}; regex =
  `push|fetch|round-?trip|clone|p50|p95|latency|throughput|real[- ]backend|transport|list_records|list_changed_since`.
  For a matched row, only `coverage_kind: real-backend` OR a compliant waiver satisfies the gate —
  `sim-only`/`mechanical`/`manual` are valid enum members (_audit_field.py:54) but do NOT satisfy it, and
  `"sim"` is not a member at all (SystemExit — PLAN-CHECK MF-1). Row #1 must therefore either avoid the
  regex in id+comment OR set `transport_claim: false` (explicit opt-out, which also makes coverage_kind optional).
- claim_vs_assertion_audit required (≥50 chars) for minted_at ≥ 2026-05-08.
- shell-subprocess rows must declare a transcript contract (transcript in expected.asserts or
  expected.artifact.transcript_path).
- pre-release-real-backend cadence rows must NOT carry a waiver block (OD-2 SystemExit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint agent-ux/ql-001-canonical-path-shape (mechanical, sim)</name>
  <files>quality/catalogs/agent-ux.json</files>
  <action>
Add a new row after the real-git-push-e2e row (~line 1380). Fields:
- id: "agent-ux/ql-001-canonical-path-shape"
- dimension: "agent-ux", kind: "mechanical"
- _provenance_note: the standard agent-ux hand-edit note (copy from a sibling row).
- comment: describe the box-independent QL-001 proof — canonical `issues/<id>.md` path shape is grep-verifiable
  (no `{:04}.md`/`{:011}.md` record-path construction survives outside reposix-core) and driven by cargo tests
  against canonical trees. AVOID the transport regex tokens in id+comment (do not write "push"/"round-trip");
  phrase as "record-path canonicalization" / "diff planner path keying".
- sources: crates/reposix-core/src/path.rs, crates/reposix-remote/src/diff.rs,
  crates/reposix-remote/src/fast_import.rs, ".planning/.../SURPRISES-INTAKE.md (QL-001 entry)".
- command / verifier.script: "quality/gates/agent-ux/ql-001-canonical-path.sh", args [], timeout_s 30, container null.
- expected.asserts (4): grep proves zero `format!("{:04}.md"`/`format!("{:011}.md"` record-path construction
  outside crates/reposix-core; a single shared issue_id_from_path exists in reposix-core; the canonical cargo
  regression tests (name them once 91-02 defines them) exist and are referenced; the QL-157 duplicate in
  main.rs is deleted.
- artifact: "quality/reports/verifications/agent-ux/ql-001-canonical-path.json"
- status: "NOT-VERIFIED", last_verified: null, freshness_ttl: null, blast_radius: "P1"
- owner_hint: point at diff.rs / fast_import.rs / path.rs if RED.
- claim_vs_assertion_audit (≥50 chars): state how the grep+name asserts would falsify the claim if a buggy
  spelling returned (a stray `{:04}.md` outside core → grep matches → exit 1).
- minted_at: "2026-07-05T00:00:00Z"  (proactive, per P90 cutoff)
- transport_claim: false   (explicit opt-out — this is a sim/box-local proof, NOT a real-backend transport claim)
- coverage_kind: OMIT entirely (transport_claim: false suppresses the gate — PLAN-CHECK MF-1/N-1;
  if the executor chooses to carry one anyway, the ONLY valid enum values are
  {real-backend, sim-only, mechanical, manual} per _audit_field.py:54 — "mechanical" fits this row;
  "sim" is NOT a member and SystemExits the load)
- waiver: null, cadences: ["pre-pr", "on-demand"]
  </action>
  <verify>python3 quality/runners/run.py --cadence on-demand 2>&1 | head -20  (catalog LOAD precedes gate execution; confirm no `SystemExit`/validate_row error appears — the exit-75 ql-001 skeleton run afterward is expected)</verify>
  <done>Row present; `run.py` loads the catalog without SystemExit; row reads NOT-VERIFIED.</done>
</task>

<task type="auto">
  <name>Task 2: Mint agent-ux/attach-sync-real-backend (shell-subprocess, real-backend)</name>
  <files>quality/catalogs/agent-ux.json</files>
  <action>
Add a new row mirroring the milestone-close-vision-litmus row's real-backend shape (1301-1337). Fields:
- id: "agent-ux/attach-sync-real-backend"
- dimension: "agent-ux", kind: "shell-subprocess"
- _provenance_note: standard agent-ux hand-edit note.
- comment: RBF-A-01/A-02/A-04 — real-backend attach + sync --reconcile against a sanctioned target
  (Confluence TokenWorld / reubenjohn/reposix / JIRA KAN); anchors agent_flow_real.rs attach_real_* /
  sync_real_* #[ignore] smokes gated by pre-release-real-backend cadence.
- sources: crates/reposix-cli/src/attach.rs, crates/reposix-cli/src/sync.rs,
  crates/reposix-cli/tests/agent_flow_real.rs, crates/reposix-remote/src/backend_dispatch.rs.
- command / verifier.script: "quality/gates/agent-ux/attach-sync-real-backend.sh" (script lands in 91-03),
  args [], timeout_s 300, container null.
- expected.asserts (must include a "transcript" mention to satisfy the shell-subprocess load gate): reposix
  attach <backend>::<project> against a sanctioned target configures extensions.partialClone + remote url;
  sync --reconcile lists real records; dual-table audit (audit_events_cache + audit_events) shows rows;
  transcript artifact emitted per RBF-FW-02 convention.
- expected.artifact.transcript_path: "quality/reports/transcripts/attach-sync-real-backend-<RFC3339>.txt"
- artifact: "quality/reports/verifications/agent-ux/attach-sync-real-backend.json"
- status: "NOT-VERIFIED", last_verified: null, freshness_ttl: null, blast_radius: "P1"
- owner_hint: real-backend attach/sync dispatch or audit wiring regressed; inspect transcript.
- claim_vs_assertion_audit (≥50 chars): if attach/sync silently hit the sim instead of the sanctioned backend,
  the transcript argv would not name the real spec and audit_events would lack real-host fingerprints.
- minted_at: "2026-07-05T00:00:00Z"
- coverage_kind: "real-backend"
- waiver: null   (MUST stay null — pre-release-real-backend cadence forbids a waiver, OD-2 SystemExit)
- cadences: ["pre-release-real-backend"]
  </action>
  <verify>python3 quality/runners/run.py --cadence on-demand 2>&1 | head -20  (no SystemExit; waiver-prohibition not tripped — load happens before scope filtering)</verify>
  <done>Row present, NOT-VERIFIED, coverage_kind real-backend, waiver null, transcript contract declared; catalog loads clean.</done>
</task>

<task type="auto">
  <name>Task 3: Write the ql-001-canonical-path.sh verifier skeleton (exits 75 until 91-02)</name>
  <files>quality/gates/agent-ux/ql-001-canonical-path.sh</files>
  <action>
Create an executable verifier that TODAY (before 91-02) writes a NOT-VERIFIED artifact and exits 75 (the code
is not yet canonicalized). Structure it so 91-02 flips it to real asserts:
- set -euo pipefail; resolve REPO_ROOT; mkdir the artifact dir.
- The real asserts (guard them behind a `# 91-02 fills these` block that currently short-circuits to exit 75):
  (1) `grep -rnE 'format!\("\{:04\}\.md"|format!\("\{:011\}\.md"' crates/ | grep -v 'crates/reposix-core/'`
      returns ZERO record-path hits (test fixtures excluded per 91-02's re-key);
  (2) `grep -c` a single `pub fn issue_id_from_path` / `pub fn record_path` in reposix-core (use
      `grep -v '^#'` hygiene — do NOT count comment lines);
  (3) assert the QL-157 duplicate is gone: `issue_id_from_path` absent from crates/reposix-remote/src/main.rs.
- Until 91-02: write `{"status":"NOT-VERIFIED","exit_code":75,"reason":"91-02 not yet landed"}` and exit 75.
- Make executable (chmod +x). Follow the milestone-close-vision-litmus.sh exit-75 convention as the template.
  </action>
  <verify>bash quality/gates/agent-ux/ql-001-canonical-path.sh; echo "exit=$?"  (exit 75, NOT-VERIFIED artifact written)</verify>
  <done>Script executable, exits 75, writes NOT-VERIFIED artifact; grep asserts present but gated off until 91-02.</done>
</task>

</tasks>

<verification>
- `python3 quality/runners/run.py --cadence on-demand` loads all catalogs with no SystemExit (there is NO
  --dry-run flag — run.py:380 defines only --cadence; load-honesty is provable because load precedes execution).
- Both new rows appear NOT-VERIFIED with the fields above.
- `bash quality/gates/agent-ux/ql-001-canonical-path.sh` exits 75.
- Pre-commit hook passes (fmt/banned-words on the shell + json).
</verification>

<success_criteria>
2 rows minted NOT-VERIFIED with correct coverage_kind/minted_at/claim_vs_assertion_audit FIRST TRY (no
SystemExit); verifier skeleton lands exit-75; ZERO implementation code touched.
</success_criteria>

<out_of_scope>
NO Rust edits. NO waiver retirement (that is 91-02 for real-git-push-e2e). NO dvcs-third-arm row edit (91-04).
NO litmus row edit (never — 91-05 touches the script only). Do NOT flip either new row's status.
</out_of_scope>

<what_to_notice>
- If a sibling agent-ux row is missing minted_at but has last_verified ≥ cutoff, note it (may be a latent
  SystemExit for a future edit) — file, don't fix here.
- If the transport regex would match "attach-sync-real-backend"'s id (it does not — no listed token), confirm
  by eye; if you are wrong the coverage_kind:real-backend already covers it.
- N-2: row #2 cites `quality/gates/agent-ux/attach-sync-real-backend.sh`, which 91-03 creates — the path
  dangles between 91-01 and 91-03. Harmless (pre-release-real-backend cadence never runs at load/pre-push/
  on-demand, and pushes land only at 91-06), but note it in the SUMMARY so a mid-phase run.py inspector
  isn't surprised.
</what_to_notice>

<commit>
docs(91): mint P91 catalog rows (ql-001-canonical-path-shape, attach-sync-real-backend) NOT-VERIFIED

Catalog-first: GREEN contract before implementation. + ql-001 verifier skeleton (exit 75 until 91-02).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
</commit>

<output>
Create `.planning/phases/91-attach-sync-real-backend-wiring/91-01-SUMMARY.md`.
</output>
