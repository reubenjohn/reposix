---
phase: 87
plan: 01
type: docs
autonomous: true
wave: 1
depends_on: [P78, P79, P80, P81, P82, P83, P84, P85, P86]
requirements: [DVCS-SURPRISES-01]
---

# Phase 87, Plan 01 — v0.13.0 Surprises Absorption (+2 reservation slot 1)

## Objective

Drain `.planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md` per CLAUDE.md OP-8. Each entry → RESOLVED | DEFERRED | WONTFIX with commit SHA or rationale. Run the verifier honesty spot-check on P78–P86 plans + verdicts (sample ≥3 per ROADMAP P87 SC2). Document v0.13.0 carry-forwards in `.planning/RETROSPECTIVE.md` per OP-9 ahead of P88's milestone-close ritual. **No new Rust code.** Pure planning + docs + catalog.

## Context

- @CLAUDE.md § "Operating Principles" OP-8 (the +2 phase practice + eager-resolution preference + verifier honesty check).
- @CLAUDE.md § "Operating Principles" OP-9 (milestone-close ritual: distill before archiving).
- @.planning/ROADMAP.md § "Phase 87" (verifier honesty contract + 6 success criteria).
- @.planning/REQUIREMENTS.md (DVCS-SURPRISES-01).
- @.planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md (the file being drained — 5 entries, 2 RESOLVED-on-discovery + 3 OPEN).
- @.planning/RETROSPECTIVE.md (the cross-milestone log being appended to).
- @.planning/milestones/v0.12.1-phases/ARCHIVE.md (P76 precedent for surprises-absorption execution).
- @quality/PROTOCOL.md § "Verifier subagent prompt template".

## Strategy

The intake has 5 entries from P78–P86 execution:

1. **Entry 1 (P80, OPEN)** — verifier-shell shape change (cargo-test wrappers vs. planned `reposix init` end-to-end). **Disposition: RESOLVED** via P86 cross-reference (P86 verdict GREEN demonstrates layered coverage shape is sanctioned house pattern; the same framing applies retroactively to P80's mirror-refs verifiers).
2. **Entry 2 (P81, RESOLVED)** — `refresh_for_mirror_head` no-op skip. Already RESOLVED in P81 T04. P87 confirms STATUS line; no action.
3. **Entry 3 (P81, OPEN)** — bind schedule T01→T04 shift. **Disposition: WONTFIX** (schedule-only shift; intent preserved; deeper tooling polish — `bind --test-pending` flag — filed as P88 GOOD-TO-HAVES candidate, not a P87 deliverable).
4. **Entry 4 (P83-02, RESOLVED)** — `make_failing_mirror_fixture core.hooksPath` override. Already RESOLVED in P83-02 T02. P87 confirms STATUS line; no action.
5. **Entry 5 (P84, OPEN)** — binstall + yanked-gix substrate gap. **Disposition: DEFERRED** to v0.13.x (release-pipeline territory; owner-runnable `scripts/webhook-latency-measure.sh` ready for re-measurement post-substrate). Documented in RETROSPECTIVE as v0.13.0 → v0.13.x carry-forward.

Honesty spot-check sample (per ROADMAP SC2): P82 (every-deviation-eager-resolved), P83-01/02 (eager + filed entry for trail), P84 (filed at HIGH for blocker), P85 (empty intake consistent with verdict), P86 (cross-reference vs. double-file). Aggregate finding GREEN.

## Tasks

### Task 1 — Catalog-first commit: mint `agent-ux/p87-surprises-absorption` row + verifier (T01)

**Type:** auto.
**Behavior:** Create `quality/gates/agent-ux/p87-surprises-absorption.sh` (TINY shape; awk-fence-aware so the schema example inside the markdown fence is not counted). Add row to `quality/catalogs/agent-ux.json` with status PASS, kind mechanical, cadence on-demand, blast_radius P3. Hand-edit per documented `bind --dimension agent-ux` gap (GOOD-TO-HAVES-01).

**Asserts (catalog row):**
- bash quality/gates/agent-ux/p87-surprises-absorption.sh exits 0 against the local working tree.
- zero `**STATUS:** OPEN` lines in `.planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md` (all 5 entries flipped to terminal status).
- at least 5 entries carry terminal STATUS (RESOLVED|DEFERRED|WONTFIX).
- `.planning/phases/87-surprises-absorption/honesty-spot-check.md` exists.

**Verification:** `jq '.rows[-1].id' quality/catalogs/agent-ux.json` returns `"agent-ux/p87-surprises-absorption"`. Verifier returns FAIL at T01 commit time (intake still has OPEN entries + honesty file missing); this is intentional — the row asserts the EVENTUAL state. T02 + T03 flip the row to genuinely PASS before push.

**Done criteria:**
- Verifier shell at `quality/gates/agent-ux/p87-surprises-absorption.sh` (executable; ≤ 50 lines).
- Catalog row added to `quality/catalogs/agent-ux.json`.
- Commit message follows pattern: `quality(catalog): mint agent-ux/p87-surprises-absorption row + verifier (P87 T01 catalog-first)`.

### Task 2 — Drain SURPRISES-INTAKE entries 1, 3, 5 + write honesty spot-check (T02)

**Type:** auto.
**Behavior:**
1. Edit `.planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md` flipping the 3 OPEN entries to terminal STATUS:
   - Entry 1 → RESOLVED (P86 verdict cross-reference; cargo-test-as-verifier sanctioned).
   - Entry 3 → WONTFIX (schedule-only shift; tooling polish filed for P88).
   - Entry 5 → DEFERRED (v0.13.0 → v0.13.x carry-forward; release-pipeline territory).
2. Confirm Entry 2 (P81 RESOLVED) and Entry 4 (P83-02 RESOLVED) STATUS lines unchanged (already terminal).
3. Write `.planning/phases/87-surprises-absorption/honesty-spot-check.md` (≥30 lines; ≥3 phase samples per ROADMAP SC2; aggregate GREEN finding).
4. Run verifier (`bash quality/gates/agent-ux/p87-surprises-absorption.sh`) — must return PASS now that intake drained + honesty file present.

**Verification:** `grep -cE '^\*\*STATUS:\*\* (RESOLVED|DEFERRED|WONTFIX)' SURPRISES-INTAKE.md` returns ≥5 (excluding the markdown-fenced schema example). Verifier exit 0.

**Done criteria:**
- 3 OPEN entries flipped (5 terminal STATUS visible in awk-fence-aware count).
- Honesty spot-check artifact present with ≥3 phase samples graded GREEN.
- Verifier passes locally.
- Commit message: `phase(P87): drain SURPRISES-INTAKE entries 1, 3, 5 (RESOLVED|WONTFIX|DEFERRED)`.

### Task 3 — RETROSPECTIVE.md v0.13.0 surprises summary + CLAUDE.md OP-8 note + plan/summary + push (T03)

**Type:** auto.
**Behavior:**
1. Append to `.planning/RETROSPECTIVE.md` a v0.13.0 surprises-absorption section (single concise paragraph) noting the 5-entry drain disposition + the binstall substrate carry-forward to v0.13.x. (Full milestone retrospective lands in P88 per OP-9; P87's contribution is the surprises summary specifically.)
2. Append to `CLAUDE.md` OP-8 section a brief in-place note that v0.13.0 surprises-absorption was completed. (One-line addition to keep the section current; full v0.13.0-shipped subsection lands in P88.)
3. Write `87-01-PLAN.md` (this file) + `87-01-SUMMARY.md` per template.
4. `git push origin main` BEFORE verifier-subagent dispatch (per CLAUDE.md "Push cadence — per-phase").

**Verification:** `git log origin/main..HEAD` empty post-push; verifier passes after T02; pre-push runner GREEN; SUMMARY exists.

**Done criteria:**
- RETROSPECTIVE.md and CLAUDE.md modifications committed.
- 87-01-PLAN.md + 87-01-SUMMARY.md present.
- `git push origin main` succeeded; pre-push gate GREEN.
- Commit message: `docs(retrospective,claude.md): v0.13.0 surprises-absorption summary + OP-8 note (P87 close)`.

## Verification

- `bash quality/gates/agent-ux/p87-surprises-absorption.sh` exits 0.
- `python3 quality/runners/run.py --cadence pre-push` returns 0 (no FAIL).
- All 6 ROADMAP P87 success criteria satisfied (see SUMMARY § Acceptance).

## Success criteria (mirrors ROADMAP P87)

1. Every entry in SURPRISES-INTAKE.md has terminal STATUS — RESOLVED | DEFERRED | WONTFIX.
2. Verifier honesty spot-check sampled ≥3 P78–P86 plan/verdict pairs; aggregate GREEN; report at `.planning/phases/87-surprises-absorption/honesty-spot-check.md` (NOT under `quality/reports/verdicts/p87/` because that path is the verifier-subagent dispatch target; the executor-side spot-check artifact is the input the dispatched verifier reads).
3. Catalog deltas: +1 row (`agent-ux/p87-surprises-absorption`); 0 retires; 0 STALE flips. Catalog clean entering P88.
4. No silent scope creep — honesty grade GREEN; phases used the framework as designed (3 eager-resolved-and-filed, 1 eager-only, 1 filed-HIGH-for-blocker, 1 cross-reference, 1 truly-empty).
5. CLAUDE.md updated in same PR (OP-8 note appended).
6. `git push origin main` BEFORE verifier dispatch; verifier subagent grades the row + signs the honesty spot-check at `quality/reports/verdicts/p87/VERDICT.md`.

## Hard constraints

- No new Rust code (pure planning + docs + catalog).
- Per-phase push BEFORE verifier dispatch.
- CLAUDE.md update in same PR.
- Schema-fence example in SURPRISES-INTAKE.md must NOT be counted as a real entry (verifier uses awk fence-tracking).
