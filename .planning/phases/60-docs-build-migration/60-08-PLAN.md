---
phase: 60-docs-build-migration
plan: 08
type: execute
wave: 8
depends_on:
  - "60-01"
  - "60-02"
  - "60-03"
  - "60-04"
  - "60-05"
  - "60-06"
  - "60-07"
files_modified:
  - CLAUDE.md
  - quality/SURPRISES.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - quality/reports/verdicts/p60/VERDICT.md
autonomous: false
requirements:
  - QG-06
  - QG-07
must_haves:
  truths:
    - "QG-07 CLAUDE.md update -- a new H3 subsection 'P60 -- Docs-build dimension live + composite cutover' lands in CLAUDE.md (anti-bloat: <=80 added lines per the P58 + P59 precedent). Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail). Cites: docs-build dimension home (4 verifiers + 4 catalog rows); SIMPLIFY-08 closure (3 git-mv migrations + 3 thin shims); SIMPLIFY-09 closure (green-gauntlet supplanted as thin shim); SIMPLIFY-10 closure (pre-push hook one-liner); BADGE-01 verifier (badges-resolve.py) ships; QG-09 P60 closure (docs/badge.json + README + docs/index.md endpoint badge); POLISH-DOCS-BUILD broaden-and-deepen sweep (Wave G outcomes)."
    - "QG-06 verifier subagent dispatch: an unbiased subagent (Path A via Task tool if available; Path B in-session disclosure per P56/P57/P58/P59 precedent if not) grades all P60-touched catalog rows GREEN. Verdict at quality/reports/verdicts/p60/VERDICT.md. The verdict template is verbatim from quality/PROTOCOL.md § 'Verifier subagent prompt template'."
    - "ALL P0+P1 rows across the P60-touched catalogs (docs-build, code, structure for the new + amended rows) are PASS, WAIVED, or filed as v0.12.1 carry-forward via MIGRATE-03. NO P0+P1 rows remain NOT-VERIFIED at Wave H close."
    - ".planning/REQUIREMENTS.md traceability table updated -- DOCS-BUILD-01, BADGE-01, SIMPLIFY-08, SIMPLIFY-09, SIMPLIFY-10, POLISH-DOCS-BUILD all flip from `planning` to `shipped (P60)`. QG-09 P60 portion shipped (the milestone-spanning ID stays open until P63 final). 6 requirement IDs land at `shipped`."
    - ".planning/STATE.md cursor advances P59 SHIPPED -> P60 SHIPPED. Roadmap-Evolution entry appended at top (newest first); P59/P58/P57/P56 entries preserved verbatim. progress frontmatter incremented (completed_phases 4 -> 5; total_plans 22 -> 30; completed_plans 22 -> 30; percent 50 -> 63)."
    - "quality/SURPRISES.md is updated with any P60 pivot entries from Waves A-G (likely 3-7 entries: Wave A short-lived waiver pattern, Wave B path-arithmetic fix, Wave C QG-09 chicken-and-egg WAVE_F_PENDING_URLS, Wave D green-gauntlet caller audit, Wave E pivot-or-no-pivot perf decision, Wave F mkdocs auto-include, Wave G polish-sweep findings). The 3 P57 + 7 P58 + 6 P59 entries from earlier phases are preserved verbatim. Anti-bloat: under 200 lines (currently ~200 from P59 close; P60 entries push to ~210-220 -> archive rotation if >200 needed; defer rotation to v0.12.1 if marginal)."
    - "Phase-close commit lands all 5 docs updates (CLAUDE.md + SURPRISES.md + STATE.md + REQUIREMENTS.md + VERDICT.md) in the SAME PR per QG-07."
    - "The Wave H verifier subagent reads the catalog rows + the artifacts + the CLAUDE.md diff with ZERO session context and grades GREEN/RED. The 4 disclosure constraints (Path B fallback) are honored verbatim per P56/P57/P58/P59 precedent: (1) evidence-only; (2) catalog-rows-as-contract; (3) refuse-GREEN-on-RED; (4) out-of-session re-grade should match."
    - "Plan is autonomous: false because the verifier-subagent dispatch in Task 4 is the human-or-checkpoint moment per P59 precedent (Wave F was autonomous: false for the same reason)."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Append (50-80 lines) at the END of the existing P57 + P58 + P59 Quality Gates dimension subsections with the P60-specific extensions: docs-build home (mkdocs-strict.sh + mermaid-renders.sh + link-resolution.py + badges-resolve.py; 4 verifiers + 4 catalog rows + the back-compat structure/badges-resolve row), SIMPLIFY-08 absorption (3 git-mv migrations: scripts/check-docs-site.sh -> mkdocs-strict.sh; scripts/check-mermaid-renders.sh -> mermaid-renders.sh; scripts/check_doc_links.py -> link-resolution.py; thin shims at old paths per OP-5), SIMPLIFY-09 absorption (scripts/green-gauntlet.sh supplanted as thin shim delegating to runner --cadence pre-pr), SIMPLIFY-10 absorption (scripts/hooks/pre-push body rewritten as cred-hygiene + runner one-liner; <=30 lines body), QG-09 P60 closure (docs/badge.json published; README + docs/index.md endpoint badge live; WAVE_F_PENDING_URLS cleared in badges-resolve.py), BADGE-01 verifier (validates 8 unique badge URLs), POLISH-DOCS-BUILD broaden-and-deepen result (Wave G's fix list + any v0.12.1 carry-forwards). Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail)."
      contains: "docs-build dimension"
      max_lines: 80
    - path: "quality/SURPRISES.md"
      provides: "Append any P60 entries from Waves A-G + this Wave H. P57+P58+P59 entries preserved verbatim. Anti-bloat: under 200 lines target (rotation to quality/SURPRISES-archive-2026-Q2.md if exceeded; defer to v0.12.1 if marginal)."
      contains: "P60"
      max_lines: 250
    - path: ".planning/STATE.md"
      provides: "Roadmap-Evolution entry appended at top for P60 SHIPPED. Current Position cursor advances to P61. progress frontmatter incremented per the math above (completed_phases 4 -> 5; total_plans 22 -> 30 [22 from P56-P59 + 8 from P60]; completed_plans 22 -> 30; percent 50 -> 63)."
      contains: "P60 SHIPPED"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table updated: DOCS-BUILD-01, BADGE-01, SIMPLIFY-08, SIMPLIFY-09, SIMPLIFY-10, POLISH-DOCS-BUILD flip planning -> shipped (P60). QG-09 P60 portion shipped (entry note added; milestone-spanning ID stays in `planning` until P63 final). Per-phase requirement counts in the footer paragraph updated: P60=6 requirements, all shipped at Wave H."
      contains: "shipped (P60)"
    - path: "quality/reports/verdicts/p60/VERDICT.md"
      provides: "Unbiased verifier subagent verdict (or in-session verdict with disclosure block per P56/P57/P58/P59 precedent). Top-line GREEN/RED. Per-row table for the 8 P60-touched rows graded. QG-07 + SIMPLIFY-08 + SIMPLIFY-09 + SIMPLIFY-10 + BADGE-01 + DOCS-BUILD-01 + POLISH-DOCS-BUILD closure citations. Recurring-criteria evidence (criteria 1-5). Recommendation: 'GREEN. P61 may begin.' OR specific RED with required-action."
      contains: "GREEN"
  key_links:
    - from: "CLAUDE.md (P60 append)"
      to: "quality/PROTOCOL.md"
      via: "cross-reference; CLAUDE.md does NOT duplicate runtime detail per anti-bloat rule"
      pattern: "quality/PROTOCOL.md"
    - from: "verifier subagent dispatch"
      to: "quality/reports/verdicts/p60/VERDICT.md"
      via: "Task tool dispatch with the prompt from quality/PROTOCOL.md § 'Verifier subagent prompt template'; subagent writes the verdict file with ZERO session context"
      pattern: "ZERO session context"
    - from: ".planning/STATE.md"
      to: "P61 agent (next session)"
      via: "Cursor advance + Roadmap-Evolution entry tells the next agent P60 GREEN + lists what they read at start of P61 (per quality/PROTOCOL.md Step 1 reading order)"
      pattern: "P60 SHIPPED"
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "Wave H verifier subagent"
      via: "the subagent verifies the 6 requirement IDs flipped to shipped; if any flip is unverified, refuses GREEN"
      pattern: "shipped (P60)"
---

<objective>
Wave H -- phase close + CLAUDE.md QG-07 + SURPRISES.md update + STATE.md cursor + REQUIREMENTS.md traceability + verifier subagent dispatch.

Per `.planning/REQUIREMENTS.md` § "Recurring success criteria across every phase (P56-P63)" criteria 2 + 3 ('CLAUDE.md update in the same PR (QG-07)' + 'Unbiased verifier-subagent dispatch on phase close (QG-06)'). Wave H is the phase-close ritual that locks the dimension contract for P61's executor + grades P60 GREEN.

Five activities, in order:

1. **QG-07 CLAUDE.md update (Task 1)**: append a P60 H3 subsection (50-80 lines) per the P58 + P59 Wave F precedent.

2. **STATE.md cursor advance (Task 2)**: roadmap-evolution entry + Current Position cursor + progress frontmatter increment.

3. **REQUIREMENTS.md traceability (Task 3)**: 6 requirement IDs flip planning -> shipped (P60) in the table; per-phase footer paragraph updated.

4. **QG-06 verifier subagent dispatch (Task 4)**: dispatch an unbiased subagent (Path A via Task tool if available; Path B in-session disclosure per P56/P57/P58/P59 precedent if not). Subagent grades GREEN/RED. Verdict at quality/reports/verdicts/p60/VERDICT.md.

5. **SURPRISES.md updates + atomic commit (Task 5)**: append any P60 pivot entries; commit all 5 docs updates in the same PR.

Plan is `autonomous: false` because the verifier-subagent dispatch is the human-or-checkpoint moment per P59 precedent.

**Pivot rules**:

- **Path A Task tool unavailable in executor**: per P56/P57/P58/P59 precedent, fall back to Path B (in-session verdict with disclosure block). The disclosure block names: (1) Evidence-only -- each PASS/WAIVED claim cites file+line OR runner output; (2) Catalog-rows-as-contract -- grade computed from row.expected.asserts <-> artifact asserts_passed/failed match, not narrative; (3) Refuse-GREEN-on-RED -- if any P0+P1 row outside {PASS, WAIVED}, mark RED; (4) Out-of-session re-grade should match.

- **A row is RED at Wave H entry**: Wave G should have caught it. If a regression slipped through (e.g. a Wave H commit accidentally touched a verifier and broke it), fix BEFORE dispatching the verifier. The verifier's contract is "the catalog at this commit SHA grades GREEN".

- **CLAUDE.md anti-bloat (P57 + P58 + P59 + P60 = 4 H3 subsections under the Quality Gates parent section)**: each subsection is <=80 lines per precedent; total Quality Gates section is now ~250-300 lines. If banned-words-lint catches the addition, fix the offending word in the new subsection (likely `replace`). MIGRATE-02 in P63 does the cohesion pass that audits cross-refs across all 4 subsections.

- **SURPRISES.md crossing 200 lines** (current ~200 post-P59): per `quality/PROTOCOL.md` § anti-bloat rules per surface, archive oldest 50 entries to `quality/SURPRISES-archive-2026-Q2.md` (P59 already created this archive). At Wave H, decide: if SURPRISES.md is at 230-240 lines after P60 entries land, do ANOTHER archive rotation (next 50 oldest -> archive). If at 210-215 lines, defer rotation to v0.12.1 (trivially over cap; not worth the rotation overhead).

- **A new pivot in this Wave H itself** (e.g. the verifier-subagent dispatch reveals a contract drift): append a final SURPRISES.md entry. Do NOT silently fix it.

**Output**: Wave H ships in 1-2 commits.
- Commit 1: `chore(p60): POLISH closure + CLAUDE.md update + STATE/SURPRISES/REQUIREMENTS advance + verifier verdict GREEN`.
- Commit 2 (optional): `chore(p60): in-phase fix for <regression>` -- ONLY IF Wave H found a regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.12.0/roadmap-and-rationale.md
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/SURPRISES-archive-2026-Q2.md
@quality/catalogs/docs-build.json
@quality/catalogs/code.json
@quality/catalogs/freshness-invariants.json
@quality/catalogs/release-assets.json
@quality/catalogs/docs-reproducible.json
@quality/catalogs/agent-ux.json
@quality/catalogs/perf-targets.json
@quality/catalogs/orphan-scripts.json
@quality/runners/run.py
@quality/runners/verdict.py
@quality/reports/verdicts/p59/VERDICT.md
@quality/reports/verdicts/p58/VERDICT.md
@.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-06-PLAN.md
@CLAUDE.md
@.planning/phases/60-docs-build-migration/60-01-PLAN.md
@.planning/phases/60-docs-build-migration/60-02-PLAN.md
@.planning/phases/60-docs-build-migration/60-03-PLAN.md
@.planning/phases/60-docs-build-migration/60-04-PLAN.md
@.planning/phases/60-docs-build-migration/60-05-PLAN.md
@.planning/phases/60-docs-build-migration/60-06-PLAN.md
@.planning/phases/60-docs-build-migration/60-07-PLAN.md

<interfaces>
**CLAUDE.md P60 H3 subsection shape (model on the existing P57/P58/P59 subsections in CLAUDE.md):**

```markdown
### P60 -- Docs-build dimension live + composite cutover (added 2026-04-27)

The Quality Gates docs-build dimension shipped in P60 with 4 verifiers backing 4 catalog rows in `quality/catalogs/docs-build.json` plus the back-compat `structure/badges-resolve` row in `quality/catalogs/freshness-invariants.json` (P57 pre-anchored; P60 implements). Verifiers + cadences:

| Verifier | Catalog row(s) | Cadence |
|---|---|---|
| `quality/gates/docs-build/mkdocs-strict.sh` | `docs-build/mkdocs-strict` | pre-push |
| `quality/gates/docs-build/mermaid-renders.sh` | `docs-build/mermaid-renders` | pre-push |
| `quality/gates/docs-build/link-resolution.py` | `docs-build/link-resolution` | pre-push |
| `quality/gates/docs-build/badges-resolve.py` | `docs-build/badges-resolve` + `structure/badges-resolve` | pre-push + weekly |

**SIMPLIFY absorption (3 closures)**:
- **SIMPLIFY-08**: 3 git-mv migrations -- `scripts/check-docs-site.sh` -> `mkdocs-strict.sh`; `scripts/check-mermaid-renders.sh` -> `mermaid-renders.sh`; `scripts/check_doc_links.py` -> `link-resolution.py` (underscore-to-hyphen rename per directory-layout convention; or kept underscore per pivot rule if any test imports it as a module). Thin shims at old paths per OP-5 reversibility (callers in CI workflows + docs continue working).
- **SIMPLIFY-09**: `scripts/green-gauntlet.sh` rewritten as thin shim that delegates to `python3 quality/runners/run.py --cadence pre-pr`. The 3 modes (--quick, default, --full) collapse to runner cadence calls. Caller audit + shim survival per OP-5.
- **SIMPLIFY-10**: `scripts/hooks/pre-push` body rewritten as a cred-hygiene + runner bifurcation. Body <=30 lines. cred-hygiene wrapper preserves the P0 credential-prefix scan VERBATIM from the pre-Wave-E hook (PATTERNS + EXCLUDE_DIRS unchanged). Runner invocation handles cargo fmt + clippy + mkdocs + mermaid + links + badges + structure invariants in one composed cadence.

**BADGE-01 closure**: `quality/gates/docs-build/badges-resolve.py` HEADs every badge URL extracted from README.md + docs/index.md (8 unique URLs post-Wave-F including the QG-09 endpoint badge). Asserts HTTP 200 + Content-Type matches `image/*` OR `application/json`. Stdlib-only Python; <=250 lines. Pre-Wave-F handled the QG-09 chicken-and-egg via a `WAVE_F_PENDING_URLS` skip-and-log mechanism; Wave F cleared the set after the publish landed.

**QG-09 P60 closure**: docs/badge.json seeded from quality/reports/badge.json + auto-included in mkdocs --strict build -> `https://reubenjohn.github.io/reposix/badge.json` resolves. README.md + docs/index.md gain the shields.io endpoint badge `![Quality](https://img.shields.io/endpoint?url=...)`. The Quality (weekly) GH Actions workflow badge from P58 is preserved alongside; the two badges convey complementary signals (workflow status vs catalog rollup).

**POLISH-DOCS-BUILD broaden-and-deepen** (Wave G): all 4 cadences (pre-push, pre-pr, weekly, post-release) GREEN at phase close. <COUNT> RED rows fixed in-phase: <SUMMARY>. <COUNT> v0.12.1 carry-forwards filed via MIGRATE-03: <SUMMARY>. Specific outcomes: mkdocs --strict GREEN; every mermaid-bearing page has a current playwright artifact; every relative link in user-facing docs resolves (or is on `.planning/notes/v0.12.1-link-backlog.md` per the >5-broken pivot rule); every badge URL HEADs to 200 + correct content-type.

**Cross-references**: `quality/PROTOCOL.md` § verifier-subagent prompt template + § anti-bloat rules; `quality/SURPRISES.md` § P60 entries (path-arithmetic, mkdocs auto-include, hook one-liner pivot decision); `quality/gates/docs-build/README.md` (dimension home + verifier conventions).
```

The above is ~50 lines including the table + the closure paragraphs. Within the <=80 cap. The actual numbers (RED count, carry-forward count, etc.) get filled in from Wave G's outcomes.

**STATE.md update shape (Task 2):**

Append at the TOP of `## Accumulated Context` -> `### Roadmap Evolution`:

```markdown
- **P60 SHIPPED (2026-04-27, v0.12.0):** Docs-build migration + composite cutover. 4 docs-build verifiers ship at canonical home (`quality/gates/docs-build/{mkdocs-strict.sh, mermaid-renders.sh, link-resolution.py, badges-resolve.py}`) backing 4 catalog rows + the back-compat `structure/badges-resolve` row from P57. SIMPLIFY-08 closure: 3 git-mv migrations preserve history; thin shims at old paths per OP-5 reversibility. SIMPLIFY-09 closure: `scripts/green-gauntlet.sh` rewritten as 30-50 line shim delegating to `python3 quality/runners/run.py --cadence pre-pr`. SIMPLIFY-10 closure: `scripts/hooks/pre-push` body <=30 lines (cred-hygiene wrapper + runner one-liner bifurcation; cred-hygiene PATTERNS + EXCLUDE_DIRS preserved verbatim from pre-Wave-E hook; the runner invocation composes cargo fmt + clippy + mkdocs + mermaid + links + badges + structure invariants). BADGE-01 verifier ships (`badges-resolve.py`; stdlib-only urllib.request HEAD; 8 unique URL set). QG-09 P60 closure: `docs/badge.json` published via mkdocs auto-include -> `https://reubenjohn.github.io/reposix/badge.json` live; README + docs/index.md gain endpoint badge; Quality (weekly) GH Actions badge from P58 preserved alongside. POLISH-DOCS-BUILD Wave G sweep: 4 cadences GREEN (pre-push exit 0; pre-pr exit 0; weekly exit 0; post-release exit 0); <COUNT> RED rows fixed in-phase; <COUNT> v0.12.1 carry-forwards via MIGRATE-03 (link-backlog if >5; clippy if transitive update; etc.). CLAUDE.md gained "P60 -- Docs-build dimension live + composite cutover" H3 subsection (~50 lines under 80 cap). SURPRISES.md gained <COUNT> P60 entries (3 P57 + 7 P58 + 6 P59 + N P60 = ~16+N entries; archive rotation deferred or executed per Wave H decision). Verifier subagent verdict GREEN at `quality/reports/verdicts/p60/VERDICT.md` (Path B in-session disclosure per P56/P57/P58/P59 precedent if Task tool unavailable; 4 disclosure constraints honored). 8 catalog rows graded; all P0+P1 PASS or WAIVED with documented carry-forwards. Carry-forwards: <list>. Next: P61 -- subjective gates skill + freshness TTL enforcement (per ROADMAP.md).
```

Update Current Position cursor:

```markdown
Phase: 61
Plan: -
Cursor: P60 SHIPPED -- Docs-build dimension migration complete. 4 verifiers + 4 catalog rows; SIMPLIFY-08/09/10 closures; BADGE-01 ships; QG-09 P60 portion live (docs/badge.json published; README + docs/index.md endpoint badge live). Pre-push hook body now a cred-hygiene + runner one-liner. POLISH-DOCS-BUILD broaden-and-deepen Wave G GREEN. CLAUDE.md gained P60 H3 subsection. Runner state: pre-push exit 0 / pre-pr exit 0 / weekly exit 0 / post-release exit 0. Verifier verdict GREEN at `quality/reports/verdicts/p60/VERDICT.md`. Carry-forwards: <list>. Next: P61 subjective gates skill (SUBJ-01..03 + POLISH-SUBJECTIVE). Gate-state precondition for P61: read `quality/reports/verdicts/p60/VERDICT.md` to confirm GREEN, read `quality/SURPRISES.md`, read this entry, then `/gsd-plan-phase 61`.
Status: shipped (P60)
Last activity: 2026-04-27 -- P60 closed with GREEN verdict at `quality/reports/verdicts/p60/VERDICT.md`. 8 catalog rows graded; all P0+P1 PASS or WAIVED. Docs-build dimension complete. CLAUDE.md / SURPRISES.md / STATE.md / REQUIREMENTS.md / verdict updates landed in Wave H. Next: P61.
```

Update progress frontmatter:
```yaml
progress:
  total_phases: 8
  completed_phases: 5  # was 4
  total_plans: 30      # was 22 (P56=4 + P57=6 + P58=6 + P59=6 + P60=8 = 30)
  completed_plans: 30  # was 22
  percent: 63          # was 50; (5/8 = 62.5%, round to 63)
```

**REQUIREMENTS.md traceability flips (Task 3):**

Update the per-phase row entries:
```
| DOCS-BUILD-01 | P60 | shipped (P60) |
| BADGE-01 | P60 | shipped (P60) |
| SIMPLIFY-08 | P60 | shipped (P60) |
| SIMPLIFY-09 | P60 | shipped (P60) |
| SIMPLIFY-10 | P60 | shipped (P60) |
| POLISH-DOCS-BUILD | P60 | shipped (P60) |
```

QG-09 stays in `planning` because the milestone-spanning ID covers P57 (verdict.py emit) + P58 (GH Actions badge) + P60 (mkdocs publish + endpoint badge) -- ALL 3 portions are now SHIPPED at v0.12.0 P60 close, but the row is closed at P63 final per P57/P58/P60 collective shipping. Add a note in the cell: `P57 + P58 + P60 portions shipped; row closes at v0.12.0 milestone end (P63)`.

Update the per-phase requirement counts paragraph footer (line ~192 of REQUIREMENTS.md):
```
P60=6 requirements, all SHIPPED (DOCS-BUILD-01, BADGE-01, SIMPLIFY-08, SIMPLIFY-09, SIMPLIFY-10, POLISH-DOCS-BUILD)
```

**Verifier subagent prompt (Task 4) -- copy verbatim from quality/PROTOCOL.md § "Verifier subagent prompt template":**

```
You have ZERO context from this session. You are the unbiased verifier for v0.12.0 Phase 60.

Inputs:
1. .planning/REQUIREMENTS.md ## v0.12.0 -- find requirements assigned to Phase 60
2. quality/catalogs/*.json -- find rows tagged phase=60 or whose verifier ships in this phase
3. CLAUDE.md -- confirm this phase's contributions are documented (per QG-07)
4. quality/SURPRISES.md -- confirm any pivots are journaled

For each catalog row in scope:
1. Read row.verifier.script and row.expected.asserts
2. If row.kind == "container", run quality/gates/docs-repro/container-rehearse.sh <row.id>
3. Else, invoke row.verifier.script with row.verifier.args
4. Compare row.artifact contents against row.expected.asserts
5. Grade: PASS (all asserts met) | FAIL (any P0+P1 assert unmet) | PARTIAL (P2 unmet only) | NOT-VERIFIED (no artifact dated this session)

For CLAUDE.md (per QG-07):
1. git diff main...HEAD -- CLAUDE.md
2. Confirm this phase's new files/conventions/gates appear in the diff
3. If absent: phase is NOT done -- block the GREEN verdict

For SURPRISES.md:
1. Read the file; confirm any pivot in the phase has a corresponding entry
2. If a pivot is mentioned in commits but absent from SURPRISES.md: ungrounded pivot -- flag

Output a scored markdown table with file:line citations. Write to quality/reports/verdicts/p60/<ISO_TIMESTAMP>.md.
Refuse to grade GREEN unless every P0+P1 catalog row is PASS or WAIVED AND CLAUDE.md is updated.
```

Path A: dispatch via Task tool (general-purpose subagent). Path B fallback: in-session verdict authored by Wave H executor with the 4 disclosure constraints honored verbatim.

**SURPRISES.md anti-bloat decision (Task 5):**

Read `quality/SURPRISES.md` line count post-P60-entries-appended:
- If <=200 lines: leave as-is.
- If 201-220 lines: leave as-is + document the marginal overage; defer rotation to v0.12.1.
- If >220 lines: archive next-50 oldest entries (P57's 3 entries + P58's 7 entries = 10 entries; not enough; would need P59's 6 entries to make 16... still not 50 lines worth). Decision rule: rotation requires actual 50+ lines being archived; partial rotations are anti-bloat overhead. Defer to v0.12.1 OR if Wave H finds a clean P57+P58 archive opportunity, do it.

Most likely outcome: 200-220 lines post-P60; defer rotation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QG-07 CLAUDE.md update -- append P60 H3 subsection</name>
  <files>CLAUDE.md</files>
  <action>Append a new H3 subsection at the END of the existing P57+P58+P59 Quality Gates subsection chain. Title: `### P60 -- Docs-build dimension live + composite cutover (added 2026-04-27)`. Body per the `<interfaces>` shape spec: verifier table (4 rows + back-compat note); SIMPLIFY-08/09/10 absorption notes; BADGE-01 closure; QG-09 P60 closure; POLISH-DOCS-BUILD broaden-and-deepen result with COUNTS from Wave G's outcomes. <=80 added lines. Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail). Banned-words check: do NOT use `replace`; use `migrate to`, `swap`, `supplant by`, `rewrite as` per the prompt + CLAUDE.md banned-words rule. Run `bash scripts/banned-words-lint.sh --all` post-edit to confirm clean.</action>
  <verify>
    <automated>added_lines=$(git diff main -- CLAUDE.md | grep -E '^\+' | wc -l); test "$added_lines" -le 80 || { echo "TOO LONG: $added_lines added lines"; exit 1; }; grep -E '\breplace\b' CLAUDE.md | head -5 && { echo "BANNED in CLAUDE.md"; exit 1; } || echo "OK no banned word"; bash scripts/banned-words-lint.sh --all 2>&1 | tail -5; grep -F 'P60' CLAUDE.md | head -3 && grep -F 'docs-build dimension' CLAUDE.md && echo "OK P60 subsection present"</automated>
  </verify>
  <done>P60 H3 subsection appended; <=80 added lines; banned-words clean; cross-references PROTOCOL.md without duplicating; cites all 6 P60 requirement IDs.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md cursor advance + roadmap-evolution entry</name>
  <files>.planning/STATE.md</files>
  <action>(1) Append a new bullet at the TOP of `## Accumulated Context > ### Roadmap Evolution` per the `<interfaces>` STATE.md shape. Cite Wave G outcomes: COUNT of RED rows fixed in-phase; COUNT of v0.12.1 carry-forwards. P57+P58+P59 entries preserved verbatim. (2) Update `## Current Position` cursor: Phase 61; Cursor cites P60 SHIPPED with the runner state line + the carry-forwards list; Last activity: 2026-04-27 P60 closed with GREEN verdict. (3) Update progress frontmatter: completed_phases 4 -> 5; total_plans 22 -> 30; completed_plans 22 -> 30; percent 50 -> 63.</action>
  <verify>
    <automated>grep -F 'P60 SHIPPED' .planning/STATE.md | head -3 && grep -F 'Phase: 61' .planning/STATE.md && grep -F 'completed_phases: 5' .planning/STATE.md && grep -F 'total_plans: 30' .planning/STATE.md && grep -F 'percent: 63' .planning/STATE.md && echo "OK STATE.md cursor advanced"</automated>
  </verify>
  <done>Roadmap-Evolution entry appended; Current Position advanced to P61; progress frontmatter incremented; P59/P58/P57/P56 entries preserved.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md traceability table flips</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>Update the traceability table (lines 141-191): flip 6 rows from `planning` -> `shipped (P60)`: DOCS-BUILD-01, BADGE-01, SIMPLIFY-08, SIMPLIFY-09, SIMPLIFY-10, POLISH-DOCS-BUILD. Add a parenthetical note in the QG-09 cell: `P57 + P58 + P60 portions shipped; row closes at v0.12.0 milestone end (P63)`. Update the per-phase requirement counts paragraph footer: P60 entry from `planning` to `all SHIPPED (DOCS-BUILD-01, BADGE-01, SIMPLIFY-08, SIMPLIFY-09, SIMPLIFY-10, POLISH-DOCS-BUILD)`. Banned-words: no `replace`.</action>
  <verify>
    <automated>grep -c 'shipped (P60)' .planning/REQUIREMENTS.md | grep -E '^([6-9]|[0-9]{2,})$' || { echo "FAIL: <6 shipped (P60) entries"; grep 'shipped (P60)' .planning/REQUIREMENTS.md; exit 1; }; grep -F 'DOCS-BUILD-01' .planning/REQUIREMENTS.md | grep -F 'shipped (P60)' && grep -F 'BADGE-01' .planning/REQUIREMENTS.md | grep -F 'shipped (P60)' && grep -F 'SIMPLIFY-08' .planning/REQUIREMENTS.md | grep -F 'shipped (P60)' && grep -F 'SIMPLIFY-09' .planning/REQUIREMENTS.md | grep -F 'shipped (P60)' && grep -F 'SIMPLIFY-10' .planning/REQUIREMENTS.md | grep -F 'shipped (P60)' && grep -F 'POLISH-DOCS-BUILD' .planning/REQUIREMENTS.md | grep -F 'shipped (P60)' && echo "OK 6 requirements flipped to shipped (P60)"</automated>
  </verify>
  <done>6 requirement IDs flipped; QG-09 cell amended with the milestone-spanning note; per-phase footer paragraph updated; banned-words clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: QG-06 verifier subagent dispatch (Path A or Path B)</name>
  <what-built>The Wave H executor needs to dispatch the unbiased verifier subagent (Path A via Task tool if available; Path B in-session disclosure per P56/P57/P58/P59 precedent if not). The verifier reads catalog rows + artifacts + CLAUDE.md diff with ZERO session context and grades GREEN/RED. Verdict at quality/reports/verdicts/p60/VERDICT.md.</what-built>
  <how-to-verify>
    1. Confirm Task tool availability: if `Task({subagent_type: 'general-purpose', ...})` is callable, use Path A. Otherwise, use Path B.
    2. **Path A**: Dispatch the subagent with the verbatim prompt from `quality/PROTOCOL.md` § "Verifier subagent prompt template" (replace `<N>` with `60` and `<ISO_TIMESTAMP>` with the current UTC timestamp). The subagent writes the verdict file. Wait for it to return.
    3. **Path B (Wave H executor authors verdict in-session with 4 disclosure constraints honored)**: Author `quality/reports/verdicts/p60/VERDICT.md` per the P59 Wave F precedent (`quality/reports/verdicts/p59/VERDICT.md` is the template). Include disclosure block + per-row table + recurring-criteria evidence + recommendation.
    4. Confirm the verdict file exists at `quality/reports/verdicts/p60/VERDICT.md` AND grades GREEN.
    5. If RED: do NOT proceed. Investigate per Wave G escalation rules; fix the RED + re-grade.
    6. Type "approved -- verdict GREEN" to resume.
  </how-to-verify>
  <resume-signal>Type "approved -- verdict GREEN" or describe the RED + required action</resume-signal>
</task>

<task type="auto">
  <name>Task 5: SURPRISES.md update + atomic Wave H commit</name>
  <files>quality/SURPRISES.md, (commit only)</files>
  <action>(1) Read `quality/SURPRISES.md`. Append P60 pivot entries from Waves A-G + this Wave H. Likely entries: Wave A short-lived waiver pattern (8 rows; same shape as P58 Wave A); Wave B path-arithmetic fix (3 verifiers depth changed 1 -> 3); Wave C QG-09 chicken-and-egg WAVE_F_PENDING_URLS; Wave D green-gauntlet caller audit (N callers); Wave E pivot-or-no-pivot perf decision (warm cache time XXs); Wave F mkdocs auto-include (worked / didn't work + pivot); Wave G polish-sweep findings (RED count + carry-forwards). (2) Anti-bloat: count lines post-edit. If <=200, leave as-is. If 201-220, defer rotation to v0.12.1. If >220, archive 50 oldest entries to `quality/SURPRISES-archive-2026-Q2.md` (which already exists from P59 Wave F). (3) Commit Tasks 1-4 + this update in 1 atomic commit titled `chore(p60): POLISH closure + CLAUDE.md update + STATE/SURPRISES/REQUIREMENTS advance + verifier verdict GREEN`. Body cites: P60 6 requirements shipped; runner GREEN; verdict GREEN; CLAUDE.md QG-07; SURPRISES.md update; STATE.md cursor advance.</action>
  <verify>
    <automated>grep -F 'P60' quality/SURPRISES.md | head -3 && lines=$(wc -l < quality/SURPRISES.md) && echo "SURPRISES.md lines: $lines"; if [ "$lines" -gt 220 ]; then echo "WARN: anti-bloat -- consider archive rotation"; fi; test -f quality/reports/verdicts/p60/VERDICT.md && grep -F 'GREEN' quality/reports/verdicts/p60/VERDICT.md | head -1 && git log -1 --format='%s' | grep -F 'p60' && echo "OK Wave H clean"</automated>
  </verify>
  <done>SURPRISES.md updated with P60 entries; anti-bloat decision documented; verdict file exists + grades GREEN; commit lands all 5 docs updates (CLAUDE.md + SURPRISES.md + STATE.md + REQUIREMENTS.md + VERDICT.md) in the same commit; pre-push hook self-verifies (which is the proof Wave E + Wave G + this commit all work).</done>
</task>

</tasks>

<verification>
After Wave H close:
- CLAUDE.md gains P60 H3 subsection (<=80 added lines; banned-words clean).
- .planning/STATE.md cursor advanced to P61; progress frontmatter incremented.
- .planning/REQUIREMENTS.md traceability table has 6 rows flipped to shipped (P60).
- quality/SURPRISES.md gains P60 entries (~3-7); anti-bloat decision documented.
- quality/reports/verdicts/p60/VERDICT.md exists + grades GREEN.
- All 4 runner cadences exit 0 (post-Wave-G).
- Pre-push hook (Wave E rewrite) self-verifies on the Wave H commit (no --no-verify).
- The next agent reading .planning/STATE.md knows P60 is SHIPPED + reads the verdict + reads the SURPRISES entries to know any P60 pivots.
- v0.12.0 milestone progress: 5/8 phases (62.5% -> 63%); 30/30 plans within shipped phases.
</verification>

<success_criteria>
1. **Catalog-first criterion 1:** N/A for Wave H (Wave A locked the contract).
2. **CLAUDE.md update (criterion 2 / QG-07):** PASS -- P60 H3 subsection landed in same PR; <=80 lines; banned-words clean.
3. **Verifier-subagent dispatch (criterion 3 / QG-06):** PASS via Path A (preferred) or Path B (precedent fallback). Verdict GREEN at `quality/reports/verdicts/p60/VERDICT.md`.
4. **SIMPLIFY absorption (criterion 4):** SIMPLIFY-08 + SIMPLIFY-09 + SIMPLIFY-10 all closed in Waves B/C/D/E. Closure cited in CLAUDE.md + SUMMARY.
5. **Fix every RED row (criterion 5):** Wave G's broaden-and-deepen handles. Wave H confirms zero P0+P1 RED at phase close via the verifier subagent verdict.
</success_criteria>

<pivot_rules>
- **Path A Task tool unavailable** in Wave H executor: per P56/P57/P58/P59 precedent, fall back to Path B (in-session verdict with disclosure block). The disclosure constraints are NON-NEGOTIABLE: evidence-only, catalog-rows-as-contract, refuse-GREEN-on-RED, out-of-session-re-grade-match. Author the verdict per the `quality/reports/verdicts/p59/VERDICT.md` template (P59 Wave F's verdict is the most-recent precedent).
- **A row is RED at Wave H entry**: Wave G should have caught it. If a regression slipped through, fix BEFORE dispatching the verifier. Do NOT bypass the verifier with `--no-verify` on commits.
- **CLAUDE.md banned-words-lint catches a hit**: identify the offending word in the new P60 subsection. Most common: `replace` (use `migrate to`, `swap`, `supplant by`, `rewrite as`). Fix and re-run lint.
- **SURPRISES.md crossing 220 lines** post-P60-entries: archive the next 50 oldest entries to `quality/SURPRISES-archive-2026-Q2.md` (which already exists from P59 Wave F). Document the rotation in a header note (per P59 precedent).
- **REQUIREMENTS.md cell amend for QG-09 P60 portion is ambiguous** (the row covers P57+P58+P60 but each portion ships at different waves): the cell stays in `planning` until P63 final because the milestone-spanning ID closes at the milestone close. Add the parenthetical: `P57 + P58 + P60 portions shipped; row closes at v0.12.0 milestone end (P63)`. The Wave H verifier accepts this.
- **A new pivot in Wave H itself** (e.g. the verifier reveals a contract drift): append a final SURPRISES.md entry. Do NOT silently fix it -- the verifier saw the catalog at this commit SHA and graded; if a fix is needed, that's a follow-up commit.
- **The verifier subagent grades RED on something Wave G missed** (e.g. a P0+P1 row that Wave G didn't address): per `quality/PROTOCOL.md` § "When stuck rules", append SURPRISES + dispatch a `gsd-phase-researcher` to investigate + propose 2-3 paths forward. Pause via STATE.md cursor update if needed.

Reference: `quality/reports/verdicts/p59/VERDICT.md` (P59 Wave F verdict template -- Path B disclosure shape); `.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-06-PLAN.md` (Wave F structure precedent for this Wave H); `quality/PROTOCOL.md` § "Verifier subagent prompt template" (verbatim verifier prompt); CLAUDE.md "Subagent delegation rules" (Path A vs Path B precedent).
</pivot_rules>

<risk_table>
| Risk | Likelihood | Mitigation |
|---|---|---|
| Path A Task tool unavailable -> Path B fallback authored by Wave H executor | medium-high (matches P56/P57/P58/P59 pattern) | The 4 disclosure constraints + the P59 verdict template provide the structure; out-of-session re-grade by a future Path A subagent should produce identical verdict |
| CLAUDE.md banned-words-lint catches `replace` in the new subsection | medium (word is common; humans default to it) | Pivot rule + Task 1 verify gate catches; fix-and-retry |
| SURPRISES.md crosses 220 lines post-P60 entries | medium | Pivot rule: archive 50 oldest to existing 2026-Q2 archive; precedent in P59 Wave F |
| Wave G sweep missed a RED row that Wave H verifier subagent surfaces | low (Wave G's recurring criterion 5 is explicit; runner sweep catches everything) | Pause Wave H; fix in a separate commit; re-dispatch verifier; document in SURPRISES |
| Pre-push hook (Wave E rewrite) blocks Wave H commit because it found a NEW RED | low (Wave G ensured GREEN) | If a regression on Wave H commit only: investigate which Wave H file broke it (most likely CLAUDE.md banned-words); fix and retry; do NOT --no-verify |
| REQUIREMENTS.md QG-09 cell update is incorrectly flipped to `shipped (P60)` (overpromising; the row spans P57+P58+P60+P63) | medium | Pivot rule: keep in `planning` with the parenthetical note about portion-by-phase tracking |
| .planning/STATE.md frontmatter math wrong (e.g. progress.percent off) | low | Math in the spec: 5/8 = 62.5%, round to 63 |
| .planning/STATE.md `total_plans` count wrong (P56=4, P57=6, P58=6, P59=6, P60=8 = 30) | low | Verify in the spec |
| The Wave H executor accidentally bypasses the verifier dispatch (autonomous: false intentionally pauses) | medium-high (executors love to autopilot) | The plan is `autonomous: false` + Task 4 is `checkpoint:human-verify`; if Path A unavailable, Path B disclosure is non-negotiable -- the Wave H executor cannot ship without authoring the disclosure + verdict |
| Anti-bloat overflow on CLAUDE.md (P57+P58+P59+P60 H3 subsections accumulate) | low (each is <=80 lines; total ~280 lines under the Quality Gates section is acceptable; MIGRATE-02 in P63 audits) | P63 MIGRATE-02 cohesion pass handles cross-link audit + dedup |
</risk_table>

<output>
After completion, create `.planning/phases/60-docs-build-migration/60-08-SUMMARY.md` capturing:
- CLAUDE.md added line count (<=80).
- STATE.md cursor advanced to P61 (verified).
- REQUIREMENTS.md 6 traceability flips (verified).
- SURPRISES.md P60 entry count + line count (and any archive rotation if exceeded).
- Verdict file path + GREEN status.
- Path A vs Path B used.
- Per-row grade table for the 8 P60-touched rows.
- Carry-forward summary: any waivers added (with `tracked_in=MIGRATE-03 v0.12.1`); any backlog files created.
- Recommendation: "P61 may begin per `quality/reports/verdicts/p60/VERDICT.md`."
- Phase-close commit SHA + commit message.
</output>
