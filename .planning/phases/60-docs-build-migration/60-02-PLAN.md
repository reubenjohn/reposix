---
phase: 60-docs-build-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "60-01"
files_modified:
  - quality/gates/docs-build/mkdocs-strict.sh
  - quality/gates/docs-build/mermaid-renders.sh
  - quality/gates/docs-build/link-resolution.py
  - scripts/check-docs-site.sh
  - scripts/check-mermaid-renders.sh
  - scripts/check_doc_links.py
  - quality/catalogs/docs-build.json
autonomous: true
requirements:
  - DOCS-BUILD-01
  - SIMPLIFY-08
must_haves:
  truths:
    - "The 3 doc-build verifiers (mkdocs-strict.sh, mermaid-renders.sh, link-resolution.py) live at their canonical home `quality/gates/docs-build/` per `.planning/research/v0.12.0/naming-and-architecture.md` § directory layout. Each verifier preserves the existing behaviour verbatim (no logic change in this wave; logic changes are scope-creep)."
    - "Each migration uses `git mv` so git history is preserved (the P59 Wave E SIMPLIFY-11 precedent — 3 files moved via git mv with history-preserved). Per CLAUDE.md OP-5 reversibility: hidden callers may exist; the move PLUS the shim establishes parity for one merge cycle."
    - "Each old path receives a thin shim per OP-5 reversibility (mirrors the P59 Wave D scripts/dark-factory-test.sh shim shape — 7 lines exec'ing the canonical path). Shims are 7-10 lines each; they `exec bash quality/gates/docs-build/<canonical>.sh \"$@\"` (or `exec python3 ...` for the .py file). Shims preserve the original argv passthrough so any hidden caller continues to work."
    - "The `quality/catalogs/docs-build.json` rows for the 3 migrated verifiers (mkdocs-strict, mermaid-renders, link-resolution) get their short-lived waivers REMOVED in the same commit (or in a follow-up commit at end of wave). The runner will flip those rows from WAIVED to PASS on the next `--cadence pre-push` invocation."
    - "Each verifier is self-tested: bash quality/gates/docs-build/mkdocs-strict.sh exits 0 (mkdocs --strict GREEN; warm cache); bash quality/gates/docs-build/mermaid-renders.sh exits 0 (artifacts current under .planning/verifications/playwright/); python3 quality/gates/docs-build/link-resolution.py exits 0 OR identifies broken links to fix in Wave G."
    - "CI workflows that invoked the old paths are audited: grep -RnE 'scripts/(check-docs-site|check-mermaid-renders|check_doc_links)' .github/ docs/ scripts/ CLAUDE.md . The shim covers any hit; no caller-side edit needed at Wave B. (CI workflow updates to canonical paths are Wave G polish if there's spare context budget; otherwise deferred -- the shims work indefinitely.)"
    - "Pre-push runner stays GREEN throughout the migration. After Wave B's commit, `python3 quality/runners/run.py --cadence pre-push` exits 0 with at least 3 newly-flipped PASS rows (mkdocs-strict + mermaid-renders + link-resolution); the docs-build/badges-resolve row stays WAIVED until Wave C; the 3 code-dim + 1 cred-hygiene rows stay WAIVED until Wave D."
    - "The pre-push hook body (`scripts/hooks/pre-push`) is NOT modified in Wave B. The existing hook continues to invoke `scripts/check-docs-site.sh` + `scripts/check-mermaid-renders.sh` on docs-touched commits; both invocations exec-replace into the canonical paths via the shims; no behaviour change. Wave E does the hook body rewrite."
  artifacts:
    - path: "quality/gates/docs-build/mkdocs-strict.sh"
      provides: "Migrated from `scripts/check-docs-site.sh` via git mv. Behaviour preserved verbatim: runs `mkdocs build --strict --site-dir <tmp>/site`, greps build log + rendered HTML for 'Syntax error in text', exits 0/1/2 per the docstring. The set -euo pipefail is preserved. The cd `cd \"$(dirname \"$0\")/..\"` line MUST be updated for the new path depth (was 1 level deep at scripts/; now 2 levels deep at quality/gates/docs-build/) -- specifically: the workspace-root cd anchor changes from `cd \"$(dirname \"$0\")/..\"` to `cd \"$(dirname \"$0\")/../../..\"` (or equivalent; mirror the P59 Wave E REPO_ROOT-arithmetic fix pattern from quality/SURPRISES.md 2026-04-27 P59 Wave E entry)."
      contains: "mkdocs build --strict"
      max_lines: 250
    - path: "quality/gates/docs-build/mermaid-renders.sh"
      provides: "Migrated from `scripts/check-mermaid-renders.sh` via git mv. Behaviour preserved verbatim: walks docs/**/*.md for triple-backtick mermaid fences, asserts each has a corresponding artifact under `.planning/verifications/playwright/<section>/<slug>.json` with valid mermaid_count + svg_counts + console_errors. The REPO_ROOT line `readonly REPO_ROOT=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")/..\" && pwd)\"` MUST be updated for the new path depth: 2 levels deep -> 3 `..`s -> `../../..`. Per the P59 Wave E SUMMARY: 'any __file__-derived REPO_ROOT needs path-arithmetic audit on migration; the depth changed from 1 to 3.'"
      contains: "pre.mermaid svg"
      max_lines: 250
    - path: "quality/gates/docs-build/link-resolution.py"
      provides: "Migrated from `scripts/check_doc_links.py` via git mv (and renamed: underscore-to-hyphen for non-test Python files per `.planning/research/v0.12.0/naming-and-architecture.md` directory-layout convention -- `link-resolution.py`, mirroring the existing pattern at `quality/gates/release/{gh-assets-present.py, ...}`). Behaviour preserved verbatim: globs DEFAULT_GLOBS, runs LINK_RE.finditer, validates relative links resolve to existing paths, returns 1 on any broken link. NO REPO_ROOT changes needed (the script uses `os.path.normpath(os.path.join(os.path.dirname(path), rel))` which is path-relative, not __file__-relative)."
      contains: "BROKEN in"
      max_lines: 250
    - path: "scripts/check-docs-site.sh"
      provides: "Thin shim post-migration. 7-10 lines. Header comment names the canonical path + the OP-5 rationale (hidden callers; one merge cycle of parity; CI workflow audit pending Wave G). Body: `exec bash \"$(dirname \"${BASH_SOURCE[0]}\")/../quality/gates/docs-build/mkdocs-strict.sh\" \"$@\"`. Mirror the shape of `scripts/dark-factory-test.sh` post-P59 Wave D (the 7-line shim precedent)."
      contains: "exec bash"
      max_lines: 12
    - path: "scripts/check-mermaid-renders.sh"
      provides: "Thin shim post-migration. Same shape as scripts/check-docs-site.sh shim. Body: `exec bash \"$(dirname \"${BASH_SOURCE[0]}\")/../quality/gates/docs-build/mermaid-renders.sh\" \"$@\"`. <=12 lines."
      contains: "exec bash"
      max_lines: 12
    - path: "scripts/check_doc_links.py"
      provides: "Thin shim post-migration. Python this time. Body: `import os, sys; os.execv(sys.executable, [sys.executable, os.path.join(os.path.dirname(os.path.realpath(__file__)), '..', 'quality', 'gates', 'docs-build', 'link-resolution.py')] + sys.argv[1:])`. <=12 lines including header comment."
      contains: "execv"
      max_lines: 12
    - path: "quality/catalogs/docs-build.json"
      provides: "After Wave B's verifier files land + the runner re-grades, the 3 short-lived waivers on mkdocs-strict / mermaid-renders / link-resolution rows are REMOVED in the same commit (waiver: null). The 4th row (docs-build/badges-resolve) keeps its short-lived waiver until Wave C. The runner's catalog_dirty() persists the status flips PASS on next --cadence pre-push invocation."
      contains: "docs-build/mkdocs-strict"
  key_links:
    - from: "scripts/check-docs-site.sh (shim)"
      to: "quality/gates/docs-build/mkdocs-strict.sh"
      via: "exec bash $REPO_ROOT/quality/gates/docs-build/mkdocs-strict.sh \"$@\""
      pattern: "exec bash"
    - from: "scripts/check-mermaid-renders.sh (shim)"
      to: "quality/gates/docs-build/mermaid-renders.sh"
      via: "exec bash"
      pattern: "exec bash"
    - from: "scripts/check_doc_links.py (shim)"
      to: "quality/gates/docs-build/link-resolution.py"
      via: "os.execv"
      pattern: "execv"
    - from: "scripts/hooks/pre-push:188 + :201 (existing invocations)"
      to: "shims at scripts/check-docs-site.sh + scripts/check-mermaid-renders.sh"
      via: "exec bash inside shim -> canonical path -> behaviour preserved verbatim"
      pattern: "scripts/check-docs-site.sh"
    - from: "Wave B file moves"
      to: "Wave H unbiased verifier subagent"
      via: "the verifier finds the 3 verifiers at their canonical homes; row.status flips PASS; the catalog_dirty() persists the flip"
      pattern: "PASS"
---

<objective>
Wave B — migrate the 3 doc-build verifiers (`scripts/check-docs-site.sh`, `scripts/check-mermaid-renders.sh`, `scripts/check_doc_links.py`) to their canonical home at `quality/gates/docs-build/`. SIMPLIFY-08 closure for the 3 verifiers (the 4th, `green-gauntlet.sh`, is SIMPLIFY-09 and lands in Wave D).

The migration is **mostly mechanical** per `.planning/research/v0.12.0/roadmap-and-rationale.md` P60 entry — rename + colocate + path-arithmetic fix. Behaviour is preserved verbatim; logic changes are scope-creep that Wave G's broaden-and-deepen sweep handles if needed.

Three deliverables ship in this wave (across 1-2 commits):

1. **`git mv scripts/check-docs-site.sh quality/gates/docs-build/mkdocs-strict.sh`** + path-arithmetic fix (workspace-root anchor was 1 level deep at `scripts/`, now 3 levels deep at `quality/gates/docs-build/`).
2. **`git mv scripts/check-mermaid-renders.sh quality/gates/docs-build/mermaid-renders.sh`** + same path-arithmetic fix (REPO_ROOT line).
3. **`git mv scripts/check_doc_links.py quality/gates/docs-build/link-resolution.py`** + the underscore-to-hyphen rename for non-test Python (per the `.planning/research/v0.12.0/naming-and-architecture.md` directory-layout convention; mirrors `quality/gates/release/gh-assets-present.py` which uses hyphens). NO path-arithmetic fix needed (the Python script doesn't derive paths from __file__).

Each old path keeps a thin shim (7-12 lines) that exec-replaces the canonical path. The shim preserves argv passthrough so any hidden caller continues to work. Per CLAUDE.md OP-5 reversibility, shims survive at least one merge cycle (P63 SIMPLIFY-12 audits).

After the 3 file moves land, the catalog rows in `quality/catalogs/docs-build.json` for mkdocs-strict / mermaid-renders / link-resolution have their short-lived waivers REMOVED in the same wave (or in a follow-up commit) so the runner re-grades them PASS on the next `--cadence pre-push` invocation.

The 4th docs-build row (badges-resolve) keeps its waiver until Wave C ships the verifier. The 3 code-dim + 1 cred-hygiene rows keep their waivers until Wave D.

**The pre-push hook body (`scripts/hooks/pre-push`) is NOT modified in Wave B.** The existing hook still invokes `scripts/check-docs-site.sh` + `scripts/check-mermaid-renders.sh` on docs-touched commits — both calls exec-replace into the canonical paths via the shims; no behaviour change visible at the hook layer. Wave E does the hook body rewrite.

**Pivot rule** per `.planning/research/v0.12.0/roadmap-and-rationale.md` P60 entry: if path moves break hooks, leave shims at old paths. Wave B IS the shim-leaving wave per OP-5 reversibility — no hook breakage possible.

**Path-arithmetic memory cue** from `quality/SURPRISES.md` 2026-04-27 P59 Wave E (SIMPLIFY-11): "any __file__-derived REPO_ROOT needs path-arithmetic audit on migration; the depth changed from 1 to 3." Same lesson applies here. The 2 bash scripts have `cd` / REPO_ROOT lines that derive workspace-root from the script's own path; those MUST be updated when the script depth changes.

Output: 1-2 commits.
- Commit 1: `refactor(p60): migrate docs-build verifiers to canonical home (SIMPLIFY-08)` — git-mv 3 files + path-arithmetic fixes + 3 shims at old paths.
- Commit 2 (optional): `chore(p60): unwaive 3 docs-build catalog rows post-migration` — 3 short-lived waivers removed; runner re-grades to PASS.

Pivot if Commit 2 doesn't fit context budget: leave the unwaive for Wave G's broaden-and-deepen sweep. The waivers expire 2026-05-15 either way; Wave G is well within window.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.12.0/naming-and-architecture.md
@.planning/research/v0.12.0/roadmap-and-rationale.md
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/catalogs/docs-build.json
@quality/catalogs/freshness-invariants.json
@quality/catalogs/code.json
@quality/runners/run.py
@quality/gates/docs-repro/README.md
@quality/gates/docs-build/README.md
@scripts/check-docs-site.sh
@scripts/check-mermaid-renders.sh
@scripts/check_doc_links.py
@scripts/hooks/pre-push
@scripts/dark-factory-test.sh
@scripts/latency-bench.sh
@.planning/phases/60-docs-build-migration/60-01-PLAN.md

<interfaces>
**Source files (verbatim from HEAD):**

`scripts/check-docs-site.sh` (54 lines; per Read above):
- shebang + docstring (POLISH-17 / OP-4 lineage; v0.11.x history; rationale)
- `set -euo pipefail`
- `cd "$(dirname "$0")/.."` <-- workspace-root anchor; FIX needed at new depth
- `TMP=$(mktemp -d) ; trap 'rm -rf "$TMP"' EXIT`
- `mkdocs build --strict --site-dir "$TMP/site" 2>&1 | tee "$TMP/build.log"`
- 2 grep-based assertions (build log + rendered HTML for "Syntax error in text")
- `echo "OK: docs site clean" >&2`

After migration to `quality/gates/docs-build/mkdocs-strict.sh`:
- preserve everything verbatim EXCEPT change `cd "$(dirname "$0")/.."` -> `cd "$(dirname "$0")/../../.."` (3 ..s for 3-level depth: quality/ -> gates/ -> docs-build/ -> back to repo root). DO NOT use `bash dirname` arithmetic shortcuts; use exactly the existing pattern with the corrected count.
- update header comment block to reference new canonical home + the SIMPLIFY-08 lineage (1 line: `# Migrated from scripts/check-docs-site.sh per P60 SIMPLIFY-08 (2026-04-27).`).

`scripts/check-mermaid-renders.sh` (99 lines; per Read above):
- shebang + 36-line docstring rationale
- `set -euo pipefail`
- `readonly REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` <-- workspace-root anchor; FIX needed at new depth
- `readonly DOCS_DIR="${REPO_ROOT}/docs"` (derived; OK)
- `readonly ARTIFACT_DIR="${REPO_ROOT}/.planning/verifications/playwright"` (derived; OK)
- `cd "$REPO_ROOT"` (uses derived REPO_ROOT)
- main loop: walks `grep -rl '^```mermaid' "$DOCS_DIR" --include='*.md'`, derives artifact path, validates artifact JSON

After migration to `quality/gates/docs-build/mermaid-renders.sh`:
- preserve everything verbatim EXCEPT `readonly REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` -> `readonly REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"` (3 ..s).
- update header comment block to reference new canonical home + the SIMPLIFY-08 lineage.

`scripts/check_doc_links.py` (69 lines; per Read above):
- shebang + 6-line docstring
- imports: `glob`, `os`, `re`, `sys` (stdlib only - good)
- `DEFAULT_GLOBS = ["docs/index.md", "docs/concepts/*.md", ...]` (path-relative, not __file__-relative; OK)
- `LINK_RE = re.compile(r"\]\(([^)]+)\)")` (OK)
- `collect()` + `check()` + `main(argv)` functions
- entry point: `if __name__ == "__main__": sys.exit(main(sys.argv))`

After migration to `quality/gates/docs-build/link-resolution.py`:
- preserve everything verbatim. NO path-arithmetic changes (script uses `os.path.dirname(path)` for the markdown file's location, not __file__).
- the script uses `glob.glob(pat)` with relative globs like `"docs/index.md"`; this works only if cwd is the repo root. The CALLER is expected to invoke from repo root (the runner does this; pre-push hook does this). NO change needed for v0.12.0 P60 scope.
- update header comment block to reference new canonical home + the SIMPLIFY-08 lineage (add a 2-line `"""... Migrated from scripts/check_doc_links.py per P60 SIMPLIFY-08 ..."""`).

**Shim shape (model on `scripts/dark-factory-test.sh` post-P59 Wave D):**

`scripts/check-docs-site.sh` (post-migration):
```bash
#!/usr/bin/env bash
# scripts/check-docs-site.sh -- thin shim post-P60 SIMPLIFY-08.
#
# The canonical home is quality/gates/docs-build/mkdocs-strict.sh. This
# shim survives for one merge cycle per OP-5 reversibility (any hidden
# caller surfaces). P63 SIMPLIFY-12 audits whether to delete or keep.
set -euo pipefail
exec bash "$(dirname "${BASH_SOURCE[0]}")/../quality/gates/docs-build/mkdocs-strict.sh" "$@"
```

`scripts/check-mermaid-renders.sh` (post-migration): same shape, body `exec bash ... mermaid-renders.sh "$@"`.

`scripts/check_doc_links.py` (post-migration):
```python
#!/usr/bin/env python3
"""scripts/check_doc_links.py -- thin shim post-P60 SIMPLIFY-08.

The canonical home is quality/gates/docs-build/link-resolution.py. This
shim survives for one merge cycle per OP-5 reversibility (any hidden
caller surfaces). P63 SIMPLIFY-12 audits whether to delete or keep.
"""
import os
import sys
target = os.path.join(os.path.dirname(os.path.realpath(__file__)), "..",
                     "quality", "gates", "docs-build", "link-resolution.py")
os.execv(sys.executable, [sys.executable, target] + sys.argv[1:])
```

**Catalog row unwaive contract:**

After the 3 files land at canonical paths, the catalog rows in `quality/catalogs/docs-build.json` for `docs-build/mkdocs-strict`, `docs-build/mermaid-renders`, `docs-build/link-resolution` have their `waiver` block set to `null` (the verifier path now exists; no need for the short-lived waiver).

The runner's `catalog_dirty()` (`quality/runners/run.py:91-98`) only persists status changes, not waiver-block edits. The waiver removal is a manual edit; the runner picks it up on next `--cadence pre-push` and grades the row PASS.

To set the rows to PASS in the same Wave B commit (atomic-with-the-migration), simply remove the waiver block from each of the 3 rows in `quality/catalogs/docs-build.json` and let the next runner invocation flip them. OR keep the waiver and let Wave G's sweep do the unwaive — context budget decides.

**Path-arithmetic regression test:**

After the move, `bash quality/gates/docs-build/mkdocs-strict.sh` MUST exit 0 from the workspace root. Test:
```
bash quality/gates/docs-build/mkdocs-strict.sh
echo $?  # must be 0; mkdocs site clean
```

If the cd lands in the wrong dir (because the `..` count is wrong), mkdocs build will fail with "config file 'mkdocs.yml' does not exist" — the canonical failure mode. Caught at the verify gate.

For mermaid-renders.sh: test `bash quality/gates/docs-build/mermaid-renders.sh` exits 0 (assumes the playwright artifacts under `.planning/verifications/playwright/` are current per CLAUDE.md "Whole-nav-section playwright walks"). Wave G can refresh artifacts if needed.

For link-resolution.py: test `python3 quality/gates/docs-build/link-resolution.py` from repo root. If broken links exist, identify which (Wave G fixes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv mkdocs-strict.sh + path-arithmetic fix</name>
  <files>quality/gates/docs-build/mkdocs-strict.sh, scripts/check-docs-site.sh</files>
  <action>(1) Run `git mv scripts/check-docs-site.sh quality/gates/docs-build/mkdocs-strict.sh`. (2) Edit `quality/gates/docs-build/mkdocs-strict.sh`: change the `cd "$(dirname "$0")/.."` line to `cd "$(dirname "$0")/../../.."` (the workspace-root depth changed from 1 to 3). Add a 1-line header comment naming the SIMPLIFY-08 lineage: `# Migrated from scripts/check-docs-site.sh per P60 SIMPLIFY-08 (2026-04-27).`. NO other edits — preserve behaviour verbatim. (3) Recreate `scripts/check-docs-site.sh` as a 7-12 line shim per the `<interfaces>` shape spec. Make it executable (`chmod +x scripts/check-docs-site.sh`). The shim's exec-replace path uses `$(dirname "${BASH_SOURCE[0]}")/../quality/gates/docs-build/mkdocs-strict.sh` so it works regardless of cwd. Self-test: `bash quality/gates/docs-build/mkdocs-strict.sh` exits 0 AND `bash scripts/check-docs-site.sh` (via the shim) exits 0.</action>
  <verify>
    <automated>test -x quality/gates/docs-build/mkdocs-strict.sh && test -x scripts/check-docs-site.sh && bash quality/gates/docs-build/mkdocs-strict.sh > /tmp/p60-mkdocs-canonical.log 2>&1 || { echo FAIL canonical; tail -30 /tmp/p60-mkdocs-canonical.log; exit 1; } && bash scripts/check-docs-site.sh > /tmp/p60-mkdocs-shim.log 2>&1 || { echo FAIL shim; tail -30 /tmp/p60-mkdocs-shim.log; exit 1; } && lines=$(wc -l < scripts/check-docs-site.sh) && test "$lines" -le 12 || { echo "shim too long: $lines"; exit 1; } && echo OK</automated>
  </verify>
  <done>git mv preserves history; canonical path runs `mkdocs build --strict` GREEN; shim at old path exec-replaces into canonical path GREEN; both exit 0; shim <=12 lines; old path file is the shim, not the original 54-line script.</done>
</task>

<task type="auto">
  <name>Task 2: git mv mermaid-renders.sh + path-arithmetic fix</name>
  <files>quality/gates/docs-build/mermaid-renders.sh, scripts/check-mermaid-renders.sh</files>
  <action>(1) Run `git mv scripts/check-mermaid-renders.sh quality/gates/docs-build/mermaid-renders.sh`. (2) Edit `quality/gates/docs-build/mermaid-renders.sh`: change the `readonly REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` line to `readonly REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)"` (3 levels deep). Add a 1-line header comment for the SIMPLIFY-08 lineage. NO other edits. (3) Recreate `scripts/check-mermaid-renders.sh` as a 7-12 line shim per the `<interfaces>` shape spec. Make executable. Self-test: `bash quality/gates/docs-build/mermaid-renders.sh` exits 0 (assumes playwright artifacts current; if RED, that's a Wave G concern, not a Wave B blocker — log + continue).</action>
  <verify>
    <automated>test -x quality/gates/docs-build/mermaid-renders.sh && test -x scripts/check-mermaid-renders.sh && bash quality/gates/docs-build/mermaid-renders.sh > /tmp/p60-mermaid-canonical.log 2>&1; ec=$?; if [ "$ec" -ne 0 ]; then echo "WARN: canonical mermaid-renders exit=$ec (artifact staleness; Wave G will refresh)"; tail -20 /tmp/p60-mermaid-canonical.log; fi; bash scripts/check-mermaid-renders.sh > /tmp/p60-mermaid-shim.log 2>&1; ec_shim=$?; if [ "$ec" != "$ec_shim" ]; then echo FAIL shim parity ec=$ec ec_shim=$ec_shim; exit 1; fi; lines=$(wc -l < scripts/check-mermaid-renders.sh) && test "$lines" -le 12 || { echo "shim too long"; exit 1; } && echo OK shim parity</automated>
  </verify>
  <done>git mv preserves history; canonical path REPO_ROOT resolves correctly to repo root; shim and canonical exit with same code (whether 0 or non-zero — parity is the contract; Wave G fixes the actual mermaid artifact staleness if any); shim <=12 lines.</done>
</task>

<task type="auto">
  <name>Task 3: git mv link-resolution.py (with hyphen-rename) + Python shim</name>
  <files>quality/gates/docs-build/link-resolution.py, scripts/check_doc_links.py</files>
  <action>(1) Run `git mv scripts/check_doc_links.py quality/gates/docs-build/link-resolution.py` (the rename includes underscore-to-hyphen for non-test Python files per `.planning/research/v0.12.0/naming-and-architecture.md` directory-layout convention; mirrors `quality/gates/release/gh-assets-present.py`). (2) Edit `quality/gates/docs-build/link-resolution.py`: add a 2-line addendum to the docstring naming the SIMPLIFY-08 lineage (`Migrated from scripts/check_doc_links.py per P60 SIMPLIFY-08 (2026-04-27).`). NO logic edits. (3) Recreate `scripts/check_doc_links.py` as the Python shim per the `<interfaces>` shape spec (uses `os.execv`). Make executable. Self-test: `python3 quality/gates/docs-build/link-resolution.py` runs from repo root; if RED, log the broken-link report and continue (Wave G fixes; Wave B's contract is "the migration works", not "all links are GREEN").</action>
  <verify>
    <automated>test -x quality/gates/docs-build/link-resolution.py && test -x scripts/check_doc_links.py && python3 quality/gates/docs-build/link-resolution.py > /tmp/p60-links-canonical.log 2>&1; ec=$?; if [ "$ec" -ne 0 ]; then echo "WARN: canonical link-resolution exit=$ec (broken links; Wave G fixes); BROKEN report:"; head -30 /tmp/p60-links-canonical.log; fi; python3 scripts/check_doc_links.py > /tmp/p60-links-shim.log 2>&1; ec_shim=$?; if [ "$ec" != "$ec_shim" ]; then echo FAIL shim parity ec=$ec ec_shim=$ec_shim; exit 1; fi; lines=$(wc -l < scripts/check_doc_links.py) && test "$lines" -le 12 || { echo "shim too long"; exit 1; } && echo OK shim parity</automated>
  </verify>
  <done>git mv preserves history; rename underscore-to-hyphen lands at link-resolution.py; canonical and shim exit with same code (parity); shim <=12 lines using os.execv; broken-link report logged for Wave G if any.</done>
</task>

<task type="auto">
  <name>Task 4: Unwaive the 3 newly-implemented docs-build catalog rows + atomic commit</name>
  <files>quality/catalogs/docs-build.json</files>
  <action>(1) Edit `quality/catalogs/docs-build.json`: for the 3 rows (`docs-build/mkdocs-strict`, `docs-build/mermaid-renders`, `docs-build/link-resolution`) -- set `"waiver": null`. The 4th row (`docs-build/badges-resolve`) keeps its short-lived waiver until Wave C. (2) Run `python3 quality/runners/run.py --cadence pre-push`. The runner should now flip the 3 rows from WAIVED to PASS (or to FAIL/PARTIAL if any verifier finds genuine RED — in which case Wave G must fix). The runner also persists the status flip back to the catalog file via `catalog_dirty()`. (3) Commit everything as a single atomic commit (or 2 commits: file moves + shims as commit 1; unwaive + status flip as commit 2). Title: `refactor(p60): migrate docs-build verifiers to canonical home (SIMPLIFY-08)`. Body cites: 3 git-mv operations + 3 path-arithmetic fixes + 3 thin shims + 3 catalog rows unwaived. Run pre-push runner one more time to confirm GREEN.</action>
  <verify>
    <automated>python3 -c "import json; d=json.loads(open('quality/catalogs/docs-build.json').read()); rows={r['id']:r for r in d['rows']}; assert rows['docs-build/mkdocs-strict']['waiver'] is None, 'mkdocs-strict still waived'; assert rows['docs-build/mermaid-renders']['waiver'] is None, 'mermaid-renders still waived'; assert rows['docs-build/link-resolution']['waiver'] is None, 'link-resolution still waived'; assert rows['docs-build/badges-resolve']['waiver'] is not None, 'badges-resolve waiver lost'; print('OK 3 unwaived; badges-resolve still waived')" && python3 quality/runners/run.py --cadence pre-push 2>&1 | tee /tmp/p60-wave-b-runner.log && tail -3 /tmp/p60-wave-b-runner.log | grep -F 'exit=0' && git log -1 --format='%s' | grep -F 'p60' && echo "OK Wave B clean"</automated>
  </verify>
  <done>3 catalog rows unwaived; runner re-grades them PASS (or PARTIAL/FAIL with the diagnostic logged for Wave G); pre-push runner exit 0; commit lands the 3 file moves + 3 path-arithmetic fixes + 3 shims + the catalog unwaive in 1-2 commits; pre-push hook still GREEN.</done>
</task>

</tasks>

<verification>
After Wave B close:
- `bash quality/gates/docs-build/mkdocs-strict.sh` exits 0 from repo root (mkdocs site clean).
- `bash quality/gates/docs-build/mermaid-renders.sh` exits 0 (assuming playwright artifacts current).
- `python3 quality/gates/docs-build/link-resolution.py` exits 0 (or identifies broken links for Wave G).
- The 3 shims at old paths exit-replace correctly and parity exit codes.
- `python3 quality/runners/run.py --cadence pre-push` exits 0 with at least 3 newly-flipped PASS rows.
- `bash scripts/hooks/pre-push` exit 0 on a docs-touched commit (the existing chain still invokes the old path; the shim covers it).
- `git log` shows the 3 moves preserved as renames (history visible via `git log --follow quality/gates/docs-build/mkdocs-strict.sh`).
</verification>

<success_criteria>
1. **Catalog-first criterion 1:** N/A for Wave B (Wave A locked the contract; Wave B implements).
2. **CLAUDE.md update:** N/A for Wave B (lands at Wave H per QG-07).
3. **Verifier-subagent dispatch:** N/A for Wave B (lands at Wave H).
4. **SIMPLIFY-08 absorption (3 of 4 verifiers):** mkdocs-strict + mermaid-renders + link-resolution migrated via git-mv with shims at old paths. The 4th SIMPLIFY-08 verifier (badges-resolve) is NEW (no predecessor) and lands in Wave C.
5. **RED-row fix:** any RED row found by the migrated verifiers (broken links, mermaid artifact staleness, mkdocs --strict failure) is LOGGED in this wave's verify output but FIXED in Wave G's POLISH-DOCS-BUILD broaden-and-deepen sweep. Wave B's contract is "migration works", not "all docs-build is GREEN".
</success_criteria>

<pivot_rules>
- **If `bash quality/gates/docs-build/mkdocs-strict.sh` fails with "config file 'mkdocs.yml' does not exist"**: the path-arithmetic fix is wrong. Recount the `..`s: from `quality/gates/docs-build/mkdocs-strict.sh`, you need to `cd ../../..` to land at the repo root (3 levels up). Mirror P59 Wave E's fix shape.
- **If `bash quality/gates/docs-build/mermaid-renders.sh` fails with "artifact missing" or "console_errors non-empty"**: Wave G fixes the actual mermaid artifact staleness via `mcp__playwright__*` walks per CLAUDE.md "Whole-nav-section playwright walks". Wave B's contract is just the migration; the row is at WORST PARTIAL/FAIL but the path is correct. Log the diagnostic and continue.
- **If `python3 quality/gates/docs-build/link-resolution.py` reports >5 broken links**: per the prompt's pivot rule, fix the worst, file a backlog item for the rest, do NOT block Wave B close. The Wave G broaden-and-deepen sweep handles the systematic fix-up.
- **If a hidden caller in `.github/`, `docs/`, `examples/`, `CLAUDE.md` invokes the old path**: the shim covers it. Only update the caller to the canonical path if the caller is also being touched in this same wave (parsimony rule). Document any caller updates in the SUMMARY.
- **If git mv complains "destination exists"**: a previous botched run left the destination file behind. Inspect, then `git rm --cached <destination>` + remove the file + re-run `git mv`. Test the canonical path again.
- **If the rename underscore-to-hyphen breaks a Python import (e.g., a test imports `link_resolution` as a module)**: Python module imports use underscores, hyphens are illegal. Per P59 Wave E SIMPLIFY-11 Option B precedent (kept underscore at `bench_token_economy.py` because tests imported it), if any test imports the script as a module, KEEP UNDERSCORES — do `git mv scripts/check_doc_links.py quality/gates/docs-build/check_doc_links.py` (no rename). Audit for imports first: `grep -rn "import check_doc_links\|from check_doc_links" .` -- empty stdout? Use hyphenated `link-resolution.py`. Non-empty? Keep `check_doc_links.py`. Document the choice in the SUMMARY.

Reference: `quality/SURPRISES.md` 2026-04-27 P59 Wave E SIMPLIFY-11 (path-arithmetic + Option B underscore precedent); `.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-04-PLAN.md` (SIMPLIFY-07 dark-factory.sh shim precedent — 7-line exec-replace).
</pivot_rules>

<risk_table>
| Risk | Likelihood | Mitigation |
|---|---|---|
| Path-arithmetic count off-by-one (`../..` vs `../../..`) breaks mkdocs lookup | medium-high | Task 1 + Task 2 verify gates explicitly run the canonical script and assert exit 0; on RED, the diagnostic ("config file 'mkdocs.yml' does not exist") names the exact failure |
| `git mv` does not preserve history because of a same-name conflict at destination | low | The destination dirs `quality/gates/docs-build/` exist (P60 Wave A built them via the README); the destination filenames are NEW (no conflict); history preserved with single git mv each |
| Underscore-to-hyphen rename for `link-resolution.py` breaks a Python module import | low (no callers found in initial codebase grep; check P58 + P59 precedents) | Pivot rule: audit imports BEFORE the rename; if any importer found, keep underscores per P59 Option B precedent |
| Mermaid playwright artifact staleness causes `mermaid-renders.sh` to grade RED | medium (artifacts last refreshed 2026-04-27; pages may have been touched since) | Wave B's verify catches this and logs a WARN; Wave G's POLISH-DOCS-BUILD sweep refreshes artifacts via the playwright MCP per CLAUDE.md "Whole-nav-section playwright walks". Wave B does NOT block on this |
| Broken link report from `link-resolution.py` floods the verify output (>5 broken) | medium | Pivot rule: log the BROKEN list + file backlog + continue. Wave G fixes the worst per the prompt |
| Wave B's commit accidentally drops the path-arithmetic fix during a rebase | low | The verify gate explicitly runs the canonical script after the commit; CI also re-runs the runner on push |
| The pre-push hook gets confused by the parallel-migration window (old paths + new paths both exist) | low | The shim is the disambiguation: old paths exec-replace into canonical paths; only ONE path actually executes per call. Hook layer sees no change |
| Catalog row unwaive in Task 4 happens BEFORE the runner can grade the new rows | low | The runner is invoked AFTER the unwaive in Task 4; verifier paths exist by that point; runner flips PASS |
</risk_table>

<output>
After completion, create `.planning/phases/60-docs-build-migration/60-02-SUMMARY.md` capturing:
- 3 git-mv operations completed; history preserved (`git log --follow`).
- Path-arithmetic fixes applied (line numbers cited).
- 3 thin shims at old paths (line counts; <=12 each).
- Whether the underscore-to-hyphen rename for `link-resolution.py` survived (per pivot rule audit).
- Catalog rows unwaived: 3 of 4 docs-build.json rows now PASS (or the WARN-list of any RED ones for Wave G).
- Any SURPRISES.md entry needed (path-arithmetic surprises, broken link counts, etc.).
- Pre-push runner output post-Wave B (must show 3 PASS + 1 WAIVED for badges-resolve + 3 WAIVED for code/cred + amended badges-resolve WAIVED).
</output>
