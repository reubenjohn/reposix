---
phase: 60-docs-build-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/catalogs/docs-build.json
  - quality/catalogs/code.json
  - quality/catalogs/freshness-invariants.json
  - quality/gates/docs-build/README.md
autonomous: true
requirements:
  - DOCS-BUILD-01
  - BADGE-01
  - SIMPLIFY-08
  - SIMPLIFY-09
  - SIMPLIFY-10
  - QG-09
must_haves:
  truths:
    - "A committed catalog set defines GREEN for P60 BEFORE any verifier code lands or any path move ships (catalog-first per quality/PROTOCOL.md Step 3 and recurring criterion 1 from REQUIREMENTS.md)."
    - "quality/catalogs/docs-build.json is a NEW catalog file (dimension=docs-build) carrying 4 rows: docs-build/mkdocs-strict (mechanical; pre-push; migrate target for scripts/check-docs-site.sh per SIMPLIFY-08), docs-build/mermaid-renders (mechanical; pre-push; migrate target for scripts/check-mermaid-renders.sh per SIMPLIFY-08), docs-build/link-resolution (mechanical; pre-push; migrate target for scripts/check_doc_links.py per SIMPLIFY-08), docs-build/badges-resolve (mechanical; pre-push + weekly; the BADGE-01 verifier ships in Wave C)."
    - "quality/catalogs/code.json gains 2 rows that catalog the existing pre-push hook chain so SIMPLIFY-10 can fold them into the runner: code/cargo-fmt-check (mechanical; pre-push; existing `cargo fmt --all -- --check` hook line 215-219; verifier wrapper at quality/gates/code/cargo-fmt-check.sh ships in Wave D), code/cargo-clippy-warnings (mechanical; pre-push; existing `cargo clippy --workspace --all-targets -- -D warnings` hook line 222-227; verifier wrapper at quality/gates/code/cargo-clippy-warnings.sh ships in Wave D). Both rows MUST carry a short-lived waiver until 2026-05-15 (≤14d) tracked_in P60 Wave D so that catalog-first does not break pre-push (per the P58 Wave A clippy-lint-loaded precedent in quality/SURPRISES.md 2026-04-27 P58 row 2)."
    - "quality/catalogs/freshness-invariants.json gains 1 row: structure/cred-hygiene (mechanical; pre-push; the credential-prefix scan at scripts/hooks/pre-push lines 63-122; verifier wrapper at quality/gates/structure/cred-hygiene.sh ships in Wave D). Same waiver pattern as code rows above (until 2026-05-15 tracked_in P60 Wave D)."
    - "quality/catalogs/freshness-invariants.json's structure/badges-resolve row is amended in this commit: the WAIVED-until-2026-07-25 waiver is REMOVED in this commit (the verifier path is unchanged at quality/gates/docs-build/badges-resolve.py; Wave C ships the verifier so the runner flips it to PASS in Wave G's broaden-and-deepen sweep). The runner's verifier-not-found branch (quality/runners/run.py:160-180) sets NOT-VERIFIED on a missing verifier; that's acceptable for a P2 row in the pre-push window, but to keep pre-push GREEN during the implementation window we attach a SHORT-LIVED replacement waiver until 2026-05-15 tracked_in P60 Wave C (mirrors the cargo-fmt-check / cargo-clippy-warnings / cred-hygiene pattern). After Wave C lands the verifier, the runner flips the row to PASS and the waiver expires harmlessly."
    - "quality/gates/docs-build/README.md is a NEW dimension home (<=50 lines, anti-bloat per quality/PROTOCOL.md). Sections: verifier table (4 rows: mkdocs-strict.sh | docs-build/mkdocs-strict | pre-push; mermaid-renders.sh | docs-build/mermaid-renders | pre-push; link-resolution.py | docs-build/link-resolution | pre-push; badges-resolve.py | docs-build/badges-resolve | pre-push + weekly), conventions (stdlib-only Python; bash POSIX; ci_friendly; exit codes 0=PASS / 1=FAIL / 2=PARTIAL per the QG-04 contract), pivot rules (path moves break hooks -> leave shims; >5 broken links found by Wave G -> file backlog; chicken-and-egg badge endpoint URL -> seed docs/badge.json directly during Wave F build)."
    - "ALL 4 docs-build.json rows + 2 new code.json rows + 1 new freshness-invariants row are status=NOT-VERIFIED in this commit. The verifier files do NOT yet exist (Waves B-D ship them); the catalog locks the contract for the Wave H unbiased verifier subagent."
    - "Wave A's commit does NOT touch scripts/check-docs-site.sh, scripts/check-mermaid-renders.sh, scripts/check_doc_links.py, scripts/green-gauntlet.sh, or scripts/hooks/pre-push. Those move/rewrite in Waves B/D/E. Wave A is contract-only."
  artifacts:
    - path: "quality/catalogs/docs-build.json"
      provides: "NEW catalog (dimension=docs-build). 4 rows: docs-build/mkdocs-strict, docs-build/mermaid-renders, docs-build/link-resolution, docs-build/badges-resolve. Wrapper ($schema, comment, dimension, rows) per P58/P59 precedent. Every row uses unified schema (id, dimension, cadence, kind, sources, command, expected, verifier, artifact, status=NOT-VERIFIED, last_verified=null, freshness_ttl=null, blast_radius, owner_hint, waiver=null OR short-lived for badges-resolve row only)."
      contains: "docs-build/mkdocs-strict"
    - path: "quality/catalogs/code.json"
      provides: "Append 2 rows to existing 4 (final total: 6 rows). New rows: code/cargo-fmt-check (verifier.script: quality/gates/code/cargo-fmt-check.sh; cadence: pre-push; blast_radius: P1; expected.asserts: 'cargo fmt --all -- --check exits 0 with no formatting drift'; waiver until 2026-05-15 tracked_in P60 Wave D), code/cargo-clippy-warnings (verifier.script: quality/gates/code/cargo-clippy-warnings.sh; cadence: pre-push; blast_radius: P1; expected.asserts: 'cargo clippy --workspace --all-targets -- -D warnings exits 0'; waiver until 2026-05-15 tracked_in P60 Wave D)."
      contains: "code/cargo-fmt-check"
    - path: "quality/catalogs/freshness-invariants.json"
      provides: "Append 1 row + amend 1 existing row. NEW row: structure/cred-hygiene (verifier.script: quality/gates/structure/cred-hygiene.sh; cadence: pre-push; blast_radius: P0; expected.asserts: 'no credential-prefix matches in outgoing ref ranges per the 4 PATTERNS in scripts/hooks/pre-push lines 41-46'; waiver until 2026-05-15 tracked_in P60 Wave D). AMENDED row: structure/badges-resolve loses its 2026-07-25 BADGE-01/P60 waiver and gains a SHORT-LIVED waiver until 2026-05-15 tracked_in P60 Wave C (verifier ships in Wave C; status flips to PASS in Wave G after the runner re-grades). The amend is a minimal-disturbance edit (the row anchors the contract that Wave C's verifier file path satisfies)."
      contains: "structure/cred-hygiene"
    - path: "quality/gates/docs-build/README.md"
      provides: "Dimension home for docs-build. <=50 lines. Sections: verifier table (4 rows), conventions (stdlib Python only; bash POSIX-where-possible; <=250 lines per verifier; <=30 lines for the migrated pre-push hook body per P60 Wave E), pivot rules (path-move shim rule per .planning/research/v0.12.0-roadmap-and-rationale.md P60 entry; chicken-and-egg endpoint badge rule per the prompt). Cross-references quality/PROTOCOL.md (anti-bloat: does NOT duplicate runtime detail). Banned word check: NO `replace`."
      contains: "docs-build/mkdocs-strict"
      max_lines: 50
  key_links:
    - from: "quality/catalogs/docs-build.json"
      to: "quality/gates/docs-build/{mkdocs-strict.sh, mermaid-renders.sh, link-resolution.py, badges-resolve.py} (Waves B + C)"
      via: "row.verifier.script field path"
      pattern: "quality/gates/docs-build"
    - from: "quality/catalogs/code.json + freshness-invariants.json (3 new rows)"
      to: "quality/gates/{code,structure}/{cargo-fmt-check.sh, cargo-clippy-warnings.sh, cred-hygiene.sh} (Wave D)"
      via: "row.verifier.script + short-lived waiver until 2026-05-15"
      pattern: "tracked_in: P60 Wave D"
    - from: "quality/catalogs/freshness-invariants.json (structure/badges-resolve waiver swap)"
      to: "Wave C verifier ship + Wave G broaden-and-deepen sweep"
      via: "waiver until 2026-05-15 tracked_in P60 Wave C; runner flips PASS after Wave C ships"
      pattern: "tracked_in: P60 Wave C"
    - from: "All 3 amended catalog files + new docs-build.json"
      to: "Wave H unbiased verifier subagent"
      via: "row.expected.asserts is the contract Wave H grades against; locked in this commit BEFORE Waves B-G land code"
      pattern: "expected"
---

<objective>
Wave A — the **catalog-first commit**. Per `quality/PROTOCOL.md` § "Per-phase protocol" Step 3 and recurring criterion 1 from `.planning/REQUIREMENTS.md` § "Recurring success criteria across every phase (P56-P63)": every phase's FIRST commit MUST write the catalog rows that define this phase's end-state contract. This wave does NOT migrate any script, does NOT write any verifier code, does NOT touch the pre-push hook — it only declares "here is what GREEN means for P60" so the unbiased verifier subagent in Wave H reads a contract that existed BEFORE the implementation landed.

Four deliverables ship in this commit:

1. **`quality/catalogs/docs-build.json`** (NEW) — 4 docs-build rows. The 3 migrate targets for SIMPLIFY-08 (mkdocs-strict, mermaid-renders, link-resolution) plus the BADGE-01 verifier (badges-resolve). All rows status=NOT-VERIFIED; verifier paths point under `quality/gates/docs-build/` where Waves B + C ship the files. ALL 4 rows are pre-push cadence; badges-resolve also has weekly cadence (cheap HEAD).

2. **`quality/catalogs/code.json` (amended)** — append 2 rows for the existing pre-push cargo gates that SIMPLIFY-10 will fold into the runner: `code/cargo-fmt-check` + `code/cargo-clippy-warnings`. Both wear a SHORT-LIVED waiver (until 2026-05-15, tracked_in P60 Wave D) so the catalog-first commit does NOT break pre-push: the verifier wrappers ship in Wave D; until then the runner returns NOT-VERIFIED, which the waiver converts to WAIVED at runner-time. P58 Wave A established this exact pattern (see `quality/SURPRISES.md` 2026-04-27 P58 row "Wave A pre-push runner reported NOT-VERIFIED for new P1 pre-push row code/clippy-lint-loaded ... resolved by attaching a short-lived waiver"). Pre-push stays GREEN through the implementation window.

3. **`quality/catalogs/freshness-invariants.json` (amended)** — append 1 row: `structure/cred-hygiene` (the credential-prefix scan currently at `scripts/hooks/pre-push` lines 63-122; same short-lived-waiver pattern as the 2 code rows above). AND amend 1 existing row: `structure/badges-resolve` loses its 2026-07-25 BADGE-01/P60 waiver (since P60 ships the verifier this phase) and gains a SHORT-LIVED waiver until 2026-05-15 tracked_in P60 Wave C, so the row stays WAIVED through the implementation window and flips PASS in Wave G's broaden-and-deepen sweep after Wave C ships the file. The end-state of the row (verifier.script: `quality/gates/docs-build/badges-resolve.py`) is unchanged; only the waiver block migrates.

4. **`quality/gates/docs-build/README.md`** (NEW) — dimension home, <=50 lines. Verifier table (4 rows), conventions, pivot rules. Anchors the dimension scope so Wave H's verifier subagent can read the README to confirm what docs-build covers.

Why ship the README stub in Wave A? Because the catalog rows reference this directory; the unbiased verifier in Wave H reads the README to confirm dimension scope. Anchoring the dimension contract in Wave A means the docs-build dimension exists before any verifier code does.

**Why no script moves yet?** SIMPLIFY-08 is a path-move (rename + relocate). Path moves require git-mv to preserve history per the OP-5 reversibility rule (P59 Wave E SIMPLIFY-11 precedent). Doing the moves in Wave A would mix "lock the contract" with "move the implementation" in one commit, which makes review harder and breaks the Wave A invariant that NO verifier code ships in this commit. Wave B handles the 3 path moves with their git-mv history preservation; Wave C ships the new BADGE-01 verifier (no predecessor to move).

**Why the short-lived waivers?** Catalog-first means rows ship BEFORE verifiers. Without a waiver, the runner's verifier-not-found branch (`quality/runners/run.py:160-180`) returns NOT-VERIFIED, and `compute_exit_code` (`quality/runners/run.py:247-253`) treats NOT-VERIFIED on P0+P1 rows as FAIL — pre-push would break for the duration of the implementation window. The short-lived waiver pattern (P58 Wave A invented it; P59 Wave A used it for perf rows) keeps pre-push GREEN. The waivers expire 2026-05-15 (≤14d from this commit at 2026-04-27); after Wave D + Wave C land their verifiers (within the same P60 implementation cycle), the runner re-grades the rows to PASS and the waivers expire harmlessly.

Plus housekeeping: NO edits to `scripts/check-quality-catalogs.py` (or `check_quality_catalogs.py`) needed — P59 Wave A already extended its dimension allow-list to accept arbitrary new dimension values (per P59-01-PLAN.md Task 3). Verify with a self-test in Task 4.

Purpose: lock the contract. After this commit, downstream waves (B/C/D/E/F/G/H) cannot quietly drift the asserts. If Wave B's mkdocs-strict.sh does not satisfy the row's `expected.asserts`, Wave H refuses to grade GREEN.

Output: 4 files committed in a single commit titled `docs(p60): catalog-first commit -- DOCS-BUILD-01 + BADGE-01 + SIMPLIFY-08/09/10 contract + dim README`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/research/v0.12.0-naming-and-architecture.md
@.planning/research/v0.12.0-roadmap-and-rationale.md
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/catalogs/README.md
@quality/catalogs/freshness-invariants.json
@quality/catalogs/code.json
@quality/catalogs/release-assets.json
@quality/catalogs/docs-reproducible.json
@quality/catalogs/agent-ux.json
@quality/catalogs/perf-targets.json
@quality/catalogs/orphan-scripts.json
@quality/runners/run.py
@quality/runners/verdict.py
@quality/reports/verdicts/p59/VERDICT.md
@quality/reports/verdicts/p58/VERDICT.md
@quality/gates/release/README.md
@quality/gates/code/README.md
@quality/gates/structure/README.md
@quality/gates/docs-repro/README.md
@quality/gates/agent-ux/README.md
@quality/gates/perf/README.md
@scripts/check-docs-site.sh
@scripts/check-mermaid-renders.sh
@scripts/check_doc_links.py
@scripts/green-gauntlet.sh
@scripts/hooks/pre-push
@scripts/hooks/test-pre-push.sh
@README.md
@docs/index.md
@CLAUDE.md

<interfaces>
**Catalog row schema (verbatim from `quality/catalogs/README.md`; same shape as P58 release-assets + P59 docs-repro rows):**

```jsonc
{
  "id":            "docs-build/<slug>",
  "dimension":     "docs-build",
  "cadence":       "pre-push",
  "kind":          "mechanical",
  "sources":       ["..."],
  "command":       "...",
  "expected":      { "asserts": ["..."] },
  "verifier":      {
    "script":      "quality/gates/docs-build/<file>",
    "args":        [...],
    "timeout_s":   ...,
    "container":   null
  },
  "artifact":      "quality/reports/verifications/docs-build/<slug>.json",
  "status":        "NOT-VERIFIED",
  "last_verified": null,
  "freshness_ttl": null,
  "blast_radius":  "P0|P1|P2",
  "owner_hint":    "...",
  "waiver":        null OR short-lived
}
```

**Catalog file wrapper format** (per P58/P59 precedent):

```jsonc
{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "comment": "Quality Gates catalog -- docs-build dimension. Schema spec: quality/catalogs/README.md. Verifier impl: quality/gates/docs-build/. Reports: quality/reports/verifications/docs-build/.",
  "dimension": "docs-build",
  "rows": [ /* 4 rows */ ]
}
```

**The 4 docs-build.json rows in order:**

**Row 1 -- `docs-build/mkdocs-strict`:**
- dimension: `"docs-build"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `["scripts/check-docs-site.sh", "mkdocs.yml", "CLAUDE.md:Docs-site validation"]`
- command: `"bash quality/gates/docs-build/mkdocs-strict.sh"` (Wave B ships the file via git-mv from `scripts/check-docs-site.sh`)
- expected.asserts:
  - `"mkdocs build --strict exits 0 (catches broken cross-refs, missing nav entries, broken admonition syntax)"`
  - `"mkdocs build log does not contain 'Syntax error in text' (mermaid HTML-entity bug guard per POLISH-03)"`
  - `"rendered HTML site does not contain 'Syntax error in text' (mermaid leaked-error guard per POLISH-03 regression mode)"`
- verifier.script: `"quality/gates/docs-build/mkdocs-strict.sh"`, args: `[]`, timeout_s: `60`, container: `null`
- artifact: `"quality/reports/verifications/docs-build/mkdocs-strict.json"`
- status: `"NOT-VERIFIED"`, last_verified: `null`, freshness_ttl: `null`
- blast_radius: `"P1"`, owner_hint: `"if RED: mkdocs build failed --strict OR mermaid HTML-entity regression -- inspect mkdocs.yml + recent docs/ edits + the build log path under /tmp/check-docs-site*/build.log"`
- waiver: `null` (Wave B ships the file via git-mv; the runner finds it on the new path on first re-grade, so no waiver needed)

**Row 2 -- `docs-build/mermaid-renders`:**
- dimension: `"docs-build"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `["scripts/check-mermaid-renders.sh", ".planning/verifications/playwright/", "CLAUDE.md:Whole-nav-section playwright walks"]`
- command: `"bash quality/gates/docs-build/mermaid-renders.sh"` (Wave B git-mv from `scripts/check-mermaid-renders.sh`)
- expected.asserts:
  - `"every docs/**/*.md page that source-references a triple-backtick mermaid fence has a corresponding playwright artifact at .planning/verifications/playwright/<section>/<slug>.json"`
  - `"each artifact's svg_counts length equals mermaid_count (no missing diagrams)"`
  - `"each artifact's svg_counts contains no zeros (no empty-render diagrams)"`
  - `"each artifact's console_errors is empty (no browser-side rendering failures)"`
- verifier.script: `"quality/gates/docs-build/mermaid-renders.sh"`, args: `[]`, timeout_s: `30`, container: `null`
- artifact: `"quality/reports/verifications/docs-build/mermaid-renders.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P1"`, owner_hint: `"if RED: a docs page added a mermaid fence without a matching playwright artifact OR an artifact got stale -- refresh via the playwright MCP walk per CLAUDE.md 'Whole-nav-section playwright walks' section"`
- waiver: `null`

**Row 3 -- `docs-build/link-resolution`:**
- dimension: `"docs-build"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `["scripts/check_doc_links.py", "docs/"]`
- command: `"python3 quality/gates/docs-build/link-resolution.py"` (Wave B git-mv from `scripts/check_doc_links.py`)
- expected.asserts:
  - `"every relative Markdown link in docs/{index.md, concepts/*.md, tutorials/*.md, how-it-works/*.md, guides/*.md, reference/*.md, benchmarks/*.md} resolves to an existing file path"`
  - `"http(s):// and mailto: and pure-anchor links are skipped (anchor validation is mkdocs --strict's job per the existing script docstring)"`
- verifier.script: `"quality/gates/docs-build/link-resolution.py"`, args: `[]`, timeout_s: `30`, container: `null`
- artifact: `"quality/reports/verifications/docs-build/link-resolution.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P2"` (link rot is real but not user-blocking on the curl-installer scale), owner_hint: `"if RED: a docs page references a deleted file OR a moved-without-redirect file -- run python3 quality/gates/docs-build/link-resolution.py locally for the BROKEN-list"`
- waiver: `null`

**Row 4 -- `docs-build/badges-resolve`:**
- dimension: `"docs-build"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `["README.md:badges (lines 10-16; 7 badges)", "docs/index.md:badges (lines 7-8; 2 badges)"]`
- command: `"python3 quality/gates/docs-build/badges-resolve.py"` (Wave C ships the file new; no predecessor)
- expected.asserts:
  - `"every badge URL extracted from README.md badge block (CI, Quality (weekly), Docs, codecov, License MIT, Rust stable, reposix-cli on crates.io) HEADs to HTTP 200"`
  - `"every badge URL extracted from docs/index.md badge block (CI, Quality (weekly)) HEADs to HTTP 200"`
  - `"every badge URL's response Content-Type contains 'image' (svg, png, etc.)"`
  - `"the new QG-09 endpoint badge URL https://reubenjohn.github.io/reposix/badge.json HEADs to HTTP 200 with Content-Type containing 'json' (added in P60 Wave F; pre-Wave-F this assert is documented and the badge URL ships in Wave F's README + docs/index.md edit)"`
- verifier.script: `"quality/gates/docs-build/badges-resolve.py"`, args: `[]`, timeout_s: `30`, container: `null`
- artifact: `"quality/reports/verifications/docs-build/badges-resolve.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P2"` (badge drift is cosmetic until codecov goes 404 mid-PR review), owner_hint: `"if RED: a badge URL is 404 or returns non-image content-type -- fix the badge URL in README.md or docs/index.md OR replace the broken badge with a current shields.io equivalent. Common: shields.io drift, codecov project rename, badge-URL typos."`
- waiver: `null` (Wave C ships the verifier within this same phase; until Wave C lands, the duplicate row at `freshness-invariants.json:structure/badges-resolve` carries the short-lived waiver -- see "amended row" below)

**The 2 NEW code.json rows (appended after the existing 4 rows):**

**Row 5 -- `code/cargo-fmt-check`:**
- dimension: `"code"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `["scripts/hooks/pre-push:215-219", ".github/workflows/ci.yml:rustfmt"]`
- command: `"cargo fmt --all -- --check"`
- expected.asserts:
  - `"cargo fmt --all -- --check exits 0 (no formatting drift in any workspace crate)"`
- verifier.script: `"quality/gates/code/cargo-fmt-check.sh"`, args: `[]`, timeout_s: `60`, container: `null` (cargo fmt is fast on warm cache; cold cache rare in pre-push)
- artifact: `"quality/reports/verifications/code/cargo-fmt-check.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P1"`, owner_hint: `"if RED: cargo fmt regressed; run cargo fmt --all locally and commit the result"`
- waiver:
  ```
  {
    "until": "2026-05-15T00:00:00Z",
    "reason": "P60 Wave A catalog-first; verifier wrapper ships in P60 Wave D (SIMPLIFY-10 pre-push hook one-liner). Until Wave D, the runner returns NOT-VERIFIED on missing verifier; this short-lived waiver keeps pre-push GREEN through the implementation window. Pattern from quality/SURPRISES.md 2026-04-27 P58 row 2 (clippy-lint-loaded P58 Wave A waiver).",
    "dimension_owner": "v0.12.0 P60 docs-build dimension owner",
    "tracked_in": "P60 Wave D (60-04)"
  }
  ```

**Row 6 -- `code/cargo-clippy-warnings`:**
- Same shape as Row 5. Sources: `["scripts/hooks/pre-push:222-227", ".github/workflows/ci.yml:clippy"]`. Command: `"cargo clippy --workspace --all-targets -- -D warnings"`. expected.asserts: `["cargo clippy --workspace --all-targets -- -D warnings exits 0"]`. verifier.script: `"quality/gates/code/cargo-clippy-warnings.sh"`, timeout_s: `300` (clippy can be 2-5min cold; warm is fast). owner_hint: `"if RED: a new clippy violation; cargo clippy --workspace --all-targets --fix to auto-fix where possible. **Pivot rule per the prompt:** if cargo clippy in pre-push exceeds 60s on warm cache, P60 Wave E may move it OUTSIDE the runner; the row stays catalogued but the pre-push hook chains it externally. Document in SURPRISES.md."` Same waiver block (until 2026-05-15, tracked_in P60 Wave D).

**The 1 NEW freshness-invariants.json row (appended after the existing 9 rows):**

**Row 10 -- `structure/cred-hygiene`:**
- dimension: `"structure"`, cadence: `"pre-push"`, kind: `"mechanical"`
- sources: `["scripts/hooks/pre-push:41-122", "CLAUDE.md:Threat model"]`
- command: `null` (the verifier is the entry point; it scans the outgoing ref-range like the existing hook body does)
- expected.asserts:
  - `"no committed file on the outgoing ref-range matches the credential-prefix regex set: ATATT3[A-Za-z0-9_+/=-]{20,} (Atlassian); Bearer ATATT3 ; ghp_[A-Za-z0-9]{20,} (GitHub classic PAT); github_pat_[A-Za-z0-9_]{20,} (GitHub fine-grained PAT)"`
  - `"the verifier respects the same EXCLUDE_DIRS as the existing hook (.git, target, node_modules, scripts/hooks)"`
  - `"the verifier exits 1 on any hit so pre-push BLOCKS the push (matches existing hook semantics; non-negotiable per CLAUDE.md threat model)"`
- verifier.script: `"quality/gates/structure/cred-hygiene.sh"`, args: `[]`, timeout_s: `15`, container: `null`
- artifact: `"quality/reports/verifications/structure/cred-hygiene.json"`
- status: `"NOT-VERIFIED"`, blast_radius: `"P0"` (credential leak is the highest-blast item in the entire catalog), owner_hint: `"if RED: a real token shape was committed -- ROTATE THE TOKEN at the issuing platform BEFORE doing anything else (Atlassian: https://id.atlassian.com/manage-profile/security/api-tokens; GitHub: https://github.com/settings/tokens)"`
- waiver: same short-lived block (until 2026-05-15, tracked_in P60 Wave D). The existing hook body keeps running until Wave E rewrites it, so the catalog row's waiver does not weaken security.

**The amended freshness-invariants.json row (`structure/badges-resolve` waiver swap):**

The existing row (lines 233-268 in `quality/catalogs/freshness-invariants.json` at HEAD~ from P57 Wave A; verify in Task 1 against current state) has:
```
"verifier.script": "quality/gates/docs-build/badges-resolve.py"  // already correct from P57
"waiver": { "until": "2026-07-25T00:00:00Z", "tracked_in": "BADGE-01 / P60", ... }
```

Replace the waiver block with the short-lived form:
```
"waiver": {
  "until": "2026-05-15T00:00:00Z",
  "reason": "P60 Wave A catalog-first contract amend; verifier ships in P60 Wave C. Until Wave C, the runner's verifier-not-found branch returns NOT-VERIFIED; this short-lived waiver keeps pre-push GREEN through the implementation window. After Wave C, the runner flips this row to PASS and the waiver expires harmlessly.",
  "dimension_owner": "v0.12.0 P60 docs-build dimension owner",
  "tracked_in": "P60 Wave C (60-03)"
}
```

The `verifier.script` field stays at `quality/gates/docs-build/badges-resolve.py` unchanged (P57 already pre-anchored it). Wave C creates that file. The verifier file path is intentionally shared between the `freshness-invariants.json:structure/badges-resolve` row (anchored at P57 for back-compat) and the `docs-build.json:docs-build/badges-resolve` row (new in this Wave A); both rows point at the same verifier script. The runner runs the verifier ONCE per row; the artifact paths differ (`structure/badges-resolve.json` vs `docs-build/badges-resolve.json`); both rows get scored. The duplicate is intentional and self-cleaning: P63 SIMPLIFY-12 audit may consolidate the rows, OR they may stay split (structure/badges-resolve guards the BADGE-01 contract from P57's perspective; docs-build/badges-resolve is the dimension-native row). Either decision is post-v0.12.0.

**`quality/gates/docs-build/README.md` shape (model on `quality/gates/docs-repro/README.md`):**
- H1: `# quality/gates/docs-build/`
- one-line description: "Verifiers backing `quality/catalogs/docs-build.json` (4 rows, docs-build dimension). Plus the back-compat shared verifier `badges-resolve.py` co-anchored from `freshness-invariants.json:structure/badges-resolve`."
- verifier table (4 rows):
  | Verifier | Catalog rows it backs | Cadence |
  |---|---|---|
  | `mkdocs-strict.sh` | `docs-build/mkdocs-strict` | pre-push |
  | `mermaid-renders.sh` | `docs-build/mermaid-renders` | pre-push |
  | `link-resolution.py` | `docs-build/link-resolution` | pre-push |
  | `badges-resolve.py` | `docs-build/badges-resolve` + `structure/badges-resolve` | pre-push + weekly |
- conventions section: stdlib-only Python (argparse, json, pathlib, subprocess, urllib.request, datetime, sys, hashlib, re, os, html.parser); bash POSIX-where-possible; <=250 lines per verifier; <=50 lines for THIS README; <=30 lines for the migrated pre-push hook body per P60 Wave E SIMPLIFY-10.
- pivot rules section (lift verbatim from prompt): path-move shim rule (if any external invocation expects the old `scripts/` path, leave a thin exec-replace shim per OP-5 reversibility); chicken-and-egg badge endpoint URL (Wave F seeds `docs/badge.json` directly during build so the URL works on first deploy); >5 broken links found by Wave G's link-resolution → fix the worst, file backlog item for the rest, do NOT block phase close (per the prompt's pivot rule list).
- cross-references: `quality/PROTOCOL.md`, `quality/catalogs/docs-build.json`, `quality/catalogs/freshness-invariants.json`, CLAUDE.md "Docs-site validation" section.

**Existing precedents to mirror:**

- `quality/catalogs/release-assets.json` (P58 Wave A; 15 rows; structural template for the wrapper shape).
- `quality/catalogs/code.json` (P58 Wave A; 4 rows + 2 WAIVED rows; format for the short-lived waiver block; the cargo-test-pass + cargo-fmt-clean WAIVED rows already wear the same waiver-block shape proposed here).
- `quality/catalogs/docs-reproducible.json` (P59 Wave A; 9 rows; structural reference for a dimension catalog with a mix of mechanical + container + manual kinds).
- `quality/catalogs/freshness-invariants.json:structure/badges-resolve` (P57 Wave A; the row already exists with the correct verifier path from P57 — this Wave A only swaps the waiver block, does NOT add the row from scratch).

**Self-test for Wave A (Task 4 verify):**
- `python3 quality/runners/run.py --cadence pre-push` MUST exit 0.
- The runner output MUST show:
  - `[WAIVED]` for the 4 newly-added rows that got short-lived waivers (cargo-fmt-check, cargo-clippy-warnings, cred-hygiene, structure/badges-resolve).
  - `[NOT-VERIFIED]` for the 4 docs-build.json rows (mkdocs-strict, mermaid-renders, link-resolution, docs-build/badges-resolve) BUT they must NOT block exit because the runner does not include them in the pre-push exit-code calc unless the row is P0+P1 — verify by checking the per-row blast_radius (P1 for first 3, P2 for badges-resolve).
- Wait — that's a problem. Three of the new docs-build rows are P1 with cadence pre-push, status NOT-VERIFIED → `compute_exit_code` flags them RED. The pre-push hook would break.
- **Mitigation:** the docs-build.json rows are P1 with `verifier.script` paths that DON'T exist yet — the runner returns NOT-VERIFIED, which `compute_exit_code` treats as exit=1 for P0+P1.
- **Solution:** mkdocs-strict + mermaid-renders + link-resolution rows ALSO need short-lived waivers until 2026-05-15 tracked_in P60 Wave B. Same pattern as cargo-fmt-check / cargo-clippy-warnings. The waivers expire harmlessly after Wave B git-mv lands the verifiers and the runner flips them to PASS.
- **Update spec:** all 3 mkdocs-strict / mermaid-renders / link-resolution rows MUST include the short-lived waiver block (until 2026-05-15, tracked_in P60 Wave B). The badges-resolve row in docs-build.json gets the short-lived waiver tracked_in P60 Wave C. The duplicated structure/badges-resolve row in freshness-invariants.json keeps its already-spec'd short-lived waiver tracked_in P60 Wave C. Total waivers added by Wave A: 4 docs-build rows + 2 code rows + 1 cred-hygiene = 7 rows wearing short-lived waivers, plus 1 amend (structure/badges-resolve from 2026-07-25 to 2026-05-15). 8 total rows in waiver state at Wave A close; all expire harmlessly within 18 days as Waves B + C + D land.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the new docs-build.json catalog + amend code.json + amend freshness-invariants.json</name>
  <files>quality/catalogs/docs-build.json, quality/catalogs/code.json, quality/catalogs/freshness-invariants.json</files>
  <action>(1) Create NEW `quality/catalogs/docs-build.json` with the 4 rows specified in the `<interfaces>` block. Wrapper format mirrors `quality/catalogs/release-assets.json`. ALL 4 rows status=NOT-VERIFIED with the short-lived waiver block (until 2026-05-15, tracked_in P60 Wave B for mkdocs-strict + mermaid-renders + link-resolution; tracked_in P60 Wave C for badges-resolve). (2) Append 2 rows to `quality/catalogs/code.json` after the existing 4: `code/cargo-fmt-check` + `code/cargo-clippy-warnings` per the spec (both with the short-lived waiver block tracked_in P60 Wave D). (3) Append 1 row + amend 1 row in `quality/catalogs/freshness-invariants.json`: NEW row `structure/cred-hygiene` (P0 blast_radius; short-lived waiver tracked_in P60 Wave D); AMEND existing `structure/badges-resolve` row's waiver block to swap the 2026-07-25 BADGE-01/P60 waiver for the short-lived 2026-05-15 tracked_in P60 Wave C waiver. The verifier.script field on the amended row stays unchanged at `quality/gates/docs-build/badges-resolve.py` (P57 pre-anchored it; do NOT edit). Wave A does NOT touch any other field on existing rows. Banned-word check: do NOT use `replace`; use `migrate to`, `swap`, `rewrite as`, `alongside` per CLAUDE.md banned-words rule.</action>
  <verify>
    <automated>python3 -c "import json; d=json.loads(open('quality/catalogs/docs-build.json').read()); assert d['dimension']=='docs-build'; assert len(d['rows'])==4; ids={r['id'] for r in d['rows']}; assert ids=={'docs-build/mkdocs-strict','docs-build/mermaid-renders','docs-build/link-resolution','docs-build/badges-resolve'}; assert all(r['waiver'] is not None and r['waiver']['until'].startswith('2026-05-15') for r in d['rows']), 'every row must have short-lived waiver until 2026-05-15'; print('OK docs-build.json')" && python3 -c "import json; d=json.loads(open('quality/catalogs/code.json').read()); assert len(d['rows'])==6; ids={r['id'] for r in d['rows']}; assert 'code/cargo-fmt-check' in ids and 'code/cargo-clippy-warnings' in ids; print('OK code.json')" && python3 -c "import json; d=json.loads(open('quality/catalogs/freshness-invariants.json').read()); ids={r['id'] for r in d['rows']}; assert 'structure/cred-hygiene' in ids, 'cred-hygiene missing'; br=[r for r in d['rows'] if r['id']=='structure/badges-resolve'][0]; assert br['waiver']['until'].startswith('2026-05-15'), f'badges-resolve waiver swap missed: {br[\"waiver\"][\"until\"]}'; assert br['waiver']['tracked_in'].startswith('P60 Wave C'), f'badges-resolve tracked_in wrong: {br[\"waiver\"][\"tracked_in\"]}'; print('OK freshness-invariants.json')" && grep -lE '\breplace\b' quality/catalogs/docs-build.json quality/catalogs/code.json quality/catalogs/freshness-invariants.json && exit 1 || echo "OK no banned word"</automated>
  </verify>
  <done>3 catalog files written/amended; 4 docs-build rows + 2 code rows + 1 freshness-invariants row added; 1 freshness-invariants row's waiver amended; ALL new rows wear short-lived waivers (until 2026-05-15) tracked_in the correct wave; no banned word `replace`.</done>
</task>

<task type="auto">
  <name>Task 2: Write the docs-build dimension README</name>
  <files>quality/gates/docs-build/README.md</files>
  <action>Create `quality/gates/docs-build/README.md` following the `<interfaces>` shape spec (model on `quality/gates/docs-repro/README.md`). <=50 lines. Sections: H1 + one-line description; verifier table (4 rows); conventions section (stdlib-only Python module list per the prompt; bash POSIX where possible; verifier line cap <=250; this README cap <=50; pre-push hook body cap <=30 per P60 Wave E SIMPLIFY-10); pivot rules section (path-move shim rule; chicken-and-egg badge URL rule; >5 broken links rule per the prompt). Cross-references to `quality/PROTOCOL.md` (anti-bloat: do NOT duplicate runtime detail), `quality/catalogs/docs-build.json`, `quality/catalogs/freshness-invariants.json`, CLAUDE.md "Docs-site validation" section. Banned-word check: do NOT use `replace`; use `migrate to`, `rewrite as`, `alongside`.</action>
  <verify>
    <automated>test -s quality/gates/docs-build/README.md && lines=$(wc -l < quality/gates/docs-build/README.md) && test "$lines" -le 50 || { echo "TOO LONG ($lines lines): quality/gates/docs-build/README.md"; exit 1; }; grep -E '\breplace\b' quality/gates/docs-build/README.md && { echo BANNED; exit 1; } || echo OK; grep -F 'docs-build/mkdocs-strict' quality/gates/docs-build/README.md && grep -F 'docs-build/badges-resolve' quality/gates/docs-build/README.md && echo "OK README has 4 verifier rows referenced"</automated>
  </verify>
  <done>README exists; <=50 lines; no banned word; verifier table names all 4 rows; conventions + pivot rules sections present; cross-references quality/PROTOCOL.md without duplicating runtime detail.</done>
</task>

<task type="auto">
  <name>Task 3: Verify pre-push runner GREEN with the new + amended catalogs</name>
  <files>(no edits — verification-only task)</files>
  <action>Run `python3 quality/runners/run.py --cadence pre-push` and confirm exit 0. The runner MUST grade the 7 newly-added rows with short-lived waivers as WAIVED (not NOT-VERIFIED), and the amended `structure/badges-resolve` row should also be WAIVED (already was; only the waiver block changed). NO row in scope should grade FAIL or PARTIAL. If the runner exits 1 OR any row grades NOT-VERIFIED in scope, investigate: did the waiver block parse? Is `parse_rfc3339` happy with the timestamp format (e.g. trailing Z)? Per `quality/runners/run.py:108-118`, the `waiver_active` function uses `parse_rfc3339` which strips a trailing Z. Confirm the waiver block format mirrors `quality/catalogs/code.json:cargo-test-pass` exactly (which P58 verified working). Anti-bloat: NO new code in this task — diagnostic only. If broken, fix in Task 1 (the catalog rows). Do NOT add a checker hack to the runner.</action>
  <verify>
    <automated>python3 quality/runners/run.py --cadence pre-push 2>&1 | tee /tmp/p60-wave-a-runner.log && tail -3 /tmp/p60-wave-a-runner.log | grep -F 'exit=0' && echo "OK runner GREEN at Wave A close"</automated>
  </verify>
  <done>`quality/runners/run.py --cadence pre-push` exits 0 on the new + amended catalog state; 7 rows grade WAIVED; no row grades FAIL/PARTIAL/NOT-VERIFIED in scope.</done>
</task>

<task type="auto">
  <name>Task 4: Atomic commit + sanity sweep</name>
  <files>(commit-only; no new files written here)</files>
  <action>Commit all 4 files in a single atomic commit. Title: `docs(p60): catalog-first commit -- DOCS-BUILD-01 + BADGE-01 + SIMPLIFY-08/09/10 contract + dim README`. Body cites: 4 docs-build rows + 2 code rows + 1 freshness-invariants row added; 1 freshness-invariants waiver swapped; the dimension README anchored. References this plan file. Run the full pre-push runner sweep one more time to confirm no regression. NO `--no-verify`; the pre-push hook MUST stay GREEN through the commit.</action>
  <verify>
    <automated>git log -1 --format='%s' | grep -F 'docs(p60)' && git diff HEAD~1 --name-only | sort | grep -E '^(quality/catalogs/(docs-build|code|freshness-invariants)\.json|quality/gates/docs-build/README\.md)$' | wc -l | grep -q '^4$' && python3 quality/runners/run.py --cadence pre-push && echo "OK Wave A clean"</automated>
  </verify>
  <done>Single commit lands all 4 files; pre-push runner exit 0; commit subject names the catalog-first contract; no other files modified.</done>
</task>

</tasks>

<verification>
After Wave A close:
- `python3 quality/runners/run.py --cadence pre-push` exits 0.
- `python3 quality/runners/run.py --cadence weekly` shows the new docs-build/badges-resolve row in scope (cadence: pre-push + weekly) with status WAIVED (short-lived waiver).
- `git diff HEAD~1 --name-only` lists exactly the 4 files.
- The Wave H verifier subagent can read every catalog row's `expected.asserts` and grade against the artifacts produced by Waves B-G.
- `bash scripts/hooks/pre-push` exit 0 (the existing hook chain still runs; SIMPLIFY-10 in Wave E rewrites the body, but the existing fmt + clippy + cred-hygiene + mkdocs + mermaid checks all still fire on outgoing ranges).
</verification>

<success_criteria>
1. **Catalog-first per recurring criterion 1:** 4 catalog files (1 new + 3 amended) + 1 dimension README land in a single atomic commit BEFORE any verifier code (Waves B-G).
2. **CLAUDE.md update is NOT in scope for Wave A** — it lands in Wave H per QG-07 (the same-PR rule applies at phase close, not per-wave).
3. **Verifier-subagent dispatch is NOT in scope for Wave A** — Wave H does it.
4. **SIMPLIFY absorption is contracted in Wave A** (the catalog rows reference the migration targets); the actual file moves land in Wave B (mkdocs-strict + mermaid-renders + link-resolution via git-mv) and Wave D (cargo-fmt-check + cargo-clippy-warnings + cred-hygiene as new wrappers; SIMPLIFY-10 absorption); Wave E does the pre-push hook body rewrite.
5. **RED-row fix is NOT in scope for Wave A** — all 7 newly-added rows are pre-emptively WAIVED via short-lived waivers so they cannot grade RED; the amended structure/badges-resolve row's waiver swap leaves it WAIVED throughout.
</success_criteria>

<pivot_rules>
- **If `quality/runners/run.py:waiver_active()` does not accept the short-lived waiver block** (e.g., RFC3339 parse error on the trailing Z): use the literal string `"2026-05-15T00:00:00Z"` exactly as `quality/catalogs/code.json:cargo-test-pass` uses (`"2026-07-26T00:00:00Z"` — same shape). The P58 Wave A verifier already proved this format works (per `quality/SURPRISES.md` 2026-04-27 P58 row 2). Do NOT modify the runner.
- **If `compute_exit_code` flags a NEW row as RED** (e.g. NOT-VERIFIED on a P1 pre-push row): the waiver block was probably mis-formatted. Inspect with `python3 -c "import json; d=json.loads(open('quality/catalogs/docs-build.json').read()); from datetime import datetime, timezone; from quality.runners.run import waiver_active, parse_rfc3339; r=d['rows'][0]; print(waiver_active(r, datetime.now(timezone.utc)))"`. If the waiver does not parse, the JSON shape is wrong — fix it in Task 1.
- **If `scripts/check-quality-catalogs.py` (or `check_quality_catalogs.py`) hard-errors on the new docs-build dimension value**: P59 Wave A already extended the allow-list per `.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-01-SUMMARY.md` to include `docs-repro`, `agent-ux`, `perf`. If `docs-build` is still rejected, append it (single-line patch; same approach as P59 Wave A). Document in SURPRISES.md if needed.
- **If duplication of `structure/badges-resolve` and `docs-build/badges-resolve` (both pointing at the same verifier) feels structurally hostile**: keep both rows. The duplicate is intentional per the spec; P63 SIMPLIFY-12 audit may decide to consolidate them post-v0.12.0. Do NOT consolidate in P60 — that's scope creep and violates the catalog-first commit principle (Wave A's commit is contract; consolidation is implementation).
- **Em-dash idempotency:** per `quality/SURPRISES.md` 2026-04-27 P57 row 1, do NOT use unicode em-dashes inside JSON strings. Use literal ASCII hyphens (`-` or `--`) in waiver reasons + comments.

Reference: `quality/SURPRISES.md` (16 entries: 3 P57 + 7 P58 + 6 P59); `.planning/phases/59-docs-repro-dimension-agent-ux-thin-home-perf-relocate/59-01-PLAN.md` (Wave A precedent for this exact contract); `.planning/phases/58-release-dimension-gates-code-absorption/58-01-PLAN.md` (Wave A precedent for short-lived waivers).
</pivot_rules>

<risk_table>
| Risk | Likelihood | Mitigation |
|---|---|---|
| Catalog row drift between Wave A spec and Waves B/C/D implementation | medium | Wave A locks `expected.asserts` verbatim from the prompt; Waves B-D read this plan file at start; Wave H's unbiased verifier cross-checks against the catalog |
| Short-lived waiver block format mistake breaks `compute_exit_code` (NEW P1 row grades NOT-VERIFIED in pre-push) | medium-high | Task 3 self-tests with `--cadence pre-push`; pivot rule names the exact precedent (P58 cargo-test-pass) to copy verbatim; manual inspection if `waiver_active` returns False |
| `scripts/check-quality-catalogs.py` rejects new `docs-build` dimension value | low (P59 extended allow-list) | If hard-rejected: 1-line patch per the pivot rule; document in SURPRISES.md |
| The duplicate `structure/badges-resolve` + `docs-build/badges-resolve` rows confuse the runner | low | Both rows have distinct artifacts (`structure/...json` vs `docs-build/...json`); the runner runs the verifier twice (once per row); the verifier emits distinct artifact paths per row by reading args; P57 already pre-anchored the verifier path so this duplication is the intended design — clarified in the docs-build README in Task 2 |
| Em-dash idempotency regression (P57 Wave B SURPRISES row 1) | low | Mitigation: literal ASCII hyphens in all waiver `reason` text + comments; Task 1 verify includes `python3 -c "json.loads(...)"` round-trip |
| The pre-push hook body still chains old scripts and the runner now reports overlapping checks | medium | Acceptable through Wave A: parallel migration window per SIMPLIFY-10 (D4 in `.planning/research/v0.12.0-decisions-log.md`); Wave E does the hard-cut. Wave A must not break either side |
| Adding a P0 cred-hygiene catalog row before Wave D ships the verifier weakens security if waiver mis-routes the check | very low | The OLD pre-push hook body (lines 90-132) keeps running until Wave E rewrites it. The catalog row's WAIVED status only affects how the runner GRADES; the hook still EXECUTES the credential scan. P0 status is a defense-in-depth signal, not the actual scan |
| BADGE-01 catalog row with the QG-09 endpoint URL assert lands BEFORE the URL exists (chicken-and-egg) | medium | Wave F's badges-resolve verifier needs to handle this gracefully: pre-Wave-F runs would fail on the QG-09 URL HEAD. Resolution per the prompt's pivot rule: the assert is documented in Wave A but the verifier in Wave C MUST allow that specific URL to be NOT-YET-PRESENT (or skipped) until Wave F lands. Document the temporal coupling in the docs-build README + the verifier docstring |
</risk_table>

<output>
After completion, create `.planning/phases/60-docs-build-migration/60-01-SUMMARY.md` capturing:
- 4 catalog files committed/amended with row counts (4 new + 2 new + 1 new + 1 amended).
- 1 dimension README committed (with line count confirming <=50).
- Whether `scripts/check-quality-catalogs.py` rejected `docs-build` and was patched.
- Any SURPRISES.md entry appended (if pivot rules fired).
- Pre-push runner output after Wave A close (must show 7 short-lived WAIVED rows + the amended badges-resolve WAIVED row; runner exits 0).
</output>
