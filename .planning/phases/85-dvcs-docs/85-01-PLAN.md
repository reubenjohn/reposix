---
phase: 85
plan: 01
type: docs
autonomous: true
wave: 1
depends_on: [P79, P80, P81, P82, P83, P84]
requirements: [DVCS-DOCS-01, DVCS-DOCS-02, DVCS-DOCS-03, DVCS-DOCS-04]
---

# Phase 85, Plan 01 — DVCS docs

## Objective

Make v0.13.0 legible to a cold reader. Three new doc surfaces ship; the troubleshooting matrix gains DVCS entries; a subjective rubric row tracks the cold-reader pass for owner verification.

## Context

- @.planning/ROADMAP.md § "Phase 85" — phase goal + 7 success criteria.
- @.planning/REQUIREMENTS.md § "DVCS docs" — DVCS-DOCS-01..04.
- @.planning/research/v0.13.0-dvcs/vision-and-mental-model.md § "Mental model" — three roles + diagram source.
- @.planning/research/v0.13.0-dvcs/architecture-sketch.md — full design.
- @.planning/research/v0.13.0-dvcs/decisions.md § Q2.2 — verbatim doc-clarity contract: *"refs/mirrors/<sot-host>-synced-at is the timestamp the mirror last caught up to <sot-host> — it is NOT a 'current SoT state' marker."*
- @docs/concepts/mental-model-in-60-seconds.md — the 60s explainer the cold reader has read.
- @docs/guides/troubleshooting.md — the matrix being extended.
- @docs/guides/dvcs-mirror-setup-template.yml — P84 workflow template, referenced from setup guide.
- @CLAUDE.md § "Cold-reader pass on user-facing surfaces" — subjective rubric protocol.
- Banned-words rules at `scripts/banned-words-lint.sh` (Layer 1/Layer 2 forbid `FUSE`, `kernel`, `partial-clone`, `promisor`, `stateless-connect`, `fast-import`, `protocol-v2`, `daemon`).

## Tasks

### Task 1 — Concept doc: `docs/concepts/dvcs-topology.md`

Type: auto. Behavior:

- Three roles (SoT-holder, mirror-only consumer, round-tripper) explained.
- Mermaid diagram showing the three roles + sync flows (bus push, webhook sync, vanilla clone).
- Mirror-lag refs explained — verbatim Q2.2 phrase: *"refs/mirrors/<sot-host>-synced-at is the timestamp the mirror last caught up to <sot-host> — it is NOT a 'current SoT state' marker."*
- When-to-choose-which-pattern guidance (three subsections, one per pattern).
- Out-of-scope subsection (bidirectional bus, multi-SoT, long-running sync process, atomic 2PC).
- See-also cross-links to mental-model, dvcs-mirror-setup, troubleshooting, how-it-works/git-layer.

Verification: file exists; banned-words clean; mermaid diagram renders to SVG (playwright capture).
Done criteria: `grep -qi 'mirror last caught up'` AND `grep -qi 'NOT a' AND 'current SoT state'`.

### Task 2 — Guide: `docs/guides/dvcs-mirror-setup.md`

Type: auto. Behavior:

- Owner walk-through (5 numbered steps): create mirror repo → drop workflow → configure secrets/variables → smoke-test manual run → configure Confluence webhook.
- Backends-without-webhooks fallback (Q4.2): cron-only mode by removing `repository_dispatch:` from `on:`.
- Cleanup procedure (5 numbered steps; tear-down).
- Quick-fix table at end + cross-link to troubleshooting matrix.
- Cross-link to `dvcs-mirror-setup-template.yml` (P84 template).

Verification: file exists; presence of `gh repo create`, `gh secret set`, `gh workflow disable` commands; `Step 1` ... `Step 5` headings; `Backends without webhooks` and `Cleanup` sections present.

### Task 3 — Append "DVCS push/pull issues" section to `docs/guides/troubleshooting.md`

Type: auto. Behavior:

- Bus-remote `fetch first` rejection (cite mirror-lag refs; walk through `reposix sync --reconcile` + `git pull --rebase`).
- Attach reconciliation warnings (table covering the 5 P79 reconciliation cases: match / no-id / backend-deleted / duplicate-id / mirror-lag; resolution per row).
- Webhook race conditions (cite `--force-with-lease` semantics + bus-vs-webhook race).
- Cache-desync recovery via `reposix sync --reconcile` (signals to suspect; recovery commands).
- See-also block updated with cross-links to dvcs-topology + dvcs-mirror-setup.

Verification: section anchor `## DVCS push/pull issues` exists; 4 entry headings present; 4 attach reconciliation cases present; `--force-with-lease` cited.

### Task 4 — Wire new pages into `mkdocs.yml` nav

Type: auto. Behavior:

- Add `concepts/dvcs-topology.md` under Concepts.
- Add `guides/dvcs-mirror-setup.md` under Guides.

Verification: `bash scripts/check-docs-site.sh` GREEN (mkdocs --strict).

### Task 5 — Update `CLAUDE.md` § "Quick links" with the new doc surfaces

Type: auto. Behavior:

- Add three bullet points referencing the new doc surfaces (topology, mirror-setup, troubleshooting DVCS section).

Verification: `git diff CLAUDE.md` shows the three new bullets.

### Task 6 — Mint catalog rows + verifier shell scripts

Type: auto. Behavior:

- Three docs-alignment rows via `target/release/reposix-quality doc-alignment bind` (presence-check verifiers; body-hash drift fires STALE_DOCS_DRIFT):
  - `docs-alignment/dvcs-topology-three-roles-bound` → `quality/gates/docs-alignment/dvcs-topology-three-roles.sh`
  - `docs-alignment/dvcs-mirror-setup-walkthrough-bound` → `quality/gates/docs-alignment/dvcs-mirror-setup-walkthrough.sh`
  - `docs-alignment/dvcs-troubleshooting-matrix-bound` → `quality/gates/docs-alignment/dvcs-troubleshooting-matrix.sh`
- One subjective rubric row appended to `quality/catalogs/subjective-rubrics.json`:
  - `subjective/dvcs-cold-reader` with `status: NOT_VERIFIED`, `freshness_ttl: 30d`, rubric criteria inline. Owner runs `/reposix-quality-review --rubric dvcs-cold-reader` post-phase to flip to PASS.

Verification: each verifier passes when invoked directly; walker exits 0; JSON parses.

### Task 7 — Capture playwright artifact for the new mermaid diagram

Type: auto. Behavior:

- Start mkdocs serve on 127.0.0.1:8766.
- Use a one-shot playwright (chromium headless) capture to load the dvcs-topology page and assert mermaid_count=1, svg_counts=[1], console_errors=[].
- Write artifact to `.planning/verifications/playwright/concepts/dvcs-topology.json` per the schema in `quality/gates/docs-build/mermaid-renders.sh`.

Verification: `bash scripts/check-mermaid-renders.sh` GREEN.

### Task 8 — Pre-push hygiene + push

Type: auto. Behavior:

- `bash quality/gates/structure/banned-words.sh` (--all) GREEN.
- `bash scripts/check-docs-site.sh` GREEN.
- `bash scripts/check-mermaid-renders.sh` GREEN.
- `bash quality/gates/docs-alignment/walk.sh` GREEN.
- 1-3 atomic commits (suggested: docs + catalog + close).
- `git push origin main` BEFORE the verifier dispatch.

## Success criteria

1. Three doc files exist with substantive content (>1k words each for topology + setup; >150 lines for the troubleshooting append).
2. Banned-words clean; mkdocs-strict clean; mermaid renders to SVG.
3. Three docs-alignment rows BOUND with passing verifiers.
4. One subjective rubric row minted with status NOT_VERIFIED and full rubric criteria inline.
5. CLAUDE.md updated in same PR.
6. mkdocs nav slots the new pages into Concepts + Guides per Diátaxis.
7. Phase pushed to origin/main BEFORE verifier subagent dispatch.

## Output

- `docs/concepts/dvcs-topology.md` (~150 lines, ~1.4k words).
- `docs/guides/dvcs-mirror-setup.md` (~155 lines, ~1.5k words).
- Append to `docs/guides/troubleshooting.md` (+~120 lines).
- `quality/gates/docs-alignment/dvcs-topology-three-roles.sh`
- `quality/gates/docs-alignment/dvcs-mirror-setup-walkthrough.sh`
- `quality/gates/docs-alignment/dvcs-troubleshooting-matrix.sh`
- `.planning/verifications/playwright/concepts/dvcs-topology.json`
- `mkdocs.yml`, `CLAUDE.md`, `quality/catalogs/doc-alignment.json`, `quality/catalogs/subjective-rubrics.json` updates.
