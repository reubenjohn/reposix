---
phase: 116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/concepts/dvcs-topology.md
  - CLAUDE.md
  - quality/catalogs/doc-alignment.json
  - quality/gates/docs-alignment/mirror-convergence-blessed.sh
autonomous: true
requirements: [ADR-01]
must_haves:
  truths:
    - "A first-time reader of root CLAUDE.md and docs/concepts/dvcs-topology.md learns webhook + 30-min cron is the AUTHORITATIVE external-mirror convergence mechanism (not a workaround)"
    - "Both live docs distinguish (a) the cache-internal observability ref refs/mirrors/<sot>-head from (b) the external GitHub mirror repo"
    - "Both live docs name scripts/refresh-tokenworld-mirror.sh as manual op-recovery only, not a convergence mechanism"
    - "A programmatic doc-alignment guard fails (STALE_DOCS_DRIFT / FAIL) if the corrected mirror-truth blessing silently regresses"
  artifacts:
    - path: "docs/concepts/dvcs-topology.md"
      provides: "Authoritative-mechanism blessing + (a)/(b) mirror-sense vocabulary in the L1/L2/L3 section"
      contains: "authoritative"
    - path: "CLAUDE.md"
      provides: "One-sentence webhook+cron blessing inside the Mirror-head refresh promise bullet, citing dvcs-mirror-setup.md"
      contains: "authoritative"
    - path: "quality/gates/docs-alignment/mirror-convergence-blessed.sh"
      provides: "Grep phrase-fragment verifier binding the corrected mirror-truth claim (discriminating on 'authoritative', not the already-present bare 'webhook')"
    - path: "quality/catalogs/doc-alignment.json"
      provides: "New bound row docs-alignment/mirror-convergence-authoritative-bound"
      contains: "mirror-convergence-authoritative"
  key_links:
    - from: "docs/concepts/dvcs-topology.md"
      to: "docs/guides/dvcs-mirror-setup.md"
      via: "cross-link citing the blessed webhook+cron mechanism"
      pattern: "dvcs-mirror-setup"
    - from: "quality/gates/docs-alignment/mirror-convergence-blessed.sh"
      to: "docs/concepts/dvcs-topology.md + CLAUDE.md"
      via: "grep -qF phrase-fragment presence checks (discriminating fragments only)"
      pattern: "grep -qF"
---

<objective>
Close the ADR-01 (Decision 1) live-doc gap: the ruled truth is that webhook + 30-min cron
is the AUTHORITATIVE external-mirror convergence mechanism. RESEARCH §1.1 verified (three
files, cross-checked) that root `CLAUDE.md` and `docs/concepts/dvcs-topology.md` do NOT
today assert the false "`sync --reconcile` heals the external mirror" claim — that false
prose lives ONLY in an ARCHIVED v0.14.0 intake (frozen, left alone). The REAL gap is a
**MISSING explicit blessing**: neither live doc names webhook+cron as *authoritative* (the
word "authoritative" appears ZERO times in either file today — verified), nor uses the
packet's (a) cache-observability-ref / (b) external-mirror-repo vocabulary. This plan ADDS
that blessing (prose-extend, do not rewrite the already-correct `sync --reconcile`
sentences), and mints a doc-alignment regression guard so the corrected claim cannot
silently regress again (RESEARCH §6 Wave-0 recommendation; this exact conflation already
regressed once — hence this phase — so a bound guard is the direct antidote).

Purpose: satisfy ADR-01 Decision 1(i)/(ii) at the live-doc surface + protect it from drift.
Output: two prose-extended live docs + one catalog row + one grep verifier.

ROADMAP reconciliation (flag, do not silently work around — RESEARCH §5.5, Noticed #4):
ROADMAP P116's `**Execution mode**: top-level (produce options + tradeoffs, gather ruling…)`
and success criteria 1–4 describe the OLD pre-ruling shape. Both rulings ALREADY happened
(`.planning/CONSULT-DECISIONS.md`, 2026-07-16, commit `8212373`); this phase EXECUTES the
ruling's follow-through (a write→verify→commit shape). It still runs top-level because a
coordinator dispatches these plans to leaf subagents. The stale ROADMAP description is
flagged in a ROADMAP planner-note blockquote (added this planning pass) + surfaced upward,
not silently rewritten.
</objective>

<execution_context>
This phase is `Execution mode: top-level` — the top-level coordinator IS the executor and
dispatches each task to a leaf subagent (sonnet default; the catalog-bind task may go to a
subagent comfortable with `reposix-quality` tooling). There is no `/gsd-execute-phase` run.
</execution_context>

<context>
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-CONTEXT.md
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-RESEARCH.md
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-PATTERNS.md
@quality/CLAUDE.md

<interfaces>
<!-- Exact analog for the new verifier — copy this grep-fragment-presence style, NOT hash-matching. -->
From quality/gates/docs-alignment/dvcs-topology-three-roles.sh (read this session):
```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &> /dev/null && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../../.." &> /dev/null && pwd)"
DOC="${REPO_ROOT}/docs/concepts/dvcs-topology.md"
# ... check for load-bearing phrase fragments (not exact verbatim) so prose can be reflowed:
for phrase in 'mirror last caught up' 'NOT a' 'current SoT state'; do
  if ! grep -qF -- "$phrase" "$DOC"; then
    echo "FAIL: docs/concepts/dvcs-topology.md missing Q2.2 phrase fragment '$phrase'" >&2
    exit 1
  fi
done
```

Catalog-bind CLI (quality/CLAUDE.md:154) — `reposix-quality doc-alignment {bind, …}`.
NEVER hand-edit doc-alignment.json; NEVER invoke `doc-alignment walk` raw (mutates the
committed catalog — quality/CLAUDE.md:165).

GREP-GATE HYGIENE (verified this session): the bare word `webhook` ALREADY appears 3× in
CLAUDE.md and 11× in dvcs-topology.md — a `grep webhook` gate is TAUTOLOGICAL (passes before
any edit). The word `authoritative` appears ZERO times in either file today — THAT is the
discriminating token. Every gate below keys on `authoritative` (+ the new phrase fragments),
never bare `webhook`.
</interfaces>

Existing text NOT to rewrite (already true per RESEARCH §1.1 — extend around it, do not
duplicate or contradict):
- root `CLAUDE.md:92-100` (§ "Mirror-head refresh promise") — already scopes `sync
  --reconcile` to the LOCAL cache correctly.
- `docs/concepts/dvcs-topology.md:174-182` (L1 bullet) — already scopes `sync --reconcile`
  to the cache and states the `files_touched > 0` no-op-skip.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint the mirror-convergence regression-guard row (catalog-first — FIRST commit)</name>
  <files>quality/gates/docs-alignment/mirror-convergence-blessed.sh, quality/catalogs/doc-alignment.json</files>
  <action>
Per the catalog-first rule (quality/CLAUDE.md) this row + verifier land as the phase's
FIRST commit, BEFORE the Task 2 prose edits that make it GREEN. Today ZERO doc-alignment
rows cover this claim (RESEARCH §5.2 jq-verified: zero rows bound to CLAUDE.md; the one row
on dvcs-topology.md anchors L5-11, unrelated) — without this guard a future edit could
re-introduce the conflation with no gate catching it.

Steps:
1. Write `quality/gates/docs-alignment/mirror-convergence-blessed.sh` mirroring the
   `dvcs-topology-three-roles.sh` style above (set -euo pipefail; resolve REPO_ROOT;
   `grep -qF` phrase-fragment presence, NOT exact-hash). Assert BOTH files contain the
   DISCRIMINATING blessing fragments Task 2 adds. Required fragments (each MUST be absent
   today so the gate is real — verified `authoritative`=0 in both):
     - `docs/concepts/dvcs-topology.md` → `authoritative` AND `refresh-tokenworld-mirror.sh`
     - `CLAUDE.md` → `authoritative` (do NOT gate on bare `webhook` or `dvcs-mirror-setup.md`
       alone — both already present, tautological)
   FAIL with a teaching message naming the missing fragment + file (Rust-compiler-grade UX
   per the ownership charter). `chmod +x` it. Sanity: the script MUST exit non-zero when run
   BEFORE Task 2 (prove it's a real gate), then exit 0 after Task 2.
2. Mint the catalog row via `reposix-quality doc-alignment bind` (per quality/CLAUDE.md;
   NEVER hand-edit the JSON). Row shape (PATTERNS §8 convention): `id` =
   `docs-alignment/mirror-convergence-authoritative-bound`; `source.file` =
   `docs/concepts/dvcs-topology.md` anchored at the NEW prose's own line range (must sit
   BELOW L11 per RESEARCH §5.2); `tests` = `[quality/gates/docs-alignment/mirror-convergence-blessed.sh]`;
   rationale citing the 2026-07-16 ruling (commit `8212373`).
3. TOOLING-GATED FALLBACK: if the `reposix-quality doc-alignment bind` CLI is unavailable in
   this environment, do NOT hand-edit doc-alignment.json. Instead skip the row, keep the
   grep verifier script, and file a one-line GOOD-TO-HAVES follow-up ("mint the
   mirror-convergence doc-alignment row once bind tooling is available"). The grep-based
   §6 checks in Task 2's verify then become the accepted phase-close verification
   (RESEARCH §6: grep-level is sufficient for grading; the bound row is the stronger,
   recommended guard).
  </action>
  <verify>
    <automated>test -x quality/gates/docs-alignment/mirror-convergence-blessed.sh && bash -n quality/gates/docs-alignment/mirror-convergence-blessed.sh && ! bash quality/gates/docs-alignment/mirror-convergence-blessed.sh 2>/dev/null</automated>
  </verify>
  <done>Verifier script exists, is executable, parses (`bash -n`), and correctly FAILS today (non-zero — proving it is a real gate that Task 2 must satisfy, not a tautology). Catalog row minted via bind CLI (or fallback GOOD-TO-HAVES filed if CLI unavailable). RETIRE_PROPOSED count still 11 (`jq '[.rows[]|select(.last_verdict=="RETIRE_PROPOSED")]|length' quality/catalogs/doc-alignment.json` == 11 — the new row is NOT a RETIRE_PROPOSED row).</done>
</task>

<task type="auto">
  <name>Task 2: Bless webhook+cron as authoritative in both live docs (prose-extend)</name>
  <files>docs/concepts/dvcs-topology.md, CLAUDE.md</files>
  <action>
Prose-extend ONLY — do not rewrite the already-correct `sync --reconcile`-scopes-to-cache
sentences (RESEARCH §1.1). Keep all new prose BELOW the existing doc-alignment anchors so no
rebind is needed (RESEARCH §5.2: dvcs-topology.md anchor is L5-11; CLAUDE.md has zero bound
rows). Keep it terse — dvcs-topology.md is at 90.9% of its 20KB size ceiling (RESEARCH §5.1);
if this edit tips it over 20,000 bytes, note it (ownership-charter noticing duty), do not let
it pass silently.

(A) `docs/concepts/dvcs-topology.md` — in the L1 bullet region (L174-182, § "Cache
coherence: L1/L2/L3 (ADR-010)"), extend using the SAME-FILE disambiguation move already
modeled at L52-93 (§ "Two refs — and where they actually live" — bold lead-in + bullet
split; PATTERNS §5). Add:
  - The (a)/(b) split: (a) the cache-internal observability ref `refs/mirrors/<sot>-head`
    vs (b) the external GitHub mirror repo. These are two distinct things both called
    "mirror"; the L1 no-op-skip governs (a), webhook+cron governs (b).
  - One sentence naming **webhook + 30-min cron as the AUTHORITATIVE external-mirror
    convergence mechanism** — use the literal word "authoritative" — citing
    `docs/guides/dvcs-mirror-setup.md` (already linked at L211 § See also — reuse that
    path/anchor style, PATTERNS §5) and the ruling (`.planning/CONSULT-DECISIONS.md`,
    2026-07-16, commit `8212373`).
  - Name `scripts/refresh-tokenworld-mirror.sh` (full basename) as **manual op-recovery
    only**, not a convergence mechanism (Decision 1(ii)).

(B) root `CLAUDE.md` — inside the § "Mirror-head refresh promise" bullet (L92-100), apply
the compress-out-of-root move already modeled by the "Bus URL / mirror fan-out" bullet
directly above it (L89-91; PATTERNS §4): add ONE clause/sentence naming
webhook+30-min-cron as the **authoritative** external-mirror convergence path (literal word
"authoritative"), citing `docs/guides/dvcs-mirror-setup.md` + the ruling. Leave the existing
(already-correct) `sync --reconcile` sentence untouched — do not duplicate or contradict it
(explicit CONTEXT constraint).

Avoid the bare word `replace` (banned exact-word in docs/ p1 layer — RESEARCH §5.3); prefer
"supersedes"/"amends". dvcs-topology.md falls under the below_fold banned-words layer — the
plumbing vocab it bans (FUSE, promisor, stateless-connect, etc.) is not naturally needed
here, but re-run banned-words after editing.
  </action>
  <verify>
    <automated>bash quality/gates/docs-alignment/mirror-convergence-blessed.sh && grep -qF "authoritative" CLAUDE.md && grep -qF "authoritative" docs/concepts/dvcs-topology.md && grep -qF "refresh-tokenworld-mirror.sh" docs/concepts/dvcs-topology.md</automated>
    <manual>bash quality/gates/docs-build/mkdocs-strict.sh && bash quality/gates/docs-build/mermaid-renders.sh && bash quality/gates/structure/banned-words.sh --all && bash quality/gates/docs-alignment/walk.sh</manual>
  </verify>
  <done>Both live docs name webhook+cron as AUTHORITATIVE (literal word present, was 0 today) + cite dvcs-mirror-setup.md; both name the refresh script as manual op-recovery only; the (a)/(b) mirror-sense split is present in dvcs-topology.md; the Task-1 verifier now passes GREEN; mkdocs-strict + mermaid + banned-words(--all) + docs-alignment walk all exit 0; no STALE_DOCS_DRIFT. If dvcs-topology.md crossed 20,000 bytes, the debt is noted (not silently passed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Docs + quality-infra only. No code path, no endpoint, no attacker-influenced byte flow is added or changed. The egress allowlist / `Tainted<T>` / audit-table cuts are untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Tampering | doc-alignment catalog JSON | mitigate | Row minted ONLY via `reposix-quality doc-alignment bind`; hand-editing the JSON is forbidden (quality/CLAUDE.md catalog-first rule) — prevents malformed/attacker-crafted catalog rows. |
| T-116-02 | Information disclosure | doc prose | accept | Docs are public; no secret/PII introduced. Blessing text references only mechanism docs and a commit SHA. Low-value target. |
</threat_model>

<verification>
- `bash quality/gates/docs-alignment/mirror-convergence-blessed.sh` exits 0 (both docs blessed).
- `grep -F "authoritative" CLAUDE.md` and `grep -F "authoritative" docs/concepts/dvcs-topology.md` both hit (were 0 before the edit — real gate, not tautology).
- Full pre-push cadence green: `python3 quality/runners/run.py --cadence pre-push`.
- No `crates/` diff from this plan: `git diff --stat -- crates/ | wc -l` == 0.
</verification>

<success_criteria>
Both live docs explicitly bless webhook+30-min-cron as the AUTHORITATIVE external-mirror
convergence mechanism, distinguish the (a)/(b) mirror senses, and name the refresh script as
manual op-recovery only — protected by a green doc-alignment guard that provably failed
before the edit, with mkdocs/mermaid/banned-words/walk all clean.
</success_criteria>

<output>
After completion, create `.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-01-SUMMARY.md`.
</output>
