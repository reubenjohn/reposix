---
phase: 116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/decisions/010-l2-l3-cache-coherence.md
autonomous: true
requirements: [ADR-01, FIX-03]
must_haves:
  truths:
    - "ADR-010 §2 records the RBF-LR-04 lever CLOSED (files_touched>0 gate STAYS unconditionally, Option D REJECTED) citing the 2026-07-16 ruling"
    - "ADR-010 §2 names webhook+cron as authoritative external-mirror convergence and records Option C's disposition as GTH-V15-38 (do not re-file / re-state trigger)"
    - "ADR-010 §3 records Option B (durable slug→id) as SANCTIONED TARGET DESIGN while the v0.13.0 WAIVED marker STAYS (qualified, not removed), with explicitly NO v0.15 build"
    - "A first-time reader of ADR-010 finds the decision packet from the References section"
  artifacts:
    - path: "docs/decisions/010-l2-l3-cache-coherence.md"
      provides: "§2 amendment (ADR-01) + §3 amendment (FIX-03) + References packet cross-link"
      contains: "SANCTIONED TARGET DESIGN"
  key_links:
    - from: "docs/decisions/010-l2-l3-cache-coherence.md §2 amendment"
      to: "GTH-V15-38"
      via: "Option C disposition citation"
      pattern: "GTH-V15-38"
    - from: "docs/decisions/010-l2-l3-cache-coherence.md §3 amendment"
      to: ".planning/GOOD-TO-HAVES.md GOOD-TO-HAVES-09"
      via: "next-milestone build-proposal home cross-ref"
      pattern: "GOOD-TO-HAVES-09"
    - from: "docs/decisions/010-l2-l3-cache-coherence.md ## References"
      to: ".planning/phases/115-live-mcp-benchmark-re-measurement/P116-ADR-010-DECISION-PACKET.md"
      via: "backtick .planning/ path bullet"
      pattern: "P116-ADR-010-DECISION-PACKET.md"
---

<objective>
Land the durable decision record for BOTH requirements inside ADR-010, and close ROADMAP
criterion 1 ("packet lives alongside the ADR"). Three surgical, terse APPENDS to
`docs/decisions/010-l2-l3-cache-coherence.md` (already over its 20KB budget — keep it tight):
(1) a §2 dated amendment block executing ADR-01 Decision 1 (RBF-LR-04 lever CLOSED); (2) a
§3 dated amendment block executing FIX-03 Decision 2 (Option B = SANCTIONED TARGET DESIGN,
design-only, waiver stays qualified); (3) a References cross-link to the packet.

CRITICAL SCOPE GUARDS:
- FIX-03 is DESIGN-ONLY. There is NO v0.15 build. Do NOT create any task touching `crates/`.
  RESEARCH §2 verified `files_touched > 0` at `write_loop.rs:295` is UNCHANGED by the ruling.
- Criterion 1 is satisfied by a CROSS-LINK ONLY (RESEARCH §4 recommendation, Option C) — NOT
  a file move. The packet physically stays at
  `.planning/phases/115-live-mcp-benchmark-re-measurement/P116-ADR-010-DECISION-PACKET.md`.
  A move would need an mkdocs nav entry and conflate a ruling-process artifact with product
  docs during the "furnished product" quality window (P117/P119).
- APPEND, never rewrite the ratified `## Decision` prose. Use the file's own dated-blockquote
  convention (PATTERNS §1/§2 — precedent at L259-279 "RESOLVED in v0.14.0 (Phase 105)").

Purpose: durable ratified-decision record for ADR-01 + FIX-03; close criterion-1 co-location.
Output: one ADR file, three appended blocks.
</objective>

<execution_context>
`Execution mode: top-level`. The coordinator dispatches this plan to a single leaf subagent
(opus recommended — nuanced ratified-decision prose spanning two requirements). No
`/gsd-execute-phase` run.
</execution_context>

<context>
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-CONTEXT.md
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-RESEARCH.md
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-PATTERNS.md
@.planning/phases/115-live-mcp-benchmark-re-measurement/P116-ADR-010-DECISION-PACKET.md

<interfaces>
<!-- Exact anchors in docs/decisions/010-l2-l3-cache-coherence.md (verified this session) -->
195: ## Decision
207: 2. **Refresh-path redesign (RBF-LR-02).** ... "the ADR leaves that lever to the fix-wave"  <- §2 amendment: APPEND a dated blockquote AFTER this item (before item 3)
221: 3. **`SotPartialFail` recovery semantics (RBF-LR-03).**
238:   > **KNOWN LIMITATION — WAIVED for v0.13.0 (RBF-LR-03).** ...                              <- §3 waiver: QUALIFY (do NOT remove)
259:   > **RESOLVED in v0.14.0 (Phase 105) — ...** ... "STILL OPEN: the duplicate-record ..."   <- §3 amendment: APPEND a dated blockquote AFTER this block (supersedes the STILL OPEN sentence)
423: ## References
425: - `.planning/milestones/v0.13.0-phases/93-cache-coherence/93-DP2-REPRO-NOTES.md` — executed repro ... <- cross-link: copy THIS backtick-.planning/-path style (NOT a markdown hyperlink)

Ruling-citation convention (PATTERNS Shared Patterns): cite `.planning/CONSULT-DECISIONS.md`
by a quoted short-label + date `2026-07-16` + commit `8212373`. Quote the ruling, do not
paraphrase into new meaning.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-010 §2 amendment — RBF-LR-04 lever CLOSED (ADR-01)</name>
  <files>docs/decisions/010-l2-l3-cache-coherence.md</files>
  <action>
APPEND a dated blockquote AFTER Decision item 2 (which spans ~L207-219, ends before item 3
at L221). Copy the shape of the L259-279 "RESOLVED in v0.14.0 (Phase 105)" blockquote
(PATTERNS §1: bold dated header → what's now closed → name the regression-guard/catalog row
→ explicitly name what remains open/deferred). Do NOT rewrite item 2's ratified prose.

Content points to land (RESEARCH §2 — implements CONTEXT Decision 1):
1. State the closure: the 2026-07-16 manager ruling
   (`.planning/CONSULT-DECISIONS.md`, commit `8212373`) CLOSES the "leaves that lever to the
   fix-wave" question — `files_touched > 0` gate STAYS unconditionally; Option D
   (unconditional refresh) REJECTED. The no-op perf-skip assertion (`perf_l1.rs:386-390`)
   stands. A push that changes nothing in the SoT is a semantic no-op skip, not a coherence
   shortcut.
2. Name the authoritative external-mirror mechanism (new info §2 never had — §2 was scoped to
   the cache-internal observability ref): webhook + 30-min cron
   (`docs/guides/dvcs-mirror-setup.md`) is BLESSED as the authoritative external-mirror
   convergence mechanism; `scripts/refresh-tokenworld-mirror.sh` remains manual op-recovery
   only.
3. Record Option C's disposition: NOT sanctioned for v0.15 — filed as `GTH-V15-38` with the
   pull-forward trigger already recorded there. Do NOT re-file or restate the trigger
   differently (CONTEXT: `GTH-V15-38` holds the verbatim trigger — do not weaken/delete it).
4. Optionally reference the mirror-convergence regression guard minted in Plan 01
   (`quality/gates/docs-alignment/mirror-convergence-blessed.sh`) as the guard now covering
   the corrected live-doc claim.

Keep it terse (ADR-010 is already 138% of its 20KB budget — warn-only, but respect
progressive disclosure). Avoid the bare word `replace` (docs/ p1 banned exact-word; prefer
"supersedes"/"amends" — RESEARCH §5.3).
  </action>
  <verify>
    <automated>grep -qF "GTH-V15-38" docs/decisions/010-l2-l3-cache-coherence.md && grep -qF "8212373" docs/decisions/010-l2-l3-cache-coherence.md && grep -qiE "files_touched > 0.*(stays|unconditional)|Option D.*(rejected|REJECTED)" docs/decisions/010-l2-l3-cache-coherence.md</automated>
  </verify>
  <done>§2 amendment appended after item 2 as a dated blockquote citing the 2026-07-16 ruling + commit 8212373; states files_touched>0 STAYS / Option D REJECTED; names webhook+cron authoritative; records Option C→GTH-V15-38 without restating the trigger. Item 2's original prose is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: ADR-010 §3 amendment — Option B SANCTIONED TARGET DESIGN, design-only (FIX-03)</name>
  <files>docs/decisions/010-l2-l3-cache-coherence.md</files>
  <action>
APPEND a dated blockquote AFTER the L259-279 "RESOLVED in v0.14.0" block (before `##
Reversibility` at L300). This new block supersedes that block's "STILL OPEN: the
duplicate-record-on-interrupted-create waiver … remains the v0.14.0 reconciliation-redesign
pivot" sentence. Do NOT remove the L238-257 WAIVED marker — QUALIFY it (PATTERNS §2: the
WAIVED→RESOLVED pair IS the file's "waiver stays, qualified by a later ruling" precedent).

Content points to land (RESEARCH §3 — implements CONTEXT Decision 2):
1. Cite the ruling: 2026-07-16 manager ruling (`.planning/CONSULT-DECISIONS.md`, commit
   `8212373`): OPTION B (durable slug→id map alongside `oid_map`) is the SANCTIONED TARGET
   DESIGN (use that exact phrase — Plan 03's GTH-09 update and the phase verifier grep for it).
2. Describe the design in build-ready terms (from the packet): mint a stable local slug
   pre-push → model create as "slug X → (pending) → backend id N" → persist alongside
   `oid_map` (today's cache tables are `oid_map` + `meta`, `crates/reposix-cache/src/meta.rs`)
   → an interrupted create leaves a pending-slug-no-confirmed-id state the retry reconciles
   against instead of re-creating.
3. Explicitly state the waiver STAYS, reworded to reflect the chosen direction: "The known
   limitation above remains WAIVED for v0.15.0 — NO build lands this milestone. The waiver is
   now qualified by a chosen target design (Option B) rather than an open design question."
4. State SC4 depth explicitly: NO v0.15 build. Record Option D (pending-create intent-log) as
   "sanctioned REDUCED-SCOPE STOPGAP only if the duplicate hazard materializes in a real
   incident before B lands — decide then, not now" (verbatim intent from the ruling).
5. Point at where the next-milestone build proposal durably lives: `GOOD-TO-HAVES-09`
   (`.planning/GOOD-TO-HAVES.md`), updated in Plan 03 — cross-ref it by ID.

THERE IS NO CODE TASK HERE. This is a design record only. Any edit under `crates/` is wrong.
  </action>
  <verify>
    <automated>grep -qF "SANCTIONED TARGET DESIGN" docs/decisions/010-l2-l3-cache-coherence.md && grep -qF "WAIVED for v0.13.0" docs/decisions/010-l2-l3-cache-coherence.md && grep -qF "GOOD-TO-HAVES-09" docs/decisions/010-l2-l3-cache-coherence.md && [ "$(git diff --stat -- crates/ | wc -l)" -eq 0 ]</automated>
  </verify>
  <done>§3 amendment appended after the RESOLVED block, records Option B as SANCTIONED TARGET DESIGN + design-only + waiver STAYS qualified + Option D incident-stopgap + cross-refs GOOD-TO-HAVES-09; the L238-257 WAIVED marker is still present (grep hits "WAIVED for v0.13.0"); zero `crates/` diff.</done>
</task>

<task type="auto">
  <name>Task 3: References cross-link to the decision packet (criterion 1)</name>
  <files>docs/decisions/010-l2-l3-cache-coherence.md</files>
  <action>
Add ONE bullet to the `## References` section (L423+), copying the L425 backtick-`.planning/`-
path style (NOT a markdown hyperlink — a `.planning/` path is not mkdocs-served; a
`[label](path)` link would 404 on the rendered site — PATTERNS §3). Land exactly:

- `` `.planning/phases/115-live-mcp-benchmark-re-measurement/P116-ADR-010-DECISION-PACKET.md`
  — the ruled options+tradeoffs packet (incl. the slug→id durable-create design) for the
  §2/§3 amendments above (ruling: `.planning/CONSULT-DECISIONS.md`, 2026-07-16, commit
  `8212373`). ``

This satisfies ROADMAP criterion 1 ("packet lives alongside ADR-010") via cross-link, per
RESEARCH §4 / the CONTEXT discretion note ("as long as a first-time reader of ADR-010 finds
the packet"). If a stricter verifier reading demands more, the documented fallback (RESEARCH
§4) is to also name the packet in the ADR body near the amendment blocks — do NOT move the
file.
  </action>
  <verify>
    <automated>grep -qF "P116-ADR-010-DECISION-PACKET.md" docs/decisions/010-l2-l3-cache-coherence.md && bash quality/gates/docs-build/mkdocs-strict.sh</automated>
  </verify>
  <done>ADR-010 References cites the packet path as a backtick code span; mkdocs-strict passes (no new nav entry, no orphan-doc risk since no file was added under docs/).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | ADR-010 is a durable-decision doc. No code, no data flow, no attacker-influenced byte crosses any boundary. `files_touched > 0` gate and all runtime cuts are UNCHANGED (design-only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03 | Tampering | ratified ADR Decision prose | mitigate | Amendments APPEND dated blockquotes; ratified `## Decision` prose is byte-unchanged (verified: item-2/WAIVED text still greps) — prevents silent rewrite of a ratified decision. |
| T-116-04 | Repudiation | decision provenance | mitigate | Every amendment cites the ruling by date + commit `8212373` + `.planning/CONSULT-DECISIONS.md` short-label — a durable, auditable provenance trail. |
</threat_model>

<verification>
- `grep -F "GTH-V15-38"`, `grep -F "SANCTIONED TARGET DESIGN"`, `grep -F "WAIVED for v0.13.0"`, `grep -F "P116-ADR-010-DECISION-PACKET.md"`, `grep -F "8212373"` all hit in the ADR.
- `git diff --stat -- crates/ | wc -l` == 0 (design-only; no build).
- `bash quality/gates/docs-build/mkdocs-strict.sh` exits 0 (ADR-010 is nav'd; no new file added).
- Full pre-push cadence green at phase close.
</verification>

<success_criteria>
ADR-010 durably records ADR-01 Decision 1 (§2, lever CLOSED) and FIX-03 Decision 2 (§3,
Option B SANCTIONED TARGET DESIGN, design-only, waiver qualified-not-removed), and a
first-time reader finds the decision packet from References — all as terse appends leaving
ratified prose byte-unchanged, with zero `crates/` diff.
</success_criteria>

<output>
After completion, create `.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-02-SUMMARY.md`.
</output>
