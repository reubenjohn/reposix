---
phase: 116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md
  - .planning/GOOD-TO-HAVES.md
autonomous: true
requirements: [ADR-01, FIX-03]
must_haves:
  truths:
    - "The LIVE v0.15.0 litmus-non-idempotency SURPRISES-INTAKE row carries a terminal RESOLVED status citing the 2026-07-16 ruling (entry NOT deleted)"
    - "GOOD-TO-HAVES-09 records Option B as SANCTIONED TARGET DESIGN with a boundary-relative TAG (build proposed at the next milestone boundary, not a hardcoded version)"
  artifacts:
    - path: ".planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md"
      provides: "Terminal RESOLVED status on the litmus-non-idempotency row (~L116)"
      contains: "RESOLVED"
    - path: ".planning/GOOD-TO-HAVES.md"
      provides: "GOOD-TO-HAVES-09 STATUS + TAG update to SANCTIONED TARGET DESIGN / boundary-relative"
      contains: "SANCTIONED TARGET DESIGN"
  key_links:
    - from: ".planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md litmus row"
      to: "GTH-V15-38"
      via: "elective Option C upgrade tracker citation in the RESOLVED rationale"
      pattern: "GTH-V15-38"
    - from: ".planning/GOOD-TO-HAVES.md GOOD-TO-HAVES-09"
      to: "docs/decisions/010-l2-l3-cache-coherence.md §3"
      via: "Pointer line (already cites ADR-010 §3 — stays accurate once §3 lands)"
      pattern: "ADR-010"
---

<objective>
Execute the two planning-ledger follow-throughs of the 2026-07-16 rulings: retire the LIVE
litmus-non-idempotency SURPRISES row (ADR-01 Decision 1(vi)) and update GOOD-TO-HAVES-09 to
record the FIX-03 sanctioned target design + next-milestone build proposal (FIX-03 Decision
2). Two independent files, two status-flips — mechanical, but with two sharp precision
guards below.

CRITICAL PRECISION GUARDS (a verifier grades these):
- RETIRE THE **LIVE** ROW, NOT THE ARCHIVED TWIN. The row to flip is the LIVE v0.15.0 entry
  at `.planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md` (`## 2026-07-14 20:42 | …
  litmus non-idempotency …`, STATUS `**STATUS:** OPEN` at ~L116). Do NOT touch the archived
  v0.14.0 twin (`.../v0.14.0-phases/surprises-intake/part-02.md:299-329`, STATUS DEFERRED —
  frozen, leave alone). The false "`--reconcile` heals the mirror" prose lives ONLY in that
  archived twin; this phase does NOT delete it. The real deliverable is the LIVE row's
  terminal-status flip (RESEARCH §1.3/§1.4).
- FIRST TERMINAL ROW IN THIS LEDGER — NO IN-FILE PRECEDENT. The live v0.15.0 ledger has ZERO
  terminal-status rows today (all OPEN). Copy the terminal shape from the ARCHIVED v0.14.0
  analog only (PATTERNS §6 / Shared Patterns — the ruling-shape at
  `v0.14.0-phases/surprises-intake/part-03.md:313`: `RULED-<verdict> (<date> [MANAGER] …)`).
  Do NOT expect an in-file precedent — a reviewer should be told there isn't one.

Purpose: durable ledger record of both rulings where next-milestone roadmapping reads (OP-8).
Output: two edited planning ledgers.
</objective>

<execution_context>
`Execution mode: top-level`. The coordinator dispatches this plan to a leaf subagent (sonnet
or haiku — mechanical status-flips with exact analogs). No `/gsd-execute-phase` run. These
are `.planning/` edits — permitted here because this is a GSD-tracked phase execution.
</execution_context>

<context>
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-CONTEXT.md
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-RESEARCH.md
@.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-PATTERNS.md

<interfaces>
<!-- Exact target lines (verified this session) -->
SURPRISES-INTAKE.md (v0.15.0-phases):
  L108: ## 2026-07-14 20:42 | discovered-by: L0 rotation #22 (t4 real-backend re-run …) | severity: MEDIUM
  L116: **STATUS:** OPEN   <- FLIP THIS ONE (the litmus-non-idempotency row's own STATUS)
  L27  (entry-format template): STATUS: OPEN (← drain phase updates to RESOLVED|DEFERRED|WONTFIX with rationale or commit SHA)

Terminal-status analog to copy (archived v0.14.0, ruling-shape):
  part-03.md:313: **STATUS:** RULED-DEFER→v0.15.0 (2026-07-13 [MANAGER] Ruling #2, …)
  => this row is closed by a RULING (not a fix commit), and the ruling makes a FINAL call
     (not a further defer), so it is RESOLVED, not DEFERRED/WONTFIX.

.planning/GOOD-TO-HAVES.md (root ledger):
  L44: ## GOOD-TO-HAVES-09 — `slug-to-id-durable-create-model` …
  L75: **TAG:** v0.15.0                         <- rewrite to boundary-relative phrasing
  L77: **STATUS:** DEFERRED — owner scope call, 2026-07-12 …   <- flip to SANCTIONED TARGET DESIGN
  Boundary-relative precedent (GTH-V15-38, PATTERNS §7): "propose as a phase at the
  then-current milestone boundary" — copy this phrasing, do NOT hardcode `v0.16.0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the LIVE litmus-non-idempotency SURPRISES row (terminal status, never delete)</name>
  <files>.planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md</files>
  <action>
Flip the litmus-non-idempotency row's OWN `**STATUS:** OPEN` (at ~L116, the STATUS line
between the "Sketched resolution" paragraph and the NEXT entry heading `## 2026-07-14 20:43`)
to a terminal RESOLVED value. Confirm you are editing the RIGHT status line: it is the one
under the entry titled `## 2026-07-14 20:42 | … L0 rotation #22 (t4 real-backend re-run,
pre-release-real-backend cadence)` whose **What** paragraph describes the vision-litmus
non-idempotency and `scripts/refresh-tokenworld-mirror.sh` pre-step. Do NOT touch the
adjacent OPEN rows (L106 belongs to the previous entry; L118+ is the next unrelated entry).

Do NOT delete the entry — flip the status in place (CONTEXT: "terminal STATUS with rationale
pointing at the ruling, not silent deletion"). Copy the archived ruling-shape (PATTERNS §6).
Land something in this family (adapt wording, keep the load-bearing fragments):

  **STATUS:** RESOLVED — 2026-07-16 [MANAGER] ruling (`.planning/CONSULT-DECISIONS.md`,
  commit `8212373`): webhook + 30-min cron blessed as the AUTHORITATIVE external-mirror
  convergence mechanism; the `scripts/refresh-tokenworld-mirror.sh` litmus pre-step remains
  documented manual op-recovery, NOT a product fix; `GTH-V15-38` tracks the elective Option
  C (post-write snapshot fan-out) upgrade if the pre-step becomes a real recurring incident.

This is the ledger's FIRST terminal-status row — note in the SUMMARY that there is no in-file
precedent (the shape came from the archived v0.14.0 ledger).
  </action>
  <verify>
    <automated>awk '/^## 2026-07-14 20:42 \| discovered-by: L0 rotation #22 \(t4 real-backend re-run, pre-release-real-backend/{f=1} f&&/^\*\*STATUS:\*\*/{print; exit}' .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md | grep -q "RESOLVED" && grep -qF "8212373" .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md && grep -qF "GTH-V15-38" .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md</automated>
  </verify>
  <done>The litmus row's STATUS is terminal RESOLVED (not OPEN), citing the ruling + commit 8212373 + GTH-V15-38, with the entry body intact (not deleted). No other row's STATUS changed. The archived v0.14.0 twin is untouched (`git diff --stat -- .planning/milestones/v0.14.0-phases/ | wc -l` == 0).</done>
</task>

<task type="auto">
  <name>Task 2: Update GOOD-TO-HAVES-09 — SANCTIONED TARGET DESIGN, boundary-relative TAG (FIX-03)</name>
  <files>.planning/GOOD-TO-HAVES.md</files>
  <action>
Update GOOD-TO-HAVES-09 (`.planning/GOOD-TO-HAVES.md:44-78`, the root ledger — NOT the
archived v0.14.0 twin) so it is the durable home for FIX-03's next-milestone build proposal
that next-milestone roadmapping reads at boundary time (OP-8, RESEARCH §3):

1. `**STATUS:**` (L77-78) — flip from `DEFERRED — owner scope call, 2026-07-12` to reflect
   the ruling, e.g.: `SANCTIONED TARGET DESIGN (Option B) — 2026-07-16 [MANAGER] ruling
   (`.planning/CONSULT-DECISIONS.md`, commit `8212373`); design recorded in ADR-010 §3;
   dedicated design+build phase proposed at the next milestone boundary. NO v0.15 build.`
2. `**TAG:**` (L75) — rewrite `v0.15.0` to boundary-relative phrasing copied from GTH-V15-38
   (PATTERNS §7: "then-current milestone boundary") rather than hardcoding `v0.16.0` (no
   `v0.16.0-phases/` dir exists yet; RESEARCH §3).
3. The `**Pointer:**` line already cites ADR-010 §3 — leave its structure; it stays accurate
   once Plan 02's §3 amendment lands. If it does not yet mention the SANCTIONED framing,
   a light touch is fine, but do NOT rewrite the entry body.

Do NOT touch GTH-V15-38 or its copy-paste-bled duplicate lines (RESEARCH §5.6 / Noticed — a
separate hygiene defect; CONTEXT forbids weakening GTH-V15-38).
  </action>
  <verify>
    <automated>grep -qF "SANCTIONED TARGET DESIGN" .planning/GOOD-TO-HAVES.md && grep -A34 "GOOD-TO-HAVES-09" .planning/GOOD-TO-HAVES.md | grep -qiE "next milestone boundary|then-current milestone boundary" && ! grep -A5 "GOOD-TO-HAVES-09" .planning/GOOD-TO-HAVES.md | grep -q "TAG:\*\* v0.15.0"</automated>
  </verify>
  <done>GOOD-TO-HAVES-09 STATUS records Option B as SANCTIONED TARGET DESIGN citing the ruling + ADR-010 §3 + NO-v0.15-build; TAG is boundary-relative (not hardcoded v0.15.0/v0.16.0); entry body otherwise intact; GTH-V15-38 untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Planning-ledger prose only. No code, no runtime, no attacker-influenced byte. The P115 11-row `RETIRE_PROPOSED` human gate is NOT touched (this phase's edits are SURPRISES-INTAKE + GOOD-TO-HAVES prose, not doc-alignment catalog RETIRE rows). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-05 | Repudiation | ledger row provenance | mitigate | Terminal-status flip cites the ruling by date + commit `8212373`; the entry body is preserved (not deleted) — an auditable "why closed" trail, matching the archived-ledger convention. |
| T-116-06 | Tampering | wrong-row edit (live vs archived twin) | mitigate | Task actions pin the exact entry heading + a scoped `awk` verifier so only the correct LIVE row flips; the archived twin's zero-diff is asserted. |
</threat_model>

<verification>
- Scoped `awk` verify confirms the LITMUS row (not a neighbor) is terminal RESOLVED.
- `grep -F "SANCTIONED TARGET DESIGN" .planning/GOOD-TO-HAVES.md` hits; TAG no longer hardcoded.
- Archived v0.14.0 ledger untouched: `git diff --stat -- .planning/milestones/v0.14.0-phases/ | wc -l` == 0.
- P115 RETIRE_PROPOSED gate untouched: `jq '[.rows[]|select(.last_verdict=="RETIRE_PROPOSED")]|length' quality/catalogs/doc-alignment.json` still 11.
- Full pre-push cadence green at phase close (freshness-invariants: no loose ROADMAP/REQUIREMENTS created).
</verification>

<success_criteria>
The LIVE litmus row carries a terminal RESOLVED status citing the ruling (entry intact, twin
untouched), and GOOD-TO-HAVES-09 records the sanctioned Option B target design with a
boundary-relative TAG — the durable next-milestone build-proposal home.
</success_criteria>

<output>
After completion, create `.planning/phases/116-adr-010-mirror-fanout-decision-packet-slug-id-durable-create/116-03-SUMMARY.md`.
</output>
