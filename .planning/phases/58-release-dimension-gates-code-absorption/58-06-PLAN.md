---
phase: 58-release-dimension-gates-code-absorption
plan: 06
type: execute
wave: 6
depends_on:
  - "58-01"
  - "58-02"
  - "58-03"
  - "58-04"
  - "58-05"
files_modified:
  - CLAUDE.md
  - quality/SURPRISES.md
  - .planning/STATE.md
  - quality/reports/verdicts/p58/VERDICT.md
autonomous: false
requirements:
  - QG-06
  - QG-07
must_haves:
  truths:
    - "CLAUDE.md gains a small append (QG-07): a paragraph documenting the v0.12.0 release-dimension home (quality/gates/release/) + cadence wiring (weekly cron Monday 09:00 UTC + post-release via workflow_run) + the SIMPLIFY-04/05 absorption record + the QG-09 P58 GH Actions badge. Cross-references quality/PROTOCOL.md (no runtime detail duplicated). Anti-bloat: under 80 added lines."
    - "quality/SURPRISES.md is updated with any P58 pivot entries from Waves A through E (likely 0-3 entries). The 5 P56 entries + 3 P57 entries from earlier phases are preserved verbatim. Anti-bloat: under 200 lines."
    - ".planning/STATE.md cursor advances from P57 SHIPPED to P58 SHIPPED. Roadmap-Evolution entry appended at top (newest first); P57 + P56 entries preserved verbatim. progress frontmatter incremented (completed_phases 2 to 3; total_plans 10 to 16; completed_plans 10 to 16; percent 25 to 38)."
    - "An unbiased verifier subagent is dispatched per QG-06 and quality/PROTOCOL.md verifier-subagent-prompt-template. The verdict lands at quality/reports/verdicts/p58/VERDICT.md. Phase does NOT close on RED. Path B fallback (in-session verdict with disclosure) is acceptable per P56/P57 precedent if Task tool unavailable."
    - "Phase-close commit lands all 4 docs updates (CLAUDE.md + SURPRISES.md + STATE.md + VERDICT.md) in the SAME PR per QG-07."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Append (50-80 lines) at the END of the existing Quality Gates dimension/cadence/kind taxonomy section (P57) with the P58-specific extensions: release-dim home, cadence wiring (weekly cron + post-release workflow_run), QG-09 P58 GH Actions badge, SIMPLIFY-04/05 absorption record. Cross-references quality/PROTOCOL.md (does NOT duplicate runtime detail)."
      contains: "release dimension"
    - path: "quality/SURPRISES.md"
      provides: "Append any P58 entries from Waves A through E (likely 0-3 entries). 5 P56 + 3 P57 entries preserved. <=200 lines."
      contains: "P58"
      max_lines: 200
    - path: ".planning/STATE.md"
      provides: "Roadmap-Evolution entry appended for P58 SHIPPED at top. Current Position cursor advances to P59. progress frontmatter incremented."
      contains: "P58 SHIPPED"
    - path: "quality/reports/verdicts/p58/VERDICT.md"
      provides: "Unbiased verifier subagent verdict (or in-session verdict with disclosure per P56/P57 precedent). Top-line GREEN/RED, per-row table for the 16 release rows + 4 code rows, QG-07 + QG-09 + SIMPLIFY-04/05 + POLISH-RELEASE + POLISH-CODE-stub closure citations, recommendation."
      contains: "GREEN"
  key_links:
    - from: "CLAUDE.md (P58 append)"
      to: "quality/PROTOCOL.md"
      via: "cross-reference; CLAUDE.md does NOT duplicate runtime detail per anti-bloat rule"
      pattern: "quality/PROTOCOL.md"
    - from: "verifier subagent dispatch"
      to: "quality/reports/verdicts/p58/VERDICT.md"
      via: "Task tool dispatch with the prompt from PROTOCOL.md verifier-subagent-prompt-template; subagent writes the verdict file"
      pattern: "ZERO session context"
    - from: ".planning/STATE.md"
      to: "P59 agent (next session)"
      via: "Cursor advance + Roadmap-Evolution entry tells the next agent P58 GREEN + lists what they read at start of P59"
      pattern: "P58 SHIPPED"
---

<objective>
Wave F — phase close. Land the CLAUDE.md update (QG-07), append any P58 SURPRISES entries (QG-05 maintenance), advance the STATE.md cursor, and dispatch the unbiased verifier subagent (QG-06) whose verdict at quality/reports/verdicts/p58/VERDICT.md is the P58 phase-close grade.

Per quality/PROTOCOL.md Steps 5 through 9:
- Step 5: CLAUDE.md update is MANDATORY in the same PR. Wave F lands it.
- Step 6: Run the verifier (run.py + verdict.py for cadence weekly, post-release, pre-push). Wave E already showed all rows GREEN/WAIVED; Wave F re-runs to confirm.
- Step 7: Dispatch unbiased verifier subagent. Per the P56/P57 precedent, Path B fallback (in-session verdict with disclosure) is acceptable when Task dispatch is unavailable.
- Step 8: Update STATE.md.
- Step 9: Append SURPRISES.md if any P58 wave pivoted.

Plan is autonomous: false because the verifier subagent dispatch is the human-or-checkpoint moment.

Output: 4 files committed in 1-2 commits + verifier dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.12.0-roadmap-and-rationale.md
@quality/PROTOCOL.md
@quality/SURPRISES.md
@quality/catalogs/release-assets.json
@quality/catalogs/code.json
@quality/catalogs/orphan-scripts.json
@quality/runners/run.py
@quality/runners/verdict.py
@.planning/phases/57-quality-gates-skeleton-structure-dimension/57-06-PLAN.md
@.planning/phases/58-release-dimension-gates-code-absorption/58-01-SUMMARY.md
@.planning/phases/58-release-dimension-gates-code-absorption/58-02-SUMMARY.md
@.planning/phases/58-release-dimension-gates-code-absorption/58-03-SUMMARY.md
@.planning/phases/58-release-dimension-gates-code-absorption/58-04-SUMMARY.md
@.planning/phases/58-release-dimension-gates-code-absorption/58-05-SUMMARY.md
@CLAUDE.md

<interfaces>
**CLAUDE.md update — what to append (per QG-07 + Wave F notes):**

Anti-bloat: append a paragraph + tables; do NOT rewrite existing sections. The new content sits AFTER the existing P57 Quality Gates section, as a P58 release-dimension subsection.

Recommended new content (50-80 lines), inserted via H3 subsection right after the P57 dimension/cadence/kind taxonomy section:

- Title: P58 — Release dimension live (added 2026-04-XX).
- Verifier table: 5 rows mapping verifier path to catalog rows + cadence.
- Cadence wiring: quality-weekly.yml cron Monday 09:00 UTC + quality-post-release.yml workflow_run on release.yml.
- QG-09 P58 GH Actions badge URL: README + docs/index.md location.
- Code dimension absorption: clippy-lint-loaded.sh (SIMPLIFY-04) + check-fixtures.py (SIMPLIFY-05 Option A) + ci-job-status.sh (POLISH-CODE P58-stub).
- Orphan-scripts ledger: now empty after Wave E removed the W-2 waiver.
- Recovery patterns: gh workflow run release.yml --ref reposix-cli-vX.Y.Z (P56 SURPRISES.md row 1 + 2 patterns).
- Cross-references list (PROTOCOL.md, catalog files, dimension READMEs).

Style notes:
- H3 subsection inside the H2 Quality Gates parent.
- GFM tables with proper separators.
- <=80 added lines.
- Use banned-words-allowed list (migrate to / for the 80% / rewrite as / alongside / complement); do NOT use the banned 'replace'.

Optional minor edit: P57 Wave F should have already added the QG-06 bullet to Subagent delegation rules; verify; only edit if missing.

**SURPRISES.md updates:**

Re-read quality/SURPRISES.md and check whether Waves A through E added entries. If new entries from Wave F itself: append them. Format: YYYY-MM-DD P58: obstacle — resolution.

Likely candidates depending on what happened:
- Wave A: catalog-first commit clean -> likely NO surprise.
- Wave B: crates.io rate-limit if hit (likely NOT at 1s sleep).
- Wave C: SIMPLIFY-05 Option A vs B decision (if pivoted, append).
- Wave D: workflow_run brittleness (if observed + pivoted, append).
- Wave E: any RED row that needed in-phase fix or waiver.

If zero pivots: add a single confirmation entry per the P57 Wave F precedent stating phase shipped without pivots, with summary of what landed clean. Anti-bloat: 1-5 lines max.

Line cap: <=200 lines (currently ~92; plenty of room).

Order: APPEND AT THE END (chronological). Do NOT touch P56 or P57 entries.

**STATE.md cursor advance:**

Frontmatter:
- last_updated: current RFC3339.
- last_activity: P58-shipped one-liner.
- progress: increment phases 2->3, plans 10->16, total_plans 10->16, percent 25->38.

Per the P57 Wave F W-4 lesson: read frontmatter at runtime, increment, do NOT hard-code the math. Sample script:

```python
import yaml
from pathlib import Path
text = Path(".planning/STATE.md").read_text()
parts = text.split("---", 2)
fm = yaml.safe_load(parts[1])
prog = fm["progress"]
prog["completed_phases"] = int(prog["completed_phases"]) + 1
prog["completed_plans"] = int(prog["completed_plans"]) + 6
prog["total_plans"] = int(prog["total_plans"]) + 6
prog["percent"] = int(round(100 * prog["completed_phases"] / int(prog["total_phases"])))
fm["last_updated"] = "<current-RFC3339>"
fm["last_activity"] = "<P58-shipped-one-liner>"
parts[1] = "\n" + yaml.safe_dump(fm, sort_keys=False, default_flow_style=False) + "\n"
Path(".planning/STATE.md").write_text("---".join(parts))
```

Body Roadmap Evolution: APPEND a new bullet at the TOP of the list (above the existing P57 bullet) with the P58 SHIPPED summary. Do NOT delete the P57 or P56 bullets.

Body Current Position: update Phase to 59, Cursor + Status + Last activity to P58-shipped state.

**Verdict subagent dispatch (QG-06):**

Path A — Task tool dispatch (preferred). Dispatch a general-purpose subagent with the prompt template from quality/PROTOCOL.md. The subagent reads zero session context — only catalog rows + artifacts + CLAUDE.md diff — and writes the verdict.

Path B — In-session verdict with disclosure (P56/P57 fallback). Wave F executor writes the verdict in-session BUT explicitly discloses: which evidence sources were consulted (catalog files, artifact JSONs, runner output); that no narrative re-interpretation was performed; that an out-of-session subagent dispatch should match the verdict exactly.

The disclosure pattern is verbatim from P57's verdict file (lines 14-30 of quality/reports/verdicts/p57/VERDICT.md — copy-paste with P58 substitutions).

**Verdict structure** (per P57 precedent, adapted):

Verdict file at quality/reports/verdicts/p58/VERDICT.md with:
- Top-line GREEN | RED.
- Catalog rows graded count: 20 (release-assets.json: 16; code.json: 4; orphan-scripts.json: 0).
- QG-07 CLAUDE.md update: PASS | FAIL.
- QG-05 SURPRISES.md presence: PASS.
- QG-06 verifier dispatch: PASS (this verdict file IS the QG-06 evidence).
- QG-09 P58 GH Actions badge: PASS.
- POLISH-RELEASE closure: PASS (every release weekly row PASS or WAIVED; post-release row PARTIAL documented).
- POLISH-CODE P58-stub closure: PASS (clippy + fixtures PASS; test + fmt WAIVED until P63).
- SIMPLIFY-04 closure: PASS (clippy-lint-loaded migrated).
- SIMPLIFY-05 closure: PASS (check-fixtures audited + migrated Option A).
- RELEASE-04 closure: PASS.
- Per-row tables (release-dim 16 rows + code-dim 4 rows) with status + evidence_citation.
- QG-07 citations from git diff main...HEAD -- CLAUDE.md.
- Recommendation: GREEN; phase closes; non-blocking carry-forwards listed (POLISH-CODE final P63; cargo-binstall full PASS deferred to MIGRATE-03 v0.12.1).

**Pre-push hook impact:**

Committed CLAUDE.md + STATE.md + SURPRISES.md + verdict.md trigger pre-push:
- structure-dim runner (P57): all 9 rows still PASS or WAIVED.
- code-dim runner (P58 new pre-push rows): clippy + fixtures PASS.
- credential scan: clean.
- mkdocs / mermaid: not touched.
- cargo: not touched.

Pre-push GREEN.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 58-06-A: Update CLAUDE.md (QG-07 — P58 release dimension live subsection)</name>
  <files>CLAUDE.md</files>
  <action>
Precondition (W-5 plan-check pattern from P57): before any edit, assert P57's prerequisite section exists. If missing, escalate.

Run: grep -q "Quality Gates — dimension/cadence/kind taxonomy" CLAUDE.md OR die.

Find the insertion point: locate the end of the existing P57 dimension/cadence/kind taxonomy subsection. Insert the new content as a "P58 — Release dimension live" subsection at the end of that section (before the next H2 heading).

Apply edit per the interfaces section recommended new content (50-80 lines):

Use the Edit tool to insert the new H3 subsection. Preserve every existing line.

Required new content:
- H3 header: ### P58 — Release dimension live (added 2026-04-XX)
- One-line intro pointing at quality/gates/release/.
- Verifier table (5 rows: gh-assets-present, installer-asset-bytes, brew-formula-current, crates-io-max-version, cargo-binstall-resolves) with their catalog rows + cadence.
- Cadence wiring paragraph (quality-weekly.yml cron Monday 09:00 UTC; quality-post-release.yml workflow_run + optional schedule-cron-as-backup if pivot fired).
- QG-09 badge sentence + URL.
- Code dimension paragraph (clippy-lint-loaded migrated per SIMPLIFY-04; check-fixtures migrated per SIMPLIFY-05 Option A; ci-job-status thin wrapper for POLISH-CODE P58-stub).
- Orphan-scripts ledger sentence (now empty after Wave E removed the W-2 waiver).
- Recovery patterns paragraph (gh workflow run release.yml --ref reposix-cli-vX.Y.Z for the P56 SURPRISES.md row 1+2 patterns).
- Cross-references list (PROTOCOL.md, release-assets.json, code.json, dimension READMEs).

Style notes:
- H3 subsection (### inside H2 Quality Gates parent).
- GFM tables.
- <=80 added lines.
- Cross-references at the bottom MUST point at quality/PROTOCOL.md, quality/catalogs/{release-assets,code}.json, quality/gates/{release,code}/README.md.
- Use banned-words-allowed list (migrate to, for the 80%, rewrite as, alongside, complement). Do NOT use the banned word 'replace'.

Optional minor edit: if existing CLAUDE.md Subagent delegation rules section does not mention QG-06, P57 already added it; verify; if missing, append the QG-06 bullet.

Self-checks:
- wc -l CLAUDE.md (note new total).
- bash scripts/banned-words-lint.sh (exit 0).
- grep -q "P58 — Release dimension live" CLAUDE.md (new subsection present).
- grep -q "quality/gates/release" CLAUDE.md.
- grep -q "SIMPLIFY-04" CLAUDE.md (absorption record).
- grep -q "quality-weekly.yml" CLAUDE.md (cadence wiring documented).
  </action>
  <verify>
    <automated>grep -q "P58.*Release dimension" CLAUDE.md && grep -q "quality/gates/release" CLAUDE.md && grep -q "SIMPLIFY-04" CLAUDE.md && grep -q "quality-weekly.yml" CLAUDE.md && bash scripts/banned-words-lint.sh && echo "CLAUDE.md OK"</automated>
  </verify>
  <done>
CLAUDE.md gains the P58 release-dimension H3 subsection with all required content (verifier table, cadence wiring, QG-09 badge URL, SIMPLIFY-04/05 absorption record, recovery patterns, cross-references). banned-words-lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 58-06-B: Append any P58 wave-pivot SURPRISES (QG-05 maintenance)</name>
  <files>quality/SURPRISES.md</files>
  <action>
Re-read quality/SURPRISES.md + each Wave's SUMMARY.md (58-01..58-05) and identify any pivots that should already be captured. If captured during their own waves: skip. If not: append now.

Likely candidates depending on what actually happened:
- Wave A: catalog-first commit clean -> likely NO surprise.
- Wave B: crates.io rate-limit if hit (likely NOT at 1s sleep).
- Wave C: SIMPLIFY-05 Option A vs B decision (if pivoted, append).
- Wave D: workflow_run brittleness (if observed + pivoted to schedule-cron-as-backup, append).
- Wave E: any RED row that needed in-phase fix or waiver.

Format per PROTOCOL.md: YYYY-MM-DD P58: one-line obstacle — one-line resolution.

If zero pivots happened: add a single confirmation entry per P57 Wave F precedent:

A confirmation line stating: "phase shipped without pivots — release-dim gates landed clean (16 weekly rows + 1 post-release row), code-dim absorption confirmed (SIMPLIFY-04 + SIMPLIFY-05 closed; check_fixtures audit chose Option A), quality-weekly + quality-post-release validated end-to-end, QG-09 P58 GH Actions badge live. — All catalog rows GREEN or WAIVED (PARTIAL on cargo-binstall-resolves documented per P56 row 3); verdict at quality/reports/verdicts/p58/VERDICT.md."

This single no-pivots entry IS valuable — documents that P58 ran clean.

Line cap: wc -l quality/SURPRISES.md <= 200. Currently ~92 lines after P57's entries; adding 1-5 P58 entries keeps us well under cap.

Order: APPEND AT THE END (chronological). Do NOT touch P56 or P57 entries above.
  </action>
  <verify>
    <automated>test -f quality/SURPRISES.md && lc=$(wc -l < quality/SURPRISES.md) && [ "$lc" -le 200 ] && [ "$(grep -cE '^2026-..-..* P58:' quality/SURPRISES.md)" -ge 1 ] && [ "$(grep -c '^2026-04-27 P56:' quality/SURPRISES.md)" -eq 5 ] && [ "$(grep -cE '^2026-04-27 P57:' quality/SURPRISES.md)" -ge 3 ] && echo "SURPRISES.md OK lines=$lc"</automated>
  </verify>
  <done>
File <=200 lines. >=1 P58 entry appended (real pivots or clean-run confirmation). 5 P56 entries + 3+ P57 entries preserved verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 58-06-C: Re-run runner + verdict.py once more for the close-of-phase artifact set</name>
  <files>quality/reports/verdicts/p58/VERDICT.md (will be created by Task D's verifier subagent dispatch — see notes), quality/catalogs/release-assets.json (status fields update from re-run), quality/catalogs/code.json (status fields update from re-run)</files>
  <action>
This task generates the in-session pre-flight verdict artifacts so Task D (verifier subagent dispatch) has fresh artifacts to grade.

Run sequence:

```bash
python3 quality/runners/run.py --cadence weekly
python3 quality/runners/run.py --cadence post-release
python3 quality/runners/run.py --cadence pre-push
python3 quality/runners/verdict.py --phase 58
```

Expected:
- Runner --cadence weekly exit 0 (every P0+P1 row PASS or WAIVED — Wave E confirmed).
- Runner --cadence post-release exit 0 or 2 (PARTIAL on cargo-binstall acceptable per documented).
- Runner --cadence pre-push exit 0 (P57 structure-dim still PASS; new code-dim pre-push rows PASS).
- 16+4+9 = 29+ artifacts updated under quality/reports/verifications/.
- verdict.py writes quality/reports/verdicts/p58/<ts>.md and updates quality/reports/badge.json.
- badge.json: color brightgreen, message "<N>/<M> GREEN" (P0+P1 rows in scope).

Sanity-check the badge:

```bash
python3 -c "import json; d=json.load(open('quality/reports/badge.json')); print(d); assert d['color']=='brightgreen', d"
```

Expected: schemaVersion: 1, label: quality gates, message: "<N>/<M> GREEN", color: brightgreen.

If runner exits non-zero on weekly OR pre-push (any RED P0+P1 row): STOP. Investigate. The Wave E owner-verify checkpoint was supposed to confirm GREEN before reaching Wave F. If RED here, something regressed since Wave E. Possibilities:
- Wave E commit accidentally re-introduced a regression.
- An external state changed (release-plz published a new per-crate release that moved the latest pointer per P56 SURPRISES.md row 1).
- The runner has a regression bug (unlikely; P57 runner has been stable).

Per PROTOCOL.md "When stuck rules": file SURPRISES.md "2026-04-XX P58 Wave F: regression detected after Wave E — <symptom>" and pause for owner input.

If runner exits 0 + verdict GREEN: artifacts are fresh + correct; Task D's verifier subagent reads them.
  </action>
  <verify>
    <automated>python3 quality/runners/run.py --cadence weekly; we=$?; python3 quality/runners/run.py --cadence post-release; pe=$?; python3 quality/runners/run.py --cadence pre-push; pp=$?; python3 quality/runners/verdict.py --phase 58; ve=$?; python3 -c "import json; d=json.load(open('quality/reports/badge.json')); assert d['color']=='brightgreen'"; echo "exits: weekly=$we post-release=$pe pre-push=$pp verdict=$ve" && [ "$we" -eq 0 ] && [ "$pp" -eq 0 ]</automated>
  </verify>
  <done>
Runner exits 0 for weekly + pre-push (post-release exit 2 acceptable for cargo-binstall PARTIAL). verdict.py writes p58 verdict file. badge.json color brightgreen. Per-row artifacts under quality/reports/verifications/{release,code}/ are timestamped now.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 58-06-D: Dispatch unbiased verifier subagent (QG-06) — or in-session verdict with disclosure (P56/P57 fallback)</name>
  <what-built>
Wave F has assembled the phase-close evidence:
- All P58 catalog rows graded by quality/runners/run.py (Task C).
- CLAUDE.md updated with the P58 release-dimension subsection (Task A; QG-07).
- SURPRISES.md updated with any P58 pivots (Task B; QG-05 maintenance).
- Per-row artifacts at quality/reports/verifications/{release,code}/*.json are fresh.

This task dispatches the unbiased verifier subagent per QG-06 + PROTOCOL.md verifier-subagent-prompt-template. The subagent reads zero session context — only the catalog rows + artifacts + CLAUDE.md diff — and writes quality/reports/verdicts/p58/VERDICT.md.

Two execution paths (per P56/P57 precedent):

Path A — Task tool dispatch (preferred). Dispatch a general-purpose subagent via Task tool with the verbatim PROTOCOL.md verifier-subagent-prompt-template + P58 substitutions.

Path B — In-session verdict with disclosure (P56/P57 fallback). Wave F executor writes the verdict in-session BUT explicitly discloses evidence sources + that no narrative re-interpretation was performed.
  </what-built>
  <how-to-verify>
1. Choose the path:
   - If running under an orchestrator that has Task tool access: Path A (preferred).
   - If running in a session where Task dispatch is not possible: Path B with disclosure.

2. Path A — Task dispatch. Dispatch a general-purpose subagent with the verbatim PROTOCOL.md verifier-subagent-prompt-template + P58 substitutions (N -> 58). The subagent's prompt MUST refuse to grade GREEN unless every P0+P1 catalog row is PASS or WAIVED AND CLAUDE.md is updated AND quality/SURPRISES.md is current.

3. Path B — in-session verdict with disclosure. Executor writes quality/reports/verdicts/p58/VERDICT.md per the structure in interfaces. Use the P57 disclosure block verbatim. Cite EXACT artifact paths + line ranges; do NOT narrate.

4. Read the verdict file:

```bash
cat quality/reports/verdicts/p58/VERDICT.md
```

Expected: Verdict: GREEN. 20 catalog rows graded (16 release + 4 code). QG-07 PASS. QG-05 PASS. QG-06 PASS. QG-09 PASS. POLISH-RELEASE PASS. POLISH-CODE P58-stub PASS. SIMPLIFY-04/05 PASS. RELEASE-04 PASS.

5. Decide:
   - GREEN approved: type "approved" -> Wave F proceeds to Task E (STATE.md cursor advance + commit).
   - RED: the verdict file describes the RED rows. Either (a) fix in this same wave (loop back to Wave E for the offending row), or (b) escalate via SURPRISES.md + STOP per PROTOCOL.md "When stuck rules". Phase does NOT close on RED.
  </how-to-verify>
  <resume-signal>Type "approved" (after reading the GREEN verdict file) or describe the RED rows.</resume-signal>
</task>

<task type="auto">
  <name>Task 58-06-E: Update STATE.md cursor + commit Wave F (phase close)</name>
  <files>.planning/STATE.md, CLAUDE.md, quality/SURPRISES.md, quality/reports/verdicts/p58/VERDICT.md</files>
  <action>
Step 1 — Update .planning/STATE.md:

Edit the frontmatter and the body per interfaces STATE.md cursor advance:
- Frontmatter last_updated: current RFC3339.
- Frontmatter last_activity: P58-shipped one-liner.
- Frontmatter progress: per the W-4 plan-check fix, read current frontmatter at runtime + increment. Do NOT hard-code the before/after numbers. Use the Python script from interfaces section.
  - Sample script reads STATE.md, parses YAML frontmatter, increments completed_phases by 1, completed_plans by 6, total_plans by 6, recomputes percent.
  - This is resilient if STATE.md was already advanced (e.g., a previous Wave F retry); always reads current values.

- Body Accumulated Context Roadmap Evolution: APPEND a new bullet at the TOP of the list (above the existing P57 bullet) with a P58 SHIPPED summary. Do NOT delete the P57 or P56 bullets.

- Body Current Position: update Phase to 59, Cursor + Status + Last activity to P58-shipped state.

Sample P58 Roadmap-Evolution bullet:

P58 SHIPPED (2026-04-XX, v0.12.0): Release-dimension gates + code-dim absorption landed. quality/gates/release/ ships 5 verifiers (gh-assets-present, installer-asset-bytes, brew-formula-current, crates-io-max-version, cargo-binstall-resolves) backing 16 catalog rows in quality/catalogs/release-assets.json. quality/gates/code/ ships clippy-lint-loaded.sh (SIMPLIFY-04 migration), check-fixtures.py (SIMPLIFY-05 Option A migration), ci-job-status.sh (POLISH-CODE P58-stub thin wrapper). 4 catalog rows in quality/catalogs/code.json. .github/workflows/quality-weekly.yml + quality-post-release.yml validated. QG-09 P58 GH Actions badge live in README + docs/index.md. orphan-scripts.json shrunk to empty (W-2 waiver removed; the dimension provides active enforcement). POLISH-RELEASE closure: every release weekly row PASS or WAIVED; cargo-binstall-resolves PARTIAL documented. POLISH-CODE P58-stub: clippy + fixtures PASS; test + fmt WAIVED until P63. Verifier subagent verdict GREEN at quality/reports/verdicts/p58/VERDICT.md (Path A | Path B disclosure). Carry-forwards under MIGRATE-03: cargo-binstall metadata + MSRV (P56 row 3); + any Wave E waivers. Next: P59 docs-repro dimension + tutorial replay + agent-ux thin home.

Step 2 — Stage Wave F's 4 files:

```bash
git add CLAUDE.md quality/SURPRISES.md .planning/STATE.md quality/reports/verdicts/p58/VERDICT.md
```

Step 3 — Commit. Single combined commit (or 2 commits if separating verdict from docs):

```
chore(p58): phase close — CLAUDE.md QG-07 + SURPRISES.md + STATE.md + verdict GREEN

Closes P58 — release dimension gates + code-dim absorption.
The verifier subagent's verdict at quality/reports/verdicts/p58/VERDICT.md
grades all 20 catalog rows (16 release + 4 code) GREEN or WAIVED, plus
PARTIAL on the documented cargo-binstall-resolves row.

Confirms QG-05 / QG-06 / QG-07 / QG-09 / POLISH-RELEASE / POLISH-CODE-stub /
SIMPLIFY-04 / SIMPLIFY-05 / RELEASE-04 closure.

Files:
- CLAUDE.md — new "P58 — Release dimension live" subsection appended
  inside the existing v0.12.0 Quality Gates parent (QG-07).
  Cross-references quality/PROTOCOL.md (no runtime detail duplicated).
  Records SIMPLIFY-04/05 absorption + recovery patterns.
- quality/SURPRISES.md — appended N P58 entries for any wave pivots;
  5 P56 entries + 3+ P57 entries preserved. <=200 lines.
- .planning/STATE.md — Roadmap-Evolution gains a P58 SHIPPED entry at
  top; Current Position cursor advances to P59. progress frontmatter
  incremented (3/8 phases = 38%).
- quality/reports/verdicts/p58/VERDICT.md — verifier verdict GREEN.
  Cites file:line for each of 20 catalog rows + QG-07 + QG-05 + QG-09 +
  SIMPLIFY-04/05 + POLISH-RELEASE + POLISH-CODE-stub closure.

P58 ships (in addition to this commit's docs):
- quality/catalogs/{release-assets.json (16 rows), code.json (4 rows)}
- quality/gates/release/{gh-assets-present.py, installer-asset-bytes.py,
  brew-formula-current.py, crates-io-max-version.py,
  cargo-binstall-resolves.py, __init__.py, README.md}
- quality/gates/code/{clippy-lint-loaded.sh, ci-job-status.sh,
  check-fixtures.py, README.md}
- .github/workflows/{quality-weekly.yml, quality-post-release.yml}
- README.md + docs/index.md QG-09 P58 GH Actions badge
- quality/catalogs/orphan-scripts.json shrunk to empty (W-2 waiver removed)
- scripts/check_clippy_lint_loaded.sh + scripts/check_fixtures.py:
  deleted (callers updated) OR thin shims (per Wave C decisions)

Carry-forwards (none block GREEN):
- POLISH-CODE final P63.
- cargo-binstall full PASS deferred to MIGRATE-03 v0.12.1.
- Any Wave E in-phase waivers cite specific MIGRATE-03 bullets.

Refs: RELEASE-04, QG-06, QG-07, QG-09 (P58), SIMPLIFY-04, SIMPLIFY-05,
POLISH-RELEASE, POLISH-CODE (P58 stub).
```

Step 4 — Pre-commit checks:

```bash
bash scripts/banned-words-lint.sh
python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])"
python3 quality/runners/run.py --cadence pre-push
python3 quality/runners/run.py --cadence weekly
python3 quality/runners/verdict.py --phase 58
```

Expect all exit 0 (post-release exit 2 OK for cargo-binstall PARTIAL).

Step 5 — Push. Pre-push hook fires; expected GREEN.
  </action>
  <verify>
    <automated>git log -1 --pretty=format:"%s" | grep -qE "p58.*phase close|P58 SHIPPED" && grep -q "P58 SHIPPED" .planning/STATE.md && grep -q "Phase: 59" .planning/STATE.md && grep -q "GREEN" quality/reports/verdicts/p58/VERDICT.md && python3 quality/runners/run.py --cadence pre-push && python3 quality/runners/run.py --cadence weekly && bash scripts/banned-words-lint.sh && echo "phase close commit OK"</automated>
  </verify>
  <done>
Single commit (or 2) lands. STATE.md cursor on Phase 59. CLAUDE.md has the new P58 subsection. SURPRISES.md >=1 P58 entry. Verdict GREEN. Runner GREEN. banned-words-lint clean. Push GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| executing agent's word ↔ verifier subagent's verdict | Wave F dispatches the subagent OR uses the P57 disclosure pattern; the verdict file IS the audit |
| CLAUDE.md update ↔ next agent's session | If CLAUDE.md is missing the P58 release-dimension subsection, the P59 agent operates ungrounded |
| STATE.md cursor ↔ next agent's first action | Next agent's first read is STATE.md; if cursor is wrong, the agent operates on stale context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-58-06-01 | Tampering | Wave F executor talks the verdict GREEN despite a real RED | mitigate | Path A subagent dispatch is the preferred path; Path B requires explicit disclosure block per P56/P57 precedent. Verdict file cites file:line evidence. |
| T-58-06-02 | Repudiation | "Where is P58's contract proof?" | mitigate | Verdict file at quality/reports/verdicts/p58/VERDICT.md is the audit. STATE.md cross-references it. CLAUDE.md cross-references PROTOCOL.md. |
| T-58-06-03 | Denial of service | verifier subagent unavailable -> phase blocked | mitigate | Path B fallback per P56/P57 precedent; the executing agent writes the verdict with the disclosure block. |
| T-58-06-04 | Tampering | CLAUDE.md update accidentally drops content | mitigate | Task A is APPEND-only (new H3 subsection inside existing H2); pre-commit grep confirms the new subsection IS present without inspecting deletions. |
| T-58-06-05 | Information disclosure | n/a | accept | All updates are public-by-design. |
| T-58-06-06 | Spoofing | n/a | accept | No auth boundary. |
| T-58-06-07 | Tampering | STATE.md frontmatter parsing drift | mitigate | Pre-commit yaml.safe_load check on the frontmatter ensures the file remains parseable by gsd-tools. |
</threat_model>

<verification>
After this plan ships:

1. grep -q "P58 — Release dimension live" CLAUDE.md (or similar P58 H3 subsection) — exit 0.
2. grep -q "quality/PROTOCOL.md" CLAUDE.md — exit 0 (cross-reference present).
3. grep -q "SIMPLIFY-04" CLAUDE.md — exit 0 (absorption record).
4. grep -cE "^2026-..-..* P58:" quality/SURPRISES.md >= 1.
5. wc -l quality/SURPRISES.md <= 200.
6. grep -q "P58 SHIPPED" .planning/STATE.md — exit 0.
7. grep -q "Phase: 59" .planning/STATE.md — exit 0.
8. python3 -c "import yaml; yaml.safe_load(open('.planning/STATE.md').read().split('---')[1])" — exit 0.
9. test -f quality/reports/verdicts/p58/VERDICT.md && grep -q "Verdict.*GREEN" quality/reports/verdicts/p58/VERDICT.md — exit 0.
10. python3 quality/runners/run.py --cadence pre-push — exit 0.
11. python3 quality/runners/run.py --cadence weekly — exit 0.
12. python3 -c "import json; d=json.load(open('quality/reports/badge.json')); assert d['color']=='brightgreen'" — exit 0.
13. bash scripts/banned-words-lint.sh — exit 0.
14. The HEAD commit lists 4 files (CLAUDE.md + STATE.md + SURPRISES.md + VERDICT.md). Commit message references RELEASE-04, QG-06, QG-07, QG-09 (P58), SIMPLIFY-04, SIMPLIFY-05, POLISH-RELEASE, POLISH-CODE.
</verification>

<success_criteria>
P58 closes GREEN. Every recurring criterion satisfied:

1. **Catalog-first** — Wave A landed the contract before any implementation; subsequent waves cited row IDs.
2. **CLAUDE.md update in same PR (QG-07)** — Wave F lands it. New "P58 — Release dimension live" H3 subsection appended; cross-references PROTOCOL.md; records absorption + recovery patterns.
3. **Verifier-subagent dispatch on phase close (QG-06)** — Wave F dispatches (Path A) or writes the in-session verdict with explicit disclosure (Path B). Verdict file at quality/reports/verdicts/p58/VERDICT.md.
4. **SIMPLIFY absorption** — Closed in Wave C (SIMPLIFY-04 + SIMPLIFY-05) and confirmed in Wave E (orphan-scripts removal). Both in-scope SIMPLIFY items ship.
5. **Fix every RED row** — Wave E was the broaden-and-deepen pass. Every P0+P1 row PASS or WAIVED. PARTIAL only on cargo-binstall-resolves (documented expected). Wave F re-confirms.

P58's scope (per traceability table in REQUIREMENTS.md v0.12.0):
- RELEASE-04 ✓ (release-dim gates + catalog rows + workflows live)
- SIMPLIFY-04 ✓ (clippy-lint-loaded migrated)
- SIMPLIFY-05 ✓ (check_fixtures audited + migrated Option A)
- POLISH-RELEASE ✓ (every release weekly row PASS or WAIVED; post-release PARTIAL documented)
- POLISH-CODE (P58 stub) ✓ (clippy + fixtures PASS; test + fmt WAIVED until P63)
- QG-09 (P58 GH Actions badge portion) ✓ (README + docs/index.md badge live)

5 active requirements scoped + POLISH-CODE-stub; 6 closed (POLISH-CODE-stub closure means stub is in place). Per-phase requirement count from REQUIREMENTS.md: P58=5 (RELEASE-04, SIMPLIFY-04..05, POLISH-RELEASE, POLISH-CODE-stub). Match (QG-09 is counted under P57 as primary owner; this phase ships its P58 portion).

P59 handoff: the next agent reads .planning/STATE.md cursor, then quality/reports/verdicts/p58/VERDICT.md, then quality/SURPRISES.md, then quality/PROTOCOL.md, then .planning/ROADMAP.md Phase 59 entry. P59 builds the docs-repro dimension (snippet extract + container rehearse + tutorial replay) + the agent-ux thin home (dark-factory).
</success_criteria>

<pivot_rules>
**Wave F pivot rules:**

- **If the verifier subagent grades RED**: phase does NOT close. Per PROTOCOL.md Step 7, executing agent does NOT get to talk the verifier out of RED. Either:
  - Fix the underlying RED rows (loop back to Wave E for the offending row); re-run runner; re-dispatch verifier.
  - File 90d-TTL waiver per the waiver protocol; re-dispatch verifier; if subagent accepts the waiver as principled, GREEN; if not, escalate.
  - Append YYYY-MM-DD P58 Wave F: verifier RED on row_id — resolution to SURPRISES.md.

- **If Task tool dispatch unavailable** (P56/P57 precedent): use Path B in-session verdict with full disclosure block. The orchestrator can re-grade out-of-session.

- **If CLAUDE.md update inadvertently uses banned words**: rewrite using "migrate to" / "for the 80%" / "rewrite as" / "complement" / "alongside". Append SURPRISES.md.

- **If pre-push hook GREEN regresses** (e.g., the new code-dim block flips RED on a row it was GREEN on in Wave E): the new runner block is fatal-on-failure for code-dim pre-push rows; fix or waive immediately. Most likely cause: Wave F's docs edit accidentally tripped the check.

- **If STATE.md frontmatter parse fails after edit**: Wave F MUST fix before commit. gsd-tools reads STATE.md frontmatter; a parse failure breaks every subsequent slash-command.

- **If the runner exits non-zero on weekly cadence at Wave F** (after Wave E proved GREEN): something regressed externally between Wave E and Wave F. Most likely cause: a per-crate release-plz publish moved the latest pointer (P56 SURPRISES.md row 1). Recovery: gh workflow run release.yml --ref reposix-cli-v<latest>. Wait for completion; re-run; confirm GREEN. Append SURPRISES.md.

Append any pivot to quality/SURPRISES.md per quality/PROTOCOL.md Step 9.
</pivot_rules>

<risk_table>
| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| Verifier subagent dispatch unavailable (Task tool not in agent toolset) | medium | low (Path B fallback covers it; P56/P57 precedent) | Plan explicitly accommodates Path B with disclosure block |
| Wave F's CLAUDE.md edit lands a banned word from research-doc copy | low | low | banned-words-lint runs in pre-commit verify |
| STATE.md frontmatter edit corrupts YAML | medium | high (breaks gsd-progress + every read of STATE.md) | Pre-commit yaml.safe_load check |
| Verifier sees a RED that Wave E's checkpoint missed | low | medium (forces a Wave E loop-back) | Wave E's checkpoint required runner exit 0; same check happens in Task C |
| Verdict file generated by an in-session Path B verdict lacks sufficient citations | medium | medium (out-of-session re-grade may diverge) | Path B template explicitly cites file:line for every claim per P56/P57 precedent |
| New CLAUDE.md content tips line cap or duplicates prior content | low | low | Wave F appends only; existing sections untouched |
| Pre-push hook check-mermaid-renders gate fires (false positive — new files don't touch mermaid) | very low | low | Hook only runs check-mermaid-renders if docs/ touched; Wave F doesn't touch docs/ |
| Cargo-binstall PARTIAL state changes between Wave E and Wave F | low | medium | PARTIAL is a documented expected state; the verifier subagent reads the catalog and sees the documentation; not a regression unless full FAIL |
| External release-plz publish between Wave E and Wave F moves the latest pointer | low | high (breaks asset URL gates) | Pivot rule above (gh workflow run release.yml recovery); document in SURPRISES.md |
</risk_table>

<output>
After completion, create .planning/phases/58-release-dimension-gates-code-absorption/58-06-SUMMARY.md with: commit SHA(s), files changed (4 expected), CLAUDE.md line count delta, SURPRISES.md P58 entries appended (count + summary), STATE.md progress numbers (before/after), verdict file path + GREEN/RED status, runner final exit codes (weekly/post-release/pre-push), badge.json color, recurring-criteria 1-5 closure summary, and a one-line "P58 SHIPPED — next: P59 docs-repro dimension + tutorial replay + agent-ux thin home" handoff.
</output>
