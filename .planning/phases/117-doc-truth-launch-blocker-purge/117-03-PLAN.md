---
phase: 117-doc-truth-launch-blocker-purge
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmarks/README.md
  - docs/social/twitter.md
  - quality/catalogs/doc-alignment.json
autonomous: true
requirements: [DOCS-05, DOCS-06]

must_haves:
  truths:
    - "benchmarks/README.md no longer claims reposix_session.txt is 'the literal output of scripts/demo.sh' (that script does not exist) and no longer points at a nonexistent RESULTS.md"
    - "docs/social/twitter.md no longer describes the deleted FUSE architecture"
    - "a NEW doc-alignment row covers benchmarks/README.md's methodology claim (catalog-first — the row is minted in THIS wave's first commit, predating the content edit)"
  artifacts:
    - path: "benchmarks/README.md"
      provides: "current P115 live-session-capture methodology description"
    - path: "docs/social/twitter.md"
      provides: "git-native partial-clone framing (no FUSE)"
    - path: "quality/catalogs/doc-alignment.json"
      provides: "new benchmarks/README-md/* row (minted_at set, next_action pending bind)"
  key_links:
    - from: "benchmarks/README.md"
      to: "quality/catalogs/doc-alignment.json benchmarks/README-md/session-provenance"
      via: "catalog-first row minted in the first commit, bound to bench_token_economy captures at W6 refresh"
      pattern: "benchmarks/README-md"
---

<objective>
Fix SC5 — the provenance fabrication and the deleted-FUSE social copy — AND close the gate gap that let SC5a drift undetected by minting the first doc-alignment row for benchmarks/README.md.

Purpose: `benchmarks/README.md:34` claims `reposix_session.txt` is "the literal output of `scripts/demo.sh`" — that script does NOT exist (RESEARCH §SC5a, three-way verified). CRITICAL: do NOT touch `docs/benchmarks/token-economy.md` — its provenance line (:78) was already fixed in P115 and is machine-generated; the REQUIREMENTS.md DOCS-05 citation is stale and following it literally would introduce a NEW lie. `docs/social/twitter.md:16` describes the pre-v0.9.0 FUSE architecture that was deleted.
Output: benchmarks/README.md rewritten, twitter.md de-FUSEd, one new catalog row.

Doc-only wave, no cargo. benchmarks/README.md is NOT in mkdocs nav (0 gate coverage today — that is WHY it drifted); its rewrite is a plain hand-edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-doc-truth-launch-blocker-purge/RESEARCH.md
@.planning/phases/117-doc-truth-launch-blocker-purge/PATTERNS.md

<interfaces>
SC5a fossil — benchmarks/README.md describes the ENTIRE retired pre-P115 methodology:
- :5 old 98.7%/"150,000 → ~2,000" figure (retired)
- :13 synthetic `mcp_jira_catalog.json` (superseded by real `mcp_github_catalog.json`)
- :21-25,:32 `ANTHROPIC_API_KEY`/`count_tokens` measurement (superseded by live-session-usage)
- :34 "**literal output of `scripts/demo.sh`**" (script absent — the lie)
- :39 "See `RESULTS.md`" (file absent — `find . -iname RESULTS.md` = 0 hits)
CURRENT truth to describe (mirror benchmarks/fixtures/README.md, already correct):
  reposix_session.txt = a REAL git-native reposix session against the live GitHub backend, captured P115 T4 (ANSI-stripped); no `/mnt/` paths, no `scripts/demo.sh`. Methodology = live session-usage capture, `benchmarks/captures/*.json`, offline regen via `quality/gates/perf/bench_token_economy.py --offline`.

SC5b — docs/social/twitter.md:16 "Result: **reposix** — a FUSE filesystem + git-remote-helper for issue trackers." (FUSE deleted v0.9.0 per filesystem-layer.md:46). twitter.md:18 carries the adjacent retired 89.1% figure. `docs/social/*` is `not_in_nav` (mkdocs.yml:56) — escapes ALL docs gates.

Catalog-first row template — copy the SHAPE of `filesystem-layer/blob-lazy-first-cat` (doc-alignment.json:8834): id, claim, source{file,line_start,line_end}, tests[], next_action, minted_at. `.source` may be object or array (GTH-V15-40) — use the object shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint the benchmarks/README.md doc-alignment row (catalog-first) THEN rewrite the file</name>
  <files>quality/catalogs/doc-alignment.json, benchmarks/README.md</files>
  <action>FIRST (catalog-first, same commit): hand-mint a new row in doc-alignment.json — id `benchmarks/README-md/session-provenance` (use the `README-md/` prefix per the legacy-majority convention, GTH-V15-39), claim = "reposix_session.txt is a real ANSI-stripped git-native reposix session captured against the live GitHub backend (P115 T4), regenerated offline via bench_token_economy.py --offline", source = benchmarks/README.md at the rewritten provenance line, tests = ["quality/gates/perf/bench_token_economy.py"] (or the captures test that regenerates the fixture), `minted_at` = today's RFC3339, `next_action` = "BIND" (the W6 coordinator refresh computes source_hash + binds). Do NOT invent a source_hash — leave it for bind.

  THEN rewrite benchmarks/README.md to describe the CURRENT P115 methodology (live session-usage capture, `benchmarks/captures/*.json`, offline regen), matching benchmarks/fixtures/README.md's already-correct framing. Strike the `scripts/demo.sh` line (:34), the dead `RESULTS.md` pointer (:39), the retired 98.7% figure (:5), the synthetic-fixture (:13) and `count_tokens` (:21-25,:32) fossils. Reference DOCS-05.</action>
  <verify>
    <automated>! grep -q "scripts/demo.sh" benchmarks/README.md && ! grep -q "RESULTS.md" benchmarks/README.md && echo "SC5a content PASS" || echo "SC5a FAIL" ; python3 -c "import json;d=json.load(open('quality/catalogs/doc-alignment.json'));ids=[r.get('id') for r in d.get('rows',d) if isinstance(r,dict)] if isinstance(d,(list,dict)) else [];print('row minted:', any('benchmarks/README-md/session-provenance'==i for i in ids))" 2>/dev/null || grep -q "benchmarks/README-md/session-provenance" quality/catalogs/doc-alignment.json && echo "row present"</automated>
  </verify>
  <done>benchmarks/README.md describes current methodology, no `scripts/demo.sh`/`RESULTS.md`; the new catalog row exists with `minted_at` and `next_action: BIND`.</done>
</task>

<task type="auto">
  <name>Task 2: SC5b — de-FUSE docs/social/twitter.md</name>
  <files>docs/social/twitter.md</files>
  <action>Rewrite line 16 from the FUSE framing to git-native partial-clone: e.g. "Result: **reposix** — a git-native partial clone + git-remote-helper for issue trackers." (mirror index.md:13's fixed form / filesystem-layer.md:46's "superseded that virtual filesystem"). Line 18's adjacent retired 89.1% figure sits in the same file, same edit pass — align it to the current CI-verified figure (index.md:17 uses "~94% fewer output tokens · ~75% cheaper") OR soften to "measured token savings (see the token-economy benchmark)"; do NOT leave the retired synthetic-fixture 89.1% standing. Reference DOCS-06. (twitter.md's one doc-alignment row `docs/social/twitter/token-reduction-92pct` is already flagged STALE_TEST_DRIFT — note it for W6.)</action>
  <verify>
    <automated>! grep -qi "FUSE" docs/social/twitter.md && echo "SC5b PASS: no FUSE" || echo "SC5b FAIL" ; grep -q "git-native\|partial clone" docs/social/twitter.md && echo "git-native framing present"</automated>
  </verify>
  <done>twitter.md has no FUSE reference, describes git-native partial clone, and the retired 89.1% figure is aligned or softened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Prose + catalog-JSON edits; no code path or input parsing. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-04 | Repudiation (doc-truth) | benchmarks/README.md provenance | mitigate | Removes a fabricated-provenance claim; the replacement is verified against benchmarks/fixtures/README.md + the actual fixture (RESEARCH §SC5a three-way). |
</threat_model>

<verification>
- No `scripts/demo.sh`, no `RESULTS.md`, no `FUSE` in the edited files.
- New catalog row loads (`python3 quality/runners/run.py --cadence pre-commit` does not reject it for a missing `minted_at`).
- Do NOT bind the new row here — W6 coordinator refresh binds it.
- OWNERSHIP CHARTER Noticing: file `render_results_markdown` dead code (`quality/gates/perf/bench_token_economy_io.py:291`, zero callers — the vestigial writer of the nonexistent RESULTS.md) as a NOTICED item / GOOD-TO-HAVE (or delete in 117-06 if <1h).
</verification>

<success_criteria>
SC5: benchmarks/README.md no longer fabricates demo-script provenance; twitter.md no longer describes deleted FUSE. The benchmarks/README.md gate gap is closed by a catalog-first doc-alignment row.
</success_criteria>

<output>
Create `.planning/phases/117-doc-truth-launch-blocker-purge/117-03-SUMMARY.md`. Targeted staging; no `--no-verify`; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Record the new row id + the STALE twitter row for the W6 refresh handoff.
</output>
