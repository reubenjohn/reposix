---
phase: 117-doc-truth-launch-blocker-purge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.md
  - docs/how-it-works/filesystem-layer.md
  - docs/reference/glossary.md
  - docs/reference/cli.md
  - docs/reference/git-remote.md
  - docs/reference/confluence.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "docs/index.md:13 describes Confluence as a wiki (distinct from issue trackers) and names `reposix init` (not `git clone`) as the bootstrap verb"
    - "filesystem-layer.md no longer claims `cat` secretly triggers a network call — in EITHER the summary (:7-13) or the body (:42) or the failure-mode bullet (:63)"
    - "the SC2 propagation set (glossary.md:20, cli.md:59, git-remote.md:23) reframes materialization to `git fetch`/`git checkout` time, never `cat` time"
  artifacts:
    - path: "docs/index.md"
      provides: "corrected hero sentence"
      contains: "reposix init"
    - path: "docs/how-it-works/filesystem-layer.md"
      provides: "truthful lazy-materialization framing bound to git checkout"
  key_links:
    - from: "docs/how-it-works/filesystem-layer.md:42"
      to: "quality/catalogs/doc-alignment.json filesystem-layer/blob-lazy-first-cat"
      via: "editing line 42 forces the BOUND row's re-extracted claim to become truthful (row REWRITE, completed at the W6 refresh)"
      pattern: "git checkout|git fetch"
---

<objective>
Fix the two highest-read doc-truth defects: SC1 (docs/index.md:13 — Confluence-as-issue-tracker + `git clone` bootstrap verb) and SC2 (the `cat`-secretly-networks lie in filesystem-layer.md + its REAL propagation set).

Purpose: index.md:13 is the site's most-read line and is currently FALSE; the `cat`-networks claim is architecture rot from the deleted-in-v0.9.0 FUSE layer and the doc-alignment machinery currently CERTIFIES it (row `filesystem-layer/blob-lazy-first-cat`).
Output: 6 edited doc pages. The catalog ROW REWRITE completes at the W6 coordinator refresh (this wave sets up the truthful source line 42 that the refresh re-extracts).

Doc-only wave, no cargo. Read each WHOLE page before editing — two defects sit on pages that ALSO contain already-correct copy (internal-consistency fixes, not blind line-patches — RESEARCH Furnished-Product #4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-doc-truth-launch-blocker-purge/RESEARCH.md
@.planning/phases/117-doc-truth-launch-blocker-purge/PATTERNS.md

<interfaces>
SC1 defect — docs/index.md:13 (verified current line, NOT the ticket's ~135-152):
> "reposix exposes REST-based issue trackers (Jira, GitHub Issues, Confluence) as a **real git working tree**. An autonomous LLM agent can `git clone`, `cat`, `grep`, edit, and `git push` tickets..."
Correct replacement (mirror README.md:5-8 phrasing):
> "reposix exposes REST-based issue trackers (Jira, GitHub Issues) and wikis (Confluence) as a **real git working tree**. An autonomous LLM agent can `reposix init`, `cat`, `grep`, edit, and `git push` tickets..."
DO NOT touch index.md:75 (the `git clone` inside the "Build from source (advanced)" <details> — it correctly clones the reposix TOOL source). index.md:79/:93/:162 already use `reposix init` — match them.

SC2 lie — filesystem-layer.md:7-10 ("the *first* read might secretly trigger a network call") + :42 ("The first `cat` … triggers one REST call") + :63 ("Network down on first read … the `cat` fails"). GROUND TRUTH (RESEARCH §SC2, verified via stateless_connect.rs): blob materialization is triggered by `git fetch`/`git checkout` (the `git checkout -B main refs/reposix/origin/main` step every quickstart runs right after `reposix init`), NEVER by the bare POSIX `cat`.
Correct summary sketch: "A reposix working tree is a real git checkout: `git checkout` (right after `reposix init`) is what pulls blob *contents* down, lazily and on demand for exactly the files being checked out. Once checked out, `cat` is a plain local file read — no network, ever."

SC2 propagation set (verified positive; the 4 files the brief named are verified-NEGATIVE — skip them):
- docs/reference/glossary.md:20 — "file *contents* arrive lazily on first `cat` or `grep`" (rows all RETIRE_CONFIRMED, low gate risk)
- docs/reference/cli.md:59 — "blobs are fetched on demand on first read" (inside BOUND row cli.md/init_documented, lines 42-65 → will drift → W6 refresh)
- docs/reference/git-remote.md:23 — "blobs are fetched lazily on first read" (BOUND row git-remote/stateless-connect-read-path at line 23 → will drift → W6 refresh)
- docs/reference/confluence.md:135-137 — borderline/lower-priority ("on first read … before the first git checkout"); tighten "on first read" phrasing only, it's already checkout-tied.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC1 — fix docs/index.md:13 hero (Confluence-is-a-wiki + `reposix init`)</name>
  <files>docs/index.md</files>
  <action>Rewrite the hero sentence at line 13 to the replacement above: Confluence grouped as a wiki distinct from issue trackers, bootstrap verb `reposix init` (not `git clone`). Leave line 75 (`git clone` of the tool source) untouched. Read the connector-capability matrix (index.md:130-137) to keep the fixed hero consistent with the page's own already-correct Confluence framing. Reference DOCS-01. NOTE: lines around 13 are BOUND doc-alignment rows (`docs/index/real-git-working-tree`, `docs/index/rest-supported-backends`) — this edit WILL trip them; that drift is budgeted for the W6 coordinator refresh, do NOT run any bind/refresh yourself.</action>
  <verify>
    <automated>sed -n '13p' docs/index.md | grep -q "reposix init" && sed -n '13p' docs/index.md | grep -q "wikis" && ! sed -n '13p' docs/index.md | grep -q "git clone" && echo "SC1 PASS" || echo "SC1 FAIL"</automated>
  </verify>
  <done>index.md:13 names `reposix init`, calls Confluence a wiki, and no longer says `git clone`; line 75 unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: SC2 — rewrite filesystem-layer.md off the `cat`-networks lie (summary + body + failure bullet)</name>
  <files>docs/how-it-works/filesystem-layer.md</files>
  <action>Rewrite the Plain-English summary (:7-13) to the truthful checkout-materializes framing (sketch above). CRITICAL: ALSO fix line 42 ("The first `cat` … triggers one REST call") to attribute materialization to the `git checkout`/`git fetch`, and line 63's failure-mode bullet ("Network down on first read … the `cat` fails") to say a `cat` AFTER a successful checkout cannot fail for network reasons (the fetch already happened at checkout). Fixing line 42 is REQUIRED, not optional: the doc-alignment row `filesystem-layer/blob-lazy-first-cat` is anchored there and currently certifies the lie — the brief mandates the row be REWRITTEN, which happens by the W6 refresh re-extracting the now-truthful line 42. Keep the page skeleton (Plain-English summary + `---` + mermaid + "What lives where" + "Failure modes" + glossary-anchored cross-links at :48,56,57). Reference DOCS-02.</action>
  <verify>
    <automated>grep -niE "secretly|secret.*network|becomes a REST call" docs/how-it-works/filesystem-layer.md | grep -vc '^[0-9]*:#' | { read n; [ "$n" -eq 0 ] && echo "SC2 PASS: no secret-network framing" || echo "SC2 FAIL: $n"; } ; sed -n '42p' docs/how-it-works/filesystem-layer.md | grep -qiE "checkout|fetch" && echo "line42 checkout-tied PASS" || echo "line42 FAIL"</automated>
  </verify>
  <done>No "secretly triggers a network call" framing remains anywhere in the file; line 42 attributes the REST call to git checkout/fetch; line 63 failure mode corrected; page structure intact.</done>
</task>

<task type="auto">
  <name>Task 3: SC2 propagation — glossary.md:20, cli.md:59, git-remote.md:23, confluence.md:135-137</name>
  <files>docs/reference/glossary.md, docs/reference/cli.md, docs/reference/git-remote.md, docs/reference/confluence.md</files>
  <action>Reframe each instance from "on first `cat`/first read" to "materializes at `git fetch`/`git checkout` time": glossary.md:20 ("first `cat` or `grep`" → "first `git checkout` of that path"); cli.md:59 ("fetched on demand on first read" → tie to git fetch/checkout); git-remote.md:23 ("fetched lazily on first read" → same); confluence.md:135-137 — lightest touch, just tighten "on first read" to "on first `git checkout`" (it already clarifies "before the first git checkout"). Do NOT edit the verified-negative files (index/git-layer/time-travel/trust-model already tie fetching to `git fetch` — RESEARCH §SC2). Keep glossary-relative-link style (`../reference/glossary.md#partial-clone`). NOTE: cli.md:59 (row cli.md/init_documented 42-65) and git-remote.md:23 (row git-remote/stateless-connect-read-path) are BOUND → drift budgeted for W6 refresh.</action>
  <verify>
    <automated>for f in docs/reference/glossary.md docs/reference/cli.md docs/reference/git-remote.md docs/reference/confluence.md; do echo "== $f =="; grep -niE "on first \`?cat|on first read|first \`cat\` or \`grep\`" "$f" | grep -vc '^[0-9]*:#' ; done ; bash quality/gates/docs-build/mkdocs-strict.sh 2>&1 | tail -3</automated>
  </verify>
  <done>None of the four files attribute materialization to `cat`/"first read"; mkdocs-strict green (structure/links intact).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Prose-only edits to published docs; no code path, no input parsing, no new trust boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-03 | Repudiation (doc-truth) | docs/index.md + filesystem-layer.md | mitigate | The whole POINT is truth-correction: fixing false claims a first-time agent would act on. Verified against code (stateless_connect.rs) per RESEARCH §SC2, not re-asserted from prose. |
</threat_model>

<verification>
- `bash quality/gates/docs-build/mkdocs-strict.sh` + `bash quality/gates/docs-build/link-resolution.py` green (no broken relative links).
- No page still says `cat` triggers/secretly-triggers a network call.
- DO NOT run doc-alignment `bind`/`walk` or `/reposix-quality-refresh` in this wave — the BOUND-row drift (index ×2, cli, git-remote, filesystem-layer blob-lazy REWRITE) is consolidated into the W6 coordinator refresh. Running walk.sh raw would mutate the committed catalog counters (quality/CLAUDE.md).
- OWNERSHIP CHARTER Noticing section required.
</verification>

<success_criteria>
SC1: index.md:13 correct on both Confluence-category and bootstrap-verb. SC2: filesystem-layer.md + the real propagation set no longer claim `cat` networks; line 42 rewritten so the BOUND row re-extracts to a truthful claim at W6 refresh.
</success_criteria>

<output>
Create `.planning/phases/117-doc-truth-launch-blocker-purge/117-02-SUMMARY.md`. Targeted staging only; no `--no-verify`; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. Record in the SUMMARY the exact list of doc-alignment rows this wave drifted (for the W6 refresh handoff).
</output>
