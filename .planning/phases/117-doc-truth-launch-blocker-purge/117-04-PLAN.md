---
phase: 117-doc-truth-launch-blocker-purge
plan: 04
type: execute
wave: 2
depends_on: [117-02]
files_modified:
  - docs/index.md
  - docs/how-it-works/filesystem-layer.md
  - docs/how-it-works/git-layer.md
  - docs/how-it-works/time-travel.md
  - docs/how-it-works/trust-model.md
autonomous: true
requirements: [GTH-V15-36]

must_haves:
  truths:
    - "docs/index.md keeps the 30-second path first + uncluttered; the alpha 'Honest scope' caveat is an admonition, not buried italic prose"
    - "the furnished-product vocabulary (grid cards for 'where to go next', admonitions for caveats) is no longer quarantined to index.md — the how-it-works quartet each gains a consistent nav-aid grid + at least one admonition where a caveat exists"
    - "a skeptical first-time reader lands on a cohesive, furnished surface, not plain-prose pages"
  artifacts:
    - path: "docs/index.md"
      provides: "IA/progressive-disclosure polish + Honest-scope admonition"
      contains: "!!! note"
    - path: "docs/how-it-works/git-layer.md"
      provides: "grid-card nav aid + admonition (leveled up to the filesystem-layer.md template)"
  key_links:
    - from: "docs/how-it-works/*.md"
      to: "docs/index.md:150-158"
      via: "propagated grid-card 'where to go next' block (the how-it-works quartet nav aid)"
      pattern: "grid cards"
---

<objective>
Deliver the furnished-product cold-reader/IA pass (GTH-V15-36) as a FIRST-CLASS wave, not leftovers. Owner mandate verbatim: *"Its good, but we can do so much better!"* The furnished-product feature set (grid cards, admonitions, tabs, details-folds, md-buttons) is QUARANTINED to index.md today — every other nav page is plain prose. That IS the gap.

Purpose: Clearing SC1-SC5 alone is explicitly NOT sufficient (GTH-V15-36). This wave (a) applies progressive disclosure on index.md, (b) propagates furnished-product polish to the how-it-works quartet.
Output: index.md IA polish + the how-it-works quartet leveled up to filesystem-layer.md's template shape.

Depends on 117-02 (index.md hero + filesystem-layer.md content already truth-corrected — polish on top of the fixed prose, do not re-introduce a fixed defect). Doc-only, no cargo.

NOTE — the `/doc-clarity-review` + `/reposix-quality-review --rubric` cold-reader passes required by GTH-V15-36 item (c) are TOP-LEVEL slash commands the EXECUTOR CANNOT self-run (depth-2 fan-out unreachable inside gsd-executor). They are sequenced as a COORDINATOR-RUN gate in 117-07. This wave produces the polished surfaces those reviews grade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-doc-truth-launch-blocker-purge/RESEARCH.md
@.planning/phases/117-doc-truth-launch-blocker-purge/PATTERNS.md

<interfaces>
Style exemplars to COPY (do not invent):
- docs/index.md — richest: grid cards (:15-21, :150-158), mermaid (:25-40), content tabs (:46-65), <details markdown> fold (:69-85), md-button CTAs (:106-107), blockquote hero (:11).
- docs/how-it-works/filesystem-layer.md — the how-it-works TEMPLATE (Plain-English lede + `---` + mermaid + "What lives where" + "Failure modes" + glossary-anchored cross-links).
- docs/concepts/reposix-vs-mcp-and-sdks.md:23 — `!!! note "About the MCP comparison (live, 2026-07-16)"` — the admonition-for-honesty pattern.
- Admonition convention: title in quotes + blank line + 4-space-indented body (first-run.md:132, integrate-with-your-agent.md:7). `admonition` + `pymdownx.details` + `attr_list` + `md_in_html` all enabled (mkdocs.yml:95-98).
Concrete targets (RESEARCH Furnished-Product §):
- index.md:168 alpha "Honest scope" italic paragraph → `!!! note "Honest scope"` admonition.
- index.md uses ZERO admonitions today despite the extension being enabled.
- Grid-card "Where to go next" pattern (index.md:150-158) → propagate to the bottom of filesystem-layer.md / git-layer.md / trust-model.md / time-travel.md as a "how-it-works quartet" nav aid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.md IA/progressive-disclosure polish</name>
  <files>docs/index.md</files>
  <action>Apply the progressive-disclosure decision table (global CLAUDE.md § Self-improving infrastructure): keep the 30-second path (hero → grid-card metrics → install tabs → "After — one commit") first and uncluttered; ensure advanced/edge material (build-from-source, deep config) stays collapsed below the fold (the 260716-fmt quick already relocated build-from-source — verify it stayed put, do not regress it). Convert the alpha "Honest scope" italic paragraph (~index.md:168) to `!!! note "Honest scope"`, matching reposix-vs-mcp-and-sdks.md:23. Audit index.md for any other buried caveat that reads better as an admonition. Do NOT alter the SC1-fixed hero (:13) content — only its presentation context. Reference GTH-V15-36 items 1-3.</action>
  <verify>
    <automated>grep -q "!!! note" docs/index.md && echo "admonition added" || echo "FAIL no admonition" ; grep -q "reposix init" docs/index.md && ! sed -n '13p' docs/index.md | grep -q "git clone" && echo "SC1 not regressed" ; bash quality/gates/docs-build/mkdocs-strict.sh 2>&1 | tail -2</automated>
  </verify>
  <done>index.md has ≥1 admonition (Honest scope), 30-second path stays first, SC1 fix intact, mkdocs-strict green.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate furnished-product polish to the how-it-works quartet</name>
  <files>docs/how-it-works/filesystem-layer.md, docs/how-it-works/git-layer.md, docs/how-it-works/time-travel.md, docs/how-it-works/trust-model.md</files>
  <action>Level the quartet up to filesystem-layer.md's template. To each of git-layer.md / time-travel.md / trust-model.md (and refine filesystem-layer.md): add a consistent "Where to go next" grid-card block at the bottom (copy the pattern from index.md:150-158) cross-linking the sibling how-it-works pages + relevant glossary/reference anchors; add at least one admonition where the page already has a caveat buried in prose (e.g. a `!!! warning`/`!!! note` for a known-limitation or a "this is not X" clarification). Keep every cross-link relative + glossary-anchored (`../reference/glossary.md#partial-clone`) so `docs-build/link-resolution` passes. Do NOT re-introduce the SC2 `cat`-networks framing into filesystem-layer.md (117-02 already removed it). Use fenced ```mermaid blocks only if adding a diagram — never hand-roll `<div class="mermaid">` (PATTERNS.md § Mermaid embed).</action>
  <verify>
    <automated>for f in git-layer time-travel trust-model filesystem-layer; do echo "== $f =="; grep -c "grid cards" docs/how-it-works/$f.md; done ; python3 quality/gates/docs-build/link-resolution.py 2>&1 | tail -3 ; bash quality/gates/docs-build/mkdocs-strict.sh 2>&1 | tail -2</automated>
  </verify>
  <done>Each how-it-works page has a "where to go next" grid + ≥1 admonition where a caveat exists; link-resolution + mkdocs-strict green; no SC2 regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-only doc polish; no code, no input parsing. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-05 | (n/a) | docs polish | accept | No security surface; risk is regression of a truth-fix — mitigated by the "do not re-introduce SC1/SC2 defect" verify gates. |
</threat_model>

<verification>
- mkdocs-strict + link-resolution green (any new diagram → a mermaid playwright artifact is owed, generated at the W6/W7 coordinator walk).
- SC1 (index.md:13) and SC2 (filesystem-layer.md) fixes NOT regressed.
- The cold-reader rubric passes (`/doc-clarity-review`, `/reposix-quality-review --rubric`) are COORDINATOR-RUN in 117-07 — this wave only builds the surfaces.
- OWNERSHIP CHARTER Noticing required (e.g. badge-resolution: index.md:7-9 + README.md:10-17 carry 4-7 badges — flag for the W7 `curl -I` check).
</verification>

<success_criteria>
GTH-V15-36 (executor-buildable portion): index.md progressive-disclosure + admonition; furnished-product vocabulary propagated off index.md onto the how-it-works quartet. A skeptical first-time reader meets a furnished surface. (Cold-reader rubric sign-off is the W7 coordinator gate.)
</success_criteria>

<output>
Create `.planning/phases/117-doc-truth-launch-blocker-purge/117-04-SUMMARY.md`. Targeted staging; no `--no-verify`; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. List index.md rows drifted by IA churn (~29 shift) for the W6 refresh handoff.
</output>
