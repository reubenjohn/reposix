---
phase: 117-doc-truth-launch-blocker-purge
plan: 07
type: execute
wave: 4
depends_on: [117-01, 117-02, 117-03, 117-04, 117-05, 117-06]
files_modified:
  - quality/catalogs/doc-alignment.json
  - .planning/verifications/playwright/index/animation.json
autonomous: false
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, DOCS-06, GTH-V15-36, GTH-V15-37]

must_haves:
  truths:
    - "every doc-alignment row this phase drifted is rebound green (no STALE_DOCS_DRIFT at pre-push)"
    - "the mp4 is live as a GitHub release asset and the index.md <video> loads it (playwright artifact proves it)"
    - "the cold-reader rubric + /doc-clarity-review sign off on index.md + README.md + rewritten landing surfaces"
    - "phase pushed to main BEFORE verifier dispatch; main's latest CI run is GREEN"
  artifacts:
    - path: ".planning/verifications/playwright/index/animation.json"
      provides: "playwright coverage artifact for the animation embed (satisfies docs-build/animation-renders)"
    - path: "quality/catalogs/doc-alignment.json"
      provides: "rebound rows (index ×2, filesystem-layer blob-lazy REWRITE, cli, git-remote, benchmarks/README-md bind, twitter)"
  key_links:
    - from: ".planning phase close"
      to: "origin/main + CI"
      via: "git push origin main BEFORE verifier; post-push run.py --cadence post-push asserts ci-green-on-main"
      pattern: "ci-green-on-main"
---

<objective>
COORDINATOR-RUN close wave. The gates that CANNOT self-resolve from inside the executor subagent tree (top-level slash commands + an external mutation) run here, then the phase pushes and dispatches the unbiased verifier.

Purpose: `/reposix-quality-refresh`, `/doc-clarity-review`, `/reposix-quality-review --rubric` are TOP-LEVEL slash commands (depth-2 fan-out unreachable inside gsd-executor, per .planning/CLAUDE.md); the mp4 release upload is an EXTERNAL MUTATION needing owner-named-target approval; the playwright artifact must be generated against the live release URL. All of these are the coordinator's job, not an executor's.
Output: rebound catalog, live mp4 + playwright artifact, cold-reader sign-off, green push.

## EXECUTION MODE: top-level / coordinator-run.
This plan is NOT dispatched to gsd-executor. The top-level coordinator runs it directly (the "orchestration-shaped phase" pattern: a pre-push that BLOCKS mid-execute is resolved by checkpointing + invoking the slash command from a fresh top-level session — .planning/CLAUDE.md). Every task below is tagged COORDINATOR-RUN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-doc-truth-launch-blocker-purge/RESEARCH.md
@.planning/CLAUDE.md
@quality/CLAUDE.md

<interfaces>
Drift set to refresh (consolidated from the W2/W3 SUMMARYs — RESEARCH § Doc-Alignment Drift Map):
- docs/index.md → rows `docs/index/real-git-working-tree` + `docs/index/rest-supported-backends` (line 13) + ~29 index rows shifted by IA churn (117-04).
- docs/how-it-works/filesystem-layer.md → row `filesystem-layer/blob-lazy-first-cat` — this is a REWRITE (the claim text must become truthful, not just re-hashed) — verify the re-extracted claim no longer says `cat` triggers the REST call.
- docs/reference/cli.md → `cli.md/init_documented` (42-65); docs/reference/git-remote.md → `git-remote/stateless-connect-read-path` (23).
- benchmarks/README.md → BIND the new `benchmarks/README-md/session-provenance` row (117-03 minted it, next_action BIND) — compute source_hash + bind to its test.
- docs/social/twitter.md → `docs/social/twitter/token-reduction-92pct` (already STALE_TEST_DRIFT).
- 5 pre-existing stale token-economy rows still asserting RETIRED 89.1%/85.5% figures — the doc content is already correct (P115); a refresh rebinds them. Fold into this pass IF <1h; else file to GOOD-TO-HAVES.
Refresh tool — `/reposix-quality-refresh <doc>` (top-level only). NEVER run the raw `reposix-quality doc-alignment walk` (mutates committed counters); grade via `bash quality/gates/docs-alignment/walk.sh` (grades against a /tmp copy).
Cold-reader — `/doc-clarity-review` (global slash command, root CLAUDE.md § Cold-reader) + `/reposix-quality-review --rubric <id>` (`.claude/skills/reposix-quality-review/dispatch.sh`).
Release upload — `gh release create docs-assets --title '...'` + `gh release upload docs-assets '/home/reuben/workspace/reposix-animation-pitch/Reposix Launch Animation.mp4'` — the EXACT tag + filename + resulting URL MUST match what 117-05 wrote into index.md's <video src> (read the 117-05 SUMMARY).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: COORDINATOR-RUN — owner-approved mp4 release upload (external mutation)</name>
  <what-built>117-05 wrote the index.md <video src> pointing at a planned GitHub release-asset URL (dedicated non-crate tag docs-assets/media-v1). The 7MB mp4 is NOT in the repo.</what-built>
  <how-to-verify>
    1. Read the 117-05 SUMMARY for the exact tag + filename + URL.
    2. Owner confirms the target tag (CLAUDE.md Non-negotiables: external mutations need owner-named-target approval).
    3. Coordinator runs: `gh release create <tag> --title 'Docs media assets'` (if absent) then `gh release upload <tag> '/home/reuben/workspace/reposix-animation-pitch/Reposix Launch Animation.mp4'`.
    4. `curl -I <asset-url>` returns 200; the URL byte-matches index.md's <video src>.
  </how-to-verify>
  <resume-signal>Owner types "approved &lt;tag&gt;" or names a different tag; coordinator uploads and confirms the URL resolves.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: COORDINATOR-RUN — consolidated /reposix-quality-refresh + playwright walk</name>
  <what-built>W2-W5 edited index.md, filesystem-layer.md, cli.md, git-remote.md, benchmarks/README.md, twitter.md, and added the animation embed — drifting the doc-alignment rows in the drift set above.</what-built>
  <how-to-verify>
    1. Run `/reposix-quality-refresh` for each drifted doc (consolidate index.md's 2 line-13 rows + ~29 IA rows in one pass). CONFIRM `filesystem-layer/blob-lazy-first-cat`'s re-extracted claim no longer certifies "cat triggers the REST call" (the REWRITE — grep the row's claim after refresh).
    2. BIND the new `benchmarks/README-md/session-provenance` row (compute source_hash, bind to its test).
    3. Fold the 5 stale token-economy rows IF the rebind is <1h; else file to GOOD-TO-HAVES.
    4. Generate the animation playwright artifact `.planning/verifications/playwright/index/animation.json` (video element present + poster resolves + zero console errors) against the LIVE release URL; re-walk any new mermaid diagrams from 117-04.
    5. `bash quality/gates/docs-alignment/walk.sh` exit 0 (no STALE_DOCS_DRIFT); `bash quality/gates/docs-build/mermaid-renders.sh` + the new `docs-build/animation-renders` row green.
  </how-to-verify>
  <resume-signal>Type "approved" when walk.sh + mermaid-renders + animation-renders all green and the blob-lazy claim reads truthfully.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: COORDINATOR-RUN — cold-reader rubric + badge resolution (GTH-V15-36 sign-off)</name>
  <what-built>117-02/03/04 truth-corrected + furnished index.md, README.md, filesystem-layer.md + the how-it-works quartet; 117-05 embedded the animation.</what-built>
  <how-to-verify>
    1. Run `/doc-clarity-review` on docs/index.md + README.md + every page this phase substantially rewrote (filesystem-layer.md, benchmarks/README.md, the how-it-works quartet). The EXECUTOR cannot self-run this — it is a top-level slash command.
    2. Run `/reposix-quality-review --rubric <cold-reader-id>` (or `--all-stale`).
    3. Badge resolution: `curl -I` each badge URL in index.md:7-9 + README.md:10-17 (global CLAUDE.md OP-1); all resolve.
    4. Address any HIGH/MEDIUM cold-reader findings in place (or file to GOOD-TO-HAVES if >1h + no new dependency).
  </how-to-verify>
  <resume-signal>Type "approved" when the cold-reader rubric passes and all badges resolve (or findings are dispositioned).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: COORDINATOR-RUN — full pre-push, push, post-push CI, verifier dispatch</name>
  <what-built>All W1-W6 commits + the W7 catalog rebind + playwright artifact are staged/committed.</what-built>
  <how-to-verify>
    1. `python3 quality/runners/run.py --cadence pre-push` — all gates PASS (incl. the new structure/social-freshness + docs-build/animation-renders rows).
    2. `git push origin main` (BEFORE the verifier — push cadence; serialize the push machine-wide, it is a hidden cargo lane).
    3. `python3 quality/runners/run.py --cadence post-push --persist` — `code/ci-green-on-main` (P0) asserts main's NEWEST ci.yml run concluded success.
    4. Dispatch the unbiased gsd-verifier subagent (zero session context) to grade the phase catalog rows goal-backward against SC1-SC5 + GTH-V15-36 + GTH-V15-37. RED loops back.
  </how-to-verify>
  <resume-signal>Type "approved" when pushed, main CI green, and the verifier grades the phase GREEN.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local repo → GitHub (push + release) | external mutations: git push, gh release upload |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-09 | Elevation / unauthorized mutation | gh release upload | mitigate | Owner-named-target approval gate (Task 1) before any external write; dedicated non-crate tag isolates from release-plz crate automation. |
| T-117-10 | Repudiation | phase-close honesty | mitigate | Unbiased verifier subagent grades from committed artifacts with zero session context; push-before-verifier + ci-green-on-main are hard gates. |
</threat_model>

<verification>
- doc-alignment walk.sh exit 0 (no STALE_DOCS_DRIFT); blob-lazy row claim reads truthfully.
- animation playwright artifact present; docs-build/animation-renders + mermaid-renders green.
- cold-reader rubric passed; badges resolve.
- Pushed BEFORE verifier; post-push ci-green-on-main P0 green.
- Verifier grades SC1-SC5 + GTH-V15-36 + GTH-V15-37 goal-backward → GREEN.
</verification>

<success_criteria>
All six doc-truth defects no longer mislead a first-time reader/agent; the docs read as a furnished product (cold-reader signed off); the launch animation is live (poster+click-to-play, mp4 as a release asset); every drifted doc-alignment row is rebound; phase pushed green.
</success_criteria>

<output>
Create `.planning/phases/117-doc-truth-launch-blocker-purge/117-07-SUMMARY.md` and the phase-level close artifacts. Targeted staging; no `--no-verify`; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
</output>
