---
phase: 117-doc-truth-launch-blocker-purge
plan: 06
type: execute
wave: 2
depends_on: [117-03]
files_modified:
  - quality/gates/structure/social-freshness.sh
  - quality/gates/structure/social-freshness.selftest.sh
  - quality/catalogs/freshness-invariants.json
  - quality/gates/perf/bench_token_economy_io.py
  - .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md
autonomous: true
requirements: [GTH-V15-36, DOCS-06]

must_haves:
  truths:
    - "a minimal freshness-grep gate now scans docs/social/** for known-dead terms (FUSE, /mnt/, mount) — closing the gate gap that let the stale-FUSE line reach launch"
    - "the vestigial render_results_markdown dead code (zero callers) is removed"
    - "any CLAUDE.md (root/scoped) asserting a now-corrected doc-truth is fixed (fix-twice meta-rule)"
  artifacts:
    - path: "quality/gates/structure/social-freshness.sh"
      provides: "the docs/social/** dead-term freshness gate"
    - path: "quality/catalogs/freshness-invariants.json"
      provides: "structure/social-freshness row (catalog-first, minted_at)"
  key_links:
    - from: "quality/catalogs/freshness-invariants.json structure/social-freshness"
      to: "quality/gates/structure/social-freshness.sh"
      via: "runner discovers the verifier by dimension tag; fires at pre-push"
      pattern: "social-freshness"
---

<objective>
Close the fix-twice loops (CLAUDE.md meta-rule) this phase opened: build the minimal docs/social/** freshness-grep gate (the backstop for exactly the class of bug — a stale-FUSE line escaping ALL docs gates — this phase is purging), delete adjacent dead code, and update any CLAUDE.md that still asserts a now-corrected doc-truth.

Purpose: `docs/social/*` is `not_in_nav` (mkdocs.yml:56) so mkdocs-strict / link-resolution / doc-alignment / banned-words never scan it — that is WHY the SC5b stale-FUSE line survived to launch (PATTERNS.md NOTICED #3). Fix-twice: fix the line (117-03) AND add the gate so it can't recur.
Output: new structure-dimension gate + catalog row + selftest, dead-code deletion, CLAUDE.md fix-twice sweep, one SURPRISES-INTAKE filing.

Depends on 117-03 (twitter.md already de-FUSEd — else the new gate would fail on the stale line it was built to catch). Doc/shell/py only, no cargo.

## FRESHNESS-GATE SIZING DECISION: BUILD in-phase.
A ~15-line grep-over-`docs/social/**` shell gate + one freshness-invariants row + a selftest is genuinely <1h and adds no dependency (models `no-loose-top-level-planning-audits.sh`). Per the CLAUDE.md meta-rule "fix it twice," this is the durable backstop, not a filing. (If mid-build it balloons past ~1h — e.g. shell-coverage ratchet complications — STOP and file to GOOD-TO-HAVES with the sketch instead.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-doc-truth-launch-blocker-purge/RESEARCH.md
@.planning/phases/117-doc-truth-launch-blocker-purge/PATTERNS.md
@quality/CLAUDE.md

<interfaces>
Gate analog — quality/gates/structure/no-loose-top-level-planning-audits.sh (simple grep-based structure gate, exit 1 on violation with a teaching message). Catalog analog — freshness-invariants.json `structure/banned-words` row (dimension "structure", cadences incl. pre-push, `minted_at`, verifier script path). Selftest analog — quality/gates/structure/file-size-limits.selftest.sh (builds a throwaway /tmp repo, NEVER the shared repo — leaf isolation).
Dead code — quality/gates/perf/bench_token_economy_io.py:291 `render_results_markdown` (exported, ZERO callers; the vestigial writer of the nonexistent RESULTS.md — RESEARCH §SC5a NOTICED).
CLAUDE.md fix-twice targets — grep root + scoped CLAUDE.md for any surviving assertion of a now-corrected lie: `FUSE`, `secretly.*network`, `cat.*triggers.*network`, `scripts/demo.sh`, `reposix detach`, Confluence-as-issue-tracker. (Root CLAUDE.md elevator pitch already says "wikis" + git-native — likely minimal, but the meta-rule requires the sweep.)
Grep-gate hygiene: count with `grep -v '^#' | grep -c token` (header prose triggers self-invalidating grep gates).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the docs/social/** freshness gate (catalog-first) + selftest</name>
  <files>quality/catalogs/freshness-invariants.json, quality/gates/structure/social-freshness.sh, quality/gates/structure/social-freshness.selftest.sh</files>
  <action>Catalog-first: add a `structure/social-freshness` row to freshness-invariants.json (dimension "structure", verifier `quality/gates/structure/social-freshness.sh`, cadences `["pre-push"]`, `minted_at` today RFC3339, expected.asserts = "no docs/social/**/*.md contains a known-dead architecture term (FUSE, /mnt/, mount)"). THEN write social-freshness.sh: grep `docs/social/**/*.md` for the dead terms (case-insensitive, but scope `mount` carefully to avoid false hits on "amount"/"paramount" — anchor to `\bFUSE\b`, `/mnt/`, `\bmount\b`), exit 1 with a teaching message naming the file:line + the fix if any match; exit 0 otherwise. Write social-freshness.selftest.sh building a throwaway /tmp repo (leaf isolation — `cd /tmp/...` in the SAME invocation) with a planted `FUSE` line to prove the gate BLOCKS, and a clean fixture to prove it PASSES.</action>
  <verify>
    <automated>bash quality/gates/structure/social-freshness.sh && echo "gate PASS on clean tree (twitter.md already fixed)" ; bash quality/gates/structure/social-freshness.selftest.sh && echo "selftest PASS (blocks planted FUSE, passes clean)" ; grep -q "structure/social-freshness" quality/catalogs/freshness-invariants.json && grep -A15 "structure/social-freshness" quality/catalogs/freshness-invariants.json | grep -q "minted_at" && echo "row catalog-first"</automated>
  </verify>
  <done>Gate passes on the (117-03-fixed) tree, selftest proves block-on-planted-term + pass-on-clean, catalog row minted with minted_at.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead code + CLAUDE.md fix-twice sweep + SURPRISES filing</name>
  <files>quality/gates/perf/bench_token_economy_io.py, .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md</files>
  <action>(a) Delete `render_results_markdown` from bench_token_economy_io.py:291 (zero callers — confirm with `grep -rn render_results_markdown quality/ crates/` = 1 hit, the def itself, before removing). (b) Grep root + scoped CLAUDE.md files for surviving now-corrected-lie assertions (FUSE/secretly-networks/scripts/demo.sh/reposix detach/Confluence-as-issue-tracker); fix any in place per the fix-twice meta-rule (edit each affected CLAUDE.md — do NOT append a narrative). If none found, record "CLAUDE.md sweep: clean, no stale doc-truth assertions" in the SUMMARY. (c) File to SURPRISES-INTAKE (severity + sketch): the leaf-isolation hook false-positives on grep patterns CONTAINING setup-verb strings (`reposix init`/`git config` inside a grep argument) — a guardrail-correctness bug (this very phase's grep-based verify steps risk tripping it). RESEARCH/brief item (c).</action>
  <verify>
    <automated>[ "$(grep -rc render_results_markdown quality/ crates/ 2>/dev/null | grep -v ':0$' | wc -l)" -eq 0 ] && echo "dead code removed" || echo "still referenced" ; python3 -c "import ast;ast.parse(open('quality/gates/perf/bench_token_economy_io.py').read())" && echo "py still parses" ; grep -qi "leaf-isolation.*false-positive\|false-positive.*grep" .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md && echo "SURPRISES filed"</automated>
  </verify>
  <done>render_results_markdown gone (file still parses), CLAUDE.md sweep done (fixed or recorded clean), leaf-isolation-hook false-positive filed to SURPRISES-INTAKE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI gate corpus | new shell gate runs at pre-push over docs/social/** |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-08 | Denial of service (false gate block) | social-freshness.sh `\bmount\b` | mitigate | Anchor patterns to word boundaries + `/mnt/` path form to avoid false hits on amount/paramount; selftest proves both block + pass paths. |
</threat_model>

<verification>
- New gate passes on the fixed tree; selftest proves it blocks a planted dead term.
- `python3 quality/runners/run.py --cadence pre-push` discovers + runs the new row (no audit-field rejection; minted_at present).
- Dead code removed; py still parses.
- OWNERSHIP CHARTER Noticing required.
</verification>

<success_criteria>
Fix-twice complete: the docs/social/** gate gap is closed with a catalog-first structure gate + selftest; dead code removed; CLAUDE.md doc-truth assertions swept; guardrail-correctness bug filed.
</success_criteria>

<output>
Create `.planning/phases/117-doc-truth-launch-blocker-purge/117-06-SUMMARY.md`. Targeted staging; no `--no-verify`; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
</output>
