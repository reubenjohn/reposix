---
phase: 117-doc-truth-launch-blocker-purge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/reposix-cli/src/list.rs
  - crates/reposix-cli/src/refresh.rs
  - crates/reposix-cli/src/attach.rs
  - .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md
autonomous: true
requirements: [DOCS-03, DOCS-04]

must_haves:
  truths:
    - "`reposix list` against a dead sim backend prints an error that names the likely cause (sim not running / wrong --origin), suggests `reposix sim`, and gives a copy-paste retry command"
    - "`reposix refresh` against a dead sim backend prints the SAME teach-the-fix error"
    - "`reposix attach` multi-SoT-conflict error no longer references the nonexistent `reposix detach` subcommand; it points at a recovery that works today"
  artifacts:
    - path: "crates/reposix-cli/src/list.rs"
      provides: "teach-the-fix bail! on sim connection failure"
      contains: "reposix sim"
    - path: "crates/reposix-cli/src/refresh.rs"
      provides: "identical teach-the-fix wrap on sim connection failure"
      contains: "reposix sim"
    - path: "crates/reposix-cli/src/attach.rs"
      provides: "multi-SoT error with a real recovery path (no phantom detach)"
  key_links:
    - from: "crates/reposix-cli/src/list.rs"
      to: "crates/reposix-cli/src/init.rs:365-373"
      via: "copies the 3-part error-message bar (teach fix / suggest alt / copy-paste recovery)"
      pattern: "reposix sim"
---

<objective>
Un-strand the two connection-refused errors (`reposix list`, `reposix refresh`) so they meet the Rust-compiler-grade 3-part bar (SC3/DOCS-03), and remove the phantom `reposix detach` reference from the multi-SoT-conflict error (SC4/DOCS-04, Option B baseline).

Purpose: These are the DEFAULT-backend (sim) failure paths every quickstart hits; today they surface a raw `Connection refused` under a terse `.context()` that teaches nothing.
Output: Three edited Rust files in `reposix-cli`, one GOOD-TO-HAVES filing for the deferred Option A.

**This is the ONLY cargo-touching wave.** Per the machine-wide one-cargo rule, this plan MUST run alone (no other executor holding the cargo/push token concurrently). Verify with `cargo` scoped to `-p reposix-cli`, never `--workspace`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-doc-truth-launch-blocker-purge/RESEARCH.md
@.planning/phases/117-doc-truth-launch-blocker-purge/PATTERNS.md
@crates/CLAUDE.md

<interfaces>
GOLD exemplar to copy — `crates/reposix-cli/src/init.rs:363-373` (the fetch-failure bail!):
```rust
bail!(
    "reposix init: could not sync `{path_str}` from backend `{url}`.\n\
     The repo was configured but nothing was fetched, so it has no commits yet.\n\
     git stderr:\n  {stderr}\n\
     Fix: confirm the backend is running and reachable — for the simulator, start it in \
     another terminal with `reposix sim` — then re-run `reposix init`, or sync in place \
     with `git -C {path_str} fetch --filter=blob:none origin`.",
);
```
Current SC3 defect — `list.rs:75-81` (Sim arm) and `refresh.rs:205-210` (Sim arm):
```rust
b.list_records(&project).await
    .with_context(|| format!("sim list_records project={project}"))?   // terse; surfaces raw Connection refused
```
Current SC4 defect — `attach.rs:142-145`:
```rust
bail!(
    "working tree already attached to {existing_sot}; multi-SoT not supported in v0.13.0 (Q1.2). \
     Run `reposix detach` first or pick the existing SoT."   // `reposix detach` does not exist anywhere
);
```
`grep -rn "Cmd::Detach\|Detach" crates/reposix-cli/src/main.rs` = 0 hits (confirmed no clap variant).
Live error text captured against a closed port (RESEARCH §SC3): 4-level `Caused by` chain ending in `Connection refused (os error 111)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Teach-the-fix bail! on the sim connection failure in list.rs</name>
  <files>crates/reposix-cli/src/list.rs</files>
  <behavior>
    - When `SimBackend::list_records` fails with a connect error (sim down / wrong --origin), the surfaced error names the likely cause, suggests starting the sim in another terminal with `reposix sim`, and gives the literal copy-paste retry command (`reposix list --project {project} --origin {origin}`).
    - The github/confluence/jira arms are UNCHANGED (they already inline the REPOSIX_ALLOWED_ORIGINS hint; out of scope for this task).
    - Non-connect sim errors still surface (do not swallow other failure classes into a "sim is down" message).
  </behavior>
  <action>Replace the terse `.with_context(|| format!("sim list_records project={project}"))?` at `list.rs:78-80` with a teaching wrap that copies the 3-part bar from `init.rs:365-373`: (1) name the cause — the sim backend at `{origin}` is unreachable (connection refused); (2) suggest the alternative — start the default simulator in another terminal with `reposix sim` (exact phrasing used at README.md:61-63); (3) copy-paste recovery — `reposix list --project {project} --origin {origin}`. Prefer decorating with `.with_context(...)` carrying all three lines (keeps the underlying reqwest chain visible under `Caused by:`) OR match on the connect-error class and `bail!` — either meets the bar; do NOT hide the root reqwest cause. Reference DOCS-03. Keep `{origin}`/`{project}` interpolated so the message is self-diagnosing.</action>
  <verify>
    <automated>cargo test -p reposix-cli list 2>&1 | tail -20 ; echo "--- manual smoke (LEAF ISOLATION: /tmp, cd in SAME invocation) ---" ; ( cd /tmp && cargo run -q -p reposix-cli --manifest-path /home/reuben/workspace/reposix/Cargo.toml -- list --project demo --origin http://127.0.0.1:19999 2>&1 | grep -q "reposix sim" && echo "SC3-list PASS: teaches sim recovery" || echo "SC3-list FAIL" )</automated>
  </verify>
  <done>`reposix list` against a closed port prints a message containing "reposix sim" AND the copy-paste `reposix list --project ... --origin ...` retry line; `cargo test -p reposix-cli` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Identical teach-the-fix wrap in refresh.rs</name>
  <files>crates/reposix-cli/src/refresh.rs</files>
  <behavior>
    - `reposix refresh` sim connection failure produces text IDENTICAL in structure to Task 1 (same three parts), retry command adapted to `reposix refresh` args.
    - Mirrors the existing teaching-bail! tone already present at `refresh.rs:227,250` (env-var gaps).
  </behavior>
  <action>Apply the same wrap as Task 1 to the Sim arm at `refresh.rs:205-210` (`fetch_issues`). Reuse the EXACT recovery-command wording chosen in Task 1 so `list` and `refresh` read consistently (a reader who saw one recognizes the other). Retry line: `reposix refresh --project {project} --origin {origin} <path>`. Reference DOCS-03.</action>
  <verify>
    <automated>cargo test -p reposix-cli refresh 2>&1 | tail -20 ; ( cd /tmp && mkdir -p rpx-sc3-refresh && cd rpx-sc3-refresh && cargo run -q -p reposix-cli --manifest-path /home/reuben/workspace/reposix/Cargo.toml -- refresh --project demo --origin http://127.0.0.1:19999 . 2>&1 | grep -q "reposix sim" && echo "SC3-refresh PASS" || echo "SC3-refresh FAIL" )</automated>
  </verify>
  <done>`reposix refresh` against a closed port teaches the sim recovery identically to `list`; crate tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Reword the multi-SoT-conflict error (SC4 Option B) + file Option A</name>
  <files>crates/reposix-cli/src/attach.rs, .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md</files>
  <action>SC4 baseline is Option B (research-recommended; see § SC4 DECISION below). Rewrite `attach.rs:142-145`'s `bail!` so it (a) states multi-SoT is unsupported in this line, (b) points at a recovery that WORKS TODAY — the manual recipe already documented at `troubleshooting.md:329`: `git remote remove <remote_name>` then re-run `reposix attach` with the new SoT (optionally remove the `extensions.partialClone` config + cache dir). Use the actual `{args.remote_name}` so the command is copy-paste-ready. Remove every mention of `reposix detach`. Reference DOCS-04.

  Then append an Option-A GOOD-TO-HAVE (`GTH-V15-4x — real `reposix detach` subcommand`): severity LOW, ~3-5h, DRIFTS the decision-009 LOCKED CLI-surface row; scope sketch = one `Cmd::Detach { path: Option<PathBuf> }` enum arm (`main.rs:40`+) + one dispatch arm + a new ~80-120-line `detach.rs` reusing `worktree_helpers::cache_path_from_worktree` + `doctor.rs`'s `git_config_get/set` pattern (unset `extensions.partialClone`, remove the reposix remote, optionally delete cache dir). Note the tradeoff: Option A makes the error's promise real but adds CLI surface to a doc-truth phase.</action>
  <verify>
    <automated>grep -n "detach" crates/reposix-cli/src/attach.rs | grep -vc '^#' | { read n; [ "$n" -eq 0 ] && echo "SC4 PASS: no detach reference in attach.rs" || echo "SC4 FAIL: $n detach refs remain"; } ; grep -q "git remote remove" crates/reposix-cli/src/attach.rs && echo "recovery present" ; cargo check -p reposix-cli 2>&1 | tail -5</automated>
  </verify>
  <done>`grep detach crates/reposix-cli/src/attach.rs` = 0; the error names `git remote remove {remote_name}` + re-attach; `cargo check -p reposix-cli` clean; Option-A GTH filed with the scope sketch.</done>
</task>

</tasks>

<sc4_decision>
## SC4 DECISION — Option B baseline; Option A is a PURELY ADDITIVE wave

Research (RESEARCH §SC4, Open Question #1) recommends Option B (reword now) and flags the fork as a RAISE for the coordinator/owner. This plan implements Option B.

**Coordinator-run BEFORE this wave executes (not the executor's call):** confirm with L0/owner that Option B is accepted. Research recommends B; there is NO existing ruling constraining it (PATTERNS.md's `CONSULT-DECISIONS.md:146` citation is a DIFFERENT SC4 — the P116 FIX-03 ruling — see RESEARCH §SC4 correction).

**Additive-structure guarantee (why Option A stays cheap if L0 overrides):**
- Option B lives ENTIRELY in the `attach.rs:142-145` string. NO other plan in this phase (117-02..07) reads or depends on the attach error text — verified: it is not a doc-alignment BOUND row (0 rows for `attach.rs`), not cross-linked, not a gate input.
- If L0 chooses Option A, the coordinator inserts a NEW additive plan `117-01a-detach-subcommand-PLAN.md` that ADDS `detach.rs` + the `Cmd::Detach` enum/dispatch arm (net-new files/lines only), and re-points this one `attach.rs` string to the now-real `reposix detach`. Rework cost = ~2 lines. Nothing else re-does.
- Therefore Option B is intentionally NOT entangled with W2–W7; the override is additive, not a rewrite.
</sc4_decision>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| backend REST → CLI error surface | reqwest error text (incl. `{origin}`) is interpolated into a user-facing error string |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Information disclosure | list.rs/refresh.rs error string | accept | `{origin}` is the user's own supplied `--origin`; no tainted backend BODY bytes are routed into the message (only the connect-failure class + the user's own args). No secret env values interpolated. |
| T-117-02 | Tampering | attach.rs recovery command | mitigate | Recovery command uses `{args.remote_name}` (validated clap arg), not attacker-influenced remote content; no shell-eval of remote data. |
</threat_model>

<verification>
- `cargo test -p reposix-cli` green (scoped, never `--workspace`).
- Both closed-port smoke checks emit "reposix sim" + a copy-paste retry line (run inside `/tmp` per leaf isolation).
- `grep detach crates/reposix-cli/src/attach.rs` = 0.
- OWNERSHIP CHARTER: report a Noticing section (e.g. NOTICED #4 stale `main.rs:4-12` module-doc listing only 6 of 9+ subcommands — eager-fix the 1-liner IF this wave already opens main.rs for anything, else file).
</verification>

<success_criteria>
SC3: `reposix list` + `reposix refresh` connection-refused errors teach the fix, suggest the sim alternative, and give copy-paste recovery — matching the `init.rs:365-373` bar. SC4: `attach.rs` multi-SoT error references no nonexistent subcommand and points at a working recovery; Option A filed.
</success_criteria>

<output>
After completion, create `.planning/phases/117-doc-truth-launch-blocker-purge/117-01-SUMMARY.md`. Commit with targeted staging only (never `git add -A`/`.`), no `--no-verify`, trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
</output>
