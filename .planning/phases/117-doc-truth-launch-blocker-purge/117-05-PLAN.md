---
phase: 117-doc-truth-launch-blocker-purge
plan: 05
type: execute
wave: 3
depends_on: [117-04]
files_modified:
  - docs/index.md
  - docs/assets/animation/reposix-launch.thumbnail.webp
  - mkdocs.yml
  - quality/catalogs/docs-build.json
  - .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md
autonomous: true
requirements: [GTH-V15-37]
user_setup:
  - service: github-release
    why: "Host the 7MB mp4 as a GitHub release asset (never committed — file-size gate) — an EXTERNAL MUTATION requiring owner-named-target approval"
    dashboard_config:
      - task: "Approve + create a dedicated non-crate release tag (docs-assets or media-v1) and upload the mp4 — COORDINATOR-RUN in 117-07, not this wave"
        location: "gh release create docs-assets && gh release upload docs-assets 'Reposix Launch Animation.mp4'"

must_haves:
  truths:
    - "docs/index.md embeds the launch animation as a poster + click-to-play <video> (no autoplay, no CDN Babel/React compile, no live CDN icon calls)"
    - "the mp4 is NOT committed to the repo — it is referenced from a GitHub release asset URL"
    - "a net-new docs/assets/animation/ convention exists (poster WebP committed, Zone.Identifier stripped)"
    - "a catalog-first docs-build row covers the animation page's playwright artifact"
  artifacts:
    - path: "docs/assets/animation/reposix-launch.thumbnail.webp"
      provides: "poster frame (3158 B, Windows Zone.Identifier stripped)"
    - path: "docs/index.md"
      provides: "poster+click-to-play <video> embed"
      contains: "<video"
    - path: "quality/catalogs/docs-build.json"
      provides: "new docs-build/animation-renders row (minted_at, artifact path)"
  key_links:
    - from: "docs/index.md"
      to: "GitHub release asset (docs-assets/media-v1 tag) mp4"
      via: "<video src> pointing at the release download URL; poster is the local committed WebP"
      pattern: "<video"
---

<objective>
Embed the owner's 80s launch animation on docs/index.md (GTH-V15-37), P117 BASELINE = mp4 poster + click-to-play. Establish the net-new `docs/assets/animation/` convention. File the interactive JSX/esbuild embed as a stretch GOOD-TO-HAVE.

Purpose: The interactive dc-runtime embed has a hard blocker — the build toolchain is ABSENT (only the generated support.js ships; it loads React+ReactDOM+Babel from unpkg and runtime-transpiles 3 .jsx) and it makes live `cdn.simpleicons.org` calls (hermetic smell). The self-contained mp4 click-to-play passes gates with zero live CDN dependency and is the correct launch baseline.
Output: poster committed under a new assets convention, `<video>` embed in index.md, mkdocs wiring, a catalog-first playwright row, two stretch GTH filings.

Depends on 117-04 (index.md IA polish applied — the embed slots into the furnished layout). Doc-only, no cargo.

## COORDINATOR-RUN, NOT EXECUTOR (deferred to 117-07):
- `gh release create/upload` of the 7MB mp4 — EXTERNAL MUTATION needing owner-named-target approval (CLAUDE.md Non-negotiables; RESEARCH Open Question #2). Use a DEDICATED non-crate tag (`docs-assets`/`media-v1`), NOT a `v0.15.x` crate tag (release-plz.toml keeps `git_release_enable=false` for crate tags).
- Generating the playwright coverage artifact against the live release URL (the video loads only once the asset exists).
This wave writes the embed against the AGREED release-asset URL and mints the row; the coordinator uploads + walks in 117-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-doc-truth-launch-blocker-purge/RESEARCH.md
@.planning/phases/117-doc-truth-launch-blocker-purge/PATTERNS.md

<interfaces>
Source (read-only, DO NOT copy the mp4 into the repo): /home/reuben/workspace/reposix-animation-pitch/
- `Reposix Launch Animation.mp4` = 7,145,308 B (~6.8 MiB) → GitHub release asset only.
- `.thumbnail` = 3,158 B WebP → poster frame (COMMIT this, tiny; strip its `:Zone.Identifier` first).
- `*.mp4:Zone.Identifier` + `uploads/*.png:Zone.Identifier` = Windows metadata, MUST NOT enter the repo.
mkdocs wiring analog — mkdocs.yml:106-113 `extra_javascript` self-host pattern. Embed-mechanics analog — index.md:15 `<div class="grid cards" markdown>` (md_in_html + attr_list, both enabled at mkdocs.yml:97-98).
Catalog row template — docs-build.json:42-76 `docs-build/mermaid-renders`: kind `mechanical`, artifact `.planning/verifications/playwright/<section>/<slug>.json`, cadences `[pre-push, pre-pr]`, `minted_at` in the SAME first commit (catalog-first). Playwright pattern = source→artifact assertion, NOT a live browser walk at commit time.
No `docs/assets/` dir exists — deliberate net-new convention (GTH-V15-37 item 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish docs/assets/animation/ + commit the poster (Zone.Identifier stripped)</name>
  <files>docs/assets/animation/reposix-launch.thumbnail.webp</files>
  <action>Create `docs/assets/animation/` and copy ONLY the poster WebP (`/home/reuben/workspace/reposix-animation-pitch/.thumbnail`, 3158 B) to `docs/assets/animation/reposix-launch.thumbnail.webp`. Verify NO `:Zone.Identifier` sibling is copied (strip Windows metadata). Do NOT copy the mp4 (7MB — `structure/file-size-limits` + repo hygiene forbid it). Establishes the convention for GTH-V15-37 item 5.</action>
  <verify>
    <automated>test -f docs/assets/animation/reposix-launch.thumbnail.webp && ! ls docs/assets/animation/ | grep -qi "Zone.Identifier" && ! git ls-files | grep -qi '\.mp4$' && SZ=$(stat -c%s docs/assets/animation/reposix-launch.thumbnail.webp) && [ "$SZ" -lt 10000 ] && echo "poster OK ${SZ}B, no mp4, no Zone.Identifier" || echo "FAIL"</automated>
  </verify>
  <done>Poster committed under docs/assets/animation/; no mp4 committed; no Zone.Identifier file.</done>
</task>

<task type="auto">
  <name>Task 2: Embed poster+click-to-play <video> in index.md + mkdocs wiring</name>
  <files>docs/index.md, mkdocs.yml</files>
  <action>Add a `<video controls preload="none" poster="assets/animation/reposix-launch.thumbnail.webp">` element to index.md at the agreed furnished slot (below the hero grid, above/near "reposix loop vs MCP loop"), inside an `md_in_html` block (mirror index.md:15's grid-card markdown wrapper). Set `src` to the AGREED GitHub release-asset download URL for the `docs-assets`/`media-v1` tag (record the exact tag+filename in the SUMMARY so the coordinator's upload in 117-07 matches). NO `autoplay` (click-to-play only, per checklist item 3). Add any needed `extra_css` for responsive sizing (`docs/stylesheets/extra.css`) if the video needs width constraints. Do NOT add any `unpkg`/Babel/React `extra_javascript` (baseline is a plain mp4, not the JSX runtime). Reference GTH-V15-37 items 1-3.</action>
  <verify>
    <automated>grep -q "<video" docs/index.md && grep -q 'poster="assets/animation/reposix-launch.thumbnail.webp"' docs/index.md && ! grep -qi "autoplay" docs/index.md && ! grep -qi "unpkg\|babel/standalone" docs/index.md && echo "embed OK" ; bash quality/gates/docs-build/mkdocs-strict.sh 2>&1 | tail -2</automated>
  </verify>
  <done>index.md has a poster+controls <video> (no autoplay, no CDN JS), src points at the release-asset URL; mkdocs-strict green.</done>
</task>

<task type="auto">
  <name>Task 3: Mint the catalog-first docs-build/animation-renders row + file stretch GTHs</name>
  <files>quality/catalogs/docs-build.json, .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md</files>
  <action>Mint a new `docs-build/animation-renders` row in docs-build.json shaped like `docs-build/mermaid-renders` (docs-build.json:42-76): kind `mechanical`, artifact `.planning/verifications/playwright/index/animation.json` (asserting index.md's <video> loads + the poster resolves + zero console errors), cadences `[pre-push, pre-pr]`, `minted_at` = today's RFC3339, status `NOT-VERIFIED` until the coordinator generates the artifact in 117-07. Catalog-first: this row predates the coverage. Then file TWO stretch GOOD-TO-HAVES: (a) `GTH-V15-4x — interactive JSX animation embed` (needs a net-new esbuild toolchain to precompile animations-v2.jsx + reposix-scenes.jsx + tweaks-panel.jsx; self-host fonts; patch the localStorage `animstage:t` persistence bug; ~medium effort; blocker = absent dc-runtime build); (b) `GTH-V15-4y — interactive embed makes live cdn.simpleicons.org calls` (hermetic smell, only relevant to the interactive stretch, moot for the mp4 baseline). Reference GTH-V15-37 items 4-5.</action>
  <verify>
    <automated>grep -q "docs-build/animation-renders" quality/catalogs/docs-build.json && grep -A30 "docs-build/animation-renders" quality/catalogs/docs-build.json | grep -q "minted_at" && echo "row minted catalog-first" ; grep -qi "interactive" .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md && echo "stretch GTH filed" ; python3 quality/runners/run.py --cadence pre-commit 2>&1 | tail -3</automated>
  </verify>
  <done>New docs-build row exists with minted_at; two stretch GTHs filed; catalog loads without an audit-field rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| docs site → external GitHub release asset | index.md loads an mp4 from a GitHub release download URL |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-06 | Information disclosure / supply-chain | <video src> external URL | accept | Asset hosted on the project's own GitHub release (trusted origin), served over HTTPS, no PII, no live CDN JS/icon calls (mp4 baseline explicitly avoids the unpkg/simpleicons paths). |
| T-117-07 | Tampering | GitHub release upload | mitigate | Upload is owner-approved (owner-named-target), to a dedicated non-crate tag isolated from release-plz crate automation. |
</threat_model>

<verification>
- Poster committed, mp4 NOT committed, no Zone.Identifier files.
- index.md <video>: poster + controls, no autoplay, no CDN Babel/React/unpkg JS.
- mkdocs-strict green; new docs-build row loads (minted_at present).
- COORDINATOR-RUN (117-07): mp4 release upload (owner approval) + playwright artifact generation against the live URL.
- OWNERSHIP CHARTER Noticing required (e.g. the dead "export video" button reposix-scenes.jsx:982 — only relevant to the interactive stretch, note it on that GTH).
</verification>

<success_criteria>
GTH-V15-37 baseline: launch animation embedded as poster+click-to-play mp4, no live CDN dependency, mp4 as a release asset (never committed), net-new docs/assets/animation/ convention, catalog-first playwright row. Interactive embed + hermetic-CDN concern filed as stretch GTHs.
</success_criteria>

<output>
Create `.planning/phases/117-doc-truth-launch-blocker-purge/117-05-SUMMARY.md`. Targeted staging; no `--no-verify`; trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. RECORD the exact release tag + mp4 filename + asset URL used in the embed so the 117-07 coordinator upload matches byte-for-byte.
</output>
