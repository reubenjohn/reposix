---
task_id: 89-08
phase: 89
wave: 4
type: mechanical
execution_mode: top-level
req_ids: [RBF-FW-01, RBF-FW-02, RBF-FW-03, RBF-FW-04, RBF-FW-05, RBF-FW-11]
effort: S
depends_on: [89-01, 89-02, 89-03, 89-04, 89-05, 89-06, 89-07]
files_modified:
  - CLAUDE.md
  - quality/reports/verdicts/p89/VERDICT.md
---

# Task 89-08 — CLAUDE.md update + push origin + verifier subagent dispatch

## Goal

Phase-close ritual per CLAUDE.md "Push cadence — per-phase" + "Verifier subagent dispatch":
1. Update CLAUDE.md § "Quality Gates" tables — extend "7 cadences" → **8 cadences** + "5 kinds" → **6 kinds**; add structure-dim entries for the new linters; add subagent-rule bullet for the milestone-close 9th probe.
2. Confirm pre-push gate-passing locally (`python3 quality/runners/run.py --cadence pre-push` exit 0).
3. **`git push origin main`** (BEFORE verifier dispatch — load-bearing per CLAUDE.md).
4. Dispatch unbiased verifier subagent (verbatim prompt from `quality/PROTOCOL.md` § "Verifier subagent prompt template") that grades the 6 P89 catalog rows from artifacts with zero session context.
5. Await GREEN verdict at `quality/reports/verdicts/p89/VERDICT.md`. If RED, loop back to the failing task per OP-7.

## Files to create / modify

| File | Action |
|---|---|
| `CLAUDE.md` | modify § "Quality Gates — dimension/cadence/kind taxonomy" (cadence + kind tables); modify § "Subagent delegation rules" (add 9th-probe bullet); add structure-dim linters mention |
| `quality/reports/verdicts/p89/VERDICT.md` | **NEW** — written by verifier subagent (NOT by 89-08's primary work) |
| `89-VERIFICATION.md` | **NEW** (in `.planning/phases/89-framework-fixes-cadence-shell-kind/`) — manual-only verification record per VALIDATION.md § "Manual-Only Verifications" |

## Steps

1. **Read the existing CLAUDE.md sections** the change targets:
   ```bash
   grep -n '7 cadences\|5 kinds\|9 dimensions' CLAUDE.md
   grep -n 'Subagent delegation rules\|Push cadence' CLAUDE.md
   ```
   Locate the cadence and kind tables; locate the subagent-rules section.

2. **Modify CLAUDE.md § "Quality Gates — dimension/cadence/kind taxonomy"**:

   a. **9 dimensions table** — NO change (no new dimension; the 3+3 split per RESEARCH § Q-CATALOG-DIM-1 lands within `agent-ux` + `structure`).

   b. **7 cadences → 8 cadences** — extend the cadence table:
      ```
      `pre-release-real-backend` (local + milestone-close, env-gated, mandatory at tag-time, blocking; default-skips when REPOSIX_ALLOWED_ORIGINS + creds unset)
      ```
      Update the section header from "7 cadences" → "8 cadences" wherever it appears (§ heading + intro prose).

   c. **5 kinds → 6 kinds** — extend the kind table:
      ```
      `shell-subprocess` (real subprocess invocation against real backend + transcript artifact at quality/reports/transcripts/<row-slug>-<RFC3339>.txt with argv + env_keys [NAMES only — security] + cwd + exit_code + stdout + stderr blocks)
      ```
      Update the section header from "5 kinds" → "6 kinds".

   d. **Add a one-paragraph entry below the kind table** (per CONTEXT D-CLM-02):
      > **Structure-dimension gates added P89 RBF-FW-04 + RBF-FW-05:** `quality/gates/structure/banned-production-tokens.sh` blocks `\bP\d{2,3}-\d+\b` phase-ID tokens in `crates/**/*.rs` outside `tests/`, `**/CHANGELOG.md`, and lines tagged `// banned-words: ok` (pre-commit + pre-push). `quality/gates/structure/deferral-pointer-linter.sh` cross-references `not yet wired in P\d+` / `lands? (alongside|in) P\d+` / `substrate-gap-deferred` strings in `crates/` against `.planning/phases/N-*/PLAN*.md` existence (pre-push). Both ship with `// banned-words: ok` allowlist marker convention for justified exceptions.

   e. **Add to § "Threat model"** (per CONTEXT Q-DOC-LINT-1) — one sentence under the lethal-trifecta table:
      > **`pre-release-real-backend` cadence (P89 RBF-FW-01)** is the gate that ensures real-backend HTTP allowlist enforcement is testable end-to-end; the cadence env-gates on `REPOSIX_ALLOWED_ORIGINS` matching `^https?://(?!127\.0\.0\.1)` AND a complete credential set, so it cannot run against a missing allowlist.

3. **Modify CLAUDE.md § "Subagent delegation rules"** — add bullet (per CONTEXT D-CLM-03):
   > **The milestone-close 9th probe (RBF-FW-03) is non-skippable.** Any milestone-close ritual that does not include `python3 quality/runners/run.py --cadence pre-release-real-backend` exit 0 grades the milestone RED. The probe runs the vision litmus test against the sanctioned real backend (TokenWorld for v0.13.0); the catalog row's `verifier.script` is `quality/gates/agent-ux/milestone-close-vision-litmus.sh`. The verdict template lives at `quality/dispatch/milestone-close-verdict.md`. The row carries `blast_radius: P0` and NEVER carries a `waiver` block — the deferral-loop guard (anti-C7).

4. **Add to § "Push cadence — per-phase"** (per CONTEXT Q-DOC-LINT-1) — one sentence at section end:
   > **Milestone-close additionally requires** `python3 quality/runners/run.py --cadence pre-release-real-backend` exit 0 (see § "Subagent delegation rules" above for the 9th-probe contract).

5. **Run pre-push gate locally** before push:
   ```bash
   python3 quality/runners/run.py --cadence pre-push
   echo "exit: $?"
   ```
   Expected: exit 0. If RED, loop back to the responsible task (89-02..89-07) and fix BEFORE pushing — per CLAUDE.md "Push cadence" verifier grades RED if push lands without gates passing.

6. **Create `89-VERIFICATION.md`** in `.planning/phases/89-framework-fixes-cadence-shell-kind/` recording the manual-only verification per VALIDATION.md § "Manual-Only Verifications":
   ```markdown
   # Phase 89 — Verification Record

   ## Manual-Only Verification (per VALIDATION.md row 1)

   **Behavior:** 9th-probe SLOT semantics (RBF-FW-03) — NOT-VERIFIED outcome correctly threads through runner exit codes + verifier subagent grading at milestone-close.

   **Confirmation:** Sub-subagent ran `bash quality/gates/agent-ux/milestone-close-vision-litmus.sh` with synthetic env (`REPOSIX_ALLOWED_ORIGINS=https://reuben-john.atlassian.net ATLASSIAN_API_KEY=fake ATLASSIAN_EMAIL=fake REPOSIX_CONFLUENCE_TENANT=fake`).

   **Result:**
   - Verifier wrote artifact `quality/reports/verifications/agent-ux/milestone-close-vision-litmus-real-backend.json` with `"status":"NOT-VERIFIED","reason":"substrate_not_landed","blocked_on":["P91","P92","P93","P94","P95"]`
   - Verifier exit code: 1 (FAIL, mapped via runner)
   - Combined with `blast_radius: P0` on the catalog row, this guarantees milestone-close grading attempt cannot succeed until P91+P92+P93+P94+P95 substrate ships
   - Without env: runner short-circuits to NOT-VERIFIED via `_realbackend.is_skipped` from 89-03

   **Confirmed:** SLOT skeleton is correctly shaped to receive the real probe later. Anti-C7 (self-licensing-deferral-loop) defense is in place.
   ```

7. **Commit** the CLAUDE.md update + verification record:
   ```
   docs(P89): CLAUDE.md updates + manual verification record (RBF-FW-01..05, RBF-FW-11)

   - § "Quality Gates": 7→8 cadences, 5→6 kinds, structure-dim linter
     entries (banned-production-tokens + deferral-pointer)
   - § "Subagent delegation rules": milestone-close 9th probe non-skippable
     bullet; blast_radius P0 + never-WAIVED contract
   - § "Threat model": pre-release-real-backend cadence cross-reference
   - § "Push cadence — per-phase": milestone-close additional gate
   - 89-VERIFICATION.md: manual-only verification of SLOT semantics

   Closes ROADMAP P89 SC #5 (CLAUDE.md updated in same PR).
   ```

8. **Push to origin** (LOAD-BEARING — per CLAUDE.md "Push cadence" this MUST happen BEFORE verifier dispatch):
   ```bash
   git push origin main
   echo "push exit: $?"
   ```
   Expected: exit 0. If push fails (pre-push hook RED), do NOT dispatch verifier — fix the failing gate, commit, retry push. Verifier grades RED if the phase shipped without the push landing.

9. **Dispatch verifier subagent.** Use the verbatim prompt from `quality/PROTOCOL.md` § "Verifier subagent prompt template". Sub-subagent (separate Task-tool invocation by the top-level orchestrator):
   - Reads the 6 catalog rows minted in 89-01
   - Reads the artifacts at `quality/reports/verifications/{agent-ux,structure}/*.json`
   - Reads the transcripts at `quality/reports/transcripts/*.txt` (for the `kind: shell-subprocess` row)
   - Grades each row's `claim_vs_assertion_audit` against its `expected.asserts` and observed artifact contents
   - Spot-checks: any catalog row's `last_verified` hand-set pre-2026-05-08 to dodge the audit-field check? (Per RESEARCH § Risks watchout)
   - Writes verdict at `quality/reports/verdicts/p89/VERDICT.md` per `quality/reports/verdicts/p87/` shape
   - Per CLAUDE.md OP-7: the executing agent does NOT grade itself — verifier subagent author MUST be distinct from the orchestrator session

10. **Await verdict + decide:**
    - **GREEN** → phase closes; update `.planning/STATE.md` cursor (`Workstream A. Phase: P89 SHIPPED <date>. Plan: shipped. Status: P89 GREEN; next P90.`); orchestrator returns `## PHASE COMPLETE` to user.
    - **RED** → verdict names failing row(s); orchestrator routes back to the responsible task (89-02..89-07) for fix. Loop until GREEN.

## Acceptance Criteria

- [ ] CLAUDE.md § "Quality Gates" cadence table contains `pre-release-real-backend`
- [ ] CLAUDE.md § "Quality Gates" kind table contains `shell-subprocess`
- [ ] CLAUDE.md mentions both new structure-dim linters (banned-production-tokens + deferral-pointer)
- [ ] CLAUDE.md § "Subagent delegation rules" contains 9th-probe bullet (non-skippable; P0; never-WAIVED)
- [ ] `python3 quality/runners/run.py --cadence pre-push` exits 0 BEFORE push
- [ ] `git push origin main` succeeds (pre-push hook GREEN)
- [ ] Verifier subagent dispatched (Task-tool invocation distinct from orchestrator session)
- [ ] Verdict at `quality/reports/verdicts/p89/VERDICT.md` reads GREEN
- [ ] `89-VERIFICATION.md` records manual SLOT-semantics check
- [ ] STATE.md cursor advanced to "P89 SHIPPED; next P90"

## Verifier Command

```bash
python3 quality/runners/run.py --cadence pre-push && git status --short && git log origin/main..HEAD && test -f quality/reports/verdicts/p89/VERDICT.md && grep -E '(GREEN|PASS)' quality/reports/verdicts/p89/VERDICT.md
```

(VALIDATION map row 89-08-01)

## Watchouts

- **Push BEFORE verifier dispatch is non-negotiable** (CLAUDE.md "Push cadence — per-phase"). The verifier reads from disk at the post-push state; if a sub-subagent dispatches the verifier first then pushes, the verifier grades a state that may not match origin and the verdict can become misleading. Order matters.
- **Verifier subagent distinct from orchestrator** (CLAUDE.md OP-7 + F-K5 author-distinctness rule). The orchestrator MUST NOT grade itself. Use the Task tool to spawn a fresh subagent with the verbatim PROTOCOL.md prompt; do not paraphrase.
- **`framework.json` override note in CLAUDE.md** — the SC #5 wording in ROADMAP names `framework.json` literally; CLAUDE.md update should NOT introduce a new `framework` dimension. Per RESEARCH § Q-CATALOG-DIM-1 the override is documented in 89-01's commit; CLAUDE.md just reflects the 9-dimension reality (unchanged).
- **`8 cadences` text alignment:** the existing CLAUDE.md § "Quality Gates" intro paragraph mentions "7 cadences"; sub-subagent searches AND replaces all instances (`grep -F '7 cadences' CLAUDE.md`). Same for "5 kinds" → "6 kinds".
- **CLAUDE.md anti-bloat (per quality/PROTOCOL.md):** prefer revising existing tables over appending new sections. Cadence + kind tables ALREADY exist in CLAUDE.md; just add rows. The "structure-dim linters" entry is one paragraph below the kind table, NOT a new top-level section.
- **`SURPRISES-INTAKE.md` review:** before declaring phase closed, sub-subagent checks if any task surfaced items to `.planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md`. If so, the entries carry severity + what + why-out-of-scope + sketched-resolution per OP-8; reservation phase P96 will drain them. The verifier subagent at phase close validates the intake-or-eager-resolution dichotomy per OP-8.
- **No-cargo footprint:** P89 has no cargo work; the pre-push gate exercises `code` dimension verifiers but those should already PASS against the unchanged `crates/` tree (modulo the allowlist-marker additions from 89-02).

## Auto-Resolution Preference (CLAUDE.md OP-8)

In-task: CLAUDE.md prose tightening; STATE.md cursor wording; verifier-subagent prompt minor tweaks. Surface to SURPRISES-INTAKE if: the verifier subagent grades RED on a structural issue not covered by a single task (e.g., a cross-task contract violation between 89-04 transcript convention and 89-07 hash field) — that's a P89-internal scope expansion that needs orchestrator routing.

## Two-Channel Rule

Sub-subagent (CLAUDE.md authoring): write full CLAUDE.md diff + pre-push runner output + git push output to disk. Return ≤300-word TLDR with: CLAUDE.md sections touched, pre-push exit code, push success.

Verifier subagent (separate Task-tool invocation): writes verdict to `quality/reports/verdicts/p89/VERDICT.md` per PROTOCOL.md template; returns ≤300-word TLDR naming GREEN/RED, per-row grade, any spot-check findings.
