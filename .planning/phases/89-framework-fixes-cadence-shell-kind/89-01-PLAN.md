---
task_id: 89-01
phase: 89
wave: 1
type: mechanical
execution_mode: top-level
req_ids: [RBF-FW-01, RBF-FW-02, RBF-FW-03, RBF-FW-04, RBF-FW-05, RBF-FW-11]
effort: XS
depends_on: []
files_modified:
  - quality/catalogs/agent-ux.json
  - quality/catalogs/freshness-invariants.json
---

# Task 89-01 — Catalog-first commit: mint 6 NOT-VERIFIED rows

## Goal

Mint **6 catalog rows** with `status: NOT-VERIFIED` BEFORE any implementation lands, in compliance with `quality/PROTOCOL.md` Step 3 (catalog-first contract) and CLAUDE.md "Quality Gates" § "Catalog-first rule". Each row carries a **`claim_vs_assertion_audit`** paragraph (≥50 chars) — the field that RBF-FW-11 introduces. This commit eats its own dogfood and gates every subsequent task: 89-02..89-08 commits cite a row id from this set.

## Files to create / modify

| File | Action | Lines / Rows |
|---|---|---|
| `quality/catalogs/agent-ux.json` | Append 3 rows to the `"rows"` array | NEW row blocks |
| `quality/catalogs/freshness-invariants.json` | Append 3 rows to the `"rows"` array (wrapper `"dimension": "structure"`) | NEW row blocks |

**Override decision (per RESEARCH § Q-CATALOG-DIM-1):** ROADMAP P89 SC #5 names `quality/catalogs/{agent-ux,framework}.json` literally. There is **no `framework` dimension** in CLAUDE.md "9 dimensions" (`code, docs-alignment, docs-build, docs-repro, release, structure, agent-ux, perf, security`). Creating `framework.json` would require a schema migration that bloats P89's scope. **Split 3+3 across `agent-ux.json` (cadence/kind/9th-probe rows) + `freshness-invariants.json` (linter/audit-field rows).** Document this override in the catalog-first commit message and propagate to 89-08's CLAUDE.md update.

## Steps

1. **Read the existing catalog wrappers** to mirror conventions (`$schema`, `comment`, `dimension`, `rows`):
   ```bash
   head -20 quality/catalogs/agent-ux.json
   head -20 quality/catalogs/freshness-invariants.json
   ```
   Confirm `freshness-invariants.json` has `"dimension": "structure"` (per RESEARCH § Q-CATALOG-DIM-1 — there is no `structure.json`; freshness-invariants.json IS the structure dimension's catalog).

2. **Append 3 rows to `quality/catalogs/agent-ux.json`** inside the existing `"rows"` array (use the unified schema from `quality/catalogs/README.md:18-41`):

   **Row 1:** `agent-ux/cadence-pre-release-real-backend`
   - `dimension`: `agent-ux`
   - `cadences`: `["pre-release-real-backend"]`
   - `kind`: `mechanical`
   - `sources`: `["quality/runners/run.py:45-47", "quality/runners/_realbackend.py", "quality/PROTOCOL.md:140-148"]`
   - `expected.asserts`: `["VALID_CADENCES contains 'pre-release-real-backend'", "rows tagged with cadence skip with NOT-VERIFIED when REPOSIX_ALLOWED_ORIGINS unset", "rows execute when REPOSIX_ALLOWED_ORIGINS + a credential set are present"]`
   - `verifier.script`: `quality/gates/agent-ux/cadence-pre-release-real-backend.sh` (NEW; ships in 89-03 — for the catalog-first commit, just declare the path; 89-03's commit creates the file)
   - `verifier.timeout_s`: 60
   - `artifact`: `quality/reports/verifications/agent-ux/cadence-pre-release-real-backend.json`
   - `status`: `NOT-VERIFIED`
   - `last_verified`: null
   - `blast_radius`: `P1`
   - `owner_hint`: `quality/runners/run.py + _realbackend.py`
   - `waiver`: null
   - `claim_vs_assertion_audit`: (template — refine for ≥50 chars):
     > "The verifier asserts `quality/runners/run.py` `VALID_CADENCES` tuple contains the literal string `pre-release-real-backend` AND that a probe row tagged with that cadence runs only when REPOSIX_ALLOWED_ORIGINS + a credential set are present. If the description's claim ('the new cadence is env-gated and default-skips in CI') were false — e.g., the runner ran the verifier with no env vars set — the assertion would fail because the env-gate short-circuit would not fire and the verifier would attempt a real-backend HTTP call against a missing allowlist."

   **Row 2:** `agent-ux/kind-shell-subprocess-worked-example`
   - `dimension`: `agent-ux`
   - `cadences`: `["pre-push", "pre-pr"]`
   - `kind`: `shell-subprocess`
   - `sources`: `["quality/catalogs/README.md:27", "quality/gates/agent-ux/lib/transcript.sh", "quality/gates/agent-ux/shell-subprocess-example.sh"]`
   - `expected.asserts`: `["worked-example verifier exits 0 against local reposix binary", "transcript artifact written to quality/reports/transcripts/<slug>-<RFC3339>.txt", "transcript contains argv + env_keys (NOT values) + exit_code + stdout + stderr blocks"]`
   - `verifier.script`: `quality/gates/agent-ux/shell-subprocess-example.sh` (NEW; ships in 89-04)
   - `verifier.timeout_s`: 30
   - `artifact`: `quality/reports/verifications/agent-ux/kind-shell-subprocess-worked-example.json`
   - `status`: `NOT-VERIFIED`
   - `blast_radius`: `P1`
   - `claim_vs_assertion_audit`:
     > "The verifier asserts that `bash quality/gates/agent-ux/shell-subprocess-example.sh` exits 0 AND writes a transcript file at `quality/reports/transcripts/kind-shell-subprocess-worked-example-<RFC3339>.txt` containing argv + env_keys + cwd + exit_code + stdout + stderr blocks. If the description's claim ('the kind drives a real subprocess + emits a transcript') were false — e.g., the verifier called a Python function inline instead of spawning `reposix --version` — the assertion would fail because no transcript file would exist OR the transcript would be missing the `argv:` line that proves subprocess invocation."

   **Row 3:** `agent-ux/milestone-close-vision-litmus-real-backend`
   - `dimension`: `agent-ux`
   - `cadences`: `["pre-release-real-backend"]`
   - `kind`: `shell-subprocess`
   - `sources`: `["quality/dispatch/milestone-close-verdict.md", "quality/gates/agent-ux/milestone-close-vision-litmus.sh", ".planning/research/v0.13.0-dvcs/vision-and-mental-model.md"]`
   - `expected.asserts`: `["agent completes vanilla-clone + reposix attach + edit + git push end-to-end against TokenWorld with zero in-context learning", "audit_events_cache AND audit_events tables both have rows for the action", "refs/mirrors/<sot>-{head,synced-at} advanced", "transcript artifact emitted per RBF-FW-02 convention"]`
   - `verifier.script`: `quality/gates/agent-ux/milestone-close-vision-litmus.sh` (NEW; SLOT verifier — ships in 89-06; legitimately returns NOT-VERIFIED until P91+P92+P93+P94+P95 land the substrate)
   - `verifier.timeout_s`: 300
   - `artifact`: `quality/reports/verifications/agent-ux/milestone-close-vision-litmus-real-backend.json`
   - `status`: `NOT-VERIFIED`
   - `blast_radius`: `P0`  ← **load-bearing per CONTEXT D-03c**: any milestone-close grading attempt with this row NOT-VERIFIED returns exit 1, defending against C7 self-licensing-deferral-loop
   - `claim_vs_assertion_audit`:
     > "The verifier asserts that the milestone-close 9th probe runs the dark-factory third-arm scenario against TokenWorld (or sanctioned alternative per docs/reference/testing-targets.md) AND that audit_events_cache + audit_events both contain rows for the round-trip AND that refs/mirrors/<sot>-{head,synced-at} advanced. This row WILL flip to PASS only when P91+P92+P93+P94+P95 ship their substrate; until then it legitimately returns NOT-VERIFIED. If the description's claim ('the litmus test runs verbatim against a real backend before the v0.13.0 tag') were false — e.g., the verifier silently ran against the in-process simulator — the assertion would fail because the transcript's argv would not name `reposix attach confluence::TokenWorld` and audit_events would not contain real-backend host fingerprints."

3. **Append 3 rows to `quality/catalogs/freshness-invariants.json`** inside the existing `"rows"` array:

   **Row 4:** `structure/banned-production-tokens`
   - `dimension`: `structure`
   - `cadences`: `["pre-commit", "pre-push", "pre-pr"]`
   - `kind`: `mechanical`
   - `sources`: `["quality/gates/structure/banned-production-tokens.sh", "scripts/banned-words-lint.sh:107-141 (mirror)"]`
   - `expected.asserts`: `["grep -nHE '\\bP[0-9]{2,3}-[0-9]+\\b' over crates/**/*.rs (excluding tests/, **/CHANGELOG.md, allowlist-marker lines) returns zero matches", "violation lines emit owner_hint pointing at allowlist marker convention"]`
   - `verifier.script`: `quality/gates/structure/banned-production-tokens.sh` (NEW; ships in 89-02)
   - `verifier.timeout_s`: 10
   - `artifact`: `quality/reports/verifications/structure/banned-production-tokens.json`
   - `status`: `NOT-VERIFIED`
   - `blast_radius`: `P1`
   - `owner_hint`: `add '// banned-words: ok' allowlist marker OR rename the phase-id token`
   - `claim_vs_assertion_audit`:
     > "The verifier asserts `grep -nHE '\\bP[0-9]{2,3}-[0-9]+\\b' crates/**/*.rs` (excluding tests/ and allowlist-marker lines and **/CHANGELOG.md) returns zero matches. If the description's claim ('phase IDs are banned in production source') were false — e.g., a `P79-02 scaffold` literal stayed in `crates/reposix-cli/src/attach.rs` — the assertion would fail because grep would report the match line and the verifier would exit 1."

   **Row 5:** `structure/deferral-pointer-linter`
   - `dimension`: `structure`
   - `cadences`: `["pre-push"]`
   - `kind`: `mechanical`
   - `sources`: `["quality/gates/structure/deferral-pointer-linter.sh"]`
   - `expected.asserts`: `["every match of 'not yet wired in P\\d+' / 'lands? (alongside|in) P\\d+' / 'substrate-gap-deferred' in crates/ resolves to .planning/phases/N-*/PLAN*.md existing", "missing PLAN file BLOCKs with structured stderr naming the file:line + missing phase number"]`
   - `verifier.script`: `quality/gates/structure/deferral-pointer-linter.sh` (NEW; ships in 89-05)
   - `verifier.timeout_s`: 15
   - `artifact`: `quality/reports/verifications/structure/deferral-pointer-linter.json`
   - `status`: `NOT-VERIFIED`
   - `blast_radius`: `P1`
   - `owner_hint`: `create the named phase's PLAN file or scrub the deferral pointer`
   - `claim_vs_assertion_audit`:
     > "The verifier asserts that for every match of three deferral patterns (`not yet wired in P\\d+`, `lands? (alongside|in) P\\d+`, `substrate-gap-deferred`) in `crates/`, the named phase number resolves to a `.planning/phases/N-*/` directory containing at least one `*PLAN*.md` file. If the description's claim ('deferral pointers cross-reference an existing downstream PLAN') were false — e.g., a comment said `// substrate-gap-deferred until P150 lands` and no .planning/phases/150-* exists — the assertion would fail because the find command would return empty and the verifier would exit 1 naming the orphaned reference."

   **Row 6:** `structure/claim-vs-assertion-audit-required`
   - `dimension`: `structure`
   - `cadences`: `["pre-push", "pre-pr"]`
   - `kind`: `mechanical`
   - `sources`: `["quality/runners/_audit_field.py", "quality/runners/run.py:72-81", "quality/catalogs/README.md:22-41"]`
   - `expected.asserts`: `["load_catalog() refuses any catalog containing a row with last_verified >= 2026-05-08 OR last_verified=null AND missing claim_vs_assertion_audit string >=50 chars", "SystemExit message names the catalog path + row id + cutoff date"]`
   - `verifier.script`: `quality/gates/structure/claim-vs-assertion-audit-required.sh` (NEW; thin bash that runs `python3 -m unittest quality.runners.test_audit_field`; ships in 89-07)
   - `verifier.timeout_s`: 10
   - `artifact`: `quality/reports/verifications/structure/claim-vs-assertion-audit-required.json`
   - `status`: `NOT-VERIFIED`
   - `blast_radius`: `P0`
   - `owner_hint`: `add a claim_vs_assertion_audit paragraph (>=50 chars) to the row explaining how the verifier would falsify the description claim if false`
   - `claim_vs_assertion_audit`:
     > "The verifier asserts that `python3 quality/runners/run.py --cadence pre-push --dry-run` over a synthetic catalog containing a row dated 2026-05-08 with no `claim_vs_assertion_audit` field exits with `SystemExit(FAIL: ...missing claim_vs_assertion_audit (>=50 chars required for rows minted on/after 2026-05-08))`. If the description's claim ('the runner enforces the field on rows minted >= 2026-05-08 and grandfathers older rows') were false — e.g., the date-cutoff was hard-coded to a future date or the field check was absent — the assertion would fail because the synthetic catalog would load successfully and the test would error."

4. **Validate the JSON syntactically** before commit:
   ```bash
   python3 -m json.tool quality/catalogs/agent-ux.json > /dev/null
   python3 -m json.tool quality/catalogs/freshness-invariants.json > /dev/null
   ```

5. **Confirm runner can load the catalogs** (this MUST succeed even though `claim_vs_assertion_audit` runner cross-check from 89-07 hasn't shipped yet — the field's presence is silently accepted by `load_catalog()` until 89-07 wires the validator):
   ```bash
   python3 quality/runners/run.py --cadence pre-push --dimension structure --dry-run 2>&1 | tail -20
   ```
   Expected: lists the 3 new structure rows as NOT-VERIFIED. Same for `--dimension agent-ux`.

6. **Commit per `quality/PROTOCOL.md` Step 3** with message:
   ```
   catalog(P89): mint 6 framework-fix rows NOT-VERIFIED

   Adds:
   - agent-ux/cadence-pre-release-real-backend       (RBF-FW-01)
   - agent-ux/kind-shell-subprocess-worked-example   (RBF-FW-02)
   - agent-ux/milestone-close-vision-litmus-real-backend (RBF-FW-03; SLOT)
   - structure/banned-production-tokens              (RBF-FW-04)
   - structure/deferral-pointer-linter               (RBF-FW-05)
   - structure/claim-vs-assertion-audit-required     (RBF-FW-11)

   Each row carries claim_vs_assertion_audit (eating own dogfood — RBF-FW-11
   introduces the field, this commit demonstrates it).

   Override: ROADMAP SC #5 names `framework.json` literally; per RESEARCH
   § Q-CATALOG-DIM-1 there is no `framework` dimension. 3+3 split across
   existing agent-ux.json + freshness-invariants.json. CLAUDE.md updated
   for the dimension count in 89-08.

   Implements: REMEDIATION-PLAN P89 SC #5 (catalog-first contract).
   ```

## Acceptance Criteria

- [ ] `quality/catalogs/agent-ux.json` parses and contains the 3 new row IDs
- [ ] `quality/catalogs/freshness-invariants.json` parses and contains the 3 new row IDs
- [ ] All 6 rows: `status == "NOT-VERIFIED"`, `last_verified == null`
- [ ] All 6 rows: `claim_vs_assertion_audit` is a string ≥50 characters
- [ ] Runner discovery surfaces all 6 rows (`run.py --cadence pre-push --dimension agent-ux,structure --dry-run`)
- [ ] First-commit-of-phase contract honored — no implementation files added in this commit

## Verifier Command

```bash
python3 quality/runners/run.py --cadence pre-push --dimension agent-ux,structure --dry-run | grep -cE '(banned-production-tokens|deferral-pointer-linter|claim-vs-assertion-audit-required|cadence-pre-release-real-backend|kind-shell-subprocess-worked-example|milestone-close-vision-litmus-real-backend)' | awk '$1 == 6 {exit 0} {exit 1}'
```

(VALIDATION map row 89-01-01 — `python3 quality/runners/run.py --cadence pre-push --dimension agent-ux,structure --dry-run`)

## Watchouts

- **JSON trailing-comma:** `python3 -m json.tool` will reject; use a JSON-aware editor.
- **`framework.json` temptation:** RESEARCH § Q-CATALOG-DIM-1 explicitly forbids creating it. If a sub-subagent argues otherwise, route back to the override decision in step 0 of this plan.
- **`claim_vs_assertion_audit` length floor:** strictly ≥50 chars (`MIN_LEN = 50` in 89-07's `_audit_field.py`). Templates above all clear 200+ chars; do not abbreviate them.
- **89-04 / 89-06 verifier scripts referenced but not created in this commit:** rows are minted with `verifier.script` paths pointing to files that don't exist yet. The runner will mark them NOT-VERIFIED on dry-run; that is the desired state. The implementation tasks (89-02..89-07) create the scripts.

## Auto-Resolution Preference (CLAUDE.md OP-8)

In-task fixes < 1 hour with no new dependency: typo in catalog metadata, missing `comment` field, JSON formatting nit. Surface to `SURPRISES-INTAKE.md` if: the existing catalog wrapper schema disagrees with `quality/catalogs/README.md` (would expand scope into a schema fix that belongs to P95 RBF-D-06).

## Two-Channel Rule

Sub-subagent: write the full diff + JSON validation output + `run.py --dry-run` output to disk; return ≤300-word TLDR with files touched + verifier exit code + any surprises filed.
