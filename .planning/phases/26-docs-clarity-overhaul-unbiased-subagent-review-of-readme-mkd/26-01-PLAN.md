---
plan: 26-01
wave: 1
phase: 26
title: "Root-level housekeeping — delete stubs, archive obsolete files, fix README version"
depends_on: []
files_modified:
  - AgenticEngineeringReference.md
  - InitialReport.md
  - MORNING-BRIEF.md
  - PROJECT-STATUS.md
  - README.md
  - docs/archive/MORNING-BRIEF.md
  - docs/archive/PROJECT-STATUS.md
  - mkdocs.yml
autonomous: true
requirements_addressed: []

must_haves:
  truths:
    - "AgenticEngineeringReference.md and InitialReport.md no longer exist at the repo root"
    - "MORNING-BRIEF.md and PROJECT-STATUS.md exist at docs/archive/ (not deleted)"
    - "README.md version references say v0.7.0, not v0.3.0 or v0.4.0"
    - "README.md phase/release table is updated to reflect shipped phases through v0.7.0"
    - "mkdocs.yml does not include docs/archive/ in the nav (so archive pages do not appear in the public docs site)"
  artifacts:
    - path: "docs/archive/MORNING-BRIEF.md"
      provides: "Archived v0.1/v0.2 morning brief"
    - path: "docs/archive/PROJECT-STATUS.md"
      provides: "Archived v0.1/v0.2 project status"
    - path: "README.md"
      provides: "Updated version references throughout"
  key_links:
    - from: "README.md"
      to: "docs/research/initial-report.md"
      via: "Cross-reference links already updated in Phase 25"
      pattern: "docs/research/"
---

<objective>
Remove stale root-level stub files, archive obsolete project-era docs, and fix all version-number references in README.md so readers get accurate information about the current v0.7.0 state.

Purpose: Root-level clutter (redirect stubs, obsolete briefs) creates friction for cold readers who land on the repo root. README.md still says v0.3.0 in multiple places.
Output: Deleted stubs, archived obsolete docs, README.md with accurate v0.7.0 version context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-docs-clarity-overhaul-unbiased-subagent-review-of-readme-mkd/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete root stubs and archive obsolete files</name>
  <files>AgenticEngineeringReference.md, InitialReport.md, MORNING-BRIEF.md, PROJECT-STATUS.md, docs/archive/MORNING-BRIEF.md, docs/archive/PROJECT-STATUS.md, mkdocs.yml</files>
  <read_first>
    - AgenticEngineeringReference.md
    - InitialReport.md
    - MORNING-BRIEF.md
    - PROJECT-STATUS.md
    - mkdocs.yml
  </read_first>
  <action>
1. Confirm `AgenticEngineeringReference.md` is only a redirect stub (single blockquote line). Then: `git rm AgenticEngineeringReference.md`
2. Confirm `InitialReport.md` is only a redirect stub (single blockquote line). Then: `git rm InitialReport.md`
3. Create `docs/archive/` directory: `mkdir -p docs/archive`
4. Move MORNING-BRIEF.md to archive: `git mv MORNING-BRIEF.md docs/archive/MORNING-BRIEF.md`
5. Move PROJECT-STATUS.md to archive: `git mv PROJECT-STATUS.md docs/archive/PROJECT-STATUS.md`
6. Add an archive notice at the top of EACH archived file. Prepend this block (before any existing content):

   For docs/archive/MORNING-BRIEF.md:
   ```
   > **Archived** — This document covers the v0.1/v0.2 era (2026-04-13). It is preserved for historical context only. For the current project state see [`HANDOFF.md`](../HANDOFF.md) and [`.planning/STATE.md`](../.planning/STATE.md).

   ---
   ```

   For docs/archive/PROJECT-STATUS.md:
   ```
   > **Archived** — This document covers the v0.1/v0.2 era (2026-04-13). It is preserved for historical context only. For current status see [`HANDOFF.md`](../HANDOFF.md) and [`.planning/STATE.md`](../.planning/STATE.md).

   ---
   ```

7. In mkdocs.yml: confirm `docs/archive/` is NOT listed in the `nav:` section. If any nav entry references archive pages, remove it. Also add to the `exclude_docs:` section (or `not_in_nav:` if that pattern is used) to suppress the MkDocs warning about orphan pages. Check current mkdocs.yml structure first to use the right pattern.
  </action>
  <verify>
    <automated>cd /home/reuben/workspace/reposix && test ! -f AgenticEngineeringReference.md && test ! -f InitialReport.md && test ! -f MORNING-BRIEF.md && test ! -f PROJECT-STATUS.md && test -f docs/archive/MORNING-BRIEF.md && test -f docs/archive/PROJECT-STATUS.md && echo "PASS: stubs deleted, archives exist"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f AgenticEngineeringReference.md` passes
    - `test ! -f InitialReport.md` passes
    - `test -f docs/archive/MORNING-BRIEF.md` passes
    - `test -f docs/archive/PROJECT-STATUS.md` passes
    - `grep -c "Archived" docs/archive/MORNING-BRIEF.md` returns >= 1
    - `grep -c "Archived" docs/archive/PROJECT-STATUS.md` returns >= 1
    - mkdocs.yml nav section does not contain `archive/`
  </acceptance_criteria>
  <done>Root stubs deleted, obsolete docs in docs/archive/ with archive notices, mkdocs.yml nav unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Fix README.md version references and phase table</name>
  <files>README.md</files>
  <read_first>
    - README.md
  </read_first>
  <action>
Read README.md fully, then make these targeted changes:

1. **Version badge / headline status block** (around line 34): The "v0.3.0 alpha" line — update to:
   `**v0.7.0.** Built across autonomous coding-agent sessions on 2026-04-13 → 2026-04-16 — single agent, GSD planning workflow, no human in the loop after kickoff. **317+ workspace tests pass**, `cargo clippy --workspace --all-targets -- -D warnings` is clean, `mkdocs build --strict` green, `bash scripts/demos/smoke.sh` 4/4. `#![forbid(unsafe_code)]` at every crate root.`

2. **Phase/release table** (the table listing Phase 11, Phase 13, etc.): Replace with an updated table that covers through v0.7.0:
   ```
   | Release | Highlights |
   |---------|------------|
   | **v0.1** | Simulator + `IssueBackend` trait + FUSE read-only mount + `git-remote-reposix` + 8 security guardrails |
   | **v0.2** | Real GitHub Issues adapter (`reposix-github`) behind the same trait |
   | **v0.3** | Confluence Cloud adapter (live against `reuben-john.atlassian.net`); ADR-002 page mapping |
   | **v0.4** | `pages/` + `tree/` nested mount layout with symlink overlay for Confluence `parentId` hierarchy |
   | **v0.4.1** | FUSE + remote helper routed through `IssueBackend` trait; deleted ~830 lines of sim-specific glue |
   | **v0.5** | Synthesized `_INDEX.md` sitemap in each FUSE bucket directory |
   | **v0.6** | Confluence write path (ADF↔Markdown, `create_issue`/`update_issue`/`delete_or_close`); labels overlay; `_INDEX.md` at tree + root level; `reposix refresh` subcommand |
   | **v0.7** | Contention/truncation/chaos hardening; honest token benchmarks (92.3% reduction); Confluence comments + attachments + whiteboards; docs reorg |
   ```

3. **"Tracking artifacts" sentence** (around line 51): Change reference from `HANDOFF.md` v0.5+ direction to v0.8+ (JIRA Cloud integration). Update to: `Tracking artifacts live in [`.planning/`](.planning/). See [`HANDOFF.md`](HANDOFF.md) for v0.8+ direction (JIRA Cloud integration, `BackendConnector` rename, `Issue.extensions` field).`

4. **Confluence quickstart version note** (around line 126, says "v0.3"): Update to `(read-only, v0.7)` or just remove the version qualifier if it just creates churn.

5. **Binary download URLs** (around lines 175–177, say v0.4.0): Update to:
   ```
   curl -fsSLO https://github.com/reubenjohn/reposix/releases/latest/download/reposix-v0.7.0-x86_64-unknown-linux-gnu.tar.gz
   tar -xzf reposix-v0.7.0-x86_64-unknown-linux-gnu.tar.gz
   export PATH="$PWD/reposix-v0.7.0-x86_64-unknown-linux-gnu:$PATH"
   ```

6. **Security section "what's enforced" heading** (says "v0.3"): Update to "v0.7".

7. **"Deferred to v0.4 / future" section**: Update heading and content to reflect that write paths are now shipped (v0.6), macOS hardening partially done (v0.7 HARD-04 partial — self-hosted runner needed), and point to v0.8 for JIRA. Change heading to "Deferred to v0.8 / future" and update the bullet points to reflect current state.

Do NOT change the architecture, the security guardrails table, the threat model prose, or any other substantive content — version number fixes and the phase table only.
  </action>
  <verify>
    <automated>cd /home/reuben/workspace/reposix && grep -c "v0\.3\.0" README.md && echo "---" && grep -c "v0\.7" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "v0\.3\.0 alpha" README.md` returns nothing (old headline gone)
    - `grep "v0\.7\.0" README.md` returns at least 1 match
    - `grep "v0\.7" README.md` returns multiple lines
    - `grep "v0\.4\.0-x86_64" README.md` returns nothing (old binary URLs gone)
    - `grep "v0\.7\.0-x86_64" README.md` returns the updated binary URL line
    - The release table contains a row for v0.7
  </acceptance_criteria>
  <done>README.md accurately states v0.7.0, release table covers v0.1–v0.7, binary download URLs updated, security section version updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is docs-only; no trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Tampering | mkdocs.yml nav | accept | Manual verification that archive/ not in nav; mkdocs build --strict will warn on orphan pages |
</threat_model>

<verification>
```bash
cd /home/reuben/workspace/reposix
# Stubs deleted
test ! -f AgenticEngineeringReference.md && echo "stub 1 gone" || echo "FAIL: stub 1 still exists"
test ! -f InitialReport.md && echo "stub 2 gone" || echo "FAIL: stub 2 still exists"
# Archives exist
test -f docs/archive/MORNING-BRIEF.md && echo "archive 1 exists" || echo "FAIL: archive 1 missing"
test -f docs/archive/PROJECT-STATUS.md && echo "archive 2 exists" || echo "FAIL: archive 2 missing"
# README version
grep "v0\.7" README.md | wc -l
grep -c "v0\.3\.0 alpha" README.md  # should be 0
# Cargo.toml workspace version (should be 0.7.0)
grep "^version" Cargo.toml | head -1
```
</verification>

<success_criteria>
- Root stubs (AgenticEngineeringReference.md, InitialReport.md) deleted from repo
- MORNING-BRIEF.md and PROJECT-STATUS.md moved to docs/archive/ with archive notices
- mkdocs.yml nav does not list archive pages
- README.md headline reads v0.7.0, not v0.3.0
- README.md release table covers v0.1 through v0.7
- README.md binary download URLs reference v0.7.0
- `cargo check --workspace` still passes (docs-only change, but sanity check)
</success_criteria>

<output>
After completion, create `.planning/phases/26-docs-clarity-overhaul-unbiased-subagent-review-of-readme-mkd/26-01-SUMMARY.md`
</output>
