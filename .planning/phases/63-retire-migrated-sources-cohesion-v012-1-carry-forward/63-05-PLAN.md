---
phase: 63-retire-migrated-sources-cohesion-v012-1-carry-forward
plan: 05
type: execute
wave: 4
depends_on: ['63-01', '63-02']
files_modified:
  - .planning/milestones/v0.12.1-phases/ROADMAP.md
  - .planning/milestones/v0.12.1-phases/REQUIREMENTS.md
  - .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh
  - CHANGELOG.md
autonomous: true
requirements:
  - MIGRATE-03
must_haves:
  truths:
    - "v0.12.1 milestone scaffold lands at `.planning/milestones/v0.12.1-phases/{ROADMAP,REQUIREMENTS}.md` per CLAUDE.md `.planning/milestones/` convention (planning docs INSIDE the matching `*-phases/` dir, never loose at top-level). Freshness verifier `freshness/no-loose-roadmap-or-requirements` MUST stay GREEN."
    - "REQUIREMENTS.md v0.12.1 lists every carry-forward placeholder named in v0.12.0 MIGRATE-03 + REQUIREMENTS.md text: PERF-01..03 (latency-vs-headline-copy, token-economy-bench, golden-path-envelope), SEC-01..02 (allowlist-enforcement, audit-immutability), CROSS-01..02 (windows-2022-rehearsal, macos-14-rehearsal), MSRV-01 (Rust 1.82->1.85 OR cap block-buffer<0.12), BINSTALL-01 ([package.metadata.binstall] blocks ~10 LOC), LATEST-PTR-01 (`gh release create --latest` OR release-plz config), RELEASE-PAT-01 (release-plz fine-grained PAT or post-tag dispatch), ERR-OTHER-01 (Error::Other 156->144 completion from v0.11.1 POLISH2-09), SUBJ-RUNNER-01 (subjective dispatch-and-preserve runner invariant from P61 Wave G), SUBJ-AUTODISPATCH-01 (CI auto-dispatch deferred), SUBJ-HARDGATE-01 (release.yml waiting on quality-pre-release.yml composite). Each placeholder has 1-3 sentences naming scope + the v0.12.0 source (commit/SURPRISES line/REQUIREMENTS line)."
    - "ROADMAP.md v0.12.1 has the standard milestone shape: scope summary, depends_on (v0.12.0 GREEN verdict), phase placeholders (Phase 64+; one phase per major-themed bundle: P64 perf, P65 security, P66 cross-platform, P67 carry-forward chores, etc -- exact phase count is the v0.12.1 planner's call, but the scaffold lists at least 4 placeholder phases). Each phase placeholder names its REQ-IDs, no detailed plans needed (those land via /gsd-plan-phase in v0.12.1)."
    - "Catalog cross-references: each carry-forward stub catalog row in `quality/catalogs/{perf-targets,security-gates,cross-platform}.json` from Wave 1 has a `tracked_in` pointing at a v0.12.1 placeholder REQ-ID; that REQ-ID exists in REQUIREMENTS.md v0.12.1. Bidirectional grep test: every `tracked_in` value resolves to a REQUIREMENTS.md v0.12.1 line."
    - "CHANGELOG.md gains a `## [v0.12.0]` finalized section listing every shipped REQ-ID across P56-P63 + naming the v0.12.1 carry-forward set. Per success criterion 5 in ROADMAP.md P63."
    - "Tag-gate script lands at `.planning/milestones/v0.12.0-phases/tag-v0.12.0.sh` per success criterion 6 in ROADMAP.md P63. Mirrors v0.11.x guard pattern (>= 6 guards: clean tree, on `main`, version match in Cargo.toml, CHANGELOG entry exists, tests green per ci.yml status, signed tag). Owner runs the tag -- orchestrator does NOT push."
    - "Banned word `replace` MUST NOT appear in any new content."
    - "Wave 5 commit subject: `docs(p63): MIGRATE-03 v0.12.1 milestone scaffold + CHANGELOG v0.12.0 finalize + tag-gate script`."
    - "Two atomic commits acceptable in this wave: (a) v0.12.1 scaffold + CHANGELOG + tag-script; (b) catalog tracked_in cross-link consistency check (if any tweaks needed). Default: ONE commit covering all three deliverables."
  artifacts:
    - path: ".planning/milestones/v0.12.1-phases/ROADMAP.md"
      provides: "v0.12.1 milestone scope + 4+ placeholder phase entries (Phase 64+)."
      max_lines: 200
    - path: ".planning/milestones/v0.12.1-phases/REQUIREMENTS.md"
      provides: "PERF-01..03, SEC-01..02, CROSS-01..02, MSRV-01, BINSTALL-01, LATEST-PTR-01, RELEASE-PAT-01, ERR-OTHER-01, SUBJ-RUNNER-01, SUBJ-AUTODISPATCH-01, SUBJ-HARDGATE-01 placeholders with scope + source pointers."
      max_lines: 250
    - path: ".planning/milestones/v0.12.0-phases/tag-v0.12.0.sh"
      provides: "Tag-cut script with >= 6 guards mirroring v0.11.x pattern."
      max_lines: 120
    - path: "CHANGELOG.md"
      provides: "[v0.12.0] entry summarizing P56-P63 + listing every shipped REQ-ID + naming v0.12.1 carry-forward set."
  key_links:
    - from: "quality/catalogs/{perf-targets,security-gates,cross-platform}.json (Wave 1 stub rows)"
      to: ".planning/milestones/v0.12.1-phases/REQUIREMENTS.md (PERF-*, SEC-*, CROSS-* lines)"
      via: "row.waiver.tracked_in is `v0.12.1 PERF-01` etc; the line MUST exist in REQUIREMENTS.md v0.12.1"
      pattern: "v0.12.1"
    - from: ".planning/milestones/v0.12.0-phases/tag-v0.12.0.sh"
      to: "v0.11.x tag scripts at .planning/milestones/v0.{8,9,10,11}.0-phases/tag-*.sh"
      via: "guard pattern lift; same shape (clean tree / branch / version-match / CHANGELOG / tests / signed)"
      pattern: "tag-"
    - from: "CHANGELOG.md ## [v0.12.0]"
      to: ".planning/REQUIREMENTS.md ## v0.12.0"
      via: "REQ-ID list in CHANGELOG matches the SHIPPED rows in REQUIREMENTS.md"
      pattern: "\\[v0.12.0\\]"
    - from: "freshness/no-loose-roadmap-or-requirements verifier"
      to: ".planning/milestones/v0.12.1-phases/{ROADMAP,REQUIREMENTS}.md (location)"
      via: "files INSIDE *-phases/, not at .planning/milestones/ top-level"
      pattern: "v0.12.1-phases"
---

<objective>
Wave 5 -- MIGRATE-03 v0.12.1 milestone scaffold + CHANGELOG finalize + tag-gate script.

Three deliverables:

1. **`.planning/milestones/v0.12.1-phases/ROADMAP.md` + `REQUIREMENTS.md`** -- per CLAUDE.md `.planning/milestones/` convention (Option B: INSIDE the `*-phases/` dir, never loose at top-level; the freshness verifier `freshness/no-loose-roadmap-or-requirements` enforces this). REQUIREMENTS.md lists every carry-forward placeholder; ROADMAP.md has at least 4 placeholder phase entries.

2. **`CHANGELOG.md` `## [v0.12.0]` finalized section** -- summarizes P56-P63 + lists every shipped REQ-ID + names the v0.12.1 carry-forward set.

3. **`.planning/milestones/v0.12.0-phases/tag-v0.12.0.sh`** -- tag-cut script with >= 6 guards mirroring v0.11.x pattern. Owner runs the tag; orchestrator does NOT push.

Wave 5 depends on Wave 1 (catalog stubs with `tracked_in` pointers) and Wave 2 (audit doc -- needed only insofar as the audit's CLAUDE.md / SURPRISES tweaks may add cross-link entries; non-blocking). Wave 5 is independent of Wave 3 (cargo wave) and Wave 4 (cohesion pass).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@quality/catalogs/perf-targets.json
@quality/catalogs/security-gates.json
@quality/catalogs/cross-platform.json
@.planning/milestones/v0.11.0-phases/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold .planning/milestones/v0.12.1-phases/{ROADMAP,REQUIREMENTS}.md</name>
  <files>.planning/milestones/v0.12.1-phases/ROADMAP.md, .planning/milestones/v0.12.1-phases/REQUIREMENTS.md</files>
  <action>
1. `mkdir -p .planning/milestones/v0.12.1-phases`.
2. Find a v0.11.x scaffold for shape reference: `ls .planning/milestones/v0.11.0-phases/` and read its ROADMAP.md + REQUIREMENTS.md to mimic the format.
3. Create `.planning/milestones/v0.12.1-phases/REQUIREMENTS.md`:
   ```markdown
   # v0.12.1 Requirements

   _Carry-forward bundle from v0.12.0 (P63 MIGRATE-03 -- 2026-04-28). Every item below is anchored to a v0.12.0 source: a stub catalog row, a SURPRISES entry, or a v0.11.x carry-forward._

   ## Scope

   v0.12.1 closes the v0.12.0 carry-forward debts: perf-dimension full implementation, security-dimension stubs->real, cross-platform rehearsals, MSRV / binstall / latest-pointer fixes, the Error::Other 156->144 completion (POLISH2-09 from v0.11.1), and three subjective-dimension runner-invariant fixes from P61 Wave G.

   ## Requirements

   ### Perf dimension (full implementation)
   - [ ] **PERF-01** -- Latency vs headline-copy cross-check. Wire `quality/gates/perf/latency-vs-headline-copy.sh` to (a) run `quality/gates/perf/latency-bench.sh`, (b) parse the bench output, (c) diff per-backend numbers against the headline numbers in `docs/benchmarks/latency.md` within +/-15% tolerance. Source: `quality/catalogs/perf-targets.json` row `perf/latency-vs-headline-copy` (P63 Wave 1 stub).
   - [ ] **PERF-02** -- Token-economy bench cross-check. Wire `quality/gates/perf/token-economy-bench.sh` to run `python3 quality/gates/perf/bench_token_economy.py` + cross-check against headline numbers in user-facing docs. Source: stub row `perf/token-economy-bench`.
   - [ ] **PERF-03** -- Golden-path envelope (post-release). Wire `quality/gates/perf/golden-path-envelope.sh` to observe latency against simulator + sanctioned real backends; assert envelope per `docs/benchmarks/latency.md`. Cron schedule + sanctioned creds. Source: stub row `perf/golden-path-envelope`.

   ### Security dimension (stubs -> real)
   - [ ] **SEC-01** -- Allowlist-enforcement gate. Wire `quality/gates/security/allowlist-enforcement.sh` to run a fuzz test that attempts outbound HTTP to an origin not in `REPOSIX_ALLOWED_ORIGINS` and asserts the `http::client()` factory rejects with the documented error. Source: stub row `security/allowlist-enforcement` + CLAUDE.md threat-model § "Outbound HTTP allowlist".
   - [ ] **SEC-02** -- Audit-immutability test. Wire `quality/gates/security/audit-immutability.sh` to attempt UPDATE/DELETE against either audit table (`audit_events_cache`, `audit_events`) and assert the SQLite layer rejects (append-only by schema, WAL mode). Source: stub row `security/audit-immutability` + CLAUDE.md OP-3.

   ### Cross-platform rehearsals
   - [ ] **CROSS-01** -- Windows-2022 GH Actions runner rehearsal of curl-installer + cargo-binstall paths. Source: stub row `cross-platform/windows-2022-rehearsal` + REQUIREMENTS.md MIGRATE-03 (d).
   - [ ] **CROSS-02** -- macOS-14 GH Actions runner rehearsal. Source: stub row `cross-platform/macos-14-rehearsal`.

   ### MSRV / binstall / release-pipeline carry-forwards (from v0.12.0 P56 SURPRISES)
   - [ ] **MSRV-01** -- Bump Rust MSRV 1.82 -> 1.85 OR cap transitive `block-buffer` at `<0.12`. Currently `cargo install reposix-cli` from crates.io fails on MSRV 1.82 because `block-buffer-0.12.0` requires `edition2024`. Source: REQUIREMENTS.md MIGRATE-03 (d) + P56 SURPRISES.md.
   - [ ] **BINSTALL-01** -- `[package.metadata.binstall]` blocks (~10 LOC) in `crates/reposix-cli/Cargo.toml` + `crates/reposix-remote/Cargo.toml` rewritten to match release.yml archive shape (`reposix-cli-v${version}` tag prefix, `reposix-v${version}-${target}.tar.gz` archive basename, x86_64-unknown-linux-musl + aarch64-unknown-linux-musl target overrides). Lifts `install/cargo-binstall` PARTIAL -> PASS. Source: REQUIREMENTS.md MIGRATE-03 (c) + P56 SURPRISES.md.
   - [ ] **LATEST-PTR-01** -- Pin `releases/latest/download/...` pointer to the cli release after every per-crate release sequence. Either `gh release create --latest` flag OR release-plz config to publish reposix-cli last. Source: REQUIREMENTS.md MIGRATE-03 (a) + P56 SURPRISES.md row 2.
   - [ ] **RELEASE-PAT-01** -- release-plz workflow uses fine-grained PAT (or adds a post-tag `gh workflow run` step) so `GITHUB_TOKEN`-pushed tags trigger `release.yml` instead of being silently dropped by GH loop-prevention. Source: REQUIREMENTS.md MIGRATE-03 (b) + P56 SURPRISES.md row 1.

   ### v0.11.1 carry-forward
   - [ ] **ERR-OTHER-01** -- Complete the `Error::Other(String)` 156 -> 144 partial migration (POLISH2-09 from v0.11.1; stubbed in v0.12.0 MIGRATE-03). No NEW `Error::Other(String)` sites accepted in v0.12.0; v0.12.1 closes the migration.

   ### Subjective dimension runner invariants (from P61 Wave G)
   - [ ] **SUBJ-RUNNER-01** -- Dispatch-and-preserve runner invariant. Extend `quality/runners/run.py` `run_row` so a row with `kind=subagent-graded` AND a recent artifact whose `dispatched_via` starts with `Wave-G-Path-A` or `Path-A` is treated as authoritative -- runner reads score + verdict from the artifact, sets row.status from the score-vs-threshold mapping, and does NOT overwrite. Source: REQUIREMENTS.md MIGRATE-03 (e) + P61 Wave G.
   - [ ] **SUBJ-AUTODISPATCH-01** -- CI auto-dispatch (Anthropic API auth on GH Actions runners). Source: REQUIREMENTS.md MIGRATE-03 (f).
   - [ ] **SUBJ-HARDGATE-01** -- Hard-gate chaining for `release.yml` waiting on `quality-pre-release.yml` verdict (composite workflow OR workflow_run trigger). Source: REQUIREMENTS.md MIGRATE-03 (g) + P56 SURPRISES row 1.

   ## Out of Scope (deferred beyond v0.12.1)

   - Threat-model rewrite for v0.9.0 architecture (deferred to a separate security-focused milestone).

   ## Traceability

   13 requirements -> >= 4 phases (assigned during /gsd-roadmapper for v0.12.1).
   ```
4. Create `.planning/milestones/v0.12.1-phases/ROADMAP.md` with at least 4 placeholder phases (e.g., Phase 64 -- perf full implementation; Phase 65 -- security stubs->real; Phase 66 -- cross-platform rehearsals; Phase 67 -- MSRV / binstall / release-pipeline + Error::Other completion; Phase 68 -- subjective runner invariants). Each entry: Goal (1-2 sentences), Requirements (REQ-IDs), Depends on (v0.12.0 GREEN OR previous phase), Success criteria placeholder. Detail can be light -- the v0.12.1 planner re-plans via `/gsd-plan-phase`.
5. Run the freshness verifier on the new files: `python3 quality/gates/structure/freshness-invariants.py --row-id structure/no-loose-top-level-planning-audits` (or the equivalent runner) -- must stay PASS. The new files are INSIDE `*-phases/`, so the loose-roadmap check is satisfied.
6. Banned-words sweep on both new files.
  </action>
  <verify>
    <automated>test -d .planning/milestones/v0.12.1-phases && test -f .planning/milestones/v0.12.1-phases/ROADMAP.md && test -f .planning/milestones/v0.12.1-phases/REQUIREMENTS.md && grep -c '^- \[ \] \*\*' .planning/milestones/v0.12.1-phases/REQUIREMENTS.md | awk '{ if ($1 < 13) { print "FAIL: "$1" REQ items, want >=13"; exit 1 } else { print "OK: "$1" REQ items" } }' && grep -c '^### Phase' .planning/milestones/v0.12.1-phases/ROADMAP.md | awk '{ if ($1 < 4) { print "FAIL: "$1" phases, want >=4"; exit 1 } else { print "OK: "$1" phases" } }'</automated>
  </verify>
  <done>v0.12.1 scaffold lands inside `*-phases/`; >=13 REQ-ID placeholders; >=4 phase placeholders; loose-roadmap freshness verifier still PASS.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG [v0.12.0] finalize + tag-v0.12.0.sh</name>
  <files>CHANGELOG.md, .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh</files>
  <action>
1. Read `CHANGELOG.md` to find the v0.12.0 stub or "Unreleased" section.
2. Edit `CHANGELOG.md` to finalize the `## [v0.12.0] -- 2026-04-28 -- Quality Gates` section. Structure:
   ```markdown
   ## [v0.12.0] -- 2026-04-28 -- Quality Gates

   First milestone shipping the dimension-tagged Quality Gates framework at `quality/{gates,catalogs,reports,runners}/`. Eight phases (P56-P63) absorb existing scripts/checks into 8 dimensions (code, docs-build, docs-repro, release, structure, agent-ux, perf, security) and ship the autonomous-mode runtime contract at `quality/PROTOCOL.md`.

   ### Shipped

   - **P56** -- Release pipeline + install-evidence pattern (RELEASE-01, RELEASE-02, RELEASE-03).
   - **P57** -- Quality Gates skeleton + structure dimension (QG-01..09, STRUCT-01..02, SIMPLIFY-01..03, POLISH-STRUCT).
   - **P58** -- Release dimension + code dimension absorption (RELEASE-04, SIMPLIFY-04..05, POLISH-RELEASE, POLISH-CODE-stub).
   - **P59** -- Docs-repro + agent-ux + perf-relocate (DOCS-REPRO-01..04, SIMPLIFY-06..07, SIMPLIFY-11, POLISH-DOCS-REPRO, POLISH-AGENT-UX).
   - **P60** -- Docs-build dimension (DOCS-BUILD-01, BADGE-01, SIMPLIFY-08..10, POLISH-DOCS-BUILD).
   - **P61** -- Subjective gates + skill-freshness TTL (SUBJ-01, SUBJ-02, SUBJ-03, POLISH-SUBJECTIVE).
   - **P62** -- Repo-org-gaps cleanup (ORG-01, POLISH-ORG).
   - **P63** -- Retire migrated sources + cohesion pass + v0.12.1 carry-forward (MIGRATE-01, MIGRATE-02, MIGRATE-03, SIMPLIFY-12, POLISH-CODE-final).

   ### v0.12.1 carry-forward

   PERF-01..03, SEC-01..02, CROSS-01..02, MSRV-01, BINSTALL-01, LATEST-PTR-01, RELEASE-PAT-01, ERR-OTHER-01 (POLISH2-09 from v0.11.1), SUBJ-RUNNER-01..HARDGATE-01. See `.planning/milestones/v0.12.1-phases/REQUIREMENTS.md`.
   ```
3. Create `.planning/milestones/v0.12.0-phases/tag-v0.12.0.sh` mirroring v0.11.x guards. If `.planning/milestones/v0.11.0-phases/tag-v0.11.0.sh` exists, lift its shape and update the version. Required guards (>= 6):
   ```bash
   #!/usr/bin/env bash
   # tag-v0.12.0.sh -- v0.12.0 milestone tag-cut. Owner runs this; orchestrator does NOT push the tag.
   set -euo pipefail
   VERSION="0.12.0"
   TAG="v${VERSION}"
   # Guard 1: clean tree
   git diff --quiet && git diff --cached --quiet || { echo "FAIL: working tree not clean"; exit 1; }
   # Guard 2: on main
   BRANCH=$(git rev-parse --abbrev-ref HEAD); [ "$BRANCH" = "main" ] || { echo "FAIL: not on main (on $BRANCH)"; exit 1; }
   # Guard 3: version match in workspace Cargo.toml (or per-crate; project uses release-plz per-crate tags but milestone tag is whole-workspace)
   grep -q "^version = \"${VERSION}\"" Cargo.toml || grep -q "^version = \"${VERSION}\"" crates/reposix-cli/Cargo.toml || { echo "FAIL: version $VERSION not found in Cargo.toml or crates/reposix-cli/Cargo.toml"; exit 1; }
   # Guard 4: CHANGELOG entry
   grep -q "^## \[v${VERSION}\]" CHANGELOG.md || { echo "FAIL: CHANGELOG.md missing ## [v${VERSION}] entry"; exit 1; }
   # Guard 5: ci.yml latest test job is GREEN for the current branch
   gh run list --workflow ci.yml --branch main --limit 1 --json conclusion --jq '.[0].conclusion' | grep -q success || { echo "FAIL: latest ci.yml on main is not success"; exit 1; }
   # Guard 6: P63 verdict GREEN (milestone-close gate)
   ls quality/reports/verdicts/p63/*.md >/dev/null 2>&1 && grep -q '^Verdict: GREEN' "$(ls -t quality/reports/verdicts/p63/*.md | head -1)" || { echo "FAIL: P63 verdict missing or not GREEN"; exit 1; }
   # Optional: tag is signed if the user has a signing key configured
   git tag -s -a "$TAG" -m "v${VERSION} -- Quality Gates" || git tag -a "$TAG" -m "v${VERSION} -- Quality Gates"
   echo "OK: tag $TAG created locally. Owner pushes via: git push origin $TAG"
   ```
4. `chmod +x .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh`.
5. Bash syntax check: `bash -n .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh`.
6. DO NOT run the tag script. The owner runs it at milestone close.
7. Banned-words sweep on both files.
  </action>
  <verify>
    <automated>grep -q '^## \[v0.12.0\]' CHANGELOG.md && grep -q 'v0.12.1 carry-forward' CHANGELOG.md && bash -n .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh && test -x .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh && GUARD_COUNT=$(grep -c '^# Guard' .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh); [ "$GUARD_COUNT" -ge 6 ] && echo "OK $GUARD_COUNT guards"</automated>
  </verify>
  <done>CHANGELOG [v0.12.0] entry + carry-forward list; tag script with >= 6 guards; bash syntax-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-link consistency check + atomic Wave 5 commit</name>
  <files>(commit only)</files>
  <action>
1. Cross-link consistency: every `tracked_in` value in `quality/catalogs/{perf-targets,security-gates,cross-platform}.json` MUST resolve to a REQ-ID line in `.planning/milestones/v0.12.1-phases/REQUIREMENTS.md`. Run:
   ```bash
   python3 -c "
   import json, re, sys
   reqs = open('.planning/milestones/v0.12.1-phases/REQUIREMENTS.md').read()
   bad = []
   for f in ['quality/catalogs/perf-targets.json','quality/catalogs/security-gates.json','quality/catalogs/cross-platform.json']:
       for r in json.load(open(f))['rows']:
           ti = (r.get('waiver') or {}).get('tracked_in','')
           m = re.search(r'v0\.12\.1\s+([A-Z]+-\d+)', ti)
           if m and not re.search(r'\*\*'+re.escape(m.group(1))+r'\*\*', reqs):
               bad.append((r['id'], ti, m.group(1)))
   print('BAD:' if bad else 'OK', bad)
   sys.exit(1 if bad else 0)
   "
   ```
   If BAD: edit REQUIREMENTS.md to add the missing REQ-ID. Re-run.
2. `git status --short` -- expect: new `.planning/milestones/v0.12.1-phases/{ROADMAP,REQUIREMENTS}.md`, new `.planning/milestones/v0.12.0-phases/tag-v0.12.0.sh`, modified `CHANGELOG.md`.
3. Banned-words final sweep on staged diff.
4. Commit (HEREDOC):
```
docs(p63): MIGRATE-03 v0.12.1 milestone scaffold + CHANGELOG v0.12.0 finalize + tag-gate script

Closes MIGRATE-03 -- v0.12.1 carry-forward filing per CLAUDE.md
`.planning/milestones/` convention (planning docs INSIDE *-phases/, never
loose at top-level; freshness/no-loose-roadmap-or-requirements verifier
stays GREEN).

v0.12.1 scaffold (.planning/milestones/v0.12.1-phases/):
- REQUIREMENTS.md: 13 carry-forward placeholders (PERF-01..03, SEC-01..02,
  CROSS-01..02, MSRV-01, BINSTALL-01, LATEST-PTR-01, RELEASE-PAT-01,
  ERR-OTHER-01 from v0.11.1 POLISH2-09, SUBJ-RUNNER-01..HARDGATE-01 from
  P61 Wave G). Each placeholder names its v0.12.0 source (catalog stub
  row OR SURPRISES line OR REQUIREMENTS line).
- ROADMAP.md: 5 placeholder phases (P64-P68) bundling the carry-forward
  scope. Detail lands via /gsd-plan-phase in v0.12.1.

CHANGELOG [v0.12.0] finalized: P56-P63 summary + shipped REQ-IDs +
v0.12.1 carry-forward set named.

Tag-gate script at .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh:
6 guards (clean tree, on main, version match, CHANGELOG entry, ci.yml
GREEN, P63 verdict GREEN). Owner runs the tag; orchestrator does NOT push.

Bidirectional cross-link verified: every catalog stub-row tracked_in
resolves to a REQUIREMENTS.md v0.12.1 REQ-ID.

Cites: MIGRATE-03.
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q 'docs(p63): MIGRATE-03 v0.12.1 milestone scaffold' && python3 -c "import json,re; reqs=open('.planning/milestones/v0.12.1-phases/REQUIREMENTS.md').read(); bad=[]; [bad.append((r['id'],r.get('waiver',{}).get('tracked_in',''))) for f in ['quality/catalogs/perf-targets.json','quality/catalogs/security-gates.json','quality/catalogs/cross-platform.json'] for r in json.load(open(f))['rows'] if (m:=re.search(r'v0\.12\.1\s+([A-Z]+-\d+)',(r.get('waiver') or {}).get('tracked_in',''))) and not re.search(r'\*\*'+re.escape(m.group(1))+r'\*\*',reqs)]; assert not bad, bad; print('OK')"</automated>
  </verify>
  <done>Wave 5 commit shipped; bidirectional cross-link verified; CHANGELOG + tag-script in place.</done>
</task>

</tasks>

<verification>
- `.planning/milestones/v0.12.1-phases/{ROADMAP,REQUIREMENTS}.md` exist INSIDE the phases dir (no loose top-level files).
- Freshness verifier `freshness/no-loose-roadmap-or-requirements` still PASS.
- CHANGELOG.md has `## [v0.12.0]` + `v0.12.1 carry-forward` line.
- `tag-v0.12.0.sh` has >= 6 guards + executable + bash-syntax-clean.
- Every catalog stub-row `tracked_in` resolves to a REQUIREMENTS.md v0.12.1 REQ-ID.
</verification>

<success_criteria>
- MIGRATE-03 v0.12.1 carry-forward fully filed in proper milestone scaffold (NOT loose, per the freshness invariant).
- CHANGELOG [v0.12.0] entry summarizes shipped + carry-forward.
- Tag script ready for owner-run at milestone close.
- Bidirectional cross-link health: catalog stubs <-> REQUIREMENTS.md v0.12.1.
</success_criteria>

<output>
After completion, create `.planning/phases/63-retire-migrated-sources-cohesion-v012-1-carry-forward/63-05-SUMMARY.md` describing: 13 REQ placeholder list, 5 phase scaffold, CHANGELOG entry, tag-script guard count, cross-link consistency outcome.
</output>
