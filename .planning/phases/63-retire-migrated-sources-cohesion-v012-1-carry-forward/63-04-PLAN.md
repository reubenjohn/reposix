---
phase: 63-retire-migrated-sources-cohesion-v012-1-carry-forward
plan: 04
type: execute
wave: 3
depends_on: ['63-01']
files_modified:
  - CLAUDE.md
  - quality/gates/code/README.md
  - quality/gates/release/README.md
  - quality/gates/docs-build/README.md
  - quality/gates/docs-repro/README.md
  - quality/gates/agent-ux/README.md
  - quality/gates/perf/README.md
  - quality/gates/structure/README.md
  - quality/gates/security/README.md
  - quality/gates/structure/cross-link-audit.py
  - quality/reports/audits/cross-link-audit-p63.md
autonomous: true
requirements:
  - MIGRATE-02
must_haves:
  truths:
    - "MIGRATE-02 cohesion pass per REQUIREMENTS.md: an AUDIT of the per-phase incremental updates from P56-P62, not a rewrite. The deliverable is the absence of contradictions + stale cross-references."
    - "Per-dim READMEs (8: code, release, docs-build, docs-repro, agent-ux, perf, structure, security) are trimmed to verifier-table + conventions only. Runtime detail (cadence semantics, waiver protocol, verifier-subagent template) is cross-linked to quality/PROTOCOL.md (already present from P57+) instead of being duplicated."
    - "A new stdlib script `quality/gates/structure/cross-link-audit.py` extracts every relative path mention from CLAUDE.md + quality/PROTOCOL.md + 8 dim READMEs and asserts each exists. Stale paths = FAIL. Wave 4 ships the verifier AND fixes any stale paths found in the first run."
    - "Per-dim README size budget: <= 250 lines. Anything larger after trim gets a flagged note + v0.12.1 follow-up entry."
    - "CLAUDE.md gains a small P63 H3 subsection (~15 lines) summarizing MIGRATE-02 audit outcome. The taxonomy section + meta-rule extension already landed in P57+P58+; this wave only confirms and stitches."
    - "Banned word `replace` MUST NOT appear in any new content. Verified across all touched files."
    - "Wave 4 commit subject: `docs(p63): MIGRATE-02 cohesion pass -- CLAUDE.md cross-link audit + per-dim README normalize`."
    - "Wave 4 is parallel-safe with Wave 3 -- disjoint files, zero cargo invocations."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Audited; gains a ~15-line P63 H3 subsection summarizing MIGRATE-02 outcome (cross-link audit shipped, X paths verified, Y stale fixed, per-dim READMEs normalized)."
    - path: "quality/gates/structure/cross-link-audit.py"
      provides: "Stdlib Python verifier: walks CLAUDE.md + PROTOCOL.md + 8 dim READMEs, regex-extracts relative paths, asserts each exists. Writes JSON + Markdown report."
      max_lines: 200
    - path: "quality/gates/<dim>/README.md (8 files)"
      provides: "Trimmed to verifier-table + conventions; runtime detail cross-linked to PROTOCOL.md."
    - path: "quality/reports/audits/cross-link-audit-p63.md"
      provides: "Audit report: per-doc paths verified, stale-list (target zero), trim notes."
  key_links:
    - from: "CLAUDE.md (Quality Gates section)"
      to: "quality/PROTOCOL.md (verbatim sections)"
      via: "cross-references like 'see quality/PROTOCOL.md § Verifier subagent prompt template'"
      pattern: "quality/PROTOCOL.md"
    - from: "quality/gates/<dim>/README.md (each)"
      to: "quality/PROTOCOL.md"
      via: "cross-link instead of repeating runtime detail"
      pattern: "PROTOCOL.md"
    - from: "Wave 6 verifier subagent"
      to: "Wave 4 cohesion pass"
      via: "subagent re-runs cross-link-audit.py from zero-context"
      pattern: "cross-link-audit"
---

<objective>
Wave 4 -- MIGRATE-02 cohesion pass + cross-link audit. Per REQUIREMENTS.md: "the final cohesion pass + cross-link audit. Per-phase QG-07 each phase already updated CLAUDE.md incrementally; MIGRATE-02 is the final cohesion pass."

Three deliverables:

1. **Cross-link audit verifier** (`quality/gates/structure/cross-link-audit.py`) -- stdlib Python that walks CLAUDE.md + quality/PROTOCOL.md + 8 dim READMEs, extracts every relative path mention, asserts each exists. Writes a report.

2. **Run the audit; fix any stale paths.** Stale = mentioned but not present (typo, moved, deleted in P63 Wave 2). Fix in-line; re-run until clean. Stale-by-design (planned v0.12.1 path) gets a `<!-- planned: v0.12.1 ... -->` marker + the verifier skips that line.

3. **Per-dim README normalize.** Each `quality/gates/<dim>/README.md` is trimmed to verifier-table + conventions. Runtime detail (cadence semantics, waiver TTL rules, verifier-subagent template) is consolidated into `quality/PROTOCOL.md` (already there); dim READMEs cross-link instead. Anything currently >250 lines or duplicating PROTOCOL.md gets trimmed.

4. **CLAUDE.md final P63 H3 subsection** -- ~15 lines under "Quality Gates" summarizing the MIGRATE-02 outcome.

This wave is parallel-safe with Wave 3 (cargo wave) -- disjoint files, zero cargo invocations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@quality/PROTOCOL.md
@quality/gates/code/README.md
@quality/gates/release/README.md
@quality/gates/docs-build/README.md
@quality/gates/docs-repro/README.md
@quality/gates/agent-ux/README.md
@quality/gates/perf/README.md
@quality/gates/structure/README.md
@quality/gates/security/README.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-link-audit.py + run + fix stale paths</name>
  <files>quality/gates/structure/cross-link-audit.py, quality/reports/audits/cross-link-audit-p63.md, quality/reports/verifications/structure/cross-link-audit-p63.json</files>
  <action>
1. Create `quality/gates/structure/cross-link-audit.py` (stdlib only):
   - argparse: `--docs` (default list of CLAUDE.md + quality/PROTOCOL.md + 8 dim READMEs), `--report` (Markdown output path), `--json` (JSON evidence path).
   - Per doc: read content; regex-extract candidate paths matching `\b(quality/(gates|catalogs|runners|reports)/[\w./-]+)`, `\b(\.planning/[\w./-]+)`, `\b(scripts/[\w./-]+)`, `\b(crates/[\w./-]+)`, `\b(docs/[\w./-]+)`, `\b(examples/[\w./-]+)`.
   - Strip trailing punctuation (. , ) : ;).
   - Skip globs (`*`, `**`), skip lines with `<!-- planned:` marker, skip code-fence content where appropriate (heuristic: skip lines starting `    ` or inside ``` fences).
   - For each unique path: `os.path.exists(path)` from repo root.
   - Write Markdown report (per-doc section, list of (path, exists Y/N), summary counts).
   - Write JSON evidence: `{"audited_docs": [...], "paths_total": N, "paths_stale": M, "stale_list": [...], "status": "PASS|FAIL", "verified_at": "<RFC3339>"}`.
   - Exit 0 only if zero stale paths.
2. `chmod +x quality/gates/structure/cross-link-audit.py`.
3. Run: `python3 quality/gates/structure/cross-link-audit.py`.
4. If stale paths found: edit the doc to fix (typo, moved-path update). For genuinely planned future paths, add `<!-- planned: v0.12.1 ... -->` adjacent. Re-run until exit 0.
5. Banned-words sweep on the script + report: zero hits.
  </action>
  <verify>
    <automated>test -x quality/gates/structure/cross-link-audit.py && python3 quality/gates/structure/cross-link-audit.py && python3 -c "import json; j=json.load(open('quality/reports/verifications/structure/cross-link-audit-p63.json')); assert j['paths_stale']==0 and j['status']=='PASS', j; print(j['paths_total'], 'paths verified, 0 stale')"</automated>
  </verify>
  <done>cross-link-audit.py runs to PASS; Markdown + JSON reports exist; zero stale paths.</done>
</task>

<task type="auto">
  <name>Task 2: Per-dim README normalize (verifier-table + conventions only)</name>
  <files>quality/gates/code/README.md, quality/gates/release/README.md, quality/gates/docs-build/README.md, quality/gates/docs-repro/README.md, quality/gates/agent-ux/README.md, quality/gates/perf/README.md, quality/gates/structure/README.md, quality/gates/security/README.md</files>
  <action>
For each of the 8 dim READMEs:

1. Read it; measure `wc -l`.
2. Identify content categories:
   - **Keep:** verifier table (row id, kind, cadence, blast_radius, owner_hint summary), file inventory (each `.sh`/`.py` in the dim), conventions specific to the dim.
   - **Cross-link instead:** generic cadence semantics, generic waiver protocol, generic verifier-subagent template (already in quality/PROTOCOL.md).
3. For each cross-link candidate, swap inlined content for `See quality/PROTOCOL.md § <section>.`
4. Confirm a small "P63 cohesion pass: trimmed to verifier-table + conventions; runtime detail at quality/PROTOCOL.md" footer line.
5. Re-measure; target <= 250 lines. Any survivor > 250 gets a note in the audit report (Task 1 output) flagging v0.12.1 follow-up.
6. Re-run cross-link-audit.py: still PASS (path edits in this task may have introduced new mentions; assert clean).
7. Banned-words sweep: zero hits.

This is editorial dedup work, not lossy compression. Preserve dimension-specific content; remove duplications of PROTOCOL.md.
  </action>
  <verify>
    <automated>for f in quality/gates/{code,release,docs-build,docs-repro,agent-ux,perf,structure,security}/README.md; do test -f "$f"; done && python3 quality/gates/structure/cross-link-audit.py >/dev/null && BANNED=$(grep -nEi '\breplace\b|\breplaces\b|\breplacing\b|\breplaced\b|\breplacement\b' quality/gates/{code,release,docs-build,docs-repro,agent-ux,perf,structure,security}/README.md 2>/dev/null | wc -l); [ "$BANNED" = "0" ] && echo "BANNED-WORDS CLEAN"</automated>
  </verify>
  <done>All 8 dim READMEs touched; cross-link-audit still PASS; banned-words clean.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md P63 H3 subsection + atomic Wave 4 commit</name>
  <files>CLAUDE.md</files>
  <action>
1. Edit CLAUDE.md. Locate the "v0.12.0 Quality Gates -- phase log" section (or the equivalent quality/-section anchor). Append a new H3 subsection:
   ```
   ### P63 -- MIGRATE-02 cohesion pass + SIMPLIFY-12 + POLISH-CODE final + v0.12.1 carry-forward (added 2026-04-28)

   Closes the v0.12.0 milestone. Cohesion pass per MIGRATE-02:

   - **Cross-link audit verifier landed** at `quality/gates/structure/cross-link-audit.py` -- walks CLAUDE.md + `quality/PROTOCOL.md` + 8 dim READMEs, asserts every relative path mention exists. Reports at `quality/reports/audits/cross-link-audit-p63.md`. Wave 6 verifier subagent re-runs from zero-context.
   - **Per-dim READMEs normalized** to verifier-table + conventions only. Runtime detail (cadence semantics, waiver TTL, verifier-subagent template) lives canonically in `quality/PROTOCOL.md`; dim READMEs cross-link.
   - **POLISH-CODE final state:** `code/cargo-fmt-clean` PASS via direct `cargo fmt --all -- --check` invocation (read-only, ~5s, safe under ONE cargo at a time rule). `code/cargo-test-pass` stays as `ci-job-status` canonical -- running `cargo nextest run --workspace` from a verifier violates the memory-budget rule + exceeds the pre-pr 10-min cadence cap; CI is the canonical enforcement venue. Tracked-forward to v0.12.1 MIGRATE-03 for per-row local cargo alternatives.
   - **SIMPLIFY-12 audit complete:** scripts-retirement-p63.md records every non-hooks `scripts/` file's disposition (DELETE / SHIM-WAIVED / KEEP-AS-CANONICAL) with caller-scan + commit SHA. Surviving shims have rows in `quality/catalogs/orphan-scripts.json` asserting shim-shape; verifier at `quality/gates/structure/orphan-scripts-audit.py` mechanizes re-grading.
   - **v0.12.1 milestone scaffolded** at `.planning/milestones/v0.12.1-phases/{ROADMAP,REQUIREMENTS}.md` per the `.planning/milestones/` convention. Carry-forward scope (PERF-*, SEC-*, CROSS-*, MSRV / binstall / latest-pointer fixes, Error::Other 156→144 completion) anchored in 3 stub catalog files (`quality/catalogs/{perf-targets,security-gates,cross-platform}.json`).

   Meta-rule extension (when an owner catches a miss): fix the issue, update CLAUDE.md, AND tag the dimension. The dimension tag routes the next agent to the right `quality/catalogs/<dim>.json` + `quality/gates/<dim>/` home.
   ```
2. Run `python3 quality/gates/structure/cross-link-audit.py` -- the new CLAUDE.md content introduces path mentions; they MUST resolve.
3. `git status` -- confirm the expected files are dirty (8 dim READMEs + CLAUDE.md + cross-link-audit.py + 2 reports).
4. `git add` only those paths.
5. Final banned-words sweep on staged diff: `git diff --cached | grep -niE '\breplace\b|\breplaces\b|\breplacing\b|\breplaced\b|\breplacement\b' || echo CLEAN`.
6. Commit (HEREDOC):
```
docs(p63): MIGRATE-02 cohesion pass -- CLAUDE.md cross-link audit + per-dim README normalize

Closes MIGRATE-02 per REQUIREMENTS.md: a final AUDIT of the per-phase
incremental updates from P56-P62. Deliverable is the absence of
contradictions + stale cross-references.

New verifier: quality/gates/structure/cross-link-audit.py walks
CLAUDE.md + quality/PROTOCOL.md + 8 dim READMEs, asserts every relative
path mention exists. N paths verified; M stale fixed in-line.
Audit report at quality/reports/audits/cross-link-audit-p63.md.

Per-dim README normalize: 8 READMEs trimmed to verifier-table +
conventions; runtime detail (cadence, waiver, verifier-subagent
template) cross-linked to quality/PROTOCOL.md instead of duplicated.

CLAUDE.md gains the P63 H3 subsection summarizing MIGRATE-02 +
SIMPLIFY-12 + POLISH-CODE final + v0.12.1 carry-forward outcomes.

Cites: MIGRATE-02.
```
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q 'docs(p63): MIGRATE-02 cohesion pass' && python3 quality/gates/structure/cross-link-audit.py >/dev/null && grep -q 'P63 -- MIGRATE-02 cohesion pass' CLAUDE.md</automated>
  </verify>
  <done>One Wave 4 commit; cross-link audit PASS; CLAUDE.md P63 subsection present.</done>
</task>

</tasks>

<verification>
- `python3 quality/gates/structure/cross-link-audit.py` exits 0.
- `grep -q "P63 -- MIGRATE-02" CLAUDE.md` succeeds.
- All 8 dim READMEs exist + `wc -l <= 250` (or noted in audit doc as v0.12.1 follow-up).
- Banned-words clean across all changed files.
</verification>

<success_criteria>
- Cross-link audit verifier shipped + green; Wave 6 can re-run mechanically.
- Per-dim READMEs are dedup'd against PROTOCOL.md; runtime detail centralized.
- CLAUDE.md cohesion confirmed (P63 H3 subsection + cross-references resolve).
- Zero ad-hoc bash audit-pipelines: the audit is a committed Python verifier per CLAUDE.md OP-4.
</success_criteria>

<output>
After completion, create `.planning/phases/63-retire-migrated-sources-cohesion-v012-1-carry-forward/63-04-SUMMARY.md` describing: paths verified count, stale fixes, README size deltas, CLAUDE.md subsection commit SHA.
</output>
