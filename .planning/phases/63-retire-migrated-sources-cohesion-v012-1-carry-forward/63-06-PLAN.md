---
phase: 63-retire-migrated-sources-cohesion-v012-1-carry-forward
plan: 06
type: execute
wave: 5
depends_on: ['63-01', '63-02', '63-03', '63-04', '63-05']
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - quality/SURPRISES.md
  - quality/reports/verdicts/p63/VERDICT.md
autonomous: false
requirements:
  - MIGRATE-01
  - MIGRATE-02
  - MIGRATE-03
  - SIMPLIFY-12
  - POLISH-CODE
must_haves:
  truths:
    - "Phase close per quality/PROTOCOL.md Step 7-9: (a) unbiased verifier subagent dispatched in-session (Path B precedent from P61) with zero session context, grades every P63-scope catalog row, writes verdict to `quality/reports/verdicts/p63/<RFC3339>.md` and `quality/reports/verdicts/p63/VERDICT.md` (canonical pointer); (b) STATE.md updated with milestone-close cursor; (c) REQUIREMENTS.md flips P63 REQ-IDs (MIGRATE-01, MIGRATE-02, MIGRATE-03, SIMPLIFY-12, POLISH-CODE) to SHIPPED with commit SHA; (d) SURPRISES.md gets the P63 entries (catalog rotations, retirement decisions, cross-link audit findings if any) appended; rotation if active section exceeds 200 lines."
    - "MILESTONE-CLOSE verdict: the verifier subagent ALSO grades cross-phase coherence per ROADMAP.md P63 success criterion 10 -- every prior phase's verdict file exists + GREEN, no orphan catalog rows, no expired waivers without tracked carry-forward. The verdict.md text MUST contain the line `Verdict: GREEN` only if every P0+P1 catalog row across ALL 8 dimensions is PASS or WAIVED (with non-expired `until`)."
    - "Verifier subagent prompt is the verbatim copy from quality/PROTOCOL.md § 'Verifier subagent prompt template'; in-session disclosure (Path B) is acceptable per P61 precedent. Subagent reads zero of this session's context: it ingests REQUIREMENTS.md + catalog files + CLAUDE.md + SURPRISES.md from disk only."
    - "If verdict = RED on any P0+P1 row: phase does NOT close. Loop back to fix the failing row (likely a Wave 2-5 deliverable that needs a follow-up commit). DO NOT negotiate the catalog row down."
    - "STATE.md update: `last_activity` line names the P63 commit SHAs + the milestone-close verdict reference. `## Current Position` flips to milestone=v0.12.0 status=shipped (or status=ready-to-tag pending owner tag-cut). Cursor advances to v0.12.1 planning."
    - "REQUIREMENTS.md flips: every v0.12.0 P63 REQ-ID gets `[x]` + commit SHA pointer in the per-REQ line. Sanity check: ALL 49 v0.12.0 REQ-IDs (40 original + 8 POLISH-* + POLISH-CODE counted in BOTH P58-stub and P63-final) end up flipped after this wave -- this wave is the LAST flip set."
    - "SURPRISES.md: any P63 pivot/discovery from Waves 2-5 gets a one-line entry. If a deletion got reverted to SHIM (Wave 2 caller-scan surprise) -- entry. If cross-link audit found a long-stale path -- entry. If POLISH-CODE wiring revealed a memory-budget edge case -- entry. Active section <= 200 lines per the rotation rule; rotate to `quality/SURPRISES-archive-2026-Q2.md` if needed (P59 precedent)."
    - "Banned word `replace` MUST NOT appear in any new content."
    - "Wave 6 commit subject: `feat(p63): phase close + milestone-close verdict GREEN + STATE/REQUIREMENTS/SURPRISES (v0.12.0 ready to tag)`. Or, if verifier subagent grades RED on any row, this commit is a Wave-6-fix instead and the GREEN-grade close ships in a follow-up commit."
    - "autonomous=false because the verifier subagent dispatch is an in-session checkpoint (Path B disclosure -- the orchestrator declares 'I am about to dispatch the verifier subagent' as a checkpoint moment so the user can intercept if needed)."
  artifacts:
    - path: "quality/reports/verdicts/p63/<RFC3339>.md"
      provides: "Timestamped verdict markdown table (per-row PASS/FAIL/WAIVED grading) from the verifier subagent. References every P63 catalog row + cross-phase coherence checks."
    - path: "quality/reports/verdicts/p63/VERDICT.md"
      provides: "Canonical pointer (symlink OR copy of latest timestamped verdict). Wave 7 of v0.12.0 milestone-close logic OR `tag-v0.12.0.sh` reads this."
    - path: ".planning/STATE.md"
      provides: "milestone-close cursor; `last_activity` names P63 commit SHAs + milestone-close verdict ref; cursor advances to v0.12.1 planning."
    - path: ".planning/REQUIREMENTS.md"
      provides: "Last-flip set: MIGRATE-01..03, SIMPLIFY-12, POLISH-CODE flipped to [x] with commit SHA. Sanity-check: ALL 49 v0.12.0 REQ-IDs are now [x]."
    - path: "quality/SURPRISES.md"
      provides: "P63 entries appended; rotated if > 200 active lines."
  key_links:
    - from: "Wave 6 verifier subagent"
      to: "every catalog row in scope of P63 + cross-phase coherence (P56-P62 verdicts)"
      via: "verbatim PROTOCOL.md § 'Verifier subagent prompt template'"
      pattern: "Verdict: GREEN"
    - from: "STATE.md last_activity"
      to: "Wave 1-6 commit SHAs"
      via: "git log --oneline | grep p63 | reference each SHA"
      pattern: "p63"
    - from: "REQUIREMENTS.md [x] flips"
      to: "Wave 6 commit (this commit) + Wave 1-5 commits"
      via: "each [x] line cites the commit that satisfied it"
      pattern: "\\[x\\]"
    - from: "tag-v0.12.0.sh Guard 6 (P63 verdict GREEN)"
      to: "quality/reports/verdicts/p63/VERDICT.md"
      via: "tag script reads `Verdict: GREEN` line; refuses tag if absent"
      pattern: "Verdict: GREEN"
---

<objective>
Wave 6 -- phase close + milestone-close verdict.

Six deliverables:

1. **Dispatch verifier subagent (Path B in-session disclosure)** -- verbatim PROTOCOL.md prompt. Subagent reads catalog files, CLAUDE.md, SURPRISES.md, REQUIREMENTS.md from disk only; grades every P63 catalog row + cross-phase coherence (P56-P62 verdicts must all be GREEN; no orphan rows; no expired-without-tracked-forward waivers).

2. **Milestone-close cross-phase coherence** -- per ROADMAP.md P63 success criterion 10. The verdict union of P56-P63 must show all P0+P1 rows PASS or WAIVED (with non-expired `until`).

3. **STATE.md update** -- milestone cursor advances; last_activity names P63 SHAs + verdict ref.

4. **REQUIREMENTS.md last-flip** -- P63 REQ-IDs flipped to [x] with commit SHAs. ALL 49 v0.12.0 REQ-IDs are now flipped.

5. **SURPRISES.md update** -- P63 entries appended; rotation if > 200 active lines (P59 precedent).

6. **Atomic Wave 6 commit** -- only proceeds if verifier verdict = GREEN.

If verdict = RED on any P0+P1 row: STOP. The orchestrator loops back to fix the failing row (likely a Wave 2-5 deliverable needing a follow-up commit). DO NOT negotiate the row down.

This wave has `autonomous=false` because the verifier-subagent dispatch is an in-session checkpoint (Path B): the orchestrator declares "about to dispatch verifier subagent for milestone-close verdict" so the user can intercept if anything looks off before the GREEN/RED grade lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@quality/PROTOCOL.md
</execution_context>

<context>
@quality/PROTOCOL.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@quality/SURPRISES.md
@quality/reports/verdicts/p62/VERDICT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verifier subagent dispatch (Path B in-session disclosure)</name>
  <what-built>
Waves 1-5 of P63 are complete. The orchestrator is about to dispatch the unbiased verifier subagent per quality/PROTOCOL.md Step 7. The subagent will read REQUIREMENTS.md + catalog files + CLAUDE.md + SURPRISES.md from disk only (zero session context) and grade every P63 catalog row + cross-phase coherence (P56-P62 verdicts).

The dispatched subagent uses the verbatim prompt at quality/PROTOCOL.md § "Verifier subagent prompt template". The output goes to:
- `quality/reports/verdicts/p63/<RFC3339>.md` (timestamped)
- `quality/reports/verdicts/p63/VERDICT.md` (canonical pointer copy)

Per Path B in-session precedent from P61: this dispatch is disclosed BEFORE it runs so the owner can intercept if any Wave 1-5 deliverable looks suspect.
  </what-built>
  <how-to-verify>
1. Confirm Waves 1-5 commits are in `git log --oneline -10`:
   - Wave 1: `docs(p63): catalog-first commit ...`
   - Wave 2: `chore(p63): SIMPLIFY-12 audit + MIGRATE-01 retirement decisions ...`
   - Wave 3: `feat(p63): POLISH-CODE final ...`
   - Wave 4: `docs(p63): MIGRATE-02 cohesion pass ...`
   - Wave 5: `docs(p63): MIGRATE-03 v0.12.1 milestone scaffold ...`
2. Confirm `python3 quality/gates/code/cargo-fmt-clean.sh` PASS, `python3 quality/gates/structure/cross-link-audit.py` PASS, `python3 quality/gates/structure/orphan-scripts-audit.py --all` PASS.
3. Confirm `.planning/milestones/v0.12.1-phases/{ROADMAP,REQUIREMENTS}.md` exist + freshness verifier still PASS.
4. If anything looks off: tell the orchestrator to fix-and-recommit BEFORE dispatching the verifier (RED verdict means re-doing the close work).
5. If everything looks good: type "dispatch verifier" or equivalent.
  </how-to-verify>
  <resume-signal>Type "dispatch verifier" to proceed, or describe issues to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Dispatch verifier subagent + collect verdict</name>
  <files>quality/reports/verdicts/p63/&lt;RFC3339&gt;.md, quality/reports/verdicts/p63/VERDICT.md</files>
  <action>
1. Construct the verifier-subagent prompt verbatim from quality/PROTOCOL.md § "Verifier subagent prompt template". Substitute `<N>` -> `63`. Add an explicit instruction: "Also grade cross-phase coherence per ROADMAP.md P63 success criterion 10 -- every prior phase verdict at quality/reports/verdicts/p<56..62>/VERDICT.md must show 'Verdict: GREEN'; no orphan catalog rows (every row's verifier.script exists); no expired waivers without `tracked_in` resolving to a v0.12.1 REQ-ID."
2. Dispatch the verifier subagent (use the project's standard mechanism -- e.g., the gsd-code-reviewer-style subagent OR an explicit `Task` tool dispatch with the verbatim prompt + zero context inheritance).
3. Subagent writes its verdict to `quality/reports/verdicts/p63/<RFC3339>.md` with the per-row scored markdown table.
4. After subagent returns: copy the timestamped verdict to `quality/reports/verdicts/p63/VERDICT.md` (canonical pointer).
5. Read the verdict's first line / summary block. If `Verdict: GREEN`: proceed to Task 3. If `Verdict: RED` or any P0+P1 row FAIL: STOP, surface the failing row(s) to the orchestrator, do NOT proceed to Task 3.
6. Banned-words sweep on the verdict file: zero hits.
  </action>
  <verify>
    <automated>test -f quality/reports/verdicts/p63/VERDICT.md && grep -q 'Verdict: GREEN' quality/reports/verdicts/p63/VERDICT.md</automated>
  </verify>
  <done>Verdict file exists; grade is GREEN; cross-phase coherence asserted.</done>
</task>

<task type="auto">
  <name>Task 3: STATE.md + REQUIREMENTS.md + SURPRISES.md + Wave 6 commit</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md, quality/SURPRISES.md</files>
  <action>
1. Update `.planning/STATE.md`:
   - `last_updated` -> RFC3339-UTC of now.
   - `last_activity` -> "2026-04-28 -- P63 SHIPPED. v0.12.0 milestone-close verdict GREEN at quality/reports/verdicts/p63/VERDICT.md. SIMPLIFY-12 audit closed (X DELETE / Y SHIM-WAIVED / Z KEEP-AS-CANONICAL per quality/reports/audits/scripts-retirement-p63.md). MIGRATE-01 retirements landed. POLISH-CODE final: code/cargo-fmt-clean PASS via direct cargo fmt invocation; code/cargo-test-pass stays as ci-status canonical per memory-budget rule (tracked-forward to v0.12.1 MIGRATE-03). MIGRATE-02 cohesion pass: cross-link audit verifier shipped + green; per-dim READMEs normalized. MIGRATE-03 v0.12.1 carry-forward filed at .planning/milestones/v0.12.1-phases/{ROADMAP,REQUIREMENTS}.md (13 REQ placeholders, 5 phase scaffold). CHANGELOG [v0.12.0] finalized. Tag-gate script ready at .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh (>= 6 guards including P63 verdict GREEN). Owner runs the tag. Cursor advances to v0.12.1 planning."
   - If `## Current Position` block exists: update milestone status (e.g., status=`ready-to-tag` or status=`shipped` per project convention).
2. Update `.planning/REQUIREMENTS.md`:
   - Find each of: MIGRATE-01, MIGRATE-02, MIGRATE-03, SIMPLIFY-12, POLISH-CODE.
   - Flip `[ ]` -> `[x]` for each.
   - Append `**SHIPPED P63.**` and the relevant commit SHA to each line, mirroring the P62 POLISH-ORG flip pattern.
   - Sanity check: `grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md` v0.12.0 section -- expect 0 unflipped REQ-IDs in the v0.12.0 section.
3. Update `quality/SURPRISES.md`:
   - Read current line count of the active section (above the rotation banner).
   - Append a P63 block with one-line entries per discovery in Waves 2-5. Examples (use only the entries that actually happened):
     - `2026-04-28 P63 Wave 2: scripts/<X> kept as SHIM-WAIVED despite zero callers because CLAUDE.md "Local dev loop" command names it; OP-5 reversibility wins.`
     - `2026-04-28 P63 Wave 3: cargo-fmt-clean wiring decision -- direct cargo fmt invocation (read-only) honored ONE cargo at a time rule; cargo-test-pass intentionally NOT wired (workspace nextest 6-15 min violates memory budget + pre-pr cap); v0.12.1 MIGRATE-03 to explore per-crate alternatives.`
     - `2026-04-28 P63 Wave 4: cross-link audit found N stale paths in dim READMEs (P56-era moved files); fixed in-line. Verifier mechanism prevents recurrence.`
     - `2026-04-28 P63 Wave 5: v0.12.1 scaffold lands INSIDE .planning/milestones/v0.12.1-phases/ per CLAUDE.md convention -- freshness/no-loose-roadmap-or-requirements verifier stays GREEN; convention is now load-bearing across 13 milestones.`
   - If active section exceeds 200 lines after the appends: rotate per P59 precedent. Move the OLDEST 50-100 lines to `quality/SURPRISES-archive-2026-Q2.md`. Update the active-section banner to reflect the rotation.
4. `git add` only: STATE.md, REQUIREMENTS.md, quality/SURPRISES.md, quality/reports/verdicts/p63/<RFC3339>.md, quality/reports/verdicts/p63/VERDICT.md, plus quality/SURPRISES-archive-*.md if rotated.
5. Banned-words final sweep on staged diff: must print CLEAN.
6. Commit (HEREDOC):
```
feat(p63): phase close + milestone-close verdict GREEN + STATE/REQUIREMENTS/SURPRISES (v0.12.0 ready to tag)

Wave 6 of P63. Closes the v0.12.0 Quality Gates milestone.

Verifier subagent verdict: GREEN (quality/reports/verdicts/p63/VERDICT.md).
Cross-phase coherence: P56-P62 verdicts all GREEN; no orphan catalog rows;
no expired waivers without tracked_in resolving to a v0.12.1 REQ-ID.

REQUIREMENTS.md flips: MIGRATE-01, MIGRATE-02, MIGRATE-03, SIMPLIFY-12,
POLISH-CODE -> SHIPPED P63. All 49 v0.12.0 REQ-IDs are now [x].

STATE.md: cursor advances to v0.12.1 planning. last_activity names every
P63 commit SHA + the milestone-close verdict ref.

SURPRISES.md: P63 entries appended (caller-scan surprises, cargo wiring
rationale, cross-link audit fixes, v0.12.1-scaffold convention). Active
section <X> lines (rotated to quality/SURPRISES-archive-2026-Q2.md if
> 200; banner updated).

Owner next step: run .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh
(6 guards including this verdict GREEN). Orchestrator does NOT push the tag.

Cites: MIGRATE-01, MIGRATE-02, MIGRATE-03, SIMPLIFY-12, POLISH-CODE.
```
7. After commit: `git log --oneline | head -10` -- show the user the full P63 commit chain (Waves 1-6 + any prereq commits like cargo fmt drift fix).
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q 'feat(p63): phase close' && grep -q 'SHIPPED P63' .planning/REQUIREMENTS.md && grep -q 'P63 SHIPPED' .planning/STATE.md && grep -q '2026-04-28 P63' quality/SURPRISES.md && UNFLIPPED=$(awk '/^## v0.12.0/,/^## v0.11/' .planning/REQUIREMENTS.md | grep -c '^- \[ \] \*\*'); [ "$UNFLIPPED" = "0" ] && echo "OK: 0 unflipped v0.12.0 REQ-IDs"</automated>
  </verify>
  <done>Wave 6 commit shipped; STATE/REQUIREMENTS/SURPRISES updated; all 49 v0.12.0 REQ-IDs flipped to SHIPPED; verdict file referenced from tag-script Guard 6.</done>
</task>

</tasks>

<verification>
- `quality/reports/verdicts/p63/VERDICT.md` exists + contains `Verdict: GREEN`.
- `.planning/REQUIREMENTS.md` v0.12.0 section: zero `^- \[ \] \*\*` unflipped REQ-IDs.
- `.planning/STATE.md` last_activity names P63 + milestone close.
- `quality/SURPRISES.md` has P63 entries; active section <= 200 lines (rotation if needed).
- `bash -n .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh` exits 0; Guard 6 reads quality/reports/verdicts/p63/VERDICT.md GREEN line.
- Owner can now run the tag script.
</verification>

<success_criteria>
- v0.12.0 milestone is ready-to-tag: verdict GREEN, REQUIREMENTS fully flipped, tag-script in place with all guards satisfiable, owner has the next-step instruction.
- Cross-phase coherence asserted by the unbiased subagent (zero session context, verbatim PROTOCOL.md prompt).
- The codebase is "pristine" per the owner directive: every dimension's catalog is all-GREEN-or-WAIVED, every WAIVED row has a non-expired `until` + tracked carry-forward in v0.12.1.
- SURPRISES.md is the audit trail of decisions; rotation honored per P59 precedent.
</success_criteria>

<output>
After completion, create `.planning/phases/63-retire-migrated-sources-cohesion-v012-1-carry-forward/63-06-SUMMARY.md` describing: verdict GREEN line + path, REQUIREMENTS flip count (5 P63 + cumulative 49 v0.12.0), STATE cursor advance, SURPRISES rotation outcome, tag-script readiness, owner next-step (run tag-v0.12.0.sh).

Then create `.planning/phases/63-retire-migrated-sources-cohesion-v012-1-carry-forward/63-MILESTONE-SUMMARY.md` with the v0.12.0 milestone-close summary: 8 phases, 49 REQ-IDs, 8 dimensions live, v0.12.1 carry-forward count, links to verdicts + tag script.
</output>
