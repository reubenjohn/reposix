---
phase: 63-retire-migrated-sources-cohesion-v012-1-carry-forward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/catalogs/orphan-scripts.json
  - quality/catalogs/code.json
  - quality/catalogs/perf-targets.json
  - quality/catalogs/security-gates.json
  - quality/catalogs/cross-platform.json
  - quality/gates/code/README.md
  - quality/gates/perf/README.md
autonomous: true
requirements:
  - SIMPLIFY-12
  - MIGRATE-01
  - MIGRATE-03
  - POLISH-CODE
must_haves:
  truths:
    - "P63's FIRST commit writes the catalog rows that define this phase's GREEN contract per quality/PROTOCOL.md Step 3 (catalog-first rule). No retirement, no cohesion edit, no v0.12.1 scaffold lands until Wave 1 ships the contracts."
    - "quality/catalogs/orphan-scripts.json gains a `phase63_audit` block (or top-level field) and rows for every script under scripts/ that survives P63 as a shim. Each row carries: id (orphan-scripts/<basename>), kind=mechanical, sources=[scripts/<file>, canonical at quality/gates/<dim>/<file>], expected.asserts naming the 'thin shim only / canonical at <path>' invariant, verifier.script=quality/gates/structure/orphan-scripts-audit.py (Wave 2 ships it), status=NOT-VERIFIED, blast_radius=P2, waiver=null OR short-lived 2026-07-26 with tracked_in=MIGRATE-03 if appropriate."
    - "Two code-dimension WAIVED rows in quality/catalogs/code.json (`code/cargo-test-pass`, `code/cargo-fmt-clean`) get their `expected.asserts` rewritten to reflect Wave 3's wire-cargo-into-verifier outcome. Specifically: `code/cargo-fmt-clean` flips its verifier.script from quality/gates/code/ci-job-status.sh to quality/gates/code/cargo-fmt-clean.sh (Wave 3 creates), keeps `cargo fmt --all -- --check` exit 0 assert, drops the 'POLISH-CODE P58-stub' second assert, removes the waiver block (set to null), status flips to NOT-VERIFIED pending Wave 3 run; `code/cargo-test-pass` retains its ci.yml-status verifier as the canonical verifier (memory budget rule — workspace nextest 6-15 min violates ONE cargo at a time + pre-pr 10-min cap), drops the POLISH-CODE-stub second assert, retains waiver but replaces reason with the documented memory-budget rationale + tracked_in=MIGRATE-03 v0.12.1 (per-row local cargo enforcement deferred), keeps until=2026-07-26."
    - "Three NEW v0.12.1 carry-forward catalog files ship as stubs with explanatory `comment` fields naming MIGRATE-03 as the home: quality/catalogs/perf-targets.json (3 stub rows: perf/latency-vs-headline-copy, perf/token-economy-bench, perf/golden-path-envelope; all status=NOT-VERIFIED + waiver until=2026-07-26 reason=v0.12.1 carry-forward per MIGRATE-03 + tracked_in=v0.12.1 PERF-* placeholders), quality/catalogs/security-gates.json (2 stub rows: security/allowlist-enforcement, security/audit-immutability; both NOT-VERIFIED + same waiver pattern + tracked_in=v0.12.1 SEC-* placeholders), quality/catalogs/cross-platform.json (2 stub rows: cross-platform/windows-2022-rehearsal, cross-platform/macos-14-rehearsal; both NOT-VERIFIED + same waiver pattern + tracked_in=v0.12.1 CROSS-* placeholders)."
    - "quality/gates/code/README.md gains a 'P63 — POLISH-CODE final wiring (added 2026-04-28)' subsection (≤20 added lines under the dim-README cap) describing: (a) cargo-fmt-clean verifier wires to actual `cargo fmt --all -- --check` (read-only, ~5s); (b) cargo-test-pass keeps the ci-job-status wrapper as canonical with rationale (workspace test suite memory budget) + carry-forward to v0.12.1 for full per-row enforcement."
    - "quality/gates/perf/README.md gains a 'P63 — v0.12.1 carry-forward stubs (added 2026-04-28)' subsection (≤15 added lines) naming the 3 perf-targets rows as stubs awaiting v0.12.1 implementation; cross-references MIGRATE-03 in REQUIREMENTS.md."
    - "Banned word 'replace' MUST NOT appear in any new string. Verified by running `bash scripts/banned-words-lint.sh` (or the canonical `quality/gates/structure/banned-words.sh`) over the changed paths before the commit lands."
    - "ALL Wave 1 changes ship in ONE commit titled `docs(p63): catalog-first commit -- POLISH-CODE wiring contract + v0.12.1 carry-forward stubs + orphan-scripts audit (rows + dim README deltas)`. Commit message cites SIMPLIFY-12, MIGRATE-01, MIGRATE-03, POLISH-CODE."
  artifacts:
    - path: "quality/catalogs/orphan-scripts.json"
      provides: "Wave 1 seeds the audit rows. Existing wrapper preserved (dimension=meta). New rows appended under rows[]. Default expectation: every script in scripts/ that survives P63 as a shim gets a row here. Empty rows array becomes 8-12 rows (one per surviving shim)."
    - path: "quality/catalogs/code.json"
      provides: "Two existing WAIVED rows mutated: code/cargo-fmt-clean flips verifier path to quality/gates/code/cargo-fmt-clean.sh + drops waiver (Wave 3 runs cargo fmt to clear). code/cargo-test-pass retains ci-job-status verifier; waiver reason rewritten to memory-budget rationale + tracked_in=v0.12.1 MIGRATE-03."
    - path: "quality/catalogs/perf-targets.json"
      provides: "NEW file. 3 stub rows for v0.12.1 PERF-* requirements. Schema per quality/catalogs/README.md unified shape."
    - path: "quality/catalogs/security-gates.json"
      provides: "NEW file. 2 stub rows for v0.12.1 SEC-* requirements."
    - path: "quality/catalogs/cross-platform.json"
      provides: "NEW file. 2 stub rows for v0.12.1 CROSS-* requirements."
    - path: "quality/gates/code/README.md"
      max_lines: 200
      provides: "Adds the 20-line P63 POLISH-CODE final wiring subsection."
    - path: "quality/gates/perf/README.md"
      max_lines: 150
      provides: "Adds the 15-line P63 v0.12.1 carry-forward stubs subsection."
  key_links:
    - from: "quality/catalogs/orphan-scripts.json (new rows)"
      to: "quality/gates/structure/orphan-scripts-audit.py (Wave 2 creates)"
      via: "row.verifier.script"
      pattern: "orphan-scripts-audit.py"
    - from: "quality/catalogs/code.json (cargo-fmt-clean row)"
      to: "quality/gates/code/cargo-fmt-clean.sh (Wave 3 creates)"
      via: "row.verifier.script + Wave 3 wires the actual cargo fmt invocation"
      pattern: "cargo-fmt-clean.sh"
    - from: "quality/catalogs/{perf-targets,security-gates,cross-platform}.json (all stub rows)"
      to: "Wave 5 .planning/milestones/v0.12.1-phases/REQUIREMENTS.md placeholders"
      via: "row.waiver.tracked_in field naming PERF-*, SEC-*, CROSS-* placeholder REQ-IDs"
      pattern: "v0.12.1"
    - from: "Wave 6 unbiased verifier subagent"
      to: "all rows changed/added in Wave 1"
      via: "row.expected.asserts is the contract Wave 6 grades against; locked BEFORE Waves 2-5 land code"
      pattern: "expected"
---

<objective>
Wave 1 — the **catalog-first commit** for P63. Per `quality/PROTOCOL.md` § "Per-phase protocol" Step 3 and the recurring success criterion in REQUIREMENTS.md ("catalog rows ship before code"). Wave 1 declares "here is what GREEN means for P63" so the unbiased verifier subagent in Wave 6 reads a contract that existed BEFORE the implementation landed.

Three deliverables ship in this commit:

1. **POLISH-CODE final contract** — mutate the two existing WAIVED rows in `quality/catalogs/code.json` so Wave 3 has clear targets:
   - `code/cargo-fmt-clean` — flip verifier to `quality/gates/code/cargo-fmt-clean.sh` (Wave 3 creates the script that actually runs `cargo fmt --all -- --check`); drop waiver; drop the "P58-stub" assert.
   - `code/cargo-test-pass` — keep `quality/gates/code/ci-job-status.sh` as canonical (running `cargo nextest run --workspace` from a verifier violates the ONE cargo at a time rule + the 10-minute pre-pr budget; CI is the correct enforcement venue); rewrite waiver `reason` to cite the memory-budget rule from CLAUDE.md; rewrite `tracked_in` to point at v0.12.1 MIGRATE-03 carry-forward for per-row local cargo enforcement; keep `until=2026-07-26`.

2. **v0.12.1 carry-forward stubs** — create three NEW catalog files capturing MIGRATE-03 scope:
   - `quality/catalogs/perf-targets.json` — 3 stub rows (latency-vs-headline-copy, token-economy-bench, golden-path-envelope) per `quality/gates/perf/README.md` v0.12.1 stub note.
   - `quality/catalogs/security-gates.json` — 2 stub rows (allowlist-enforcement, audit-immutability) per CLAUDE.md threat model § "Outbound HTTP allowlist" + "Audit log is append-only".
   - `quality/catalogs/cross-platform.json` — 2 stub rows (windows-2022-rehearsal, macos-14-rehearsal) per REQUIREMENTS.md MIGRATE-03 (d).

3. **SIMPLIFY-12 audit rows** — seed `quality/catalogs/orphan-scripts.json` with rows for every shim that survives P63. Wave 2 does the caller-scan + populates the file with the actual surviving shims; Wave 1 anchors the schema (id, kind=mechanical, verifier=quality/gates/structure/orphan-scripts-audit.py, asserts naming the "thin shim only / canonical at <path>" invariant, waiver pattern).

Two dimension READMEs gain ≤20-line P63 subsections: code/README.md (POLISH-CODE final wiring), perf/README.md (v0.12.1 stubs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@quality/PROTOCOL.md
@quality/catalogs/README.md
@quality/catalogs/code.json
@quality/catalogs/orphan-scripts.json
@quality/catalogs/freshness-invariants.json
@quality/gates/code/README.md
@quality/gates/perf/README.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mutate code.json POLISH-CODE rows + create 3 v0.12.1 stub catalog files</name>
  <files>quality/catalogs/code.json, quality/catalogs/perf-targets.json, quality/catalogs/security-gates.json, quality/catalogs/cross-platform.json</files>
  <action>
1. Edit quality/catalogs/code.json:
   - Locate row id=`code/cargo-fmt-clean`. Set `verifier.script` to `quality/gates/code/cargo-fmt-clean.sh`. Set `verifier.args` to `[]`. Set `verifier.timeout_s` to `60`. Drop the second assert string (the one starting "POLISH-CODE P58-stub:"). Set `waiver` to `null`. Set `status` to `NOT-VERIFIED`. Set `last_verified` to `null`. Update `sources` to `["quality/gates/code/cargo-fmt-clean.sh", ".github/workflows/ci.yml:rustfmt", "scripts/hooks/pre-push (cargo fmt step)"]`. Update `owner_hint` to `"if RED: cargo fmt regressed; run cargo fmt --all locally and commit the result. P63 wired the verifier to actually run cargo fmt (read-only, ~5s) so the row enforces locally as well as in CI."`.
   - Locate row id=`code/cargo-test-pass`. Drop the second assert (the "POLISH-CODE P58-stub:" one). Rewrite `waiver.reason` to: `"Per-row local cargo nextest enforcement is deferred to v0.12.1 because cargo nextest run --workspace consumes 6-15 minutes + violates the CLAUDE.md ONE cargo at a time memory-budget rule + exceeds the pre-pr 10-minute cadence cap. ci.yml's test job remains the canonical enforcement venue; the ci-job-status wrapper at quality/gates/code/ci-job-status.sh checks that the most recent ci.yml run for the current branch exited 0. v0.12.1 MIGRATE-03 will explore alternatives (per-crate per-row gates, sccache-warmed verifier in CI only, or accepting CI as the only home and removing the local row)."`. Rewrite `waiver.tracked_in` to `"v0.12.1 MIGRATE-03 -- per-row local cargo enforcement"`. Keep `until=2026-07-26T00:00:00Z`. Keep status WAIVED. Update `owner_hint` to: `"if RED: ci.yml test job is failing for the current branch -- run gh run view <run-id> --log-failed. The verifier intentionally does not run cargo locally (memory-budget rule); CI is the canonical enforcement venue per the P63 wiring decision."`.
2. Create quality/catalogs/perf-targets.json with shape (modeled on code.json):
   ```jsonc
   {
     "$schema": "https://json-schema.org/draft-07/schema#",
     "comment": "Quality Gates catalog -- perf dimension. v0.12.0 ships file-relocate-only stubs (P59 SIMPLIFY-11 moved bench scripts to quality/gates/perf/); v0.12.1 MIGRATE-03 wires the actual cross-check logic (latency vs headline-copy bench output diff; token-economy bench cross-check). All 3 rows ship as NOT-VERIFIED stubs with short-lived waivers tracked_in v0.12.1 PERF-* placeholders. Schema spec: quality/catalogs/README.md.",
     "dimension": "perf",
     "rows": [
       { "id": "perf/latency-vs-headline-copy", "dimension": "perf", "cadence": "weekly", "kind": "mechanical", "sources": ["quality/gates/perf/latency-bench.sh", "docs/benchmarks/latency.md"], "command": "quality/gates/perf/latency-bench.sh && diff vs docs/benchmarks/latency.md headline numbers", "expected": {"asserts": ["v0.12.1 stub -- latency bench output cross-checks against the headline numbers in docs/benchmarks/latency.md within +/-15% per-backend"]}, "verifier": {"script": "quality/gates/perf/latency-vs-headline-copy.sh", "args": [], "timeout_s": 600, "container": null}, "artifact": "quality/reports/verifications/perf/latency-vs-headline-copy.json", "status": "NOT-VERIFIED", "last_verified": null, "freshness_ttl": null, "blast_radius": "P2", "owner_hint": "v0.12.1 PERF-01: this row is a stub; verifier script does not yet exist. v0.12.0 ships only the file-relocate (SIMPLIFY-11). Real cross-check logic ships in v0.12.1 per MIGRATE-03.", "waiver": {"until": "2026-07-26T00:00:00Z", "reason": "v0.12.1 carry-forward per MIGRATE-03; verifier script intentionally does not exist yet -- this row anchors the contract for the v0.12.1 implementer.", "dimension_owner": "v0.12.0 P63 perf dimension owner", "tracked_in": "v0.12.1 PERF-01"} },
       { "id": "perf/token-economy-bench", "dimension": "perf", "cadence": "weekly", "kind": "mechanical", "sources": ["quality/gates/perf/bench_token_economy.py", "benchmarks/fixtures/"], "command": "python3 quality/gates/perf/bench_token_economy.py", "expected": {"asserts": ["v0.12.1 stub -- token-economy bench output cross-checks against headline numbers in user-facing docs (docs/index.md, README.md) within tolerance"]}, "verifier": {"script": "quality/gates/perf/token-economy-bench.sh", "args": [], "timeout_s": 600, "container": null}, "artifact": "quality/reports/verifications/perf/token-economy-bench.json", "status": "NOT-VERIFIED", "last_verified": null, "freshness_ttl": null, "blast_radius": "P2", "owner_hint": "v0.12.1 PERF-02 stub.", "waiver": {"until": "2026-07-26T00:00:00Z", "reason": "v0.12.1 carry-forward per MIGRATE-03.", "dimension_owner": "v0.12.0 P63 perf dimension owner", "tracked_in": "v0.12.1 PERF-02"} },
       { "id": "perf/golden-path-envelope", "dimension": "perf", "cadence": "post-release", "kind": "mechanical", "sources": ["docs/benchmarks/latency.md (golden-path envelope per backend)"], "command": null, "expected": {"asserts": ["v0.12.1 stub -- post-release latency observation against simulator + sanctioned real backends stays inside the documented golden-path envelope"]}, "verifier": {"script": "quality/gates/perf/golden-path-envelope.sh", "args": [], "timeout_s": 900, "container": null}, "artifact": "quality/reports/verifications/perf/golden-path-envelope.json", "status": "NOT-VERIFIED", "last_verified": null, "freshness_ttl": null, "blast_radius": "P2", "owner_hint": "v0.12.1 PERF-03 stub.", "waiver": {"until": "2026-07-26T00:00:00Z", "reason": "v0.12.1 carry-forward per MIGRATE-03; requires sanctioned real-backend creds + cron schedule.", "dimension_owner": "v0.12.0 P63 perf dimension owner", "tracked_in": "v0.12.1 PERF-03"} }
     ]
   }
   ```
3. Create quality/catalogs/security-gates.json with the same wrapper shape; rows:
   - id=`security/allowlist-enforcement`, dimension=security, cadence=pre-pr, kind=mechanical, sources=["crates/reposix-core/src/http.rs", "REPOSIX_ALLOWED_ORIGINS env var"], expected.asserts=["v0.12.1 stub -- a fuzz test attempts outbound HTTP to an origin not in REPOSIX_ALLOWED_ORIGINS and the http::client() factory rejects with the documented error", "the disallowed_methods clippy lint catches direct reqwest::Client::new outside core/http.rs (already covered by code/clippy-lint-loaded; this row is the runtime allowlist test)"], verifier.script=quality/gates/security/allowlist-enforcement.sh, status=NOT-VERIFIED, blast_radius=P0, waiver.until=2026-07-26 reason="v0.12.1 carry-forward per MIGRATE-03; CLAUDE.md threat-model § 'Outbound HTTP allowlist' lift", tracked_in=v0.12.1 SEC-01.
   - id=`security/audit-immutability`, dimension=security, cadence=pre-pr, kind=mechanical, sources=["crates/reposix-cache/src/audit.rs (audit_events_cache)", "crates/reposix-core/src/audit.rs (audit_events)"], expected.asserts=["v0.12.1 stub -- a test attempts UPDATE/DELETE against either audit table and the SQLite layer rejects (append-only by schema)", "WAL mode confirmed", "both tables present in the test fixture"], verifier.script=quality/gates/security/audit-immutability.sh, status=NOT-VERIFIED, blast_radius=P0, waiver as above, tracked_in=v0.12.1 SEC-02.
4. Create quality/catalogs/cross-platform.json with the same wrapper; rows:
   - id=`cross-platform/windows-2022-rehearsal`, dimension=cross-platform, cadence=pre-release, kind=container, sources=[".github/workflows/release.yml (matrix.os)", "examples/*/run.sh"], expected.asserts=["v0.12.1 stub -- the curl-installer + cargo-binstall paths rehearse green on a windows-2022 GH Actions runner"], verifier.script=quality/gates/cross-platform/windows-2022-rehearsal.sh, status=NOT-VERIFIED, blast_radius=P1, waiver.until=2026-07-26 reason="v0.12.1 carry-forward per MIGRATE-03; windows GH runners cost ~$0.08/min and v0.12.0 ships ubuntu-only matrix.", tracked_in=v0.12.1 CROSS-01.
   - id=`cross-platform/macos-14-rehearsal`, same shape, mac-14 runner, tracked_in=v0.12.1 CROSS-02.
5. Validate every JSON file with `python3 -c "import json,sys; [json.load(open(p)) for p in sys.argv[1:]]" quality/catalogs/{code,perf-targets,security-gates,cross-platform,orphan-scripts}.json`.
6. Sanity-grep for the banned word: `grep -rni '\breplace\b\|replaces\|replacing\|replaced\|replacement' quality/catalogs/code.json quality/catalogs/perf-targets.json quality/catalogs/security-gates.json quality/catalogs/cross-platform.json` MUST return zero hits before commit.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; rows=json.load(open('quality/catalogs/code.json'))['rows']; cf=[r for r in rows if r['id']=='code/cargo-fmt-clean'][0]; ct=[r for r in rows if r['id']=='code/cargo-test-pass'][0]; assert cf['verifier']['script']=='quality/gates/code/cargo-fmt-clean.sh', cf; assert cf['waiver'] is None, cf; assert cf['status']=='NOT-VERIFIED', cf; assert ct['waiver']['tracked_in'].startswith('v0.12.1 MIGRATE-03'), ct; assert 'memory-budget' in ct['waiver']['reason'].lower() or 'memory budget' in ct['waiver']['reason'].lower(), ct; perf=json.load(open('quality/catalogs/perf-targets.json')); assert len(perf['rows'])==3 and perf['dimension']=='perf'; sec=json.load(open('quality/catalogs/security-gates.json')); assert len(sec['rows'])==2 and sec['dimension']=='security'; cp=json.load(open('quality/catalogs/cross-platform.json')); assert len(cp['rows'])==2 and cp['dimension']=='cross-platform'; print('OK')"</automated>
  </verify>
  <done>code.json mutated, 3 new stub catalog files created, all valid JSON, zero banned-word hits in changed files.</done>
</task>

<task type="auto">
  <name>Task 2: Seed orphan-scripts.json audit rows + update code/perf dim READMEs</name>
  <files>quality/catalogs/orphan-scripts.json, quality/gates/code/README.md, quality/gates/perf/README.md</files>
  <action>
1. Edit quality/catalogs/orphan-scripts.json. Keep the existing wrapper (`$schema`, `comment`, `dimension`=`meta`). Update `comment` to: `"Quality Gates catalog -- orphan-scripts waivers. SIMPLIFY-12 (P63) audits scripts/ at milestone close. Each row documents a script that survives P63 (after caller-scan in P63 Wave 2) as a thin shim because canonical removal would break documented dev-loop UX or external doc references. Pure deletions land in Wave 2 with no row here. Schema: id=orphan-scripts/<basename>, kind=mechanical, verifier=quality/gates/structure/orphan-scripts-audit.py (Wave 2 ships it). Wave 2 populates rows[] based on the actual caller-scan results."`. Leave `rows: []` empty (Wave 2 will append). This Wave 1 commit only locks the schema + the verifier path contract.
2. Edit quality/gates/code/README.md. Append (under existing content, ≤20 added lines) a new H3 section `### P63 -- POLISH-CODE final wiring (added 2026-04-28)` describing:
   - `cargo-fmt-clean` row flipped from ci-job-status wrapper to direct `cargo fmt --all -- --check` invocation via `quality/gates/code/cargo-fmt-clean.sh` (Wave 3 ships the script). Read-only, ~5s, safe under the ONE cargo at a time rule (no compile).
   - `cargo-test-pass` row keeps the `ci-job-status.sh` wrapper as canonical -- running `cargo nextest run --workspace` from a verifier consumes 6-15 minutes (memory-budget rule violation per CLAUDE.md "Build memory budget") + exceeds the pre-pr 10-min cadence cap. CI is the canonical enforcement venue; v0.12.1 MIGRATE-03 explores per-crate alternatives.
   - One-line cross-reference to `.planning/REQUIREMENTS.md` § POLISH-CODE.
3. Edit quality/gates/perf/README.md. Append (≤15 added lines) a new H3 section `### P63 -- v0.12.1 carry-forward stubs (added 2026-04-28)` naming the 3 perf-targets rows as stubs awaiting v0.12.1 cross-check implementation; one-line cross-reference to MIGRATE-03 in REQUIREMENTS.md.
4. Run `bash scripts/banned-words-lint.sh` (or the canonical equivalent) over the 3 changed files. Zero hits required.
  </action>
  <verify>
    <automated>python3 -c "import json; o=json.load(open('quality/catalogs/orphan-scripts.json')); assert o['dimension']=='meta' and o['rows']==[] and 'SIMPLIFY-12' in o['comment'] and 'orphan-scripts-audit.py' in o['comment'], o; print('OK')" && grep -q "P63 -- POLISH-CODE final wiring" quality/gates/code/README.md && grep -q "P63 -- v0.12.1 carry-forward stubs" quality/gates/perf/README.md</automated>
  </verify>
  <done>orphan-scripts.json comment + verifier-path contract locked (rows still empty for Wave 2 to populate); both dim READMEs gained their P63 H3 subsections.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic Wave 1 commit</name>
  <files>(commit only)</files>
  <action>
1. `git status --short` -- confirm only the expected paths are dirty: quality/catalogs/{code,orphan-scripts,perf-targets,security-gates,cross-platform}.json, quality/gates/code/README.md, quality/gates/perf/README.md.
2. `git diff --stat` -- sanity check (no accidental large blobs, no .gitignored files staged).
3. `git add` only those 7 paths (NEVER `git add -A`).
4. Run a final banned-words sweep across the staged diff: `git diff --cached | grep -ni '\breplace\b\|replaces\|replacing\|replaced\|replacement' || echo CLEAN` -- must print CLEAN.
5. Commit with HEREDOC:
```
docs(p63): catalog-first commit -- POLISH-CODE wiring contract + v0.12.1 carry-forward stubs + orphan-scripts audit (rows + dim README deltas)

Wave 1 of P63 -- the final v0.12.0 milestone phase. Per quality/PROTOCOL.md
catalog-first rule, this commit ships the GREEN contracts BEFORE any code
or retirement lands.

POLISH-CODE final wiring contract (P58-stub -> P63-final):
- code/cargo-fmt-clean: verifier flipped to quality/gates/code/cargo-fmt-clean.sh
  (Wave 3 ships the script that runs cargo fmt --all -- --check directly).
  Waiver dropped; row will flip NOT-VERIFIED -> PASS in Wave 3.
- code/cargo-test-pass: keeps ci-job-status wrapper as canonical. Running
  cargo nextest run --workspace from a verifier violates CLAUDE.md ONE cargo
  at a time memory-budget rule + exceeds pre-pr 10-min cadence. Waiver reason
  rewritten to cite the rationale; tracked_in -> v0.12.1 MIGRATE-03 carry-forward.

v0.12.1 carry-forward stubs (MIGRATE-03):
- quality/catalogs/perf-targets.json (3 stubs: latency-vs-headline-copy,
  token-economy-bench, golden-path-envelope) -> v0.12.1 PERF-01..03.
- quality/catalogs/security-gates.json (2 stubs: allowlist-enforcement,
  audit-immutability) -> v0.12.1 SEC-01..02.
- quality/catalogs/cross-platform.json (2 stubs: windows-2022-rehearsal,
  macos-14-rehearsal) -> v0.12.1 CROSS-01..02.

SIMPLIFY-12 audit contract:
- quality/catalogs/orphan-scripts.json comment + verifier-script path locked.
  Wave 2 populates rows[] after caller-scan + retirement decisions land.

Dim README deltas (per QG-07 incremental update rule):
- quality/gates/code/README.md gains P63 POLISH-CODE final wiring subsection.
- quality/gates/perf/README.md gains P63 v0.12.1 carry-forward stubs subsection.

Banned-words clean. JSON validated. Cites: SIMPLIFY-12, MIGRATE-01, MIGRATE-03,
POLISH-CODE.
```
6. `git log -1 --stat` -- confirm exactly the 7 paths shipped.
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -E '^quality/(catalogs/(code|orphan-scripts|perf-targets|security-gates|cross-platform)\.json|gates/(code|perf)/README\.md)$' | wc -l | grep -q '^7$' && git log -1 --pretty=%s | grep -q 'docs(p63): catalog-first commit'</automated>
  </verify>
  <done>One Wave 1 commit shipped, exactly 7 files, subject line correct, banned-words clean, JSON valid.</done>
</task>

</tasks>

<verification>
- `git log -1 --pretty=%s` starts with `docs(p63): catalog-first commit`.
- `python3 -c "import json; [json.load(open('quality/catalogs/'+f+'.json')) for f in ['code','orphan-scripts','perf-targets','security-gates','cross-platform']]"` exits 0.
- The two POLISH-CODE row mutations are visible in `git show HEAD -- quality/catalogs/code.json`.
- Zero banned-word hits in the commit diff.
</verification>

<success_criteria>
- Wave 1 contract is locked: every Wave 2-5 deliverable has a corresponding catalog row asserting what GREEN means.
- POLISH-CODE final wiring direction is decided IN THE CATALOG (not in code yet): cargo-fmt-clean wires to direct cargo fmt; cargo-test-pass stays as ci-status with documented memory-budget rationale.
- v0.12.1 carry-forward scope is captured in 3 stub catalog files Wave 5 will reference when scaffolding the v0.12.1 milestone.
- SIMPLIFY-12 schema + verifier-path contract is locked; Wave 2 populates rows[] based on caller-scan.
</success_criteria>

<output>
After completion, create `.planning/phases/63-retire-migrated-sources-cohesion-v012-1-carry-forward/63-01-SUMMARY.md` describing: catalog rows shipped, POLISH-CODE wiring decision rationale, v0.12.1 stub-row inventory, banned-words sweep result, links to commit SHA.
</output>
