---
phase: 62-repo-org-gaps-cleanup
plan: 05
type: execute
wave: 5
depends_on: [03, 04]
files_modified:
  - CLAUDE.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/research/v0.11.1-repo-organization-gaps.md
autonomous: true
requirements:
  - ORG-01
  - POLISH-ORG
must_haves:
  truths:
    - "Wave 5 ships the docs trifecta: (a) CLAUDE.md QG-07 mandatory per-phase update with a P62 H3 subsection; (b) STATE.md cursor advance + last_activity update; (c) REQUIREMENTS.md status flips for ORG-01 and POLISH-ORG → shipped."
    - "Plus (d): one-line banner update on `.planning/research/v0.11.1-repo-organization-gaps.md` at the very top: `> **AUDITED + CLOSED under P62 (2026-04-28).** See `quality/reports/audits/repo-org-gaps.md` for per-gap closure. This document is now historical reference; the closure is the catalog rows in `quality/catalogs/freshness-invariants.json` plus their verifier branches in `quality/gates/structure/freshness-invariants.py`.` This satisfies P62 ROADMAP success criterion 3 (the document is no longer a forgotten todo)."
    - "CLAUDE.md QG-07 update is the load-bearing item. Anti-bloat: per P58/P59/P60/P61 precedent, the new H3 subsection is <=80 lines. Plus 1-2 in-place updates if needed (none expected for P62 because the ORG fixes don't introduce a new convention; the catalog rows ARE the convention, and they live in their dimension README)."
    - "CLAUDE.md update format: an H3 subsection 'P62 — Repo-org-gaps cleanup + audit closure' under the existing 'Quality Gates' section, AFTER the P61 H3 subsection. Sections inside: 'What shipped' (1 paragraph naming the audit + 3 catalog rows + verifier branches + relocations + SURPRISES rotation), 'Recovery patterns' (e.g. 'audit RED → re-run `python3 scripts/check_repo_org_gaps.py` to find unclosed dispositions'), 'Cross-references' (audit doc, catalog rows, verifier path)."
    - "Banned-words-lint clean for all 4 modified files. The lint runs over the whole repo by default; just confirm exit 0."
    - "STATE.md update: bump completed_phases 6→7, completed_plans 38→43+ (Wave 1-5 plans + this wave's commits), append a P62 entry to ROADMAP-EVOLUTION section in the same prose pattern as P56-P61 entries (~50-80 lines); update last_updated + last_activity strings."
    - "REQUIREMENTS.md flips: locate ORG-01 + POLISH-ORG entries in `## v0.12.0 Requirements`; change `[ ]` → `[x]`; update the traceability table (`| ORG-01 | P62 | shipped (P62) |` and same for POLISH-ORG)."
    - "Wave 5 is the LAST commit before Wave 6 dispatches the verifier subagent. After Wave 5, the catalog should be in its final shippable state."
    - "Single combined commit OR split into 2 commits (CLAUDE.md+banner = one commit; STATE.md+REQUIREMENTS.md = the other) — split is preferred for diff review tractability per P58/P60 precedent. Either is fine if banned-words clean and pre-push GREEN."
  artifacts:
    - path: "CLAUDE.md"
      provides: "P62 H3 subsection (~50-80 added lines) under the Quality Gates section. No section rewrites; append-only per anti-bloat rules."
      max_lines_added: 80
    - path: ".planning/STATE.md"
      provides: "Cursor advanced; last_activity narrates P62 closure; ROADMAP-EVOLUTION P62 entry appended."
    - path: ".planning/REQUIREMENTS.md"
      provides: "ORG-01 + POLISH-ORG flipped to [x]; traceability table updated."
    - path: ".planning/research/v0.11.1-repo-organization-gaps.md"
      provides: "Top banner added — points at quality/reports/audits/repo-org-gaps.md as the closure record."
  key_links:
    - from: "CLAUDE.md (P62 H3 subsection)"
      to: "quality/reports/audits/repo-org-gaps.md + quality/catalogs/freshness-invariants.json + quality/gates/structure/README.md"
      via: "Cross-references section in the H3 subsection"
      pattern: "Cross-references"
    - from: ".planning/research/v0.11.1-repo-organization-gaps.md (banner)"
      to: "quality/reports/audits/repo-org-gaps.md"
      via: "explicit pointer"
      pattern: "AUDITED + CLOSED"
---

<objective>
Wave 5 — phase-close docs trifecta (+ banner update). The fix work is done in Waves 1-4; Wave 5 makes the work discoverable to future agents per CLAUDE.md QG-07 (mandatory per-phase update).

Four deliverables (1-2 commits):

**(1) CLAUDE.md update** — append a new H3 subsection `### P62 — Repo-org-gaps cleanup + audit closure` under the existing Quality Gates section, AFTER the P61 H3 subsection. Anti-bloat: <=80 added lines. Format mirrors P58/P59/P60/P61:

- "What shipped" — 1 paragraph: P62 closes ORG-01 + POLISH-ORG. Audit at `quality/reports/audits/repo-org-gaps.md` covers all items in `.planning/research/v0.11.1-repo-organization-gaps.md`. 3 new structure-dimension rows in `quality/catalogs/freshness-invariants.json` (`structure/no-loose-top-level-planning-audits`, `structure/no-pre-pivot-doc-stubs`, `structure/repo-org-audit-artifact-present`) prevent recurrence. Verifier branches at `quality/gates/structure/freshness-invariants.py`. Relocations: top-level milestone audits → `.planning/milestones/audits/`; SESSION-END-STATE artifacts → `.planning/archive/sessions/`. `scripts/__pycache__/` purged. SURPRISES.md rotated (302 → ≤200; P57+P58 to archive).
- "Recovery patterns" — 1-2 examples: "audit doc has unclosed disposition → run `python3 scripts/check_repo_org_gaps.py` for the diagnostic; fix or escalate"; "structure/no-loose-top-level-planning-audits RED → relocate the new top-level audit doc under `.planning/milestones/audits/`."
- "Cross-references" (do NOT duplicate runtime detail per CLAUDE.md anti-bloat rules):
  - `quality/reports/audits/repo-org-gaps.md` — the closure audit.
  - `quality/catalogs/freshness-invariants.json` — 3 P62 rows.
  - `quality/gates/structure/freshness-invariants.py` — verifier branches.
  - `quality/gates/structure/README.md` — dimension home with P62 row table.
  - `scripts/check_repo_org_gaps.py` — the audit-completeness verifier.
  - `quality/SURPRISES-archive-2026-Q2.md` — Q2 archive (P57+P58 entries).

**(2) `.planning/research/v0.11.1-repo-organization-gaps.md` banner** — add a single blockquote line at the very top, ABOVE the existing `# Repo structure + organization audit (post-v0.11.0)` heading:

```
> **AUDITED + CLOSED under P62 (2026-04-28).** See `quality/reports/audits/repo-org-gaps.md`
> for per-gap closure paths. The recurrence-guards live as catalog rows in
> `quality/catalogs/freshness-invariants.json` (P62 Wave 1) with verifier branches
> in `quality/gates/structure/freshness-invariants.py` (P62 Wave 3). This document
> is now historical reference.
```

**(3) STATE.md update** — bump `completed_phases` 6 → 7, `completed_plans` 38 → 43+ (count actual Wave 1-5 plans). Update `last_updated` to current RFC3339 UTC, `last_activity` to a 1-paragraph P62 SHIPPED narrative in the prose style of the existing P56-P61 entries (~50-80 lines). Append a `## Accumulated Context` ROADMAP-EVOLUTION section entry naming the P62 deliverables verbatim.

**(4) REQUIREMENTS.md flip** — find ORG-01 + POLISH-ORG entries in `## v0.12.0 Requirements`. Change `- [ ] **ORG-01**:` to `- [x] **ORG-01**:` (and same for POLISH-ORG). In the traceability table at the bottom of the milestone section, update the rows: `| ORG-01 | P62 | shipped (P62) |` and `| POLISH-ORG | P62 | shipped (P62) |`.

**Commit splitting (recommended):**
- Commit 1 — `docs(p62): CLAUDE.md QG-07 P62 subsection + audit-doc closure banner`
- Commit 2 — `chore(p62): STATE.md cursor advance + REQUIREMENTS.md ORG-01 + POLISH-ORG → shipped`

Two commits keeps the docs-trifecta diff legible. Single commit is acceptable if the executor judges the diffs small enough; either way, banned-words clean and pre-push GREEN.

**Why no SURPRISES update in this wave?** Wave 4 already captured the P62 surprises. If Wave 5 hits a new pivot, append a single-line entry to active SURPRISES.md as part of Wave 5's commits.

**Why update STATE.md before Wave 6?** Because the verifier subagent in Wave 6 reads STATE.md as part of its grading evidence (per `quality/PROTOCOL.md` § "Reading order for an agent picking up a phase"). The cursor must show "P62 SHIPPED" in last_activity for the verifier to grade GREEN against the post-fix state.

**Caveat:** Wave 6's verifier dispatch may notice that STATE/REQUIREMENTS naming a phase as "shipped" is a self-claim by the executor. The verifier's contract is to grade catalog rows + audit artifact, not the STATE narrative. STATE/REQUIREMENTS reflect the executor's claim; the catalog rows reflect the truth. If the verifier finds a RED row, STATE/REQUIREMENTS need rolling back in a follow-up commit before phase-close. This is the standard P57+ pattern — STATE is hopeful; the verifier is the verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.11.1-repo-organization-gaps.md
@quality/reports/audits/repo-org-gaps.md
@quality/PROTOCOL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md QG-07 P62 subsection</name>
  <files>CLAUDE.md</files>
  <action>
Read `CLAUDE.md`. Locate the P61 H3 subsection (search for `### P61 — Subjective gates skill` or similar). After it, append a new H3 subsection `### P62 — Repo-org-gaps cleanup + audit closure (added 2026-04-28)`.

Format mirrors P58/P59/P60/P61. Aim for 50-80 added lines. Sections:
- One paragraph "What shipped" (per the objective).
- A small markdown table or 2-3 bullet "Recovery patterns".
- "Cross-references" bulleted list (per the objective).

Banned-words-lint check: `bash scripts/banned-words-lint.sh`. Confirm exit 0.

Final file size: prior CLAUDE.md was ~600 lines (per `wc -l`); after this addition it's ~650-680 lines. Within bloat tolerance per the file's existing pattern.
  </action>
  <verify>
    <automated>grep -q "P62 — Repo-org-gaps cleanup" CLAUDE.md && bash scripts/banned-words-lint.sh && echo OK</automated>
  </verify>
  <done>CLAUDE.md has P62 H3 subsection 50-80 lines. Banned-words clean.</done>
</task>

<task type="auto">
  <name>Task 2: Audit doc banner update</name>
  <files>.planning/research/v0.11.1-repo-organization-gaps.md</files>
  <action>
Read the file. Add the blockquote banner from the objective at the very top, BEFORE the existing `# Repo structure + organization audit (post-v0.11.0)` heading. Single-edit. <=8 added lines.

Banned-words-lint clean.
  </action>
  <verify>
    <automated>head -10 .planning/research/v0.11.1-repo-organization-gaps.md | grep -q "AUDITED + CLOSED under P62"</automated>
  </verify>
  <done>Banner present at top of file.</done>
</task>

<task type="auto">
  <name>Task 3: Commit 1 — CLAUDE.md + banner</name>
  <files></files>
  <action>
Stage `CLAUDE.md` and `.planning/research/v0.11.1-repo-organization-gaps.md`. Commit:

```
docs(p62): CLAUDE.md QG-07 P62 subsection + audit-doc closure banner

- CLAUDE.md: ~50-80 line H3 subsection 'P62 — Repo-org-gaps cleanup
  + audit closure (added 2026-04-28)' under Quality Gates section.
  Anti-bloat: append-only; no rewrites of existing sections.
- .planning/research/v0.11.1-repo-organization-gaps.md: top blockquote
  banner naming P62 closure + audit pointer.

Refs ORG-01, POLISH-ORG. Mandatory per QG-07.
```

Push.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "p62.*CLAUDE.md\|p62.*QG-07"</automated>
  </verify>
  <done>Commit landed; pushed; pre-push GREEN.</done>
</task>

<task type="auto">
  <name>Task 4: STATE.md + REQUIREMENTS.md updates</name>
  <files>.planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>
**STATE.md:**
- Bump frontmatter `completed_phases: 6` → `7`.
- Bump `completed_plans` to reflect P62 plan count (38 + the actual P62 plans landed; for now use 43 as estimate; final count may be 6 if Wave 5 lands as 1 plan or 8 if more granular).
- Update `last_updated` to current RFC3339 UTC.
- Update `last_activity` to a 1-paragraph P62 SHIPPED narrative in the prose style of the P56-P61 entries (50-80 lines). Cover: 3 catalog rows shipped; verifier branches; audit at `quality/reports/audits/repo-org-gaps.md`; relocations (audits → milestones/audits/, SESSION-END-STATE → archive/sessions/, __pycache__ purged); SURPRISES rotation (active 302 → ≤200 lines); CLAUDE.md QG-07 update; banner on the source doc.
- In `## Accumulated Context` → `### Roadmap Evolution`, append a P62 entry following the same format as the P61 entry above (likewise dense; one bullet point bottom-aligned with the heading).

**REQUIREMENTS.md:**
- Locate `- [ ] **ORG-01**:` line — change `- [ ]` → `- [x]`.
- Locate `- [ ] **POLISH-ORG**` line — change `- [ ]` → `- [x]`.
- In the traceability table, update the ORG-01 row: `| ORG-01 | P62 | shipped (P62) |` (was `planning`).
- Update the POLISH-ORG row: `| POLISH-ORG | P62 | shipped (P62) |`.

Banned-words-lint clean.

Stage and commit:

```
chore(p62): STATE.md cursor advance + REQUIREMENTS.md flips ORG-01 + POLISH-ORG

- STATE.md: completed_phases 6→7; last_activity narrates P62 closure;
  ROADMAP-EVOLUTION entry for P62 added in style of P56-P61 entries.
- REQUIREMENTS.md: ORG-01 + POLISH-ORG flipped to [x] + traceability
  table updated to shipped (P62).

Refs ORG-01, POLISH-ORG.
```

Push.
  </action>
  <verify>
    <automated>grep -q "completed_phases: 7" .planning/STATE.md && grep -q "\\[x\\] \\*\\*ORG-01\\*\\*" .planning/REQUIREMENTS.md && grep -q "\\[x\\] \\*\\*POLISH-ORG\\*\\*" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>STATE/REQUIREMENTS updated. Pre-push GREEN. Pushed.</done>
</task>

</tasks>

<verification>
- CLAUDE.md has P62 H3 subsection (`grep "P62 — Repo-org-gaps"`).
- `.planning/research/v0.11.1-repo-organization-gaps.md` has top banner.
- STATE.md shows `completed_phases: 7` and a P62 last_activity narrative.
- REQUIREMENTS.md shows `[x]` for ORG-01 + POLISH-ORG.
- All commits pushed; pre-push GREEN.
- Banned-words-lint clean.
</verification>

<success_criteria>
- 4 files updated (CLAUDE.md, banner, STATE, REQUIREMENTS).
- 2 commits (split for diff review).
- Pre-push GREEN both times.
- Phase ready for Wave 6 verifier dispatch.
</success_criteria>

<output>
After completion, create `.planning/phases/62-repo-org-gaps-cleanup/62-05-SUMMARY.md`
</output>
