---
phase: 62-repo-org-gaps-cleanup
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - quality/SURPRISES.md
  - quality/SURPRISES-archive-2026-Q2.md
autonomous: true
requirements:
  - POLISH-ORG
must_haves:
  truths:
    - "quality/SURPRISES.md is at 302 lines (over the 240-line trigger documented in CLAUDE.md / quality/PROTOCOL.md). P59 established the rotation precedent (5 P56 entries archived to SURPRISES-archive-2026-Q2.md). P62 continues the precedent."
    - "Rotation rule (P59 precedent): keep the 'most recent' entries in active SURPRISES.md targeted at <=200 lines; archive older entries by appending them to `quality/SURPRISES-archive-2026-Q2.md` (file already exists with 66 lines from P59's rotation)."
    - "What's 'older' for P62? The P57 + P58 entries (16+ lines combined per STATE: 3 P57 + 7 P58 = 10 entries) are good archival candidates — they're ~6-7 phases back. Keep P59/P60/P61 in active because they're recent + still likely-cited by the next phase agent (P63). If even more space is needed, P59 entries can join the archive too; P60+P61 stay active (most recent)."
    - "The active SURPRISES.md after rotation is <=200 lines (target — soft cap; 220 acceptable if a single entry is bulky). The archive file is append-only (older entries first per P59 convention; preserve chronological order)."
    - "The archive entries are migrated VERBATIM — no rewriting, no reformatting, no condensation. The header at the top of `SURPRISES-archive-2026-Q2.md` says 'append-only quarterly archive; ordered oldest-first'. P62 appends P57 + P58 entries (and maybe P59) at the END of the archive file (newest-archived go to the bottom)."
    - "After the active file shrinks, the active file's top banner is updated: 'rotated YYYY-MM-DD; archived P57+P58 (and P59 if applicable) to SURPRISES-archive-2026-Q2.md'."
    - "P62's own SURPRISES entries (anything pivoted in P62) are added to the active file BEFORE rotation in this wave, so the active file has fresh P62 entries + recent P60/P61 (and P59 if not archived). P62 entries are appended in the standard format: `YYYY-MM-DD P62: <surprise>` (one per encountered pivot)."
    - "Single commit for the rotation. No code change. Banned-words clean."
  artifacts:
    - path: "quality/SURPRISES.md"
      provides: "Active journal trimmed to <=200 lines (target). Top banner updated to name the rotation date + archive target. P62 entries appended (if any pivots happened in Waves 1-3)."
      max_lines: 220
    - path: "quality/SURPRISES-archive-2026-Q2.md"
      provides: "Archive file extended with P57+P58 (and P59 if needed) entries appended at end. Verbatim copy from active. Header at top of file remains stable."
  key_links:
    - from: "quality/SURPRISES.md (active)"
      to: "quality/SURPRISES-archive-2026-Q2.md (archive)"
      via: "P57+P58 entries moved verbatim"
      pattern: "SURPRISES-archive-2026-Q2"
    - from: "Active file top banner"
      to: "rotation date + archive pointer"
      via: "the banner is the supersession trail for the next phase agent"
      pattern: "rotated"
---

<objective>
Wave 4 — SURPRISES.md rotation. Active file is at 302 lines (over the 240 trigger). Apply the P59 precedent: archive older entries to `quality/SURPRISES-archive-2026-Q2.md`; trim the active file to ≤200 lines (target).

Three steps:

1. **Append P62-discovered surprises to active file FIRST.** Before any rotation, capture this phase's pivots. Each surprise is one line in standard format `YYYY-MM-DD P62: <discovered surprise>`. Examples (judgement-call as appropriate to actual pivots from Waves 1-3):
   - "P62: pre-check before drafting Wave 2 audit revealed N% of v0.11.1-repo-organization-gaps.md recommendations were already CLOSED by SIMPLIFY-* / P56-P61 sweeps; Wave 2 became 'audit and verify closures' rather than 'plan new fixes'. The audit document was a snapshot from 2026-04-26; the QG framework does most of its job."
   - "P62: catalog-first commit pattern continues to dominate planning — Wave 1 contract for ORG-01 + POLISH-ORG locks 3 structure rows BEFORE Wave 2 renders the audit. Without the contract-first commit, the audit doc could quietly omit items."
   - (Add any actual pivot encountered during execution.)

2. **Identify entries to archive.** Read the active file. Per CLAUDE.md / quality/PROTOCOL.md and P59 precedent, archive entries are typically the oldest. Cut points:
   - Active file currently has: P57 (3 entries) + P58 (7 entries) + P59 (6 entries) + P60 (4 entries) + P61 (some entries) = ~302 lines combined with section headers.
   - Archive: P57 + P58 (10 entries) → likely ~80-120 lines moved. Active file would drop to ~180-200 lines.
   - If still >220 after that, also archive P59 entries.
   - Keep P60 + P61 + P62 in active file (most recent + most relevant to P63 / v0.12.1).

3. **Move + commit.** Use `sed`/`Python` or a manual cut-paste — the operation is mechanical. Append the moved block to `SURPRISES-archive-2026-Q2.md` PRESERVING the section headers. Update the active file's top banner. Run a banned-words sweep on both files.

Single commit:

```
docs(p62): rotate SURPRISES.md (active 302→<=200; archive P57+P58 entries)

quality/SURPRISES.md is over the 240-line rotation trigger. Apply
the P59 precedent: archive P57+P58 entries (and P59 if needed)
to quality/SURPRISES-archive-2026-Q2.md verbatim; update active
top banner with rotation date.

P62 discovery entries appended to active before rotation:
- pre-check revealed N% of v0.11.1 recs were already closed by
  prior sweeps; Wave 2 became 'verify closures' not 'plan fixes'.
- catalog-first commit dominates: Wave 1 locked the contract
  before Wave 2 rendered the audit table.
- [any other actual pivot]

Refs POLISH-ORG (housekeeping per CLAUDE.md SURPRISES rotation rule).
```

Push.

**Why a separate wave?** Two reasons. First, the rotation depends on Wave 3's lessons being captured (so Wave 4 is downstream of Wave 3). Second, the rotation is a mechanical bulk-text move, separate from content fixes. Bundling it with Wave 3 would muddy the diff.

**Why NOT split P59 into its own archive?** The archive header reads "2026-Q2" which covers April-June; both P57 and P58 are within Q2. P59 also Q2. Keep one archive file per quarter. New archive (`SURPRISES-archive-2026-Q3.md`) starts in July if rotation happens then.

**Anti-bloat:** the archive file grows; that's the point. Active file shrinks; that's the point. The rotation is the documented escape hatch from journal bloat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@quality/SURPRISES.md
@quality/SURPRISES-archive-2026-Q2.md
@quality/PROTOCOL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append P62 entries to active SURPRISES.md</name>
  <files>quality/SURPRISES.md</files>
  <action>
Read `quality/SURPRISES.md`. Identify the section for P62 (likely create one if not present). Append entries in the format used elsewhere in the file (header `## P62 — Repo-org-gaps cleanup` if section-style, or just `2026-04-28 P62: <surprise>` line if line-style — match the file's existing pattern).

Capture actual pivots from Waves 1-3:
- Wave 2 pre-check revealed how much was already closed by prior sweeps.
- Catalog-first locked the contract before audit rendering.
- Any specific gotcha (e.g. SESSION-END-STATE files turned out to require an archive README; or `git mv` on a file already-tracked needed `git add` after; or the freshness-invariants.py extension required helper-module extraction).

Aim for 3-5 P62 entries. Each is 1-3 lines.

Banned-words clean.
  </action>
  <verify>
    <automated>grep -c "P62" quality/SURPRISES.md | awk '$1 >= 3 {print "OK: " $1 " P62 mentions"}'</automated>
  </verify>
  <done>P62 entries appended to active file. Banned-words clean.</done>
</task>

<task type="auto">
  <name>Task 2: Cut P57+P58 (and P59 if needed) entries; append to archive</name>
  <files>quality/SURPRISES.md, quality/SURPRISES-archive-2026-Q2.md</files>
  <action>
Read both files.

In active `SURPRISES.md`: identify the P57 and P58 sections. Cut them (preserve headers + entries verbatim). Cut P59 too if active file is still >220 lines after P57+P58 removal.

Append the cut content to the END of `quality/SURPRISES-archive-2026-Q2.md`:

```
## P57 — Quality Gates skeleton + structure dimension (archived 2026-04-28 by P62 Wave 4)

<P57 entries verbatim>

## P58 — Release dimension gates + code-dim absorption (archived 2026-04-28 by P62 Wave 4)

<P58 entries verbatim>

(P59 section if cut)
```

Preserve chronological order (oldest first; P57 before P58 before P59). The archive file convention is oldest-first per P59's seed.

In active `SURPRISES.md`: update the top banner to add a line:

```
**Rotated 2026-04-28 by P62 Wave 4.** Archived P57 + P58 (+ P59 if applicable)
to `quality/SURPRISES-archive-2026-Q2.md`. Active journal retains P60 onward.
```

Verify active file <=220 lines (target <=200; soft cap 220):

```bash
wc -l quality/SURPRISES.md
```

Banned-words clean.
  </action>
  <verify>
    <automated>test $(wc -l < quality/SURPRISES.md) -le 220 && grep -q "P57" quality/SURPRISES-archive-2026-Q2.md && grep -q "P58" quality/SURPRISES-archive-2026-Q2.md && grep -q "Rotated 2026-04-28" quality/SURPRISES.md && bash scripts/banned-words-lint.sh && echo OK</automated>
  </verify>
  <done>Active file ≤220 lines. Archive file extended verbatim. Active top banner updated. Banned-words clean.</done>
</task>

<task type="auto">
  <name>Task 3: Single commit for rotation</name>
  <files></files>
  <action>
Stage both files. Commit with the message in the objective. Push (pre-push GREEN — these are docs-only changes, no catalog/runner touch).
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "rotate SURPRISES" && git status --porcelain | wc -l | awk '$1 == 0 {print "OK"}'</automated>
  </verify>
  <done>Single commit landed. Working tree clean. Pushed.</done>
</task>

</tasks>

<verification>
- `wc -l quality/SURPRISES.md` shows ≤220 (target ≤200).
- `grep "P57\|P58" quality/SURPRISES-archive-2026-Q2.md` finds the moved sections.
- Active file's top banner names the rotation date.
- Working tree clean after commit.
</verification>

<success_criteria>
- Active SURPRISES.md ≤220 lines.
- Archive extended with verbatim P57+P58 (+P59) sections.
- 3-5 P62 entries captured.
- Single commit; banned-words clean; pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/62-repo-org-gaps-cleanup/62-04-SUMMARY.md`
</output>
