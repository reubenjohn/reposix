---
phase: 62-repo-org-gaps-cleanup
plan: 06
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - quality/reports/verdicts/p62/VERDICT.md
autonomous: false
requirements:
  - ORG-01
  - POLISH-ORG
must_haves:
  truths:
    - "Wave 6 dispatches the unbiased verifier subagent per QG-06 (`quality/PROTOCOL.md` § 'Step 7 — Dispatch unbiased verifier subagent'). The executing agent does NOT grade itself; an unbiased subagent reads the catalog rows + artifacts and writes a verdict to `quality/reports/verdicts/p62/VERDICT.md`."
    - "Path B precedent (P56/P57/P58/P59/P60/P61): when the Task subagent tool is unavailable in the executor, the verdict is authored in-session by the Wave 6 executor with explicit disclosure of the four constraints (evidence-only, catalog-rows-as-contract, refuse-GREEN-on-RED, out-of-session-regrade-should-match). The executor signs the verdict with the Path B disclosure block at the top."
    - "Verdict input artifacts: (a) `quality/catalogs/freshness-invariants.json` 3 P62 rows; (b) `quality/reports/verifications/structure/{no-loose-top-level-planning-audits,no-pre-pivot-doc-stubs,repo-org-audit-artifact-present}.json`; (c) `quality/reports/audits/repo-org-gaps.md`; (d) `python3 scripts/check_repo_org_gaps.py --json` output; (e) `python3 quality/runners/run.py --cadence pre-push` last sweep result; (f) CLAUDE.md P62 H3 subsection presence; (g) STATE.md cursor showing P62 SHIPPED."
    - "Grading rules (refuse-GREEN-on-RED): if any P0+P1 P62 row has status outside {PASS, WAIVED-with-active-waiver}, verdict is RED. If the audit doc has any unclosed disposition, verdict is RED. If the verifier script `check_repo_org_gaps.py` exits non-zero, verdict is RED. If CLAUDE.md is missing the P62 H3 subsection, verdict is RED. If STATE.md still shows `completed_phases: 6`, verdict is RED (Wave 5 didn't land cleanly)."
    - "On RED: Wave 6 does NOT close the phase. The verdict file documents the RED rows with citations + remediation pointer; STATE.md gets a follow-up commit reverting last_activity to 'P62 IN-PROGRESS — RED on <row id>'; phase reopens for fix-up before re-dispatch."
    - "On GREEN: verdict file declares `**Verdict: GREEN**` + recommends 'P63 may begin'. Phase closes. P63 entry condition is met."
    - "Anti-bloat: verdict file targets <=120 lines per P56-P61 precedent. Sections: Disclosure block (Path B disclosure if applicable); Top-line summary table (counts per status); Per-row grading (3 P62 rows); Audit grading (audit-doc completeness from check_repo_org_gaps.py); STATE/CLAUDE.md/REQUIREMENTS.md grading; Verdict declaration; Recommendations."
    - "If verifier subagent is dispatched via Task tool (Path A): the prompt template in `quality/PROTOCOL.md` § 'Verifier subagent prompt template' is copied verbatim with placeholder substitution (PHASE=62; CATALOG_PATHS=... ; ARTIFACT_PATHS=...; AUDIT_DOC=quality/reports/audits/repo-org-gaps.md). The subagent's response is written to the verdict file."
    - "Verdict file contains a single commit AFTER GREEN. Title: `chore(p62): verifier subagent verdict GREEN -- P62 closes (ORG-01 + POLISH-ORG shipped)`. Push."
  artifacts:
    - path: "quality/reports/verdicts/p62/VERDICT.md"
      provides: "The phase-close verdict. Anti-bloat <=120 lines. Sections per the P56-P61 precedent (disclosure + top-line table + per-row grading + verdict declaration)."
      max_lines: 120
  key_links:
    - from: "quality/reports/verdicts/p62/VERDICT.md (GREEN declaration)"
      to: "P63 entry condition (per quality/PROTOCOL.md gate-state precondition rule)"
      via: "next phase agent reads quality/reports/verdicts/p62/VERDICT.md as Step 6 of phase reading order"
      pattern: "P63 may begin"
    - from: "Verifier subagent input"
      to: "Wave 1 catalog contract (the GREEN definition)"
      via: "subagent reads catalog rows + artifacts; grades against expected.asserts that pre-existed implementation"
      pattern: "expected"
---

<objective>
Wave 6 — phase close via QG-06 verifier subagent dispatch. The phase does NOT close on the executing agent's word; an unbiased subagent grades the catalog rows + artifacts.

Two paths:

**Path A — Task tool dispatch (preferred when available).** Use the Agent / Task subagent tool to dispatch a `general-purpose` subagent with the verbatim prompt from `quality/PROTOCOL.md` § "Verifier subagent prompt template", substituting:
- `PHASE` = 62
- `CATALOG_PATHS` = `quality/catalogs/freshness-invariants.json` (3 P62 rows)
- `ARTIFACT_PATHS` = `quality/reports/verifications/structure/no-loose-top-level-planning-audits.json`, `quality/reports/verifications/structure/no-pre-pivot-doc-stubs.json`, `quality/reports/verifications/structure/repo-org-audit-artifact-present.json`
- `AUDIT_DOC` = `quality/reports/audits/repo-org-gaps.md`
- `VERDICT_PATH` = `quality/reports/verdicts/p62/VERDICT.md`

The subagent runs the verifier (`python3 quality/runners/run.py --cadence pre-push` for the 3 rows + `python3 scripts/check_repo_org_gaps.py --json`), reads the artifacts, applies the 4 grading rules from PROTOCOL.md (evidence-only, catalog-rows-as-contract, refuse-GREEN-on-RED, out-of-session-regrade-should-match), and writes the verdict file.

**Path B — In-session disclosure (P56-P61 precedent fallback).** If Task tool dispatch is unavailable in the executor's environment (the documented v0.12.0 limitation per P56-P61 verdicts), the executing agent authors the verdict in-session with an explicit disclosure block at the top:

```markdown
# P62 Verdict — GREEN

**Verdict: GREEN** (or RED)
**Phase:** v0.12.0 P62 — Repo-org-gaps cleanup + audit closure
**Graded:** 2026-04-28
**Path:** B (in-session disclosure per P56/P57/P58/P59/P60/P61 precedent — Task tool unavailable)
**Recommendation:** P63 may begin. (or: P62 RED on <row>; fix and re-dispatch.)

## Disclosure block (Path B)

This verdict is authored in-session by the Wave 6 executor. The four rules below are honored verbatim:

1. **Evidence-only.** Every PASS / WAIVED claim cites a file+line OR a runner output line below. No narrative-only grading.
2. **Catalog-rows-as-contract.** Each row's grade is computed from row.expected.asserts ↔ artifact asserts_passed/failed match. Not from narrative.
3. **Refuse-GREEN-on-RED.** If any P0+P1 row had status outside {PASS, WAIVED-with-documented-carry-forward} after the final runner sweep, this file would be RED.
4. **Out-of-session re-grade should match.** A future Path A subagent reading the catalog files + artifacts + CLAUDE.md diff at this commit SHA should produce the same verdict. The catalog state at this verdict's authorship is the regrade input.
```

Either path produces the same artifact: `quality/reports/verdicts/p62/VERDICT.md`.

**Verdict file contents (Path A or B):**

1. Top: verdict declaration + path B disclosure (if applicable).
2. Top-line summary table: rows graded count; PASS / FAIL / WAIVED breakdown; runner cadence outputs (pre-push exit code).
3. Per-row grading section (3 rows): for each, cite the artifact file + the asserts_passed/failed numbers + the status. Refuse-GREEN-on-RED check.
4. Audit grading: `check_repo_org_gaps.py --json` output — total items audited, count per disposition, number of unclosed dispositions (must be 0 for GREEN).
5. STATE/CLAUDE.md/REQUIREMENTS.md grading: confirm CLAUDE.md has P62 H3 subsection (file+line cite); STATE.md shows `completed_phases: 7`; REQUIREMENTS.md shows `[x]` for ORG-01 + POLISH-ORG.
6. Verdict declaration + recommendation.

**Anti-bloat:** target <=120 lines (P56-P61 precedent). Tables are dense; per-row sections are one block each.

**On RED:** the verdict file documents which row(s) failed + the recommended fix. The phase does NOT close. STATE.md is reverted to "IN-PROGRESS" via a follow-up commit. The executor (or a follow-up phase) fixes the RED row + re-dispatches the verifier.

**On GREEN:** the verdict closes the phase. Wave 6 commits the verdict file (and any STATE.md fix-ups required if the verifier flagged something Wave 5 missed). Phase 63 entry condition is met.

**This wave is NOT autonomous.** The verifier dispatch (Path A) routes through a subagent; Path B requires explicit in-session disclosure. Neither is a fully-autonomous "agent grades itself" path; both apply the QG-06 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@quality/PROTOCOL.md
@quality/catalogs/freshness-invariants.json
@quality/reports/audits/repo-org-gaps.md
@quality/reports/verdicts/p61/VERDICT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Wave 1-5 of P62 are landed:
- Wave 1: 3 catalog rows + dim README delta committed.
- Wave 2: audit at `quality/reports/audits/repo-org-gaps.md` + `scripts/check_repo_org_gaps.py` committed.
- Wave 3: relocations + verifier extension + audit-table flip committed (2 commits).
- Wave 4: SURPRISES rotation committed.
- Wave 5: CLAUDE.md QG-07 + banner + STATE/REQUIREMENTS flips committed (1-2 commits).

The catalog state is the GREEN contract Wave 1 locked.
  </what-built>
  <how-to-verify>
1. Confirm Wave 1-5 commits all landed:
   ```bash
   git log --oneline | head -10 | grep -c p62
   ```
   Expect ≥6 commits naming `p62`.

2. Run final pre-push runner sweep:
   ```bash
   python3 quality/runners/run.py --cadence pre-push
   ```
   Expect exit 0; the 3 P62 rows show PASS or WAIVED-active.

3. Run audit verifier:
   ```bash
   python3 scripts/check_repo_org_gaps.py --json | python3 -m json.tool
   ```
   Expect exit 0; JSON shows `total_items >= 25` and `wave3_items == []`.

4. Confirm STATE.md cursor:
   ```bash
   grep -E "completed_phases:|last_activity:" .planning/STATE.md | head -5
   ```
   Expect `completed_phases: 7` and last_activity naming P62 SHIPPED.

5. Confirm REQUIREMENTS.md flips:
   ```bash
   grep -E "\\*\\*ORG-01\\*\\*|\\*\\*POLISH-ORG\\*\\*" .planning/REQUIREMENTS.md | head -5
   ```
   Expect `[x]` checkboxes.

6. Confirm CLAUDE.md QG-07:
   ```bash
   grep -A 3 "P62 — Repo-org-gaps" CLAUDE.md | head -5
   ```
   Expect H3 subsection present.

If any check fails, return to the corresponding wave for fix-up before proceeding to verdict authorship.
  </how-to-verify>
  <resume-signal>Type "verified" or describe issues to address before verdict authorship.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author verdict (Path A or Path B)</name>
  <files>quality/reports/verdicts/p62/VERDICT.md</files>
  <action>
Mkdir `quality/reports/verdicts/p62/` if missing.

**Try Path A first:** dispatch a Task subagent with `subagent_type: general-purpose` and the verbatim prompt from `quality/PROTOCOL.md` § "Verifier subagent prompt template". Substitute:
- `{PHASE}` = 62
- `{CATALOG_FILE}` = `quality/catalogs/freshness-invariants.json`
- `{P62_ROW_IDS}` = `structure/no-loose-top-level-planning-audits, structure/no-pre-pivot-doc-stubs, structure/repo-org-audit-artifact-present`
- `{ARTIFACT_DIR}` = `quality/reports/verifications/structure/`
- `{AUDIT_DOC}` = `quality/reports/audits/repo-org-gaps.md`
- `{AUDIT_VERIFIER}` = `python3 scripts/check_repo_org_gaps.py --json`
- `{RUNNER_CADENCE}` = `pre-push`
- `{VERDICT_PATH}` = `quality/reports/verdicts/p62/VERDICT.md`
- `{REQUIREMENTS}` = `ORG-01, POLISH-ORG`

The subagent reads the inputs, runs the verifier + audit script, applies the 4 PROTOCOL grading rules, and writes the verdict file.

**If Path A unavailable** (Task tool not present in executor environment per P56-P61 precedent): author the verdict in-session with the Path B disclosure block at top. The verdict file format mirrors `quality/reports/verdicts/p61/VERDICT.md` exactly:
- Top: verdict declaration with Path B disclosure block.
- Top-line summary table.
- Per-row grading (3 rows).
- Audit grading.
- Docs grading (CLAUDE.md, STATE, REQUIREMENTS).
- Recommendations.

Anti-bloat: <=120 lines.

**On RED:** verdict declares RED with cited row + recommended fix. Do NOT proceed to Task 3; instead, escalate to a follow-up fix-up commit and re-dispatch.

**On GREEN:** proceed to Task 3.
  </action>
  <verify>
    <automated>test -f quality/reports/verdicts/p62/VERDICT.md && grep -q "Verdict: GREEN\|Verdict: RED" quality/reports/verdicts/p62/VERDICT.md && test $(wc -l < quality/reports/verdicts/p62/VERDICT.md) -le 130 && echo OK</automated>
  </verify>
  <done>Verdict file exists, declares GREEN or RED, ≤130 lines, follows the Path A or Path B format from PROTOCOL.md.</done>
</task>

<task type="auto">
  <name>Task 3: Commit verdict on GREEN; phase closes</name>
  <files></files>
  <action>
Only run this task if Task 2's verdict is GREEN.

`bash scripts/banned-words-lint.sh` — confirm exit 0.

Stage `quality/reports/verdicts/p62/VERDICT.md`. Commit:

```
chore(p62): verifier subagent verdict GREEN -- P62 closes

ORG-01 + POLISH-ORG shipped. 3 catalog rows graded
(structure/no-loose-top-level-planning-audits,
 structure/no-pre-pivot-doc-stubs,
 structure/repo-org-audit-artifact-present); all PASS or
WAIVED-active. Audit at quality/reports/audits/repo-org-gaps.md
covers N items; zero unclosed dispositions. CLAUDE.md QG-07
update + STATE/REQUIREMENTS flipped in Wave 5.

Path A | Path B per quality/PROTOCOL.md.

P63 may begin (final cohesion + retire migrated sources +
v0.12.1 carry-forward).
```

Push.

If RED: do NOT commit verdict-as-final. Instead, file the verdict as documentation of the RED state, then route back to Wave 3 (or whichever wave created the RED row) for fix-up. The phase reopens.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "verdict GREEN\|verdict RED" && grep -q "Verdict: GREEN" quality/reports/verdicts/p62/VERDICT.md</automated>
  </verify>
  <done>Verdict committed and pushed (on GREEN). Phase 62 closes. P63 entry condition met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor ↔ verifier | Path A subagent has zero session context; Path B disclosure documents the in-session shortcut |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-V01 | Repudiation | "executor self-graded GREEN" | mitigate | Path A subagent dispatch (preferred); Path B disclosure block (fallback) names the rule + commits the constraint |
| T-62-V02 | Tampering | "executor manipulates verdict to claim GREEN on RED" | mitigate | Refuse-GREEN-on-RED rule in PROTOCOL.md + the verdict cites artifact files for every PASS claim |
</threat_model>

<verification>
- `quality/reports/verdicts/p62/VERDICT.md` exists with GREEN or RED declaration.
- Verdict file ≤120 lines (P56-P61 precedent).
- 4 PROTOCOL grading rules honored.
- Per-row grading cites artifact files.
- Audit grading cites `check_repo_org_gaps.py --json` output.
- Banned-words clean.
- Single commit on GREEN; pushed.
</verification>

<success_criteria>
- Verdict file written via Path A or Path B.
- On GREEN: committed + pushed; phase 62 closes; P63 entry condition met.
- On RED: documented; phase remains open; route to fix-up wave.
</success_criteria>

<output>
After completion, create `.planning/phases/62-repo-org-gaps-cleanup/62-06-SUMMARY.md`. The verdict file is the durable artifact; the SUMMARY is a brief narration for the phase log.
</output>
