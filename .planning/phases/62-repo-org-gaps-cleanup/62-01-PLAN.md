---
phase: 62-repo-org-gaps-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/catalogs/freshness-invariants.json
  - quality/catalogs/orphan-scripts.json
  - quality/gates/structure/README.md
autonomous: true
requirements:
  - ORG-01
  - POLISH-ORG
must_haves:
  truths:
    - "P62's FIRST commit writes the catalog rows that define this phase's GREEN contract per quality/PROTOCOL.md Step 3 (catalog-first rule). No POLISH-ORG fix work, no SURPRISES rotation, no audit table rendering happens until Wave 1 lands."
    - "Two new structure-dimension catalog rows ship in quality/catalogs/freshness-invariants.json: structure/no-loose-top-level-planning-audits (asserts no *-MILESTONE-AUDIT.md or SESSION-END-STATE*.md files at .planning/ top level — the rec #49 + rec for SESSION-END-STATE concerns) and structure/no-pre-pivot-doc-stubs (asserts no top-level docs/demo.md / docs/architecture.md / docs/security.md / docs/why.md stub-only files outside the redirect map, OR the file is in mkdocs.yml redirect_maps — extends the existing structure/no-orphan-docs invariant)."
    - "One new structure-dimension row ships: structure/repo-org-gaps-doc-closed (asserts .planning/research/v0.11.1-repo-organization-gaps.md carries a top banner string `Fully audited and closed under P62` AND a pointer to the audit artifact — flips the doc from forgotten-todo-list to closed reference)."
    - "All 3 new rows ship with status=NOT-VERIFIED + short-lived waiver until 2026-05-15 tracked_in P62 Wave 3 (POLISH-ORG fix wave) + Wave 6 (banner update). The waivers expire harmlessly after Wave 3 lands the fixes + Wave 6 lands the banner; runner re-grades to PASS."
    - "ORG-01 audit-render row already exists conceptually in P62 ROADMAP success criterion 2 (audit results committed under quality/reports/verifications/repo-org-gaps/<ts>.md). Wave 1 anchors a row structure/repo-org-audit-artifact-present that asserts an artifact at quality/reports/audits/repo-org-gaps.md exists AND was modified in the current phase (mtime within phase window OR git log naming P62)."
    - "quality/catalogs/orphan-scripts.json gains zero new waiver rows in this wave (per P58 the file is empty; we keep it that way unless a real waiver is needed in Wave 3). The Wave 1 commit only TOUCHES it if a contract-locking waiver row is needed for the v0.12.1 perf-stub-imports drift. Default: no edit."
    - "quality/gates/structure/README.md gains a small section (10-20 added lines under the 60-line P57 cap) describing the 3 new rows + their owner_hint pointing at the freshness-invariants verifier (quality/gates/structure/freshness-invariants.py extended in Wave 3 to handle the 3 new asserts)."
    - "Banned-words clean: no use of the banned word in any of the new strings. Verified via scripts/banned-words-lint.sh (pre-push gate already enforces)."
    - "ALL changes in ONE commit titled `docs(p62): catalog-first commit -- ORG-01 audit + POLISH-ORG recurrence guards (3 rows + dim README delta)`. The commit message cites ORG-01 and POLISH-ORG."
  artifacts:
    - path: "quality/catalogs/freshness-invariants.json"
      provides: "3 NEW rows appended under existing rows array. Wrapper unchanged. Each row uses unified schema per quality/catalogs/README.md (id, dimension=structure, cadence=pre-push, kind=mechanical, sources, expected.asserts, verifier.script=quality/gates/structure/freshness-invariants.py, verifier.args=['--row-id', '<id>'], artifact, status=NOT-VERIFIED, last_verified=null, freshness_ttl=null for mechanical, blast_radius P1, owner_hint, waiver=short-lived 2026-05-15)."
      contains: "structure/no-loose-top-level-planning-audits"
    - path: "quality/gates/structure/README.md"
      provides: "Dimension home updated. Adds a 10-20 line section listing the 3 P62 rows with one-liner descriptions + owner_hint each. Cross-references quality/PROTOCOL.md (no runtime detail duplication)."
      max_lines: 80
    - path: "quality/catalogs/orphan-scripts.json"
      provides: "Untouched in Wave 1 (default). Listed here only because if a Wave 3 absorption reveals a required waiver row, that row's contract is locked here in Wave 1 retroactively (atomic: delete this artifact entry from must_haves if no waiver needed in Wave 3)."
  key_links:
    - from: "quality/catalogs/freshness-invariants.json (3 new rows)"
      to: "quality/gates/structure/freshness-invariants.py"
      via: "row.verifier.script + row.verifier.args=['--row-id', <id>] dispatching to a new branch in the existing freshness-invariants verifier (Wave 3 implements the asserts)"
      pattern: "freshness-invariants.py"
    - from: "All 3 rows (waiver.tracked_in)"
      to: "P62 Wave 3 (POLISH-ORG fix) + Wave 6 (banner update)"
      via: "row.waiver.tracked_in field naming the wave that retires the waiver"
      pattern: "P62 Wave"
    - from: "structure/repo-org-audit-artifact-present row"
      to: "quality/reports/audits/repo-org-gaps.md (Wave 2 produces it)"
      via: "row.expected.asserts cites the file path Wave 2 will materialize"
      pattern: "quality/reports/audits/repo-org-gaps.md"
    - from: "quality/catalogs/freshness-invariants.json + quality/gates/structure/README.md"
      to: "Wave 6 unbiased verifier subagent"
      via: "row.expected.asserts is the contract Wave 6 grades against; locked in this commit BEFORE Waves 2-5 land code"
      pattern: "expected"
---

<objective>
Wave 1 — the **catalog-first commit**. Per `quality/PROTOCOL.md` § "Per-phase protocol" Step 3 and the recurring success criterion in REQUIREMENTS.md ("Recurring criterion 1: catalog rows ship before code"). This wave does NOT execute the audit, does NOT close any gaps, does NOT rotate SURPRISES.md — it only declares "here is what GREEN means for P62" so the unbiased verifier subagent in Wave 6 reads a contract that existed BEFORE the implementation landed.

Three deliverables ship in this commit:

1. **`quality/catalogs/freshness-invariants.json`** (extended; existing rows untouched) — append 3 new structure-dimension rows:
   - `structure/no-loose-top-level-planning-audits` — `find .planning -maxdepth 1 -name '*MILESTONE-AUDIT*' -o -name 'SESSION-END-STATE*' returns empty`. The audit doc rec #49 calls out 3 such loose files (`v0.9.0-MILESTONE-AUDIT.md`, `v0.10.0-MILESTONE-AUDIT.md`, `SESSION-END-STATE*.md`) that should live under `.planning/milestones/audits/` or `.planning/archive/`. P1 blast radius. Wave 3 fixes by relocating; this row guards against recurrence.
   - `structure/no-pre-pivot-doc-stubs` — every `docs/*.md` stub <500 bytes either has an entry in `mkdocs.yml` `plugins.redirects.redirect_maps` OR is currently in `mkdocs.yml` `nav:`. Extends the existing `structure/no-orphan-docs` invariant with a stub-specific assertion (a stub at top-level is a navigation orphan unless the redirect map covers it). P1 blast radius. Wave 3 confirms `docs/demo.md` is properly mapped.
   - `structure/repo-org-audit-artifact-present` — asserts the audit table file exists at `quality/reports/audits/repo-org-gaps.md` AND has been modified within the current milestone window (mtime check OR git log -1 commit message naming `p62`). P1 blast radius. Wave 2 produces the artifact; this row guards that future repo-org audits land in a known location, not as one-off research docs.

2. **`quality/gates/structure/README.md`** (extended) — add a small section under existing content listing the 3 new P62 rows with one-line descriptions + the owner_hint each. Anti-bloat: 10-20 added lines, total file <=80 lines (P57 set the cap at 60; P62 carve-out justifies +20).

3. **`quality/catalogs/orphan-scripts.json`** (likely untouched) — only edited if Wave 3 unearths a real waiver requirement. Default: no edit. Listed in `files_modified` for the atomic Wave-3-retroactive-waiver case only; remove from frontmatter if Wave 3 confirms no waivers.

**Why ship the README delta in Wave 1?** Because the 3 new rows live in a dimension (structure) whose README is the dimension scope contract. Adding row descriptions in Wave 1 means Wave 6's verifier subagent has a clear scope statement to read.

**Why no actual fixes yet?** Per catalog-first, fixes ship in Wave 3 (POLISH-ORG) AFTER the contract is locked. If we fix-and-catalog in one commit, the verifier subagent cannot prove the catalog row pre-existed the fix — drift mitigation fails.

**Why short-lived waivers?** Same reason as P58/P59/P60/P61 Wave 1: the 3 new rows have `kind=mechanical` and the verifier branch for them does not yet exist in `quality/gates/structure/freshness-invariants.py` until Wave 3 extends it. Without a waiver, the runner returns NOT-VERIFIED and `compute_exit_code` treats P1+NOT-VERIFIED as exit=1, blocking pre-push. Short-lived waiver until 2026-05-15 (~18d from this commit at 2026-04-27) keeps every cadence GREEN through the implementation window. Waivers expire harmlessly after Wave 3 + Wave 6.

Purpose: lock the contract. After this commit, Wave 2's audit cannot quietly skip a gap, Wave 3 cannot quietly waive a P0/P1 fix without an explicit waiver row, and Wave 6 cannot grade GREEN unless the asserts pass.

Output: 2-3 files committed in a single commit titled `docs(p62): catalog-first commit -- ORG-01 audit + POLISH-ORG recurrence guards (3 rows + dim README delta)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/v0.11.1-repo-organization-gaps.md
@quality/PROTOCOL.md
@quality/catalogs/README.md
@quality/catalogs/freshness-invariants.json
@quality/gates/structure/README.md
@quality/gates/structure/freshness-invariants.py

<interfaces>
<!-- Existing freshness-invariants.json row shape (extracted from current rows). New rows MUST conform. -->

```jsonc
{
  "id": "structure/<slug>",
  "dimension": "structure",
  "cadence": "pre-push",
  "kind": "mechanical",
  "sources": ["<file>:<line>", "..."],
  "command": null,
  "expected": {
    "asserts": ["<concrete predicate>"]
  },
  "verifier": {
    "script": "quality/gates/structure/freshness-invariants.py",
    "args": ["--row-id", "structure/<slug>"],
    "timeout_s": 30,
    "container": null
  },
  "artifact": "quality/reports/verifications/structure/<slug>.json",
  "status": "NOT-VERIFIED",
  "last_verified": null,
  "freshness_ttl": null,
  "blast_radius": "P1",
  "owner_hint": "<one-liner>",
  "waiver": {
    "until": "2026-05-15T00:00:00Z",
    "reason": "Catalog-first: row ships before verifier branch lands in Wave 3. Short-lived; expires after Wave 3 + Wave 6.",
    "dimension_owner": "structure",
    "tracked_in": "P62 Wave 3 (POLISH-ORG)"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 3 NOT-VERIFIED rows to quality/catalogs/freshness-invariants.json</name>
  <files>quality/catalogs/freshness-invariants.json</files>
  <action>
Read the existing file. Append 3 new rows to the `rows` array (do NOT touch existing rows; preserve formatting). Each row uses the interface shape above.

Row 1 — `structure/no-loose-top-level-planning-audits`:
- sources: ["`.planning/research/v0.11.1-repo-organization-gaps.md`:49 (rec #49 — top-level audits should live under .planning/milestones/audits/)", "CLAUDE.md:Freshness invariants"]
- expected.asserts: ["`find .planning -maxdepth 1 -type f \\( -name '*MILESTONE-AUDIT*.md' -o -name 'SESSION-END-STATE*' \\) | grep -v archive` returns empty stdout"]
- blast_radius: P1
- owner_hint: "any new milestone-audit or session-end-state document at .planning/ top level — relocate under .planning/milestones/audits/ or .planning/archive/"
- waiver as above (until 2026-05-15, tracked_in P62 Wave 3)

Row 2 — `structure/no-pre-pivot-doc-stubs`:
- sources: ["`.planning/research/v0.11.1-repo-organization-gaps.md`:82-83 (rec — top-level doc stubs)", "CLAUDE.md:Freshness invariants"]
- expected.asserts: ["For each `docs/*.md` whose byte size <500 AND whose top-level slug appears as a key in `mkdocs.yml` `plugins.redirects.redirect_maps`, the redirect target resolves to a file present under `docs/`. Stubs without a redirect entry AND without a `nav:` entry are flagged."]
- blast_radius: P1
- owner_hint: "any new top-level docs/<slug>.md stub <500 bytes — add to mkdocs.yml redirect_maps or remove"
- waiver as above

Row 3 — `structure/repo-org-audit-artifact-present`:
- sources: ["`.planning/REQUIREMENTS.md`:ORG-01 (audit must be committed under quality/reports/)", "`.planning/ROADMAP.md`:P62 success criterion 2"]
- expected.asserts: ["File `quality/reports/audits/repo-org-gaps.md` exists", "File contains a top-section with audit table mapping every numbered gap from `.planning/research/v0.11.1-repo-organization-gaps.md` § 'Top 10 cleanup recommendations' to a closure path (closed-by-catalog-row | closed-by-existing-gate | waived)"]
- blast_radius: P1
- owner_hint: "Wave 2 produces the audit; future repo-org audits land at the same path"
- waiver as above

Validate JSON parses with `python3 -c 'import json; json.load(open("quality/catalogs/freshness-invariants.json"))'`.
  </action>
  <verify>
    <automated>python3 -c 'import json; d = json.load(open("quality/catalogs/freshness-invariants.json")); ids = [r["id"] for r in d["rows"]]; assert "structure/no-loose-top-level-planning-audits" in ids; assert "structure/no-pre-pivot-doc-stubs" in ids; assert "structure/repo-org-audit-artifact-present" in ids; print("OK: 3 P62 rows present")'</automated>
  </verify>
  <done>3 new rows present in catalog. Existing rows untouched (diff shows only additions). JSON parses. Row schemas match the interface block.</done>
</task>

<task type="auto">
  <name>Task 2: Extend quality/gates/structure/README.md with P62 row descriptions</name>
  <files>quality/gates/structure/README.md</files>
  <action>
Read the existing README. Append a new section under the existing content. Anti-bloat: 10-20 added lines; do NOT rewrite existing sections.

Section header: `## P62 — repo-org recurrence guards`

Body: a 3-row markdown table with columns | Row id | What it asserts | Owner hint | — one row per new catalog entry. Below the table, one paragraph (3-5 lines) stating: "These 3 rows ship in P62 Wave 1 catalog-first commit. The verifier branch in `quality/gates/structure/freshness-invariants.py` is extended in P62 Wave 3 to handle the new `--row-id` values. Until Wave 3 lands, the rows wear a short-lived waiver until 2026-05-15. After Wave 3 + Wave 6, the runner re-grades to PASS and the waivers expire harmlessly."

Cross-reference `quality/PROTOCOL.md` for runtime details (do NOT duplicate).

Verify final file is <=80 lines via `wc -l`.
  </action>
  <verify>
    <automated>test $(wc -l < quality/gates/structure/README.md) -le 80 && grep -q "P62" quality/gates/structure/README.md && grep -q "no-loose-top-level-planning-audits" quality/gates/structure/README.md && echo OK</automated>
  </verify>
  <done>README has P62 section with 3-row table + paragraph. <=80 lines total. Existing content untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Banned-words self-check + JSON contract self-check + commit</name>
  <files></files>
  <action>
Run `bash scripts/banned-words-lint.sh` against the 2 modified files (the lint runs over the whole repo by default; just confirm exit 0). If any P62 string trips the lint, fix it (rewording is fine; no banned word survives).

Run `python3 scripts/check_quality_catalogs.py` (or `scripts/check-quality-catalogs.py`, whichever is the canonical one) to confirm the new rows pass schema validation. If a `dimension` allow-list rejects something, that's a bug — but `structure` is already in the list so this should pass clean.

Stage ONLY the 2 files (or 3 if orphan-scripts.json was actually touched in Task 1; default it should not be). Commit with message:

```
docs(p62): catalog-first commit -- ORG-01 audit + POLISH-ORG recurrence guards

- quality/catalogs/freshness-invariants.json: 3 new rows
  (structure/no-loose-top-level-planning-audits,
  structure/no-pre-pivot-doc-stubs,
  structure/repo-org-audit-artifact-present);
  short-lived waivers until 2026-05-15 tracked_in P62 Wave 3.
- quality/gates/structure/README.md: P62 row table + intro paragraph.

Per quality/PROTOCOL.md catalog-first rule. Implementation
(verifier branches + audit artifact + banner update + SURPRISES
rotation) lands in P62 Waves 2-5; verifier subagent grades in Wave 6.

Refs ORG-01, POLISH-ORG. Phase 62 entry.
```

Push (pre-push gates run; the new rows are waived so should not block).
  </action>
  <verify>
    <automated>bash scripts/banned-words-lint.sh && python3 scripts/check_quality_catalogs.py 2>/dev/null || python3 scripts/check-quality-catalogs.py</automated>
  </verify>
  <done>Banned-words clean. Catalog schema validator passes. Commit landed; pre-push GREEN; pushed to origin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog row ↔ verifier | Wave 1 row ships before verifier branch — runner must respect waiver to not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | catalog rows | mitigate | Catalog-first commit + verifier subagent at phase close confirms rows pre-existed implementation |
| T-62-02 | Repudiation | "I closed gap X" claim | mitigate | Wave 2 produces a per-gap audit table at quality/reports/audits/repo-org-gaps.md citing closure mechanism |
</threat_model>

<verification>
- `python3 -c 'import json; ...'` confirms 3 new rows present.
- `bash scripts/banned-words-lint.sh` exit 0.
- `python3 scripts/check_quality_catalogs.py` exit 0.
- Pre-push hook GREEN (waivers prevent NOT-VERIFIED-blocks-pre-push).
</verification>

<success_criteria>
- 3 new structure-dimension rows committed in `quality/catalogs/freshness-invariants.json`.
- `quality/gates/structure/README.md` has a P62 section <=80 lines total.
- Single commit with the specified title format. Banned-words clean.
- Pre-push GREEN. Push succeeded.
</success_criteria>

<output>
After completion, create `.planning/phases/62-repo-org-gaps-cleanup/62-01-SUMMARY.md`
</output>
