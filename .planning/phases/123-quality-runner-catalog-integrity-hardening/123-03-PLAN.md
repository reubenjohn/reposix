---
phase: 123-quality-runner-catalog-integrity-hardening
plan: 03
type: execute
wave: 2
depends_on: [123-01]
files_modified:
  - quality/gates/code/ci-green-on-main.sh
  - quality/gates/agent-ux/lib/t4-real-backend-flow.sh
  - quality/gates/agent-ux/lib/t4-real-backend-flow.selftest.sh
  - quality/catalogs/code.json
autonomous: true
requirements: [DRAIN-01, DRAIN-10]

must_haves:
  truths:
    - "The phase-close CI-green probe fails RED if EITHER ci.yml OR release-plz.yml's latest run on main is not success — not just ci.yml"
    - "When the real-backend t4 gate's checkout step fails for a reason OTHER than git version, the error message shows the actual git stderr, not a hardcoded and now-known-false git-version claim"
  artifacts:
    - path: quality/gates/code/ci-green-on-main.sh
      provides: "loops over a required-workflow list [ci.yml, release-plz.yml] instead of a single hardcoded WORKFLOW"
    - path: quality/gates/agent-ux/lib/t4-real-backend-flow.sh
      provides: "captures real git stderr from both checkout call sites instead of a hardcoded fallback string"
  key_links:
    - from: "quality/gates/code/ci-green-on-main.sh"
      to: "gh run list --workflow=<each of ci.yml, release-plz.yml> --branch=main --limit=1"
      via: "a per-workflow loop; any unknowable/red workflow overrides an otherwise-green aggregate"
      pattern: "for wf in.*WORKFLOWS|WORKFLOWS="
---

<objective>
Close DRAIN-10 (SC5a) and DRAIN-01 (SC5b) — two independent, small, "gate honesty" fixes bundled
in one plan because neither touches `run.py`/the shared `structure` catalog file (no file conflict
with plans 02/04/05/06), and both are the sonnet-tier half of this phase per the coordinator's
routing note.

Purpose: SC5a stops a persistently-RED `release-plz` on main from rotting unnoticed (the phase-close
probe currently watches ONLY `ci.yml`). SC5b stops the real-backend t4 gate from teaching the wrong
fix — the calling script already confirmed git >= 2.34 before the affected checkout call, so blaming
git version at that point is ALWAYS wrong.

Output: `quality/gates/code/ci-green-on-main.sh` watches `[ci.yml, release-plz.yml]`;
`quality/gates/agent-ux/lib/t4-real-backend-flow.sh` surfaces real captured stderr; a new hermetic
selftest proves the SC5b fix without needing real Confluence credentials.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@quality/PROTOCOL.md

Files this plan edits (read current state before editing):
@quality/gates/code/ci-green-on-main.sh
@quality/gates/agent-ux/lib/t4-real-backend-flow.sh
@quality/gates/agent-ux/t4-conflict-rebase-ancestry-real-backend.sh (the CALLER — confirms git >=2.34 is already gated at lines ~179-193 BEFORE `_t4_real_backend_flow` ever runs; do NOT edit this file, just read it for context)

Empirical evidence backing the WORKFLOWS list choice (verified during planning, 2026-07-18):
- `.github/workflows/ci.yml`: `on: push: branches: [main]` (no path filter)
- `.github/workflows/release-plz.yml`: `on: push: branches: [main]` (no path filter, identical trigger shape to ci.yml)
- `.github/workflows/audit.yml`: `on: push: branches: [main], paths: [**/Cargo.toml, **/Cargo.lock]` — EXCLUDED, conditional
- `.github/workflows/docs.yml`: `on: workflow_run: workflows: ["CI"]` — EXCLUDED, not push-triggered
- `.github/workflows/quality-post-release.yml`: `on: workflow_run: workflows: ["release"]` — EXCLUDED, not push-triggered
- `gh run list --workflow=release-plz.yml --limit 8`: 8/8 recent runs on main concluded `success` (no `skipped` observed)
</context>

<interfaces>
`hard_fail_exit(label, detail)` — already defined in `t4-conflict-rebase-ancestry-real-backend.sh`
and in scope wherever `lib/t4-real-backend-flow.sh` is sourced:
```bash
hard_fail_exit() {  # hard_fail_exit <label> [<detail>]
  local label="$1" detail="${2:-}"
  ...
  exit 1
}
```
This task does not change its signature — only what string gets passed as `detail`.
</interfaces>

<execution_constraints>
## Binding constraints (embedded verbatim — apply to every task in this plan)

- ONE cargo invocation machine-wide; cargo is FOREGROUND-only, never backgrounded/detached (OOM
  risk — this VM OOM-crashed 3x). Prefer `-p <crate>`. (Minimal cargo here -- Python/shell phase.)
- Leaf test setup (`reposix init`/sim-seed/`git config`/`git commit` for fixtures) runs in a
  `/tmp` clone, `cd`-ing in the SAME Bash invocation -- NEVER the shared repo (PreToolUse
  `leaf-isolation-guard.sh` exit 2 enforces).
- Uncommitted = didn't happen; commit before ending any turn; mid-phase commits stay LOCAL until
  phase-close push. No `--no-verify`. Targeted staging (never `-A`/`.`). One tree-writer at a
  time. No tag push.
- Never hand-pick gates before a push -- run `python3 quality/runners/run.py --cadence pre-push`
  (FULL). Docs edits pass BOTH `docs-build/*` AND `structure/banned-words`. Doc-alignment
  rebind-in-same-commit for any line carrying a doc-alignment binding.

## OD-3 ownership charter (embedded verbatim — carry this into every task)

1. You own what you touch. Acceptance criteria are the floor, not the ceiling.
2. Noticing is a deliverable -- report lying doc claims, tests that don't assert what their names
   promise, teaching-free errors, dead code, missing edge cases. An empty noticing section from
   code-touching work is a red flag.
3. Eager-fix or file -- never silently skip. <1h + no new dependency -> fix in place; else ->
   SURPRISES-INTAKE / GOOD-TO-HAVES with severity + sketch.
4. Verify against reality -- run it, render it, hit the backend. A claim without an artifact is
   not done.
5. North star -- Rust-compiler-grade UX: every user-facing error teaches the fix, suggests the
   alternative, gives a copy-paste recovery command. Would a skeptical first-time dev come away
   impressed?
</execution_constraints>

<tasks>

<task type="auto">
  <name>Task 1: SC5a — parameterize ci-green-on-main.sh into a required-workflow list</name>
  <files>quality/gates/code/ci-green-on-main.sh, quality/catalogs/code.json</files>
  <action>
Replace the single `WORKFLOW="ci.yml"` (line 43) with `WORKFLOWS=("ci.yml" "release-plz.yml")`.
These are the ONLY two workflows confirmed (see context above) to fire unconditionally on
`push: branches: [main]` with no path filter.

Loop over `WORKFLOWS`, running the existing `gh run list --workflow="$wf" --branch=main --limit=1
--json databaseId,conclusion,status` + the same Python VERDICT-parsing logic, once per workflow.
Aggregate semantics:
  - ANY workflow's `gh` call failing (missing/unauth), reporting no runs, or in-progress -> overall
    NOT-VERIFIED (exit 75), naming WHICH workflow was unknowable.
  - Else if ANY workflow's latest run concluded non-success -> overall FAIL (exit 1), naming WHICH
    workflow is red and its conclusion.
  - Else (every watched workflow's latest run concluded success) -> overall PASS (exit 0).

Extend the JSON artifact to carry a `workflows` object keyed by workflow name with each one's raw
verdict, in addition to the existing top-level `asserts_passed`/`asserts_failed` (one string per
watched workflow, e.g. `"latest release-plz.yml run on main concluded success"`).

Update the header comment block to document the required-workflow-list rationale, citing the
empirical `gh run list --workflow=release-plz.yml --limit 8` evidence (8/8 success, checked
2026-07-18) and explicitly naming WHY `audit.yml`/`docs.yml`/`quality-post-release.yml` are
excluded (conditional trigger / workflow_run-triggered, not 1:1 with the just-landed push).

Run it for real against the current main tip and confirm PASS for both workflows, then mint:
`python3 quality/runners/run.py --cadence post-push --persist` (the row is tagged `post-push`).
  </action>
  <verify>
    <automated>bash quality/gates/code/ci-green-on-main.sh; echo "exit=$?"</automated>
  </verify>
  <done>Script exits 0 against the real main tip; artifact JSON shows both `ci.yml` and
  `release-plz.yml` as success; catalog row `code/ci-green-on-main` reads PASS after the mint
  run.</done>
</task>

<task type="auto">
  <name>Task 2: SC5b — t4 real-backend-flow surfaces real checkout stderr</name>
  <files>quality/gates/agent-ux/lib/t4-real-backend-flow.sh, quality/gates/agent-ux/lib/t4-real-backend-flow.selftest.sh</files>
  <action>
In `quality/gates/agent-ux/lib/t4-real-backend-flow.sh` (lines 47-50), extract the repeated
checkout+fail pattern into a small named helper:

```bash
_t4_checkout_or_fail() {  # _t4_checkout_or_fail <tree> <label>
  local tree="$1" label="$2" err
  err="$(git -C "$tree" checkout -B main refs/reposix/origin/main 2>&1 >/dev/null)" \
    || hard_fail_exit "git checkout -B main (${label}) failed" "$err"
}
```

Replace the two direct call sites with `_t4_checkout_or_fail "$a" "A"` and
`_t4_checkout_or_fail "$b" "B"`. The `2>&1 >/dev/null` idiom captures ONLY stderr into `err`
(stdout discarded) — verify this ordering is correct (stderr duped to the current stdout target
first, THEN stdout redirected to /dev/null) before committing.

This surfaces the REAL captured stderr as the failure detail instead of the hardcoded
`"requires git >= 2.34 stateless-connect fetch"` string, which is misleading BY CONSTRUCTION at
this call site: the caller (`t4-conflict-rebase-ancestry-real-backend.sh`, lines ~179-193) already
verified `git --version >= 2.34` before this function ever runs, so any failure reaching here is
NEVER a version problem — most commonly the Confluence list-vs-get oid-drift class documented in
`.planning/phases/114-t4-confluence-oid-drift-fix-first-reconcile-audit/114-VERIFICATION.md`.

Do NOT touch the sim-arm sibling `quality/gates/agent-ux/t4-conflict-rebase-ancestry.sh` (lines
120-123) — visually similar code, but a DIFFERENT bug class (the sim backend cannot produce a
Confluence oid-drift; DRAIN-01/the SURPRISES-INTAKE entry scope this fix to the real-backend arm
only). Confirm via `git diff` that this file is untouched when you're done.

Write a NEW hermetic selftest `quality/gates/agent-ux/lib/t4-real-backend-flow.selftest.sh`
(throwaway `/tmp` fixture, mirroring the `quality/gates/structure/file-size-limits.selftest.sh`
precedent — a plain `git init` in `/tmp`, NOT `reposix init`/sim-seed, so the leaf-isolation-guard
hook does not apply at all here): source the lib, create a bare-minimum `/tmp` git repo WITHOUT a
`refs/reposix/origin/main` ref, define a stub `hard_fail_exit` that records its args instead of
exiting (so the selftest can assert on them), call `_t4_checkout_or_fail` against the fixture repo,
and assert (a) the stub `hard_fail_exit` was invoked, and (b) the captured detail argument contains
real git stderr text (matches `error:|fatal:|pathspec`) and does NOT contain the string
`"requires git >= 2.34"`. This proves the structural fix without needing real Confluence
credentials.
  </action>
  <verify>
    <automated>bash quality/gates/agent-ux/lib/t4-real-backend-flow.selftest.sh</automated>
  </verify>
  <done>Selftest passes, proving the captured-stderr path replaces the hardcoded fallback;
  `git diff --stat` confirms the sim-arm sibling script is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `gh` CLI output -> phase-close verdict | `gh run list` output is trusted (authenticated GitHub API via the operator's own `gh auth`), not attacker-influenced |
| Real git stderr -> catalog artifact | Captured stderr from a local `git checkout` in a throwaway working tree; local filesystem/git errors, not external network content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-05 | Denial of Service | Two sequential `gh run list` calls could exceed the row's timeout budget | mitigate | bumped `verifier.timeout_s` 60->90 in 123-01's catalog-first edit; each `gh` call is a single lightweight API request, not expected to approach that budget |
| T-123-06 | Information Disclosure | Captured real git stderr could theoretically echo unexpected local paths | accept | stderr here is local git plumbing output (oid/ref errors) in a throwaway `/tmp` working tree, not credential material — consistent with the project's existing practice of surfacing raw git stderr in other error paths |
</threat_model>

<verification>
`bash quality/gates/code/ci-green-on-main.sh` exits 0 against real main.
`bash quality/gates/agent-ux/lib/t4-real-backend-flow.selftest.sh` exits 0.
`git diff --stat` shows exactly the 4 declared `files_modified` touched.
</verification>

<success_criteria>
`code/ci-green-on-main` watches `[ci.yml, release-plz.yml]` and is minted PASS against the real
tip. `t4-real-backend-flow.sh`'s checkout failures surface real stderr, proven by a hermetic
selftest; the sim-arm sibling is untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/123-quality-runner-catalog-integrity-hardening/123-03-SUMMARY.md`
</output>
