---
phase: 123-quality-runner-catalog-integrity-hardening
plan: 07
type: execute
wave: 6
depends_on: [123-02, 123-03, 123-04, 123-05, 123-06]
files_modified:
  - .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md
  - .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - quality/CLAUDE.md
  - quality/PROTOCOL.md
autonomous: true
requirements: [DRAIN-01, DRAIN-03, DRAIN-04, DRAIN-05, DRAIN-06, DRAIN-10]

must_haves:
  truths:
    - "Every SURPRISES-INTAKE entry this phase closes has a terminal STATUS with a real commit cross-ref, not a residual TBD"
    - "quality/CLAUDE.md and quality/PROTOCOL.md document every new convention this phase introduced (--allow-downgrade, the flock behavior, the new verifier-script-exists gate, the required-workflow-list config, .env self-sourcing) so the next agent doesn't rediscover them"
    - ".planning/REQUIREMENTS.md and .planning/STATE.md both reflect P123 CLOSED before the phase-close push lands"
    - "main's latest CI run is green after the phase-close push"
  artifacts:
    - path: quality/CLAUDE.md
      provides: "fix-twice documentation of SC1-SC5's new conventions, tagged to the right dimension"
    - path: .planning/STATE.md
      provides: "P123 CLOSED GREEN entry, completed_phases 9->10, percent 60->67"
  key_links:
    - from: ".planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md"
      to: "the 4 entries this phase resolves"
      via: "STATUS: RESOLVED lines citing real commit SHAs from this phase's own git log"
      pattern: "STATUS: RESOLVED"
---

<objective>
Close wave for Phase 123: intake hygiene, fix-twice documentation, ROADMAP/STATE/REQUIREMENTS
advance, and the phase-close push + post-push cadence. Depends on ALL 5 implementation plans
(123-02 through 123-06) having landed.

Purpose: this is where the phase's paper trail catches up with reality — every SURPRISES-INTAKE and
GOOD-TO-HAVES entry this phase closes gets a terminal status with a real cross-ref (not a promise to
close it later), every new convention gets documented in quality/CLAUDE.md per the mandatory
fix-twice rule, and the phase actually ships (push lands, main's CI goes green on the new tip).

Output: 4 SURPRISES-INTAKE entries + 3 GOOD-TO-HAVES entries marked RESOLVED with cross-refs; 1 new
GOOD-TO-HAVE filed (pre-ADF residual carry-forward from P114); quality/CLAUDE.md + PROTOCOL.md
updated; REQUIREMENTS.md + STATE.md advanced; the phase pushed with main's CI confirmed green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@quality/PROTOCOL.md
@quality/CLAUDE.md

Entries this task resolves (read the exact current text before editing — do not paraphrase from
memory):
@.planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md (the `2026-07-14 20:40` HIGH oid-drift
entry, the `2026-07-14 20:41` MEDIUM misleading-message entry, the `2026-07-14 20:43` HIGH
env-loading-gap entry, the `2026-07-14 20:44` HIGH silent-downgrade entry)
@.planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md (GTH-V15-01, GTH-V15-03, GTH-V15-07)
@.planning/phases/114-t4-confluence-oid-drift-fix-first-reconcile-audit/114-VERIFICATION.md (the
cross-ref target for the oid-drift entry's resolution, and the source of the OQ1 pre-ADF residual
this task files as a new GOOD-TO-HAVE)
@.planning/REQUIREMENTS.md (the DRAIN-01/03/04/05/06/10 checkbox lines ~L162-238 and the coverage
table ~L303-330)
</context>

<execution_constraints>
## Binding constraints (embedded verbatim — apply to every task in this plan)

- ONE cargo invocation machine-wide; cargo is FOREGROUND-only, never backgrounded/detached (OOM
  risk — this VM OOM-crashed 3x). Prefer `-p <crate>`. (Minimal cargo here -- Python/shell phase.)
- Leaf test setup (`reposix init`/sim-seed/`git config`/`git commit` for fixtures) runs in a
  `/tmp` clone, `cd`-ing in the SAME Bash invocation -- NEVER the shared repo (PreToolUse
  `leaf-isolation-guard.sh` exit 2 enforces).
- Uncommitted = didn't happen; commit before ending any turn; mid-phase commits stay LOCAL until
  phase-close push. No `--no-verify`. Targeted staging (never `-A`/`.`). One tree-writer at a
  time. No tag push.
- Never hand-pick gates before a push -- run `python3 quality/runners/run.py --cadence pre-push`
  (FULL). Docs edits pass BOTH `docs-build/*` AND `structure/banned-words`. Doc-alignment
  rebind-in-same-commit for any line carrying a doc-alignment binding.

## OD-3 ownership charter (embedded verbatim — carry this into every task)

1. You own what you touch. Acceptance criteria are the floor, not the ceiling.
2. Noticing is a deliverable -- report lying doc claims, tests that don't assert what their names
   promise, teaching-free errors, dead code, missing edge cases. An empty noticing section from
   code-touching work is a red flag.
3. Eager-fix or file -- never silently skip. <1h + no new dependency -> fix in place; else ->
   SURPRISES-INTAKE / GOOD-TO-HAVES with severity + sketch.
4. Verify against reality -- run it, render it, hit the backend. A claim without an artifact is
   not done.
5. North star -- Rust-compiler-grade UX: every user-facing error teaches the fix, suggests the
   alternative, gives a copy-paste recovery command. Would a skeptical first-time dev come away
   impressed?
</execution_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Intake hygiene — SURPRISES-INTAKE + GOOD-TO-HAVES resolution + new filing</name>
  <files>.planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md, .planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md</files>
  <action>
First: `git log --oneline -20` to collect the REAL commit SHAs for each of this phase's plans (123-01
through 123-06) — do not fabricate SHAs.

In `.planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md`, append (do not delete the entry
body — this is an append-only ledger) a `**STATUS:**` update for FOUR entries:

1. The `2026-07-14 20:40 | ... severity: HIGH` Confluence oid-drift entry (currently
   `STATUS: OPEN`): change to `STATUS: RESOLVED — 2026-07-15, Phase 114 (list_records/get_record
   render-parity fix, ADF body-format alignment). Cross-ref:
   .planning/phases/114-t4-confluence-oid-drift-fix-first-reconcile-audit/114-VERIFICATION.md (SC1
   — live TokenWorld checkout incl. page 7766017, zero OidDrift). Stale bookkeeping: this HIGH row
   predates P114's fix landing; do NOT re-open or re-fix. The OQ1 pre-ADF list-path
   storage-fallback residual P114 flagged is tracked separately as GTH-V15-80 (filed in this same
   close wave).`
2. The `2026-07-14 20:41 | ... severity: MEDIUM` misleading-git-version-message entry: change to
   `STATUS: RESOLVED — Phase 123 SC5b (<cite the real 123-03 commit SHA>;
   quality/gates/agent-ux/lib/t4-real-backend-flow.sh now captures and surfaces real git checkout
   stderr instead of the hardcoded git-version fallback).`
3. The `2026-07-14 20:43 | ... severity: HIGH` env-loading-gap entry: change to
   `STATUS: RESOLVED — Phase 123 SC1 (<cite the real 123-02 commit SHA>;
   quality/runners/_env_load.py, run.py self-sources ./.env when present).`
4. The `2026-07-14 20:44 | ... severity: HIGH` --persist silent-downgrade entry: change to
   `STATUS: RESOLVED — Phase 123 SC2 (<cite the real 123-04 commit SHA>; --allow-downgrade
   opt-in, refuse-by-default guard in quality/runners/_persist_guard.py).`

In `.planning/milestones/v0.15.0-phases/GOOD-TO-HAVES.md`:
- Append `· STATUS: **RESOLVED in Phase 123 (<today's date>)** — tag structure/persist-concurrency
  (<cite the real 123-05 commit SHA>).` to GTH-V15-01's Source bullet line.
- Append `· STATUS: **RESOLVED in Phase 123 (<today's date>)** — tag structure/verifier-integrity
  (<cite the real 123-06 commit SHA>).` to GTH-V15-03's Source bullet line.
- Append `· STATUS: **RESOLVED in Phase 123 (<today's date>)** — tag code/ci-green-on-main (<cite
  the real 123-03 commit SHA>).` to GTH-V15-07's Source bullet line. Note this entry's two
  owner-gate open questions (does release-plz run on every push; how is a no-op run's conclusion
  classified) were answered with empirical evidence already cited in the 123-03 catalog-row edit —
  do not re-open them.
- Add a NEW entry titled `### GTH-V15-80 — Confluence list-path lacks the storage-format fallback
  get_record has (pre-ADF pages would still oid-drift)` under a new `## From Phase 123 close (P114
  OQ1 residual carry-forward)` heading (append near the end of the file, following the existing
  dated-heading convention). Severity MEDIUM. Source: P114 `114-VERIFICATION.md` OQ1 residual
  (carried forward, NOT re-diagnosed by this phase). What: the LIST path requests
  `body-format=atlas_doc_format` with NO storage fallback, while `get_record` DOES fall back to
  `body-format=storage` — a pre-ADF (storage-only) Confluence page would therefore still trip the
  oid-drift check on checkout. Fix-sketch: add the same storage-format fallback to the list path
  that `get_record` already has, in the Confluence connector under `crates/reposix-core`. STATUS:
  OPEN.

Do NOT touch GTH-V15-04/05/06/09 (already resolved by Phase 122's DRAIN-07/08/09) or GTH-V15-02
(cross-reference only, not this phase's scope).
  </action>
  <verify>
    <automated>grep -c "STATUS: RESOLVED" .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md</automated>
  </verify>
  <done>4 SURPRISES-INTAKE entries carry a terminal RESOLVED status with real cross-refs; 3
  GOOD-TO-HAVES entries (GTH-V15-01/03/07) carry a RESOLVED status; GTH-V15-80 is filed OPEN for
  the pre-ADF residual.</done>
</task>

<task type="auto">
  <name>Task 2: Fix-twice consolidated doc update + REQUIREMENTS.md advance</name>
  <files>quality/CLAUDE.md, quality/PROTOCOL.md, .planning/REQUIREMENTS.md</files>
  <action>
**quality/CLAUDE.md** (append paragraphs; do NOT rewrite existing sections, per this file's own
documented anti-bloat convention): add content covering, each tagged to the right dimension —
- The `--allow-downgrade` flag on `quality/runners/run.py` and the refuse-by-default downgrade
  guard: add near the existing "GRADE/PERSIST split (D-P96-01)" cross-reference — a short paragraph
  here, fuller mechanics in `quality/PROTOCOL.md`'s "New runner/validator semantics" section (see
  below).
- The advisory-flock persist-concurrency guard: same location, one paragraph.
- `structure/verifier-script-exists` as a new structure-dimension gate: add to the "Structure-
  dimension gates (P89)" section's list of gates.
- `run.py`'s conditional `./.env` self-sourcing: add near wherever the `pre-release-real-backend`
  cadence is described.
- `code/ci-green-on-main`'s required-workflow-list (`[ci.yml, release-plz.yml]`): update wherever
  that gate is currently mentioned.

**quality/PROTOCOL.md**: add the `--allow-downgrade` mechanics as a new bullet in the "New
runner/validator semantics" progressive-disclosure index (cross-referencing this phase, P123), and
note the flock behavior alongside the existing GRADE/PERSIST split description in "Step 6 — Run the
verifier".

**.planning/REQUIREMENTS.md**:
- Flip the 6 checkboxes for DRAIN-01, DRAIN-03, DRAIN-04, DRAIN-05, DRAIN-06, DRAIN-10 (currently
  `- [ ] **DRAIN-0N**...`, ~L162-238) to `- [x]`.
- Update the coverage table (~L303-330): change the `Status` column for the DRAIN-01/03/04/05/06/10
  rows from `Pending` to `Complete` (mirroring the DRAIN-07/08/09 rows, already `Complete` since
  Phase 122).

NOTICED (do not fix in this task — out of Phase 123's scope, file only): `.planning/REQUIREMENTS.md`'s
coverage table shows `Pending` for several ALREADY-CLOSED phases (e.g. UX-02/Phase 121, several
Phase 114-120 rows) — this table has not been kept current except for Phase 122's own DRAIN-07/08/09
flip. File a LOW hygiene GOOD-TO-HAVE (next available number, GTH-V15-81) in GOOD-TO-HAVES.md noting
this broader staleness for a future drain phase. Do not expand this task's scope to fix every stale
row yourself.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*DRAIN-01\*\*\|\[x\] \*\*DRAIN-03\*\*\|\[x\] \*\*DRAIN-04\*\*\|\[x\] \*\*DRAIN-05\*\*\|\[x\] \*\*DRAIN-06\*\*\|\[x\] \*\*DRAIN-10\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>quality/CLAUDE.md + PROTOCOL.md document every new SC1-SC5 convention; REQUIREMENTS.md's 6
  DRAIN checkboxes + coverage-table rows flip to Complete; one new GOOD-TO-HAVE (GTH-V15-81) filed
  for the broader REQUIREMENTS.md staleness noticing.</done>
</task>

<task type="auto">
  <name>Task 3: STATE.md advance + pre-push suite + push + post-push cadence</name>
  <files>.planning/STATE.md</files>
  <action>
Update STATE.md frontmatter: `completed_phases: 9 -> 10`, `percent: 60 -> 67`, `last_updated` /
`last_activity` to now, noting "P123 CLOSED GREEN". Append a new
`Phase: **P123 (Quality-runner & catalog integrity hardening, v0.15.0 DRAIN-01/03/04/05/06/10) —
CLOSED GREEN <date>.**` paragraph to `## Current Position` mirroring the P122 entry's structure and
detail level exactly: cite the 5 SCs verified against reality, the catalog rows minted, cross-ref
the 4 SURPRISES-INTAKE resolutions + 3 GOOD-TO-HAVES resolutions + the GTH-V15-80/81 filings, note
"10/15 v0.15.0 'Floor' phases complete (P114-P123); next = P124". Update the `## Current Focus`
next-step pointer from `/gsd-plan-phase 123` to the P124-pointing equivalent, mirroring whatever
exact phrasing pattern the P122->P123 transition used (do not invent new phrasing).

Before pushing: run the FULL pre-push suite — `python3 quality/runners/run.py --cadence pre-push
--persist` (never hand-pick individual gates, per root CLAUDE.md § Docs-site + freshness gates and
§ GSD workflow push cadence). Confirm exit 0. This is where SC4's `verifier-script-exists` gate and
SC2's `--allow-downgrade` guard get their first real full-suite exercise together with every other
existing gate — if the downgrade guard trips on an UNRELATED pre-existing row during this run,
investigate whether it's a genuine regression (fix it) or an artifact of this phase's own
catalog-first rows being correctly graded PASS for the FIRST time (expected — a row with no
git-HEAD baseline is exempt from the downgrade check per 123-04's design, so this should not
actually trip; if it does, that itself is a regression in the exemption logic to fix before
proceeding).

Then: `git fetch origin && git rebase origin/main` (resolve any drift against the two prior close
waves' pushes), `git push origin main`. After the push lands:
`python3 quality/runners/run.py --cadence post-push --persist` (mints `code/ci-green-on-main`
against main's real latest CI run — this is also the FIRST live exercise of SC5a's new
required-workflow-list logic against production `gh` data on the actual new tip). Confirm PASS for
both `ci.yml` and `release-plz.yml`.

Do NOT dispatch the gsd-verifier subagent yourself — that is the orchestrator's Step 7
(`quality/PROTOCOL.md`), performed after this plan's tasks complete; `gsd-executor` has no `Task`
tool and cannot spawn it.
  </action>
  <verify>
    <automated>git log --oneline -1 origin/main</automated>
  </verify>
  <done>Pre-push suite green; push landed on origin/main; post-push `code/ci-green-on-main` mint
  confirms both required workflows green on the new tip; STATE.md reflects P123 CLOSED GREEN with
  completed_phases=10, percent=67.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan's intake edits -> future agents' trust in the ledger | SURPRISES-INTAKE/GOOD-TO-HAVES are read as ground truth by future planning/verification sessions; a RESOLVED status without a real cross-ref is itself a form of the "silent corruption" this phase exists to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-14 | Repudiation | Intake entries marked RESOLVED without a real cross-ref commit | mitigate | Task 1 requires citing actual `git log` SHAs collected at task-execution time, not invented — the same anti-fabrication discipline this phase's SC1-SC4 code fixes apply to the paper trail |
| T-123-15 | Tampering | Push landing without a genuinely green pre-push/post-push suite | mitigate | Task 3 explicitly runs the FULL pre-push suite (never hand-picked gates) and confirms post-push code/ci-green-on-main PASS before declaring done |
</threat_model>

<verification>
`grep -c "STATUS: RESOLVED" .planning/milestones/v0.15.0-phases/SURPRISES-INTAKE.md` shows the 4 new
resolutions. `git log --oneline -1 origin/main` shows the phase's push landed.
`python3 quality/runners/run.py --cadence post-push --persist` reports PASS for
`code/ci-green-on-main`.
</verification>

<success_criteria>
All intake hygiene done with real cross-refs; quality/CLAUDE.md + PROTOCOL.md document every new
convention; REQUIREMENTS.md + STATE.md + ROADMAP.md (already updated at planning time) all reflect
P123 CLOSED; the phase-close push landed and main's CI is green on the new tip.
</success_criteria>

<output>
After completion, create `.planning/phases/123-quality-runner-catalog-integrity-hardening/123-07-SUMMARY.md`
</output>
