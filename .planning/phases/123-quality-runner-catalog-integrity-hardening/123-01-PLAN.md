---
phase: 123-quality-runner-catalog-integrity-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quality/catalogs/freshness-invariants.json
  - quality/catalogs/code.json
  - quality/catalogs/agent-ux.json
autonomous: true
requirements: [DRAIN-01, DRAIN-03, DRAIN-04, DRAIN-05, DRAIN-06, DRAIN-10]

must_haves:
  truths:
    - "Every SC1-SC5 contract this phase will implement is visible in a committed catalog row BEFORE any implementation code lands (quality/PROTOCOL.md Step 3, catalog-first)"
    - "A verifier-subagent reading only the catalog (zero session context) can tell what GREEN looks like for each of the 5 success criteria"
  artifacts:
    - path: quality/catalogs/freshness-invariants.json
      provides: "4 new structure-dimension rows (SC1 env-sourcing, SC2 downgrade-refusal, SC3 persist-lock, SC4 verifier-script-exists), each NOT-VERIFIED with a named but not-yet-existing verifier script"
    - path: quality/catalogs/code.json
      provides: "code/ci-green-on-main row's expected.asserts rewritten to require a required-workflow list, not a hardcoded ci.yml"
    - path: quality/catalogs/agent-ux.json
      provides: "agent-ux/t4-conflict-rebase-ancestry-real-backend row's expected.asserts rewritten to require real captured stderr, not a hardcoded git-version fallback"
  key_links:
    - from: "quality/catalogs/freshness-invariants.json new rows"
      to: "quality/runners/run.py (future implementation, plans 02/04/05/06)"
      via: "row.verifier.script path naming the not-yet-written gate/test-wrapper file"
      pattern: "verifier.*script"
---

<objective>
Author the phase's GREEN-contract catalog rows BEFORE any implementation lands (quality/PROTOCOL.md
Step 3). This is Wave 1 — pure catalog authoring, no code, no scripts.

Purpose: force every SC1-SC5 contract to be specific and falsifiable up front, so later
implementation plans build against a fixed target and the phase-close verifier subagent grades
against rows that predate the implementation (not rows written to match whatever got built).

Output: 4 new structure-dimension rows (SC1 env-sourcing, SC2 downgrade-refusal, SC3 persist-lock,
SC4 verifier-script-exists) in `quality/catalogs/freshness-invariants.json`, all `NOT-VERIFIED`; 2
existing rows (`code/ci-green-on-main`, `agent-ux/t4-conflict-rebase-ancestry-real-backend`) with
their `expected.asserts` rewritten to the new post-P123 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@quality/PROTOCOL.md
@quality/catalogs/README.md

Template row to mirror (unittest-backed structure-dimension row, same shape SC1/SC2/SC3 will use):
@quality/catalogs/freshness-invariants.json (row id `structure/catalog-immutable-on-read`, ~L1141-1178)
@quality/gates/structure/catalog-immutable-on-read.sh

Template row for a cheap mechanical structure gate (SC4 will mirror this cadence/shape):
@quality/catalogs/freshness-invariants.json (row id `structure/file-size-limits`, ~L635-679)

Rows to UPDATE in place (read their current shape before editing):
@quality/catalogs/code.json (row id `code/ci-green-on-main`, ~L294-331)
@quality/catalogs/agent-ux.json (row id `agent-ux/t4-conflict-rebase-ancestry-real-backend`, ~L1740-1776)
</context>

<interfaces>
Unified row schema (quality/catalogs/README.md) — every new row needs: `id`, `dimension`, `kind`,
`comment`, `sources`, `command` (may be null), `expected.asserts`,
`verifier.{script,args,timeout_s,container}`, `artifact`, `status`, `last_verified` (null until
first grade), `minted_at` (RFC3339, write-once — REQUIRED for a row landing now), `freshness_ttl`
(null for mechanical rows), `blast_radius`, `owner_hint`, `waiver` (null),
`claim_vs_assertion_audit` (>=50 chars, REQUIRED — every row here is minted post-2026-05-08),
`cadences`. `_audit_field.validate_row` (every `load_catalog()` call, `run.py:121`) enforces
`claim_vs_assertion_audit` + `minted_at` at load time — a row missing either raises `SystemExit`
and breaks EVERY row in the file, not just yours.
</interfaces>

<execution_constraints>
## Binding constraints (embedded verbatim — apply to every task in this plan)

- ONE cargo invocation machine-wide; cargo is FOREGROUND-only, never backgrounded/detached (OOM
  risk — this VM OOM-crashed 3x). Prefer `-p <crate>`. (Minimal cargo here -- Python/shell phase.)
- Leaf test setup (`reposix init`/sim-seed/`git config`/`git commit` for fixtures) runs in a
  `/tmp` clone, `cd`-ing in the SAME Bash invocation -- NEVER the shared repo (PreToolUse
  `leaf-isolation-guard.sh` exit 2 enforces).
- Uncommitted = didn't happen; commit before ending any turn; mid-phase commits stay LOCAL until
  phase-close push. No `--no-verify`. Targeted staging (never `-A`/`.`). One tree-writer at a
  time. No tag push.
- Never hand-pick gates before a push -- run `python3 quality/runners/run.py --cadence pre-push`
  (FULL). Docs edits pass BOTH `docs-build/*` AND `structure/banned-words`. Doc-alignment
  rebind-in-same-commit for any line carrying a doc-alignment binding.

## OD-3 ownership charter (embedded verbatim — carry this into every task)

1. You own what you touch. Acceptance criteria are the floor, not the ceiling.
2. Noticing is a deliverable -- report lying doc claims, tests that don't assert what their names
   promise, teaching-free errors, dead code, missing edge cases. An empty noticing section from
   code-touching work is a red flag.
3. Eager-fix or file -- never silently skip. <1h + no new dependency -> fix in place; else ->
   SURPRISES-INTAKE / GOOD-TO-HAVES with severity + sketch.
4. Verify against reality -- run it, render it, hit the backend. A claim without an artifact is
   not done.
5. North star -- Rust-compiler-grade UX: every user-facing error teaches the fix, suggests the
   alternative, gives a copy-paste recovery command. Would a skeptical first-time dev come away
   impressed?
</execution_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 new structure-dimension catalog rows (SC1-SC4 contracts)</name>
  <files>quality/catalogs/freshness-invariants.json</files>
  <action>
Append 4 new rows to the `rows` array (dimension `"structure"`). Hand-author directly in JSON —
the accepted catalog-first convention for non-doc-alignment dimensions (see `_provenance_note`
fields already present on hand-edited agent-ux rows). `minted_at` = current UTC RFC3339 for all 4.
Each `claim_vs_assertion_audit` >=50 chars, explaining how the verifier's asserts would falsify the
claim if false. Mirror the unittest-backed shape of `structure/catalog-immutable-on-read` (Layer-A
hermetic proof) for rows 1-3, and the cheap-mechanical shape of `structure/file-size-limits` for
row 4.

**Row 1 — `structure/quality-runner-sources-dotenv`** (SC1 / DRAIN-03):
- comment: closes the false-green-preflight / silent-skip gap (2026-07-14 20:43 HIGH
  SURPRISES-INTAKE entry) — `run.py` must conditionally self-source `./.env` (only if present, only
  for vars not already set) so `pre-release-real-backend` exercises real creds instead of silently
  skipping every row to NOT-VERIFIED.
- expected.asserts: ["a `./.env` at repo root is parsed and its KEY=VALUE pairs exported into
  os.environ, but ONLY for keys not already present (explicit shell env always wins)", "no `.env`
  present is a silent no-op — no error, no CI behavior change", "no secret VALUE is ever printed to
  stdout/stderr — only which KEY names were loaded, if any"]
- verifier.script: `"quality/gates/structure/quality-runner-sources-dotenv.sh"` (not yet written —
  expected at catalog-first time)
- verifier.timeout_s: 30, container: null
- artifact: `"quality/reports/verifications/structure/quality-runner-sources-dotenv.json"`
- status: `"NOT-VERIFIED"`, last_verified: null, freshness_ttl: null, blast_radius: `"P1"`
- owner_hint: point at `scripts/preflight-real-backends.sh` as the sibling convention to mirror.
- cadences: `["pre-push", "pre-pr"]`

**Row 2 — `structure/persist-refuses-downgrade`** (SC2 / DRAIN-04):
- comment: closes the silent-catalog-corruption gap (2026-07-14 20:44 HIGH entry — a real
  near-miss: a run downgraded `vision-litmus` PASS→FAIL, caught only by manual `git restore` before
  commit) — `--persist` must refuse to write a row from a committed-GREEN status (per
  `git show HEAD:<catalog>`, meaning `PASS`/`WAIVED`) to worse without `--allow-downgrade`.
- expected.asserts: ["a row whose committed status is PASS or WAIVED, and whose freshly graded
  status is anything else, is NOT persisted unless `--allow-downgrade` is passed", "the refusal
  prints the row id, old status, new status, and the literal recovery command `python3
  quality/runners/run.py --cadence <c> --persist --allow-downgrade`", "a row absent from the
  git-HEAD committed catalog (brand-new) is exempt and mints freely", "`--allow-downgrade` restores
  the prior unconditional-persist behavior, still printing a loud per-row notice"]
- verifier.script: `"quality/gates/structure/persist-refuses-downgrade.sh"`
- verifier.timeout_s: 30, artifact:
  `"quality/reports/verifications/structure/persist-refuses-downgrade.json"`
- status: NOT-VERIFIED, blast_radius: `"P0"` (exact silent-corruption class that nearly shipped)
- cadences: `["pre-push", "pre-pr"]`

**Row 3 — `structure/persist-catalog-write-locked`** (SC3 / DRAIN-05, cites GTH-V15-01):
- comment: two concurrent `--persist` runners must not interleave writes to the same catalog file —
  an advisory OS-level `flock` wraps the whole check+write critical section.
- expected.asserts: ["a second `--persist` invocation targeting the same catalog while a first
  holds the lock BLOCKS until release, rather than interleaving writes", "the lock is scoped to
  `--persist` runs only — a validate-only run never contends for it", "single-writer `--persist`
  behavior (mint / no-op / RED, from the existing TestPersistGate suite) is unchanged"]
- verifier.script: `"quality/gates/structure/persist-catalog-write-locked.sh"`
- verifier.timeout_s: 30, artifact:
  `"quality/reports/verifications/structure/persist-catalog-write-locked.json"`
- status: NOT-VERIFIED, blast_radius: `"P1"`
- cadences: `["pre-push", "pre-pr"]`

**Row 4 — `structure/verifier-script-exists`** (SC4 / DRAIN-06, cites GTH-V15-03):
- comment: a row backed by a missing or non-executable `verifier.script` is a framework-integrity
  hole — a false PASS could ride on a verifier that can't run. Scans every catalog row (excluding
  `*-allowlist.json` and the `docs-alignment` dimension, which has no per-row `verifier.script`
  field) and asserts every declared script exists AND is executable.
- expected.asserts: ["every row with a non-null verifier.script in every quality/catalogs/*.json
  (excluding *-allowlist.json and docs-alignment) resolves to a file that exists on disk", "every
  resolved verifier.script carries the executable bit (os.access(path, os.X_OK))", "a row whose
  verifier.script is null/absent (outside docs-alignment) is ALSO a violation", "a violation prints
  the offending catalog + row id + path + a concrete fix (chmod +x, or author/correct the path)"]
- verifier.script: `"quality/gates/structure/verifier-script-exists.sh"`
- verifier.timeout_s: 30, artifact:
  `"quality/reports/verifications/structure/verifier-script-exists.json"`
- status: NOT-VERIFIED, blast_radius: `"P1"`
- cadences: `["pre-commit", "pre-push", "pre-pr"]` (mirrors `structure/file-size-limits` — cheap,
  no subprocess spawn, pre-commit-budget friendly)

For every row, `sources` should cite this phase's own catalog-first plan (this file) plus the
SURPRISES-INTAKE / GOOD-TO-HAVES entry it closes, so a future reader can trace provenance.
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0,'quality/runners'); import run; [run.load_catalog(p) for p in run.discover_catalogs()]; print('LOADS_OK')"</automated>
  </verify>
  <done>4 new NOT-VERIFIED rows exist in freshness-invariants.json, each with minted_at + a
  claim_vs_assertion_audit >=50 chars; run.discover_catalogs()/load_catalog() loads every catalog
  file without raising SystemExit.</done>
</task>

<task type="auto">
  <name>Task 2: Update code.json + agent-ux.json contracts for SC5a/SC5b</name>
  <files>quality/catalogs/code.json, quality/catalogs/agent-ux.json</files>
  <action>
Update TWO existing rows in place. Append to each row's `comment` (append-only — do NOT delete
existing comment history, this codebase's established convention). Rewrite `expected.asserts`. Do
NOT hand-type a new `status` — only `run.py`'s own `--persist` mechanism may write `status`.

**1. `code/ci-green-on-main`** (`quality/catalogs/code.json`, ~L294-331): append to `comment` that
P123/SC5a extends this row from a single hardcoded `WORKFLOW=ci.yml` to a required-workflow LIST
`[ci.yml, release-plz.yml]`. Both trigger unconditionally on `push: branches: [main]` with no path
filter (confirmed by reading both workflow files); `audit.yml` is excluded (path-filtered to
Cargo.toml/lock, doesn't fire on every push); `docs.yml`/`quality-post-release.yml` are excluded
(trigger via `workflow_run`, not `push`).

Rewrite `expected.asserts` to: ["the LATEST run of EVERY workflow in `[ci.yml, release-plz.yml]`
concluded success — not merely ci.yml", "gh missing/unauth OR ANY required workflow's latest run
in-progress -> overall NOT-VERIFIED (exit 75), naming WHICH workflow was unknowable", "ANY required
workflow's latest run concluded non-success -> overall FAIL (exit 1), naming WHICH workflow and its
conclusion", "audit.yml/docs.yml/quality-post-release.yml are deliberately NOT watched (conditional
or workflow_run-triggered, not 1:1 with the just-landed push)"].

Add to `claim_vs_assertion_audit`: cite the empirical evidence — `gh run list
--workflow=release-plz.yml --limit 8` returned 8/8 recent main runs concluded `success` (checked
2026-07-18), answering GTH-V15-07's two open questions (release-plz DOES fire on every push,
identical trigger shape to ci.yml; a no-op run concludes `success`, never `skipped`).

Bump `verifier.timeout_s` 60->90 (two sequential `gh run list` calls). Flip `status` to
`"NOT-VERIFIED"` — plan 123-03 re-mints it. Do not leave a stale PASS describing the obsolete
single-workflow contract.

**2. `agent-ux/t4-conflict-rebase-ancestry-real-backend`** (`quality/catalogs/agent-ux.json`,
~L1740-1776): append to `comment` that P123/SC5b fixes
`quality/gates/agent-ux/lib/t4-real-backend-flow.sh`'s two `git checkout -B main ... ||
hard_fail_exit "..." "requires git >= 2.34 stateless-connect fetch"` call sites (lines 47-50) — the
fallback detail is ALWAYS misleading when reached, because the caller
(`t4-conflict-rebase-ancestry-real-backend.sh`, ~L179-193) already confirmed git >= 2.34 upstream;
a failure here is never a version problem, most commonly the Confluence list-vs-get oid-drift class
fixed for page 7766017 in Phase 114
(`.planning/phases/114-t4-confluence-oid-drift-fix-first-reconcile-audit/114-VERIFICATION.md`), not
proven universal (114's OQ1 residual: a pre-ADF page would still drift). Cites the 2026-07-14 20:41
MEDIUM entry.

Rewrite `expected.asserts` to add: "a `git checkout -B main` failure surfaces the REAL captured git
stderr as the `hard_fail_exit` detail — never the hardcoded git-version fallback — since the
version precondition is already gated earlier in the same script".

Do NOT change `status` — this row is `pre-release-real-backend` (OD-2 env-gated; no real
TokenWorld creds available this session). Leave `status` as-is for the next real cadence run.

Validate both files remain syntactically valid JSON and still load cleanly.
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0,'quality/runners'); import run; [run.load_catalog(p) for p in run.discover_catalogs()]; print('LOADS_OK')"</automated>
  </verify>
  <done>code/ci-green-on-main's expected.asserts describe the required-workflow-list contract and
  status is NOT-VERIFIED; agent-ux/t4-conflict-rebase-ancestry-real-backend's comment documents
  the coming SC5b fix without touching its env-gated status; both catalogs load cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Catalog JSON -> verifier subagent | An unbiased phase-close verifier reads these rows with zero session context; a vague/wrong `expected.asserts` here cannot be corrected later without re-opening catalog-first discipline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | Hand-authored NOT-VERIFIED row could be mis-typed to accidentally carry `status: PASS` | mitigate | Task action explicitly forbids hand-typing status; the load-check verify step catches JSON-level schema violations, not semantic status errors — reviewer must eyeball status fields before commit |
| T-123-02 | Repudiation | Extending an existing row's contract without preserving history | mitigate | append-only `comment` convention (already established in this codebase) preserved in both Task 2 edits |
</threat_model>

<verification>
Both catalog files (plus the untouched third) load without `SystemExit` via
`run.discover_catalogs()` + `run.load_catalog()`. `git diff --stat` shows only the 3 declared
`files_modified`. No `.sh`/`.py` files created in this plan (pure data authoring).
</verification>

<success_criteria>
4 new structure rows exist, NOT-VERIFIED, schema-valid, each pointing at a not-yet-existing
verifier script. 2 existing rows' contracts describe the post-P123 behavior. Every requirement ID
this phase addresses (DRAIN-01/03/04/05/06/10) has a traceable row.
</success_criteria>

<output>
After completion, create `.planning/phases/123-quality-runner-catalog-integrity-hardening/123-01-SUMMARY.md`
</output>
