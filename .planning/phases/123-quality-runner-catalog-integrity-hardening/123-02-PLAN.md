---
phase: 123-quality-runner-catalog-integrity-hardening
plan: 02
type: execute
wave: 2
depends_on: [123-01]
files_modified:
  - quality/runners/_env_load.py
  - quality/runners/run.py
  - quality/runners/test_run.py
  - quality/gates/structure/quality-runner-sources-dotenv.sh
  - .planning/CLAUDE.md
  - docs/reference/testing-targets.md
autonomous: true
requirements: [DRAIN-03]

must_haves:
  truths:
    - "Running `python3 quality/runners/run.py --cadence pre-release-real-backend` with real-backend creds ONLY in a repo-root `.env` file (not pre-sourced into the shell) exercises the real backends instead of silently skipping every row to NOT-VERIFIED"
    - "A shell that already exports the real creds is unaffected — `.env` never overrides an explicitly-set env var"
    - "No `.env` file present (the normal CI case) is a silent no-op"
  artifacts:
    - path: quality/runners/_env_load.py
      provides: "load_dotenv_if_present(repo_root) -> None; sets os.environ for unset keys only, never logs values"
    - path: quality/gates/structure/quality-runner-sources-dotenv.sh
      provides: "verifier backing catalog row structure/quality-runner-sources-dotenv"
  key_links:
    - from: "quality/runners/run.py:main()"
      to: "quality/runners/_env_load.py:load_dotenv_if_present"
      via: "one import + one call as the FIRST line inside main(), before any catalog/real-backend gating runs"
      pattern: "_env_load\\.load_dotenv_if_present"
---

<objective>
Close DRAIN-03 / SC1: `quality/runners/run.py` conditionally self-sources `./.env` so a
`pre-release-real-backend` cadence invocation works correctly even when the operator's shell
doesn't already have real-backend creds exported. Note: this plan's wave (2) is higher than its
`depends_on` alone would suggest — it shares `quality/catalogs/freshness-invariants.json` with
plans 04/05/06, which forces those THREE plans (not this one) to sequential later waves; this plan
itself only depends on 123-01's catalog-first row existing.

Purpose: close the false-green-preflight / silent-skip gap named in the 2026-07-14 20:43 HIGH
SURPRISES-INTAKE entry — `scripts/preflight-real-backends.sh` already sources `.env`, but `run.py`
did not, so an operator could see preflight report "backends reachable" while the actual cadence
run silently skipped every row to NOT-VERIFIED.

Output: `quality/runners/_env_load.py` (new sibling module, mirrors the anti-bloat pattern already
established by `_freshness.py`/`_realbackend.py`/`_audit_field.py`/`_shell_verdict.py` — keeps
`run.py`'s own line count from growing further past its documented ceiling), wired into `run.py`,
backed by a unit test, and the catalog row minted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@quality/PROTOCOL.md

Sibling convention to mirror (same `.env`-conditional-source semantics, already proven working):
@scripts/preflight-real-backends.sh (lines ~13-30: "Source .env if present and creds not already exported")

Existing test-file conventions to extend (do NOT create a new test file — add a TestCase class
here, matching the established `TestPersistGate` colocation pattern):
@quality/runners/test_run.py

`run.py`'s existing sibling-module import pattern (mirror this exact style):
@quality/runners/run.py (lines 44-51: `from _freshness import ...`, `import _realbackend`, etc.)
</context>

<interfaces>
`run.py`'s `main()` entry point (the ONLY call site this task adds a line to):

```python
def main(argv: list[str] | None = None) -> int:
    args = _build_arg_parser().parse_args(argv)
    mode = (...)
    print(f"quality/runners/run.py --cadence {args.cadence}  [{mode}]")
    now = datetime.now(timezone.utc)
    ...
```

Add the `load_dotenv_if_present(REPO_ROOT)` call as the FIRST statement inside `main()`, before
`args = _build_arg_parser().parse_args(argv)` is even reached is not required — after arg-parsing
is fine too, but it MUST run before the per-cadence loop that calls `run_row()` (which is where
`_realbackend.is_skipped(row, os.environ)` consults `os.environ`).
</interfaces>

<execution_constraints>
## Binding constraints (embedded verbatim — apply to every task in this plan)

- ONE cargo invocation machine-wide; cargo is FOREGROUND-only, never backgrounded/detached (OOM
  risk — this VM OOM-crashed 3x). Prefer `-p <crate>`. (Minimal cargo here -- Python/shell phase.)
- Leaf test setup (`reposix init`/sim-seed/`git config`/`git commit` for fixtures) runs in a
  `/tmp` clone, `cd`-ing in the SAME Bash invocation -- NEVER the shared repo (PreToolUse
  `leaf-isolation-guard.sh` exit 2 enforces).
- Uncommitted = didn't happen; commit before ending any turn; mid-phase commits stay LOCAL until
  phase-close push. No `--no-verify`. Targeted staging (never `-A`/`.`). One tree-writer at a
  time. No tag push.
- Never hand-pick gates before a push -- run `python3 quality/runners/run.py --cadence pre-push`
  (FULL). Docs edits pass BOTH `docs-build/*` AND `structure/banned-words`. Doc-alignment
  rebind-in-same-commit for any line carrying a doc-alignment binding.

## OD-3 ownership charter (embedded verbatim — carry this into every task)

1. You own what you touch. Acceptance criteria are the floor, not the ceiling.
2. Noticing is a deliverable -- report lying doc claims, tests that don't assert what their names
   promise, teaching-free errors, dead code, missing edge cases. An empty noticing section from
   code-touching work is a red flag.
3. Eager-fix or file -- never silently skip. <1h + no new dependency -> fix in place; else ->
   SURPRISES-INTAKE / GOOD-TO-HAVES with severity + sketch.
4. Verify against reality -- run it, render it, hit the backend. A claim without an artifact is
   not done.
5. North star -- Rust-compiler-grade UX: every user-facing error teaches the fix, suggests the
   alternative, gives a copy-paste recovery command. Would a skeptical first-time dev come away
   impressed?
</execution_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `_env_load.py`, wire into run.py, unit test, mint the row</name>
  <files>quality/runners/_env_load.py, quality/runners/run.py, quality/runners/test_run.py, quality/gates/structure/quality-runner-sources-dotenv.sh</files>
  <behavior>
    - Test 1: a temp dir with `.env` containing `FOO=bar\n# comment\n\nBAZ=qux\n` and an EMPTY
      `os.environ` -> after `load_dotenv_if_present(tmp)`, `os.environ["FOO"] == "bar"` and
      `os.environ["BAZ"] == "qux"`.
    - Test 2: same `.env`, but `os.environ["FOO"]` already set to `"keep-me"` before the call ->
      after the call, `os.environ["FOO"]` is STILL `"keep-me"` (explicit shell env wins; `.env`
      never overrides an already-set var).
    - Test 3: no `.env` file present in the temp dir -> the call is a silent no-op (no exception,
      `os.environ` unchanged).
    - Test 4: capture stderr during a call that DOES load vars -> the captured text names the
      loaded KEY names but contains NEITHER the string `"bar"` nor `"qux"` (no secret values
      leaked to any log/stream).
  </behavior>
  <action>
Create `quality/runners/_env_load.py` exporting `load_dotenv_if_present(repo_root: Path) -> None`:
if `(repo_root / ".env").exists()`, read it line by line; skip blank lines and lines starting with
`#`; split each remaining line on the first `=`; strip whitespace and optional surrounding quotes
from the value; call `os.environ.setdefault(key, value)` for each (never `os.environ[key] = value`
— an already-exported value must always win). Malformed lines (no `=`) are skipped, not fatal.
After loading, if any keys were newly set, print ONE line to stderr:
`run.py: sourced N var(s) from ./.env: KEY1, KEY2, ...` — key NAMES only, never values (mirrors
this codebase's established `kind: shell-subprocess` transcript convention of `env_keys: NAMES
only — no =value pairs`, per quality/CLAUDE.md).

Wire into `quality/runners/run.py`: add `from _env_load import load_dotenv_if_present` to the
existing sibling-import block (alongside `_freshness`/`_realbackend`/`_audit_field`/
`_shell_verdict`), and call `load_dotenv_if_present(REPO_ROOT)` as the first statement inside
`main()`. This runs for every cadence unconditionally — it's a harmless no-op for cadences that
never consult real-backend creds.

Add a `TestEnvSelfSourcing(unittest.TestCase)` class to `quality/runners/test_run.py` implementing
the 4 behavior cases above. Use `tempfile.TemporaryDirectory()` + `mock.patch.dict(os.environ, {},
clear=True)` (or a scoped subset), mirroring the existing `_drive()` helper's isolation style. This
is a pure-Python temp-dir test — not a git/reposix fixture — so the leaf-isolation guard does not
apply, but still root all fixture paths under `tempfile`, never write outside the temp dir.

Write `quality/gates/structure/quality-runner-sources-dotenv.sh` mirroring
`quality/gates/structure/catalog-immutable-on-read.sh`'s "Layer A hermetic unit proof" shape: run
`python3 -m unittest quality.runners.test_run.TestEnvSelfSourcing -v`, capture output, write the
standard JSON artifact (`ts`, `row_id`, `exit_code`, `asserts_passed`, `asserts_failed`), exit 0/1
accordingly. `chmod +x` it.

Run the new unittest, confirm 4/4 pass, then mint the row:
`python3 quality/runners/run.py --cadence pre-push --persist` (the row is tagged `pre-push`, so a
`--persist` pre-push run will grade + mint it to PASS — this is also the first real exercise of
the new sibling-module wiring against a live `run.py` invocation).
  </action>
  <verify>
    <automated>python3 -m unittest quality.runners.test_run.TestEnvSelfSourcing -v</automated>
  </verify>
  <done>4/4 new unittest cases pass; `bash quality/gates/structure/quality-runner-sources-dotenv.sh`
  exits 0; catalog row `structure/quality-runner-sources-dotenv` reads `status: PASS` after the
  mint run.</done>
</task>

<task type="auto">
  <name>Task 2: Fix-twice — correct the pre-release-real-backend invocation docs</name>
  <files>.planning/CLAUDE.md, docs/reference/testing-targets.md</files>
  <action>
Per DRAIN-03's explicit fix-twice clause: update every `pre-release-real-backend` invocation
reference in `.planning/CLAUDE.md` (the "Milestone-close 9th probe is non-skippable" bullet + any
other `run.py --cadence pre-release-real-backend` mention) and `docs/reference/testing-targets.md`
to note that `run.py` now self-sources `./.env` when present (crediting
`quality/runners/_env_load.py`) — manually prefixing `set -a; . ./.env; set +a` before the
invocation is no longer required (though harmless if still done, since `.env` values would simply
already be exported and `os.environ.setdefault` is idempotent).

Do NOT remove or modify `scripts/preflight-real-backends.sh`'s own `.env` sourcing — that script is
an independent pre-flight reachability check and remains useful on its own.
  </action>
  <verify>
    <automated>grep -rli "self-sourc" .planning/CLAUDE.md docs/reference/testing-targets.md | wc -l | grep -q "^2$" && echo FIX_TWICE_OK</automated>
  </verify>
  <done>Both docs reflect the corrected invocation; the next agent does not manually re-source
  `.env` under a now-false belief it's still required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `.env` file -> process environment | A repo-root `.env` (gitignored, operator-authored) is read into `os.environ`; downstream code may pass those values to real external backends (GitHub/Confluence/JIRA) per OP-1's egress allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-03 | Information Disclosure | `_env_load.py` stderr logging | mitigate | log KEY names only, never values — proven by Test 4 (asserts neither secret value string appears in captured output) |
| T-123-04 | Tampering | A malicious `.env` in a compromised working tree could inject unexpected env vars | accept | `.env` is gitignored and locally operator-authored — same trust level as the shell environment itself, not a new trust boundary; `.env`'s existing use in `scripts/preflight-real-backends.sh` already established this trust level |
</threat_model>

<verification>
`python3 -m unittest quality.runners.test_run -v` (full suite) still green — no regression to the
pre-existing `TestPersistGate` cases. `bash quality/gates/structure/quality-runner-sources-dotenv.sh`
exits 0 against the real repo.
</verification>

<success_criteria>
`run.py` self-sources `./.env` conditionally and safely; the new unit tests + gate prove it;
catalog row `structure/quality-runner-sources-dotenv` is PASS; both doc references are corrected in
the same commit sequence (fix-twice).
</success_criteria>

<output>
After completion, create `.planning/phases/123-quality-runner-catalog-integrity-hardening/123-02-SUMMARY.md`
</output>
