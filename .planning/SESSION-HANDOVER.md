# SESSION-HANDOVER.md — v0.14.0 tag: item 5 APPROVED-RESTORE (execute); item 7 RESOLVED-DEFER; recount DONE — 2026-07-13 (→ successor #8)

For the incoming top-level workhorse (L0) — a top-level ROUTING coordinator: routes via GSD + subagents, never leaf-works. Map, not territory — detail lives in git + linked files. HEAD = live state only; delete closed/superseded entries rather than appending. The outer-loop MANAGER (herdr pane w1:p7) watches this pane and relays owner decisions; `.planning/MANAGER-HANDOVER.md` is the live owner-directive channel. Resume an agent via SendMessage, never fork (ORCHESTRATION.md §11).

## 0. State (verify: `git rev-parse --short HEAD`, `git status --porcelain`, `gh run list --branch main --limit 8 --json headSha,status,conclusion,workflowName`)
- HEAD = origin/main ≈ `c46ae55` (+ this handover commit), tree clean — **verify live with `git rev-parse --short HEAD`.** Main not red at pause (CI was in_progress on `f7be1d7`, CodeQL green; re-verify newest `ci.yml` before opening new work).
- Real-backend creds: all env vars UNSET in the interactive shell, but **`.env` IS present** (repo root, 3582 B) and the runner sources it — the `pre-release-real-backend` 9th probe IS runnable this session. Do NOT read `.env` (secrets).
- TokenWorld backend currently = **EXACTLY 2 durable pages** (`7766017` parent + `7798785` child). Verify: `python3 scripts/confluence_tokenworld.py list`.
- Commits (successor #7): `73f374b` (item-5 DROP recorded) · `466b6a6` (intake-honesty recount) · `a1b06c3` (item-5 HALT+re-escalation) · `c46ae55` (item-5 closed APPROVED-RESTORE).

## 1. ACTIVE CHARTER — items 5, 7, 8 → STOP at READY-TO-TAG (tag push is the MANAGER's, never yours)

### Item 5 — APPROVED-RESTORE (Option 1), manager standing authority — EXECUTE (THE TAG CRITICAL PATH).
Full ruling + code evidence: `.planning/CONSULT-DECISIONS.md` § "RESOLVED 2026-07-13 — APPROVED-RESTORE" (commit `c46ae55`). The manager code-verified the finding and WITHDREW the DROP-only/exactly-2-pages guardrail (false premise). **DROP is dead; RESTORE the sacrificial editable page.** Execute the lane with these BINDING guardrails:
1. **ONLY backend mutation = untrash `2818063` via the NAMED tool:** `python3 scripts/confluence_tokenworld.py restore` (status=current). NEVER ad-hoc curl. The protected pair `7766017`/`7798785` stays untouched (the script refuses to delete them).
2. **Verify before+after** with `python3 scripts/confluence_tokenworld.py list` — protected pair intact, end state = **EXACTLY 3 current pages** (`7766017`,`7798785`,`2818063`).
3. **NO mirror surgery** — the mirror record `pages/2818063.md` STAYS (that is the point of RESTORE; GUARD A then reconciles `matched=3/backend_deleted=0`).
4. **Re-run the litmus on the UNMODIFIED Pattern-C harness** (`quality/gates/agent-ux/dark-factory/dvcs-third-arm.sh`, or via the 9th probe `run.py --cadence pre-release-real-backend --persist`). If the final push hits the v1-vs-v7 version mismatch and the ONE documented rebase recovery (`litmus-flow.sh:105`, `git pull --rebase reposix main`) does NOT self-heal → **surface it RED honestly** (real coherence bug; fix-first applies — do NOT paper over).
5. **Fix-it-twice doc-truth (eager-fix within this lane):** correct fixture shape = 2 protected pages never deleted + 1 sacrificial editable (`2818063`, current or trashed between runs). Fix `docs/reference/testing-targets.md` (and any other doc asserting "exactly 2 pages"). The §2 constraint line below is already corrected. Manager fixes `MANAGER-HANDOVER.md`.
6. **File the broken mirror-sync Action** (run `25223195636`, `cargo binstall reposix-cli` P84 gap) to SURPRISES-INTAKE if not already — it stays broken under RESTORE (mirror refreshes only via a bus push).

Real-backend creds: env vars unset in the shell but **`.env` is present** (runner sources it) — the probe IS runnable. Single-writer: the RESTORE lane does a backend mutation + a litmus run + likely an evidence commit — run it as the SOLE tree-writer.

### Item 7 — RESOLVED: DEFER to v0.15.0 (recommendation B accepted, 2026-07-13). No product code change.
The p93 "Real-Confluence partial-failure RECOVERY does not converge" HIGH is the **architectural RBF-LR-03** slug→id gap (owner-signed WAIVED, ADR-010 §3 / `docs/decisions/010-*.md:238-253`; documented known limitation `docs/concepts/dvcs-topology.md:206`) → hits the fix-first "turns architectural → STOP" carve-out.
- **The honest harness ALREADY exists + is GREEN:** commit `1c424d7` rewrote p93 (`crates/*/tests/agent_flow_real.rs:763`) from a lying CREATE-recovery claim to a genuine UPDATE-recovery proof; ran exit-0 on live TokenWorld (2026-07-13T15:28:36Z). NOT a hollow green (CREATE-recovery was never a documented promise).
- **Item-8 TODOs from this:** (a) **[persist gap]** the p93 catalog row is `NOT-VERIFIED / last_verified:null` — the GREEN was never `--persist`ed; the 9th probe MUST run creds+`--persist` or the P0 row reads NOT-VERIFIED at tag time. (b) **[stale doc]** re-STATUS `part-03.md:59-61` (reads "active p93 blocker" — overtaken by `1c424d7`). (c) File an explicit v0.15.0 GTH for the slug→id create-reconciliation (only "directional inspiration" exists, no scheduled phase). (d) LOW: 2 pre-rewrite "p93 smoke A" orphans may still need a human title-sweep in TokenWorld.
- **PROMINENT flag for the READY-TO-TAG report (verbatim-ready):** "p93 is GREEN as an UPDATE-recovery proof against live TokenWorld (`1c424d7`, exit 0). It does NOT cover CREATE-recovery: a partial-fail whose landed action was a create against an id-assigning backend genuinely does not converge (retry re-creates/duplicates). This is the owner-signed WAIVED known limitation of ADR-010 §3 / RBF-LR-03, hand-recoverable, routed to v0.15.0. Tagging v0.14.0 ships no regression here — it ships a documented, pre-existing waiver."

### Item 8 — §4 mechanicals (GATED on item 5 GREEN). STOP at READY-TO-TAG.
Sequence once item 5 is GREEN: honest `pre-release-real-backend` probe exit 0 (creds+`--persist`; runs BOTH the litmus AND p93) → re-mint `quality/reports/verdicts/milestone-v0.14.0/VERDICT.md` GREEN → FRESH unbiased ratification subagent (`quality/PROTOCOL.md` § Verifier subagent / `quality/dispatch/milestone-close-verdict.md`) → author `.planning/milestones/v0.14.0-phases/tag-v0.14.0.sh` (pattern `.../v0.13.0-phases/tag-v0.13.0.sh`) → **STOP at READY-TO-TAG** (compact report: SHAs, artifact paths, key numbers) to the MANAGER (pane w1:p7).
- **KNOWN GATE RACE (HIGH, filed, NOT fixed):** `quality/gates/code/ci-green-on-main.sh` grades PASS off the newest `gh run list` run WITHOUT asserting `headSha` == pushed HEAD. Manually cross-check the graded run's `headSha` against `git rev-parse HEAD` before trusting the final tag decision.
- **OP-9 distillation TODO (BLOCKS ratification):** the intake now honestly shows **8 OPEN** items (all in `part-03.md`; recount `466b6a6`). Each must be triaged to RESOLVED / DEFERRED-to-v0.15.0-with-rationale, then distilled into `.planning/RETROSPECTIVE.md`, before ratification. Likely dispositions: `part-03:82` (B1 attach-recovery, HIGH) → verify RESOLVED-by-item-4a `eb824f3`; `part-03:210` (ci-green race, HIGH) → v0.15.0-defer + keep manual-crosscheck mitigation; `part-03:59` (p93) → re-STATUS per item 7; the rest (MED/LOW harness/process) → v0.15.0 GOOD-TO-HAVES.

## 2. Constraints (unchanged)
Sim-first for code; real backends only via `REPOSIX_ALLOWED_ORIGINS`; sanctioned targets ONLY — **TokenWorld fixture shape (CORRECTED by manager 2026-07-13): 2 PROTECTED pages never deleted (`7766017`+`7798785`) + 1 SACRIFICIAL EDITABLE page (`2818063`, current or trashed between runs) — the earlier "exactly 2 pages" doctrine was WRONG (the unmodified litmus needs the editable 3rd); verify `python3 scripts/confluence_tokenworld.py list`**; **NO tag push ever** (manager's); never open work over a red main; ONE cargo invocation machine-wide (prefer `-p`); /tmp leaf isolation (`cd` in the SAME bash call); single-writer (one tree-mutating agent at a time; read-only agents may parallelize). A `fork` is never a safe discard — end the turn instead. Relief ~100k soft / ~150k hard (absolute) → REPLACE this file, commit+push, end turn. Resume a child via SendMessage, never fork.

## 3. Ops lessons (carried) + hygiene debt
- **`ci-green-on-main` headSha race** — cross-check `headSha` manually until fixed (§1 item 8).
- **CONSULT-DECISIONS.md is 30531 chars (WARN > 20000):** accumulated CLOSED entries that doctrine says to DELETE (git is the archive). Prune superseded B1 restore/reconcile entries + implemented [SELF] D4/D5/D6 at the next clean moment.
- **Display-freeze false alarm** — a survey-modal can freeze a pane's display while the background coordinator keeps running; health-check via GROUND-TRUTH git, not the pane view.
- **A dispatched `fork` inherits full context and becomes a live parallel tree-writer** — never a safe no-op placeholder (ORCHESTRATION §11).

---
History lives in git — `git log` / `git show`, not restated here.
