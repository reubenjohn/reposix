# v0.15.0 Good-to-haves / carried-forward hardening — Part 10 of 10

> Split from `GOOD-TO-HAVES.md` for the file-size gate (OP-8 drain). Index: `../GOOD-TO-HAVES.md`. Entries preserved verbatim.

## From Phase 124 close-bookkeeping lane (STATE/ROADMAP advance + intake filing, 2026-07-18)

### GTH-V15-86 — shell-coverage aggregate headroom thinned to ~3.7pts after P124's 4 container harness scripts
- **Source:** NOTICED at P124 phase-close (close-bookkeeping lane) — the new container/harness scripts enter the kcov aggregate denominator at ~0% covered. · **Severity: LOW (health / maintenance — non-blocking today)** · STATUS: OPEN — tag quality / coverage.
- **What:** P124 added ~4 container-rehearse harness scripts (`container-congruence-earned.sh` + selftest, `container-rehearse-sigkill-safe.sh`, `container-rehearse-exit-from-artifact.sh`, `lib/sim-lifecycle.sh`, ~270 lines total) that run under docker/SIGKILL substrate the local kcov harness cannot instrument, so they land in the aggregate denominator at ~0% covered. Aggregate shell coverage is now **16.70%** against the **13%** floor — headroom **~3.7pts**, down from the pre-P124 margin. A few more uninstrumented harness scripts (or the GTH-V15-84 `lib/` extraction adding more sourced files) will breach the floor.
- **Fix-sketch:** add targeted shell tests for the container-rehearse harness — the `--selftest-*` docker-free modes are already host-runnable and kcov-instrumentable, so exercising them under kcov (or adding a small shell/bats test that drives the harvest + exit-from-artifact logic host-side) reclaims coverage without needing docker in CI. Do it BEFORE the floor is breached, else the floor either blocks or must be lowered.
- **Effort:** small-medium (~1h) — a few host-side selftest invocations wired into the kcov harness set.

### GTH-V15-87 — zsh `${PIPESTATUS[0]}` is 1-indexed — a pipeline-exit-code grounding gotcha that bit a P124 leaf
- **Source:** NOTICED at P124 phase-close — a P124 leaf mis-read a pipeline exit code once via `${PIPESTATUS[0]}` in this project's zsh. · **Severity: LOW (grounding / footgun — no shipped defect, but a repeatable trap)** · STATUS: OPEN — tag docs / grounding.
- **What:** in bash, `${PIPESTATUS[0]}` is the FIRST pipeline element's exit (0-indexed); in this project's zsh the equivalent array (`pipestatus`) is **1-indexed**, so `${PIPESTATUS[0]}` mis-reads (empty / wrong element) and a guard like `[ "${PIPESTATUS[0]}" -ne 0 ]` silently mis-fires. A P124 leaf hit this once. Correct zsh forms: `${pipestatus[1]}` for the first element, or avoid the array entirely and verify via a non-piped `$?`.
- **Fix-sketch:** add a one-line shell-convention note to a grounding doc (e.g. `crates/CLAUDE.md` build-discipline section or a `scripts/` conventions note) — "zsh `pipestatus` is 1-indexed; use `${pipestatus[1]}` or a non-piped `$?`, never `${PIPESTATUS[0]}` for the first pipe stage in this repo's zsh" — so future leaves don't re-trip it. Fix-twice candidate (document the gotcha where the next agent reads it).
- **Effort:** trivial (<15min) — one doc line.

### GTH-V15-88 — `container-rehearse-sigkill-safe.sh` CONTROL leg can degrade to NOT-VERIFIED on a loaded VM (flake window, never false-green)
- **Source:** NOTICED at P124 phase-close — the SC2 selftest's self-falsifiability CONTROL leg occasionally read NOT-VERIFIED on a loaded VM, then PASS on re-run. · **Severity: LOW-MED (flake — safe-by-construction, but a momentary stall risk)** · STATUS: OPEN — tag quality / flake.
- **What:** the `container-rehearse-sigkill-safe.sh` self-falsifiability CONTROL leg (which proves the check CAN fail, so a PASS is meaningful) can degrade to NOT-VERIFIED on a loaded VM when the sim has not yet bound port 7878 by the time the SIGKILL timer arms — then PASSes on a re-run. It is **never false-green** (the degradation is toward NOT-VERIFIED, not a spurious PASS), but a momentary NOT-VERIFIED on the control leg could stall a phase-close push that treats NOT-VERIFIED as blocking.
- **Fix-sketch:** harden the control leg to poll until the sim is actually port-bound (e.g. `ss`/`lsof` on 7878, or the sim's readiness line) BEFORE arming the SIGKILL timer, closing the race window so the leg is deterministic. Reuses the readiness-poll pattern the harness's own stale-orphan gate already has.
- **Effort:** small (~30min) — add a bounded readiness poll before the timer in the selftest.

## From gsd-quick roadmap-strip lane (docs/roadmap.md Progress-right-now strip + phase-close doctrine, 2026-07-18)

### GTH-V15-89 — machine-verify the `docs/roadmap.md` "Progress right now" strip against `STATE.md` (structure-dimension freshness gate)
- **Source:** FILED by the gsd-quick lane that added the `docs/roadmap.md` "Progress right now" strip + encoded the per-phase-close refresh into `.planning/CLAUDE.md` phase-close doctrine (R3 fix-twice). The prose doctrine is the enforcement floor; this is the machine backstop that would keep the strip from silently rotting again (the exact failure that left the OWNER staring at a milestone-frozen page while 11 phases closed). · **Severity: MEDIUM (health / freshness — non-blocking today; the prose doctrine + close-checklist item carry it until this lands)** · STATUS: OPEN — tag **structure** (`quality/gates/structure/`).
- **What:** the strip's four moving lines (milestone **name**, phases-closed **fraction + percent**, **capability line**, **last-updated date**) are refreshed by human/agent discipline at every phase close. Nothing mechanically catches a stale strip — if a close-bookkeeping lane forgets the refresh, the public roadmap silently drifts behind `STATE.md` again with no gate firing. The strip is deliberately **binding-free** (NOT a `doc-alignment.json` row — a binding on a line that changes every close re-drifts → `STALE_DOCS_DRIFT`, the P117 W3 cascade), so the check must live in the **structure** dimension's freshness invariants, not doc-alignment.
- **Fix-sketch:** add a `structure`-dimension freshness invariant (extend `quality/gates/structure/freshness-invariants.py`, or a sibling `quality/gates/structure/roadmap-strip-fresh.py`) that: (1) parses the strip's `**NN / MM**` fraction + `(**PP%**)` from `docs/roadmap.md`; (2) parses `completed_phases` / `total_phases` / `percent` from `.planning/STATE.md` frontmatter; (3) BLOCKS with a teaching error if they disagree ("roadmap strip shows 11/15 but STATE.md shows 12/15 — refresh the Progress-right-now strip in your close-bookkeeping commit"); (4) optionally warns (not blocks) if the strip's `_updated <date>_` is older than STATE.md's `last_updated` date. Mint the catalog row FIRST (catalog-first rule) under the `structure` dimension. Keep it pure-stdlib Python3 (no new dependency) so it rides the existing runner.
- **Effort:** small-medium (~1–1.5h) — mostly the two-file parse + the catalog row + a self-test proving it BLOCKS on a deliberately-stale strip. Deferred from the quick lane per charter #3 (>~1h / gate design) — prose doctrine + close-checklist item landed instead.

## From Phase 125 W1 plan 125-01 (SC3/DRAIN-12 mirror-drift hint fix, 2026-07-18)

### GTH-V15-90 — two `structure/file-size-limits` residual files grew further over ceiling in 125-01
- **Source:** NOTICED by the 125-01 executor — the plan mandated its regression test live in `crates/reposix-remote/tests/push_conflict.rs` (already 21481 chars, over the 20000 `*.rs`-outside-crates ceiling) and its doc note live in `docs/guides/troubleshooting.md` (already ~28k, over the 20000 `*.md` ceiling), so both grew further: push_conflict.rs → 28154 chars, troubleshooting.md → 28415 chars. Both are PRE-EXISTING over-budget residuals under the active `structure/file-size-limits` waiver (until 2026-08-08); 125-01 could not split them without violating the plan's explicit file targets. · **Severity: LOW (health — non-blocking; covered by the existing waiver + the Phase-123 file-size early-warning GTH)** · STATUS: OPEN — tag **structure** (`quality/gates/structure/file-size-limits`).
- **What:** feeds the existing `structure/file-size-limits` residual drain list (the waiver `reason` on the row). `push_conflict.rs` is a natural split candidate — the RPX-050x tag tests + the new mirror-lag test could move to a sibling `tests/push_conflict_mirror_lag.rs` (or `tests/helper_reject_hints.rs`) sharing the `common.rs` harness. `troubleshooting.md` wants progressive-disclosure into child pages (the DVCS push/pull section is a self-contained candidate).
- **Fix-sketch:** when the file-size waiver drains (before 2026-08-08), split `push_conflict.rs`'s reject-hint tests into a sibling integration-test file and factor the DVCS troubleshooting section into a linked child page. Neither is urgent while the waiver holds.
- **Effort:** small (~30–45min each) — mechanical test-file split + a docs child-page extraction.

## From Phase 125 close (SC2 self-heal test-coverage gap, gsd-verifier VERDICT #2, 2026-07-19)

### GTH-V15-91 — SC2 litmus self-heal (backend-drift preflight + mirror reconcile-before-marker-push ordering) has ZERO self-contained tests
- **Source:** NOTICED by the fresh gsd-verifier at P125 phase-close (`quality/reports/verdicts/p125/VERDICT.md` NOTICED #2) — no reference to `_litmus_mirror_reconcile` / `_litmus_fixture_preflight` exists outside `lib/litmus-*.sh`; the self-heal LOGIC is exercised only by the env-gated real-backend milestone-close vision-litmus, which itself runs only at tag-time. · **Severity: MEDIUM (quality / coverage — a reconcile-ordering regression escapes every pre-push/CI gate)** · STATUS: OPEN — tag **agent-ux** / quality (`quality/gates/agent-ux/`).
- **What:** the SC2/DRAIN-12 self-heal (delivered in plan 125-02) runs its two branches — `_litmus_fixture_preflight` (backend-drift: restore/reparent the protected TokenWorld ids) and `_litmus_mirror_reconcile` (mirror-drift: `git fetch reposix main` → `git rm -r … pages/` → `git checkout FETCH_HEAD -- pages/` → commit, ordered strictly BEFORE the marker edit/push at `litmus-flow.sh:85/106`) — ONLY inside the env-gated real-backend litmus. A regression in the reconcile ORDERING (marker edited before reconcile), the `git rm`+`checkout FETCH_HEAD` overlay, or the bus-remote fetch would slip past every pre-push/CI gate and surface only in a live TokenWorld run at tag-time — the worst place to first learn of it.
- **Fix-sketch:** add a local git-fixture harness (a fake `reposix` bus remote + a deliberately-drifted mirror clone, both throwaway `/tmp` repos per the leaf-isolation rule) that drives `_litmus_mirror_reconcile` / `_litmus_fixture_preflight` and asserts the overlay ordering (fetch → rm → checkout → marker) WITHOUT a real backend — the same way `refresh-tokenworld-mirror.sh`'s mechanism could be fixture-tested. Runnable in pre-push/CI (no creds, no network). Mint the catalog row FIRST (catalog-first rule) under the `agent-ux` dimension. Bonus: reclaims some `code/shell-coverage` on the `lib/litmus-*.sh` scripts (cross-ref GTH-V15-86 headroom).
- **Effort:** small-medium (~1.5–2h) — the fixture harness (fake bus remote + drifted mirror) is the bulk; the ordering assertions + catalog row + self-test are mechanical.

## From owner-directed session-analyzer expansion (global skill, top-level session 40b207f3, 2026-07-19)

### GTH-V15-92 — wire a session-retrospective step into milestone-close / pre-release ceremony (expanded session-analyzer U6)
- **Source:** FILED via `/gsd-capture` by the owner-directed expansion of the global `session-analyzer` skill (~/.home commits `ef21bca..19c7829`, shipped 2026-07-19) — owner intent (2026-07-18): retrospectives are part of the pre-release ceremony, to find frictions and pain points in agent sessions and capture tech-enabler candidates for the next milestone. · **Severity: MEDIUM (process / health — non-blocking)** · STATUS: OPEN — tag process / planning doctrine.
- **What:** at milestone close, run the expanded skill's U6 multi-session retrospective (`report.py` over the milestone's sessions in `~/.claude/projects/-home-reuben-workspace-reposix`, date-range/last-N selectors, parent-vs-subagent split, idle-gap / bash-hotspot / permission-friction / context-growth sections, optional self-contained HTML via `inject_report.py`) to surface efficiency/speed drags; file the resulting tech-enabler candidates into the NEXT milestone's intake. Nothing mechanically prompts this today — without a doctrine hook the retrospective silently doesn't happen.
- **Fix-sketch:** add the step to milestone-close doctrine (`.planning/PRACTICES.md` OP-9 distill-before-archive section + the close checklist), citing the skill's U1–U7 route table (U6 row); first run rehearses on v0.15.0's own sessions.
- **Effort:** small (~30–45min doctrine edit + one ceremony rehearsal).

## From /gsd-quick CI-infra fix (pre-pr timeout band-aid, 260719-*, 2026-07-19)

### GTH-V15-93 — improve CI rust-cache hit rate so `quality gates (pre-pr)` cold builds don't approach the timeout wall (the REAL fix; the 15→28min raise landed the SAME commit is a band-aid)
- **Source:** FILED by a `/gsd-quick` leaf dispatched by L0 to stop the recurring `quality gates (pre-pr)` job cancellation on `main` — 4 `cancelled` ci.yml runs in ~2 days initially misdiagnosed as queue contention (see `.planning/notes` / STATE relief log `#65→#66`). Forensic re-read of job timing (not just run `conclusion`) found the real driver. · **Severity: MEDIUM-HIGH (CI health — currently masked by a band-aid, will recur as the workspace grows)** · STATUS: OPEN — tag code / CI infra.
- **What — the misdiagnosis:** GitHub reports a `timeout-minutes` kill as `conclusion: cancelled` with NO job-level `timed_out` flag, and `ci.yml`'s `concurrency: { cancel-in-progress: true }` block (`ci.yml:16-18`) ALSO produces `cancelled` runs when a push supersedes an in-flight one — so a timeout-wall hit and a benign supersede-cancel are indistinguishable from `conclusion` alone. Breakdown of the 4 cancelled runs: **1/4 benign** — run `29677548830` / commit `495b8357`, `cancel-in-progress` auto-cancel (superseded by the next push; every job in that run died together, consistent with a group cancel, not a per-job timeout). **3/4 real timeout-wall** — the `quality gates (pre-pr)` job ran ALONE to 15m51s–20m00s while every sibling job in the same run finished in minutes; it was killed at the then-15-min cap.
- **Warm-vs-cold proof:** commit `394ebc3a`'s pre-pr job — attempt 1: 20:00 (killed, cold rust-cache miss). attempt 2: 20:00 (killed, cold rust-cache miss again). attempt 3: **3m39s success** (warm cache hit). Other confirmed cold hits: run `29672608656` / commit `6472f020` = 15m51s; run `29671165231` / commit `28a8646b` = 20:00. A later green baseline (run `29677846998` / commit `592ae4c0`, 2026-07-19) measured pre-pr at a warm 4m06s — consistent with the ~3-4m warm-cache floor. The `cargo build --workspace --bins` "Warm cargo build" step (`ci.yml` pre-pr job, step `Warm cargo build`) is the step that pays the cold-compile cost; every other pre-pr step (mkdocs install, jq/sqlite3 install, `quality/runners/run.py --cadence pre-pr`) is fast and cache-insensitive.
- **The band-aid landed in this same commit:** `timeout-minutes: 15` → `28` on the `quality gates (pre-pr)` job (headroom over the observed ~20m cold-cache max) + explicit `timeout-minutes` added to the 3 sibling jobs that previously inherited GitHub's silent 360-min default (`test`: 6min, observed 2m28s-2m45s; `coverage`: 6min, observed 2m34s-2m53s; `shell-coverage`: 3min, observed 1m07s-1m18s — all ~2x observed, bounded fast-fail). This buys headroom and stops silent-default sibling jobs from running unbounded, but does NOT fix the underlying cache-miss rate — a sufficiently large cold miss (workspace growth over time) can still approach 28min, and a genuinely-cold miss still burns ~20min of CI wall-clock and $ before it even matters.
- **Fix-sketch (the REAL fix):** improve the `Swatinem/rust-cache@v2` hit rate for the `quality gates (pre-pr)` job specifically. Candidates worth investigating in order of likely leverage: (1) confirm the pre-pr job's cache key isn't being invalidated more often than necessary (e.g. by `Cargo.lock` churn from unrelated crates, or a `save-if:` misconfiguration causing every run to write a fresh cache instead of reusing); (2) consider sharing/warming the cache from the `clippy` or `test` job (which run earlier / in parallel and already prime a workspace build) via `Swatinem/rust-cache@v2`'s `shared-key` option, so pre-pr's `cargo build --workspace --bins` can hit a cache another job already populated in the SAME run instead of needing its own prior-run cache; (3) check whether `CARGO_INCREMENTAL: 0` (set globally at `ci.yml:13` for reproducible warnings-as-errors builds) is fighting the cache's ability to reuse artifacts across runs — verify empirically before touching this since it may be load-bearing for `-D warnings` determinism; (4) once the hit rate is measurably improved, consider whether `timeout-minutes: 28` can be safely lowered back down.
- **Effort:** medium (~2-3h) — needs actual cache-hit-rate instrumentation (e.g. temporarily log `Swatinem/rust-cache`'s cache-hit output across a handful of runs) before picking a fix, plus verification that any change doesn't regress `-D warnings` determinism or other jobs' cache behavior.
