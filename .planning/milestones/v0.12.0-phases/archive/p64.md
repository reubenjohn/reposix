# P64 — Docs-alignment dimension framework + skill + runner integration (added 2026-04-28)

- **P64 SHIPPED (2026-04-28, v0.12.0):** Docs-alignment dimension framework + skill + runner integration. The 9th quality dimension goes from design-bundle to end-to-end live. **Wave 1 (catalog-first commit `d0d4730`)**: empty-state catalog `quality/catalogs/doc-alignment.json` (schema_version 1.0; 9-key summary block; floor 0.50; rows []), 3 structure-dim freshness rows guarding catalog presence + summary-block-valid + floor-monotonicity, dimension-home README at `quality/gates/docs-alignment/README.md`, schema spec extension to `quality/catalogs/README.md`, 7 preflight skill files (`.claude/skills/reposix-quality-doc-alignment/{SKILL.md, refresh.md, backfill.md, prompts/extractor.md, prompts/grader.md}` + `.claude/skills/reposix-quality-{refresh,backfill}/SKILL.md`). 12 files, 907 insertions, ~25 min wall-clock. **Wave 2 (crate skeleton `98dcf11` + verb bodies `86036c5`)**: NEW workspace crate `crates/reposix-quality/` — self-contained, `#![forbid(unsafe_code)]` + `#![warn(clippy::pedantic)]`, two `[[bin]]` targets (`reposix-quality` umbrella + `hash_test_fn` standalone). Full clap surface: `doc-alignment {bind, propose-retire, confirm-retire, mark-missing-test, plan-refresh, plan-backfill, merge-shards, walk, status}` + generic `run --gate/--cadence` + `verify --row-id` + `walk` alias. `syn`-based hash binary at `quality/gates/docs-alignment/hash_test_fn` hashes Rust function bodies as `to_token_stream()` sha256 (comments + whitespace normalize away via token-stream round-trip; comment-edit invariance + rename detection asserted). Walker NEVER refreshes stored hashes — only `bind` does, and only after grader returns GREEN; asserted by `crates/reposix-quality/tests/walk.rs`. `merge-shards` writes `CONFLICTS.md` on same-claim-different-test, exits non-zero, never partial-writes the catalog. `confirm-retire` env-guards on `$CLAUDE_AGENT_CONTEXT` set OR non-tty. 28 tests (10 unit + 5 bind_validation + 3 cli_smoke + 2 confirm_retire_envguard + 4 hash_test_fn + 2 merge_shards + 2 walk) all PASS. ~15 min wall-clock; 17 created + 2 modified files. **Wave 3 (this commit + verifier dispatch)**: runner integration via new `docs-alignment/walk` gate row at cadence=pre-push (P0) shelling to `quality/gates/docs-alignment/walk.sh` (release-first, debug-fallback wrapper that forwards stderr verbatim so the slash-command hint reaches the user); two project-wide principles in `quality/PROTOCOL.md` (Principle A subagents-propose-tools-validate-and-mint + Principle B tools-fail-loud-structured-agent-resolvable) with cross-tool examples enumerated; CLAUDE.md gained docs-alignment row in dimension matrix (8 → 9 dimensions) + P64 H3 subsection (~28 added lines under 40 cap; banned-words clean). CLAUDE.md size 31734 bytes (under 40KB cap; no archive needed). `.githooks/pre-push` unchanged structurally — chain through `python3 quality/runners/run.py --cadence pre-push` picks the new gate up via the registry. Verifier subagent verdict GREEN at `quality/reports/verdicts/p64/VERDICT.md` (Path B in-session disclosure per P56-P63 precedent — Task tool unavailable in executor; 4 disclosure constraints honored). Runner state: pre-push exit 0 (22 PASS + 3 WAIVED) / pre-pr exit 0. 7 requirements flipped to shipped (P64): DOC-ALIGN-01..07. v0.12.1 MIGRATE-03 carry-forward added: (h) walker `last_walked` mutation creates per-pre-push catalog churn — either move into artifact or extend `catalog_dirty()` to ignore summary.last_walked drift. Wall-clock for full P64: Wave 1 (~25min) + Wave 2 (~15min) + Wave 3 (within plan budget). Suspicion-of-haste rule honored — verifier scrutinized 14 success criteria with primary-source evidence; spot-checked 3 catalog rows + 3 tests; cargo workspace re-ran clean. **Next: P65 — top-level execution mode; backfill audit; orchestrator dispatches ~25–35 shard subagents in waves of 8 (Haiku tier; Path A via Task); runs `merge-shards`; surfaces `PUNCH-LIST.md`; populates the empty-state catalog.** v0.12.0 milestone tag held until P65 ships + milestone-close verifier dispatched.
