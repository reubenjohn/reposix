# P65 — Docs-alignment backfill (added 2026-04-28)

- **P65 SHIPPED (2026-04-28, v0.12.0; phase verifier GREEN, milestone-close RED on G1 only):** Docs-alignment backfill executed in top-level mode (orchestrator IS executor — `gsd-executor` lacks `Task`; brief `06-p65-backfill-brief.md` is normative). **Catalog-first commit `ae3207d`**: 24-shard `MANIFEST.json` from `reposix-quality doc-alignment plan-backfill` (covers `docs/**/*.md` + `README.md` + archived `REQUIREMENTS.md` v0.8-v0.11; v0.6/v0.7 archives don't exist in repo; deterministic chunker; ≤3 files/shard; directory-affinity); 24 shards lands on the LOW side of the 25-35 envelope. **Wave 1 of 3 (8 shards in parallel; ~3 min wall-clock)**: README + 4 archived REQUIREMENTS + architecture/security/benchmarks; 161 rows (56 BOUND / 90 MISSING_TEST / 15 RETIRE_PROPOSED). **Wave 2 (8 shards; ~5 min)**: concepts + connectors + decisions + development + guides + how-it-works; 132 rows (76/55/1). **Wave 3 (8 shards; ~3 min)**: trust-model + reference + research + social + tutorials; 105 rows (66/14/25). **Three shards required orchestrator surgery**: shard 016 wrote 17 rows to LIVE catalog instead of its shard catalog (recovered via jq move + reset); shard 012 first attempt invented bespoke schema (`test_kind`, `test_file`, `bound`); shard 023 first attempt produced incomplete row (missing `test`/`test_body_hash`/`last_verdict`). Both 012 + 023 re-dispatched with "MUST USE BINARY" emphasis. **Schema cross-cuts surfaced** (filed v0.12.1 MIGRATE-03 (i)): `bind` writes `Row.source` as `SourceCite` object but `merge-shards` deserializer expected `Source` enum (Single|Multi); reconciled via jq before merge. `Row.test` was emitted as multi-test array by some shards but binary expects `String`; flattened to first entry. **Merge `8ac6b44` + `a263868`**: zero conflicts. Final catalog: **388 rows total — 181 BOUND / 166 MISSING_TEST / 41 RETIRE_PROPOSED**. `alignment_ratio` 0.466. **Floor waived in TWO places** (matched TTL 2026-07-31): `summary.floor_waiver` in `quality/catalogs/doc-alignment.json` covers the alignment_ratio<floor BLOCK; `docs-alignment/walk` row waiver in `quality/catalogs/freshness-invariants.json` covers the per-row blocking states (MISSING_TEST + RETIRE_PROPOSED). The 3 P64 catalog-integrity rows (`structure/doc-alignment-{catalog-present,summary-block-valid,floor-not-decreased}`) continue to PASS at pre-push and assert catalog hygiene independent of walker waiver. **PUNCH-LIST.md** at `quality/reports/doc-alignment/backfill-20260428T085523Z/PUNCH-LIST.md` (505 lines; clusters by user-facing surface; 14 distinct clusters; v0.12.1 P71-P80 stub phases suggested). **Smoking gun confirmed**: `docs/reference/confluence.md` 3 MISSING_TEST rows for the FUSE mount + page-tree symlink shape removed in v0.9.0 — exactly the silent regression that motivated this milestone. Other notable clusters: `Reference: glossary` over-extracted with 24 RETIRE_PROPOSED (bulk-confirm review for v0.12.1 P76); `Benchmarks: headline numbers` 20 MISSING_TEST (perf verifiers exist as shell scripts, not Rust tests; MIGRATE-03 carry-forward); `Connector authoring guide` 24 MISSING_TEST (contract assertions exist in code without named test fns); `~92% token reduction` social-vs-benchmark 89.1% drift; `24 ms cold init` mental-model-vs-latency.md 27 ms drift. **CLAUDE.md** gained P65 H3 subsection (15 lines under 40 cap; total file 34521 bytes / 40 KB cap). **REQUIREMENTS.md flips**: DOC-ALIGN-08..10 → [x] at `c619904`; the 32 P56-P61 IDs + 6 POLISH-* IDs flipped at `5d68dac` to close the milestone-close G3 drift. **CHANGELOG.md** `[v0.12.0]` rewritten at `5d68dac`: P64 + P65 contributions; 9-dimension count; "no crates/ changed" claim replaced with "PENDING owner decision". Runner state: pre-push exit 0 (21 PASS + 4 WAIVED including new `docs-alignment/walk`); pre-pr exit 0 (2 PASS + 3 WAIVED); cargo workspace check/clippy/fmt/test all clean. **P65 verifier verdict GREEN** at `quality/reports/verdicts/p65/VERDICT.md` (Path A — top-level orchestrator dispatched `gsd-verifier` subagent via `Task`; ZERO session context; 8 PASS / 1 PARTIAL [claims_total 388 vs 100-200 envelope] / 2 DEFERRED [self-reference + milestone-close] / 0 FAIL across 11 success criteria; 3-row spot-check executed under suspicion-of-haste rule, all 3 substantively aligned). 3 requirements flipped (P65): DOC-ALIGN-08, DOC-ALIGN-09, DOC-ALIGN-10. Wall-clock total: chunker (5 min) + 3 waves of 8 (11 min) + 2 retry shards (4 min) + merge-shards (1 min) + PUNCH-LIST + waivers + commits (8 min) = ~29 min P65. **Milestone-close verifier (Path A re-dispatch at commit `8a446b2`) GRADED RED on G1 only**: workspace `Cargo.toml` version `0.11.3` vs `tag-v0.12.0.sh` Guard 3 expects `0.12.0`. G2 (CHANGELOG stale) + G3 (REQUIREMENTS drift) closed in `5d68dac`. Owner-decision territory: bump-and-publish OR planning-only-tag — see `quality/reports/verdicts/milestone-v0.12.0/VERDICT.md` § Gap-block G1 for structured options. Orchestrator does NOT bump the workspace version because either path is consequential for release-plz auto-publish behavior. v0.12.1 MIGRATE-03 carry-forwards added by P65: (h) walker `last_walked` artifact promotion; (i) `Source`/`Row.test` schema unification; (j) subagent default-catalog refusal env-guard. **STOP per overnight-protocol.md.** **Next (owner action required)**: pick Path A (bump Cargo.toml + release-plz cycle) or Path B (publish=false + edit Guard 3); then run `bash .planning/milestones/v0.12.0-phases/tag-v0.12.0.sh` from a TTY; tag pushes manually.
