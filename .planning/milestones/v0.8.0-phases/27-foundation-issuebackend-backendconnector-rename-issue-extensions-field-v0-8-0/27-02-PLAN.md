---
plan: 27-02
wave: 2
phase: 27
title: "Rename BackendConnector call-sites across all crates"
depends_on: [27-01]
files_modified:
  - crates/reposix-confluence/src/lib.rs
  - crates/reposix-confluence/tests/roundtrip.rs
  - crates/reposix-confluence/tests/contract.rs
  - crates/reposix-github/src/lib.rs
  - crates/reposix-github/tests/contract.rs
  - crates/reposix-cli/src/list.rs
  - crates/reposix-cli/src/refresh.rs
  - crates/reposix-remote/src/main.rs
  - crates/reposix-fuse/src/main.rs
  - crates/reposix-fuse/src/fs.rs
  - crates/reposix-fuse/src/lib.rs
  - crates/reposix-fuse/tests/readdir.rs
  - crates/reposix-fuse/tests/sim_death_no_hang.rs
  - crates/reposix-fuse/tests/nested_layout.rs
  - crates/reposix-swarm/src/contention.rs
  - crates/reposix-swarm/src/sim_direct.rs
  - crates/reposix-swarm/src/confluence_direct.rs
autonomous: true
requirements_addressed: [RENAME-01]

must_haves:
  truths:
    - "No Rust source file outside reposix-core uses the symbol IssueBackend"
    - "cargo test --workspace passes (all tests green)"
    - "cargo clippy --workspace --all-targets -- -D warnings passes"
    - "grep -r IssueBackend crates/ --include=*.rs returns zero lines"
  artifacts:
    - path: "crates/reposix-confluence/src/lib.rs"
      provides: "ConfluenceBackend implementing BackendConnector"
      contains: "impl BackendConnector for ConfluenceBackend"
    - path: "crates/reposix-github/src/lib.rs"
      provides: "GithubReadOnlyBackend implementing BackendConnector"
      contains: "impl BackendConnector for GithubReadOnlyBackend"
    - path: "crates/reposix-fuse/src/fs.rs"
      provides: "FUSE dispatch using BackendConnector"
      contains: "BackendConnector"
  key_links:
    - from: "crates/reposix-fuse/src/fs.rs"
      to: "reposix_core::BackendConnector"
      via: "use reposix_core::BackendConnector (or via dyn trait)"
      pattern: "BackendConnector"
    - from: "crates/reposix-cli/src/list.rs"
      to: "reposix_core::BackendConnector"
      via: "Box<dyn BackendConnector>"
      pattern: "BackendConnector"
---

<objective>
Update every Rust source file outside `reposix-core` that references `IssueBackend`. After Plan 01 renamed the trait in `reposix-core`, these files still compile against the old name — this plan does the sweep. Covers 17 files across 5 crates.

Purpose: Complete the rename so `grep -r IssueBackend crates/ --include="*.rs"` returns zero. No logic changes — mechanical symbol replacement only.

Output: All 17 external files updated; full workspace test suite green; clippy clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-foundation-issuebackend-backendconnector-rename-issue-extensions-field-v0-8-0/27-01-SUMMARY.md
@.planning/phases/27-foundation-issuebackend-backendconnector-rename-issue-extensions-field-v0-8-0/CONTEXT.md

<interfaces>
<!-- After Plan 01 lands, reposix-core exports: -->
```rust
// crates/reposix-core/src/lib.rs
pub use backend::{BackendConnector, BackendFeature, DeleteReason};
// (IssueBackend is gone)
```

<!-- Pattern every external file uses that must change: -->
<!-- Import pattern (varies by crate): -->
use reposix_core::{IssueBackend, ...};         // → BackendConnector
use reposix_core::backend::IssueBackend;       // → BackendConnector
use reposix_core::IssueBackend;                // → BackendConnector

<!-- Impl pattern: -->
impl IssueBackend for ConfluenceBackend { ... }  // → BackendConnector
impl IssueBackend for GithubReadOnlyBackend { .. } // → BackendConnector

<!-- Trait bound / dyn pattern: -->
dyn IssueBackend          // → dyn BackendConnector
Box<dyn IssueBackend>     // → Box<dyn BackendConnector>
Arc<dyn IssueBackend>     // → Arc<dyn BackendConnector>
impl IssueBackend         // → impl BackendConnector (where used as bound)
T: IssueBackend           // → T: BackendConnector
B: IssueBackend           // → B: BackendConnector
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update confluence, github crates (impls + tests)</name>
  <files>
    crates/reposix-confluence/src/lib.rs,
    crates/reposix-confluence/tests/roundtrip.rs,
    crates/reposix-confluence/tests/contract.rs,
    crates/reposix-github/src/lib.rs,
    crates/reposix-github/tests/contract.rs
  </files>
  <read_first>
    - crates/reposix-confluence/src/lib.rs (full file — scan for IssueBackend occurrences)
    - crates/reposix-confluence/tests/roundtrip.rs
    - crates/reposix-confluence/tests/contract.rs
    - crates/reposix-github/src/lib.rs (full file — scan for IssueBackend occurrences)
    - crates/reposix-github/tests/contract.rs
  </read_first>
  <action>
For EACH of the five files: read the file, then replace EVERY occurrence of `IssueBackend` with `BackendConnector`. This includes:
- Import statements (`use reposix_core::IssueBackend` → `use reposix_core::BackendConnector`)
- Trait impl headers (`impl IssueBackend for ConfluenceBackend` → `impl BackendConnector for ConfluenceBackend`)
- Trait impl headers (`impl IssueBackend for GithubReadOnlyBackend` → `impl BackendConnector for GithubReadOnlyBackend`)
- Trait bounds in generic params (`T: IssueBackend` → `T: BackendConnector`)
- `dyn IssueBackend` → `dyn BackendConnector`
- Doc comments referencing `IssueBackend` → `BackendConnector`
- Any `#[allow]`/`#[cfg]` attributes that mention `IssueBackend`

After editing all five files, run:
```bash
cargo check -p reposix-confluence -p reposix-github
```
It must exit 0. If it does not, read the compiler errors and fix them before proceeding.
  </action>
  <verify>
    <automated>grep -rn "IssueBackend" crates/reposix-confluence/ crates/reposix-github/ --include="*.rs" | wc -l &amp;&amp; cargo check -p reposix-confluence -p reposix-github 2>&amp;1 | grep "^error" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "IssueBackend" crates/reposix-confluence/ crates/reposix-github/ --include="*.rs"` returns zero lines
    - `cargo check -p reposix-confluence -p reposix-github` exits 0
    - `grep -n "impl BackendConnector for ConfluenceBackend" crates/reposix-confluence/src/lib.rs` returns one line
    - `grep -n "impl BackendConnector for GithubReadOnlyBackend" crates/reposix-github/src/lib.rs` returns one line
  </acceptance_criteria>
  <done>reposix-confluence and reposix-github compile clean against BackendConnector; zero IssueBackend references remain in these crates.</done>
</task>

<task type="auto">
  <name>Task 2: Update fuse, cli, remote, swarm crates + workspace green-gauntlet</name>
  <files>
    crates/reposix-cli/src/list.rs,
    crates/reposix-cli/src/refresh.rs,
    crates/reposix-remote/src/main.rs,
    crates/reposix-fuse/src/main.rs,
    crates/reposix-fuse/src/fs.rs,
    crates/reposix-fuse/src/lib.rs,
    crates/reposix-fuse/tests/readdir.rs,
    crates/reposix-fuse/tests/sim_death_no_hang.rs,
    crates/reposix-fuse/tests/nested_layout.rs,
    crates/reposix-swarm/src/contention.rs,
    crates/reposix-swarm/src/sim_direct.rs,
    crates/reposix-swarm/src/confluence_direct.rs
  </files>
  <read_first>
    - crates/reposix-fuse/src/fs.rs (this is the largest file, ≈21 IssueBackend occurrences; read fully before editing)
    - crates/reposix-fuse/src/lib.rs
    - crates/reposix-fuse/src/main.rs
    - crates/reposix-fuse/tests/readdir.rs
    - crates/reposix-fuse/tests/sim_death_no_hang.rs
    - crates/reposix-fuse/tests/nested_layout.rs
    - crates/reposix-cli/src/list.rs
    - crates/reposix-cli/src/refresh.rs
    - crates/reposix-remote/src/main.rs
    - crates/reposix-swarm/src/contention.rs
    - crates/reposix-swarm/src/sim_direct.rs
    - crates/reposix-swarm/src/confluence_direct.rs
  </read_first>
  <action>
For EACH of the twelve files: read the file, then replace EVERY occurrence of `IssueBackend` with `BackendConnector`. The same substitution patterns apply as Task 1:
- Import statements
- Trait impl headers
- Trait bounds (generic params like `B: IssueBackend`, `T: IssueBackend`)
- `dyn IssueBackend` → `dyn BackendConnector`
- `Box<dyn IssueBackend>` → `Box<dyn BackendConnector>`
- `Arc<dyn IssueBackend>` → `Arc<dyn BackendConnector>`
- Doc comments

**CRITICAL: `crates/reposix-fuse/src/fs.rs` has ≈21 occurrences** — read this file fully and ensure every occurrence is replaced. Common patterns in fs.rs:
- Struct fields holding `Arc<dyn IssueBackend>` → `Arc<dyn BackendConnector>`
- Function params `backend: &dyn IssueBackend` → `backend: &dyn BackendConnector`
- `impl<B: IssueBackend>` → `impl<B: BackendConnector>`
- Generic type constraints in struct definitions

After editing ALL twelve files, run the full workspace verification suite:
```bash
cargo check --workspace 2>&1 | grep "^error" | head -20
```
If any errors remain, read them and fix. Then run:
```bash
cargo test --workspace 2>&1 | tail -20
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | grep "^error" | head -10
```

Both must exit 0.

Finally, confirm zero remaining IssueBackend references:
```bash
grep -rn "IssueBackend" crates/ --include="*.rs"
```
Must return zero lines. If any remain, fix them.
  </action>
  <verify>
    <automated>grep -rn "IssueBackend" crates/ --include="*.rs" | wc -l &amp;&amp; cargo test --workspace 2>&amp;1 | grep -E "^(test result|FAILED|error)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "IssueBackend" crates/ --include="*.rs"` returns zero lines
    - `cargo test --workspace` exits 0 (all tests pass, no FAILED lines)
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
    - `grep -n "BackendConnector" crates/reposix-fuse/src/fs.rs | wc -l` returns ≥ 20 (confirms fs.rs was fully updated)
  </acceptance_criteria>
  <done>Entire workspace compiles clean; all tests pass; clippy is clean; IssueBackend is completely purged from all Rust source files in the workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new in this plan | Mechanical symbol rename across crates; no behaviour change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | Multi-crate rename | accept | Compiler enforces exhaustive coverage; `cargo test --workspace` regression-guards behaviour |
</threat_model>

<verification>
After both tasks complete:
```bash
# Zero IssueBackend references anywhere in Rust source
grep -rn "IssueBackend" crates/ --include="*.rs"

# Workspace tests green
cargo test --workspace

# Clippy clean
cargo clippy --workspace --all-targets -- -D warnings
```
All three commands must exit 0 / return empty output.
</verification>

<success_criteria>
- `grep -rn "IssueBackend" crates/ --include="*.rs"` returns zero lines
- `cargo test --workspace` exits 0
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0
- `impl BackendConnector for ConfluenceBackend` in reposix-confluence/src/lib.rs
- `impl BackendConnector for GithubReadOnlyBackend` in reposix-github/src/lib.rs
- ≥20 BackendConnector occurrences in reposix-fuse/src/fs.rs
</success_criteria>

<output>
After completion, create `.planning/phases/27-foundation-issuebackend-backendconnector-rename-issue-extensions-field-v0-8-0/27-02-SUMMARY.md`
</output>
