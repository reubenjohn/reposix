---
phase: 91-attach-sync-real-backend-wiring
plan: 03
type: execute
wave: 3
depends_on: [91-01, 91-02]
files_modified:
  - crates/reposix-remote/src/lib.rs
  - crates/reposix-remote/src/main.rs
  - crates/reposix-remote/src/backend_dispatch.rs
  - crates/reposix-remote/Cargo.toml
  - crates/reposix-cli/Cargo.toml
  - crates/reposix-cli/src/attach.rs
  - crates/reposix-cli/src/sync.rs
  - crates/reposix-cache/src/reconciliation.rs
  - crates/reposix-cli/tests/agent_flow_real.rs
  - .github/workflows/ci.yml
  - quality/gates/agent-ux/attach-sync-real-backend.sh
  - .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md
autonomous: true
requirements: [RBF-A-01, RBF-A-02, RBF-A-03, RBF-A-04]
must_haves:
  truths:
    - "reposix attach confluence::<project> / github::<project> / jira::<project> constructs the real connector"
    - "Confluence/JIRA connectors keep the with_audit chain (OP-3 preserved)"
    - "No P\\d+-\\d+ / P82+ phase-ID token survives in attach.rs/sync.rs production strings"
    - "attach_real_* / sync_real_* #[ignore] smokes exist, default-skip without creds"
  artifacts:
    - path: "crates/reposix-remote/src/lib.rs"
      provides: "pub mod backend_dispatch (lib target alongside the bin)"
    - path: "crates/reposix-cli/src/attach.rs"
      provides: "delegates to reposix_remote::backend_dispatch::{parse_remote_url, instantiate}"
    - path: "crates/reposix-cache/src/reconciliation.rs"
      provides: "ForkAsNew resolved (no TODO P82+), Abort naming/doc reconciled"
  key_links:
    - from: "crates/reposix-cli/src/attach.rs"
      to: "reposix_remote::backend_dispatch::instantiate"
      via: "shared factory (inherits with_audit for Confluence/JIRA)"
      pattern: "backend_dispatch::(instantiate|parse_remote_url)"
    - from: "crates/reposix-cli/src/sync.rs"
      to: "reposix_remote::backend_dispatch::instantiate"
      via: "same shared factory"
      pattern: "backend_dispatch::"
---

<objective>
Land the work P79-03 silently dropped (D91-03): wire confluence/github/jira into `attach` and `sync --reconcile`
by delegating to the mature `backend_dispatch.rs` — expose reposix-remote as a lib, add a reposix-cli path
dependency, and call the shared factory instead of writing a third/fourth dispatch copy. Preserve OP-3
`with_audit`. Scrub phase-ID tokens (D91-09). Resolve ForkAsNew (D91-04) + Abort naming. Ship the
attach_real_* / sync_real_* #[ignore] smokes. Forward JIRA_TEST_PROJECT in ci.yml (D91-09).

Purpose: `reposix attach confluence::TokenWorld` currently bails "not yet wired in P79-02 scaffold" — the exact
HIGH-severity T2-attach F6 friction the litmus REOPEN gate will catch.
Output: real-backend attach/sync + honest reconciliation + real-backend smokes.
</objective>

<ownership_charter>
Binding: (1) floor not ceiling; (2) noticing is a deliverable; (3) eager-fix <1h else file; (4) verify against
reality; (5) polish for adoption. A hand-rolled dispatch arm that drops with_audit is a RED (D91-03).
</ownership_charter>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/91-attach-sync-real-backend-wiring/91-DECISIONS.md   (D91-03, D91-04, D91-09)
@.planning/phases/91-attach-sync-real-backend-wiring/91-RESEARCH-code.md   (§ 2.1–2.5 — stubs, dispatch, audit)

<interfaces>
The mature factory to reuse (crates/reposix-remote/src/backend_dispatch.rs, 724 lines, all pub(crate) today):
- `parse_remote_url(url) -> ParsedRemote { kind, origin, project }` (:105-179) — handles confluence/jira markers.
- `instantiate(parsed) -> Result<Arc<dyn BackendConnector>>` (:222-229) → sim/github/build_confluence/build_jira.
- `build_confluence`/`build_jira` (:284-324) — collect_missing env check → creds → open_connector_audit →
  `.with_audit(audit)` (the OP-3 wiring). `instantiate_github` (:236-239) does NOT wire audit (read-only this
  milestone — correct). Tests connector_audit_wired_confluence/jira (:674-722) are the template for new asserts.
- Currently exposed pub(crate): parse_remote_url, instantiate, BackendKind, ParsedRemote,
  sanitize_project_for_cache — bump to pub for the lib.

Consolidation shape (D91-03 default): add `crates/reposix-remote/src/lib.rs` (`pub mod backend_dispatch;` +
whatever siblings it needs), thin main.rs to `use` the lib, add `reposix-remote = { path=..., version=... }`
to reposix-cli/Cargo.toml. Lib target name defaults to `reposix_remote`. Warning #3: this pulls parking_lot
as a NEW prod dep for reposix-cli (grep confirmed absent today) — NOTE it in the dependency diff; if the diff
drags MORE than parking_lot + what cli already links via confluence/github/jira/cache, CHECKPOINT to coordinator.

Stubs to replace:
- attach.rs:143-165 — sim-only match; catch-all bails "not yet wired in P79-02 scaffold" (carries a
  `// banned-words: ok — P91 RBF-A-03` marker; remove BOTH the marker and the string).
- sync.rs:83-95 — sim-only; catch-all bails "...land alongside the bus-remote work in P82+". The `sync.rs:94`
  `P82+` is caught by NO structure gate (framework B(f)) — its removal is verified ONLY by grep in this plan
  + the verifier subagent. Also sync.rs:44 doc string, attach.rs:163, reconciliation.rs:60,182.

reconciliation.rs (D91-04):
- ForkAsNew stub :180-186 (pure eprintln "TODO P82+"). FIRST check whether 91-02's path fix makes it work for
  free: diff.rs:163-165 already classifies a tree path absent from prior_by_path as Create, so ForkAsNew's job
  may be ONLY "don't delete, don't abort" — which the current arm already does. If free → replace the eprintln
  with a correct-behavior comment + a test proving the next push creates the orphan. If a real impl is S-sized
  → implement (orphan → create_record on SoT, audit row, counted). If M+ → teaching error ("fork-as-new not
  supported yet; use --orphans=keep or delete <path>; see docs/guides/troubleshooting.md") + v0.14 intake.
  CHECKPOINT to coordinator before building speculative machinery.
- Abort (:166-189) never actually aborts (research NOTICED #2; test attach.rs:403 comment confirms "default
  just warns"). Reconcile: fix the doc comment to stop saying "Abort attach", OR rename to WarnOnly/Report.

agent_flow_real.rs (D91-09, research §2.4): mirror the #[ignore = "real-backend; requires <VARS>"] +
skip_if_no_env! idiom (:126-184). Add attach_real_{confluence,github,jira} + sync_real_{confluence,github,jira}
that vanilla `git init` + `reposix attach $BACKEND::$PROJECT` + assert post-conditions (git config +, where
creds present, a real list_records). FIX the stale module doc :25-27 ("helper still hardcodes SimBackend") in
the SAME commit — it is false since backend_dispatch landed.

ci.yml (D91-09, research §2.7): jobs at :267-290 (integration-contract-jira-v09) + :292-349 (bench-latency-v09)
set JIRA_EMAIL/JIRA_API_TOKEN/REPOSIX_JIRA_INSTANCE but omit JIRA_TEST_PROJECT. Add
`JIRA_TEST_PROJECT: ${{ secrets.JIRA_TEST_PROJECT }}` to both env blocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose reposix-remote as a lib; delegate attach.rs + sync.rs to backend_dispatch (preserve OP-3)</name>
  <files>crates/reposix-remote/src/lib.rs, crates/reposix-remote/src/main.rs, crates/reposix-remote/src/backend_dispatch.rs, crates/reposix-remote/Cargo.toml, crates/reposix-cli/Cargo.toml, crates/reposix-cli/src/attach.rs, crates/reposix-cli/src/sync.rs</files>
  <action>
Add src/lib.rs exposing `pub mod backend_dispatch;` (+ any modules it needs); thin main.rs to use the lib; add
`[lib]` to reposix-remote/Cargo.toml. Bump parse_remote_url/instantiate/BackendKind/ParsedRemote to pub. Add the
reposix-cli → reposix-remote path dependency. Replace attach.rs:143-165 and sync.rs:83-95 sim-only match arms
with `backend_dispatch::parse_remote_url` + `instantiate`. Verify Confluence/JIRA go through build_confluence/
build_jira → .with_audit (OP-3). Remove the "not yet wired" bail strings + the banned-words:ok marker.
Note the parking_lot new-prod-dep in the SUMMARY dependency diff; CHECKPOINT if the diff is larger than expected.
  </action>
  <verify>cargo check -p reposix-remote 2>&1 | tail -5 && cargo check -p reposix-cli 2>&1 | tail -5</verify>
  <done>reposix-remote builds as lib+bin; attach/sync delegate to the factory; Confluence/JIRA retain with_audit; bail strings gone.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub phase-ID tokens; resolve ForkAsNew (D91-04) + Abort naming (reconciliation.rs)</name>
  <files>crates/reposix-cli/src/attach.rs, crates/reposix-cli/src/sync.rs, crates/reposix-cache/src/reconciliation.rs, .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md</files>
  <action>
Scrub P79-02/P79-03/P82+ tokens from attach.rs (:163), sync.rs (:44,:94), reconciliation.rs (:60,:182) —
replace with recovery-oriented teaching text, no phase IDs. Resolve ForkAsNew per the § interfaces decision
tree (free / S-impl / teaching-error+intake — CHECKPOINT before speculative machinery). Reconcile Abort's
name/doc. Flip the ForkAsNew SURPRISES-INTAKE entry (line ~150) to RESOLVED with the chosen disposition + SHA.
D91-09: if the no-suffix `P\d+\+` shape can be cheaply added to banned-production-tokens.sh, do it; else file
an intake entry.
  </action>
  <verify>cargo test -p reposix-cache reconciliation 2>&1 | tail -5; grep -rnE 'P[0-9]{2,3}[-+]' crates/reposix-cli/src/attach.rs crates/reposix-cli/src/sync.rs crates/reposix-cache/src/reconciliation.rs (zero production-string hits)</verify>
  <done>Zero phase-ID tokens in the 3 files' production strings; ForkAsNew resolved (no TODO P82+); Abort name/doc coherent; intake RESOLVED.</done>
</task>

<task type="auto">
  <name>Task 3: Ship attach_real_* / sync_real_* smokes + fix stale doc; forward JIRA_TEST_PROJECT; wire attach-sync-real verifier</name>
  <files>crates/reposix-cli/tests/agent_flow_real.rs, .github/workflows/ci.yml, quality/gates/agent-ux/attach-sync-real-backend.sh</files>
  <action>
Add attach_real_{confluence,github,jira} + sync_real_{confluence,github,jira} #[ignore] smokes mirroring the
existing idiom (skip_if_no_env! first line; default-skip without creds). Correct the stale module doc :25-27.
Add JIRA_TEST_PROJECT to both ci.yml JIRA job env blocks. Write quality/gates/agent-ux/attach-sync-real-backend.sh
(the verifier the 91-01 row cites): under creds + REPOSIX_ALLOWED_ORIGINS, drive `cargo test -p reposix-cli --
--ignored attach_real_confluence sync_real_confluence` against a sanctioned target and emit a transcript via
quality/gates/agent-ux/lib/transcript.sh; exit 75 when env is unset (env-gate); hard-FAIL (not 75) on a
non-sanctioned target or creds-present-but-unreachable (OD-2). Do NOT flip the row status (coordinator runs it).
  </action>
  <verify>cargo test -p reposix-cli --no-run 2>&1 | tail -5 (smokes compile); cargo test -p reposix-cli agent_flow_real 2>&1 | tail -5 (default-skip clean, no creds); bash quality/gates/agent-ux/attach-sync-real-backend.sh; echo "exit=$?" (75 without creds)</verify>
  <done>Smokes compile + default-skip; stale doc fixed; ci.yml forwards JIRA_TEST_PROJECT; verifier script lands (exit 75 sans creds).</done>
</task>

</tasks>

<verification>
- `cargo check -p reposix-remote` (lib+bin), `cargo check/test -p reposix-cli`, `cargo test -p reposix-cache` pass.
- `cargo clippy -p reposix-cli --all-targets -- -D warnings` clean; fmt clean.
- `grep -rnE 'P[0-9]{2,3}[-+]' crates/reposix-cli/src/{attach,sync}.rs crates/reposix-cache/src/reconciliation.rs`
  returns zero production-string hits (allowlist markers excepted).
- `cargo run -p reposix-cli -- attach confluence::REPOSIX /tmp/x 2>&1` no longer emits any P\d+-\d+ token to stderr (SC-3).
- attach-sync-real verifier exits 75 without creds; catalog still loads clean.
</verification>

<success_criteria>
attach/sync construct real connectors via the shared factory with OP-3 preserved; zero phase-ID tokens;
ForkAsNew/Abort honest; real-backend smokes + verifier landed; JIRA_TEST_PROJECT forwarded.
</success_criteria>

<out_of_scope>
NO real-backend mutation write-path through attach/sync (that is P92 RBF-B-05). NO dvcs-third-arm harness (91-04).
NO litmus (91-05). NO REQUIREMENTS/CLAUDE doc flips (91-06). Comments/attachments stay untouched (D91-05→P95).
Do NOT flip the attach-sync-real-backend row status.
</out_of_scope>

<what_to_notice>
- If refresh.rs's fetch_issues is a THIRD read-only copy of the env-check pattern (research §2.2), note it as a
  GOOD-TO-HAVE (retarget at the shared helper) — do NOT absorb into P91.
- If the lib split forces more pub visibility bumps than parse_remote_url/instantiate, list them.
- If ForkAsNew turns out to need cross-attach id-collision handling (research §2.3 open question), that is the
  M+ signal → teaching error + intake, checkpoint.
- Watch for any attach/sync path that constructs a connector WITHOUT going through instantiate (would bypass
  with_audit silently) — that is the OP-3 regression risk.
</what_to_notice>

<commit>
Sketch:
- feat(remote): expose backend_dispatch as lib; delegate attach/sync to shared factory (RBF-A-01/02, D91-03)
- refactor(cache,cli): scrub phase-ID tokens, resolve ForkAsNew + Abort naming (RBF-A-03, D91-04/09)
- test(cli): attach_real_*/sync_real_* smokes + fix stale doc; ci.yml JIRA_TEST_PROJECT; attach-sync-real verifier (RBF-A-04)
Each: Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
</commit>

<output>
Create `.planning/phases/91-attach-sync-real-backend-wiring/91-03-SUMMARY.md` — include the dependency diff
(parking_lot etc.), the ForkAsNew disposition, and OP-3 wiring confirmation.
</output>
