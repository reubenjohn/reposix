---
phase: 91-attach-sync-real-backend-wiring
plan: 06
type: execute
wave: 6
depends_on: [91-01, 91-02, 91-03, 91-04, 91-05]
files_modified:
  - .planning/milestones/v0.13.0-phases/REQUIREMENTS.md
  - CLAUDE.md
  - docs/
  - .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md
autonomous: false
requirements: [RBF-A-06, RBF-A-07]
must_haves:
  truths:
    - "REQUIREMENTS.md DVCS-ATTACH-01..04 status honestly reflects real-backend coverage (not sim-only [x] shipped)"
    - "CLAUDE.md attach example is coherent (clone target and attach SoT match; no bare P82+ token)"
    - "No user-facing doc promises comment/attachment working-tree access (D91-05)"
    - "SURPRISES-INTAKE comments/attachments + swarm entries flipped to ROUTED-P95"
    - "git push origin main lands; CI green INCLUDING pre-pr executing real-git-push-e2e"
  artifacts:
    - path: ".planning/milestones/v0.13.0-phases/REQUIREMENTS.md"
      provides: "honest DVCS-ATTACH-01..04 status flip (RBF-A-06)"
    - path: "CLAUDE.md"
      provides: "coherent attach example (RBF-A-07, framework B(e))"
  key_links:
    - from: "CLAUDE.md attach example"
      to: "docs/concepts/dvcs-topology.md Pattern C"
      via: "coherent clone-target = attach-SoT-mirror"
      pattern: "reposix attach"
---

<objective>
Land the remaining docs (RBF-A-06, RBF-A-07, D91-05), sweep the ROUTED-P95 intake entries, then execute phase
close: pre-push sweep green, git push origin main, gh run watch CI green (INCLUDING the pre-pr job actually
executing real-git-push-e2e), then HAND OFF to the coordinator for the litmus REOPEN gate + verifier subagent.
The executor does NOT self-grade the phase.

Purpose: honest requirement status + coherent adoption-facing docs + a clean landed push.
Output: docs flipped, intake swept, origin green, handoff to coordinator.
</objective>

<ownership_charter>
Binding: (1) floor not ceiling; (2) noticing is a deliverable; (3) eager-fix <1h else file; (4) verify against
reality — CI green is verified with `gh run watch`, not inferred; (5) polish for adoption.
</ownership_charter>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/91-attach-sync-real-backend-wiring/91-DECISIONS.md   (D91-05, D91-12)
@.planning/phases/91-attach-sync-real-backend-wiring/91-RESEARCH-framework.md   (B(d) REQUIREMENTS, B(e) CLAUDE.md)

<interfaces>
REQUIREMENTS.md (RBF-A-06, framework B(d)): the "[x] shipped" DVCS-ATTACH-01..04 rows (lines 45-48) + the
traceability table (lines 124-133, "P79 | shipped") are TRUE only for sim; github/confluence/jira attach was
never wired until 91-03. Flip to honestly reflect the real-backend coverage now landed in P91 (e.g. "P79 (sim) /
P91 (real-backend)"). NOTE (framework correction): the status table lives in REQUIREMENTS.md:124-139, NOT in
architecture-sketch/index.md — do not hunt for a table that isn't there. If architecture-sketch has any
"shipped" overstatement of its own, fix at the real location.

CLAUDE.md (RBF-A-07, framework B(e)): the attach example at CLAUDE.md:108-112 is incoherent — clones a
placeholder GitHub mirror then attaches sim::demo (unrelated pieces, p79 F8 shape) + carries a bare P82+ token.
Replace with the coherent bus/mirror form (clone target = the mirror of the attach SoT; use a sanctioned
target; drop P82+ for the "§ Load-bearing behaviors" cross-ref already used two lines above). If you prefer not
to name TokenWorld before the flow is fully proven, the minimal coherent fix (clone reposix.git + attach
github::reubenjohn/reposix) is acceptable — but prefer the confluence::TokenWorld bus form to match Pattern C.
Also update any new-convention sections P91 introduced (attach real-backend wiring, dvcs-third-arm populated,
litmus real). Same-PR rule: CLAUDE.md revisions ride this phase's push.

Comments/attachments (D91-05): grep docs/ for any promise of comment/attachment working-tree access; fix any
found as an XS doc edit. Do NOT wire or delete the Confluence comment/attachment surface (M-sized, ROUTED-P95).
Flip the comments/attachments SURPRISES-INTAKE entry (~line 400) STATUS to ROUTED-P95 with the D91-05 rationale.
Flip the swarm write-contention entry (~line 410) STATUS to ROUTED-P95 (D91-12), home updated.

Docs gates: if docs/ touched, `mkdocs-strict.sh` + `mermaid-renders.sh` must pass; freshness invariants
(verdict.py) must stay green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Honest REQUIREMENTS.md flip + coherent CLAUDE.md attach example + new-convention updates</name>
  <files>.planning/milestones/v0.13.0-phases/REQUIREMENTS.md, CLAUDE.md</files>
  <action>
REQUIREMENTS.md: flip DVCS-ATTACH-01..04 rows (45-48) + the traceability table (124-133) to honestly reflect
real-backend coverage landed in P91 (sim P79 / real-backend P91). CLAUDE.md: replace the incoherent attach
example (108-112) with the coherent bus/mirror form (framework B(e)); drop the bare P82+ token; update any
sections describing attach/sync backend support, the dvcs-third-arm harness, and the litmus to their new P91
state. Cold-reader check: an agent opening CLAUDE.md cold sees a coherent attach flow.
  </action>
  <verify>grep -nE 'P82\+|sim::demo' CLAUDE.md (attach example no longer incoherent) ; grep -n 'DVCS-ATTACH' .planning/milestones/v0.13.0-phases/REQUIREMENTS.md | head</verify>
  <done>REQUIREMENTS status honest (sim vs real-backend distinguished); CLAUDE.md attach example coherent, no bare P82+; new conventions reflected.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no doc promises comment/attachment access (D91-05); sweep ROUTED-P95 intake entries; docs gates</name>
  <files>docs/, .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md</files>
  <action>
Grep docs/ for any user-facing promise of comment/attachment working-tree access; fix any found as XS doc edits
(do NOT wire or delete the surface — ROUTED-P95). Flip the comments/attachments intake entry (~400) and the
swarm write-contention entry (~410) STATUS to ROUTED-P95 with rationale (D91-05, D91-12). Confirm docs-build
gates pass if docs/ touched.
  </action>
  <verify>grep -rniE 'comment|attachment' docs/ | grep -iE 'working tree|cat |git' | head ; bash quality/gates/docs-build/mkdocs-strict.sh 2>&1 | tail -3 ; grep -n 'ROUTED-P95' .planning/milestones/v0.13.0-phases/SURPRISES-INTAKE.md</verify>
  <done>No doc promises comment/attachment access; both intake entries ROUTED-P95; mkdocs strict passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All P91 code + framework + docs across 91-01..91-06.</what-built>
  <how-to-verify>
Phase close ordering — the executor performs steps 1-3, then STOPS and hands to the coordinator (do NOT
self-grade):
1. Pre-push sweep: `python3 quality/runners/verdict.py session-end` (or the pre-push hook) must be GREEN. Fix
   any RED before pushing. Confirm the intra-phase transient states (real-git-push-e2e local exit-75;
   dvcs-third-arm non-vacuous asserts) are their intended terminal shape, not accidental breakage.
2. `git push origin main`.
3. `gh run watch` the triggered CI run to completion. GREEN is verified by reading the run log — specifically
   confirm the pre-pr quality job ACTUALLY EXECUTED `agent-ux/real-git-push-e2e` (git ≥2.34 on ubuntu-latest)
   and it PASSED (D91-02 phase-close proof obligation — verify in the log, not by inference). If CI is RED, fix
   + re-push before proceeding.
4. HAND OFF to coordinator: the coordinator (a) runs the milestone-close litmus script + dispatches the fresh
   dark-factory T2 agent against TokenWorld (D91-07 REOPEN gate — ≥1 HIGH ⇒ phase REOPENS), and (b) dispatches
   the unbiased verifier subagent that grades the P91 catalog rows from artifacts with zero session context,
   writing the verdict to quality/reports/verdicts/p91/VERDICT.md. The executor does NEITHER.
  </how-to-verify>
  <resume-signal>Executor confirms origin green + pre-pr real-git-push-e2e executed-and-passed in the CI log; coordinator takes over the litmus REOPEN gate + verifier dispatch.</resume-signal>
</task>

</tasks>

<verification>
- `grep -nE 'P82\+' CLAUDE.md` returns no bare attach-example token; attach example coherent.
- REQUIREMENTS.md DVCS-ATTACH status honestly distinguishes sim vs real-backend.
- No doc promises comment/attachment access; both intake entries ROUTED-P95.
- `mkdocs-strict.sh` + `mermaid-renders.sh` pass; verdict.py session-end GREEN.
- `gh run watch` shows CI green with pre-pr real-git-push-e2e executed-and-passed.
</verification>

<success_criteria>
Docs honest + coherent; intake swept; origin main green with the e2e proof executed in CI; phase handed to the
coordinator for the litmus REOPEN gate + verifier — not self-graded.
</success_criteria>

<out_of_scope>
Do NOT wire or delete the comment/attachment surface (P95). Do NOT run the T2 REOPEN grade or the verifier
subagent yourself (coordinator's job). Do NOT flip the litmus row status. Do NOT touch pages 7766017/7798785.
</out_of_scope>

<what_to_notice>
- If CLAUDE.md's dark-factory shim vs canonical-path references are still inconsistent (framework NOTICED #4),
  close it cheaply while editing the Commands section.
- If the reposix-agent-flow skill's GitHub cleanup claim disagrees with testing-targets.md (framework NOTICED
  #5), note it (XS fix or intake).
- If pre-push surfaces a freshness invariant violation from a P91 doc move, read the error — it names the fix.
</what_to_notice>

<commit>
Sketch:
- docs(91): honest DVCS-ATTACH-01..04 status flip + coherent CLAUDE.md attach example (RBF-A-06/A-07)
- docs(91): verify no comment/attachment promise; route swarm + comments/attachments to P95 (D91-05/D91-12)
Each: Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Terminal push: git push origin main (phase close).
</commit>

<output>
Create `.planning/phases/91-attach-sync-real-backend-wiring/91-06-SUMMARY.md` — include the CI-log evidence that
pre-pr real-git-push-e2e executed and passed, and the explicit handoff note to the coordinator.
</output>
