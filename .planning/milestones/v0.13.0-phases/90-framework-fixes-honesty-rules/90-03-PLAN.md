---
task_id: 90-03
phase: 90
wave: B
type: mechanical
execution_mode: top-level
req_ids: [RBF-FW-09, SC3-subagent-graded-migration]
effort: S (3-4h)
model_tier: sonnet (regex scoping + baseline judgment)
depends_on: [90-01]
files_modified:
  - quality/gates/agent-ux/test-name-vs-asserts.sh     # NEW gate
  - quality/catalogs/agent-ux.json                     # dvcs-third-arm kind flip
  - quality/catalogs/subjective-rubrics.json           # dvcs-cold-reader wiring
  - .claude/skills/reposix-quality-review/lib/dispatch_dvcs_cold_reader.sh  # NEW (wire for real)
  - .claude/skills/reposix-quality-review/dispatch.sh  # add --rubric case (the REAL dispatcher — quality/gates/agent-ux/dispatch.sh does NOT exist; D90-12 item 3)
  - scripts/migrations/p90-subagent-graded-migration.sh # NEW (if scripted; else direct edits)
  - crates/**/tests/*.rs                               # per-line honesty markers where justified
---

# Task 90-03 — test-name-vs-asserts gate (F-K8) + subagent-graded migration (D90-10)

## Goal

Ship the `quality/gates/agent-ux/test-name-vs-asserts.sh` triage gate (RBF-FW-09,
row `agent-ux/test-name-vs-asserts`) wired at pre-push, and execute the
subagent-graded kind migration per D90-10. **No cargo** — the gate is a single
`rg` pass; the migration is JSON edits + shell dispatch wiring.

## Subtask A — `test-name-vs-asserts.sh` gate (RBF-FW-09 / F-K8, SC1)

Follow the R2 § G authoring precedent (`banned-production-tokens.sh:37-64` +
`deferral-pointer-linter.sh`): `set -euo pipefail`; `SCRIPT_DIR`→`REPO_ROOT` cd
preamble; single `rg -n --type rust` pass over `crates/`; structured stderr
(`✖ path:line: <fn>` + `owner_hint:` + `see: quality/catalogs/agent-ux.json row
agent-ux/test-name-vs-asserts`); header carrying the **regex-scope trade-off
narrative in-file** (mirror `banned-production-tokens.sh:7-30`).

**Detection logic:**
1. Match `#[test]`/`#[tokio::test]` fns whose name matches
   `(real|dark_factory|round_?trip|end_to_end|e2e|push|fetch)` (case-insensitive).
2. RAISE unless the body (i) invokes `Command::new("git").arg("push|fetch|clone")`
   or a `Command::cargo_bin("git-remote-reposix")` export stream, OR (ii) hits a
   non-`127.0.0.1` host, OR (iii) carries `#[ignore]` real-backend gating.
3. **False-positive scoping (R2 § C — the ~40 honest `*_round_trip*`/`*push*`
   serde/codec unit tests would false-positive on a naive grep):** adopt
   **Option (b)** — a per-line allowlist marker `// test-name-honesty: ok —
   <reason>` (mirrors `// banned-words: ok` precedent), AND per the
   deferral-linter's no-PNN lesson, **a bare marker with no `— <reason>` itself
   RAISEs**. Seed the exemption markers on the ~40 honest units enumerated in
   R2 § C.1 so the gate runs clean at pre-push from day one.
4. **Baseline contract (R2 § G):** exit 0 when the RAISE set == the committed
   baseline (the 4 entries in R2 § C.2, recorded in `raise-list-p90.md` § 2 by
   90-05); exit 1 on any NEW un-baselined RAISE. Exit 75 not needed. The 4
   baseline entries (`dark_factory_sim_happy_path`, `doctor_blob_limit_zero_warns
   _on_real_remote`, `dark_factory_blob_limit_teaching_string_present`,
   `dark_factory_conflict_teaching_string_present`) are DISHONEST/BORDERLINE and
   stay RAISEd (they are the RAISE-LIST seed, not gate failures) — their fixes
   route to P91/P92 via the RAISE LIST, NOT P90.

> **Decision — baseline mechanism:** the gate reads its expected-RAISE set from a
> committed fixture. Prefer a small dedicated file
> `quality/gates/agent-ux/test-name-vs-asserts.baseline` (one `file:line:fn` per
> line) over parsing the human-facing `raise-list-p90.md` (brittle). 90-05's
> RAISE-LIST § 2 documents the same 4 entries for humans; the `.baseline` file is
> the machine contract. Both list the identical 4 entries.

**Latency (R-6):** single `rg` pass <1s; comfortably inside pre-push <60s.

**Pre-push wiring:** the row's `cadences: ["pre-push"]` (minted in 90-01) is the
only wiring needed — the runner discovers + composes by cadence
(`run.py:discover_catalogs` + `is_in_scope`). No new pre-push hook script.

## Subtask B — subagent-graded migration (D90-10, SC3)

Two rows, opposite dispositions (R2 § B):

**B1 — `agent-ux/dvcs-third-arm`: FLIP kind → `mechanical`.** Evidence R2 § B.1:
`dark-factory/dvcs-third-arm.sh` is pure deterministic shell, zero
`claude`/Task/rubric calls anywhere. Edit `agent-ux.json` row (kind at
`agent-ux.json:994`) `subagent-graded` → `mechanical`. **Eager-fix the header
over-claim** (R2 noticing 6 / § C.3): `dvcs-third-arm.sh:6-7` says "bus-push
end-to-end" while `:20-23` admits push is not exercised — soften the header to
match reality in the SAME commit (XS, <5 min).

**B2 — `subjective/dvcs-cold-reader`: WIRE dispatch for real.** Evidence R2 § B/H2:
this is the ACTUAL decorative-kind row (`kind: subagent-graded`, no dispatcher
case — falls to the Path-B stub).

> **Dispatcher path correction (D90-12 item 3 — verified on disk):** the real
> dispatcher is **`.claude/skills/reposix-quality-review/dispatch.sh`** with its
> siblings under `.claude/skills/reposix-quality-review/lib/`
> (`dispatch_cold_reader.sh`, `dispatch_inline_subagent.sh`, …).
> `quality/gates/agent-ux/dispatch.sh` does NOT exist — R2's bare
> `dispatch.sh:NN` citations refer to the skill tree. All wiring + verification
> greps target the skill tree.

Add a `--rubric subjective/dvcs-cold-reader` case to
`.claude/skills/reposix-quality-review/dispatch.sh:58-76` → a new
`.claude/skills/reposix-quality-review/lib/dispatch_dvcs_cold_reader.sh` that
invokes `claude /doc-clarity-review` on its 3 target docs
(`docs/concepts/dvcs-topology.md` + the 2 DVCS guides), mirroring
`lib/dispatch_cold_reader.sh`. Row stays `kind: subagent-graded` (intent WAS real
grading — F-K4c's "wire if intent was real grading" branch).

**Migration mechanism (D-CONV-3):** since it's 2 rows, prefer **direct catalog
edits** (a script would be over-engineering for 2 rows) — but record the
before/after in `raise-list-p90.md` § 4 (90-05). IF the executor finds the flip
touches >4 rows once the live catalog is re-walked, promote to
`scripts/migrations/p90-subagent-graded-migration.sh` (idempotent, per D-CONV-3).

## Verification

```bash
bash quality/gates/agent-ux/test-name-vs-asserts.sh; echo "exit=$?"   # expect 0 (RAISE==baseline)
# introduce a synthetic dishonest name in a scratch file → expect exit 1 (do NOT commit the scratch)
python3 -c "import json;r=[x for x in json.load(open('quality/catalogs/agent-ux.json'))['rows'] if x['id']=='agent-ux/dvcs-third-arm'][0];assert r['kind']=='mechanical'"
grep -q 'subjective/dvcs-cold-reader' .claude/skills/reposix-quality-review/dispatch.sh
test -f .claude/skills/reposix-quality-review/lib/dispatch_dvcs_cold_reader.sh
python3 -m json.tool quality/catalogs/agent-ux.json >/dev/null
python3 -m json.tool quality/catalogs/subjective-rubrics.json >/dev/null
```

## Acceptance criteria

- [ ] Gate exits 0 (RAISE set == 4-entry baseline); exits 1 on a synthetic new dishonest name.
- [ ] ~40 honest units (R2 § C.1) carry `// test-name-honesty: ok — <reason>`; a bare marker RAISEs.
- [ ] Header regex-scope narrative present in-file.
- [ ] `agent-ux/dvcs-third-arm` kind == mechanical; its script header no longer over-claims.
- [ ] `subjective/dvcs-cold-reader` dispatch wired for real in the SKILL tree (`.claude/skills/reposix-quality-review/`: dispatch.sh case + lib script present) — D90-12 item 3.
- [ ] Rows `agent-ux/test-name-vs-asserts` flips NOT-VERIFIED → PASS.

## Must NOT touch

`quality/runners/*` (90-02 owns). verdict.py / PROTOCOL / dispatch templates
(90-04). The RAISE LIST content (90-05 fills; 90-03 only records B1/B2
before/after for 90-05 to lift). Any cargo/Rust SOURCE (test-file honesty markers
are comment-only edits, no recompile needed — but do NOT run cargo).

## Two-channel rule

Sub-subagent writes the gate script + full `rg` match list + the marker-seeding
diff + the 2 catalog row diffs to disk; returns ≤300-word TLDR (gate exit,
baseline count, migration before/after, surprises).
