---
task_id: 90-04
phase: 90
wave: B
type: mechanical + unit
execution_mode: top-level
req_ids: [RBF-FW-10, RBF-FW-12]
effort: S (3-4h)
model_tier: sonnet
depends_on: [90-01]
files_modified:
  - quality/dispatch/absorption-honesty-spot-check.md   # NEW (F-K5, D90-08)
  - quality/dispatch/milestone-adversarial.md           # NEW (RBF-FW-12, D90-09)
  - quality/gates/agent-ux/absorption-honesty-template-present.sh  # NEW verifier
  - quality/gates/agent-ux/milestone-adversarial-pass.sh          # NEW verifier
  - quality/runners/verdict.py                          # RBF-FW-12 hook + FW-07a error render
  - quality/runners/test_verdict.py                     # adversarial-gate tests
  - quality/PROTOCOL.md                                 # document both + new runner semantics
  - PRACTICES.md                                        # OP-8 cross-ref
---

# Task 90-04 — Absorption + adversarial templates, PROTOCOL, verdict.py hook

## Goal

Ship the two dispatch templates (F-K5 absorption spot-check, RBF-FW-12
adversarial rubric), their file-presence+hash verifiers, the verdict.py
milestone-close GREEN-block hook, and the PROTOCOL/PRACTICES documentation.
**verdict.py is edited ONLY in this wave (P90-D3)** so Wave B has no contention.
Tests are python `unittest` — **no cargo**.

## Subtask A — absorption honesty-check template (RBF-FW-10 / F-K5, D90-08, SC4)

Create `quality/dispatch/absorption-honesty-spot-check.md` (sibling of
`milestone-close-verdict.md`) carrying the **F-K5 meta-rule verbatim**, consumed
by P96:
- **(a)** sample MUST include **EVERY** phase that closed without filing intake.
- **(b)** spot-check author **≠ milestone orchestrator** (fresh independent
  subagent).
- **(c)** rubric question = **"walk one critical example end-to-end mentally —
  does it work?"** (NOT "did the phase use the framework correctly?").
- **(d)** verifier **hash-binds** the spot-check content (not mere file
  existence).
Cross-reference from PRACTICES.md OP-8 and PROTOCOL.md so P96 cannot miss it.

Verifier `quality/gates/agent-ux/absorption-honesty-template-present.sh`: asserts
file exists AND its sha256 matches a committed expected hash AND the 4 clauses
are grep-present. Backs row `agent-ux/absorption-honesty-template-present`.

> **Hash-binding mechanism:** store the expected sha256 in the verifier script (a
> `EXPECTED_SHA=…` constant) OR in the catalog row's `expected.asserts`. Prefer
> the script constant (single source; the catalog row's `claim_vs_assertion_audit`
> explains the binding). Editing the template later requires updating the
> constant in the same commit — that IS the content-hash binding contract.

## Subtask B — milestone-close adversarial rubric (RBF-FW-12 / D90-09, SC6)

Create `quality/dispatch/milestone-adversarial.md`: a fresh subagent reads
catalog row **descriptions ONLY** (no implementation context) and grades whether
each row's assertion **would falsify its description** if the description were
false, writing:

```json
{ "milestone": "v0.13.0", "dispatched_at": "…Z", "subagent": "<author != orchestrator>",
  "rows_audited": N, "rows_failed": [ {"id": "…", "reason": "…"} ], "verdict": "PASS" }
```
to **`quality/reports/verifications/adversarial/<version>.json`** (D90-09 path —
NOT R1's `milestone-adversarial/` path).

## Subtask C — verdict.py GREEN-block hook (RBF-FW-12, D90-09, SC6)

Per R1 § FW-12 (fits the ~33-line headroom; 367+~18=385 < 400):
- Add `milestone_adversarial_gate(repo_root, version) -> (blocked, reason)`:
  blocks if the `adversarial/<version>.json` artifact is absent OR reports ≥1
  `rows_failed`.
- Add a `--milestone <version>` arg to `main()`; when set, after computing
  `color`, if the gate blocks → force `color = "red"` and print the reason.
  **Darken-only** — composes with `compute_color`/`compute_exit_code`, preserves
  the D-CONV-2 3-state contract (green→red only, never lighten).
- Verifier `quality/gates/agent-ux/milestone-adversarial-pass.sh`: thin bash
  running `python3 -m unittest quality.runners.test_verdict` (adversarial cases).
  Backs row `agent-ux/milestone-adversarial-pass`.
- Tests (`test_verdict.py`, R1's 3 cases): artifact absent → red even when all
  rows PASS; empty `rows_failed` → follows `compute_color`; 1 failed row → red.

## Subtask D — FW-07a `error` marker rendering (R1 noticing 2; P90-D3 keeps it here)

R1 noticing item 2: `run.py:243` writes `error: "verifier not found at …"` but
`verdict.py`'s NOT-VERIFIED section (`:235-247`) never renders it — a
missing-script NOT-VERIFIED looks identical to a stale one. **2-line add at
`verdict.py:244`:** render `error` when present. This lives here (not 90-02)
because verdict.py is this wave's file (P90-D3). Add a `test_verdict` assertion.

> **Also (R1 noticing 5, XS eager-fix if <15 min):** `verdict.py:269` does
> `from run import parse_duration` inside a function (lazy import to dodge a
> cycle). Both modules already import `_freshness.parse_duration` — switch
> verdict.py to import from `_freshness` directly. If it's not a clean 1-line
> swap, do NOT fix and do NOT write GOOD-TO-HAVES.md yourself — **hand the
> candidate entry to 90-07's drain task in your TLDR** (D90-12 item 4:
> single-writer; 90-02 is the only Wave-B writer of GOOD-TO-HAVES.md). Do NOT
> expand scope.

## Subtask E — PROTOCOL + PRACTICES documentation (SC6)

- `quality/PROTOCOL.md`: document (1) the absorption honesty-spot-check dispatch
  (§ near the verifier-subagent template), (2) the milestone-close adversarial
  pass + its verdict GREEN-block, (3) the new runner semantics from 90-02
  (missing-verifier demote, env-skip preserve, minted_at anchor, coverage_kind
  split, shell-subprocess transcript runtime, F-K4b asserts-congruence). Keep it
  reference-layered (progressive disclosure) — the deep detail cites R1 file:line.
- `PRACTICES.md`: OP-8 cross-ref to the absorption template (P96 consumer).

## Verification

```bash
python3 -m unittest quality.runners.test_verdict -v
bash quality/gates/agent-ux/absorption-honesty-template-present.sh
bash quality/gates/agent-ux/milestone-adversarial-pass.sh
# adversarial darken-only smoke: absent artifact forces red on an all-PASS synthetic
python3 quality/runners/verdict.py --milestone v0.13.0 2>&1 | tail -5   # expect red + reason (artifact absent)
sha256sum quality/dispatch/absorption-honesty-spot-check.md   # matches verifier constant
wc -l quality/runners/verdict.py                              # report; must stay < 400
```

## Acceptance criteria

- [ ] Absorption template carries all 4 F-K5 clauses verbatim; verifier hash-binds.
- [ ] Adversarial rubric present; writes to `adversarial/<version>.json` (D90-09 path).
- [ ] `milestone_adversarial_gate` blocks GREEN on absent artifact AND on ≥1 failed row; darken-only (never lightens).
- [ ] verdict.py renders `error` marker in NOT-VERIFIED section; stays < 400 lines.
- [ ] PROTOCOL documents both dispatches + all new runner semantics; PRACTICES OP-8 cross-ref added.
- [ ] Rows `agent-ux/{absorption-honesty-template-present,milestone-adversarial-pass}` flip NOT-VERIFIED → PASS.
- [ ] PROTOCOL/PRACTICES edits: anticipate docs-alignment STALE (R-5) — coordinate rebind with 90-07's doc-edit rebind step (these are NOT under docs/ so likely unbound, but confirm no doc-alignment row cites PROTOCOL.md/PRACTICES.md before push).

## Must NOT touch

`quality/runners/run.py` / `_audit_field.py` / `_realbackend.py` (90-02 owns —
this wave touches verdict.py ONLY among runners). The catalog row blocks (90-01
minted). The test-name gate (90-03). **GOOD-TO-HAVES.md** (D90-12 item 4:
90-02 is the only Wave-B writer; 90-04's candidate entries route through
90-07's drain task via the TLDR).

## Two-channel rule

Sub-subagent writes both templates + verifier scripts + verdict.py diff +
test_verdict output + the darken-only smoke to disk; returns ≤300-word TLDR
(clauses present, gate darken-only confirmed, verdict.py LOC, GOOD-TO-HAVES
candidate entries for 90-07's drain, surprises).
