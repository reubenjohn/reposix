---
task_id: 90-05
phase: 90
wave: C
type: mechanical + synthesis
execution_mode: top-level
req_ids: [RBF-FW-06-legacy-RAISE, SC5, waiver-cliff-D90-02]
effort: M (4-5h)
model_tier: sonnet (synthesis of R2 evidence into decision-ready dispositions)
depends_on: [90-02, 90-03]
files_modified:
  - quality/reports/raise-list-p90.md          # FILL the skeleton (SC5)
  - quality/catalogs/docs-reproducible.json     # waiver renewals (5 expired)
  - quality/catalogs/cross-platform.json        # waiver renewals + tracked_in repoint
  - quality/catalogs/perf-targets.json          # waiver renewals + dangling-verifier note
  - quality/catalogs/security-gates.json        # waiver renewals + fix 2 dangling paths
  - quality/catalogs/subjective-rubrics.json    # 3 subjective waiver renewals
  - quality/catalogs/release-assets.json        # cargo-binstall: land, don't renew
  - crates/reposix-cli/Cargo.toml               # cargo-binstall pkg-url alignment (~10 LOC)
---

# Task 90-05 — RAISE LIST (SC5) + waiver-cliff disposition (D90-02)

## Goal

Generate the committed, decision-ready **RAISE LIST** that seeds P92/P94/P95
(SC5), and give every waiver a **conscious per-waiver disposition** (D90-02) —
no waiver silently expires into FAIL. This wave READS 90-02's coverage_kind
detector output and 90-03's gate baseline; it does NOT re-implement them.

## Subtask A — Fill `quality/reports/raise-list-p90.md` (SC5)

Fill the 5 sections seeded by 90-01, lifting evidence verbatim from R2:

1. **§ 1 Transport/perf coverage_kind dispositions** — from R2 § A.1/A.2/A.3.
   For each flagged row: current coverage reality (sim/localhost/static-grep/
   vacuous) + disposition (`description-soften` / `coverage_kind: sim` +
   honest-description / `WAIVED + until_date`). Name the **genuine overclaims**
   (R2 § A.3.2): `dark-factory-sim` ("emit"→source-grep), `webhook-latency-floor`
   (vacuous n=1), `webhook-trigger-dispatch` (YAML≠runtime),
   `bus-write-no-helper-retry` (source-grep for runtime claim),
   `benchmark-claim/{8ms,89.1%}` (verifier-less headlines). Flag: **zero green
   real-backend transport coverage exists today** (R2 § A.3.3) — the quantified
   justification for P91/P92. **Legacy coverage_kind fixes route to P95 RBF-D-06;
   P90 only RAISE-lists (D90-05).**
2. **§ 2 Dishonest-test baseline** — the 4 entries from R2 § C.2
   (`dark_factory_sim_happy_path`, `doctor_blob_limit_zero_warns_on_real_remote`,
   `dark_factory_{blob_limit,conflict}_teaching_string_present`) with verdicts +
   the THIN-but-exempt `#[ignore]` cases (R2 § C.2 adjacent). This section is the
   human-facing twin of 90-03's `.baseline` file — keep them identical (4 entries).
   Fixes route to P91 (deepen `agent_flow_real.rs`) / P92.
3. **§ 3 Magic-fixture hazards** — the **triple** path-shape schism from R2 § E:
   `builder.rs:90` (`issues/1.md`) vs `refresh.rs:120` (11-pad) vs
   `diff.rs:106`/`fast_import.rs:63` (4-pad bare). List the HIGH "test-green-
   depends-on-the-bug" fixtures (§ E HIGH table) + the MED no-`record_path()`
   root cause. **Fix-first recommendation → P91** (canonical `record_path(id)` in
   reposix-core; re-key fixtures so they go RED until BUG-1 fixed).
4. **§ 4 Subagent-graded migration record** — lift 90-03 B1/B2 before/after
   (dvcs-third-arm → mechanical; dvcs-cold-reader → wired).
5. **§ 5 Waiver dispositions** — the table from Subtask B below.

## Subtask B — Waiver-cliff disposition (D90-02, per-waiver, from R2 § D)

| id | disposition | action in P90 |
|---|---|---|
| `release/cargo-binstall-resolves` | **LAND, don't renew** (D90-02a) | ~10-LOC `pkg-url` alignment in `crates/reposix-cli/Cargo.toml` vs `release.yml` archive shape; flips PARTIAL→PASS; clear the waiver. **This is the only cargo-adjacent edit — but it's a `Cargo.toml` metadata edit, NOT a cargo BUILD; do not run cargo here.** |
| `docs-repro/{example-01,02,04,05,tutorial-replay}` (5 EXPIRED 2026-05-12) | **RENEW — still-broken** (R2 § D evidence) | renew with honest `tracked_in` (container-never-spawns-sim root cause; tutorial-replay cold-build budget + QL-001). Home the fix at P95/P97, not P90. |
| `cross-platform/{windows,macos}-rehearsal` (2026-07-26) | **RENEW**, repoint `tracked_in` | verifier "intentionally absent"; repoint dead "v0.12.1 CROSS-0x" → a live phase (P95/P97/launch-readiness). |
| `perf/{latency-bench,token-economy-bench}` (2026-07-26) | **RENEW**, repoint | sim/fixture-derived; headline cross-check deferred. Repoint dead v0.12.1 label. |
| `perf/headline-numbers-cross-check` (2026-07-26) | **RENEW + dangling-verifier flag** | verifier `.py` absent; flag in RAISE § 1; repoint. |
| `security/allowlist-enforcement` + `security/audit-immutability` (2026-07-26, **P0**) | **RENEW + FIX/RE-POINT the 2 dangling script paths** (D90-02d, R-8) | scripts don't exist under `quality/gates/security/`; under 90-02's FW-07a demote they'd flip NOT-VERIFIED the moment the waiver lapses. Re-point to a real script OR name the gap; add an **explicit P92 line item** in RAISE § 5 (security e2e), not accidental discovery. |
| `subjective/{cold-reader-hero-clarity,install-positioning,headline-numbers-sanity}` (2026-07-26) | **RENEW — NOT mooted by dispatch wiring** (D90-02b) | waiver covers runner-sweep artifact clobbering (MIGRATE-03), not wiring; honest `tracked_in`. |
| `code/cargo-test-pass` (2026-07-26) | **RENEW** | local nextest violates memory budget; CI is canonical. |
| `structure/file-size-limits` (2026-08-08) | leave (not at cliff) | note the 10 undrained violations + symlink-exclusion XS in RAISE. |
| `agent-ux/real-git-push-e2e` (2026-07-31) | **DO NOT RENEW** (D90-01) | route to P91 QL-001 fix; waiver is the backstop if P91 slips. |
| `docs/index/git-checkout-branch-command` (2026-07-31) | **STAYS WAIVED** (QL-001-blocked, D90-01/-07) | route to P91. |
| 5 doc-alignment MISSING_TEST rows (2026-07-31) | handled in **90-06** (un-waive after real tests) | note the pointer here. |

**Mass-renewal warning (R2 § D):** every renewal MUST swap `tracked_in` away from
the dead "v0.12.1 MIGRATE-03/SEC-0x/CROSS-0x" label to a phase that EXISTS in
ROADMAP (P92/P95/P97/launch-readiness) — else the 2026-07-26 cliff repeats in 3
weeks (this is the C7 deferral-loop shape the framework fixes exist to prevent).

## Verification

```bash
test -s quality/reports/raise-list-p90.md
grep -c '^## ' quality/reports/raise-list-p90.md   # 5 sections filled
# every renewed waiver has a future `until` AND a non-v0.12.1 tracked_in:
python3 - <<'PY'
import json,glob,datetime
for f in glob.glob('quality/catalogs/*.json'):
    d=json.load(open(f))
    for r in d.get('rows',[]):
        w=r.get('waiver')
        if w and 'v0.12.1' in str(w.get('tracked_in','')):
            print('STALE tracked_in still present:', f, r['id'])
PY
python3 quality/runners/run.py --cadence pre-push --dry-run 2>&1 | tail -5   # loads clean
```

## Acceptance criteria

- [ ] RAISE LIST has all 5 sections filled from R2 evidence; decision-ready for P92/P94/P95.
- [ ] § 2 matches 90-03's `.baseline` (4 entries).
- [ ] `release/cargo-binstall-resolves` waiver cleared; `pkg-url` aligned (PARTIAL→PASS path).
- [ ] Every renewed waiver: future `until`, non-`v0.12.1` `tracked_in` naming a live phase.
- [ ] 2 dangling security verifier paths fixed-or-re-pointed; explicit P92 line item in RAISE § 5.
- [ ] `agent-ux/real-git-push-e2e` + `docs/index/git-checkout-branch-command` NOT renewed (route P91).

## Must NOT touch

`quality/runners/*` (90-02/90-04 own). The 9 P90 rows minted in 90-01 (edits are
none — they flip PASS via their verifiers). The 5 MISSING_TEST rows' un-waive
(90-06 owns, after the real tests exist). Do NOT land legacy coverage_kind
migration (P95). Do NOT run cargo (the Cargo.toml metadata edit is not a build).

## Two-channel rule

Sub-subagent writes the full RAISE LIST + per-waiver diff + the tracked_in sweep
output to disk; returns ≤300-word TLDR (sections filled, waivers disposed, dangling
paths fixed, cargo-binstall status, surprises).
