---
phase: 30
plan: 04
type: execute
wave: 1
depends_on: [30-01, 30-02]
files_modified:
  - mkdocs.yml
autonomous: true
requirements: [DOCS-07, DOCS-08]
must_haves:
  truths:
    - "`mkdocs.yml` nav matches the IA sketch from `.planning/notes/phase-30-narrative-vignettes.md` § 'Proposed nav' — Home / Why / Mental model / vs-MCP / Tutorial / How it works / Guides / Reference / Decisions / Research / Development."
    - "`mkdocs.yml` theme.features removes `navigation.expand` and adds `navigation.footer`, `navigation.tabs`, `content.action.edit`, `content.action.view`."
    - "`mkdocs.yml` plugins include `- social` (per-page social cards via mkdocs-material[imaging] — CairoSVG/pillow already installed)."
    - "Every page listed in nav has a corresponding file on disk (no dangling entries); `mkdocs build --strict` succeeds after this plan lands."
    - "The nav does NOT reference `docs/architecture.md`, `docs/security.md`, `docs/connectors/guide.md` (deletion happens in Wave 3 plan 30-08; nav must already be off those entries)."
    - "`docs/demo.md` remains present but is flagged `not_in_nav` pending its deletion in Wave 3 — this avoids `--strict` complaining about orphan page during the transition window."
  artifacts:
    - path: "mkdocs.yml"
      provides: "Restructured nav + theme features + social plugin"
      contains: "- social"
  key_links:
    - from: "mkdocs.yml nav"
      to: "docs/how-it-works/index.md"
      via: "nav entry 'Overview: how-it-works/index.md'"
      pattern: "how-it-works/index.md"
    - from: "mkdocs.yml nav"
      to: "docs/guides/write-your-own-connector.md"
      via: "nav entry under Guides"
      pattern: "guides/write-your-own-connector.md"
    - from: "mkdocs.yml nav"
      to: "docs/reference/simulator.md"
      via: "nav entry under Reference"
      pattern: "reference/simulator.md"
---

<objective>
Restructure `mkdocs.yml` to implement the locked IA sketch, tune the theme for the narrative voice, and add the social-cards plugin. After this plan lands:

- `mkdocs build --strict` passes end-to-end with the new nav.
- The navigation tree matches the IA sketch from `.planning/notes/phase-30-narrative-vignettes.md` §"Proposed nav" verbatim, with two ordering choices explicitly locked below.
- The theme visually signals "careful, deliberate product" — adding `navigation.footer` (prev/next at bottom of every page) and `navigation.tabs` (top-level section tabs) reinforces the Diátaxis structure; removing `navigation.expand` cleans up the sidebar.
- The `social` plugin generates per-page OpenGraph/Twitter cards — dependencies (`CairoSVG` 2.7.1 + `pillow` 10.4.0) are already present on the dev host and installed in CI via plan 30-01's `mkdocs-material[imaging]` upgrade.
- Edit/view source GitHub pencils appear on every page (`content.action.edit` + `content.action.view`).
- `docs/demo.md` is moved to `not_in_nav` so orphan-page warnings don't fail `--strict` during the transition window before Wave 3 deletes it.

Purpose: DOCS-07 (nav restructure + Diátaxis) + DOCS-08 (theme tuning + social cards). These are independent of copy content (plan 30-03) and diagrams (plan 30-05) and tutorial (plan 30-06) — all four Wave-1 plans touch different files and can run in parallel.

Output: one modified file (`mkdocs.yml`) — ~30 lines of net diff.

**Locked decisions honored:**
- Nav structure — verbatim from source-of-truth lines 296-345 (Home / Why / Mental model / vs-MCP / Tutorial / How it works / Guides / Reference / Decisions / Research / Development).
- Markdown-native landing (no Jinja override) per RESEARCH.md §"Landing-page template decision" — `navigation.instant` stays enabled.
- Color palette (deep purple + amber) unchanged per RESEARCH.md §"mkdocs-material Theme Tuning".
- Social plugin uses `default/variant` layout (no custom color override this phase — defer to post-launch iteration).
- `docs/demo.md` handling: `not_in_nav` this plan; Wave 3 plan 30-08 deletes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-docs-ia-and-narrative-overhaul-landing-page-aha-moment-and-p/CONTEXT.md
@.planning/phases/30-docs-ia-and-narrative-overhaul-landing-page-aha-moment-and-p/30-RESEARCH.md
@.planning/phases/30-docs-ia-and-narrative-overhaul-landing-page-aha-moment-and-p/30-PATTERNS.md
@.planning/notes/phase-30-narrative-vignettes.md

@mkdocs.yml

<interfaces>
Existing mkdocs-material configuration contracts (RESEARCH.md §"mkdocs-material Theme Tuning"):

- `theme.features` is a list; order does not matter but presence/absence does.
- `theme.palette` controls light/dark schemes; unchanged from current.
- `plugins` order DOES matter — `social` must come after `search` but before `minify`. Per mkdocs plugin-pipeline semantics.
- `nav` is an ordered list of dicts; top-level keys become sidebar section headers when `navigation.sections` is enabled.
- `not_in_nav` accepts glob patterns; current config uses `archive/*`.

Dependencies already present on the dev host (verified 2026-04-17 by listing Python packages):
- CairoSVG 2.7.1
- pillow 10.4.0
- mkdocs-material 9.7.1

CI picks up `mkdocs-material[imaging]` via plan 30-01's workflow edit.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mkdocs.yml` edit -> `mkdocs build --strict` | A malformed YAML or a dangling nav entry fails the build, blocking CI. |
| `social` plugin -> published site | Generates per-page OpenGraph/Twitter card images; images are public. No PII risk in titles/descriptions (they mirror page H1s + frontmatter). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Denial of Service | Nav entry references missing file | mitigate | Every nav entry is cross-checked against plan 30-02's skeleton output (14 new files); acceptance criteria runs `mkdocs build --strict` before done. |
| T-30-04-02 | Tampering | YAML syntax error breaks `--strict` | mitigate | Run `python -c "import yaml; yaml.safe_load(open('mkdocs.yml'))"` before commit. |
| T-30-04-03 | Information Disclosure | Social cards pipeline leaks internal paths / env data | accept | Card generation reads only page H1 + frontmatter description; no env read. |
| T-30-04-04 | Elevation of Privilege | `content.action.edit` points at repo main branch | accept | `edit_uri: edit/main/docs/` is already configured; pencils link to GitHub edit UI which enforces authentication. No new risk. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite mkdocs.yml with new nav + theme tuning + social plugin</name>
  <files>mkdocs.yml</files>
  <read_first>
    - `mkdocs.yml` (current 103 lines — full rewrite below; read to confirm palette + markdown_extensions + extra.social stay identical)
    - `.planning/phases/30-docs-ia-and-narrative-overhaul-landing-page-aha-moment-and-p/30-PATTERNS.md` §mkdocs.yml (lines 520-660 — target nav + theme features)
    - `.planning/phases/30-docs-ia-and-narrative-overhaul-landing-page-aha-moment-and-p/30-RESEARCH.md` §"mkdocs-material Theme Tuning — concrete diffs" (lines 800-898)
    - `.planning/notes/phase-30-narrative-vignettes.md` §"Proposed nav" (lines 296-345 — the LOCKED IA)
  </read_first>
  <action>
    Replace the ENTIRE contents of `mkdocs.yml` with the following YAML. All elements are deliberate; do NOT edit without updating one of the source documents above first.

```yaml
site_name: reposix
site_description: Common tracker ops for autonomous agents — cat instead of curl, git push instead of PATCH.
site_url: https://reubenjohn.github.io/reposix/
repo_name: reubenjohn/reposix
repo_url: https://github.com/reubenjohn/reposix
edit_uri: edit/main/docs/

theme:
  name: material
  palette:
    - scheme: default
      primary: deep purple
      accent: amber
      toggle:
        icon: material/brightness-7
        name: Switch to dark mode
    - scheme: slate
      primary: deep purple
      accent: amber
      toggle:
        icon: material/brightness-4
        name: Switch to light mode
  features:
    - navigation.instant
    - navigation.tracking
    - navigation.sections
    - navigation.tabs
    - navigation.footer
    - navigation.top
    - search.suggest
    - search.highlight
    - content.code.copy
    - content.code.annotate
    - content.tabs.link
    - content.action.edit
    - content.action.view
  icon:
    repo: fontawesome/brands/github

not_in_nav: |
  archive/*
  demo.md
  demo.transcript.txt
  demo.typescript
  connectors/*
  architecture.md
  security.md

plugins:
  - search
  - social
  - minify:
      minify_html: true

markdown_extensions:
  - pymdownx.highlight:
      anchor_linenums: true
      line_spans: __span
      pygments_lang_class: true
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format
  - pymdownx.inlinehilite
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.snippets
  - pymdownx.tasklist:
      custom_checkbox: true
  - pymdownx.caret
  - pymdownx.mark
  - pymdownx.tilde
  - pymdownx.details
  - admonition
  - attr_list
  - md_in_html
  - footnotes
  - toc:
      permalink: true

extra:
  social:
    - icon: fontawesome/brands/github
      link: https://github.com/reubenjohn/reposix

nav:
  - Home: index.md
  - Why reposix: why.md
  - Mental model: mental-model.md
  - reposix vs MCP / SDKs: vs-mcp-sdks.md
  - Try it in 5 minutes: tutorial.md
  - How it works:
      - Overview: how-it-works/index.md
      - The filesystem layer: how-it-works/filesystem.md
      - The git layer: how-it-works/git.md
      - The trust model: how-it-works/trust-model.md
  - Guides:
      - Connect to GitHub: guides/connect-github.md
      - Connect to Jira: guides/connect-jira.md
      - Connect to Confluence: guides/connect-confluence.md
      - Write your own connector: guides/write-your-own-connector.md
      - Integrate with your agent: guides/integrate-with-your-agent.md
      - Troubleshooting: guides/troubleshooting.md
  - Reference:
      - Crates overview: reference/crates.md
      - CLI: reference/cli.md
      - HTTP API (simulator): reference/http-api.md
      - Simulator: reference/simulator.md
      - git remote helper: reference/git-remote.md
      - Confluence backend: reference/confluence.md
  - Decisions:
      - ADR-001 GitHub state mapping: decisions/001-github-state-mapping.md
      - ADR-002 Confluence page mapping: decisions/002-confluence-page-mapping.md
      - ADR-003 Nested mount layout: decisions/003-nested-mount-layout.md
  - Research:
      - Architectural argument: research/initial-report.md
      - Agentic engineering reference: research/agentic-engineering-reference.md
  - Development:
      - Contributing: development/contributing.md
      - Roadmap (v0.9+): development/roadmap.md
```

Key changes relative to the existing `mkdocs.yml`:

**site_description:** changed from the old "Git-backed FUSE filesystem…" (which leaked P2 terms into the `<meta>` description used by social cards) to the new "Common tracker ops for autonomous agents…" (Val Town positioning-line pattern D per RESEARCH.md).

**theme.features:** removed `navigation.expand`; added `navigation.tabs`, `navigation.footer`, `content.action.edit`, `content.action.view`.

**not_in_nav:** expanded to include `demo.md`, `demo.transcript.txt`, `demo.typescript`, `connectors/*`, `architecture.md`, `security.md` so `--strict` doesn't fail on orphans during the Wave 3 transition window.

**plugins:** added `- social` between `search` and `minify`.

**nav:** wholesale restructure — 11 top-level entries (was 9). `Architecture` + `Security` + `Demos` + `Connectors` removed; `Mental model` + `vs MCP / SDKs` + `Try it in 5 minutes` + `How it works` + `Guides` added; `Reference` expanded to include `Simulator`; `Development` roadmap bumped v0.2+ → v0.9+.

**NOT changed:** palette (deep purple + amber), markdown_extensions (mermaid fence wiring preserved), extra.social (GitHub icon link).

After writing, verify YAML syntax and run the build:

```bash
python3 -c "import yaml; yaml.safe_load(open('mkdocs.yml'))"
# expected: no output, exit 0

mkdocs build --strict
# expected: exit 0. All 14 new-page nav entries resolve to files (from plan 30-02).
# No orphan warnings because demo.md + connectors/* + architecture.md + security.md
# are in not_in_nav.
```

If `mkdocs build --strict` fails, the error message tells you which page is missing. Cross-check against plan 30-02's `files_modified` — every nav entry must match a file created there.

Also smoke-test the social plugin (first run caches fonts):

```bash
mkdocs build --strict 2>&1 | tee /tmp/mkdocs-30-04.log
# expected lines: "INFO - social: Generating X social cards"
# If it errors on fonts, add `sudo apt-get install -y fonts-roboto` to local setup.
```
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('mkdocs.yml'))" && grep -c '^  - social$' mkdocs.yml | grep -q '^1$' && grep -c 'navigation.footer' mkdocs.yml | grep -q '^1$' && grep -c 'navigation.tabs' mkdocs.yml | grep -q '^1$' && grep -c 'navigation.expand' mkdocs.yml | grep -q '^0$' && grep -c 'content.action.edit' mkdocs.yml | grep -q '^1$' && grep -c 'Mental model' mkdocs.yml | grep -q '^1$' && grep -c 'how-it-works/index.md' mkdocs.yml | grep -q '^1$' && ! grep -q 'Architecture: architecture.md' mkdocs.yml && ! grep -q 'Security: security.md' mkdocs.yml && mkdocs build --strict</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('mkdocs.yml'))"` exits 0 (valid YAML).
    - `mkdocs build --strict` exits 0 (no broken links, no missing files, no orphan warnings).
    - `grep -c '^  - social$' mkdocs.yml` returns `1` (social plugin registered).
    - `grep -c 'navigation.footer' mkdocs.yml` returns `1` (added).
    - `grep -c 'navigation.tabs' mkdocs.yml` returns `1` (added).
    - `grep -c 'navigation.expand' mkdocs.yml` returns `0` (removed).
    - `grep -c 'content.action.edit' mkdocs.yml` returns `1` (added).
    - `grep -c 'content.action.view' mkdocs.yml` returns `1` (added).
    - `grep -c 'Mental model' mkdocs.yml` returns `1` (new nav entry).
    - `grep -c 'vs MCP' mkdocs.yml` returns `1` (new nav entry).
    - `grep -c 'Try it in 5 minutes' mkdocs.yml` returns `1` (new nav entry).
    - `grep -c 'how-it-works/index.md' mkdocs.yml` returns `1` (section landing).
    - `grep -c 'Simulator:' mkdocs.yml` returns `1` (moved to Reference).
    - `grep -c 'Architecture: architecture.md' mkdocs.yml` returns `0` (old entry removed).
    - `grep -c 'Security: security.md' mkdocs.yml` returns `0` (old entry removed).
    - `grep -c 'Connectors:' mkdocs.yml` returns `0` (old section removed).
    - `grep -c 'Overview: demos/index.md' mkdocs.yml` returns `0` (old entry removed).
    - `grep -cE 'demo\.md' mkdocs.yml` returns `1` (appears only in `not_in_nav`, not in `nav:`).
    - `grep -cE '(FUSE|daemon|inode|kernel|syscall)' mkdocs.yml` returns `0` (P2 ban — site_description must not leak).
  </acceptance_criteria>
  <done>
    mkdocs.yml fully restructured. `mkdocs build --strict` is green. Social plugin installed and cards generating. `navigation.tabs` + `navigation.footer` present. site_description reframed to Val Town positioning-line. All 14 new pages from plan 30-02 resolve through nav. Orphan pages are `not_in_nav`'d pending Wave 3 deletion.
  </done>
</task>

</tasks>

<verification>
1. `mkdocs build --strict` exits 0.
2. `grep -E '^  - |^\s+-\s' mkdocs.yml` visually matches the locked nav from source-of-truth lines 296-345.
3. `python3 scripts/check_phase_30_structure.py` — the mkdocs-nav checks (social plugin present, navigation.footer present, old pages not in nav) now pass.
4. `mkdocs serve` locally on port 8000 — home page renders without JS errors. Sidebar shows 11 top-level entries. "How it works" expands to 4 sub-pages.
</verification>

<success_criteria>
- mkdocs.yml nav matches the locked IA sketch.
- Theme features updated for narrative signal (tabs, footer, edit pencils).
- Social plugin installed; cards generate on build.
- `mkdocs build --strict` green.
- No P2 banned term leaked into site_description.
</success_criteria>

<output>
After completion, create `.planning/phases/30-docs-ia-and-narrative-overhaul-landing-page-aha-moment-and-p/30-04-SUMMARY.md` documenting:
- The final nav tree (11 top-level entries listed)
- Theme features added/removed
- Whether social-card generation was successful (cite log line count)
- Any build warnings that were silenced via `not_in_nav`
</output>
</content>
