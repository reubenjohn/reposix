← [back to index](./index.md)

# Task 2: Create how-it-works section (index + 3 sub-pages with placeholder mermaid fences)

<task type="auto">
  <name>Task 2: Create how-it-works section (index + 3 sub-pages with placeholder mermaid fences)</name>
  <files>docs/how-it-works/index.md, docs/how-it-works/filesystem.md, docs/how-it-works/git.md, docs/how-it-works/trust-model.md</files>
  <read_first>
    - `.planning/phases/30-docs-ia-and-narrative-overhaul-landing-page-aha-moment-and-p/30-PATTERNS.md` §docs/how-it-works/index.md, §docs/how-it-works/filesystem.md, §docs/how-it-works/git.md, §docs/how-it-works/trust-model.md
    - `.planning/phases/30-docs-ia-and-narrative-overhaul-landing-page-aha-moment-and-p/30-RESEARCH.md` §Example 6 (canonical mermaid specs)
    - `docs/index.md` (lines 44-62 — grid-cards syntax reference)
    - `docs/architecture.md` (source material to be carved in Wave 2 — do NOT copy content yet)
    - `.planning/notes/phase-30-narrative-vignettes.md` §"IA sketch" (lines 309-345 — locked IA)
  </read_first>
  <action>
    Create `docs/how-it-works/index.md` with EXACT content (this page IS Layer 3 — Vale's ProgressiveDisclosure rule is exempted by the `[docs/how-it-works/**]` scope in `.vale.ini` from plan 30-01):

```markdown
# How reposix works

Under the hood, reposix is three pieces: a FUSE daemon that projects your tracker as a real filesystem, a git remote helper that turns pushes into API calls, and a sandboxed simulator that lets you run the whole thing offline. The three pages below open each of those, one at a time.

<div class="grid cards" markdown>

-   :material-folder-open: **[The filesystem layer](filesystem.md)**

    How `cat issues/PROJ-42.md` becomes an HTTP GET and back. Read path, write path, and filename validation as the security boundary.

-   :material-source-branch: **[The git layer](git.md)**

    How `git push` becomes a series of `PATCH`s, and how optimistic-concurrency conflicts land as text-file merge conflicts instead of silent 409s.

-   :material-shield-lock: **[The trust model](trust-model.md)**

    The lethal-trifecta scenario reposix lives inside (private data, untrusted input, egress) and the cuts — taint typing, outbound allowlist, append-only audit, bulk-delete cap.

</div>

!!! note "Stub — Wave 2 plan will tighten the bridge paragraph after the three sub-pages are carved from `docs/architecture.md` + `docs/security.md`."
```

Create `docs/how-it-works/filesystem.md` with EXACT content (Layer 3 — FUSE/kernel permitted in prose):

```markdown
# The filesystem layer

!!! note "Stub — Wave 2 carver plan lifts content from `docs/architecture.md` §Read path + §Write path + §The async bridge."

reposix exposes your tracker as a real POSIX filesystem via FUSE. A `cat` or `sed` in your shell becomes a `read`/`write` syscall, which becomes an HTTP call to your tracker, which becomes bytes back.

## Read path

Wave 2 fills.

\`\`\`mermaid
sequenceDiagram
  autonumber
  actor A as You (shell or agent)
  participant K as POSIX
  participant F as reposix
  participant R as Your tracker
  A->>K: cat issues/PROJ-42.md
  K->>F: read("PROJ-42.md")
  F->>R: GET /issue/PROJ-42
  R-->>F: issue JSON
  F-->>K: frontmatter + markdown bytes
  K-->>A: text
\`\`\`

## Write path

Wave 2 fills.
```

Create `docs/how-it-works/git.md` with EXACT content:

```markdown
# The git layer

!!! note "Stub — Wave 2 carver plan lifts content from `docs/architecture.md` §git push + §Optimistic concurrency as git merge."

`git push` is the central verb. A commit is a diff; reposix translates the diff into `PATCH`/`POST`/`DELETE` calls against your tracker's REST API. Optimistic concurrency — when two agents race on the same ticket — surfaces as an ordinary text-file merge conflict, not a silent 409.

## git push — the round trip

Wave 2 fills.

\`\`\`mermaid
flowchart LR
  A["Your commit<br/>status: Done"] -->|git push| B["reposix<br/>(diff + dispatch)"]
  B -->|PATCH + If-Match| C["Your tracker"]
  C -->|200 or 409| B
  B -->|success or conflict markers| A
\`\`\`

## Optimistic concurrency as git merge

Wave 2 fills.
```

Create `docs/how-it-works/trust-model.md` with EXACT content (preserve the color palette convention from PATTERNS.md §"Shared Patterns → Mermaid diagrams"):

```markdown
# The trust model

!!! note "Stub — Wave 2 carver plan lifts content from `docs/security.md` (full lethal-trifecta narrative + SG-01..08 table) and `docs/architecture.md` §Security perimeter."

Every deployment of reposix is a textbook lethal trifecta[^1]: private data (tracker contents), untrusted input (attacker-influenced issue bodies), and an exfiltration channel (`git push`). reposix cuts one leg architecturally (egress allowlist), hardens another (taint typing on ingress), and accepts the third (sanitize-on-egress) as incurable without giving up the core feature. The audit log is the ledger.

## Lethal trifecta (the threat model opener)

Wave 2 fills.

\`\`\`mermaid
flowchart LR
  subgraph Tainted["Tainted (attacker-influenced)"]
    BODY["Issue bodies"]
    TITLE["Titles / comments"]
  end
  subgraph Trusted["Trusted (yours)"]
    CORE["reposix core<br/>sealed HttpClient"]
    AUDIT["append-only<br/>SQLite audit log"]
  end
  subgraph Egress["Egress (allowlisted only)"]
    NET["HTTP to allowlisted origin"]
    DISK["Filesystem writes<br/>(mount point only)"]
  end
  BODY -.->|typed as Tainted&lt;T&gt;| CORE
  TITLE -.->|typed as Tainted&lt;T&gt;| CORE
  CORE -->|sanitize + allowlist| NET
  CORE --> AUDIT
  style Tainted fill:#d32f2f,stroke:#fff,color:#fff
  style Egress fill:#ef6c00,stroke:#fff,color:#fff
  style Trusted fill:#00897b,stroke:#fff,color:#fff
\`\`\`

## The cuts — allowlist, taint, audit, bulk-delete cap

Wave 2 fills — preserve the exact SG-01..08 rows from `docs/security.md` lines 21-30 per the "no new claims" constraint in PATTERNS.md §docs/how-it-works/trust-model.md.

[^1]: [Simon Willison, "The lethal trifecta for AI agents"](https://simonwillison.net/2025/Jun/16/the-lethal-trifecta/), revised April 2026.
```

Verify the mermaid fences render: run `mkdocs serve` locally, navigate to `http://127.0.0.1:8000/how-it-works/filesystem/`, and confirm the diagram renders without JS errors.
  </action>
  <verify>
    <automated>test -f docs/how-it-works/index.md && test -f docs/how-it-works/filesystem.md && test -f docs/how-it-works/git.md && test -f docs/how-it-works/trust-model.md && grep -c '```mermaid' docs/how-it-works/filesystem.md | grep -q '^1$' && grep -c '```mermaid' docs/how-it-works/git.md | grep -q '^1$' && grep -c '```mermaid' docs/how-it-works/trust-model.md | grep -q '^1$' && grep -q 'how-it-works/filesystem.md' docs/how-it-works/index.md && grep -q 'how-it-works/git.md' docs/how-it-works/index.md && grep -q 'how-it-works/trust-model.md' docs/how-it-works/index.md</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist.
    - Each of `filesystem.md`, `git.md`, `trust-model.md` contains exactly one `` ```mermaid `` fence.
    - `docs/how-it-works/index.md` contains grid-card links to all three sub-pages.
    - Running Vale on these four files exits 0 (exempt by `.vale.ini` scope).
    - The `trust-model.md` mermaid uses palette: `#d32f2f` (Tainted), `#ef6c00` (Egress), `#00897b` (Trusted).
    - `grep -c '^\[\^1\]:' docs/how-it-works/trust-model.md` returns `1` (lethal-trifecta footnote).
  </acceptance_criteria>
  <done>
    How-it-works section created with proper grid-cards index and three sub-pages each carrying one placeholder mermaid fence. Nav in plan 30-04 can reference these files safely.
  </done>
</task>
