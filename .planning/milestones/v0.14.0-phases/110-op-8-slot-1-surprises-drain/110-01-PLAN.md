---
phase: 110
plan: 01
type: docs
autonomous: true
wave: 1
depends_on: [P102, P103, P104, P105, P106, P107, P108, P109]
requirements: [D2-SURPRISES-01]
---

# Phase 110, Plan 01 — v0.14.0 Surprises Absorption (+2 reservation slot 1)

## Objective

Drain `.planning/milestones/v0.14.0-phases/SURPRISES-INTAKE.md` per CLAUDE.md OP-8. Each of
the 16 entries → RESOLVED | DEFERRED | WONTFIX with commit SHA or rationale. Write the F-K5
verifier honesty spot-check (`quality/dispatch/absorption-honesty-spot-check.md` four
clauses) over the v0.14.0 phases that closed without filing intake (P103/P106/P107/P108/P109)
plus a reasoned sample of filing phases. **No new Rust code.** Pure planning + docs + catalog.
Mirrors the v0.13.0 P87 precedent verbatim, swapping v0.13.0 → v0.14.0.

## Context

- @CLAUDE.md § "Operating Principles" OP-8 (the +2 phase practice + eager-resolution preference + verifier honesty check).
- @.planning/PRACTICES.md § OP-8 (long-form).
- @.planning/milestones/v0.14.0-phases/ROADMAP.md § "Phase 110".
- @.planning/milestones/v0.14.0-phases/SURPRISES-INTAKE.md (the file being drained — 16 entries).
- @quality/dispatch/absorption-honesty-spot-check.md (F-K5 four binding clauses).
- @.planning/phases/87-surprises-absorption/ (v0.13.0 precedent: plan, honesty-spot-check, catalog row).

## Tasks

### Task 1 — Catalog-first: mint `agent-ux/p110-surprises-absorption` row + verifier (T01)

**Type:** auto.
**Behavior:** Create `quality/gates/agent-ux/p110-surprises-absorption.sh` (fence-aware awk;
adapted from the P87 verifier; TERMINAL floor >= 10; writes
`quality/reports/verifications/agent-ux/p110-surprises-absorption.json`). Add row to
`quality/catalogs/agent-ux.json` with **status FAIL** (catalog-first: the row asserts the
EVENTUAL drained state), kind mechanical, cadence on-demand, blast_radius P2, full schema
field set mirroring the P87 row.

**Done criteria:** verifier executable; catalog row present + valid JSON; row is FAIL and
predates the drain. Commit: `chore(quality): P110 catalog-first — mint surprises-absorption verifier + row (FAIL)`.

### Task 2 — Drain SURPRISES-INTAKE.md (all 16 entries) + write F-K5 honesty spot-check (T02)

**Type:** auto.
**Behavior:**
1. Edit `.planning/milestones/v0.14.0-phases/SURPRISES-INTAKE.md` — every entry gets a
   terminal STATUS line (6 already-terminal P102/P113 rows verified against real SHAs; 10
   OPEN rows → RESOLVED/DEFERRED/WONTFIX per the coordinator dispositions).
2. Write `.planning/phases/110-op-8-slot-1-surprises-drain/honesty-spot-check.md` honoring the
   F-K5 four clauses (sample every zero-intake phase; author ≠ orchestrator; outcome-facing
   rubric; content hash-bound by the verifier).
3. Run verifier — must exit 0 (0 OPEN, >=10 terminal, honesty file present).

**Done criteria:** 0 OPEN; >=10 terminal; honesty spot-check present; verifier PASS.
Commit: `docs(planning): P110 OP-8 Slot 1 — drain SURPRISES-INTAKE to terminal status + F-K5 honesty spot-check`.

### Task 3 — Commit orphaned P106/P107 evidence (T03)

**Type:** auto.
**Behavior:** `git add` the untracked, closed-phase evidence artifacts
(`quality/reports/verifications/docs-repro/` from P106; the P107 verification MD) under
explicit paths — carried-ledger honesty closure.

**Done criteria:** evidence committed separately.
Commit: `chore(evidence): commit orphaned P106 docs-repro + P107 verification artifacts — P110 honesty closure`.

## Verification

- `bash quality/gates/agent-ux/p110-surprises-absorption.sh` exits 0.
- `grep -cE '^\*\*STATUS:\*\* (RESOLVED|DEFERRED|WONTFIX)'` (fence-aware) returns >=10.
- 0 `**STATUS:** OPEN` entries remain (fence-aware).

## Success criteria (mirrors ROADMAP P110)

1. Every entry in SURPRISES-INTAKE.md has terminal STATUS — RESOLVED | DEFERRED | WONTFIX.
2. F-K5 honesty spot-check sampled every zero-intake P102–P109 phase; report present, hash-bindable.
3. Catalog deltas: +1 row (`agent-ux/p110-surprises-absorption`, FAIL→PASS after drain).
4. No silent scope creep — deferrals name a target milestone + rationale; the HIGH
   push-correctness bug (fetch-ref lock) is surfaced to the owner for a dedicated debug phase.

## Hard constraints

- No new Rust code (pure planning + docs + catalog).
- Coordinator owns the push; executor commits locally only.
- `quality/catalogs/code.json` is FOREIGN-LOCKED — never touch/stage it.
- Schema-fence example in SURPRISES-INTAKE.md must NOT be counted as a real entry (verifier uses awk fence-tracking).
