---
plan: 25-01
wave: 1
phase: 25
title: "Cross-ref audit, root clutter cleanup, mkdocs.yml nav update"
depends_on: []
files_modified:
  - CLAUDE.md
  - mkdocs.yml
  - .planning/research/threat-model-and-critique.md
autonomous: true
requirements_addressed: []

must_haves:
  truths:
    - "CLAUDE.md Quick-links section references docs/research/ paths, not root stubs"
    - "mkdocs.yml nav contains a Research section with both moved docs"
    - ".planning/research/threat-model-and-critique.md references use the new docs/research/ paths"
    - "Root stubs InitialReport.md and AgenticEngineeringReference.md still exist as visible redirect notes"
    - "No .md file outside docs/research/ or root stubs contains a bare InitialReport.md or AgenticEngineeringReference.md link"
  artifacts:
    - path: "mkdocs.yml"
      provides: "nav entry for docs/research/initial-report.md and docs/research/agentic-engineering-reference.md"
      contains: "Research"
    - path: "CLAUDE.md"
      provides: "Quick links pointing at docs/research/ paths"
      contains: "docs/research/initial-report.md"
  key_links:
    - from: "mkdocs.yml nav"
      to: "docs/research/initial-report.md"
      via: "Research section"
      pattern: "initial-report\\.md"
    - from: "CLAUDE.md §Quick links"
      to: "docs/research/"
      via: "updated path"
      pattern: "docs/research/initial-report\\.md"
---

<objective>
Update all cross-references to the two moved research docs, add them to the mkdocs.yml nav, and verify the root redirect stubs are visible markdown (not invisible HTML comments).

Purpose: The file moves are already done; lingering old-path references and a missing mkdocs nav entry are the only open items.
Output: Updated CLAUDE.md, mkdocs.yml, and .planning/research/threat-model-and-critique.md; root stubs contain visible markdown redirect links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix all cross-references to moved docs</name>
  <files>CLAUDE.md, .planning/research/threat-model-and-critique.md</files>
  <read_first>
    - CLAUDE.md (full file — check §Quick links and §Project elevator pitch)
    - .planning/research/threat-model-and-critique.md (full file — has ~15 bare `AgenticEngineeringReference.md` and `InitialReport.md` references)
    - InitialReport.md (confirm it contains a visible markdown redirect note, not an HTML comment)
    - AgenticEngineeringReference.md (same check)
  </read_first>
  <action>
    Step 1 — Verify root stubs are visible markdown redirects. Both `InitialReport.md` and `AgenticEngineeringReference.md` must start with a visible blockquote redirect link, e.g.:
    ```
    > This document has moved to [docs/research/initial-report.md](docs/research/initial-report.md).
    ```
    If either stub is an invisible HTML comment (`<!-- Moved to ... -->`), replace it with the visible blockquote form above. Both are confirmed visible from context, so this is a verification step only.

    Step 2 — Update CLAUDE.md. The §"Project elevator pitch" line already reads `docs/research/initial-report.md` and `docs/research/agentic-engineering-reference.md`. The §"Quick links" section (around lines 131–132) must also reference `docs/research/initial-report.md` and `docs/research/agentic-engineering-reference.md`. If either quick-link still says `InitialReport.md` or `AgenticEngineeringReference.md` (bare root paths), update them to the `docs/research/` paths.

    Step 3 — Update `.planning/research/threat-model-and-critique.md`. This file has roughly 15 citations of bare `AgenticEngineeringReference.md` and `InitialReport.md`. These are informal citation markers inside body text (not hyperlinks), e.g. `AgenticEngineeringReference.md §5`. Update every occurrence to use the new paths:
    - `AgenticEngineeringReference.md` → `docs/research/agentic-engineering-reference.md`
    - `InitialReport.md` → `docs/research/initial-report.md`
    Preserve the `§section` annotations that follow these references.

    Step 4 — Verify README.md already uses the correct paths (confirmed: line 30 references `docs/research/initial-report.md`). No change needed.

    Step 5 — Run a final audit to confirm no remaining bare root-path references:
    ```bash
    grep -rn "InitialReport\.md\|AgenticEngineeringReference\.md" \
      --include="*.md" \
      /home/reuben/workspace/reposix/ \
      --exclude-dir=target \
      --exclude-dir=site \
      | grep -v "^/home/reuben/workspace/reposix/InitialReport\.md:" \
      | grep -v "^/home/reuben/workspace/reposix/AgenticEngineeringReference\.md:"
    ```
    Any lines output here are remaining cross-ref bugs to fix. Fix them before proceeding.
  </action>
  <verify>
    <automated>
      cd /home/reuben/workspace/reposix && grep -rn "InitialReport\.md\|AgenticEngineeringReference\.md" --include="*.md" --exclude-dir=target --exclude-dir=site . | grep -v "^\./InitialReport\.md:" | grep -v "^\./AgenticEngineeringReference\.md:" | wc -l
    </automated>
    Expected output: 0 (zero remaining bare cross-refs outside the stub files themselves).
  </verify>
  <acceptance_criteria>
    - `grep -c "docs/research/initial-report.md" CLAUDE.md` >= 2 (elevator pitch + quick links)
    - `grep -c "docs/research/agentic-engineering-reference.md" CLAUDE.md` >= 2
    - `grep -c "docs/research/" .planning/research/threat-model-and-critique.md` >= 15
    - `grep -c "InitialReport\.md\|AgenticEngineeringReference\.md" .planning/research/threat-model-and-critique.md` = 0
    - Root stubs `InitialReport.md` and `AgenticEngineeringReference.md` each contain a visible blockquote redirect (not an HTML comment)
  </acceptance_criteria>
  <done>All cross-references to the two moved docs updated to docs/research/ paths; no bare root-path references remain outside the stub files.</done>
</task>

<task type="auto">
  <name>Task 2: Add Research section to mkdocs.yml nav + verify build</name>
  <files>mkdocs.yml</files>
  <read_first>
    - mkdocs.yml (full file — need current nav structure to find insertion point)
    - docs/research/initial-report.md (confirm file exists and has a title heading)
    - docs/research/agentic-engineering-reference.md (same)
  </read_first>
  <action>
    Insert a `Research:` section in the mkdocs.yml `nav:` block. Place it after the `Decisions:` section and before `Reference:`. The exact nav entry to add:

    ```yaml
        - Research:
            - Architectural argument: research/initial-report.md
            - Agentic engineering reference: research/agentic-engineering-reference.md
    ```

    Note: mkdocs nav paths are relative to the `docs/` source directory, so `research/initial-report.md` (not `docs/research/initial-report.md`).

    After editing, verify mkdocs.yml is valid YAML:
    ```bash
    python3 -c "import yaml; yaml.safe_load(open('mkdocs.yml'))" && echo OK
    ```

    Then attempt a mkdocs build to confirm no broken nav references:
    ```bash
    cd /home/reuben/workspace/reposix && python3 -m mkdocs build --strict 2>&1 | tail -20
    ```
    If `mkdocs` is not installed, skip the build check and note it in the task output. The YAML validity check is mandatory.
  </action>
  <verify>
    <automated>
      cd /home/reuben/workspace/reposix && grep -c "research/initial-report.md\|research/agentic-engineering-reference.md" mkdocs.yml
    </automated>
    Expected output: 2 (one entry per file).
  </verify>
  <acceptance_criteria>
    - `grep -q "Research" mkdocs.yml` exits 0
    - `grep -q "research/initial-report.md" mkdocs.yml` exits 0
    - `grep -q "research/agentic-engineering-reference.md" mkdocs.yml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('mkdocs.yml'))"` exits 0 (valid YAML)
  </acceptance_criteria>
  <done>mkdocs.yml nav includes a Research section with both moved docs; YAML is valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → docs | All changes are to documentation files; no trust boundary crossings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Tampering | mkdocs.yml nav | accept | Docs-only change; no code execution path |
</threat_model>

<verification>
After both tasks complete:
1. `grep -rn "InitialReport\.md\|AgenticEngineeringReference\.md" --include="*.md" --exclude-dir=target --exclude-dir=site . | grep -v "^\./InitialReport\.md:\|^\./AgenticEngineeringReference\.md:"` → 0 lines
2. `grep -c "docs/research/initial-report.md" CLAUDE.md` → ≥ 2
3. `grep -q "Research" mkdocs.yml && echo OK` → OK
4. `python3 -c "import yaml; yaml.safe_load(open('mkdocs.yml'))"` → exits 0
</verification>

<success_criteria>
- All cross-references point to docs/research/ paths
- mkdocs.yml contains a Research nav section with both docs
- Zero bare root-path links remain outside the stub files
- YAML validity confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/25-op-11-docs-reorg-initialreport-md-and-agenticengineeringrefe/25-01-SUMMARY.md`
</output>
