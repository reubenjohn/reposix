# v0.12.1 — phases archive

**Status:** in flight (started 2026-04-28).

**Scope:** Carry-forwards from v0.12.0 (MIGRATE-03 items), docs-alignment cleanup cluster (P72+ closing the punch-list surfaced by P65), coverage_ratio metric (P66), perf full implementation, security-dim stubs→real, cross-platform, MSRV/binstall, subjective-runner.

**Source-of-truth narrative:**
- `.planning/milestones/v0.12.1-phases/{ROADMAP.md, REQUIREMENTS.md}` — milestone-level scoping.
- `quality/reports/doc-alignment/backfill-20260428T085523Z/PUNCH-LIST.md` — P65 surfaced cluster list driving P72+.
- `quality/reports/verdicts/p<N>/VERDICT.md` — per-phase verifier verdicts (Path A or B per phase).

**Per-phase contributions (split out from `.planning/STATE.md`):**

### P66 — coverage_ratio metric (added 2026-04-28)

- **P66 SHIPPED (2026-04-28, v0.12.1):** coverage_ratio metric — docs-alignment second axis. The dimension grades along TWO axes now: `alignment_ratio` (claims_bound / non-retired) answers "of the claims we extracted, how many bind to passing tests?"; `coverage_ratio` (lines_covered / total_eligible_lines) answers "of the prose we said we'd mine, what fraction did we actually cover?". Together they yield the agent's 2x2 mental model. **Catalog-first commit `0b1ee6f`**: v0.12.1 ROADMAP renumbered P66-P70 -> P67-P71 (mechanical bump per P64+P65 v0.12.0 precedent); new P66 entry inserted with goal + 5 success criteria; COVERAGE-01..05 added to v0.12.1 REQUIREMENTS.md (traceability bumped 13 -> 18 reqs across >= 5 phases); phase dir `.planning/phases/66-coverage-ratio/` with CONTEXT.md (10 design decisions D-01..D-10 including no-waiver-re-add invariant) + 66-01-PLAN.md (6-commit plan with merge_ranges semantics, multi-source attribution, out-of-eligible warn+skip, walker BLOCK, status verb per-file table, measured-baseline capture). **Code commit `83dd3c7`**: Summary struct gains 4 fields with `#[serde(default)]` back-compat (coverage_ratio + lines_covered + total_eligible_lines + coverage_floor; default_coverage_floor() -> 0.10). NEW module `crates/reposix-quality/src/coverage.rs` (310 LOC; 6 public fns + PerFileCoverage struct + 8 inline unit tests for merge_ranges covering overlap/adjacent/disjoint/single/unsorted/full-overlap/empty/global-empty). `eligible_files()` mirrors the chunker's `collect_backfill_inputs` (single point of update). `merge_ranges` folds overlapping AND adjacent inclusive 1-based ranges. `compute_per_file` emits stderr warnings (deduped by `(row_id, file)`) for rows whose source.file is outside the eligible set; their lines NOT counted (forensic signal). Output sorted ascending by ratio so worst-covered surfaces first — agent's gap-target view. `compute_global` empty-state semantics (0,0,0.0) — differs from alignment_ratio's 1.0 by design (cannot claim full coverage when there is nothing to cover). Walker integration: AFTER recompute_summary, populates lines_covered + total_eligible_lines + coverage_ratio. NEW BLOCK condition: `coverage_ratio < coverage_floor` pushes structured stderr line naming `/reposix-quality-backfill` as recovery. Walker NEVER auto-tunes coverage_floor. `crates/reposix-quality/tests/coverage.rs` ships 10 integration tests; process-wide `cwd_lock` Mutex serializes CWD mutations because cargo test runs in parallel. `crates/reposix-quality/tests/walk.rs` gains `coverage_floor: 0.0` in synthetic seed catalog so temp-dir-rooted walker tests don't trip the new BLOCK on unrelated data (Rule 1 fix). **Status verb commit `76eeb6c`** (also folded Commit D after the orchestrator's hook-fix-commit `45b6526` interleaving): Verb::Status enum gains `--top N` (default 20) + `--all` flags; `--json` extended to emit `{ global: {...}, per_file: [...] }` with per_file sorted ascending by ratio. Table mode shows TWO sections: `== global ==` (both axes with floors + numerator/denominator detail); `== per-file (worst-covered first) ==` aligned columns; ZERO ROWS hint when `row_count==0 && total_lines>50`; `... (N more; use --all)` truncation footer. quality/catalogs/README.md gains "Two metrics: alignment + coverage" subsection with 2x2 matrix + coverage_floor ratchet semantics + eligible-set definition + out-of-eligible warning shape. CLAUDE.md gains new "v0.12.1 — in flight" H2 + "P66 — coverage_ratio metric" H3 (~22 lines under 30 cap). Banned-words clean (no NEW "replace" matches). **Workspace cargo gates**: cargo check / clippy --all-targets -D warnings / fmt --check / test --workspace ALL clean (single sequential pass per memory budget). **Verifier verdict commit `6512a70`** (Path B in-session per P56-P64 precedent): GREEN across all 5 COVERAGE-* success criteria with primary-source evidence (file:line refs + command output captured verbatim); 18 coverage-specific tests pass (10 integration + 8 unit); 12 out-of-eligible warnings observed in live walk output; live walk exits non-zero with structured stderr; ZERO ROWS hint observed on docs/reference/crates.md; suspicion-of-haste spot-checks 3 cells (merge_ranges adjacent fold, multi-source attribution, out-of-eligible dedup) all align with declared behavior. **Measured baseline against the LIVE 388-row catalog**: alignment_ratio 0.4407 (171 bound / 388 non-retired) / alignment_floor 0.5000 (BLOCKING — intended); coverage_ratio 0.2055 (1132 covered / 5508 total eligible lines) / coverage_floor 0.1000 (PASS — baseline for ratchet). 46 eligible files; worst 4 at 0.000 ratio (docs/connectors/guide.md 9 lines, docs/reference/crates.md 147 lines ZERO ROWS, docs/security.md 9 lines, docs/why.md 12 lines) — concrete v0.12.1 P72+ mining targets. **Pre-push exit non-zero on docs-alignment/walk is INTENDED** for v0.12.1 — gate is hard until cluster phases (P72+) close enough rows AND/OR widen coverage. NO floor_waiver re-added. NO docs-alignment/walk waiver re-added. The freshness-invariants.json + doc-alignment.json owner-pre-existing waiver-removal changes preserved. **v0.12.1 carry-forwards filed**: symmetric `coverage_floor`-monotone audit row (parallel to existing `structure/doc-alignment-floor-not-decreased`); eligible-set drift consolidation between coverage::eligible_files and chunker's collect_backfill_inputs (currently duplicated); out-of-eligible warning batch reduction (12 warnings every walk run waiting to be acted on by P72+). 5 requirements flipped to shipped (P66): COVERAGE-01..05. v0.12.1 phase counter advances P66 -> P67+ (perf-dim full implementation moves to P67; security-dim stubs->real to P68; cross-platform to P69; MSRV/binstall to P70; subjective-runner to P71; doc-alignment gap-closure cluster phases at P72+). Wall-clock total: scope (10 min) + code+tests (15 min) + status verb + schema + CLAUDE.md (10 min) + workspace cargo (5 min) + verdict (5 min) = ~45 min P66.
