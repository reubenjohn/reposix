# Archived: SESSION-END-STATE artifacts

These three files (`SESSION-END-STATE.md`, `SESSION-END-STATE.json`,
`SESSION-END-STATE-VERDICT.md`) are the historical record of the §0.8
SESSION-END-STATE framework that v0.12.0 superseded.

**Supersession:** see `quality/PROTOCOL.md` — the autonomous-mode runtime
contract (catalog rows + verifier subagent grading per phase close + the
`quality/runners/run.py --cadence <c>` interface) replaces the §0.8
session-end-state ritual.

These files are kept here for forensic reference (git history, audit
trails) but are no longer read by any active workflow.

Relocated from `.planning/` top level by P62 Wave 3 (rec #31 of the
repo-organization-gaps audit closure at
`quality/reports/audits/repo-org-gaps.md`).
