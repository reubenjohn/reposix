# Rubric: subjective/install-positioning

**Phase:** P61 Wave C scaffold (this stub).

**Implementation phase:** Wave E (61-05).

This stub is a placeholder so `dispatch.sh` can resolve the path. Wave E rewrites this file with the verbatim install-positioning prompt body that checks README.md + docs/index.md hero install snippets lead with `cargo binstall` / `brew install` / `cargo install` (NOT `git clone && cargo build`) per CLAUDE.md freshness invariants.

Numeric scoring: 10 = leads with package manager + cites supported targets + names binstall fallback; 5 = leads with package manager but missing target list; 2 = leads with git-clone+build. PASS threshold >=7.

See `.planning/phases/61-subjective-gates-skill-freshness-ttl/61-05-PLAN.md` for the prompt design.
