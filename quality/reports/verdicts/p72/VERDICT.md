# P72 Verdict — lint-config invariants

**Verifier:** unbiased subagent (Path A, top-level orchestrator dispatch)
**Phase contract:** PLAN.md + CONTEXT.md D-01..D-10
**Verdict:** GREEN
**Graded:** 2026-04-29 13:10 PT (2026-04-29 20:10 UTC)

## Dimension scores

| #  | Dimension                                    | Score   | Evidence                                                                                                                                                                                                                |
| -- | -------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | 9 catalog rows BOUND                         | COVERED | `jq` over `quality/catalogs/doc-alignment.json` confirms all 9 named LINT-CONFIG row ids show `last_verdict: "BOUND"` and `tests[0]` under `quality/gates/code/lint-invariants/`.                                       |
| 2  | 8 verifier files exit 0                      | COVERED | All 8 `.sh` scripts under `quality/gates/code/lint-invariants/` are executable and exited 0 on the current workspace: `demo-script-exists`, `forbid-unsafe-code` (PASS: 14 entry points), `rust-msrv`, `rust-stable-channel`, `cargo-check-workspace`, `errors-doc-section` (0 hits), `cargo-test-count` (368 binaries / floor 368), `tests-green`. |
| 3  | D-06 test-count prose pre-bind               | COVERED | `docs/development/contributing.md:20` reads `>= 368 test binaries`; verifier floor = 368. Live `cargo test --workspace --no-run` produced 402 compiler-artifact events deduped to 368 unique `target.name`s by the verifier (matches floor). Commit `bc6e7f6` (re-measure + sync prose) PRECEDES bind commit `14f8224`. README.md cites no test count (verified via `grep -nE '[0-9]+ tests?'`). |
| 4  | D-01 verifier-sharing                        | COVERED | Both `README-md/forbid-unsafe-code` and `docs-development-contributing-md/forbid-unsafe-per-crate` reference the SAME path `quality/gates/code/lint-invariants/forbid-unsafe-code.sh`.                                  |
| 5  | Catalog-first commit                         | COVERED | First P72 commit `3a1bd0c P72(catalog-first): scaffold quality/gates/code/lint-invariants/ stubs (LINT-CONFIG-01..09)` lands stubs + sub-area README + BEFORE snapshot before any implementation. The 9 row binds (`14f8224`) land 11 commits later, after all 8 verifier implementations. |
| 6  | Atomic commits (one/task)                    | COVERED | `git log --oneline` shows exactly 14 P72 commits, each citing `LINT-CONFIG-NN` (or `LINT-CONFIG-01..09` for cross-cutting tasks). One commit per task per the plan; no squash; no fold beyond the planned eager-fix on `crates/reposix-sim/src/main.rs` folded transparently into commit `181a0fa` (D-09 / OP-8). |
| 7  | CLAUDE.md update + banned-words              | COVERED | `### P72 — Lint-config invariants` H3 lives at CLAUDE.md:327-342 (16 lines, well under 30-line cap), correctly nested under `## v0.12.1 — in flight` H2 at line 325. Names verifier home, lists all 8 verifiers + 9 rows in a table, cross-refs `quality/PROTOCOL.md` (Principle A) and CLAUDE.md "Build memory budget" (D-04). `bash scripts/banned-words-lint.sh` exited 0; manual grep over the new H3 found no banned vocabulary (`FUSE`, `partial-clone`, `replace`, etc.). |
| 8  | OP-8 honesty check                           | COVERED | 1 LOW SURPRISES entry appended to `.planning/milestones/v0.12.1-phases/SURPRISES-INTAKE.md` (2026-04-29 19:58) — properly shaped per OP-8 with severity, discovered-by, what, why-out-of-scope, sketched resolution, STATUS=OPEN. The eager-fix on `crates/reposix-sim/src/main.rs` (missing `#![forbid(unsafe_code)]`) is documented in commit `181a0fa` body and SUMMARY.md "+2 phase practice (OP-8) audit trail" section — < 5 files, no new dep, folded into the discovering task. The intake item (2 unrelated rows flipping STALE_DOCS_DRIFT mid-walk) genuinely sits outside P72's planned modification set; honest scope-boundary call. No evidence of skipped findings. |
| 9  | alignment_ratio delta plausibility           | COVERED | `summary-before.json`: bound=313, missing=22, ratio=0.8743. `summary-after.json`: bound=320, missing=13, ratio=0.8938. Δmissing=−9 matches the 9 P72 rows exactly. Δbound=+7 instead of +9 is honestly explained in `delta.txt` and SUMMARY.md: 2 pre-existing BOUND rows (polish-03-mermaid-render, cli-subcommand-surface) flipped to STALE_DOCS_DRIFT during the post-bind walk because their cited sources had drifted earlier, and P72's walk was the first to detect it. Both rows cite files OUTSIDE P72's modification set; no surprise retire-promotions inside this phase. |
| 10 | No prohibited actions                        | COVERED | `git log origin/main..HEAD` shows 14 local P72 commits on `main`; no `git push` evident (commits are ahead of origin). `git tag --contains 3a1bd0c` returns empty — no tag landed. No `cargo publish` invocation in any commit. No `confirm-retire` commit in the P72 range. Per HANDOVER-v0.12.1.md: local commits only — honored.            |

## Findings

No AMBER or RED dimensions. A few low-signal observations recorded for the orchestrator's awareness, none of which downgrade the verdict:

- The 14 P72 commits remain local on `main` (not pushed). This is consistent with HANDOVER-v0.12.1.md's local-only constraint and the success-criteria item #12 in PLAN.md. Push timing is the orchestrator's call, not a phase blocker.
- `claims_bound` rose by +7 (not +9) net because 2 unrelated pre-existing BOUND rows hit STALE_DOCS_DRIFT during the post-bind walk. This is faithfully documented in `delta.txt`, the SURPRISES-INTAKE.md entry, and SUMMARY.md. The surprises entry is correctly shaped (LOW severity, P76 to drain) and represents honest scope-boundary observance rather than a P72 regression.
- Live `cargo test --workspace --no-run --message-format=json` emits 402 `compiler-artifact` events; the verifier dedupes by `target.name` to 368 unique test binaries — matching prose. The `docs/development/contributing.md:20` line reads "≥ 368 test binaries", a deliberate semantic shift from "133 tests" (#[test] fns) to test-binary count, called out in SUMMARY.md and the cargo-test-count.sh docstring (D-06). Reasonable, defensible.
- The CLAUDE.md banned-words enforcer (`scripts/banned-words-lint.sh`) only scans `docs/`, not `CLAUDE.md` itself. SUMMARY.md's "banned-words lint passed" claim is therefore a manual grep assertion (verified: no banned vocabulary in the new H3). No hidden gap; just noting the lint's scope for the next agent.

## Recommendations to orchestrator

Phase ready to close. **Advance to P73 (connector-contract rows).** No fixes required from the executing agent. The single LOW SURPRISES entry is correctly deferred to P76 per OP-8.
