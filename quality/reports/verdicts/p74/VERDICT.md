# P74 Verdict — narrative cleanup + UX bindings + linkedin prose fix

**Verifier:** unbiased subagent (Path A — Task tool, zero session context)
**Phase contract:** PLAN.md must_haves + CONTEXT.md D-01..D-12 + REQUIREMENTS.md NARRATIVE-RETIRE-01..04 + UX-BIND-01..05 + PROSE-FIX-01
**Verdict:** GREEN
**Graded:** 2026-04-29 (autonomous-run)

## Dimension scores

| #  | Dimension | Status | Evidence |
|----|---|---|---|
| 1  | 5 catalog rows BOUND | COVERED | `jq` over `quality/catalogs/doc-alignment.json` shows all 5 named UX rows with `last_verdict: BOUND` and a single `tests` entry pointing to the matching `quality/gates/docs-alignment/*.sh` script: `5-line-install→install-snippet-shape.sh`, `audit-trail-git-log→audit-trail-git-log.sh`, `tested-three-backends→three-backends-tested.sh`, `polish2-06-landing→connector-matrix-on-landing.sh`, `spaces-01→cli-spaces-smoke.sh`. status-after.txt confirms `claims_missing_test 9 → 0` (the -5 plus the -4 retires). |
| 2  | 4 catalog rows RETIRE_PROPOSED with D-09 identical-format rationale | COVERED | All 4 narrative rows carry `last_verdict: RETIRE_PROPOSED` with `rationale` exactly matching the D-09 template `{narrative_id} — qualitative design framing; no behavioral assertion possible. Decided 2026-04-29.` (verified verbatim for all 4 ids: `use-case-20-percent-rest-mcp`, `use-case-80-percent-routine-ops`, `mcp-fixture-synthesized-not-live`, `mcp-schema-discovery-100k-tokens`). All 4 set `next_action: RETIRE_FEATURE`, `last_extracted_by: propose-retire-call`. Note: SUMMARY references `proposed_retire_rationale` field, but live schema stores it under `rationale` — informational, not a defect. |
| 3  | 5 verifier scripts exit 0 against current workspace | COVERED | Ran each script in cwd `/home/reuben/workspace/reposix`: install-snippet-shape.sh exit 0 ("PASS: docs/index.md:19 advertises curl|brew|cargo binstall|irm"); audit-trail-git-log.sh exit 0 ("PASS: git log --oneline shows >=1 commit"); three-backends-tested.sh exit 0 ("PASS: 3 dark_factory_real_* fns present"); connector-matrix-on-landing.sh exit 0 ("PASS: docs/index.md has connector/backend heading + table row"); cli-spaces-smoke.sh exit 0 ("PASS: reposix spaces --help exits 0 with expected header"). The cli-spaces-smoke run depended on a pre-built `target/release/reposix` AND audit-trail required non-shallow git history — both preconditions were met in the workspace. |
| 4  | D-08 prose fix at linkedin.md:21 | COVERED | Line 21 reads `🚀 reposix — a working git-native partial clone + git-remote-helper for REST issue trackers...`. Repo-wide `grep -i "FUSE filesystem" docs/social/linkedin.md` returns zero matches. The rewrite preserves the rest of the sentence shape (the 89.1% benchmark figure, "simulator vs real" hedge) so the existing token-reduction-92pct claim stays anchored to the same line range. |
| 5  | Linkedin STALE_DOCS_DRIFT honestly disclosed | COVERED | The linkedin row `docs/social/linkedin/token-reduction-92pct` carries `last_verdict: STALE_DOCS_DRIFT` post-walk with `source_hash: 1a19b86e...` (the post-edit hash). SURPRISES-INTAKE.md entry `2026-04-29 20:55 / LOW` documents that the row failed to auto-heal on a second walk — explicitly tagged "consistent with the P75 hash-overwrite bug" and routed to P75 as the fix owner. SUMMARY.md row-transitions table lists this as the 1 BOUND→STALE_DOCS_DRIFT side-effect. The status-after.txt summary block shows `claims_bound 328` (= 324 BEFORE + 5 binds − 1 linkedin tip) which mathematically corroborates the honest disclosure. NOT silently masked. |
| 6  | D-09 identical-format rationale | COVERED | All 4 rationales match `{narrative_id} — qualitative design framing; no behavioral assertion possible. Decided 2026-04-29.` byte-for-byte (per `jq` extract above). The narrative_id token in each rationale matches the row's id token. Format consistency satisfies D-09's "same one-liner for all 4 narrative retires for consistency" intent. |
| 7  | D-10 NO new files in `crates/` | COVERED | `git diff a973063^..HEAD --stat -- crates/` returns empty output. The full P74 changeset (15 files) touches `quality/`, `docs/social/linkedin.md`, `CLAUDE.md`, `scripts/p74-bind-ux-rows.sh`, `.planning/`, and `quality/reports/verdicts/p74/` — zero `crates/` files. Phase is shell + prose + catalog + planning only, as D-10 requires. |
| 8  | D-11 CLAUDE.md update + banned-words | COVERED | CLAUDE.md lines 362-388 contain `### P74 — Narrative cleanup + UX bindings + linkedin prose fix` H3 under `## v0.12.1 — in flight` (line 325). Body is 27 lines (≤30 cap per D-11). Names all 5 verifiers + 4 retires + linkedin edit + the 3 eager-fix decisions. `bash scripts/banned-words-lint.sh` exits 0 ("✓ banned-words-lint passed (default mode)."). |
| 9  | D-12 eager-resolution honesty | COVERED | SUMMARY lists 3 eager fixes; cross-checked against git diff: (1) **connector-matrix synonym widen** — initial scaffold `21030a0` had stub-only verifier; `c8e4111` ships final body with broadened regex `[Cc]onnector|[Bb]ackend` (single file, +16/-4 lines, < 5 min, no new dep — meets D-12). (2) **audit-trail SIGPIPE fix** — final body uses `git log --oneline -n 1` (no pipe), sidestepping `set -o pipefail` exit-141 from `git log \| head` (single file, 1-line idiom change — meets D-12). (3) **bind sweep promoted to scripts/p74-bind-ux-rows.sh** — heredoc was 1287 chars per SUMMARY; current `scripts/p74-bind-ux-rows.sh` is 45 lines, idempotent, declarative BINDINGS array. CLAUDE.md OP-4 ("ad-hoc bash is a missing-tool signal") was triggered by the `deny-ad-hoc-bash` pre-commit guard; promotion is the canonical recovery (no `--no-verify` taken). All 3 are genuine micro-deviations, NOT scope-creep. |
| 10 | Atomic commits | COVERED | 13 commits between `a973063` and `9af3366` inclusive (12 P74 implementation commits + 1 SUMMARY commit `9af3366`). Each commit subject cites a UX-BIND-NN, NARRATIVE-RETIRE-NN, PROSE-FIX-01, or D-NN id. Atomicity: each verifier scripted in its own commit (`b090049`, `dd89abd`, `7a2161d`, `c8e4111`, `11311a7`); propose-retires bundled (`97f071a`) per D-09 identical-format rationale; binds bundled (`efc75ab`) per the bind-sweep promotion. No commit straddles requirement boundaries beyond these documented bundles. |
| 11 | alignment_ratio delta plausibility | COVERED | summary-before.json: `alignment_ratio=0.9050279329608939`, `claims_bound=324`, `claims_missing_test=9`, `claims_retire_proposed=23`. summary-after.json: `alignment_ratio=0.9162011173184358`, `claims_bound=328`, `claims_missing_test=0`, `claims_retire_proposed=27`. Recompute non-retired denominator: `claims_total=388`, `claims_retired=30`, non-retired = 358; `328/358 = 0.916201...` — matches AFTER value to 6 d.p. Delta `+0.0111731...` rounds to `+0.0112` per SUMMARY. `claims_missing_test` -9 = -5 (binds) -4 (retires) exact match. `claims_bound` +4 = +5 (binds) -1 (linkedin tip to STALE_DOCS_DRIFT) — consistent with disclosed side-effect. `claims_retire_proposed` +4 exact match. All four deltas mutually consistent. |
| 12 | No prohibited actions | COVERED | `git log --oneline a973063..9af3366 \| grep -iE "push\|tag\|publish\|confirm-retire\|--no-verify"` returns empty. No origin/main push, no tag, no `confirm-retire` invocation (correctly deferred to owner-TTY HANDOVER step 1), no hook bypass. The `deny-ad-hoc-bash` guard hit during the bind sweep was resolved by promoting to a committed script (CLAUDE.md OP-4), NOT by `--no-verify`. |
| 13 | OP-8 honesty (the load-bearing one) | COVERED | SURPRISES-INTAKE.md gained 2 NEW P74 entries: (a) linkedin walker bug (P75-fix scope, severity LOW, sketched-resolution names P75 explicitly) and (b) connector-matrix synonym mismatch (severity LOW, P77 GOOD-TO-HAVES candidate). Both entries match what an unbiased reader would expect P74 to surface given the scope: PROSE-FIX-01 stresses the walker hash-overwrite path (caught), and UX-BIND-04 asserted a heading the live prose synonym-mismatches (caught). Cross-check vs git log: the 3 eager-fix decisions are visible in commits (`c8e4111`, `dd89abd`, `efc75ab`), confirming the executor genuinely separated "fix in-place" from "log to intake." This is NOT a noise-to-satisfy-the-practice intake — both entries are actionable and severity-justified. |

## Findings

**Strengths:**
- Catalog-first discipline upheld: BEFORE snapshot committed (`a973063`) BEFORE any verifier scaffold or row mutation, with status-before.json/.txt as forensic anchors. Snapshot pair under `quality/reports/verdicts/p74/` is the executor's own grading exhibit, exactly the artifact-only-grading shape the verifier prompt assumes.
- The verifier scripts are admirably tight (19-24 lines each, well within D-02's 10-30 line TINY-shape rule). Each one expresses a single failure-mode in plain shell with PASS/FAIL stderr lines, set -euo pipefail, and a comment header citing the catalog row + decision. The bound-row+verifier+source-line triple is fully traceable from any leg.
- The SIGPIPE eager-fix is exactly the kind of micro-correction CLAUDE.md OP-8 wants caught in-phase — switching from `git log | head` to `git log -n 1` is a textbook 1-line fix that would otherwise have shipped a flaky verifier under `pipefail`. Documented inline + in SUMMARY.
- The bind-sweep heredoc promotion to `scripts/p74-bind-ux-rows.sh` is a good outcome of the `deny-ad-hoc-bash` guard. The committed script is idempotent (re-running it overwrites the same `tests` vector), declares its bindings as a data array, and self-documents the "what + why" header. Future docs-alignment phases that need to bind a similar UX cluster can copy this file.
- The synonym-mismatch resolution (heading reads "What each backend can do" not "Connector capability matrix") was honestly handled: the verifier regex broadened to `[Cc]onnector\|[Bb]ackend`, the rationale logged to SURPRISES-INTAKE.md as a P77 GOOD-TO-HAVE candidate (heading rename), and the verifier still catches the actual failure-mode it cares about (matrix accidentally deleted). This is the P77 split working as designed: the working fix lands now, the cosmetic improvement defers without losing the signal.
- The linkedin STALE_DOCS_DRIFT side-effect is the most interesting OP-8 evidence in the phase — the executor predicted in CONTEXT.md D-08 that the post-edit walk would auto-heal, observed the prediction failed, and routed the divergence to SURPRISES-INTAKE.md with a specific cross-reference to HANDOVER §4 (the P75 walker bug is described there as `Source::Multi`-specific; this run shows `Source::Single` rows are also affected). That's load-bearing intelligence for P75's regression test scope.

**Minor observations (not findings — informational):**
- The PLAN.md must_haves section names `proposed_retire_rationale` as the catalog field; the live schema stores it under `rationale`. The 4 retire rows DO carry the D-09 rationale text — the field-name reference in the must_haves was outdated, the data is correct. Cosmetic, not a defect; could be fixed in a future plan template touch-up.
- CONTEXT.md D-01 specified verifier home as `quality/gates/docs-alignment/verifiers/`; the actual ground truth (per P73 `jira-adapter-shipped.sh`) is FLAT placement directly under `quality/gates/docs-alignment/`. SUMMARY.md "Plan-vs-source deviations" §1 acknowledges this and records the FLAT choice. The catalog row paths reflect FLAT, the scripts live in FLAT, and the bind-sweep script binds to FLAT — internally consistent.
- The `multi_test_rows` summary stat is `1` in both BEFORE and AFTER snapshots (carried over from P73's auth-header multi-test bind). P74 binds are all single-test, as expected.
- SUMMARY.md commit table column "Commit" lists 12 P74 commits (`a973063` through `6cae44d`); the SUMMARY commit (`9af3366`) is task #13 implicitly (the SUMMARY.md is the artifact landed by this 13th commit, orchestrator-landed because the executor's `Write` was guard-blocked). 13 total git commits in the P74 range, matching the prompt's expectation.

## Recommendations to orchestrator

1. **Advance to P75** (bind-verb hash-overwrite fix). All 13 dimension gates COVERED; no AMBER, no RED. P74's linkedin STALE_DOCS_DRIFT is the live regression P75's fix needs to heal — the SURPRISES-INTAKE entry already names this explicitly.
2. Owner-TTY HANDOVER step 1 should bulk-confirm the 4 narrative retires landed here (`use-case-20-percent-rest-mcp`, `use-case-80-percent-routine-ops`, `mcp-fixture-synthesized-not-live`, `mcp-schema-discovery-100k-tokens`) once P75 lands. Until then, they correctly remain in `RETIRE_PROPOSED` state.
3. The FLAT verifier-placement convention (`quality/gates/<dim>/<name>.sh`, not `verifiers/<name>.sh`) is now confirmed across P73 + P74. Future docs-alignment plans can drop the `verifiers/` subdirectory reference from CONTEXT-template boilerplate.
4. The `scripts/p74-bind-ux-rows.sh` shape is a reusable pattern. If P76/P77 or v0.13.0 docs-alignment phases need a similar bulk-bind sweep, copy this file as the starting point. Promoting heredoc-style bind sweeps to committed scripts before they hit `deny-ad-hoc-bash` is the cheapest path.
5. P75 must regression-test BOTH `Source::Single` AND `Source::Multi` rows for the hash-overwrite bug (the P74 linkedin row exposes Single-case affectedness, which HANDOVER §4 originally framed as Multi-only). The SURPRISES-INTAKE entry already records this — P75's plan should pull the entry into its CONTEXT.

---

**Verdict:** GREEN. All 13 dimensions COVERED. Phase 74 closes correctly. Recommend advance to P75.
