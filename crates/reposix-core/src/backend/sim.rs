//! TODO: `SimBackend` lives here.
//!
//! This placeholder module is filled in by Task 8-B-3 with a concrete
//! [`SimBackend`](crate::backend::sim::SimBackend) that implements
//! [`IssueBackend`](crate::backend::IssueBackend) against the in-process
//! `reposix-sim` HTTP routes.
